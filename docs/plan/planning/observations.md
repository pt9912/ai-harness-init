# Beobachtungs-Register

Regeln dieses Registers: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer schreibt, wer liest, wann gestrichen wird,
welche Form ein Beleg hat, und dass eine leere Tabelle `— keine —` trägt statt
zu verschwinden.

**Wer schreibt:** die **Slice-Closure** — neue Kennung vergeben **oder** Zähler erhöhen und Beleg
ergänzen. Der Zähler läuft damit mit jedem geschlossenen Slice und nicht mit der Welle. Das ist
hier nicht bloß bequem: dieses Repo führt Wellen-Betrieb **und** wellenlose Slices
([`MR-016`](../../../harness/conventions.md#mr-016--welle-oder-nicht-und-wo-wellenlose-arbeit-geführt-wird)),
und ein wellen-getragener Zähler hätte für die zweite Hälfte keinen Träger.

**Wer liest:** die **Welle-Closure** liest, was **3×** erreicht hat; die **Slice-Planung** liest in
§8 ihres Plans, was darunter steht. Wer nur den ersten Schritt kennt, sieht alles unter 3× nie
wieder an.

**Belege sind formgebunden:** Slice-Kennung `slice-<NNN>`, kein Freitext, so viele wie der Zähler
sagt — und die Slice-Datei liegt in `docs/plan/planning/done/`.

**Die Sub-Area-Spalte** trägt einen Namen, den die Modus-Deklaration in
[`harness/conventions.md`](../../../harness/conventions.md#modus-deklaration-pro-sub-area) führt.
Steht dort ein anderer, ist entweder die Zuordnung falsch oder die Deklaration unvollständig.

| Kennung | Beobachtung | Sub-Area | Zähler | Belege | Stand |
|---|---|---|---|---|---|
| BEO-001 | Der dritte Ausgang für ein offenes Risiko — *weiter offen → Beobachtungs-Register* — hat keinen Ort, also wird jede Beobachtung ein Folge-Slice | `*` (gesamtes Repo) | 6× | slice-080, slice-081, slice-130, slice-132, slice-133, slice-138 | **verkörpert** in dieser Datei und in den drei Anweisungssätzen unter [`.claude/commands/`](../../../.claude/commands/) (`seit slice-137`) — die Schwelle war beim Erstauftreten im Register bereits überschritten, der Lese-Schritt der nächsten Welle-Closure findet die Zeile fertig vor |
| BEO-002 | Eine Registerzeile hat keine Spalte für einen Träger: ein geschnittener Slice mit gemessener DoD, Datei-Plan und benannten Risiken passt nicht in sie | `*` (gesamtes Repo) | 1× | slice-137 | offen — geprüft über den vollen Bestand, nicht über eine Stichprobe: **jeder** Slice in `docs/plan/planning/open/` trägt einen DoD-Liefer-Punkt, eine Plan-Tabelle in §3 und mindestens ein benanntes Risiko in §6, und keiner ist überführbar. Die Zahl dazu steht hier nicht, weil sie mit jedem Schnitt wandert — sie ist ein Kommando (`ls docs/plan/planning/open/slice-*.md \| wc -l`), und der Stand zum Prüfzeitpunkt liegt in der Closure-Notiz von slice-137. Was fehlt, ist ein Ausgang für *real, aber nicht jetzt* zwischen `open/` und diesem Register |
| BEO-003 | Der Abgleich nach einem Lifecycle-`git mv` läuft von Hand, und eine Adresse auf eine Slice-Datei tritt in mehreren Formen auf, die dabei regelmäßig brechen — 13 gemessene Präfix-Formen plus eine 14., präfixlose Form ganz ohne Verzeichnis-Segment, die als einzige in **beide** Richtungen bricht (Ziel wegzieht **und** tragende Datei wegzieht) | `*` (gesamtes Repo) | 2× | slice-137, slice-144 | teilweise verkörpert seit `slice-144` in `harness/tools/slice-mv.sh`/`make slice-mv`: alle 13 Präfix-Formen (repo-weit, eingehend) und die ausgehende Hälfte der präfixlosen Form sind gedeckt. Offen bleibt die eingehende Hälfte der präfixlosen Form — eine andere, unbewegte Datei referenziert die bewegte Datei ohne Verzeichnis-Segment; ihr fehlt das Verzeichnis-Literal, an dem die Ersetzung ankert (Grenze 3, Skriptkopf `harness/tools/slice-mv.sh`) — bleibt hier offen geführt |
| BEO-004 | Die Sub-Area-Spalte dieses Registers unterscheidet nichts: die Modus-Deklaration führt `*` (gesamtes Repo), und `*` schließt jede Berührung ein | `*` (gesamtes Repo) | 1× | slice-137 | offen — die Auflösung ist eine Zeile in [`harness/conventions.md`](../../../harness/conventions.md#modus-deklaration-pro-sub-area) und damit Architect-Arbeit ([`AGENTS.md`](../../../AGENTS.md) §3.8). Ein Planner-Slice kann sie benennen, nicht schreiben; bis dahin trägt jede Zeile hier denselben Namen |
| BEO-005 | Der Zähler startet bei null: eine Klasse, die in den geschlossenen Slices schon dreimal aufgetreten ist, erreicht die Schwelle nicht von selbst, sondern erst beim nächsten Auftreten | `*` (gesamtes Repo) | 1× | slice-137 | offen — der Weg steht und ist einmal gegangen (`BEO-001` führt sechs Belege aus `docs/plan/planning/done/`), aber er beginnt jedes Mal mit einem neuen Auftreten. Eine Vollinventur über den Bestand in `docs/plan/planning/done/` ist ausgeschlossen: sie verlangte je Datei das Urteil *ist das dieselbe Beobachtung?* |
| BEO-006 | Die maschinell entscheidbare Hälfte der Register-Paarung — jede in `done/` zitierte `BEO-<NNN>` hat eine Zeile, und jede Zeile trägt mindestens einen Beleg — hat in keinem gepinnten Doku-Gate-Stand ein Modul | `*` (gesamtes Repo) | 1× | slice-137 | offen — netzlos über beide Digests gemessen, den aktiven aus [`.d-check.yml`](../../../.d-check.yml) und das Ziel von [slice-135](open/slice-135-d-check-pin-v0661.md): `docker run --rm --network none ghcr.io/pt9912/d-check@<digest> --print-config \| grep -ci 'observation'` → **0** und **0**. Das ist eine fehlende Fähigkeit eines Fremd-Werkzeugs, keine Grenze dieses Repos; der Eigenbau wiegt schwerer als der Nutzen. Der beobachtbare Trigger ist erreicht (die erste Kennung ist vergeben), der Nutzen über den heutigen Umfang dieses Registers ist es nicht |
| BEO-007 | Wer die Anweisungssätze unter [`.claude/commands/`](../../../.claude/commands/) schreiben darf, sagt keine Quelle | `*` (gesamtes Repo) | 3× | slice-137, slice-144, slice-147 | **Schwelle erreicht**, weiter offen — dritter Beleg `slice-147`, und er liegt **außerhalb** von `.claude/commands/`: für die Spec-Straten (`spec/spezifikation.md`, `spec/architecture.md`) benennt ebenfalls keine Quelle eine schreibende Rolle, das Feld `Verantwortlich:` steht dort als Setzung. Die Zeile führt damit die **Klasse** (ein Norm-Artefakt ohne benannte schreibende Rolle), nicht mehr nur ihren Erstauftritts-Ort; die Bezeichnung bleibt unverändert, damit die Zuordnung gelingt. Zweiter Beleg `slice-144`: die Frage manifestierte sich als HIGH-Finding mit Rollen-Widerspruch (`docs/reviews/2026-08-31-slice-144-review.md`), Modul 8s Konflikt-Pfad löste [`ADR-0028`](../adr/0028-anweisungssatz-gehoert-der-ausfuehrenden-rolle.md) aus (`Proposed`, Annahme-Trigger `slice-145` in `next/`). Sie beantwortet die Frage für **Command**-Artefakte (Eigentum = die Rolle, die den Ablauf ausführt), lässt `.claude/agents/*.md` in Festlegung 3 aber ausdrücklich offen. **Der Lese-Schritt liegt bei der Closure von [welle-10](welle-10-re-baseline.md)** — dieses Repo fährt Wellen-Betrieb, Modul 6 §Wellen-Closure-Prozedur Schritt 3, Rollen-Zug Planner → Architect → Planner nach Modul 8 |
| BEO-008 | Achse 1 eines Adaptions-Durchgangs (*„regelt die neue Fassung das, wofür diese Adaption angelegt wurde?"*) ist nicht mit *„die Baseline behandelt jetzt dasselbe Thema"* beantwortet — der Kurzschluss trägt nicht, geprüft werden muss, ob der neue Text genau die Pflicht erfüllt, für die der Eintrag entstand | `*` (gesamtes Repo) | 1× | slice-082 | offen — bislang ein Auftreten: zwei von 29 geprüften Einträgen ([`MR-019`](../../../harness/conventions.md#mr-019--technik-stratum-als-rang-2-der-source-precedence), [`MR-020`](../../../harness/conventions.md#mr-020--aufgehobener-eintrag-behält-kopf-und-zeiger-statt-rumpf)) hätten unter dem Kurzschluss einen falschen Ausgang bekommen, siehe die Closure-Notiz von slice-082 §7 und den Beleg in §9 desselben Plans. Wird derselbe Kurzschluss in einem künftigen Adaptions-Durchgang (slice-083/084 dieser Welle oder einer künftigen Re-Baseline) ein zweites Mal beobachtet, ist er ein Symptom, kein Einzelfall |
| BEO-009 | Ein Fix, der Verhalten ändert, korrigiert die **Ableitung**, lässt aber eine daneben stehende **Zusage** (Skript-Ausgabe, Testname, Doku-Absatz) unverändert stehen — kein Gate prüft den Wahrheitsgehalt einer Aussage, nur die Existenz eines genannten Sensors | `*` (gesamtes Repo) | 2× | slice-144, slice-131 | offen — zweiter Beleg `slice-131`, und er verschiebt die Klasse vom Code in den Baseline-Sprung: der Tausch auf `v5.12.0` zog die Pfade nach und ließ die daneben zitierten Ergebnisse stehen, sichtbar am größten Sprung (`grep -rh '^### Ziel-Form' .harness/baseline/v5.12.0/regelwerk/ \| wc -l` → **11**, im Text stand **7**; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der Ausgang für die Klasse ist eine Regel ohne Sensor: [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) verlangt je Aussage einen der drei Ausgänge, prüft sie aber nicht — kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Satz gegen den Baum, den sein Pfad adressiert (`grep -n '^modules:' .d-check.yml`). Erstauftreten war `slice-144`, gleich dreifach: die `100 %`-Zusage der Skript-Ausgabe (Delta-Review MEDIUM-2, behoben `bc38f97`), der Testname „alle 14 gemessenen Praefix-Formen" (Erstreview LOW-1, behoben `fc1fc54`) und, nach `8737ca7`, der `harness/README.md`-Absatz plus die `test/slice-mv.bats`-Kopfzeile (Verifikation, behoben `b1ef306`) — jedes Mal fand es ein *anderer* Lauf, nie ein Gate |
| BEO-010 | Eine Re-Baseline wird ohne vorgeschalteten Inventur-Slice eröffnet, und die Form-Pflichten der neuen Fassung kommen einzeln als Nachzügler zurück statt gebündelt in den Schnitt | `*` (gesamtes Repo) | 1× | slice-149 | offen — Erstauftreten, gemessen an [welle-10](welle-10-re-baseline.md): geschnitten mit **6** Slices (`080`–`085`, [`harness/conventions.md`](../../../harness/conventions.md#mr-000--baseline-aussage) §Baseline), heute **14** Mitglieder (`awk '/^## 4\. Slices/,/^## 5\./' docs/plan/planning/welle-10-re-baseline.md \| grep -c '^\| slice-'`; die Zahl wandert mit dem Schnitt und ist kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die 8 Nachzügler verteilen sich auf **drei** Ursachen-Klassen, und nur eine davon trüge der Inventur-Slice: vendored Baum als Eingabe/Emissionskanal (130–133, vier) · Singleton-Form-Pflichtfeld (136, 147, 148 und der zweite Schnitt von 083, vier) · trägerloser Ziel-Prozedur-Rest (149, einer). Der Gegenstand wäre ein Form-Diff aller Singleton-Vorlagen alt/neu plus Trefferzahl je neuer Pflicht, als erster Liefer-Punkt vor dem Rest-Schnitt — er nähme die **vier** der zweiten Klasse vorweg, nicht die sieben der ersten und dritten |
| BEO-011 | Baseline-Sprünge werden gesammelt statt einzeln adoptiert, und die Kosten einer Re-Baseline wachsen mit der Sprungweite statt mit dem Prozess | `*` (gesamtes Repo) | 1× | slice-149 | offen — Erstauftreten. Kostenreihe je Sprung nach [`harness/conventions.md`](../../../harness/conventions.md#mr-000--baseline-aussage) §Baseline: `v3.1.0` → **2** Slices (slice-011/012) · `v3.5.0` → **1** (slice-019) · `v3.5.1` → **1** (slice-043) · `v3.5.2` → **1** (slice-049); `v3.5.2` → `v5.12.0` (welle-10, **zwei übersprungene Major-Versionen**) → **14** Mitglieder. Der Unterschied ist die Sprungweite, nicht der Prozess. **Der nächste Fall läuft bereits auf:** `make baseline-freshness` meldet am 2026-09-01 `gepinnt: v5.12.0`, `latest: v5.18.0` bei Exit 2 — sechs Minor-Stände sammeln sich an, während welle-10 noch offen ist (beide Angaben wandern mit dem Upstream-Stand und sind keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Ob daraus über eine geschärfte Regel hinaus eine ADR wird, ist eine Norm-Aussage über den Adoptions-Rhythmus und damit Architect-Sache ([`AGENTS.md`](../../../AGENTS.md) §3.8), verwandt mit [`ADR-0018`](../adr/0018-ziel-fassung-regiert-die-migration.md) §Wer den Zielstand bewegt, die dieselbe Frage für den Einzelfall entscheidet |
| BEO-012 | Ein rotierender Prüf-Gegenstand — welcher Baseline-Abschnitt zuletzt gegen den ausgefüllten Bestand geprüft wurde — hat keinen stehenden Ort: er steht allein im Rumpf des Slice, der ihn prüfte, und der nächste Durchgang wählt ohne Gedächtnis | `*` (gesamtes Repo) | 1× | slice-084 | offen — Erstauftreten, gemessen am dritten Durchgang von [welle-10](welle-10-re-baseline.md): die Kennung des geprüften Abschnitts steht repo-weit nur in den zwei Dateien, die aus diesem Durchgang hervorgingen — dem prüfenden Slice und seinem Folge-Slice (`git grep -l 'Multi-Stage-Build' -- '*.md' ':!.harness/baseline'`; die Menge wandert mit dem Bestand und ist kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Keine stehende Datei des Planning-Layouts führt einen Rotations-Stand (`ls docs/plan/planning/*.md` → dieses Register, die README und die offenen Welle-Dateien), und kein Modul des Doku-Gates liest einen ([`.d-check.yml`](../../../.d-check.yml): `grep -n '^modules:' .d-check.yml` → `modules: [links, anchors, ids, matrix, codepaths, spans]`). Ein Träger wäre zu bauen — eine stehende Liste der geprüften Abschnitte oder eine Spalte, die den Stand führt —; ob er den Aufwand trägt, entscheidet der Lese-Schritt bei 3× |
| BEO-013 | Ein Adaptions-Durchgang, der nach dem **Delta** der neuen Fassung fragt, findet eine Deckung nicht, die ein Durchgang findet, der jeden Eintrag gegen den **Volltext** des adoptierten Stands hält — die Fehlerrichtung ist *bleibt gültig* statt *gegenstandslos*, und sie fällt erst auf, wenn ein zweiter Durchgang denselben Eintrag aus einem anderen Grund liest | `*` (gesamtes Repo) | 1× | slice-083 | offen — Erstauftreten, gemessen an **zwei** Einträgen der eingefrorenen Bezugsmenge von [welle-10](welle-10-re-baseline.md) §3 (`git show b902b60:harness/conventions.md \| grep -c '^### MR-'` → **29**; die Zahl ist der Bestand am Tausch-Commit und kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die zwei Verdikte des Delta-Durchgangs stehen in [slice-082](done/slice-082-adaptions-durchgang.md) §9, je Eintrag ein Kommando (`grep -n '^\| 008 \|' docs/plan/planning/done/slice-082-adaptions-durchgang.md`, dasselbe mit `008` → `015`): [`MR-008`](../../../harness/conventions.md#mr-008--ausfüll-templates-referenziert-statt-kopiert) *„bleibt gültig"* und [`MR-015`](../../../harness/conventions.md#mr-015--change-request-bei-personalunion-von-auftraggeber-und-entwickler) Setzung 3 *„nicht eingetreten"*, während der adoptierte Stand beide Setzungen wörtlich führt (`grep -c 'keine Blank-Kopie im Repo' .harness/baseline/v5.12.0/regelwerk/modul-02-harness-bootstrap.md` → **1**, `grep -c 'die Verweis-Spalte nennt diesen Vorgang statt eines' .harness/baseline/v5.12.0/regelwerk/grundlagen-source-precedence.md` → **1**). Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Eintrag gegen die Baseline (`grep -n '^modules:' .d-check.yml`); Träger ist der jeweils nächste Durchgang. Der Ausgang für die zwei Einträge selbst ist [slice-150](done/slice-150-drei-eintraege-tragen-den-adoptierten-stand.md), nicht diese Zeile — die Zeile führt die **Klasse** |
| BEO-014 | Ein erheblicher Teil des Adaptions-Blocks beschreibt keine Abweichung von einer Baseline-Regel, sondern **Buchführung über den Block selbst** — wie ein überholter, ein retirierter, ein nachgetragener Eintrag aussieht | `*` (gesamtes Repo) | 1× | slice-150 | offen — Erstauftreten. Vier Einträge sind einzeln dieser Frage gewidmet ([`MR-020`](../../../harness/conventions.md#mr-020--aufgehobener-eintrag-behält-kopf-und-zeiger-statt-rumpf), [`MR-032`](../../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger), [`MR-038`](../../../harness/conventions.md#mr-038--ein-retirierender-eintrag-nennt-den-baseline-stand-der-seinen-trigger-feuerte), [`MR-043`](../../../harness/conventions.md#mr-043--ein-nachgetragenes-pflichtfeld-schlägt-die-einordnung-im-rumpf)) — den vierten hat der Lauf hinzugefügt, der diese Zeile schreibt. Größenvergleich mit dem Nachbar-Repo nach demselben Sprung `v3.5.2` → `v5.12.0`: `grep -c '^### MR-' harness/conventions.md` → **44** in **3141** Zeilen gegen `grep -c '^\| \[\?MR-' /Development/a-check/harness/conventions.md` → **16** in **219** (keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2; das Nachbar-Repo führt die **Verzeichnis-Form**, also Index-Zeile plus Datei je Eintrag). Die Baseline nennt genau diese Form den Default und den Inline-Block die Wahl (`grundlagen-harness-dateien.md` §Konventionsspeicher); ihr Umbau ist in [welle-10](welle-10-re-baseline.md) §6 ausdrücklich ausgeschlossen und damit ein eigener, ungeschnittener Vorgang |
| BEO-015 | Eine Zahl steht neben dem Kommando, das sie liefern soll, aber das Kommando ist nie gefahren worden: es gibt einen anderen Wert aus, und ein Filter darin greift gar nicht — die Form von [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) ist erfüllt, die Zusage nicht | `*` (gesamtes Repo) | 1× | slice-147 | offen — Erstauftreten, gemessen an §1/§3 des Plans von `slice-147`: der Satz nennt **23** lebende Referenzstellen, das danebenstehende Kommando gibt **24** aus, und die Menge, die der Satz meint, hat **19** (`grep -rl 'spezifikation.md#' --include='*.md' . \| grep -v '\.harness/baseline' \| grep -v 'docs/reviews' \| grep -v 'planning/done' \| wc -l`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Ursache ist ein Filter-Idiom: `grep -rl <muster> .` gibt die Pfade **ohne** `./`-Präfix aus, also greift kein nachgeschaltetes `grep -v` mit `^\./`-Anker — die Zeitdokumente und die vendored Vorlage bleiben in der Menge. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) fährt ein Kommando aus einem Markdown-Block (`grep -n '^modules:' .d-check.yml`); Träger ist der Lauf, der die Zahl benutzt |

## Gestrichene Einträge

Regeln dieser Sektion: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer eine Zeile still löscht, macht sie
ununterscheidbar von einer, die es nie gab.

| Kennung | Beobachtung | Gestrichen am | Warum sie nicht mehr auftreten kann |
|---|---|---|---|
| — keine — | | | |
