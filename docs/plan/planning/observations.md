# Beobachtungs-Register

Regeln dieses Registers: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer schreibt, wer liest, wann gestrichen wird,
welche Form ein Beleg hat, welchen der drei Ausgänge ein Eintrag ab 3× trägt,
und dass eine leere Tabelle `— keine —` trägt statt zu verschwinden.

**Wer schreibt:** die **Slice-Closure** — neue Kennung vergeben **oder** Zähler erhöhen und Beleg
ergänzen. Der Zähler läuft damit mit jedem geschlossenen Slice und nicht mit der Welle. Das ist
hier nicht bloß bequem: dieses Repo führt Wellen-Betrieb **und** wellenlose Slices
([`MR-016`](../../../harness/conventions.md#mr-016--welle-oder-nicht-und-wo-wellenlose-arbeit-geführt-wird)),
und ein wellen-getragener Zähler hätte für die zweite Hälfte keinen Träger.

**Wer liest:** die **Welle-Closure** liest, was **3×** erreicht hat; die **Slice-Planung** liest in
§8 ihres Plans, was darunter steht. Wer nur den ersten Schritt kennt, sieht alles unter 3× nie
wieder an.

**Belege sind formgebunden:** die Kennung eines **abgeschlossenen Vorgangs**, kein Freitext, so
viele wie der Zähler sagt. Regelfall ist der Slice (`slice-<NNN>`, Datei in
`docs/plan/planning/done/`); auch eine Welle und ein Review-Report sind abgeschlossene Vorgänge und
taugen als Beleg — die Lage ist dann der Ort, an dem ihre Klasse abschließt.

**Ein Vorgang zählt einmal.** Zwei Funde im selben Slice sind eine Gelegenheit, kein zweites
Auftreten: Der Zähler misst Wiederholung über Vorgänge hinweg, nicht die Zahl der Funde. Ein
Vorkommen **ohne** abgeschlossenen Vorgang bekommt keinen Beleg und bewegt den Zähler nicht; es
gehört trotzdem in den Eintrag — *benannt, nicht gezählt*.

**Ab 3× trägt die Spalte `Stand` genau einen von drei Ausgängen** — eine geschlossene Menge, kein
Freitext:

| Ausgang | Wann | Wohin |
|---|---|---|
| **verkörpert** | die Regel steht | Zielort **und** Herkunfts-Anker (`seit welle-<NN>` bzw. `seit slice-<NNN>`) |
| **geplant** | die Regel ist beschlossen, aber noch nicht geschrieben | Kennung des Slice oder der Welle, die sie schreibt |
| **gestrichen** | die Beobachtung kann nicht mehr auftreten | Sektion *Gestrichene Einträge*, **mit Begründung** |

Zugewiesen wird der Ausgang vom **Lese-Schritt**. Unterhalb der Schwelle steht `offen` — dort ist
er kein Ausgang, sondern der Normalzustand; zwischen dem Beleg, der den Zähler auf 3 hebt, und dem
Lese-Schritt steht er auch darüber, vorübergehend. Ein Eintrag, der eine Closure ohne Ausgang
übersteht, ist unzulässig. An der Schwelle hängen nur **verkörpert** und **geplant**;
**gestrichen** ist von ihr unabhängig.

**Die Sub-Area-Spalte** trägt einen Namen, den die Modus-Deklaration in
[`harness/conventions.md`](../../../harness/conventions.md#modus-deklaration-pro-sub-area) führt.
Steht dort ein anderer, ist entweder die Zuordnung falsch oder die Deklaration unvollständig.

| Kennung | Beobachtung | Sub-Area | Zähler | Belege | Stand |
|---|---|---|---|---|---|
| BEO-001 | Der dritte Ausgang für ein offenes Risiko — *weiter offen → Beobachtungs-Register* — hat keinen Ort, also wird jede Beobachtung ein Folge-Slice | `*` (gesamtes Repo) | 6× | slice-080, slice-081, slice-130, slice-132, slice-133, slice-138 | **verkörpert** in dieser Datei und in den drei Anweisungssätzen unter [`.claude/commands/`](../../../.claude/commands/) (`seit slice-137`) |
| BEO-002 | Eine Registerzeile hat keine Spalte für einen Träger: ein geschnittener Slice mit gemessener DoD, Datei-Plan und benannten Risiken passt nicht in sie | `*` (gesamtes Repo) | 1× | slice-137 | offen — geprüft über den vollen Bestand, nicht über eine Stichprobe: **jeder** Slice in `docs/plan/planning/open/` trägt einen DoD-Liefer-Punkt, eine Plan-Tabelle in §3 und mindestens ein benanntes Risiko in §6, und keiner ist überführbar. Die Zahl dazu steht hier nicht, weil sie mit jedem Schnitt wandert — sie ist ein Kommando (`ls docs/plan/planning/open/slice-*.md \| wc -l`), und der Stand zum Prüfzeitpunkt liegt in der Closure-Notiz von slice-137. Was fehlt, ist ein Ausgang für *real, aber nicht jetzt* zwischen `open/` und diesem Register |
| BEO-003 | Der Abgleich nach einem Lifecycle-`git mv` läuft von Hand, und eine Adresse auf eine Slice-Datei tritt in mehreren Formen auf, die dabei regelmäßig brechen — 13 gemessene Präfix-Formen plus eine 14., präfixlose Form ganz ohne Verzeichnis-Segment, die als einzige in **beide** Richtungen bricht (Ziel wegzieht **und** tragende Datei wegzieht) | `*` (gesamtes Repo) | 5× | slice-137, slice-144, slice-157, slice-161, slice-172 | **verkörpert** in `harness/tools/slice-mv.sh` / `make slice-mv` (`seit slice-144`). **Die Grenze der Verkörperung ist benannt und gemessen:** gedeckt sind alle 13 Präfix-Formen (repo-weit, eingehend) und die ausgehende Hälfte der präfixlosen Form; **nicht** gedeckt ist deren eingehende Hälfte — eine andere, unbewegte Datei referenziert die bewegte Datei ohne Verzeichnis-Segment, und ihr fehlt das Verzeichnis-Literal, an dem die Ersetzung ankert (Grenze 3, Skriptkopf `harness/tools/slice-mv.sh`). Genau dort liegt der dritte Beleg: der `open/` → `in-progress/`-Move von `slice-157` brach den präfixlosen Verweis auf seine Datei in [slice-165](done/slice-165-praesens-aussagen-gegen-v5180.md), und `make docs-check` meldete ihn als `target-missing`. Die zweite Ersetzungs-Regel, die diese Grenze schlösse — eine, die statt am Verzeichnis-Literal am Dateinamen ankert und die Nachbarschaft der bewegten Datei kennt —, hat keinen Träger; eine Folge-Slice-Kennung steht hier deshalb **nicht**, sie behauptete eine Datei, die es nicht gibt. **Vierter Beleg `slice-161`, und er trifft die Grenze an zwei Stellen zugleich:** der `open/` → `in-progress/`-Move brach die präfixlosen Verweise auf seine Datei in [slice-162](open/slice-162-versions-sensor-baseline-pins.md) und [slice-168](open/slice-168-adaptions-eintraege-trennen-abweichung-von-buchfuehrung.md), beide von `make docs-check` als `target-missing` gemeldet. Beide tragen jetzt die präfixierte Form — eine Reparatur je Fundstelle, keine Regel **Fünfter Beleg `slice-172`, und er trifft dieselbe Grenze an derselben Stelle:** der `open/` → `in-progress/`-Move brach die präfixlosen Verweise auf seine Datei in [slice-173](done/slice-173-archive-welle-als-unterkommando.md) und [slice-174](open/slice-174-archivierung-emittieren.md), **7** Fundstellen, alle von `make docs-check` als `target-missing` gemeldet. Beide tragen jetzt die präfixierte Form — wieder eine Reparatur je Fundstelle, keine Regel |
| BEO-004 | Die Sub-Area-Spalte dieses Registers unterscheidet nichts: die Modus-Deklaration führt `*` (gesamtes Repo), und `*` schließt jede Berührung ein | `*` (gesamtes Repo) | 1× | slice-137 | offen — die Auflösung ist eine Zeile in [`harness/conventions.md`](../../../harness/conventions.md#modus-deklaration-pro-sub-area) und damit Architect-Arbeit ([`AGENTS.md`](../../../AGENTS.md) §3.8). Ein Planner-Slice kann sie benennen, nicht schreiben; bis dahin trägt jede Zeile hier denselben Namen |
| BEO-005 | Der Zähler startet bei null: eine Klasse, die in den geschlossenen Slices schon dreimal aufgetreten ist, erreicht die Schwelle nicht von selbst, sondern erst beim nächsten Auftreten | `*` (gesamtes Repo) | 1× | slice-137 | offen — der Weg steht und ist einmal gegangen (`BEO-001` führt sechs Belege aus `docs/plan/planning/done/`), aber er beginnt jedes Mal mit einem neuen Auftreten. Eine Vollinventur über den Bestand in `docs/plan/planning/done/` ist ausgeschlossen: sie verlangte je Datei das Urteil *ist das dieselbe Beobachtung?* |
| BEO-006 | Die maschinell entscheidbare Hälfte der Register-Paarung — jede in `done/` zitierte `BEO-<NNN>` hat eine Zeile, und jede Zeile trägt mindestens einen Beleg — hat in keinem gepinnten Doku-Gate-Stand ein Modul | `*` (gesamtes Repo) | 1× | slice-137 | offen — netzlos über beide Digests gemessen, den aktiven aus [`.d-check.yml`](../../../.d-check.yml) und das Ziel von [slice-135](open/slice-135-d-check-pin-v0661.md): `docker run --rm --network none ghcr.io/pt9912/d-check@<digest> --print-config \| grep -ci 'observation'` → **0** und **0**. Das ist eine fehlende Fähigkeit eines Fremd-Werkzeugs, keine Grenze dieses Repos; der Eigenbau wiegt schwerer als der Nutzen. Der beobachtbare Trigger ist erreicht (die erste Kennung ist vergeben), der Nutzen über den heutigen Umfang dieses Registers ist es nicht |
| BEO-007 | Wer die Anweisungssätze unter [`.claude/commands/`](../../../.claude/commands/) schreiben darf, sagt keine Quelle | `*` (gesamtes Repo) | 4× | slice-137, slice-144, slice-147, slice-148 | **geplant**: [slice-152](open/slice-152-adr-0029-acceptance-trigger.md) · [slice-151](open/slice-151-spec-straten-haben-eine-schreibende-rolle.md). Der Ausgang ist dreigeteilt, je Teil eine eigene Quelle. **Command-Artefakte** sind entschieden und **verkörpert**: [`ADR-0028`](../adr/0028-anweisungssatz-gehoert-der-ausfuehrenden-rolle.md) steht auf `Accepted` (Eigentum = die Rolle, die den Ablauf ausführt), und [`AGENTS.md`](../../../AGENTS.md) §3.8 trägt den Zeiger darauf (`seit slice-145`) · **`.claude/agents/*.md`**, das jene Festlegung 3 ausdrücklich offen lässt, entscheidet [`ADR-0029`](../adr/0029-agenten-typkarten-derivativ-gemischte-originale.md) (`Proposed`, Annahme trägt [slice-152](open/slice-152-adr-0029-acceptance-trigger.md)) · die **Spec-Straten** (`spec/spezifikation.md`, `spec/architecture.md`) haben keine Quelle, Träger ist [slice-151](open/slice-151-spec-straten-haben-eine-schreibende-rolle.md). Die Zeile trägt darum weiter **geplant** und schließt, wenn auch die zwei offenen Teile eine **angenommene** Quelle haben |
| BEO-008 | Achse 1 eines Adaptions-Durchgangs (*„regelt die neue Fassung das, wofür diese Adaption angelegt wurde?"*) ist nicht mit *„die Baseline behandelt jetzt dasselbe Thema"* beantwortet — der Kurzschluss trägt nicht, geprüft werden muss, ob der neue Text genau die Pflicht erfüllt, für die der Eintrag entstand | `*` (gesamtes Repo) | 1× | slice-082 | offen — bislang ein Auftreten: zwei von 29 geprüften Einträgen ([`MR-019`](../../../harness/conventions.md#mr-019--technik-stratum-als-rang-2-der-source-precedence), [`MR-020`](../../../harness/conventions.md#mr-020--aufgehobener-eintrag-behält-kopf-und-zeiger-statt-rumpf)) hätten unter dem Kurzschluss einen falschen Ausgang bekommen, siehe die Closure-Notiz von slice-082 §7 und den Beleg in §9 desselben Plans. Wird derselbe Kurzschluss in einem künftigen Adaptions-Durchgang (slice-083/084 dieser Welle oder einer künftigen Re-Baseline) ein zweites Mal beobachtet, ist er ein Symptom, kein Einzelfall |
| BEO-009 | Ein Fix, der Verhalten ändert, korrigiert die **Ableitung**, lässt aber eine daneben stehende **Zusage** (Skript-Ausgabe, Testname, Doku-Absatz) unverändert stehen — kein Gate prüft den Wahrheitsgehalt einer Aussage, nur die Existenz eines genannten Sensors | `*` (gesamtes Repo) | 10× | slice-144, slice-131, slice-085, slice-136, slice-159, slice-165, slice-169, slice-158, slice-173, slice-175 | **geplant**: [slice-153](open/slice-153-wellen-commands-nennen-die-roadmap-abschnitte.md) — er zieht die drei Stellen nach **und** hängt sie an einen Wächter. **Ein neuer Sensor ist dafür nicht zu bauen, weil der Zielort schon existiert:** Für die Unterklasse *zitierter Abschnittsname* deckt das Modul `anchors` der [`.d-check.yml`](../../../.d-check.yml) den Fall, sobald die Nennung als Anker-Link geschrieben ist (`grep -n '^modules:' .d-check.yml`). Der Gegenstand jener Reparatur sind die zwei Anweisungssätze, die `§Aktuelle Welle` gegen die Roadmap-Sektion `## Offene Wellen` halten (`git grep -c 'Aktuelle Welle' -- .claude/commands/` → `close-welle.md:2`, `plan-welle.md:1`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2); wer die zwei Dateien schreiben darf, ist die Frage aus `BEO-007`, und der Slice hängt an ihr. **Offen bleibt jede Unterklasse, in der die Zusage kein Anker ist** — Skript-Ausgabe, Testname, Prosa-Zahl, Präsens-Satz über den vendored Baum: dort ist der Ausgang eine Regel ohne Sensor ([`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) verlangt je Aussage einen der drei Ausgänge und prüft sie nicht), und kein Modul hält einen Satz gegen den Baum, den sein Pfad adressiert. **Der fünfte Beleg liegt in genau dieser Unterklasse und hat keinen Träger:** Der Kopftext dieses Registers führt die Vorgangs-Beleg-Regel, `implement-slice.md` führt daneben die abgelöste Slice-Form (`git grep -c 'formgebunden' -- .claude/commands/ internal/emit/templates/commands/` → je **1** in beiden Fassungen; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Wer die Datei schreiben darf, ist dieselbe offene Frage aus `BEO-007`. **Der sechste Beleg misst die Reichweite von [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) und findet sie einseitig:** Beim Sprung `v5.12.0` → `v5.18.0` bewegten sich fast alle Zahlen **nicht** auf der Baum-Seite, sondern auf der **repo-eigenen** Seite desselben Satzes, in dem der Pfad gezogen wurde — `wc -c < harness/README.md` **16 161** → **19 116**, die Abschnittszahl derselben Datei **6** → **7** (`grep -c '^## ' harness/README.md`), der Adaptions-Speicher **148 721** → **339 546** Zeichen (`cat harness/conventions.md harness/conventions/*.md | wc -c`). Auf der Baum-Seite bewegte sich in derselben Menge zweierlei: die Konventions-Vorlage des vendored Satzes wuchs von **8 785** auf **9 658** Zeichen, und der Vorlagen-Satz gewann zwei wiederkehrende Vorlagen. Keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2. Die Sortier-Regel [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) verlangt einen Ausgang für die **Baum**-Hälfte einer solchen Aussage; für die repo-eigene Hälfte daneben greift nur die Cutoff-Klausel von [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) (*„wer eine solche Zeile ohnehin anfasst, zieht sie nach"*), und die ist kein Auftrag. In einem Fall stürzte die **Folgerung**: die Gliederungs-Aussage in [slice-114](open/slice-114-jede-aussage-hat-einen-abschnitt.md) las *zwei fehlende Abschnitte*, gemessen fehlt **einer** **Der siebte Beleg liegt in derselben Sektion, die die Regel stellt, und er bewegt eine Folgerung:** Der Zensus in [`AGENTS.md`](../../../AGENTS.md) §3.7 ist repo-eigen — er läuft gegen den Index, nicht gegen den vendored Baum —, und vier seiner sechs Werte standen gegen den Bestand veraltet; `zulässige Feld-Form` steht statt auf **0** auf **1** (`git grep -lE '· seit (welle\|slice)-' -- "${PS[@]}" \| wc -l`, mit dem Pathspec `PS`, den der Abschnitt selbst setzt; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Für diese Hälfte verlangt keine Quelle den Lauf, und kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält eine Zahl gegen das Kommando daneben. **Der achte Beleg liegt in einem Anweisungssatz und traf eine Schritt-Zahl:** Der Baum-Tausch auf `v5.18.0` bewegte die Ableitung — die Wellen-Closure der adoptierten Fassung führt **sechs** Schritte —, während beide Fassungen von `close-welle.md` weiter *„fünf Schritte"* zusagten (`git grep -c 'fünf' 470b5d3 -- .claude/commands/close-welle.md internal/emit/templates/commands/close-welle.md` → je **4**; Tree-Operand nach [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) Ausgang 2, kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die Unterklasse ist dieselbe wie beim fünften Beleg — eine Zusage, die kein Anker ist —, und sie hat weiterhin keinen Sensor; Träger war der Slice, der die Quelle daneben las. **Der neunte Beleg liegt in derselben Datei wie die Ableitung, die ihn erzeugt:** der Kopf von `cmd/ai-harness-init/archive_welle.go` sagt *„hier steht der Dispatch und die eine Stelle, an der `git` läuft"*, während zehn Zeilen tiefer *„die zwei lesenden git-Aufrufe unten"* stehen und `gitStatusPorcelain`/`gitLsFiles` als *„die einzigen zwei Stellen"* geführt werden (`grep -c 'die eine Stelle, an der' cmd/ai-harness-init/archive_welle.go` gegen `grep -c 'die einzigen zwei Stellen' cmd/ai-harness-init/archive_welle.go`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der Ausgang steht in §6 von [slice-173](done/slice-173-archive-welle-als-unterkommando.md). **Der zehnte Beleg traf dieses Register selbst, und er misst die Ortsfrage:** [slice-175](done/slice-175-archive-welle-schreibender-pfad.md) grenzte das Risiko vorab auf zwei Orte ein — den Kopf des abgelösten Shell-Trägers und [`harness/README.md`](../../../harness/README.md) —, und an beiden ist es nicht eingetreten. Getroffen hat es die `Stand`-Zellen von `BEO-025`, `BEO-026` und `BEO-029`, die den entfernten Pfad im Präsens führten; sie sind mit jener Closure nachgezogen. Die Eingrenzung ist der Fehler, nicht die Reparatur: eine Zusage steht dort, wo niemand sie sucht, und `codepaths` sieht den Pfad seit dem Tombstone in [`.d-check.yml`](../../../.d-check.yml) referenz-weit nicht mehr (`grep -n 'archive-welle.sh' .d-check.yml`; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). |
| BEO-010 | Eine Re-Baseline wird ohne vorgeschalteten Inventur-Slice eröffnet, und die Form-Pflichten der neuen Fassung kommen einzeln als Nachzügler zurück statt gebündelt in den Schnitt | `*` (gesamtes Repo) | 2× | slice-149, slice-148 | offen — Erstauftreten, gemessen an [welle-10](done/welle-10-re-baseline.md): geschnitten mit **6** Slices (`080`–`085`, [`harness/conventions.md`](../../../harness/conventions.md#mr-000--baseline-aussage) §Baseline), heute **15** Mitglieder (`awk '/^## 4\. Slices/,/^## 5\./' docs/plan/planning/done/welle-10-re-baseline.md \| grep -c '^\| slice-'`; die Zahl wandert mit dem Schnitt und ist kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die 8 Nachzügler verteilen sich auf **drei** Ursachen-Klassen, und nur eine davon trüge der Inventur-Slice: vendored Baum als Eingabe/Emissionskanal (130–133, vier) · Singleton-Form-Pflichtfeld (136, 147, 148 und der zweite Schnitt von 083, vier) · trägerloser Ziel-Prozedur-Rest (149, einer). Der Gegenstand wäre ein Form-Diff aller Singleton-Vorlagen alt/neu plus Trefferzahl je neuer Pflicht, als erster Liefer-Punkt vor dem Rest-Schnitt — er nähme die **vier** der zweiten Klasse vorweg, nicht die sieben der ersten und dritten **Zweiter Beleg `slice-148`, und er misst die Klasse an einem einzelnen Artefakt:** das Form-Diff-Protokoll führt für `spec/architecture.md` **eine** Position (das `ID`-Feld), gegen die Ziel-Form sind es **fünf** — dazu das Kopf-Pflichtfeld `Rolle:`, zwei Verbote mehr in der Hard Rule, ein anderer Spaltensatz in §2 und zwei anders benannte Abschnitte §4/§5 (`grep -n '^#\{2,3\} ' spec/architecture.md` gegen dasselbe Kommando auf `.harness/baseline/v5.12.0/templates/spec/architecture.template.md`). Eine davon ist **gesperrt**: §5 umzubenennen bräche den Anker, auf den zwei `Accepted`-ADRs zeigen. **Für den Sprung `v5.12.0` → `v5.18.0` hat die Klasse einen Träger:** der vollständige Diff-Katalog steht in [slice-155](done/slice-155-inventur-vor-dem-schnitt.md) §9 — 21 Positionen, jede mit Zuordnung und Ausgang —, und die Mitglieder von [welle-14](done/welle-14-re-baseline.md) §4 sind daraus geschnitten statt vorab geschätzt. Ob damit ein Nachzügler ausbleibt, entscheidet ihre Closure; ein Nachzügler wäre die dritte Instanz |
| BEO-011 | Baseline-Sprünge werden gesammelt statt einzeln adoptiert, und die Kosten einer Re-Baseline wachsen mit der Sprungweite statt mit dem Prozess | `*` (gesamtes Repo) | 1× | slice-149 | offen — Erstauftreten. Kostenreihe je Sprung nach [`harness/conventions.md`](../../../harness/conventions.md#mr-000--baseline-aussage) §Baseline: `v3.1.0` → **2** Slices (slice-011/012) · `v3.5.0` → **1** (slice-019) · `v3.5.1` → **1** (slice-043) · `v3.5.2` → **1** (slice-049); `v3.5.2` → `v5.12.0` (welle-10, **zwei übersprungene Major-Versionen**) → **15** Mitglieder. Der Unterschied ist die Sprungweite, nicht der Prozess. **Der nächste Fall läuft bereits auf:** `make baseline-freshness` meldet am 2026-09-01 `gepinnt: v5.12.0`, `latest: v5.18.0` bei Exit 2 — sechs Minor-Stände sammeln sich an, während welle-10 noch offen ist (beide Angaben wandern mit dem Upstream-Stand und sind keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Ob daraus über eine geschärfte Regel hinaus eine ADR wird, ist eine Norm-Aussage über den Adoptions-Rhythmus und damit Architect-Sache ([`AGENTS.md`](../../../AGENTS.md) §3.8), verwandt mit [`ADR-0018`](../adr/0018-ziel-fassung-regiert-die-migration.md) §Wer den Zielstand bewegt, die dieselbe Frage für den Einzelfall entscheidet |
| BEO-012 | Ein rotierender Prüf-Gegenstand — welcher Baseline-Abschnitt zuletzt gegen den ausgefüllten Bestand geprüft wurde — hat keinen stehenden Ort: er steht allein im Rumpf des Slice, der ihn prüfte, und der nächste Durchgang wählt ohne Gedächtnis | `*` (gesamtes Repo) | 1× | slice-084 | offen — Erstauftreten, gemessen am dritten Durchgang von [welle-10](done/welle-10-re-baseline.md): die Kennung des geprüften Abschnitts steht repo-weit nur in den zwei Dateien, die aus diesem Durchgang hervorgingen — dem prüfenden Slice und seinem Folge-Slice (`git grep -l 'Multi-Stage-Build' -- '*.md' ':!.harness/baseline'`; die Menge wandert mit dem Bestand und ist kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Keine stehende Datei des Planning-Layouts führt einen Rotations-Stand (`ls docs/plan/planning/*.md` → dieses Register, die README und die offenen Welle-Dateien), und kein Modul des Doku-Gates liest einen ([`.d-check.yml`](../../../.d-check.yml): `grep -n '^modules:' .d-check.yml` → `modules: [links, anchors, ids, matrix, codepaths, spans]`). Ein Träger wäre zu bauen — eine stehende Liste der geprüften Abschnitte oder eine Spalte, die den Stand führt —; ob er den Aufwand trägt, entscheidet der Lese-Schritt bei 3× |
| BEO-013 | Ein Adaptions-Durchgang, der nach dem **Delta** der neuen Fassung fragt, findet eine Deckung nicht, die ein Durchgang findet, der jeden Eintrag gegen den **Volltext** des adoptierten Stands hält — die Fehlerrichtung ist *bleibt gültig* statt *gegenstandslos*, und sie fällt erst auf, wenn ein zweiter Durchgang denselben Eintrag aus einem anderen Grund liest | `*` (gesamtes Repo) | 1× | slice-083 | offen — Erstauftreten, gemessen an **zwei** Einträgen der eingefrorenen Bezugsmenge von [welle-10](done/welle-10-re-baseline.md) §3 (`git show b902b60:harness/conventions.md \| grep -c '^### MR-'` → **29**; die Zahl ist der Bestand am Tausch-Commit und kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die zwei Verdikte des Delta-Durchgangs stehen in [slice-082](done/slice-082-adaptions-durchgang.md) §9, je Eintrag ein Kommando (`grep -n '^\| 008 \|' docs/plan/planning/done/slice-082-adaptions-durchgang.md`, dasselbe mit `008` → `015`): [`MR-008`](../../../harness/conventions.md#mr-008--ausfüll-templates-referenziert-statt-kopiert) *„bleibt gültig"* und [`MR-015`](../../../harness/conventions.md#mr-015--change-request-bei-personalunion-von-auftraggeber-und-entwickler) Setzung 3 *„nicht eingetreten"*, während der adoptierte Stand beide Setzungen wörtlich führt (`grep -c 'keine Blank-Kopie im Repo' .harness/baseline/v5.12.0/regelwerk/modul-02-harness-bootstrap.md` → **1**, `grep -c 'die Verweis-Spalte nennt diesen Vorgang statt eines' .harness/baseline/v5.12.0/regelwerk/grundlagen-source-precedence.md` → **1**). Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Eintrag gegen die Baseline (`grep -n '^modules:' .d-check.yml`); Träger ist der jeweils nächste Durchgang. Der Ausgang für die zwei Einträge selbst ist [slice-150](done/slice-150-drei-eintraege-tragen-den-adoptierten-stand.md), nicht diese Zeile — die Zeile führt die **Klasse**. **Für den nächsten Durchgang ist die Volltext-Hälfte gebunden:** [slice-157](done/slice-157-adaptions-durchgang-v5180.md) trägt sie als eigenen DoD-Punkt, und der Diff-Katalog in [slice-155](done/slice-155-inventur-vor-dem-schnitt.md) §9 weist sich selbst ausdrücklich als Delta aus |
| BEO-014 | Ein erheblicher Teil des Adaptions-Blocks beschreibt keine Abweichung von einer Baseline-Regel, sondern **Buchführung über den Block selbst** — wie ein überholter, ein retirierter, ein nachgetragener Eintrag aussieht | `*` (gesamtes Repo) | 3× | slice-150, slice-166, slice-167 | **geplant**: [slice-168](open/slice-168-adaptions-eintraege-trennen-abweichung-von-buchfuehrung.md). **Sechs** Einträge sind einzeln dieser Frage gewidmet ([`MR-020`](../../../harness/conventions.md#mr-020--aufgehobener-eintrag-behält-kopf-und-zeiger-statt-rumpf), [`MR-032`](../../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger), [`MR-038`](../../../harness/conventions.md#mr-038--ein-retirierender-eintrag-nennt-den-baseline-stand-der-seinen-trigger-feuerte), [`MR-043`](../../../harness/conventions.md#mr-043--ein-nachgetragenes-pflichtfeld-schlägt-die-einordnung-im-rumpf), [`MR-045`](../../../harness/conventions.md#mr-045--der-adaptions-block-läuft-in-der-verzeichnis-form), [`MR-046`](../../../harness/conventions.md#mr-046--die-verzeichnis-position-ist-binär-und-trägt-die-kopf-marke-nicht)); der sechste entscheidet die Auflösungs-Trigger der Form selbst und ist damit dieselbe Fracht. **Keine verkörperbare Regel steht am Ende dieser Zeile:** Die Beobachtung sagt nicht, welche Form falsch ist, sondern dass ein Teil des Blocks über sich selbst spricht — ihr Ausgang ist ein Schnitt über den **Inhalt** dieser sechs. Den Schnitt macht der Planner, das Verdikt je Eintrag schreibt der Architect ([`AGENTS.md`](../../../AGENTS.md) §3.8 gibt ihm den Block, nicht die Planung). Der Größenvergleich mit dem Nachbar-Repo misst dasselbe Instrument, weil beide die Verzeichnis-Form führen: `grep -c '^\| \[MR-' harness/conventions.md` → **48** Index-Zeilen in **168** Zeilen gegen `grep -c '^\| \[\?MR-' /Development/a-check/harness/conventions.md` → **16** in **219** (keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2) |
| BEO-015 | Eine Zahl steht neben dem Kommando, das sie liefern soll, aber das Kommando ist nie gefahren worden: es gibt einen anderen Wert aus, und ein Filter darin greift gar nicht — die Form von [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) ist erfüllt, die Zusage nicht | `*` (gesamtes Repo) | 2× | slice-147, slice-148 | offen — Erstauftreten, gemessen an §1/§3 des Plans von `slice-147`: der Satz nennt **23** lebende Referenzstellen, das danebenstehende Kommando gibt **24** aus, und die Menge, die der Satz meint, hat **19** (`grep -rl 'spezifikation.md#' --include='*.md' . \| grep -v '\.harness/baseline' \| grep -v 'docs/reviews' \| grep -v 'planning/done' \| wc -l`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Ursache ist ein Filter-Idiom: `grep -rl <muster> .` gibt die Pfade **ohne** `./`-Präfix aus, also greift kein nachgeschaltetes `grep -v` mit `^\./`-Anker — die Zeitdokumente und die vendored Vorlage bleiben in der Menge. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) fährt ein Kommando aus einem Markdown-Block (`grep -n '^modules:' .d-check.yml`); Träger ist der Lauf, der die Zahl benutzt Zweiter Beleg `slice-148`: dasselbe Idiom in §1 jenes Plans, dort **5** statt der genannten **2** — die Aussage hielt, ihr Beleg nicht. Damit ist die Klasse ein **Symptom**, kein Einzelfall |
| BEO-016 | Ein Slice-Plan dieses Repos trägt ein Vielfaches der Zeilenzahl, die das Schwester-Repo für dieselbe Arbeitsklasse braucht — die Beweisführung im Plan (Kommando-Belege, Zwischenmessungen, narrative Herleitung) wächst weit über das hinaus, was die Umsetzung liest | `*` (gesamtes Repo) | 2× | slice-136, slice-176 | offen — **Wiederauftreten an derselben Arbeitsklasse wie das Vorbild.** Der Inventur-Slice zu `v6.0.0` misst **452** Zeilen, sein Katalog-Abschnitt allein **226** — gegen **405** bzw. **134** beim Inventur-Slice des vorigen Sprungs ([slice-155](done/slice-155-inventur-vor-dem-schnitt.md)), obwohl das Delta kleiner ist — **14** Dateien und **282** geänderte Zeilen gegen **21** und **459** (`git diff --shortstat v5.18.0 v6.0.0 -- lab/regelwerk lab/templates` gegen dieselbe Abfrage für `v5.12.0 v5.18.0`, am lokalen Kurs-Klon). Das Schwester-Repo erledigt denselben Bump in **261** Zeilen (`wc -l < docs/plan/planning/done/slice-176-inventur-vor-dem-schnitt-v600.md`, `wc -l < docs/plan/planning/done/slice-155-inventur-vor-dem-schnitt.md`, und `unzip -p /Development/d-check/docs/plan/planning/done/welle-88/archiv.zip docs/plan/planning/done/slice-193-baseline-v600-bump.md \| wc -l`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Der Zuwachs sitzt in der Beweisführung, nicht in der Liste:** die Positions-Tabelle ist 15 Zeilen lang, die zwei Mess-Abschnitte daneben tragen den Rest. **Erstauftreten**, gemessen an [slice-136](done/slice-136-roadmap-traegt-die-ziel-form.md): **471** Zeilen für **drei** Liefer-Punkte (`git show 3b5bff1:docs/plan/planning/open/slice-136-roadmap-traegt-die-ziel-form.md \| wc -l`; drei ist der Deckel aus `modul-05-planning-harness.md` §Ziel-Form: Slice). Vergleichsklasse im Nachbar-Repo — Urteile über eine benannte Menge fällen und je einen Ausgang eintragen —: **129** Zeilen (`wc -l /Development/a-check/docs/plan/planning/done/slice-097-adaptions-urteile-ausfuehren.md`). Keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2. Der Bestand wird **nicht** umgeschrieben — das wäre rückwirkende Historie an Zeitdokumenten und ein eigener, größerer Schnitt; diese Zeile registriert, sie löst nicht auf |
| BEO-017 | Ein vom Prozess **vorgeschriebener** Ortswechsel macht eine Adresse in einem nach [`AGENTS.md`](../../../AGENTS.md) §3.4 eingefrorenen Artefakt tot: der `git mv`, den die Baseline für die Auflösung bzw. die Closure verlangt, ist zugleich das Ereignis, das den Zeiger auf das bewegte Artefakt bricht — und die Quelle darf niemand reparieren | `*` (gesamtes Repo) | 3× | slice-154, slice-156, slice-179 | offen — Erstauftreten, drei gemessene Instanzen: [`ADR-0026`](../adr/0026-eingefrorene-referenz-referenz-weit-ausgenommen.md) (Baseline-Pfad nach dem Tag-Tausch), [`ADR-0027`](../adr/0027-tote-adresse-in-eingefrorener-adr.md) (Carveout-Pfad nach dem `done/`-Move, Modul 7) und [`ADR-0030`](../adr/0030-eingefrorene-adresse-auf-den-planning-lifecycle.md) (Wellenplan-Pfad nach dem Closure-Move, Modul 6). Jede kostete eine eigene ADR und eine eigene Gate-Senkung. [`ADR-0030`](../adr/0030-eingefrorene-adresse-auf-den-planning-lifecycle.md) hält die Klasse an beiden Enden an — Festlegung 3 (Verweis-Form vor dem Einfrieren) vorwärts, Festlegung 4 (Entscheidung vor dem Move) auf den Bestand —, aber **beide ohne Sensor**: kein Modul des Doku-Gates liest Status und Adress-Form zusammen, und keines liest Lifecycle-Reihenfolgen. Der Zähler bleibt darum offen: ein viertes Mitglied ist gemessen und geladen — [`ADR-0011`](../adr/0011-telemetrie-erfassung-policy.md) adressiert die Welle-Plan-Datei von `welle-09` als Markdown-Link (`git grep -noE '\]\([^)]*planning/[^)]*\)' -- 'docs/plan/adr/[0-9]*.md'`, keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2) **Eine fünfte Instanz ist stumm, und das trennt sie von allen vieren davor:** vier `Accepted`-ADRs ([`ADR-0024`](../adr/0024-derivatives-register-gehoert-der-rolle-seines-originals.md), [`ADR-0026`](../adr/0026-eingefrorene-referenz-referenz-weit-ausgenommen.md), [`ADR-0027`](../adr/0027-tote-adresse-in-eingefrorener-adr.md), [`ADR-0030`](../adr/0030-eingefrorene-adresse-auf-den-planning-lifecycle.md)) nennen `.harness/baseline/v5.12.0/…` als Kommando-Operanden, deren Pfad seit dem Tag-Tausch ins Leere zeigt (`git grep -c '\.harness/baseline/v5\.12\.0/' -- 'docs/plan/adr/[0-9]*.md'`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Gate-sichtbar ist davon **nichts**: `codepaths.roots` der [`.d-check.yml`](../../../.d-check.yml) ist `[spec, docs, harness]` (`grep -n 'roots' .d-check.yml`), und `.harness/` liegt außerhalb — `make docs-check` meldet über diesen Stand `505 Datei(en), 0 Befund(e)`. Damit kostet diese Instanz, anders als die drei ADR-tragenden davor, **keine** ADR, **keine** Gate-Senkung und **keinen** Folge-Slice; was bleibt, ist diese Zeile. Die zwei `Proposed`-ADRs mit derselben Adress-Form ([`ADR-0025`](../adr/0025-register-mit-gemischten-originalen.md), [`ADR-0028`](../adr/0028-anweisungssatz-gehoert-der-ausfuehrenden-rolle.md)) sind kein Bestand: ihren Beleg bringt der Accept-Übergang in Form ([`ADR-0016`](../adr/0016-verweis-traegt-tag-und-zitat.md) Festlegung 3 (a)). **Sechste Instanz:** Der Wegfall der Register-Datei bricht genau **eine** eingefrorene Adresse ([`ADR-0028`](../adr/0028-anweisungssatz-gehoert-der-ausfuehrenden-rolle.md), gemessen als **ein** Befund unter **75** betroffenen Dateien); ihr Ausgang ist das vierte namentlich geschnittene Ventil aus [`ADR-0034`](../adr/0034-register-verzeichnis-form-und-die-ortsfestigkeit-der-register-datei.md) Festlegung 2. Dessen Festlegung 5 nimmt der Klasse eine Erlaubnis, die [`ADR-0030`](../adr/0030-eingefrorene-adresse-auf-den-planning-lifecycle.md) Festlegung 3 noch gab: Die stehende Register-Datei gilt nicht mehr als ortsfest. Mit **3×** liegt der Ausgang dieser Zeile beim Lese-Schritt der Closure von `welle-15` |
| BEO-018 | Die Out-of-Scope-Liste einer Welle und die Doku-DoD eines ihrer Slices sprechen über dieselbe neue Datei und sagen Verschiedenes: die eine schließt eine Klasse von Folge-Arbeit aus, die andere verlangt für **jeden** neu geschnittenen Slice eine Zeile in der Slice-Tabelle der Welle | `*` (gesamtes Repo) | 1× | slice-155 | offen — Erstauftreten, gemessen an genau einer Position des Diff-Katalogs: [welle-14](done/welle-14-re-baseline.md) §6 schließt *Sensor-Neubauten* aus, DoD-Punkt 5 von [slice-155](done/slice-155-inventur-vor-dem-schnitt.md) verlangt „eine Zeile je neu geschnittenem Slice". Aufgelöst ist der Einzelfall — [slice-162](open/slice-162-versions-sensor-baseline-pins.md) liegt in `open/` und ist in §4 der Welle als **Nicht**-Mitglied benannt —, nicht die Klasse: keine Quelle sagt, ob ein aus einem Wellen-Slice hervorgegangener Folge-Slice ein Mitglied jener Welle sein muss. Kein Modul des Doku-Gates liest das (`grep -n '^modules:' .d-check.yml`); Träger ist der Lauf, der beide Sätze nebeneinander liest |
| BEO-019 | Eine Byte-Gleichheits-Messung an einem Regelwerks-**Abschnitt** wird als Aussage über die **Regel** gelesen, die er stellt — obwohl der Abschnitt Fragen in andere Dateien delegiert, die ein Delta haben; die Fehlerrichtung ist *unverändert* statt *andere Pflicht* | `*` (gesamtes Repo) | 2× | slice-163, slice-176 | offen — **zweimal an derselben Stelle gemessen, beim zweiten Mal eine Ebene feiner.** `v5.18.0` → `v6.0.0`: derselbe Abschnitt wieder byte-gleich (je 123 Zeilen, `diff` leer; die ganze Datei trägt kein Delta, `git diff --shortstat v5.18.0 v6.0.0 -- lab/regelwerk/modul-02-harness-bootstrap.md` → leer), und wieder trägt genau **einer** der vier Delegate ein Delta — `grundlagen-harness-dateien.md` mit **11** geänderten Zeilen, die drei übrigen mit **0** (Kommandos in [slice-176](done/slice-176-inventur-vor-dem-schnitt-v600.md) §9). Diesmal ist sogar die delegierte **Tabellenzeile** byte-gleich; gekippt ist die Prosa darunter, die ihre Bedingung auflöst — aus *„Wo Kennungen kein Segment tragen, entfällt die Spalte"* wird *„Die Spalte ist nicht bedingt."* Wer nur Zeilen vergleicht, liest hier *unverändert* und meint *andere Pflicht*. **Erstauftreten**, gemessen am selben Abschnitt: zwischen `v5.12.0` und `v5.18.0` byte-gleich (je 123 Zeilen, `diff` leer — die zwei Kommandos stehen in [`ADR-0031`](../adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md) §Kontext), während er vier Fragen in Dateien delegiert, die ein Delta tragen: `grundlagen-harness-dateien.md` **17** geänderte Zeilen, `modul-07-carveouts.md`, `modul-04-adrs.md` und `grundlagen-bootstrap.md` je **2**. Eine dieser 17 ändert genau die delegierte Pflichtgliederung für [`harness/conventions.md`](../../../harness/conventions.md) — die Zeile *Modus-Deklaration pro Sub-Area* verlangt am neuen Stand eine Kürzel-Spalte. **Die Argumentform ist im Bestand verbreitet:** sieben `Accepted`- oder `Proposed`-ADRs schließen einen Delta-Nachweis über Byte-Gleichheit tragender Quellen (`git grep -c 'byte-gleich' -- 'docs/plan/adr/[0-9]*.md'`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2) — der Befund entwertet keinen davon, er benennt eine Prüfung, die keiner von ihnen ausweist. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) folgt den Verweisen eines Regelwerk-Abschnitts (`grep -n '^modules:' .d-check.yml` → `modules: [links, anchors, ids, matrix, codepaths, spans]`); Träger ist der Lauf, der die Messung benutzt |
| BEO-020 | Ein Auflösungs-Trigger, der auf eine Form-Migration zeigt, feuert **mit** ihr — aber die Entscheidung, ob der Eintrag dadurch gegenstandslos wird, hat im migrierenden Vorgang keinen Träger: der Vorgang wechselt den Träger, das Urteil verlangt eine inhaltliche Prüfung je Eintrag | `*` (gesamtes Repo) | 2× | slice-166, slice-167 | offen, zweites Auftreten. Die drei Einträge haben ihr Urteil: [`MR-046`](../../../harness/conventions.md#mr-046--die-verzeichnis-position-ist-binär-und-trägt-die-kopf-marke-nicht) entscheidet je einzeln, dass **keiner** der drei Trigger im tragenden Teil eingetreten ist — die Verzeichnis-Position ist binär (aktiv / aufgelöst) und trägt die Teil-Ablösung nicht, deren Eintrag aktiv bleibt; [`MR-032`](../../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger) und [`MR-039`](../../../harness/conventions.md#mr-039--ein-fehlendes-pflichtfeld-wird-nachgetragen-ein-retirierter-eintrag-bekommt-keines) verlieren je einen Mess-Block, [`MR-043`](../../../harness/conventions.md#mr-043--ein-nachgetragenes-pflichtfeld-schlägt-die-einordnung-im-rumpf) nichts. **Die Klasse bleibt offen, und der zweite Beleg zeigt warum:** den Träger stellte wieder ein eigener Slice — `slice-166` feuerte die drei Trigger, `slice-167` entschied sie —, nicht der migrierende Vorgang selbst. **Die Fehlerrichtung ist *gefeuert und unbemerkt*:** kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) liest einen Auflösungs-Trigger gegen den Zustand, den er beschreibt (`grep -n '^modules:' .d-check.yml`), und `make mutate` kennt keine Fehlschlag-Form dafür. Träger ist der Trigger-Audit der Closure |
| BEO-021 | Ein `grep` auf einen zitierten Baseline-Satz gibt **0** aus, obwohl der Satz wörtlich am geprüften Stand steht — ein Zeilenumbruch oder ein Inline-Markup innerhalb des Zitats trennt das Muster; die Fehlerrichtung ist *der Satz ist fort* statt *der Satz steht* | `*` (gesamtes Repo) | 1× | slice-157 | offen — Erstauftreten, **sieben** Instanzen in einem Durchgang über **fünf** Dateien des vendored Baums. Das Paar, an dem die Klasse sichtbar wird, ist je ein volles und ein gekürztes Muster über denselben Satz: `grep -c 'Das alte Verzeichnis fällt erst, wenn der Review durch ist' .harness/baseline/v5.18.0/regelwerk/modul-02-harness-bootstrap.md` → **0** gegen `grep -c 'Das alte Verzeichnis fällt' <dieselbe Datei>` → **1**. In allen sieben trennt ein Zeilenumbruch, in dreien liegt zusätzlich ein Inline-Markup im Zitat (zweimal fett, einmal kursiv); die Aufzählung je Eintrag steht in §9 des Plans von `slice-157`. Keine Erwartungswerte ([`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2) — beide Zahlen wandern mit dem vendored Baum. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält ein Zitat gegen seine Quelle (`grep -n '^modules:' .d-check.yml`); Träger ist der Durchgang, der den Abschnitt **liest**, statt ihn zu greppen |
| BEO-022 | Der Lese-Schritt einer Closure erkennt den 3×-Übertritt, aber der Ausgang verlangt eine Handlung, die der Rolle dieser Closure nicht zusteht — die Zeile bleibt ohne Ausgang stehen und wird von der nächsten Closure geerbt | `*` (gesamtes Repo) | 2× | slice-157, slice-167 | offen — zweites Auftreten, an genau zwei Zeilen dieses Registers gemessen: `git show 8ca4a28:docs/plan/planning/observations.md \| grep -c 'behauptete eine Datei, die es nicht gibt'` → **2** (Tree-Operand nach [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) Ausgang 2; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Beide Closures liefen als **Architect**-Lauf (`git log --format='%s' --all \| grep -E '^Rolle Architect: slice-(157\|167)'`), und beider Ausgang war ein Slice-Schnitt — den macht der Planner ([`AGENTS.md`](../../../AGENTS.md) §3.8 gibt dem Architect den Konventionsspeicher und die Hard Rules, nicht die Planung). Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) liest eine Rollen-Zuständigkeit gegen einen Registereintrag (`grep -n '^modules:' .d-check.yml`); Träger ist die nächste Closure in der zuständigen Rolle |
| BEO-023 | Ein Folge-Slice wartet in `open/` über einen Baseline-Sprung hinweg, und der Sprung ändert die **Pflicht**, die er halten soll — kein Schritt hält den Bestand offener Slice-Pläne gegen den neuen Stand; die Fehlerrichtung ist *der Plan gilt weiter* statt *seine Frage hat sich verschoben* | `*` (gesamtes Repo) | 2× | slice-160, slice-176 | offen — **Wiederauftreten mit beziffertem Bestand.** `v6.0.0` schreibt in `slice.template.md` die DoD- und die §7-Zeile des Registers um (*kein Zähler wird gesetzt*); die abgelöste Form steht in **10** von **60** lebenden Plänen (`git grep -lE 'Beobachtungs-Register \(`\.\./observations\.md`\) fortgeschrieben' -- 'docs/plan/planning/open/*.md' 'docs/plan/planning/next/*.md' 'docs/plan/planning/in-progress/*.md' \| wc -l` gegen `ls docs/plan/planning/open/*.md docs/plan/planning/next/*.md docs/plan/planning/in-progress/slice-*.md \| wc -l`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der Ausgang für **diesen** Fall ist [slice-184](open/slice-184-register-form-im-bestand-nachziehen.md); dass ein Plan durch den Sprung nicht nur anders formuliert, sondern anders **verpflichtet** sein kann, bleibt dort ausdrücklich Urteil je Plan und steht als Rückführung in dessen §4. **Erstauftreten**, gemessen an [slice-146](open/slice-146-modul-14-multi-stage-build-abweichungen-deklarieren.md): geschnitten aus [slice-084](done/slice-084-stichprobe-gegen-bestand.md) gegen `v5.12.0`, seither unverändert in `open/`. Seine DoD (2) fragt nach dem Ausgang der Image-Hash-Regel; `v5.18.0` fügt einen Abschnitt hinzu, der die Vorfrage teilweise beantwortet — `grep -c 'ein Wiederholungs-Schlüssel ist es nicht' .harness/baseline/v5.18.0/regelwerk/modul-14-docker-harness.md` → **1** gegen `git show db83415^:.harness/baseline/v5.12.0/regelwerk/modul-14-docker-harness.md \| grep -c '^### Zwei Formen des Reproduzierbarkeits-Ankers'` → **0** (Tree-Operand nach [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) Ausgang 2; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Der Inventur-Slice deckt die Richtung nicht:** [slice-155](done/slice-155-inventur-vor-dem-schnitt.md) §9 misst das Delta der **Baseline** und weist Position 14 diesem Lauf zu — den Bestand offener Pläne gegen dieses Delta hält er nicht. Die ausgesetzte Menge ist `ls docs/plan/planning/open/slice-*.md \| wc -l` (wandert mit dem Schnitt). Abzugrenzen von [`BEO-013`](observations.md) (Bezugsmenge dort: die Adaptions-Einträge) und von [slice-165](done/slice-165-praesens-aussagen-gegen-v5180.md) (Gegenstand dort: die **Tag-Nennung**, nicht die verschobene Pflicht). Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Slice-Plan gegen den vendored Baum (`grep -n '^modules:' .d-check.yml`); Träger ist der Lauf, der den Plan übernimmt |
| BEO-024 | Für eine **Präsens-Aussage über ein repo-eigenes lebendes Artefakt** in einem Text, der nach [`AGENTS.md`](../../../AGENTS.md) §3.4 eingefroren wird, benennt keine Quelle eine Form — die Fehlerrichtung ist *die Aussage altert nach dem Einfrieren, und niemand darf sie mehr korrigieren* | `*` (gesamtes Repo) | 1× | slice-145 | offen — Erstauftreten. Die vier Konsistenzrunden an [`ADR-0028`](../adr/0028-anweisungssatz-gehoert-der-ausfuehrenden-rolle.md) vor der Freigabe trugen dieselbe Ursache und zählen als **ein** Vorgang: derselbe Prüf-Gegenstand in demselben Slice, und der Zähler misst Wiederholung über Vorgänge, nicht die Zahl der Funde. **Drei Quellen grenzen je eine Nachbarklasse ab und lassen diese offen:** [`ADR-0016`](../adr/0016-verweis-traegt-tag-und-zitat.md) Festlegung 2 bindet den Beleg einer **Regelwerks**-Aussage, [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) die Präsens-Aussage über den **vendored Baum** — sein §Geltungsbereich nimmt `docs/plan/adr/` ausdrücklich aus —, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) die **Zahl**. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Satz gegen das Artefakt, über das er spricht (`grep -n '^modules:' .d-check.yml`); Träger ist die Runde, die den Text vor dem Accept-Übergang liest |
| BEO-025 | Eine Zusage im Skript- oder Funktionskopf nennt einen Geltungsbereich, den der Code darunter nicht hält — in der schärfsten Form nennt sie einen **Sensor**, der den ausgegrenzten Rest auffangen soll, und der Sensor sieht genau diese Form nicht | `*` (gesamtes Repo) | 4× | slice-170, slice-173, slice-175, slice-180 | **geplant**: [slice-181](open/slice-181-grenzen-liste-vollstaendig-oder-fail-closed.md) — die Regel lautet *ein Wächter, der über einer Quelldatei urteilt, zerlegt ihre Form, oder er urteilt nicht*, und jener Slice trägt sie samt dem einen offenen gemessenen Fall. Erstauftreten `slice-170`, **vier Funde, ein Vorgang, ein Zähler-Schritt:** drei aus dem Review (Vorbedingung „sauberer Arbeitsbaum" ohne die untrackten Dateien · Grenzen-Liste „fünf, gemessen" ohne die vom Werkzeug selbst erzeugte sechste · Funktionskommentar mit mehr H1-Formen, als Test und Code tragen), einer aus der Verifikation. Der vierte trägt die Sensor-Variante und ist an einer Sonde über einem Klon **gemessen**: eine Grenzen-Zeile sagt für einen Verweis in Inline-Code ohne Verzeichnis-Segment *„`make docs-check` nach dem Lauf zeigt den Rest"* — die Sonde meldet für diese Form **nichts**, während derselbe Dateiname mit Verzeichnis-Segment `codepath-missing` und als Link-Ziel `target-missing` auslöst. Die stille Form trägt der Bestand **94**-mal in **56** Dateien (`git grep -oE "$(printf '\140')(slice\|welle)-[0-9]+[a-z]?[^/$(printf '\140')]*\.md$(printf '\140')" -- '*.md' ':!.harness/baseline' \| wc -l`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Abgrenzung zu `BEO-009`:** dort macht ein Fix eine zuvor wahre Zusage falsch, hier war sie nie wahr — sie entstand mit dem Code. `make comment-claims` prüft, dass ein genannter Sensor **existiert**, nicht dass er den zugesagten Fall **deckt**, und kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält eine Zusage gegen die Reichweite des Sensors, den sie nennt (`grep -n '^modules:' .d-check.yml`). **Zweiter Beleg `slice-173`, und er zeigt die Klasse im Port an neuer Stelle:** `Suchraum` (`internal/archive/scan.go`) führt jeden Eintrag durch `strings.TrimSpace`, während `gitLsFiles` (`cmd/ai-harness-init/archive_welle.go`) mit `-z` liest und das mit *„weil ein Dateiname ein Zeilenende tragen darf"* begründet — ein getrackter Pfad mit Randleerraum fällt damit aus dem Suchraum, den das `git grep` des Trägers durchsucht. Im Bestand tritt die Form nicht auf: **0** von **1013** (`git ls-files -z \| tr '\0' '\n' \| grep -cE '^[[:space:]]\|[[:space:]]$'` gegen `git ls-files \| wc -l`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der Ausgang steht in §6 von [slice-173](done/slice-173-archive-welle-als-unterkommando.md). **Dritter Beleg [slice-175](done/slice-175-archive-welle-schreibender-pfad.md), und er ist die Schwelle:** acht Review-Runden, und sieben davon fanden dieselbe Familie an je einer Ebene weiter außen — Logik, Parameter, Strecke, Verdrahtung, Bedien-Einstieg, Kalibrierungs-Größe, Namens-Muster. Alle Funde jenes Slice sind **ein** Vorgang und darum ein Zähler-Schritt. Offen bleibt der letzte in ihrer schärfsten Form: `test/unterkommando-kopplung.bats` vergleicht gegen die Menge der `case`-Marken am Zeilenanfang (`dispatch_muster='^[[:space:]]*case "[^"]*":'`) und sagt daneben zu, eine Marke in einer Kommentar-Zeile dispatche nichts — der Ausdruck trennt die `//`-Form ab, die `/* … */`-Form nicht, und die Zusage nennt nur **eine** Grenze. Der Restschaden ist gemessen und ungleich verteilt: für `archive-welle` fällt derselbe mutierte Baum in `make test-go`, für `span-report` steht daneben nichts in `make gates`; `cmd/ai-harness-init/main.go` führt heute keinen Block-Kommentar (`grep -cE '^[[:space:]]*/\*' cmd/ai-harness-init/main.go` → **0**; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Vierter Beleg [slice-180](done/slice-180-mutations-sensor-beleg-statt-lauf.md), und er trifft die schärfste Form an einer Zusage, die mit dem Code desselben Slice entstand:** Der Kommentar am `isolation_key`-Aufrufpunkt in `main()` nannte die fünfte fail-closed-Bedingung als Träger für den vollen Host-Baum; deren `# files:`-Ziele decken **57** von **1060** Schlüssel-Pfaden (`sed -n 's/^# files: //p' test/mutations/*.sh \| tr ' ' '\n' \| sed '/^$/d' \| LC_ALL=C sort -u \| wc -l` gegen `bash -c 'source harness/tools/mutate.sh 2>/dev/null\|\|true; isolation_key_files \| wc -l'`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2 — beide Zahlen wandern mit dem Fall- bzw. Datei-Bestand). Der Beleg ist ein eigener Vorgang und kein zweiter Blick auf `slice-175`: die Zusage entstand neu, wurde im Review desselben Slice gefunden und im selben Diff auf das eingeschränkt, was der Schlüssel deckt — die Deckungslücke bleibt, sie ist jetzt benannt statt einem Sensor zugeschrieben. Der Ausgang der Zeile bewegt sich damit nicht; er bleibt **geplant** auf [slice-181](open/slice-181-grenzen-liste-vollstaendig-oder-fail-closed.md). **Ein zweiter Fund im selben Vorgang, darum benannt und nicht gezählt:** der Kopf von `harness/tools/slice-mv.sh` führt seine Grenzen als geschlossene Liste (`grep -n 'GRENZEN (gemessen' harness/tools/slice-mv.sh`), und keine der drei deckt den Pfad, der als Tree-Operand an einer Commit-Kennung hängt — die vierte Grenze steht als [`BEO-036`](observations.md). Es ist derselbe Gegenstand, den jener Folge-Slice trägt |
| BEO-026 | Ein Zähler-Label nennt eine andere Einheit als der Zähler zählt: die Lauf-Ausgabe gibt eine Zahl aus, gegen die der Aufrufer sein Ergebnis abzählt, und sie zählt etwas anderes | `*` (gesamtes Repo) | 2× | slice-170, slice-173 | offen — zweites Auftreten. Im ersten Vorgang zwei Funde und darum **ein** Zähler-Schritt. Runde 1: `praefix_treffer` zählte Paare aus verweisender × bewegter Datei und hieß „Datei(en)". Die Behebung ließ die Klasse an neuer Stelle wieder entstehen — `unsauber_grund` zählt Zeilen aus `git status --porcelain`, und eine Zeile kann ein untracktes **Verzeichnis** mit beliebig vielen Dateien sein (`git status --porcelain -uall \| grep -c '^?? '` gegen die gemeldete Zahl). Der zweite Fund ist **offen**: der Abbruch ist richtig, seine Zahl nicht. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält ein Label gegen seinen Zähler (`grep -n '^modules:' .d-check.yml`). **Zweiter Beleg `slice-173`:** dieselbe Klasse im Port — die `Verweise:`-Zeile führte drei Fundstellen-Summen unter einem `Datei(en)`-Label. Die Go-Fassung trennt die Einheiten jetzt (`TestSchreibeTrenntDateienVonFundstellen`) und meldet in `UnsauberGrund` (`internal/archive/clean.go`) `untrackte(r) Eintrag/Eintraege` (`TestUnsauberGrundNenntEintragNichtDatei`). Der zweite Fund ist damit an seinem Ort geschlossen: die Go-Fassung ist seit [slice-175](done/slice-175-archive-welle-schreibender-pfad.md) die einzige Fassung der Operation, und das Target führt genau sie (`grep -n '^archive-welle:' Makefile`; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die **Klasse** bleibt offen — sie hängt an keinem der zwei Träger, sondern daran, dass kein Modul ein Label gegen seinen Zähler hält |
| BEO-027 | Ein lebendes Plan-Artefakt fasst eine referenzierte Entscheidung zusammen, und die Zusammenfassung ist **stärker** als die Quelle — die Fehlerrichtung ist *die Quelle sagt mehr zu, als sie sagt* | `*` (gesamtes Repo) | 2× | slice-172, slice-179 | offen — Erstauftreten, gemessen an §1 des Plans von `slice-172`: er liest [`ADR-0022`](../adr/0022-erfassungsschicht-traeger-aus-dem-produkt-binaer.md) als *„mit dem Ergebnis, dass jedes gebootstrappte Ziel sie bekommt"*, während deren Festlegung 5(a) den Fall der gescheiterten Träger-Platzierung und 5(b) den frischen Klon ausdrücklich ausnehmen (`grep -c 'frischer Klon des Adopter-Repos hat ihn nicht' docs/plan/adr/0022-erfassungsschicht-traeger-aus-dem-produkt-binaer.md` → **1**; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Die Folge war keine Kosmetik: die daraus entstandene Festlegung ist **ortsgebunden** statt allquantifiziert. **Abgrenzung:** bei [`BEO-009`](observations.md) macht ein Fix eine zuvor wahre Zusage falsch — hier war die Zusammenfassung nie deckungsgleich; bei [`BEO-019`](observations.md) wird eine **Messung** über-gelesen, hier eine **Zusammenfassung**. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält eine Zusammenfassung gegen das Artefakt, auf das sie zeigt (`grep -n '^modules:' .d-check.yml`): `links` prüft die Auflösbarkeit, nicht die Aussage. Träger ist der Lauf, der die referenzierte Quelle **vollständig** liest. **Zweitauftreten**, gemessen an §1 des Plans von `slice-179`: er führt die Verzeichnis-Entscheidung des Nachbar-Repos als die Quelle, die dort den Wegfall der Register-Datei und die fünf `ignore-refs`-Paare trägt. Gelesen trägt jene Entscheidung den Konfigurations-Modus des **Werkzeugs**; Wegfall und Ventile stehen in einem anderen Vorgang und einem eigenen Adaptions-Eintrag des Nachbarn. Dieselbe Fehlerrichtung — die genannte Quelle sagt weniger zu, als der Plan ihr zuschreibt |
| BEO-028 | Ein Mutations-Fall nennt eine andere Datei als die, die sein Wächter liest — nach einem Umzug zeigt er auf den alten Träger, trifft nichts und bleibt grün, statt seinen Wächter rot zu färben | `*` (gesamtes Repo) | 1× | slice-173 | offen — Erstauftreten, gemessen an `test/mutations/221-ignore-refs-restbreite.sh`: sein Kopf nennt `harness/conventions.md` (`sed -n 's/^# files: //p' test/mutations/221-ignore-refs-restbreite.sh`), während `test/ignore-refs-restbreite.bats` je Paar die Datei aus dem `in:`-Schlüssel des Top-Level-Blocks von [`.d-check.yml`](../../../.d-check.yml) liest und keines der drei Paare diese Datei nennt (`grep -n '^ *- in:' .d-check.yml`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2 — der Rumpf von [`MR-021`](../../../harness/conventions.md#mr-021--das-span-schema-zieht-ins-technik-stratum-sein-eintrag-wird-aufgehoben) liegt in der Verzeichnis-Form, und [`ADR-0032`](../adr/0032-eingefrorene-referenz-folgt-ihrem-rumpf.md) zieht den Schlüssel nach). Ein zweiter Link in der alten Trägerdatei bewegt damit keine Paar-Zählung. Gemeldet wird die Klasse allein von `make mutate` (Nicht-Gate-Verify, [`AGENTS.md`](../../../AGENTS.md) §4); `make gates` führt den Treiber nicht. **Abgrenzung zu `BEO-009`:** dort bleibt eine **Zusage** im Text stehen, hier das **Gegenbeispiel**, das einen Wächter halten soll. Der Einzelfall liegt außerhalb des Diffs von slice-173; Träger ist der Lauf, der den Fall nachzieht |
| BEO-029 | Ein Closure-Kriterium hält zwei Fassungen auf einer Fläche gegeneinander, auf der sie nicht auseinanderlaufen können, und setzt dazu ein Instrument voraus, das im Prüf-Fall nicht bis zur Messstelle läuft — die Fehlerrichtung ist *das Kriterium prüft* statt *es kann nichts finden* | `*` (gesamtes Repo) | 1× | slice-173 | offen — Erstauftreten, gemessen am Prüf-Fall dieses Repos: das Kriterium verlangte die vier Einsammel-Zahlen der Vergleichsfassung, deren Ausgabe hinter deren Untergrenzen-Sperre lag; die Sperre greift für jede Welle, solange kein Archiv die Untergrenze setzt (`ls docs/plan/planning/done/*/archiv.zip 2>/dev/null \| wc -l` → **0**; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Dazu rechnen beide Fassungen die vier Zahlen aus **derselben** Einsammel-Regel, während der Suchraum — die Achse, an der sie auseinanderlaufen — in keine von ihnen eingeht. **Abgrenzung zu `BEO-015`:** dort steht eine Zahl neben einem nie gefahrenen Kommando; hier steht ein Kriterium über einem Instrument, das im Prüf-Fall nicht bis zur Messstelle läuft. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) liest ein Closure-Kriterium (`grep -n '^modules:' .d-check.yml`); Träger ist die Closure, die es einlöst. Der Einzelfall ist aufgelöst: §5 von [slice-173](done/slice-173-archive-welle-als-unterkommando.md) trägt die zwei Achsen |
| BEO-030 | Der Sichtungs-Schritt eines Slice-Plans (§8) zitiert einen Zähler-Stand aus diesem Register, den das Register nicht trägt — und die Schwellen-Folgerung darüber kippt mit ihm; die Fehlerrichtung ist *keiner erreicht die Schwelle* statt *einer erreicht sie* | `*` (gesamtes Repo) | 2× | slice-175, slice-180 | offen — Erstauftreten, gemessen an §6 und §8 des Plans von `slice-175`: er führte `BEO-009` und `BEO-025` je um **eins** zu niedrig und schloss daraus *„Keiner erreicht mit diesem Slice die Schwelle neu"*, während `BEO-025` sie mit ihm erreichte. Beide Zahlen standen **ohne** das Kommando, das sie liefert — die Form, die [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2 verlangt und die den Fehler beim Schreiben sichtbar gemacht hätte (`awk -F'\|' '/BEO-0(09\|25)/ {print $2, $5}' docs/plan/planning/observations.md`). **Abgrenzung zu [`BEO-015`](observations.md):** dort steht eine Zahl neben einem Kommando, das nie gefahren wurde; hier steht sie neben gar keinem. **Abgrenzung zu [`BEO-027`](observations.md):** dort ist eine Zusammenfassung stärker als ihre Quelle; hier ist eine Zahl niedriger als ihre Quelle, und die Folge ist ein **ausbleibender** Lese-Schritt statt einer überdehnten Aussage. Die Folge ist nicht kosmetisch: der Sichtungs-Schritt ist in einem Repo ohne Wellen-Betrieb der einzige Leser unterhalb der Schwelle, und ein zu niedrig abgeschriebener Stand macht ihn blind für genau den Übertritt, den er finden soll. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält eine Zahl in einem Slice-Plan gegen die Registerzeile, auf die sie sich beruft (`grep -n '^modules:' .d-check.yml`); Träger ist der Review, der beide Dateien nebeneinander liest. **Zweiter Beleg [slice-180](done/slice-180-mutations-sensor-beleg-statt-lauf.md), zwei Funde in einem Vorgang und darum ein Zähler-Schritt:** §8 jenes Plans führte `BEO-025` als 2× gegen 3× im Register (`git show 65b5b3e:docs/plan/planning/in-progress/slice-180-mutations-sensor-beleg-statt-lauf.md \| grep -oE 'BEO-025[^(]*\([0-9]+×'` gegen dasselbe `awk` wie oben auf `git show 65b5b3e:docs/plan/planning/observations.md`; Tree-Operand nach [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) Ausgang 2) und führt `BEO-009` als 9× gegen 10× im Register (`grep -oE 'BEO-009[^(]*\([0-9]+×' docs/plan/planning/done/slice-180-mutations-sensor-beleg-statt-lauf.md` gegen dasselbe `awk` wie oben; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der erste Fund ist im Diff jenes Slice korrigiert, der zweite steht. **Die Ursache des zweiten liegt zwischen zwei Vorgängen und nicht im Plan:** der Sichtungs-Schritt liest den gemergten Stand beim Schnitt, eine fremde Closure bewegt ihn danach, und keine Stelle liest ihn vor der Umsetzung erneut — beim Schnitt trug das Register 9× (`git log --format='%ci' -1 91c9085` gegen `git log --format='%ci' -1 6b3f61d`). **Die Schwellen-Folgerung kippt in diesem Vorgang nicht:** beide Zeilen stehen in beiden Ständen über der Schwelle und tragen ihren Ausgang. Die Fehlerrichtung ist dieselbe — der zitierte Stand liegt unter dem Register —, der Schaden ist es nicht; die Klasse hängt an der Zahl, nicht an ihrer Folge |
| BEO-031 | Ein baum-abgeleiteter Beleg deckt nicht den vollen Prüfgegenstand: Docker-Cache-Zustand und Host-Werkzeuge liegen strukturell außerhalb jedes Inhalts-Hashs über einem Arbeitsbaum, Datei-**Modi** liegen innerhalb des Baums und trotzdem außerhalb — ein wiederverwendeter Beleg konserviert damit ein Verdikt, das ein anderer, nicht gehashter Zustand widerlegt hätte | `*` (gesamtes Repo) | 1× | slice-180 | offen — Erstauftreten, zwei Funde in **einem** Vorgang und darum ein Zähler-Schritt. `isolation_key()` hasht den **Inhalt** der Pfade aus `isolation_key_files()`, gezogen aus derselben Ausschluss-Definition wie die Isolationskopie (`grep -n 'ISOLATION_EXCLUDES=' harness/tools/mutate.sh`); die Image-Digests stehen in getrackten Dateien und bewegen den Schlüssel mit, der gebaute Layer-Cache und die Host-Werkzeug-Version stehen in keinem Baum-Zustand. Der zweite Fund liegt näher: ein reiner `chmod` bewegt den Schlüssel nicht, weil der Hash Inhalt liest und keine Metadaten — ein Fehlerpfad dazu ist nicht benannt, kein Fall aus `test/mutations/` läuft modus-abhängig (`grep -n 'bash "\$case_file"' harness/tools/mutate.sh`). Gemildert, nicht geschlossen, durch `MUTATE_FORCE` und eine Übersprung-Meldung, die den Rest ausspricht (`grep -n 'MUTATE_FORCE=1 erzwingt' harness/tools/mutate.sh`); [`ADR-0035`](../adr/0035-beleg-statt-lauf-und-die-bezugsmenge-des-schluessels.md) Festlegung 4 lässt ihn ausdrücklich als benannten Rest stehen. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) und kein Fall aus `test/mutations/` kann die Klasse prüfen — der ungedeckte Anteil liegt außerhalb dessen, was der Schlüssel liest; Träger ist die Zusage im Kopf von `harness/tools/mutate.sh` |
| BEO-032 | Eine deklarierte Ausnahmeliste wird mechanisch nur auf **Existenz und Form** geprüft, nie auf **inhaltliche Berechtigung** — ob ein Eintrag darin richtig ist, bleibt dauerhaft ein Urteil, und ein zweiter, unbegründeter Eintrag bliebe für den Wächter unsichtbar | `*` (gesamtes Repo) | 1× | slice-180 | offen — Erstauftreten, gemessen an `ISOLATION_KEY_EXEMPT`, das genau einen Eintrag trägt (`grep -n 'ISOLATION_KEY_EXEMPT=' harness/tools/mutate.sh` → `ISOLATION_KEY_EXEMPT=(./.git)`; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der Wächter dazu (`test/mutate-driver.bats`, Zusicherung *jeder von prepare_isolation kopierte Pfad geht in den Schluessel ein oder steht in der Ausnahmeliste*) hält, **dass** jeder kopierte Pfad im Schlüssel oder in der Liste steht — nicht, **ob** ein Eintrag der Liste seine Begründung trägt. [`ADR-0035`](../adr/0035-beleg-statt-lauf-und-die-bezugsmenge-des-schluessels.md) Festlegung 3 verlangt Deklaration am Ort der Definition, genannten Grund und Zählung als Rest; die Prüfung dieser drei Bedingungen ist ein Urteil. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Ausnahme-Eintrag gegen seine schriftliche Begründung (`grep -n '^modules:' .d-check.yml`); Träger ist die Runde, die eine Erweiterung der Liste liest |
| BEO-033 | Eine Grün-Aussage nennt ihre Herkunft nicht: *„`make mutate` grün"* trägt denselben Wortlaut für einen gefahrenen Fall-Satz und für einen zitierten Beleg, und kein Sensor hält einen späteren Text gegen die Lauf-Art, aus der er stammt | `*` (gesamtes Repo) | 1× | slice-180 | offen — Erstauftreten. Die **Meldungs**-Form ist gedeckt: der Übersprung nennt den Beleg-Stand und behauptet keine Fall-Zahl (`grep -n 'Kein Fall-Lauf' harness/tools/mutate.sh`). Ungedeckt ist jeder **spätere** Text — DoD-Zeile, Closure-Notiz, Review-Kommentar —, der die Unterscheidung *gemessen* gegen *belegt* nicht trifft. **Abgrenzung zu [`BEO-026`](observations.md):** dort nennt ein Zähler-**Label** eine falsche Einheit; hier fehlt die **Herkunfts-Angabe** einer Grün-Aussage. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) liest eine Closure-Notiz (`grep -n '^modules:' .d-check.yml`), und `make mutate` kennt keine Fehlschlag-Form dafür; Träger ist der Review, der eine solche Aussage liest |
| BEO-034 | Eine bats-Zusicherung benutzt eine GNU-only Coreutils-Erweiterung, die im gepinnten Alpine/BusyBox-Testbild lautlos scheitert — die Vergleichsmenge bleibt leer statt zu warnen, und die Zusicherung ist unter **jeder** Mutation grün, unabhängig vom geprüften Code | `*` (gesamtes Repo) | 1× | slice-180 | offen — Erstauftreten, gemessen an `find … -printf '%P\n'` in `test/mutate-driver.bats`: unter Host-`bash` mit GNU-`find` färbte die Zusicherung korrekt rot und grün, im Bild aus `BATS_IMAGE` (`grep -n 'BATS_IMAGE ?=' Makefile`) ist `-printf` keine BusyBox-Option, der Aufruf scheiterte im `$(...)`-Capture, und der Fall blieb strukturell grün. Gefunden hat es kein Lesen, sondern `make mutate` über den zugehörigen Fall aus `test/mutations/`. Das im Repo etablierte portable Muster trägt die Stelle jetzt, und `-printf` kommt in keiner bats-Datei mehr vor (`grep -c -- '-printf' test/*.bats`; kein Erwartungswert, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Abgrenzung zu [`BEO-028`](observations.md):** dort zeigt ein Mutations-Fall auf die falsche Datei; hier trifft er die richtige, und das Testwerkzeug kann die Frage nicht beantworten. Weder `make shell-lint` noch ein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält eine bats-Datei gegen den Options-Umfang des gepinnten Bildes (`grep -n '^modules:' .d-check.yml`); Träger ist der Autor einer neuen Zusicherung |
| BEO-035 | Die Datei-Namensformen zweier Rollen-Reports sind nicht disjunkt: die eine Rolle hat ein reserviertes Suffix, die andere einen freien Gegenstands-Teil, der es verschlucken kann — bemerkt wird die Kollision erst von der Rolle, die später schreibt, und die weicht dann von ihrer **eigenen** vorgeschriebenen Form ab | `*` (gesamtes Repo) | 1× | slice-180 | offen — Erstauftreten, gemessen an den zwei Vorgaben selbst: [`.claude/agents/reviewer.md`](../../../.claude/agents/reviewer.md) setzt `docs/reviews/<YYYY-MM-DD>-<gegenstand>.md`, [`.claude/agents/verifier.md`](../../../.claude/agents/verifier.md) setzt `docs/reviews/<YYYY-MM-DD>-<gegenstand>-verify.md` (`grep -n 'YYYY-MM-DD' .claude/agents/reviewer.md .claude/agents/verifier.md`) — der freie Teil der ersten Form enthält die zweite. Drei Reviewer-Reports tragen dadurch die Verifier-Endung (`grep -lE '^\*\*Rolle:\*\* Reviewer' docs/reviews/*-verify*.md \| wc -l` → **3** von **48** Dateien in dieser Endung, `ls docs/reviews/ \| grep -cE -- '-verify(-runde-[0-9]+)?\.md$'`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Der Bestand wird nicht umbenannt:** `docs/reviews/**` sind Zeitdokumente, ein Umzug bräche eingehende Verweise aus Plan und Report, und keine Quelle verbietet der Reviewer-Form ihren Gegenstands-Teil — die Reparatur wäre eine Verschärfung der einen oder eine Reservierung in der anderen Vorgabe, und beide sind eine Entscheidung, kein Nachzug. Kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) hält einen Dateinamen gegen die Rolle in seinem Kopf (`grep -n '^modules:' .d-check.yml`); Träger ist die Rolle, die als zweite in denselben Namensraum schreibt |
| BEO-036 | Der Verweis-Nachzug eines Lifecycle-Moves ersetzt auch den Pfad, der als **Tree-Operand** an einer Commit-Kennung hängt (`<sha>:<pfad>`) — dort ist der alte Pfad der richtige, und die Ersetzung macht aus einem laufenden Kommando eines, das `fatal` meldet | `*` (gesamtes Repo) | 1× | slice-180 | offen — Erstauftreten, im Closure-Lauf dieses Slice ausgelöst und im selben Lauf repariert: der Nachzug hängte `git show <sha>:docs/plan/planning/in-progress/slice-180-…md` auf `…/done/…` um, und `git` meldet für diesen Stand *„befindet sich im Dateisystem, aber nicht in"* der Kennung. **Der Bestand ist gemessen, nicht geschätzt:** **37** Tree-Operanden stehen in den lebenden Markdown-Artefakten, davon war genau **einer** tot, und zwar der eben erzeugte (`git grep -ohE '\b[0-9a-f]{7,40}:[A-Za-z0-9_./-]+\.md' -- '*.md' ':!.harness/baseline' \| sort -u \| wc -l`, dieselbe Liste je Eintrag durch `git cat-file -e`; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). **Kein Gate sieht die Klasse:** der Pfad steht in keinem Link und in keiner Codepath-Position — `make docs-check` meldet über dem reparierten wie über dem kaputten Stand dasselbe, und die Ersetzung ist genau die Handlung, die [`MR-040`](../../../harness/conventions.md#mr-040--drei-ausgänge-für-eine-präsens-aussage-über-den-vendored-baum) Ausgang 2 unmöglich machen will. **Abgrenzung zu [`BEO-003`](observations.md):** dort bleibt ein Verweis nach dem Move *unrepariert*; hier wird einer repariert, der richtig war. **Abgrenzung zu [`BEO-025`](observations.md):** dort steht die zu weite **Zusage** (die Grenzen-Liste im Skriptkopf nennt drei Grenzen und nicht diese vierte), hier die **Handlung** des Werkzeugs. Träger ist der Lauf, der nach einem Move seine eigenen Kommandos nachfährt |
| BEO-037 | Eine Vollständigkeits-Zusage über ein Delta misst auf der **Datei**-Ebene, während ihr Gegenstand auf der **Hunk**-Ebene lebt: eine Datei trägt mehrere unabhängige Positionen, und eine Position spannt über mehrere Dateien — die Zusage kann grün sein, während eine Position ohne Ausgang dasteht | `*` (gesamtes Repo) | 1× | slice-176 | offen — Erstauftreten, gemessen an der DoD dieses Slice: sie verlangt, dass *„die Datei-Liste des Katalogs `git diff --name-status` deckt"*. Am gemessenen Delta trägt `modul-06-roadmap.md` **fünf** Hunks mit fünf verschiedenen Gegenständen (Carveout-Frist · Archivierungs-Träger · das ganze §Das Beobachtungs-Register · Wellen-Eröffnung Schritt 2 · Register-Paarung (c)), während **eine** Position drei Dateien bündelt (`modul-05`, `modul-06`, `modul-10` für den wellenlosen Archivierungs-Träger) — **21** Hunks über **14** Dateien (`git diff v5.18.0 v6.0.0 -- lab/regelwerk lab/templates \| grep -c '^@@'` gegen dieselbe Abfrage mit `--name-status \| wc -l` am lokalen Kurs-Klon; keine Erwartungswerte, [`MR-025`](../../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 2). Der Katalog ist deshalb gegen die **Hunk**-Menge geführt, jeder Hunk genau einer Position zugeordnet. **Abgrenzung zu [`BEO-025`](observations.md):** dort nennt eine Zusage einen **Sensor**, der die Form nicht sieht; hier misst eine Zusage von Hand auf einer zu groben **Einheit**, und es gibt gar keinen Sensor. **Abgrenzung zu [`BEO-013`](observations.md):** dort ist die Bezugsmenge falsch (Delta statt Volltext), hier die Granularität. **Kein Gate sieht die Klasse** — kein Modul aus `modules:` der [`.d-check.yml`](../../../.d-check.yml) liest einen Diff (`grep -n '^modules:' .d-check.yml`); Träger ist der Lauf, der die Zusage formuliert |

## Gestrichene Einträge

Regeln dieser Sektion: Baseline-Regelwerk `modul-06-roadmap.md`
§Das Beobachtungs-Register — wer eine Zeile still löscht, macht sie
ununterscheidbar von einer, die es nie gab.

| Kennung | Beobachtung | Gestrichen am | Warum sie nicht mehr auftreten kann |
|---|---|---|---|
| — keine — | | | |
