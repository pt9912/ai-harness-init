**Vorgang:** slice-182
**Fund:** `slice-182` zog beim Baseline-Tausch die `# files:`-Köpfe von `test/mutations/219`, `220` und `218` mechanisch auf den neuen Tag, obwohl der Vorgang die genannte Datei entfernte — in einem Fall (`218`) sogar den injizierten Namen selbst tilgte —, sodass die Fälle grün blieben statt rot zu färben.
