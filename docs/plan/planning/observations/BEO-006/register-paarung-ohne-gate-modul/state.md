**Stand:** offen

Netzlos über beide Digests gemessen (den aktiven und das Ziel des Pin-Nachzugs):
`--print-config | grep -ci 'observation'` → 0 in beiden. Fehlende Fähigkeit eines Fremd-Werkzeugs,
keine Grenze dieses Repos; der Eigenbau wiegt schwerer als der Nutzen.
