# Adaptions-Block spricht über sich selbst

**Sub-Area:** `*` (gesamtes Repo)

Ein erheblicher Teil des Adaptions-Blocks beschreibt keine Abweichung von einer Baseline-Regel,
sondern Buchführung über den Block selbst — wie ein überholter, ein retirierter, ein
nachgetragener Eintrag aussieht.
