**Stand:** offen

Ein Sensor für die Klasse ist in diesem Register nicht benannt; ob `make shell-lint` `.bats`-Dateien
liest, ist nicht gemessen. Träger ist das Review, das eine Negation gegen ihre Stellung im Fall hält,
und die Form, die der Slice im Bestand vorgemacht hat: eine Funktion, die den Status des `grep`
selbst trägt (`nirgends` in `test/tap-nachzug.bats`), statt `!` mitten im Fall.
