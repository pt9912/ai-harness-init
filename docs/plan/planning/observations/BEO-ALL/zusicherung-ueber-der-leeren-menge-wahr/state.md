**Stand:** offen — Schwelle erreicht (`ls docs/plan/planning/observations/BEO-ALL/zusicherung-ueber-der-leeren-menge-wahr/evidence/*.md | wc -l` → 3, gelesen 2026-09-26, keine Erwartung). Der Ausgang steht aus: `geplant` verlangt die Kennung eines Slice, der die Regel oder den Sensor schreibt — keiner besteht —, und `verkörpert` ein Norm-Artefakt mit Zielort und Herkunfts-Anker, das der Architect schreibt. Übergabe an den Architect: ob und wo die Klasse eine Regel bekommt; Kandidat ist die Frage, die der Träger unten nennt, als Zeile im Reviewer-Skill oder als Satz zu §3.6. Der dritte Beleg ist ein Urteil des Planners (der Trap leert die Menge, statt dass sie wegfällt); trägt der Architect die Zuordnung nicht, steht der Eintrag bei 2× und `offen` ist der Normalzustand.

Ein Wächter besteht nicht: `make mutate` sieht die Vakuität nur, wenn ein gelisteter Fall genau die
Bezugsmenge entfernt — die Klasse ist damit an denselben Fall-Satz gebunden, dessen Lücke sie ist.
Träger ist der Lauf, der die Zusicherung schreibt, und die Frage danach, was ihr Ausdruck liefert,
wenn seine Eingabe leer ist.
