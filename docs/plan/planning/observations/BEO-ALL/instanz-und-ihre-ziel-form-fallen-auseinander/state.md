**Stand:** offen

Ein Wächter besteht nicht: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) hält eine Instanz gegen die Vorlage, die das
Instanz-Register ihr zuordnet — beide Review-Runden, die die Klasse gemeldet haben, führen für sie
`verifizierbar: nein`. Das Register selbst ist eine Tabelle ohne Sensor. Träger ist der Lauf, der
den Nachzug schreibt, und das Review danach.
