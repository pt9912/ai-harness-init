**Vorgang:** slice-193
**Fund:** Der beim Tausch angelegte Baum kam aus dem `git`-Baum des Kurs-Repos statt aus dem Release-Asset, dessen sha256 alle fünf Pin-Stellen nennen. Gemessen im Zustand vor der Korrektur: **26** Dateien des vendored Baums trugen Verweise der Form `../../kurs/de/…`, die aus `.harness/baseline/<tag>/regelwerk/` auf ein `.harness/kurs/de/` zeigen, das in keinem adoptierenden Repo existiert (`git grep -l '\.\./\.\./kurs/de/' 962c1722^ -- '.harness/baseline/v6.5.0' | wc -l`; heute `git grep -l '\.\./\.\./kurs/de/' -- '.harness/baseline' | wc -l` → **0**). **Keine Erwartungswerte** — die erste Zahl ist an einen Tree-Operanden gebunden und darum fest, die zweite wandert. Kein Gate sah die Adressen: `scan.ignore` nimmt `.harness/baseline/**` aus. Das Symptom ist mit `962c1722` behoben, die Ursache nicht: Der Baum wird weiter von Hand gelegt, und dass er byte-identisch mit dem Asset ist, belegt **netzlos kein Sensor** — `make baseline-verify` misst gegen ein `SHA256SUMS`, das derselbe Vorgang erzeugt.
