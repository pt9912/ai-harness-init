**Vorgang:** slice-tap-nachzug-ist-schritt-der-release-prozedur
**Fund:** Vier Aussagen in Schritt 7 von `docs/user/releasing.md` altern mit dem Modus `sync` und dem Release-Job: der Nachzug als *„Handgriff dieses Schritts"*, das *„einzige Tap-Ziel im Makefile"* (`grep -nE '^[a-z-]*tap[a-z-]*:' Makefile` → eine Zeile), dass ein Nachzug von Hand den Vorwärts-Schutz nicht hat, und das Kommando `grep -ci version harness/tools/tap-nachzug.sh harness/tools/tap-nachzug-nutzlast.sh` samt dem Satz, `check` vergleiche Bytes und keine Versionen — sobald `sync` die `version`-Zeile liest ([ADR-0064](../../../../../adr/0064-tap-nachzug-ein-skript-zwei-aufrufer-byte-kontrolle-gegen-das-asset.md) Festlegung 3 d, nur `sync`), liefert das Kommando mehr als 0. Die Bedingung, dass der Slice für `sync` den Schritt umbaut, steht allein im Review-Report (R1-3), im Verifikations-Report vom 2026-09-25 (Übergabe 3) und in §7 dieses Slice. Kein Artefakt, das der Lauf von `sync` als Eingang liest, nennt sie: sein Plan besteht nicht, und ein Sensor für die Alterung besteht nicht.
