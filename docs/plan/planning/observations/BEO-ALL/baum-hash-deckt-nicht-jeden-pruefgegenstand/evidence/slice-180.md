**Vorgang:** slice-180
**Fund:** `slice-180` fand zwei Funde in einem Vorgang: `isolation_key()` hasht nur Inhalte getrackter Dateien — Docker-Cache-Zustand und Host-Werkzeug-Version stehen in keinem Baum-Zustand, und ein reiner `chmod` bewegt den Schlüssel ebenfalls nicht, weil der Hash Inhalt liest.
