**Stand:** offen

Ein Wächter besteht nicht. Die Form ist über den Bestand der Fall-Dateien messbar
(`grep -lE "sed -i '[0-9]+s" test/mutations/*.sh`), gebaut ist kein Sensor darauf; `make mutate`
urteilt über die Wirkung eines Falls, nicht über die Form seines Ankers.
