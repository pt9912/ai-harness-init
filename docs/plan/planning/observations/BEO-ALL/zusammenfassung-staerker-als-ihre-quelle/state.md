**Stand:** offen

Kein Modul aus `modules:` der `.d-check.yml` hält eine Zusammenfassung gegen das Artefakt, auf das
sie zeigt: `links` prüft die Auflösbarkeit, nicht die Aussage. Träger ist der Lauf, der die
referenzierte Quelle vollständig liest.
