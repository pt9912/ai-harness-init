**Stand:** offen

Ein Wächter besteht nicht: Die Klasse zeigt sich einer Sonde, die eine Eingabe-Klasse durch die
unveränderte Funktion schickt; die Tabelle der Tests hält nur ihre Fälle. Träger ist das Review, das
solche Klassen gegen die Funktion probiert, statt die Tabelle zu lesen.
