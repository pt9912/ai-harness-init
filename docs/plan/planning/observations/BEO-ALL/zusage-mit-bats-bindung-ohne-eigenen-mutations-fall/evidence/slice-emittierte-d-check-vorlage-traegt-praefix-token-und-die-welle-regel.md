**Vorgang:** slice-emittierte-d-check-vorlage-traegt-praefix-token-und-die-welle-regel
**Fund:** Die Zusage des Herkunfts-Kommentars der Vorlage, `.d-check.yml` werde nur an einem freien Pfad geschrieben, hängt am generischen skip-if-present-Mechanismus (`test/mutations/50-skipifpresent-clobbert.sh`, `test/mutations/51-wire-skipifpresent-clobbert.sh`); für die Datei selbst besteht kein Fall, und kein Go-Test der Emission nennt sie (`grep -n 'd-check.yml' internal/emit/*_test.go | grep -iE 'skip|clobber|adopter|vorhanden'` → kein Treffer, gemessen 2026-09-25). Das Verhalten stimmt — ein Ziel mit vorhandener Datei bleibt byte-gleich —, gehalten hat es eine Sonde des Verifiers, kein Fall. Die Bindung ist schwächer als in der ersten Lage der Klasse: dort hängt die Zusage an einer Assertion in einem `bats`-Fall, hier an keiner für diese Datei.
