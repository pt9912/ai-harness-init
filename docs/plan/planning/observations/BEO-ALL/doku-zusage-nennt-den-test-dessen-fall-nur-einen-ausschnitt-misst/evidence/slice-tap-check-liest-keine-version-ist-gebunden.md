**Vorgang:** slice-tap-check-liest-keine-version-ist-gebunden
**Fund:** Der Slice heilte die Instanz M-1 der Kurzrunde und trug am ersten Text (Commit `026c1aff`) zwei weitere Instanzen derselben Klasse in Schritt 7 (*Grenze*) von `docs/user/releasing.md`: die Überschrift *„Nicht gebunden"* über zwei Formen der Versions-Lektüre, die `test/tap-nachzug.bats` über andere Fälle bindet (Review R1-1: eine kleinere Tap-Version als gleich färbt drei Fälle, eine gleiche `version`-Zeile bei ungleichen Bytes fünf), und den Allsatz *„ein Vergleich, der … liest"*, den nur die Form der Mutation 452 bindet, nicht jede Variante der Beschreibung (R1-2: eine Variante *„größere Tap-Version und weitere Zeilen weichen ab"* überlebt die 38 Fälle). Beide sind in `e873b633` gezogen, der Verifier fuhr die Aussagen einzeln. Ein Wächter, der `releasing.md` gegen die Fälle hält, besteht weiter nicht.
