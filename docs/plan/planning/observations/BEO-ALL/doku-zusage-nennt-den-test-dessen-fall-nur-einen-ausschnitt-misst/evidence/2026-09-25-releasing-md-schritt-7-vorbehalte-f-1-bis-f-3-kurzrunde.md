**Vorgang:** 2026-09-25-releasing-md-schritt-7-vorbehalte-f-1-bis-f-3-kurzrunde
**Fund:** Schritt 7 von `docs/user/releasing.md` sagte *„der Vergleich liest keine `version`-Zeile"* und nannte den Fall `version-zeile: in check kein Gegenstand …` als Halter. Der Fall fährt nur gleiche Bytes; eine Mutation in `gleich()` der Nutzlast, die bei ungleichen Bytes die `version`-Zeilen liest und bei größerer Tap-Version mit Exit 0 endet, überlebt alle 37 Fälle von `test/tap-nachzug.bats` (Kurzrunde M-1, gegengeprobt: dieselbe Stelle mit `!=` statt `>` färbt drei Fälle). Der Text ist auf das gezogen, was der Fall bindet, und benennt, dass der Zahn fehlt.
