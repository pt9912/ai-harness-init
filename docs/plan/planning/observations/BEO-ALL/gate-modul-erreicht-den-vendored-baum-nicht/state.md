**Stand:** offen

Kein Modul aus `modules:` der [`.d-check.yml`](../../../../../../.d-check.yml) prüft die Klasse:
`codepaths` erreicht die Quell-Dateien nicht (`roots: [spec, docs, harness]`), `links` sieht nur
die Link-Form, und `make comment-claims` nimmt jede Markdown-Datei dauerhaft aus seinem
Prüfbereich. Ob der Prüfbereich erweitert wird oder die Grenze benannt stehen bleibt, ist als
`slice-201` geschnitten. Träger ist bis dahin der Lauf, der den Pfad schreibt.
