**Vorgang:** slice-193
**Fund:** Nach dem Tausch stehen tote Baseline-Pfade in **lebenden** Artefakten, und `make docs-check` meldet keinen davon — gemessen am Stichtag 2026-09-08 über den lebenden Bestand (**keine Erwartungswerte**): `git grep -l '\.harness/baseline/v6\.0\.0' -- '*.md' '*.go' '*.sh' '*.yml' 'Makefile' ':!docs/plan/planning/done' ':!docs/reviews' ':!harness/conventions/done' ':!docs/plan/adr' ':!docs/plan/planning/observations'` nennt **6** Dateien — `AGENTS.md`, `.harness/skills/reviewer.md`, zwei Slice-Pläne in `open/`, ein Adaptions-Eintrag und der Plan dieses Slice —, und `make docs-check 2>&1 | awk -F'\t' 'NF>2{split($1,a,":"); print a[1]}' | sort -u` führt **keine** davon. Alle 36 Befunde des Laufs liegen in einfrierenden Bäumen; die lebenden Vorkommen sind stumm, weil sie ohne Link-Klammer stehen und `codepaths` ihre Dateien nicht in seinen `roots` führt. Der Unterschied ist damit nicht die Eigenschaft der Adresse, sondern ihre Markup-Form.
