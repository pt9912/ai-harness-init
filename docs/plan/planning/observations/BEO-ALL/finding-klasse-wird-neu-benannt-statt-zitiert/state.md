**Stand:** offen

Kein Wächter meldet die Klasse. Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) hält eine Klassen-Zeile gegen den Slug-Bestand
unter `docs/plan/planning/observations/`, und `make mutate` kennt dafür keine Fehlschlag-Form —
beide Seiten wären messbar, denn der Slug-Bestand ist ein `ls`. Träger ist heute allein die
Slice-Closure, die die Liste zuordnet; sie ist zugleich der einzige Leser, der den Fehler
überhaupt sehen kann.
