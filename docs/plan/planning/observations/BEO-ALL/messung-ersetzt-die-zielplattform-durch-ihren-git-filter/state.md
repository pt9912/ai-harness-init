**Stand:** offen

Unterhalb der Schwelle; `offen` ist hier der Normalzustand und kein Ausgang — *geplant* und
*verkörpert* sind die Antwort auf die Schwelle
(`modul-06-roadmap.md` §Das Beobachtungs-Register), und sie ist mit einem Beleg nicht erreicht.

Ein Wächter besteht nicht: Die Windows-Runner tragen keine Linux-Container, und die Stufe
`zeilenenden_im_klon` in `harness/tools/full-smoke.sh` liest `git`, nicht Windows. Träger ist der
Lauf, der eine Zusage über die Plattform schreibt — er nennt im selben Satz, dass sie am
Git-Filter unter Linux gemessen ist.
