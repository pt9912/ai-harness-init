**Vorgang:** slice-123
**Fund:** DoD (2) sagte einen Rot-Nachweis für die Zuordnung *history-lesende Schritte ⊆ Checkouts mit voller Tiefe* zu. Beide Mengen sind leer (`grep -rnE 'doc-immutable|doc-commits' .github/workflows/ | grep -v ':[0-9]*:#' | wc -l` und `grep -rn 'fetch-depth' .github/workflows/ | grep -v ':[0-9]*:#' | wc -l`, je 0), das Kriterium damit vakuos wahr und unter keiner Mutation rot zu bekommen — kein Sensor liest Workflows, `make ci-lint` prüft Syntax. Der Ausweg war der einzige verfügbare: die Zusage auf das einschränken, was der Bau hält, und den Beleg auf die Begründung im Workflow-Kopf legen. Bemerkenswert ist, wer es fand: nicht die zwei Review-Runden, sondern der Planner-Lauf, der die DoD neu fasste.
