**Stand:** offen

Ein Wächter besteht nicht: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) hält eine Prosa-Behauptung über die Baseline gegen
den adoptierten Stand, und `make comment-claims` nimmt jede Markdown-Datei dauerhaft aus. Träger ist
der Lauf, der die Berufung schreibt, und das Review danach.
