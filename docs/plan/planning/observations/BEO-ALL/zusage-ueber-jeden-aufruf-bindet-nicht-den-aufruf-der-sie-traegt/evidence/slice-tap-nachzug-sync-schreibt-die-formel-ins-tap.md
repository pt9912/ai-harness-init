**Vorgang:** slice-tap-nachzug-sync-schreibt-die-formel-ins-tap
**Fund:** Der Fall `sync token` in `test/tap-nachzug.bats` hielt *„jede Kopfdatei, die der `curl`-Stub sieht, trägt Modus 0600 und den Bearer"*, und der Kopfkommentar der Nutzlast sagte den Header für den Schreibaufruf zu. Der Review nahm `-H "@$hdr"` aus dem **Schreib**aufruf (Schwächung H): 52 von 52 Fällen grün, kein Fall behauptete, dass der Schreibaufruf das Token trägt (Review R-1, MEDIUM). Der Implementer band den Header an die Methode (`STUB_LOG_HDR_PUT`, `bb583255`); der Verifier sah die Schwächung jetzt rot und die Gegenprobe (Zeile entfernt, Schwächung bleibt) grün — die Zeile bindet allein.
