**Stand:** offen

Ein Wächter besteht nicht: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) urteilt darüber, welchen Ausschnitt einer Regel ein
anderer Sensor abdeckt, und `make comment-claims` prüft, ob ein genannter Sensor **existiert**,
nicht, welche Kante er trifft. Träger ist der Lauf, der die Zusage formuliert.
