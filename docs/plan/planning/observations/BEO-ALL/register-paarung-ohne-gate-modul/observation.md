# Register-Paarung ohne Gate-Modul

**Sub-Area:** `*` (gesamtes Repo)

Die maschinell entscheidbare Hälfte der Register-Paarung — jede in `done/` zitierte `BEO-<NNN>`
hat eine Zeile, und jede Zeile trägt mindestens einen Beleg — hat in keinem gepinnten
Doku-Gate-Stand ein Modul.
