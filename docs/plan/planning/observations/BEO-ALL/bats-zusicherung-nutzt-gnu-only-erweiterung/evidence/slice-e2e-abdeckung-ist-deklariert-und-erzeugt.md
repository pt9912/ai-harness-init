**Vorgang:** slice-e2e-abdeckung-ist-deklariert-und-erzeugt
**Fund:** Der Erzeuger `harness/tools/e2e-abdeckung.sh` leitet den Anker-Slug aus der `### LH-…`-Ueberschrift des Lastenhefts ab und benutzte dafuer eine sed-**Bereichs**-Aufzaehlung (`[.-@]`); Host-sed (glibc) und das busybox-sed des gepinnten Testbilds lesen sie verschieden — derselbe Quelltext ergab damit zwei Tabellen. Die committete `docs/user/e2e-abdeckung.md` stammte aus dem Host-Lauf, geprueft wurde im Container: erst der Halter (`test/e2e-abdeckung.bats`, vierte Laufform, `cmp -s` gegen die committete Datei) hat die Abweichung rot gefaerbt. Der Erzeuger laeuft in keiner Kette — ohne diesen Halter waere die Abweichung still geblieben.
