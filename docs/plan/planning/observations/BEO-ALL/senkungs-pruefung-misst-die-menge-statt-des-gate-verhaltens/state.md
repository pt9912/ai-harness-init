**Stand:** offen

Kein Gate sieht die Klasse: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) liest zwei Konfigurations-Stände gegeneinander,
und `make mutate` kennt keine Fehlschlag-Form für eine Messung, die den falschen Gegenstand
trifft. Träger ist der Lauf, der die Senkungs-Frage stellt — er baut die Sonde oder er hat die
Frage nicht beantwortet.
