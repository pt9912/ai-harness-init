**Vorgang:** slice-175
**Fund:** `slice-175` durchlief acht Review-Runden, von denen sieben dieselbe Familie an einer je weiter außen liegenden Ebene fanden; `test/unterkommando-kopplung.bats` deckt bis heute nur die `//`-Kommentarform, nicht `/* … */`.
