**Stand:** offen

Kein Modul aus `modules:` der [`.d-check.yml`](../../../../../../.d-check.yml) hält eine
abgeschaffte Kennungs-Form gegen ein Artefakt, das die Ablage-Regel einfriert, und `make mutate`
kennt dafür keine Fehlschlag-Form. Träger ist der Lauf, der die Form abschafft.
