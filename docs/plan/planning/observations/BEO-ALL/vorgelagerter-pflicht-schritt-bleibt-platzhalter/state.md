**Stand:** offen

Kein Modul aus `modules:` der [`.d-check.yml`](../../../../../../.d-check.yml) hält einen
Abschnitt gegen die Frage, ob er ein Ergebnis oder eine Absichtserklärung trägt, und die Fähigkeit
`closure` desselben Moduls urteilt über Abschnitt 7, nicht über Abschnitt 8. Träger ist der Lauf,
der den Plan aus `next/` übernimmt.
