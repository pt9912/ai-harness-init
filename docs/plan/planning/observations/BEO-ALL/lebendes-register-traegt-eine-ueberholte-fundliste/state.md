**Stand:** offen

Ein Wächter besteht nicht: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) liest eine Prosa-Aufzählung gegen die Ausgabe des
Kommandos, das sie belegen soll, und `make mutate` kennt dafür keine Fehlschlag-Form. Träger ist der
Lauf, der den Eintrag gegen seine eigene Messung liest — der Review daneben.
