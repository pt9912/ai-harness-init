**Vorgang:** slice-tap-nachzug-ist-schritt-der-release-prozedur
**Fund:** Fünf Stellen in Schritt 7 von `docs/user/releasing.md` gaben den Vertrag von `make tap-check` weiter oder anders wieder, als Skript und ADR ihn tragen: die Aufzählung der Klasse 2 ohne die wahrscheinlichste Ursache und als abschließend gelesen (Review R1-1), die Vorab-Regel in einer Kurzform, die `v1.0.0+build-1` anders entscheidet als das Skript (R1-2), *„es wurde nichts verglichen"* für einen Docker-Ausfall, den das Skript als *„unbekannt"* meldet (Verifier F-1), ein Kommando, das das Wort `version` bindet, unter einem Satz über die Eigenschaft (F-2), und *„genau einmal bei Exit 1 und 2"* ohne die Ausnahmen der ADR (F-3). Alle fünf sind im Diff des Slice gezogen; die zwei letzten Commits hat kein Reviewer gelesen, der Verifier fuhr ihre Aussagen.
