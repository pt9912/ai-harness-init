**Vorgang:** slice-tap-nachzug-sync-schreibt-die-formel-ins-tap
**Fund:** Der Umbau von Schritt 7 in `docs/user/releasing.md` begründete das Kommando `grep -ci version harness/tools/tap-nachzug.sh harness/tools/tap-nachzug-nutzlast.sh` mit *„liefert mit dem Modus `sync` je Datei mehr als `0`, weil `sync` die Zeile für den Vorwärts-Schutz liest"*. Die Zählung stimmte (`1` und `11`, gemessen vom Review und vom Verifier), die Begründung nur für die Nutzlast: der eine Treffer im Host-Skript ist das Wort im Kopfkommentar, das Host-Skript liest keine `version`-Zeile. Der Review fand es als R-3; der Implementer zog es in `c45140c1`, und der Verifier fand in der Wiedergabe der Klassen und Meldungen keinen weiteren Fehler. Im selben Vorgang nannte der Vorab-Satz des Schritts einen Wegfall des Nachzugs, der ohne `TAP_TOKEN` nicht gilt (Verifier V-3, Schritt b geht Schritt c voraus) — derselbe Fund, keine zweite Gelegenheit. Ein Vorgang zählt einmal.
