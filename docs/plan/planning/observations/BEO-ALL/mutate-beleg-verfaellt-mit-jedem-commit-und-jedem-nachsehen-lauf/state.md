**Stand:** offen

Ein Wächter besteht nicht: Die Klasse ist eine Eigenschaft der Bauart, keine Verletzung einer
Regel. Gemildert wird sie durch `MUTATE_JOBS` (Dauer) und die Übersprung-Meldung; die Rolle, die
den Beleg braucht, fährt den Lauf am Endstand selbst.
