**Vorgang:** slice-193
**Fund:** Die vendored `slice.template.md` nennt in ihrem Reconciliation-Item den Register-Pfad als Inline-Code (`grep -n -i reconcil .harness/baseline/v6.5.0/templates/docs/plan/planning/slice.template.md` → Zeile 88), und dieses Repo führt die Datei nicht (`ls docs/plan/planning/reconciliation.md` → *nicht vorhanden*); `codepaths` prüft Inline-Code-Pfade auf Existenz. **Der Tausch behebt es nicht:** `v6.5.0` fasst die Vorlage in zwei Schritten an, die Reconciliation-Zeile aber nicht. Im Dogfood bleibt es stumm, weil `scan.ignore` den vendored Baum ausnimmt und dieser Plan den Pfad als Kommando-Operanden schreibt; in einem frisch gebootstrappten Greenfield-Repo trägt jeder kopierte Slice-Plan den Befund. Der Fund gehört damit auf die **emittierte** Ebene, deren Beleg `make full-smoke` ist und nicht `make gates` — und dort ist er heute ungemessen.
