**Vorgang:** slice-stilllegungs-kanten-sind-gemessen
**Fund:** Der Claim-Commit `cbb49bf2`, geschrieben von `make slice-mv`, trägt keine Traceability-Kennung (`git show -s --format=%s cbb49bf2 | grep -cE 'ADR-[0-9]{4}|LH-[A-Z]{2}-[0-9]{2}|MR-[0-9]{3}|slice-[0-9]+'` → 0), wie jeder Commit des Werkzeugs für einen benannten Slice. Er ging durch, weil in diesem Klon `core.hooksPath` nicht gesetzt ist (`git config --get core.hooksPath` → Exit 1). Die Klasse steht als Zeile der Reichweiten-Tabelle in `harness/README.md` §Traceability, mit der Adresse `slice-werkzeug-commits-tragen-eine-kennung` (§6 Risiko 4 des Slice).
