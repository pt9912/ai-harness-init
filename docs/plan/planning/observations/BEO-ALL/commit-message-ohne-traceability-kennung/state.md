**Stand:** offen

Ein Wächter besteht nicht: Das Modul `commits` steht nicht in `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml), und `make mutate` kennt keine Fehlschlag-Form für
eine Commit-Message. Zwei Slices tragen die Frage bereits — `slice-126` (der Sensor) und
`slice-121` (was eine Message nennen darf); die Bezugseinheit *jeder Commit* gegen *die Änderung als
ganze* ist dort mit zu entscheiden.
