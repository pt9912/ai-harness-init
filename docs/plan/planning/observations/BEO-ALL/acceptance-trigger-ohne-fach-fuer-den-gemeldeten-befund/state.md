**Stand:** offen

Ein Wächter besteht nicht: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) liest einen Acceptance-Trigger, und ob ein Befund
in ein Fach fällt, ist ein Urteil über zwei Texte. Träger ist die prüfende Runde, die den fehlenden
Fall benennt, solange die Datei `Proposed` ist.
