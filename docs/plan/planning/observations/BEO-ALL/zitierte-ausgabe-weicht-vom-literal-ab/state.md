**Stand:** offen

Ein Wächter besteht nicht: Die Modul-Liste der [`.d-check.yml`](../../../../../../.d-check.yml)
führt kein Zitat-Modul (`check-lines` ist hier nicht aktiviert), und `make comment-claims` nimmt
jede Markdown-Datei dauerhaft aus seinem Prüfbereich. Träger ist der Lauf, der das Zitat schreibt,
und die Kalibrierung daneben — dieselbe Datei zitiert Lauf-Ausgaben sonst zeichengleich.
