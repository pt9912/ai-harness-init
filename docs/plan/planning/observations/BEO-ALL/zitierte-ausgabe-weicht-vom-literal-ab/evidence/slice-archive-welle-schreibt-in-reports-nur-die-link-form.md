**Vorgang:** slice-archive-welle-schreibt-in-reports-nur-die-link-form
**Fund:** Die Sensor-Doku `harness/sensors/archive-welle.md` zitierte den Rückweg, den das Werkzeug nach einem Fehler zwischen den zwei Commits nennt, als `git clean -fd -- done/<welle-id>`; die Meldung von `NachCommit1Fehler` gibt `git clean -fd -- docs/plan/planning/done/<welle>` aus. Aus der Repo-Wurzel ist der Pfad der Doku kein Pfad, ein wörtliches Abtippen räumt nichts (Verifikation V-1). Im Vorgang nachgezogen (`f982adb8`); der Doku-Text ist an keinen Test gebunden, der Test des Fehlerpfads prüft nur die `git reset`-Hälfte.
