**Stand:** offen

Kein Modul aus `modules:` der [`.d-check.yml`](../../../../../../.d-check.yml) liest einen DoD-Punkt,
und `make mutate` kennt keine Fehlschlag-Form, in der ein Abnahmemaßstab rot wird. Der Vergleich,
der diese Klasse trennt — dasselbe Kommando über dem Eltern-Stand fahren und sehen, ob es dort
bereits grün ist —, braucht weder Docker noch Sensor-Lauf, nur einen `git archive`; als Schritt
eines Anweisungssatzes besteht er nicht. Träger ist die Rolle, die den Maßstab schreibt
([`AGENTS.md`](../../../../../../AGENTS.md) §3.10).
