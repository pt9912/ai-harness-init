**Vorgang:** slice-104
**Fund:** DoD (3) maß mit `grep -l '^# files:.*full-smoke' test/mutations/*.sh` und sagte dazu
*„heute leer"* — gemessen über dem Baum vor der Arbeit trifft es **drei** fremde Fälle, deren
`# verify:` eine andere Stufe nennt und deren Mutation eine andere Funktion trifft; der gelieferte
Fall erscheint darin nicht, weil seine `# files:`-Zeile die gepatchte Quelle nennt statt des
Sensors, dessen Rot erwartet wird.
