**Vorgang:** slice-archive-welle-schreibt-in-reports-nur-die-link-form
**Fund:** Der Doc-Kommentar der Link-Regel in `internal/archive/refs.go` sagte zu, sie überquere weder eine Link- noch eine Zeilengrenze und der Report-Baum ende am Verzeichnis. Der Review (Runde 1, Stand `5d7a1de2`) nahm `\n` aus beiden Zeichenklassen, dann den Schrägstrich hinter dem Verzeichnis, dann die Maskierung des Namens: alle drei Schwächungen ließen die Suite grün. Im Vorgang gebunden sind die Zeilengrenze und die Verzeichnisgrenze (zwei Go-Tests, Fälle `472` und `473`, Commits `fc638444` und `a038a349`); die Maskierung des Namens bleibt ohne Zahn und ist nicht behoben.
