**Vorgang:** slice-tap-nachzug-sync-schreibt-die-formel-ins-tap
**Fund:** Liefer-Punkt 2 der DoD sagte *„`make docs-check` färbt sich in beiden Fällen (`targets`)"* für das Fehlen der README-Zeile und das Fehlen des Eintrags in `targets.exempt-targets`, gestützt auf den Satz der Folgepflicht 1 der ADR, ohne Eintrag färbe das Doku-Gate im ersten Lauf rot. Der Verifier maß in vier Scratchpad-Kopien: Eintrag allein entfernt → Exit 0, README-Zeile allein entfernt → Exit 0, beides entfernt → Exit 2 (`gate-undocumented`). Die Annahme war ungemessen und wurde vom Vorgang widerlegt; das gelieferte Artefakt ist richtig, der Punkt wie geschrieben nicht wahr abhakbar, und der Implementer und der Reviewer durften ihn nicht umschreiben. Der Planner formulierte ihn bei der Closure auf das Gemessene (Verifier V-1). Unterform der Klasse: eine Zusage über zwei redundante Träger, deren Sensor erst bei Ausfall beider färbt.
