**Vorgang:** slice-129
**Fund:** Der korrigierte Kommentar von `test/mutations/288-closure-dir-anderer-pfad.sh:9` sagt im
Indikativ *„faerbt darauf 45 `closure-note-thin`-Befunde (EXIT 1)"*. Die Zahl war bei Niederschrift
richtig, ist aber keine Eigenschaft der Stelle, sondern der Inventur: sie zählt die `slice-*.md`
unter `docs/plan/planning/open/` mit dünnem §7, und jeder neue offene Plan wie jeder `slice-mv`
verschiebt sie. Weder Kommando noch Mess-Datum steht daneben. Innerhalb desselben Vorgangs schon
gewandert: die Verifikation maß zwei Tage später **43**. Die tragende Hälfte des Satzes — *der
falsche Bestand wird gemeldet, nur unter der falschen Diagnose* — ist von der Zahl unabhängig und
bleibt wahr; kein Gate erreicht die Stelle, `test/` liegt außerhalb von `make comment-claims`.
