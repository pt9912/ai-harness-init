**Vorgang:** slice-125
**Fund:** Der Sensor, den dieser Slice gebaut hat, fängt seine eigene Closure: Wird die Plan-Datei per `git mv` nach `done/` bewegt, trägt `in-progress/` nur noch die Roadmap, und ohne die Rückstellung des Ruhe-Markers meldet `docs-check` sofort `planning-drift`, Exit 1 — vom Verifier an einer Kopie außerhalb des Arbeitsbaums selbst reproduziert. Die Bedingung stand zum Zeitpunkt der Freigabe in **keinem** Artefakt, das die Closure liest: der Slice-Plan §6 benannte nur die Umsetzungs-Hälfte (*„der Gate muss grün sein, während `in-progress/` besetzt ist"*), §7 war leer, `make slice-mv` zieht Pfade nach und keine Zustandssätze, und die Übergabe-Liste des freigebenden Review-Laufs führte sie nicht mehr. Erzwungen war sie allein mechanisch — vom Stop-Hook, der vor Session-Ende einen frischen grünen `make gates`-Lauf verlangt.
