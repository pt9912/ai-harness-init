**Vorgang:** slice-193
**Fund:** Der `in-progress → done`-Move dieser Closure leert `in-progress/` und macht damit das Fehlen des Ruhe-Markers *„Nichts in Arbeit."* unter *Offene Wellen* falsch; der Ausgleich lief wieder als eigener Commit von Hand. `make slice-mv` führte den Move aus und zog die Verweise nach — das Zustandsfeld rührte es nicht an, wie seine eigene Zusage sagt (*Pfade nach, keine Zustandssätze*). Kein Anweisungssatz, den der bewegende Lauf liest, nennt den Ausgleichs-Schritt: [`.claude/commands/close-welle.md`](../../../../../../../.claude/commands/close-welle.md) führt den Lifecycle-Move der Welle-Plan-Datei und die Roadmap-Fortschreibung als Schritt 3 und 6, den Ruhe-Marker nennt es an keiner Stelle — und die zwei Geschwister-Anweisungssätze ebenso wenig (`grep -h -c 'Nichts in Arbeit' .claude/commands/*.md | awk '{s+=$1} END{print s}'` → 0 über alle drei; `grep -rl 'Ruhe-Marker' .claude/commands/ | wc -l` → 0). Dieser Beleg hebt den Zähler auf 3 und damit über die Schwelle.
