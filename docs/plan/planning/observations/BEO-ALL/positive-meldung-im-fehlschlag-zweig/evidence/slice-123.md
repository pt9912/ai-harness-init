**Vorgang:** slice-123
**Fund:** Zweimal im selben Slice, das zweite Mal in dem Commit, der das erste behob. Zuerst nahm `decide()` einen nicht-numerischen Wert entgegen, gab `[: Ganzzahliger Ausdruck erwartet` auf `stderr` aus und endete mit `aufgeloest, abc Commit(s) — OK`, Exit 0. Nach dem Fix trug `decide_staged()` — im selben Commit entstanden — dieselbe Lücke: `--decide-staged abc` bewertete nichts und endete mit Exit 0, `--decide-staged 2` galt stumm als „gestagte Änderung vorhanden", obwohl der Vertrag `<0|1>` lautet. Beide Zweige sind jetzt fail-closed mit Exit 2 und haben je einen Fall in `test/mutations/`. Das Bemerkenswerte ist die Wiederholung im Fix selbst: die Klasse wurde erkannt, korrigiert und in derselben Datei zwölf Zeilen tiefer neu geschrieben.
