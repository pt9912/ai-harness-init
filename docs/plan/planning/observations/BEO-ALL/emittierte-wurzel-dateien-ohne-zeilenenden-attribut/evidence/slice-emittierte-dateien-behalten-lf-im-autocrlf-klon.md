**Vorgang:** slice-emittierte-dateien-behalten-lf-im-autocrlf-klon
**Fund:** Die Restmenge außerhalb der fünf Verzeichnisse gibt die Stufe `zeilenenden_im_klon` aus: im autocrlf-Klon tragen 28 Dateien CR — die Wurzel-Dateien des Adopters (`Makefile`, `d-check.mk`, `.d-check.yml`, `Dockerfile`), `.claude/` außerhalb der Hooks, `docs`, `spec`, `harness/*.md`, `cmd`, `go.mod` (Momentaufnahme des Verifiers im Verifikationsbericht vom 2026-09-25, kein Erwartungswert). Für das `Makefile` unter GNU Make 4.3 ist kein Bruch gemessen, für ein Windows-`make` und BuildKit nichts ([Plan §6](../../../../done/slice-emittierte-dateien-behalten-lf-im-autocrlf-klon.md), zweites Risiko).
