**Vorgang:** slice-184
**Fund:** Die Klasse ist bis in die Nutzerdoku durchgeschlagen: die Änderungshistorie des Benutzerhandbuchs beschrieb den Register-Ort zunächst als etwas, das der Bootstrap leer anlegt — `git grep -n 'observations' -- internal/emit/ internal/bootstrap/` nennt für das aufgesetzte Repo nur die drei Anweisungssätze, die den Ort **lesen**, und keinen, der ihn anlegt.
