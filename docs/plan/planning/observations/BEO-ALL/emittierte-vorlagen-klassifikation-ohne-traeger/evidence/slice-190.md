**Vorgang:** slice-190
**Fund:** Die Klasse trifft nicht mehr einen Ort, sondern drei zugleich: `codepaths` über einem frisch gebootstrappten Ziel meldet sechs Fundstellen, an denen ein mitemittierter Text ein Verzeichnis im Indikativ führt, das kein Emissions-Pfad anlegt — `harness/conventions/`, `docs/plan/carveouts/done/` und `docs/plan/planning/observations/`. Nur der erste ist hier angelegt worden (`sed -n '/^func structureGitkeeps/,/^}/p' internal/emit/templates.go | grep -c '^\t\t"'` → **7**, kein Erwartungswert); der zweite bekommt einen Ignore-Marker statt einer Anlage, und für den dritten bleibt die Lage unverändert: die drei Rückstände der Messreihe sind namentlich seine.
