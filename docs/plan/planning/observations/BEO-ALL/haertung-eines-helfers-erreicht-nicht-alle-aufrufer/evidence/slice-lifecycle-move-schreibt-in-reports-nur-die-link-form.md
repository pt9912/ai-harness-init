**Vorgang:** slice-lifecycle-move-schreibt-in-reports-nur-die-link-form
**Fund:** Die Härtung von `psed_i` in `harness/tools/slice-mv.sh` (Status 2, kein Zurückschreiben nach gescheitertem `sed`) erreicht `rewrite_incoming_bare_in_file` und `rewrite_outgoing_bare_in_file` nicht: bei ausfallendem `sed -E` melden beide Status 0 mit Zähler 2 bzw. 1 (V-2, PATH-Wrapper, Verifier); beide liegen in der Kern-Liste und sind in der emittierten Fassung wortgleich. Die emittierte Fassung trägt `psed_i` in der alten Form außerhalb der Kern-Liste (V-3, gelesen); ihre Präfix-Schleife steht dort ohne `||` unter `set -e` und ist davon nicht betroffen (abgeleitet, nicht gefahren). Die Sensor-Doku benennt die Lücke; der Umbau ist nicht Teil des Vorgangs (`docs/reviews/2026-09-26-verify-slice-lifecycle-move-schreibt-in-reports-nur-die-link-form.md`).
