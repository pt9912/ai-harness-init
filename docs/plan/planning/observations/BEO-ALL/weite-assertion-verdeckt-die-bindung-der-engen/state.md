**Stand:** offen

Gemeldet wird die Klasse von keinem Sensor: `make mutate` verlangt, dass der Name des Tests in der
roten Ausgabe steht, und liest keine Assertion. Träger ist die Gegenprobe, die jede Assertion eines
Falls **einzeln** abschwächt und nicht nur die, die der Zahn nennt.
