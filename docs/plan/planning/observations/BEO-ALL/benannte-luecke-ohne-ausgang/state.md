**Stand:** offen

Ein Wächter besteht nicht, und zwar in beide Richtungen: Kein Modul aus `modules:` der
[`.d-check.yml`](../../../../../../.d-check.yml) urteilt über den Umfang von Prosa, und
`make mutate` kennt dafür keine Fehlschlag-Form. Die eine Regel, die eine Aussage auf das
einschränkt, was die Stelle trägt, reicht hier nicht hin:
[`AGENTS.md`](../../../../../../AGENTS.md) §3.7 nennt als Geltungsbereich Code, Konfiguration,
Skripte und die Zustandsfelder der lebenden Register — Markdown-Fließtext steht in keinem davon,
und `make comment-claims` nimmt jede Markdown-Datei dauerhaft aus
([`harness/README.md`](../../../../../../harness/README.md) §Sensors, Punkt 2). Träger ist der
Lauf, der den Absatz schreibt.
