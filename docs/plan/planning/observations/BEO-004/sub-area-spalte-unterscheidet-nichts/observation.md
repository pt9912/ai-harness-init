# Sub-Area-Spalte unterscheidet nichts

**Sub-Area:** `*` (gesamtes Repo)

Die Sub-Area-Spalte dieses Registers unterscheidet nichts: die Modus-Deklaration führt `*`
(gesamtes Repo), und `*` schließt jede Berührung ein.
