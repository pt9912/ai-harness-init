# Review-Report: slice-089 (Code) — 2026-08-22

**Review-Art:** **Code** — geprüft wird der Diff gegen den Slice-Plan, gegen die Accepted-ADR,
deren Folgepflichten er vollzieht, und gegen die Hard Rules (Modul 10 §Drei Review-Arten). Zwei
Plan-Runden liegen davor; sie sind Eingang, nicht Gegenstand.

**Gegenstand:** `fa4a05d..83b3f83` — **ein** Commit, **fünf** Dateien
(`git diff --stat fa4a05d..83b3f83` → `5 files changed, 81 insertions(+), 37 deletions(-)`;
`git log --oneline fa4a05d..83b3f83` → eine Zeile). `06588c8` (ADR-0018-Retarget) liegt außerhalb
und ist **nicht** geprüft; dass sein Inhalt nicht in den Slice-Commit geraten ist, ist gemessen
(unten). Baum sauber beim Start und beim Abschluss (`git status --porcelain` → leer).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** claude-opus-5[1m] · **Datum:** 2026-08-22

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne diese Liste ist der Lauf nicht
reproduzierbar):

- **Diff:** `fa4a05d..83b3f83`, dazu `git show 83b3f83` im Volltext
- **Slice-Plan:**
  [slice-089](../plan/planning/in-progress/slice-089-carveout-co-002-ueberfuehren.md) (Stand
  `in-progress/`, vollständig gelesen)
- **Die ADR, deren Folgepflichten der Slice vollzieht:**
  [ADR-0021](../plan/adr/0021-verbrauchs-achse-je-rolle-ohne-quelle.md) (**Accepted**,
  Festlegung 5 und die Folgepflichten 1, 2, 5 im Original; dazu Festlegung 2 und 3 und der
  Abschnitt *Was diese ADR NICHT entscheidet*). Mitgelesen:
  [ADR-0011](../plan/adr/0011-telemetrie-erfassung-policy.md) Festlegung 2/3,
  [ADR-0012](../plan/adr/0012-haupt-kontext-ohne-token-bilanz.md),
  [ADR-0019](../plan/adr/0019-agent-guard-prueft-die-aufrufform.md),
  [ADR-0020](../plan/adr/0020-emittierte-modul-15-regeln.md),
  [ADR-0017](../plan/adr/0017-doku-gate-ausnahme-fuer-ein-eingefrorenes-adr.md) (nur für die
  Grenz-Konstruktion, die ADR-0021 zitiert)
- **`LH-*`:** [LH-QA-01](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
  [LH-QA-02](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §2 (Source Precedence), §3.3, §3.4, §3.6, §3.7,
  §3.8, §4
- **Konventionen:** [`harness/conventions.md`](../../harness/conventions.md) MR-025 (Geltungsbereich
  + Setzung 1 und 2), MR-001 (Kennungs-Link-Pflicht), MR-016
- **Vorherige Findings am gleichen Modul:**
  [Plan-Runde 1](2026-08-22-slice-089-plan-review.md),
  [Plan-Runde 2](2026-08-22-slice-089-plan-review-runde-2.md) sowie die vier ADR-0021-Runden
  ([1](2026-08-22-adr-0021-bestaetigungsrunde.md),
  [2](2026-08-22-adr-0021-bestaetigungsrunde-runde-2.md),
  [3](2026-08-22-adr-0021-verdikt-runde.md), [4](2026-08-22-adr-0021-schlussrunde.md))
- **Weitere Verträge:** [`CO-002`](../plan/carveouts/CO-002-token-achse-je-rolle.md),
  [`docs/plan/carveouts/README.md`](../plan/carveouts/README.md),
  [`spec/spezifikation.md`](../../spec/spezifikation.md) §5,
  `.claude/hooks/pretooluse-agent-guard.sh`, `internal/span/response.go` und
  `internal/span/response_test.go` (gelesen, nicht geändert), `harness/tools/mutate.sh`
  (Kopf-Vertrag der Fälle), das vendored `carveout.template.md` und
  `carveouts/README.template.md`
- **Regelwerk `v3.5.2`:** `README.md` (Index) und `modul-10-review-harness.md` on-demand

---

## Selbst gefahren (nichts aus Commit-Message oder Übergabe übernommen)

Jede Zahl steht neben dem Kommando, das **genau sie** liefert
(MR-025 Setzung 1). Wandernde Zahlen sind als solche gekennzeichnet (Setzung 2).

| Kommando | Ergebnis | Behauptung | Urteil |
|---|---|---|---|
| `git log --oneline fa4a05d..83b3f83` | **eine** Zeile (`83b3f83`) | Plan §3 / DoD (1): „**ein** Commit" | **stimmt** |
| `git show 83b3f83 --find-renames=30% --name-status` | `M M M M` + `A test/mutations/151-…`, **kein** `R` | DoD (1): „**kein `git mv`**" | **stimmt** |
| `git show 83b3f83 --name-only --pretty=format: \| grep -c 'carveouts/done'` | **0** | Plan §3: „ein `carveouts/done/` entsteht nicht" | **stimmt** |
| `git diff --stat fa4a05d..83b3f83 -- docs/plan/adr/` | leer | §3.4 / Datei-Tabelle: ADR-Baum unberührt; kein Inhalt aus `06588c8` | **stimmt** — der ADR-0018-Retarget ist nicht mitgelaufen |
| `grep -n 'zu verschieben' docs/plan/carveouts/CO-002-token-achse-je-rolle.md` | leer, **Exit 1**; Vorzustand `git show fa4a05d:… \| grep -c` → **1** | DoD (1) Rot-Kommando 1 | **stimmt**, und der Vorzustand ist gegengeprüft |
| `grep -n '^## Verifikation' <dieselbe Datei>` | leer, **Exit 1**; Vorzustand → **1** | DoD (1) Rot-Kommando 2 | **stimmt** |
| `grep -n 'd-check:ignore' <dieselbe Datei>` | leer, **Exit 1**; Vorzustand → **1** | DoD (1) Gegenprobe | **stimmt** |
| `grep -n 'CO-002' spec/spezifikation.md .claude/hooks/pretooluse-agent-guard.sh` | **6** Zeilen in **2** Dateien; `grep -c` je Datei: **5** / **1** | DoD (2): „weiter sechs Zeilen in zwei Dateien (5/1)" | **stimmt** — keine Adresse bewegt |
| `grep -n 'ADR-0' spec/spezifikation.md` | genau **1** Treffer, Zeile **724**, in §7 Historie | Plan §3: keine bare Kennung, kein Abwärts-Link | **stimmt** — §7 Historie ist in `.d-check.yml` `matrix.exclude-sections` |
| `make docs-check` | `343 Datei(en) geprüft, 0 Befund(e)`, Exit 0 | — | grün. **Die Datei-Zahl wandert** mit dem Markdown-Bestand und ist kein Erwartungswert (MR-025 Setzung 2) |
| `make comment-claims` | `40 Datei(en) geprueft, 0 Befund(e)`, Exit 0 | — | grün; **die Datei-Zahl wandert** (derselbe Vorbehalt) |
| `grep -c 'path: \[\]string' internal/span/response.go` | **9** | Plan DoD (3) und ADR-0021 §Fitness Function | **stimmt** |
| `git diff HEAD -- internal/span/` | leer | Vorfall (c): der versehentlich mutierte Arbeitsbaum ist zurückgesetzt | **stimmt** — und im Commit-Diff ist `internal/span/` gar nicht enthalten |
| Wegwerf-Kopie **außerhalb** des Repos, `sed`-Zeile aus Fall 151 angewendet | `path: []string` **9 → 8**; `diff` zeigt **genau eine** entfernte Zeile (`response.go:69`, `cache_creation_input_tokens`) | Fall 151: „`cache_creation_input_tokens` faellt aus `responseKeys()`, die uebrigen acht bleiben stehen" | **stimmt** — die `sed`-Adresse trifft genau einen Eintrag |
| `grep -rn 'CacheCreationInputTokens\|cache_creation_input_tokens' --include='*.go' .` | **6** Zeilen in **2** Dateien; außerhalb von `response.go` nur `response_test.go:80` (Fixture), `:108` (`mustContain`), `:345` (`mustNotContain`) | Fall 151: „ROT WIRD GENAU EINER" | **stimmt** — `:108` bricht, `:345` prüft Abwesenheit und bleibt grün |
| Lesung `internal/span/response_test.go:181` | Gegenprobe prüft `SpawnedRole`, `TotalTokens`, `InputTokens`, `ModelVersion`; ihre Payload führt **keinen** Cache-Zähler | Fall 151: „ein Eintrag aus dieser Ecke laesst ihn gruen" | **stimmt** — an der Gegenprobe gelesen, nicht gezählt |
| Verbatim-Probe: Kopf-Kommentar `sed -n '2,19p' \| sed 's/^# \{0,1\}//' \| tr '\n' ' ' \| tr -s ' '`, dann `grep -qF <<<` gegen die Tabellenzelle | **trägt** (Exit 0), ebenso das Absatz-Label *„DIE BETRIEBSART PRUEFT ER NICHT"* | Punkt (b) der Übergabe | **stimmt** — mechanisch nachgeprüft, nicht geglaubt |
| Verbatim-Proben der zwei ADR-0021-Zitate aus §Auflösungs-Trigger (`tr '\n' ' '` + `grep -qF`) | beide **tragen** | Plan §3: „die ADR zitiert ihn zweimal verbatim" | **stimmt** — die Streichung hat keine Zitate zweier eingefrorener ADRs quellenlos gemacht |
| `grep -n 'carveouts/done\|Verifikation (nach Auflösung)' docs/plan/adr/0019-*.md docs/plan/adr/0020-*.md` | leer, Exit 1 | — | die gestrichene Checkliste wird von **keiner** eingefrorenen ADR zitiert |
| `make mutate` (Lauf gestartet 17:33, beendet **17:48:38**) | `mutate: 144 ok, 0 Befund(e)`; darin `mutate: ok 151-span-positivliste-eintrag-entfernt -> TestNoResponseFreetextReachesSpan rot` | DoD (3) | **stimmt** — Fall 151 färbt den benannten Wächter rot, und zwar in einem Lauf, den dieser Report selbst zu Ende gesehen hat |
| `git log --oneline -- spec/spezifikation.md \| wc -l` → **17**, davon `--since=2026-08-03` → **8**; Historie-Zeilen `awk '/^## 7\. Historie/,0' \| grep -c '^\| 2026-'` → **2** | s. links | — | die §7-Historie der Spec wird ohnehin nicht pro Änderung fortgeschrieben; das Fehlen einer Zeile ist **kein** Defekt dieses Slice |

**Isolations-Hinweis zu `make mutate`.** Beim ersten Versuch brach der eigene Lauf mit
`ABBRUCH — ein Lauf ist bereits aktiv (.harness/state/mutate.lock)` ab. Ursache war **kein**
stale Lock: `ps` zeigte `bash harness/tools/mutate.sh` (PID 2637504, START 17:33) als laufenden
Prozess. Der Lock ist **nicht** entfernt worden; stattdessen ist dieser Lauf bis zu seiner
Summenzeile beobachtet worden. Der Arbeitsbaum ist dabei unberührt geblieben
(`git status --porcelain` leer, `git diff HEAD -- internal/span/` leer). Alle eigenen Mutations-Proben
liefen in einer Wegwerf-Kopie **außerhalb** des Repos.

---

## Findings

Jedes Finding folgt dem **§Output-Schema des Reviewer-Skills** — der verbindlichen Single Source
of Truth. Die Felder unten sind nur **gespiegelt**, nicht neu definiert; bei Abweichung gilt der
Skill bzw. dessen Quelle
[Kurs Modul 10 §Output-Schema](https://github.com/pt9912/ai-harness-course/blob/v3.5.2/kurs/de/04-qualitaet/modul-10-review-harness.md#worked-example-eine-reviewer-skill-datei-schreiben).

### F-1 — Der Carveout-Index schreibt als Eigenschaft der **Gattung**, was ADR-0021 ausdrücklich nur für diesen einen Fall entscheidet

- `kategorie`: **MEDIUM**
- `quelle`: ADR-0021 §Entscheidung, vierter Posten
  (*„Festlegung 5 gilt für diesen Carveout, nicht für die Gattung … Der nächste Fall wird einzeln
  entschieden, mit seiner eigenen Messung."*) · dieselbe ADR, Folgepflicht 1 (die den Abschnitt
  anordnet) und Folgepflicht 7
- `pfad`: `docs/plan/carveouts/README.md:18-23`
- `befund`: Der neue Abschnitt definiert zuerst seine **Mitgliedschaft** (*„Nicht aufgelöst,
  sondern entschieden: der Trigger tritt nicht ein, und die Einordnung steht in einer ADR"*) und
  stellt darunter eine Eigenschaft **aller** Mitglieder fest: *„Diese Carveouts bleiben an ihrer
  Adresse liegen."* Genau diese intensionale Form hat ADR-0021 für sich verweigert und mit
  ADR-0017 begründet (*„eine solche Regel autorisierte den nächsten Fall im Voraus"*); die
  Ortsfestigkeit ruht dort auf einer fallbezogenen Messung (18 eingehende Verweise in zwei nach
  §3.4 eingefrorenen ADRs), nicht auf einer Klassen-Regel.
- `verifizierbar`: **nein** — kein Modul von `.d-check.yml` liest Prosa; `make docs-check` ist über
  genau diesem Stand grün (343/0, Exit 0). Belegt wird der Befund durch Nebeneinanderlegen:
  `sed -n '18,23p' docs/plan/carveouts/README.md` gegen den vierten Posten der ADR-Entscheidung.
  **Versagen:** der nächste Carveout erreicht den permanenten Ausgang; wer ihn schreibt, findet im
  Index die Klassen-Aussage, lässt ihn liegen und fährt die fallbezogene Messung nicht, die
  ADR-0021 für jeden weiteren Fall verlangt — der Preis der Ortsfestigkeit (`target-missing` in
  eingefrorenen Artefakten gegen datei-weite Gate-Senkung) wird dann nie beziffert.

### F-2 — Die Kontroll-Beobachtung steht als Tatsache im Technik-Stratum und im Hook-Kommentar; ihre Belegklasse steht nicht dabei

- `kategorie`: **MEDIUM**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 (*„Richtig: benennen, was wirklich deckt — oder
  dass nichts deckt"*; die Zusage-Liste nennt den **Doc-Kommentar** ausdrücklich) · ADR-0021
  §Die Kontroll-Beobachtung ist prinzipiell nicht belegbar und Annahme (d) · LH-QA-02
- `pfad`: `spec/spezifikation.md:172-173` und `.claude/hooks/pretooluse-agent-guard.sh:13-15`
- `befund`: Beide Stellen schreiben unqualifiziert *„das eingesetzte Eingabeobjekt **wird
  übernommen**"* bzw. *„wird uebernommen"*. Das Mess-Zeitdokument führt dieselbe Aussage
  ausdrücklich als Grenze (*„Die Kontroll-Beobachtung … ist mit den Mitteln dieses Repos
  prinzipiell nicht belegbar"*, §7), und ADR-0021 stellt ihre Entscheidung **nicht** auf sie,
  sondern führt sie als Annahme. Die zwei Nachbarsätze derselben Spec-Sektion tragen für schwächer
  belegte Aussagen jeweils eine ausgeschriebene *Belegklasse* (`:191` *„fremde Doku, im Repo
  NICHT vorliegend"*; `:204` *„die Wirkungslosigkeit ist gemessen"*); die neue Aussage hat
  keine. Weil `spec/spezifikation.md` in der Source Precedence **Rang 2** trägt und die ADR
  **Rang 4**, liest ein späterer Lauf die unqualifizierte Fassung als die höherrangige.
- `verifizierbar`: **nein** — kein Gate prüft Behauptungen (`make comment-claims` prüft die
  **Existenz** eines genannten Sensors und erfasst ohnehin kein Markdown; `make docs-check` prüft
  Links, Anker, Kennungen, Matrix, Codepfade, Spans). **Versagen:** ADR-0021 nennt den teuersten
  Fehler dieser Messung selbst — *„ein Negativ aus der falschen Ursache: ein verworfenes
  `updatedInput` sähe genauso aus wie ein ignoriertes Feld"*. Wer die Spec-Zeile als repo-belegte
  Messung liest, hält die einzige Gegenkraft für erledigt und wiederholt den Kontroll-Lauf nicht,
  auf den die ADR ihre Re-Evaluierung ausdrücklich stützt.

### F-3 — `make mutate` → „144 ok, 0 Befunde" ist gemeldet worden, bevor ein Lauf diese Zeile erzeugt hatte

- `kategorie`: **MEDIUM**
- `quelle`: [`CLAUDE.md`](../../CLAUDE.md) (*„Kein Erfolg ohne echte Gate-Ausgabe"*) ·
  [`AGENTS.md`](../../AGENTS.md) §3.6 · MR-025 Setzung 1
  (*„wer sie schreibt, hat es über dem Baum gefahren, von dem sie spricht"*, dem Wortlaut nach
  auf Markdown begrenzt — die **Klasse** ist dieselbe)
- `pfad`: kein Repo-Artefakt — die Zahl steht in der Übergabe an den Review-Lauf, **nicht** in
  `git show 83b3f83` (dessen Message nennt keine Gate- oder Sensor-Ergebnisse)
- `befund`: Der einzige `make mutate`-Lauf zu diesem Commit startete um **17:33** (Lock-Verzeichnis
  `.harness/state/mutate.lock`, mtime 17:33; `ps`: PID 2637504, START 17:33) und lief bis
  **17:48:38**. Um **17:39** stand sein Protokoll bei **62** Zeilen, letzter Fall `147-…`, und
  `grep '151-span'` war leer; die Übergabe mit der Zahl lag zu diesem Zeitpunkt bereits vor. Ein
  zweites, früher abgeschlossenes Protokoll existiert im geteilten Arbeitsbereich nicht (dort
  liegen für slice-089 sechs `gates-089-*.log` von 16:01–17:02 und genau **ein** Mutations-Log).
  Die **Summenzeile** `mutate: 144 ok, 0 Befund(e)` entstand 13 Minuten nach ihrer Meldung; die
  Kopfzeile desselben Laufs lautet `mutate: 144 Faelle`.
- `verifizierbar`: **ja** — `make mutate`, vollständig zu Ende gefahren; in diesem Lauf getan, mit
  dem Ergebnis oben. **Versagen:** der Verifikations-Lauf hakt den DoD-Punkt *„`make mutate` ohne
  Befund"* auf einer Zahl ab, die kein Lauf ausgegeben hatte; hätten die verbleibenden 117 Fälle
  einen Befund geliefert, wäre über einem roten Sensor grün abgenommen worden. **Zur Sache ist der
  Befund inzwischen erledigt** — die Zahl stimmt, gemessen an einem beendeten Lauf.

### F-4 — Die Geltungs-Konfigurations-Zelle beschreibt den Kopf-Kommentar mit **einem** Review-Dokument; er nennt jetzt zwei

- `kategorie`: **LOW**
- `quelle`: ADR-0021 Folgepflicht 1 (vier Änderungen am Stub, abschließend aufgezählt) ·
  Maintainability (Doku-Drift)
- `pfad`: `docs/plan/carveouts/CO-002-token-achse-je-rolle.md:131`
- `befund`: Die Zelle sagt, der Kopf-Kommentar *„zeigt auf die Messung im Review-Dokument **und**
  auf diesen Carveout"* (Singular). Nach dem Nachzug nennt der Kommentar zwei Mess-Dokumente
  (`2026-08-15-agent-guard-tool-vertrag.md` und `2026-08-21-updatedinput-messung.md`). Das
  eingebettete Verbatim-Zitat derselben Zelle trägt weiter — mechanisch nachgeprüft —, die
  Beschreibung daneben ist unvollständig geworden.
- `verifizierbar`: **nein** — das Prüfkommando der Tabelle selbst
  (`grep -n "CO-002" .claude/hooks/pretooluse-agent-guard.sh spec/spezifikation.md`) misst die
  **Anwesenheit** des Zeigers, nicht die Richtigkeit der Wert-Spalte; es ist grün. **Versagen:**
  wer die Tabelle gegen den Kommentar hält, findet dort zwei Dokumente und hier eines und kann
  nicht unterscheiden, ob eines hinzugekommen oder die Tabelle veraltet ist.

### F-5 — Die „vier der neun" im Mutations-Fall tragen ihre Beleg-Grenze nicht mit

- `kategorie`: **INFO**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 · MR-025 §Geltungsbereich
  (*„die lebenden, repo-eigenen **Markdown**-Artefakte"*) ·
  [slice-089](../plan/planning/in-progress/slice-089-carveout-co-002-ueberfuehren.md) DoD (3)
- `pfad`: `test/mutations/151-span-positivliste-eintrag-entfernt.sh:21-23`
- `befund`: Der Plan trägt zu derselben Zahl den Zusatz *„Die vier sind an der Gegenprobe gelesen,
  nicht gezählt: kein Kommando trennt sie"*; der Kommentar des Falls nennt die vier Feldnamen, den
  Zusatz nicht. MR-025 bindet Shell-Skripte nicht — sein Geltungsbereich ist auf Markdown
  gemessen —, weshalb hier keine Konventions-Verletzung vorliegt; genannt wird, dass die
  Lücken-Kennzeichnung, die zwei Plan-Runden ausdrücklich erarbeitet haben, beim Übergang ins
  Skript nicht mitgewandert ist. Dass die vier namentlich dastehen, macht sie am Testcode
  nachlesbar.
- `verifizierbar`: **nein** — kein Gate; `make comment-claims` erfasst `.claude/hooks/*.sh`, aber
  nicht `test/**` (Prüfbereich = vier Pfad-Muster, [`AGENTS.md`](../../AGENTS.md) §4).

### F-6 — Zweite Ziel-Form-Abweichung ohne Register-Eintrag: der Index bekommt einen dritten Abschnitt, der Stub einen dritten Status-Wert

- `kategorie`: **INFO**
- `quelle`: ADR-0021 Folgepflicht 1 und 7 ·
  [slice-089](../plan/planning/in-progress/slice-089-carveout-co-002-ueberfuehren.md) §6 (letzter
  Risiko-Punkt) · `.harness/baseline/v3.5.2/templates/docs/plan/carveouts/`
- `pfad`: `docs/plan/carveouts/README.md:18` und
  `docs/plan/carveouts/CO-002-token-achse-je-rolle.md:3-4`
- `befund`: Der Plan benennt in §6 **eine** Ziel-Form-Abweichung ohne Register-Eintrag — den
  Wegfall von `## Verifikation (nach Auflösung)`. Gemessen am vendored Template kommen zwei
  weitere hinzu: `README.template.md` führt genau zwei Abschnitte (*Aktive Carveouts*,
  *Aufgelöste Carveouts*), der Index jetzt drei; `carveout.template.md` führt für `Status` genau
  zwei Werte (*Aktiv | Aufgelöst*), der Stub jetzt einen dritten. Beide sind von ADR-0021
  Festlegung 5 bzw. Folgepflicht 1 **angeordnet** und im Regelwerk (`modul-07-carveouts.md`
  §Carveout-Audit-Slice, §Werkzeug-Wahl) gedeckt; die Ablage-Frage, für die Folgepflicht 7 den
  fehlenden Adaptions-Eintrag begründet, ist eine andere. Genannt, damit ein späterer
  Ziel-Form-Abgleich nicht drei Abweichungen findet, wo der Plan eine führt.
- `verifizierbar`: **nein** — `.d-check.yml` führt kein `structure`-Modul
  (`grep -c 'structure' .d-check.yml` → **0**); kein Gate liest Carveout-Struktur.

---

## Die drei zur Prüfung gestellten Punkte — eigene Urteile

### (a) Drei der sechs Zeiger blieben unverändert — als Urteil, nicht per `grep`

**Urteil: die Einschätzung des Implementers trifft zu; die drei Abschnitte sind zu Recht
unangetastet geblieben.** Alle drei sind im Volltext gelesen, nicht gesucht:

- **Wächter-Absatz zu Bedingung 2** (`spec/spezifikation.md:234-246`): durchgehend Indikativ über
  einen Zustand — *„Für Bedingung 2 gibt es keinen Wächter mehr, weil es nichts zu bewachen gibt"*,
  *„die Betriebsart ist kein Gegenstand"*, *„Was damit NICHT zugesagt ist …"*. Keine ausstehende
  Messung, keine offene Frage. Der Absatz bleibt zudem konsistent mit dem geänderten
  Hook-Kommentar (*„ER SETZT DIE BETRIEBSART AUCH NICHT EIN"*).
- **Abweichung 1** (`:365-388`): *„Erfassbar sind … und genau diese Antwort entsteht nicht mehr
  ([`CO-002`]; die Erfassung selbst steht unverändert und nimmt die Zähler, sobald sie wieder
  ankommen)."* Zustand im Indikativ, plus der Zeiger. Die Eingangszeile trennt *„für den
  Haupt-Kontext dauerhaft"* von *„für Subagenten-Läufe, seit der Vordergrund nicht mehr
  anforderbar ist"* — sie behauptet **nicht**, der Ausfall sei temporär, und sie schreibt das
  Verdikt **nicht** aus; das Zweite wäre der zweite Ort, den Festlegung 5 ausschließt. Beides
  spricht dafür, sie stehen zu lassen.
- **Abweichung 5** (`:477-555`): Prüfschritt 2 sagt *„Vermeidbar? Nein — der Weg, der sie vermied,
  ist geschlossen"*, der Schluss-Satz *„Sie entfällt ersatzlos, sobald die `tool_response` eines
  Hintergrund-Laufs Zähler trägt. Die Quelle dafür ist nicht gepinnt und wird von keinem Gate
  geprüft."* Das ist eine **Bedingung**, deckungsgleich mit dem zweiten Re-Evaluierungs-Trigger der
  ADR — keine ausstehende Messung.

**Über den `grep` des Implementers hinaus gesucht** (`updatedInput|nicht gemessen|ungeprüft|noch zu
messen|steht aus|abzuwarten|Weg 1|zweite[rn]? Weg`, über die ganze Datei): die einzigen weiteren
Treffer sind `:469` (Abweichung 3, *„ob ein vom Nutzer direkt abgesetzter Aufruf einen Span
erzeugt"*) und `:569-572` (Abweichung 6, Haupt-Kontext) — **anderer Gegenstand**, ADR-0012, nicht
unter den sechs Zeigern. Sie bleiben zu Recht stehen. Der Hinweis, dass das Rot-Kommando von
DoD (2) diesen Punkt nicht deckt, ist zutreffend: es misst die Über-Löschung, nicht das Ziehen.

### (b) Die Konstruktions-Entscheidung im Stub — „stehen lassen und anhängen"

**Urteil: die Konstruktion ist richtig, und die Tabelle macht keine falsche Aussage über den Hook.**
Mechanisch nachgeprüft (nicht geglaubt): den Kommentar-Block entkommentiert, zeilenweise
zusammengezogen, `grep -qF` gegen die Tabellenzelle → **Exit 0**; dasselbe für das Absatz-Label
*„DIE BETRIEBSART PRUEFT ER NICHT"* → **Exit 0**; Gegenprobe über `git show fa4a05d:` → vorher
ebenso. Der neue Text steht **vor** dem zitierten Satz und hängt an ihn nur an (*„; wie sie
ausgegangen ist, sagt der Status im Kopf jener Datei"*), ohne ihn zu brechen. Die Zeile in der
Spalte *Zeile/Section* trifft weiter: die neue Wendung liegt **innerhalb** desselben Absatzes, es
entsteht kein zweiter. Eine fünfte Stub-Änderung wäre über die vier hinausgegangen, die ADR-0021
Folgepflicht 1 abschließend aufzählt — der Verzicht ist begründet und nicht bequem.
**Der Rest ist F-4 (LOW):** die Wert-Spalte spricht von *einem* Review-Dokument, der Kommentar
nennt jetzt zwei. Das ist eine unvollständige Beschreibung, keine falsche Aussage über den Hook —
deshalb LOW und nicht höher.

### (c) Der gemeldete Vorfall — Mutation im Arbeitsbaum statt in der Wegwerf-Kopie

**Urteil: im Commit-Diff steckt keine Spur davon, und das ist unabhängig gemessen.**
`git show 83b3f83 --name-status` führt fünf Pfade, keiner unter `internal/`;
`git diff --stat fa4a05d..83b3f83 -- internal/` ist leer; `git log --oneline -3 --
internal/span/response.go` endet bei `e46baeb` (slice-076) — der Slice-Commit taucht dort nicht
auf. Der Arbeitsbaum ist sauber (`git status --porcelain` leer, `git stash list` leer,
`grep -c 'path: \[\]string' internal/span/response.go` → **9**). **Und die Rückstellung ist
zusätzlich dynamisch belegt:** der `make mutate`-Lauf, den dieser Report zu Ende gesehen hat,
prüft nach Bedingung 5 die Mutations-Zieldateien im **Host**-Baum vor, während und nach jedem
Fall; ein abweichender Stand hätte ihn mit Befund und Abbruch beendet. Er endete mit
`144 ok, 0 Befund(e)`. Der Vorfall ist damit folgenlos für den Gegenstand dieses Reviews — als
Prozess-Beobachtung bleibt er beim Verifier bzw. in der Closure-Notiz.

---

## Negativbefunde

- **geprüft, ohne Befund — Zuschnitt des Commits.** Ein Commit, fünf Dateien, kein `git mv`, kein
  `carveouts/done/`, kein Pfad unter `docs/plan/adr/`; §3.3 greift nicht (es gibt nur den Rewrite),
  §3.4 ist eingehalten, §3.8 ist nicht berührt (weder `AGENTS.md` noch `harness/conventions.md`
  liegen im Diff).
- **geprüft, ohne Befund — DoD (1) am Text, nicht am Kommando.** Die vier angeordneten Änderungen
  liegen vor und **nur** sie: Status im Kopf, `Letzte Prüfung` **2026-08-22** samt Geschichte-Zeile,
  Handlungs-Anweisung raus und Vorspann-Satz rein, `## Verifikation (nach Auflösung)` als Ganzes
  weg. Der Status-Wortlaut deckt sich mit Festlegung 5 (*„Permanent — übergeführt in ADR-0021"*,
  die Kennung als Link wegen MR-001). Der Abschnitt `## Auflösungs-Trigger` **bleibt**, und die
  zwei verbatim von ADR-0021 zitierten Sätze tragen weiter (Verbatim-Probe oben).
- **geprüft, ohne Befund — der Index-Abschnitt ist der von der ADR verlangte.** Der Eintrag steht
  in einem **eigenen** Abschnitt *„Permanent — in eine ADR übergeführt"* zwischen *Aktiv* und
  *Aufgelöst*, **nicht** unter *Aufgelöst*; *Aufgelöst* führt weiter *(noch keine)*. §Aktiv trägt
  jetzt nur noch CO-001, dessen Ausgang offen ist — genau die Trennung, die Festlegung 5 verlangt.
  (Der über die Anordnung hinausgehende Klassen-Satz desselben Absatzes ist F-1.)
- **geprüft, ohne Befund — DoD (2) hält die zwei versperrten Formen ein.** Sechs Zeilen in zwei
  Dateien, unverändert; kein Link vom Spec-Stratum auf die ADR (`matrix-forbidden`), keine bare
  `ADR-`-Kennung (`id-unlinked`) — `grep -n 'ADR-0' spec/spezifikation.md` liefert genau einen
  Treffer, und der steht in §7 Historie, einem der von `matrix.exclude-sections` ausgenommenen
  Abschnitte. `make docs-check` grün.
- **geprüft, ohne Befund — die Grenze aus ADR-0021 Festlegung 3 ist gehalten.** Die neue
  Spec-Aussage ruht auf dem **Hintergrund-Start**, den ADR-0021 als *„zweifach beobachtet"* (sofortige
  Rückkehr des Werkzeugs, Hintergrund-Meldung der Sitzung) und ausdrücklich **span-unabhängig**
  führt; die span-abhängige Aussage — *„auch die Zähler nicht"* — ist **nicht** in die Spec
  gewandert. Ein Span trägt hier also keine Zusage in einem Norm-Artefakt. (Was fehlt, ist die
  Belegklasse der Kontroll-Beobachtung — F-2, ein anderer Gegenstand.)
- **geprüft, ohne Befund — DoD (2) ist ein Ziehen, keine Über-Löschung.** Gefallen sind genau die
  vier Sätze, die eine Messung als ausstehend führten (*„Ein zweiter Weg ist benannt und
  ungemessen"*, *„hat niemand geprüft"*, *„ist damit nicht mitentschieden"*, *„Die Messung und ihre
  zwei Ausgänge führt derselbe Carveout"*) sowie ein Konjunktiv (*„könnte"* → *„kann"*). Erhalten
  sind alle fünf `CO-002`-Zeiger, der Verweis auf **Abweichung 5** und die
  Belegklassen-Konstruktion der START-KONVENTION.
- **geprüft, ohne Befund — §3.7 am neuen Hook-Absatz.** *„ER SETZT DIE BETRIEBSART AUCH NICHT EIN"*
  ist eine **Abgrenzung** plus **Rang-Zeiger**, durchgehend Indikativ, ohne Konjunktiv über eine
  verworfene Alternative und ohne einen Satz über abwesenden Text (kein *„die frühere Fassung …"*).
  Er schreibt an den, der die Stelle ändert — genau an den, der versucht wäre, `updatedInput`
  wieder einzubauen; ADR-0021 Festlegung 4 hat dieselbe Gegenrichtung entschieden.
  `make comment-claims` grün.
- **geprüft, ohne Befund — DoD (3) Kopf-Vertrag und Sensor-Wahl.** `# files:` und `# expect:` sind
  je genau einmal vorhanden und einzeilig (`run_case` meldet Doppel-Köpfe als Befund); `# expect:
  TestNoResponseFreetextReachesSpan` fällt in `narrow_sensor` unter `Test[A-Z]*` und wählt
  `test-go` mit dem Fehlschlag-Muster `--- FAIL:`. Die `sed`-Adresse ist eine BRE mit korrekt
  escapten Klammern und trifft **genau einen** Eintrag (gemessen). Der Fall ist ausführbar
  (`100755`), `set -euo pipefail` steht davor.
- **geprüft, ohne Befund — die Begründung der Cache-Zähler-Wahl trägt.**
  `TestOnlyAgentToolGetsResponseValues` prüft in seiner Gegenprobe vier Werte, und seine Payload
  führt gar keinen Cache-Zähler; er bleibt grün. `TestFailedAgentCallCapturesNothing` prüft die
  **Abwesenheit** und bleibt ebenfalls grün. Rot wird genau `TestNoResponseFreetextReachesSpan`
  (`response_test.go:108`). Nach der Mutation wird `intoCacheCreation` zu einer ungenutzten
  Paket-Funktion — für den Go-Compiler und das `go test`-eigene `vet`-Subset folgenlos.
- **geprüft, ohne Befund — LH-QA-01.** Kein neuer Gate-Name, kein neues Make-Target, kein Eintrag
  in einer der zwei Gate-Tabellen; der Fall läuft in `make mutate` (Nicht-Gate-Verify) und
  behauptet nichts über eine leere Prüffläche.
- **geprüft, ohne Befund — MR-025 über die drei berührten Markdown-Dateien.** Der Diff schreibt in
  `spec/spezifikation.md`, `CO-002…md` und `carveouts/README.md` **keine** neue Zahl in
  Messwert-Rolle; die neuen Ziffern sind Daten (2026-08-21 / 2026-08-22). Die zwei Nicht-Markdown-
  Dateien liegen außerhalb des gemessenen Geltungsbereichs von MR-025 (F-5 nennt die Folge).
- **geprüft, ohne Befund — kein zweiter Ort für das Verdikt.** Weder der Vorspann-Satz noch die
  Geschichte-Zeile noch der Index-Abschnitt schreiben die **Begründung** nach: der Vorspann zeigt
  auf den Kopf, der Kopf auf die ADR, die Geschichte-Zeile nennt das Ereignis und verweist auf
  Festlegung 5. Der Trichter, die 18 `target-missing` und die Regelwerks-Lesung sind nirgends
  zweitgeschrieben.
- **geprüft, ohne Befund — die vorherigen Findings am gleichen Modul.** Der Rest aus Plan-Runde 2
  G-3 (Einleitungssatz von DoD (1)) ist im `in-progress/`-Stand behoben (*„Die ersten zwei decken
  je die Änderung, neben der sie stehen; das dritte ist eine Gegenprobe und deckt seine nicht"*).
  G-1/G-2/G-6 betreffen den Wellen-Plan und sind in diesem Diff nicht berührt — er fasst
  `welle-09` und die Roadmap nicht an, wie die Datei-Tabelle es anordnet.
- **geprüft, ohne Befund — Fremd-Commit-Abgrenzung.** `06588c8` berührt genau
  `docs/plan/adr/0018-…md` und `docs/plan/adr/README.md`; keine dieser Dateien liegt im
  Slice-Commit.
- **geprüft, ohne Befund — spec §7 Historie.** Keine neue Zeile; die Konvention wird in diesem Repo
  ohnehin nicht pro Änderung fortgeschrieben (17 Commits auf die Datei, 8 seit dem letzten
  Historie-Eintrag, 2 Zeilen in §7). Kein Defekt dieses Slice.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 3 |
| LOW | 1 |
| INFO | 2 |

## Verdikt

**Merge-blockierend: ja — wegen F-1 und F-2.** Beide sind Textstellen, kein Gate sieht sie, und
beide schreiben mehr, als ihre Quelle deckt: F-1 macht aus einer fallbezogenen Entscheidung eine
Klassen-Regel, die ADR-0021 namentlich verweigert hat; F-2 nimmt einer Beobachtung die Grenze, die
ihr Mess-Dokument und die ADR ihr ausdrücklich mitgeben, und legt sie in dem Stratum ab, das in der
Source Precedence über der ADR steht.

**F-3 blockiert nicht, und das ist die begründete Abweichung.** Der Sensor ist inzwischen zu Ende
gefahren und grün (`mutate: 144 ok, 0 Befund(e)`, Fall 151 rot); es steht nichts Rotes zum Merge
an. Was bleibt, ist die Klasse — eine Zahl, gemeldet vor dem Lauf, der sie ausgibt —, und die
gehört in die Closure-Notiz und vor den nächsten Sensor-Bericht, nicht in eine Merge-Sperre.

**Die Sache selbst ist nicht angegriffen.** Der Vollzug der Folgepflichten 1, 2 und 5 aus ADR-0021
ist an seinem Gegenstand vollständig: der Stub trägt den Verdikt-Status an seiner alten Adresse,
die zwei Ort-Anweisungen sind aufgehoben, die sechs Zeiger stehen und führen keine Messung mehr als
ausstehend, und der fällige Mutations-Fall existiert und ist unter seiner eigenen Mutation rot
gesehen — in einem Lauf, den dieser Report selbst beobachtet hat. Beanstandet sind zwei Sätze, mit
denen der Vollzug beschrieben wird, nicht der Vollzug.

**Übergabe:** Findings gehen an die Implementation (Rückkante Review → Plan bei Plan-Defekt; hier
liegt kein Plan-Defekt vor — der Plan ordnet weder den Klassen-Satz noch die unqualifizierte
Beleg-Aussage an). Der Report ersetzt keine Verifikation — DoD-/Spec-Konformität prüft der Verifier
separat (Modul 11; anderes Prüf-Artefakt, anderer Eingabe-Kontext). **Für den Verifier ausdrücklich
vermerkt:** F-3 bedeutet, dass die `make mutate`-Zahl aus der Übergabe nicht als Beleg taugt; der
in diesem Report protokollierte Lauf (Ende 17:48:38, `144 ok, 0 Befund(e)`) ist der erste, der sie
trägt.
