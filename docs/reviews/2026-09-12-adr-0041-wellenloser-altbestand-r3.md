# Review-Report — ADR-0041 (wellenloser Altbestand), Runde 3

**Datum:** 2026-09-12 · **Rolle:** Reviewer, frischer Kontext (ADR-0040 Festlegung 2) · **Baseline:** v6.5.0
**Gegenstand:** `a09c1c2f` (ADR, 3 Zeilen) und `06d64897` (`docs/plan/planning/done/slice-183-ausloeser-der-wellenlosen-archivierung.md`, 56+/14−)
**Eingang:** Diff · LH-QA-01, LH-QA-02 · ADR-0016/0030/0033/0040 (gemessen alle `**Status:** Accepted`) · AGENTS.md §3 · Slice-Plan · Vorrunden r1/r2 in `docs/reviews/`

## Findings

- **LOW** · Maintainability · `docs/plan/adr/0041-wellenloser-altbestand-geht-in-ein-sammel-archiv.md:325` · Die B-Zelle sagt zur Betriebsart *„sie hebt **drei** benannte Ausgänge auf"*, Folgepflicht 1 (`:356`) sagt zu derselben Handlung *„vier also"*. Beide Zahlen sind verankert (drei beobachtete Sperren vs. vier Code-Ausgänge) und in §Kontext (6) `:198` ausdrücklich versöhnt — aber dasselbe Verb trägt in einer ab `Accepted` einfrierenden Datei zwei Zahlen. *verifizierbar:* nein (kein Modul liest Zahlen-Konsistenz). *klasse:* zwei Zählweisen unter einem Verb im einfrierenden Artefakt.
- **INFO** · AGENTS.md §3.7 · `docs/plan/planning/done/slice-183-ausloeser-der-wellenlosen-archivierung.md:363` · Die Chronik-Spur *„die vorige Fassung dieses Abschnitts trug zwei überholte Werte"* bleibt zu Recht stehen — aber **nicht** aus dem genannten Grund. Zwei Messungen: sie wurde von `06d64897` gar nicht berührt (`git blame` → `b23dcef45`; `git show 06d64897 -- <plan> | grep -c 'vorige Fassung dieses Abschnitts'` → **0**; der Commit fasste in §8 den *anderen* Vorgelagert-Block an, `:350`–`:355`), und §3.7 reicht ohnehin nicht hin: ihr Geltungsbereich ist *„Code, Konfiguration, Skripte und die Zustandsfelder der lebenden Register"*, eine Prosa-Klammer in §8 eines Slice-Plans ist keines von beiden. Tragend ist der Geltungsbereich, nicht der Cutoff. *verifizierbar:* nein. *klasse:* richtiger Ausgang auf nicht tragender Begründung.
- **INFO** · Prüfauftrag Punkt 6 · Die Prämisse *„Runde 2 hat byte-gleich gegen `5969a86b` bestätigt"* ist zu eng gelesen. Gemessen sind die Festlegungs-Blöcke gegen `5969a86b` **nicht** byte-gleich (3 Hunks, +751 Bytes, Zitat-Ergänzungen in Festlegung 2, 3 und 5) — sie stammen aus `379def11`, dem Runde-1-Fix, und r2 qualifiziert das selbst (*„nur Zitat-Ergänzungen, kein normativer Satz bewegt"*). Die Frage, die zählt, ist gegen `379def11` gestellt und beantwortet: `diff` leer, Exit 0. `a09c1c2f` hat die Festlegungen nicht angefasst. *verifizierbar:* ja, `diff <(git show 379def11:<adr> | awk '…') <(git show HEAD:<adr> | awk '…')`.

## Negativbefunde

- **Prüfpunkt 1 — MEDIUM-2 aufgelöst, nicht gespiegelt.** `internal/archive/collect.go` kennt für `b.Wellenlose` **keinen** Filter: `Slices()` hängt Mitglieder und Wellenlose unbedingt aneinander, und `einordnen` entscheidet allein über `istWellenlos(feld)` — die Begründung fällt nicht, sie trägt. Die zweite Hälfte ist am Träger gemessen: `--vorschau welle-01` zeigt **genau** `[untergrenze]` und `[haenger]`, Exit 3 — von den vier Ausgängen der Folgepflicht 1 (`ergebnisnotiz`, `kein-plan`, `mehrdeutiger-plan`, `untergrenze`; `grep -c 'Kennung: "' internal/archive/vorschau.go` → **8** insgesamt) berührt E allein `untergrenze`. Die in `a09c1c2f` ergänzte Zeile `[untergrenze] [haenger]` macht den Beleg-Block erstmals tragfähig für die Zelle, die auf ihn zeigt.
- **Prüfpunkt 2 — LOW-1 aufgelöst.** Das Kommando im Block gibt heute `153 von 349` aus, wörtlich wie notiert; der Vermerk *„kein Erwartungswert"* steht jetzt da (MR-025 Setzung 2).
- **Prüfpunkt 3 — Fundmenge und Zitate.** Eigener Sweep über **jede** Zeile des Plans, die `Trigger`, `gefeuert`/`feuer` oder `ADR-0033` führt: keine überlebende Fassung der falschen Prämisse; `:19`, `:71` und `:174` lesen übereinstimmend *„keiner ihrer fünf"*. Alle vier neu eingesetzten Zitate sind verbatim (je 1 Treffer nach Whitespace-Normalisierung): Trigger-1- und Trigger-2-Wortlaut samt Nachsatz aus ADR-0033, *„deshalb gehört die Operation in ein Werkzeug und nicht in Handarbeit"* und die Zuordnungs-Delegation aus Schritt 4 der vendored `modul-06-roadmap.md`. Die drei Mess-Kommandos reproduzieren: **5** Trigger, `a6c5f22c`, **1**.
- **Prüfpunkt 4 — DoD 2 ist erfüllbar.** Alle fünf Aussagen haben ihr Gegenstück in §Entscheidung / *Was diese Entscheidung nicht tut* (`:269`–`:295`): kein `Supersedes`, Festlegung 1 unberührt, keiner der fünf Trigger gefeuert, die zwei nahen einzeln gemessen, die übrigen drei mit Grund (Trigger 3–5 stimmen wörtlich überein). Der Block liegt innerhalb `## Entscheidung`; `.d-check.yml` nimmt unter `vcs.exclude-sections` allein `Geschichte` aus — er steht nach `Accepted` im geschützten Kern, DoD 2 misst also gegen eingefrorenen Text.
- **Prüfpunkt 5 — §3.4-Tauglichkeit.** Jede Zahl steht neben ihrem Kommando; nachgerechnet: **48** adressierte über **10** Wellen und **9** ohne Adressat reproduzieren exakt samt Verteilung, **91** = 148 − 57, **8** Kennungen, `--vorschau altbestand` liefert 0/57/91/144 und vier Sperren bei Exit 3. Die Mengen-Aussage *„Dieselben 57 stehen in der Vorschau jeder anderen Welle"* ist nicht bloß bestichprobt, sondern strukturell gedeckt: **keines** der 57 `**Welle:** ohne Welle`-Felder nennt eine `welle-NN`, `nenntWelle` kann `istWellenlos` also nie vorwegnehmen (Gegenprobe an `welle-02`, `welle-08`, `welle-15`: je 57).
- **§3.11 / Adressen.** Die ADR nennt kein Artefakt des Planning-Lifecycle per Pfad; alle Ziele sind ortsfest (ADR-Geschwister, das Verzeichnis `docs/plan/planning/done`, Beobachtungs-Verzeichnisse, ein Glob über `docs/reviews/` im Kommando).
- **§3.8 / §3.10 / Commit-Zuschnitt.** Jeder der zwei Commits berührt genau **eine** Datei seiner eigenen schreibenden Rolle und nennt sie in der Message; die ADR ist vom Planner-Lauf nicht angefasst, `slice-183` liegt unverändert in `in-progress/`, keine Closure-Artefakte im Diff.
- **Halluziniertes Gate (LH-QA-01).** §Fitness Function behauptet keinen Wächter: `grep -n '^modules:' .d-check.yml` bestätigt die dort genannte Modul-Liste, und *„Geschuldet, nicht geliefert"* steht an jeder ungedeckten Zeile.

## Kategorie-Summary

0 HIGH · 0 MEDIUM · 1 LOW · 2 INFO. Wiederkehrende Klasse über die drei Runden: **Beleg-Block trägt die Aussage nicht, die auf ihn zeigt** (r1 HIGH-1 Baseline-Zitate, r2 MEDIUM-2 Sperren-Liste) — in dieser Runde nicht mehr aufgetreten.

## Verdikt

**Kein blockierender Befund.** Der eine LOW und die zwei INFO blockieren den Merge nicht: Die LOW-Diskrepanz ist in der Datei selbst aufgelöst, und beide INFO stellen Begründungen richtig, ohne ein Ergebnis zu ändern.

**Dies ist der Beleg, den der Acceptance-Trigger von ADR-0041 verlangt** (*„wenn eine Reviewer-Runde sie gegen ADR-0033, ADR-0030 und die zitierte Stelle der adoptierten Baseline `v6.5.0` auf Konsistenz geprüft hat und ihr Report ohne blockierenden Befund in `docs/reviews/` liegt"*) — geprüft wurde gegen alle drei, in frischem Kontext und damit als eigenständige Runde nach ADR-0040 Festlegung 2, nicht als Nachmessung des auflösenden Laufs. Die Accept-Zeile der §Geschichte nennt diesen Report als Kennung.
