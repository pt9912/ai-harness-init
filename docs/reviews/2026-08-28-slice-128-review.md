# Code-Review slice-128 — der Kopf von `d-check.mk` sagt, was gilt

**Rolle:** Reviewer (Modul 10) · **Datum:** 2026-08-28 · **Gegenstand:** HEAD `1edbae4`,
Arbeitsbaum sauber · **Runde:** 1

## Kopf-Metadaten

| Feld | Wert |
|---|---|
| Commit-Range | `b3d6dcc..1edbae4` — `08cdaae` (`open`→`next`), `29e9aed` (`next`→`in-progress`), `05964ad` (Link-Abgleich), `1edbae4` (Kopf, eine Datei, 27+/9−) |
| Umsetzungs-Diff | `d-check.mk` Zeilen 1–49, ausschließlich der Kommentarblock |
| Slice-Plan | `docs/plan/planning/in-progress/slice-128-d-check-kopf-sagt-was-gilt.md` |
| Betroffene Anforderungen | `LH-QA-01`, `LH-QA-02` |
| Referenzierte Adaptions-Einträge | `MR-010`, `MR-011`, `MR-012`, `MR-013`, `MR-016`, `MR-024`, `MR-025`, `MR-027` |
| Geprüfte Hard Rules | 3.3, 3.5, 3.6, 3.7, 3.8, 3.9 |
| Vorherige Findings am gleichen Modul | `docs/reviews/2026-08-28-slice-122-review.md` (LOW-1, INFO-2 — die zwei Posten, aus denen dieser Slice geschnitten ist), `docs/reviews/2026-08-28-slice-122-verify.md` |
| Nicht geprüft (fremde Rolle) | DoD-Abhakung, Gate-Lauf-Bestätigung — Modul 11 |
| Nicht gefahren (Auflage) | `make mutate` — frisch grün über `1edbae4`, 198 ok / 0 Befund(e) |

**Nachbar-Kontext.** Der Slice ist der Rest von slice-122 und hat dessen zwei offene Posten
geerbt (Rang-Zeiger, Kopf-Aussagen). Die dritte Zahl-Stelle hat der Planner selbst
nachgeschnitten. Der Kopf ist damit dreimal in Folge Prüfgegenstand — die Muster-Frage dieses
Reports ist deshalb nicht *„stimmen die Zahlen"*, sondern *„trägt, was an ihre Stelle tritt"*.

**Die zentrale Frage vorab beantwortet.** Der Slice hat die drei Zahlen nicht korrigiert,
sondern gestrichen und durch das Kriterium *„`make docs-check` fährt den gepinnten v0.65.0 und
ist grün, und jeder `make gates`-Lauf prüft es neu"* ersetzt. **Das trägt** — Begründung in
Negativbefund N-1. Die Findings unten liegen daneben, nicht darin.

## Findings

### HIGH-1 — Der neue Kopf beschreibt den Text, der bis zu diesem Commit an seiner Stelle stand, und nennt den Slice, der ihn entfernt hat

- **kategorie:** HIGH
- **quelle:** `AGENTS.md` §3.7 (Hard Rule — *„Ein Kommentar beschreibt, was da ist"*)
- **pfad:** `d-check.mk:31-33`
- **befund:** Die drei neuen Zeilen lauten *„Hier steht dazu bewusst KEINE Zahl. Die
  Marker-Menge waechst mit dem Bestand; eine eingefrorene Zaehlung an dieser Stelle war schon am
  Tag ihrer Niederschrift falsch — sie war es (slice-128 DoD (2), MR-025 Setzung 2)."* Der erste
  Satz ist eine Abgrenzung und beschreibt den Ist-Zustand. Der Nachsatz *„— sie war es"* mit dem
  Verweis *„(slice-128 DoD (2))"* tut beides, was §3.7 als Falsch-Klasse führt: er spricht über
  **abwesenden** Text (*„die frühere Fassung prüfte nur die Länge" — beschreibt abwesenden Text;
  „die vorige hält `git`"*) und er trägt die **Entstehung** des eigenen Textes in die Datei
  (§3.7: *„die Historie in `git`"*). Der Verweis zeigt zudem auf ein Dokument, das mit dem
  Closure nach `docs/plan/planning/done/` wandert — die Kategorie, die `MR-025`
  §Geltungsbereich ausdrücklich als **Zeitdokument** führt, das nicht nachgezogen wird. Der
  Kopf begründet damit seine geltende Abgrenzung aus einer Quelle, die kein Lauf wieder
  aufschlägt.
- **verifizierbar:** nein — kein Gate liest diese Datei. Gemessen:
  `git ls-files 'internal/*.go' 'internal/**/*.go' 'cmd/**/*.go' 'harness/tools/*.sh' '.claude/hooks/*.sh' | grep -c 'd-check.mk'` → **0**
  (`make comment-claims` → `46 Datei(en) geprueft, 0 Befund(e)`); `make docs-check` und
  `make doc-targets` melden beide `435 Datei(en) geprüft, 0 Befund(e)` über Markdown, nicht über
  `.mk`. Nachweisbar ist der Befund allein am Text.
- **Warum HIGH.** §3.7 ist eine Hard Rule, ihr Cutoff bindet *„den Kommentar, der geschrieben
  oder geändert wird"*, und diese drei Zeilen sind in diesem Commit neu geschrieben. Der
  Geltungsbereich der Regel — *„Code, Konfiguration und Skripte, die dieses Repo besitzt"* —
  trifft den Adopter-Kopf: `MR-010` Setzung 1 weist ihn als Handgriff des Repos aus, nicht als
  Tool-Ausgabe.

### MEDIUM-1 — „Dass die Menge überhaupt trägt" wird mit einem Beleg geführt, der ohne Entwertung genauso zutrifft

- **kategorie:** MEDIUM
- **quelle:** `MR-025` Setzung 1 · `AGENTS.md` §3.6
- **pfad:** `d-check.mk:39-42`
- **befund:** Die Zeilen behaupten *„DASS DIE MENGE UEBERHAUPT TRAEGT, ist gegengemessen und
  nicht angenommen: mit entwerteten Markern melden BEIDE Versionen dieselbe Befundmenge, `diff`
  der sortierten Ausgaben leer."* Der angeführte Beleg — Gleichheit der zwei Versionen, leerer
  `diff` — gilt über demselben Baum **auch ohne** Entwertung und unterscheidet die zwei Läufe
  daher nicht. Was sie unterschied, war genau die Zahl, die dieser Commit gestrichen hat (vorher
  `26`, in `MR-027` heute `37` gegen `0`). Die vorige Fassung trug den Beleg; die neue nennt ihn
  nicht mehr und delegiert ihn an `MR-027`, wo derselbe Eintrag die Befundzahl ausdrücklich als
  **nicht tragend** ausweist (*„Tragend sind die Gleichheit der zwei Mengen und die leere
  `diff`-Ausgabe, nicht die Datei- oder Befundzahl"*). Der Satz ist damit in genau der Form
  wahr, gegen die er geschrieben ist: eine Null gegen eine Null.
- **verifizierbar:** ja. Kopie außerhalb des Repos (`git archive 1edbae4`), beide Digests
  netzlos, Mount `:ro`, **ohne** jede Entwertung:

  ```
  docker run --rm --network none -v "<kopie>:/repo:ro" \
    ghcr.io/pt9912/d-check@sha256:5ea03abe7918381c68203d8ac078a78d0d4ab91b5478e87c66b5a7b4fda41288
  docker run --rm --network none -v "<kopie>:/repo:ro" \
    ghcr.io/pt9912/d-check@sha256:3996a593b9cb71aa3bcb4f3ddf8f637e7409db31b3a2dac7eedc28d65814cacf
  ```

  → beide `d-check: 435 Datei(en) geprüft, 0 Befund(e)`, Exit 0, `diff` der sortierten Ausgaben
  **leer**. Die Bedingung des Satzes ist erfüllt, die Entwertung hat daran keinen Anteil.

### MEDIUM-2 — Das Kommando neben der Zahl `VIER` liefert, wörtlich gefahren, `1` — und zwar mit Exit 0

- **kategorie:** MEDIUM
- **quelle:** `MR-025` Setzung 1 (*„das Kommando, das **genau sie** ausgibt … ein ungefähr
  passendes Kommando danebenzustellen ist der Fehler, nicht die Lücke"*)
- **pfad:** `d-check.mk:44-45`
- **befund:** Der Kopf führt *„es sind VIER Handgriffe, nicht zwei — abzaehlbar mit"* und
  darunter `diff <(docker run --rm --network none $(DCHECK_REF) --print-mk) d-check.mk | grep -c '^[0-9]'`.
  `$(DCHECK_REF)` ist eine make-Variable; in einem Kommentar expandiert make sie nie, und in der
  Shell ist `$(…)` Kommando-Substitution. Wörtlich in bash gefahren meldet die Zeile
  `DCHECK_REF: Befehl nicht gefunden` und `unknown flag: --print-mk`, `docker` liefert nichts,
  `diff` vergleicht gegen eine leere Eingabe und `grep -c` gibt **`1`** aus — Exit **0**. Der
  Fehlschlag ist an der Zahl nicht zu erkennen: eine plausible kleine Ganzzahl statt einer
  Fehlermeldung. Dass der Wert `4` über der wirklichen Ausgabe stimmt, ist unabhängig gemessen
  (unten, N-4) — nicht mit dem Kommando, das danebensteht.
- **verifizierbar:** ja. Die Zeile aus `d-check.mk:45` unverändert in eine bash-Zeile kopieren:
  Ausgabe `1`, Exit 0. Mit aufgelöster Referenz
  (`ghcr.io/pt9912/d-check@sha256:5ea03abe…`) dagegen `4`.

### MEDIUM-3 — Die als richtig ausgewiesene Zähl-Form zählt nicht: `git grep -c` gibt Treffer **je Datei** aus

- **kategorie:** MEDIUM
- **quelle:** `MR-025` Setzung 1
- **pfad:** `d-check.mk:34-38`
- **befund:** Der Absatz stellt eine Regel auf und ein Kommando daneben: *„Wer doch zaehlt,
  schneidet ueber den PFAD und nicht ueber den Text:
  `git grep -c 'd-check:ignore' -- '*.md' ':!.harness/baseline'`"*, gefolgt vom Satz
  *„ein Kommando neben einer Zahl belegt sie erst, wenn das Kommando den Gegenstand
  schneidet."* Die Pathspec-Hälfte stimmt; die Zähl-Hälfte nicht: `git grep -c` gibt **eine
  Zeile je Datei** im Format `pfad:anzahl` aus und nie eine Summe. Über `1edbae4` sind das
  **142** Ausgabezeilen, während die Marker-Zeilen, um die es geht, **268** sind. Wer der Zeile
  folgt, bekommt keine Zahl, sondern eine Liste — und wer die Zeilen der Liste zählt, bekommt
  die Zahl der **Dateien** statt der Marker. `MR-027`, das dieselbe Aussage trägt, benutzt die
  Form `git grep -h … | wc -l`.
- **verifizierbar:** ja.
  `git grep -c 'd-check:ignore' -- '*.md' ':!.harness/baseline' | wc -l` → **142**;
  `git grep -h 'd-check:ignore' -- '*.md' ':!.harness/baseline' | wc -l` → **268**
  (mit `':!.harness/baseline/**'` identisch **268** — die Pathspec-Hälfte ist damit ausdrücklich
  bestätigt, nicht mitverworfen).

### LOW-1 — Die Aufzählung der vier Handgriffe beschreibt Handgriff 3 als Umbenennung; der Hunk enthält daneben eine inhaltliche Erweiterung

- **kategorie:** LOW
- **quelle:** `MR-010` Setzung 1 · `AGENTS.md` §3.7
- **pfad:** `d-check.mk:44-49`
- **befund:** Punkt 3 lautet *„`doc-check` -> `docs-check` samt seiner Hilfe-Zeile"*. Der
  zugehörige Hunk (`22,23c62,63`) ersetzt `.PHONY: doc-check` und
  `doc-check: ## Doku-Referenzen prüfen (Befund-Gate)` durch `.PHONY: docs-check` und
  `docs-check: ## Doku-Referenzen prüfen (Befund-Gate; links/anchors/ids/codepaths laut .d-check.yml) — netzlos`.
  Die Hilfe-Zeile des Tools enthält die Zeichenkette `doc-check` **nicht**; die gelebte Fassung
  entsteht daher nicht durch eine Umbenennung, sondern durch eine zusätzliche inhaltliche
  Erweiterung, die die Aufzählung nicht nennt. Zugleich zählt das danebenstehende Kommando
  `diff … | grep -c '^[0-9]'` **Hunks**, nicht Handgriffe: wer die vier Punkte wörtlich
  ausführt und die Erweiterung wegfallen lässt, bekommt weiterhin **4** — die genannte Zahl
  kann den Verlust nicht anzeigen. Dieselbe Granularitäts-Lücke trägt `MR-010` Setzung 1
  (*„Target und Hilfetext"*); der Kopf gibt sie unverändert weiter.
- **verifizierbar:** ja.
  `diff <(docker run --rm --network none ghcr.io/pt9912/d-check@sha256:5ea03abe… --print-mk) d-check.mk`
  → vier Hunks `1,9c1,49` · `11c51` · `22,23c62,63` · `67,68c107,108`; der dritte enthält beide
  Änderungen.

### LOW-2 — Die neue `sources`-Abgrenzung nennt den Ort, aber nicht den Eintrag, der sie entschieden hat

- **kategorie:** LOW
- **quelle:** `MR-013` · `AGENTS.md` §3.7 (Klasse *Rang-Zeiger*)
- **pfad:** `d-check.mk:11-14`
- **befund:** Der neue Satz führt *„`sources` faehrt trotzdem, naemlich in
  `make regelwerk-check` (Makefile, `--enable sources`, mit Netz, NICHT in `make gates`)"*. Er
  nennt Ort und Flag, aber nicht `MR-013` — den Eintrag, der genau diese Entscheidung trägt und
  ihre Begründung führt (*„es ist ein Netz-Modul und bräche den netzlosen `docs-check`/`make
  gates`"*, dazu die Zwei-Pin-Kopplung über `test/sources-pin.bats`). Jede andere substanzielle
  Aussage desselben Kopfes nennt ihre Quelle (`LH-QA-01`, `LH-QA-02`, `MR-011`), und der
  Rang-Zeiger in Zeile 2 wurde in diesem Commit angefasst, ohne `MR-013` aufzunehmen. Wer das
  Rezept in `Makefile:170` ändert, findet vom Kopf aus nicht die Stelle, die den Ausschluss aus
  `gates` begründet.
- **verifizierbar:** teilweise —
  `git grep -n -- '--enable sources' -- Makefile d-check.mk harness/conventions.md .d-check.yml`
  zeigt die vier Fundorte (ausführend nur `Makefile:170`); dass der Zeiger fehlt, ist am Text
  ablesbar, nicht an einem Gate.

### LOW-3 — Zwei mitwandernde Zahlen bleiben ohne Kommando auf einer Zeile, die dieser Commit umgebrochen hat

- **kategorie:** LOW
- **quelle:** `MR-025` Setzung 2
- **pfad:** `d-check.mk:15-17`
- **befund:** *„Von den sechs fokussierten advisory-Recipes disablen FUENF alle drei"* — beide
  Zahlen messen die Tool-Ausgabe und wandern mit ihr: ein weiteres opt-in-Modul in einer
  künftigen d-check-Version bewegt sie, ohne dass am Gegenstand (der Adaption) etwas bricht,
  und kein Kommando steht daneben. Heute sind sie richtig (gemessen unten). Der Auflösungs-
  Trigger von `MR-010` bindet an den Re-Pin die Target-Aufzählung aus Setzung 2, nicht diese
  beiden Zahlen. Die Zeile wurde in diesem Commit umgebrochen und neu gesetzt; die Werte selbst
  sind unverändert geblieben.
- **verifizierbar:** ja. `grep -cE '^\t.*--enable' d-check.mk` → **6**;
  `grep -E '^\t.*--enable' d-check.mk | grep -c -- '--disable citations.*--disable sources.*--disable structure'`
  → **5**.

### INFO-1 — Das Beleg-Kommando der Commit-Message liefert über dem Baum nach dem Commit `2`, nicht `1`

- **kategorie:** INFO
- **quelle:** `MR-025` §Begründung (die Klasse nennt *„eine Commit-Message"* ausdrücklich als Fundort)
- **pfad:** Commit `1edbae4`, Message-Block *„DoD (3) a)"*
- **befund:** Die Message führt
  `grep -rn 'enable sources' Makefile d-check.mk .github/workflows/*.yml` → *„1 Treffer:
  Makefile:170"*. Über dem Baum **nach** dem Commit liefert dasselbe Kommando **2** Treffer:
  `Makefile:170` und `d-check.mk:12` — der Kopf zitiert das Flag jetzt selbst. Die Aussage der
  Message war über dem Vor-Baum richtig; nachgefahren wird sie am Nach-Baum. Der DoD-eigene
  Rot-Befehl (`grep -n 'enable sources' Makefile`) ist davon unberührt und liefert weiter **1**.
- **verifizierbar:** ja, mit dem zitierten Kommando über `1edbae4`.

### INFO-2 — „Fährt den gepinnten v0.65.0" koppelt zwei Zeichenketten, die nichts aneinander hält

- **kategorie:** INFO
- **quelle:** `LH-QA-02` · `MR-027`
- **pfad:** `d-check.mk:29-31` (Kriterium), `d-check.mk:50-51` (Pin)
- **befund:** Das neue Kriterium sagt, `make docs-check` fahre *„den gepinnten v0.65.0"*.
  Welche Version wirklich läuft, entscheidet allein `DCHECK_DIGEST`; der Tag-String in
  `DCHECK_IMAGE` ist reine Beschriftung, weil ein gesetzter Digest ihn sticht (`d-check.mk:55-60`).
  Gekoppelt sind im Repo nur `internal/emit/emit.go` und `d-check.mk`
  (`TestDefaultImage_MatchesCanonical`, `TestDefaultDigest_MatchesCanonical`) — Tag gegen
  Digest hält nichts. Ein Re-Pin, der eine der beiden Zeilen vergisst, lässt die Kopf-Aussage
  stillschweigend falsch werden. Das ist keine Neuerung dieses Commits (Zeile 5–6 sagt es seit
  `MR-024`), aber das neue Kriterium stützt sich darauf.
- **verifizierbar:** teilweise —
  `git grep -ln 'DCHECK_DIGEST\|DCHECK_IMAGE' -- 'test/**' 'internal/**' 'harness/**' '.github/**'`
  nennt keine Datei, die Tag und Digest gegeneinander hält.

### INFO-3 — Die Sonden-Beschreibung im Kopf nennt einen der zwei Gegenstände, die `MR-027` misst

- **kategorie:** INFO
- **quelle:** `MR-027` §Zweck
- **pfad:** `d-check.mk:20-27`
- **befund:** Der Kopf beschreibt die Verengungs-Sonde als *„Vier Lagen ueber einem toten
  Codepath"*. `MR-027` misst dieselbe Sonde *„über einem toten Codepath **und über einer
  unverlinkten Kennung**"* und trägt als Aussage, dass `codepaths` und `ids` in allen vier Lagen
  gleich antworten — der Kopf gibt davon die Hälfte wieder. Kein Widerspruch, eine Verkürzung.
  Die Zeilen sind in diesem Commit **nicht** angefasst worden (Kontext-Zeilen des Hunks
  `@@ -22,13 +26,27 @@`) und liegen damit außerhalb des §3.7-Cutoffs.
- **verifizierbar:** ja, `git show 1edbae4 -- d-check.mk` gegen `MR-027` §Zweck.

### INFO-4 — Von den drei opt-in-Modulen hat eines nirgends einen Lauf; die verallgemeinernde Zeile sagt es nicht

- **kategorie:** INFO
- **quelle:** `MR-011` (*„`citations`-Modul bewusst nicht aktiviert"*) · `LH-QA-01`
- **pfad:** `d-check.mk:13-14`
- **befund:** Der neue Satz definiert: *„„Nicht aktiviert" heisst hier: nicht in der Modul-Liste
  des Befund-Gates, nicht: es gibt keinen Lauf."* Er steht hinter der Aufzählung **aller drei**
  opt-in-Module, nennt einen Lauf aber nur für `sources` und `structure`. Für `citations` gibt
  es keinen: `--enable citations` kommt im ganzen Repo nicht vor. Die Definition ist für zwei der
  drei zutreffend und lädt für das dritte zur falschen Umkehrung ein.
- **verifizierbar:** ja.
  `git grep -c -- '--enable citations' -- Makefile d-check.mk .github .d-check.yml` ist leer
  (Exit 1); `git grep -o -- '--enable [a-z]*' -- Makefile d-check.mk | sort -u` führt
  `commits`, `planning`, `sources`, `structure`, `targets`, `tracked`, `vcs`.

### INFO-5 — `MR-025` wird auf ein Artefakt angewendet, das sein eigener Geltungsbereich nicht führt

- **kategorie:** INFO
- **quelle:** `MR-025` §Geltungsbereich
- **pfad:** `d-check.mk:33` (Verweis *„MR-025 Setzung 2"*), Slice-Plan §Bezug
- **befund:** `MR-025` bindet *„die **lebenden**, repo-eigenen **Markdown**-Artefakte"* und
  misst seinen Geltungsbereich mit `git ls-files '*.md' …`. `d-check.mk` ist kein
  Markdown-Artefakt und liegt damit außerhalb. Der Slice wendet die Setzung freiwillig an — eine
  Verschärfung, kein Verstoß; nur folgt aus dem Kopf-Verweis nicht, dass `MR-025` `.mk`-Dateien
  erfasst. Wer das später als Präzedenz liest, weitet einen Geltungsbereich, den der Eintrag
  selbst per Kommando definiert.
- **verifizierbar:** ja. `git ls-files '*.md' ':!docs/reviews/**' ':!docs/plan/planning/done/**' ':!.harness/baseline/**' | grep -c 'd-check.mk'`
  → **0** (Exit 1).

## Negativbefunde

- **N-1 — Der Kriterien-Ersatz für die gestrichenen Zahlen trägt; geprüft, ohne Befund.** Die
  Sorge, ein Kriterium sei leer, weil das Gate ohnehin grün sein muss, greift hier **nicht**.
  Die Aussage des Absatzes ist *„über diesem Baum kostet die Verengung nichts"*, und sie ist mit
  *„`make docs-check` ist grün"* nicht tautologisch verbunden, sondern **deckungsgleich**: ein
  `d-check:ignore` in barer Prosa, das unter `v0.62.0` einen echten Befund unterdrückte, meldet
  unter `v0.65.0` und färbt `docs-check` rot. Das Rot ist im selben Kopf zwei Absätze höher
  gemessen (Sonden-Tabelle `d-check.mk:20-27`, `MR-027` §Zweck: *„blanke Prosa … v0.65.0
  MELDET"*). Der Sensor ist benannt, existiert, liegt in `make gates`
  (`Makefile:299`: `gates: baseline-verify docs-check lint build test shell-lint ci-lint
  comment-claims host-bin span-check record-gates`) und ist heute grün:
  `make docs-check` → `d-check: 435 Datei(en) geprüft, 0 Befund(e)`, Exit 0. Ein Kriterium, das
  ein wiederkehrender Lauf prüft, ist die stärkere Form gegenüber einer eingefrorenen Zahl —
  genau die Richtung, die `MR-025` Setzung 2 verlangt.
- **N-2 — Der Grund für Streichen statt Korrigieren ist nachgemessen; geprüft, ohne Befund.**
  Die Behauptung des Slice, die alte Zahl sei am Entstehungstag falsch gewesen, hält: über
  `be6348c` liefert die Pathspec-Form
  `git grep -h 'd-check:ignore' be6348c -- '*.md' ':!.harness/baseline' | wc -l` → **249** und
  derselbe Strom durch `grep -c '<!--[^>]*d-check:ignore'` → **174**, also **75** nicht in
  Kommentar-Form, während der Kopf **71 von 242** führte. Der alte Filter reproduziert seine
  eigenen falschen Werte: dieselbe Kopie außerhalb des Repos, `grep -rn 'd-check:ignore'
  --include='*.md' . | grep -v '.harness/baseline'` → **242**, davon **171** in Kommentar-Form
  → **71**. Die Diagnose *„der Fehler sitzt im Kommando, nicht im Zählen"* ist damit belegt und
  nicht behauptet.
- **N-3 — Der Rang-Zeiger in Zeile 2 zeigt auf Einträge, die diese Datei wirklich adaptieren;
  geprüft, ohne Befund.** `grep -c '^### MR-.* — d-check-Pin v0\.65\.0' harness/conventions.md`
  → **1**; jeder der fünf genannten Einträge nennt `d-check.mk` in seinem §Geltungsbereich
  (`MR-010` das Fragment selbst, `MR-011`/`MR-012` je den Pin, `MR-024` und `MR-027` je Pin
  **und** Kopfkommentar). Kein genannter Eintrag ist superseded oder aufgehoben.
- **N-4 — Die Zuordnung der vier Handgriffe zu ihren Hunks stimmt; geprüft, ohne Befund
  (Detail in LOW-1).** Gemessen über eine frische `--print-mk`-Ausgabe des gepinnten Digests,
  netzlos: `1,9c1,49` = Adopter-Kopf (Punkt 1), `11c51` = `DCHECK_DIGEST ?=` leer → gepinnt
  (Punkt 2), `22,23c62,63` = `.PHONY`/Target `doc-check`→`docs-check` (Punkt 3), `67,68c107,108`
  = `doc-help`-Hilfetext und `grep -hE '^docs?-…'` statt `'^doc-…'` (Punkt 4). Vier Hunks, vier
  Punkte, keine Vertauschung. Die Reihenfolge weicht von `MR-010` Setzung 1 ab — dort ist der
  Rename Handgriff 1 —, was die Aufzählung ausdrücklich als Diff-Reihenfolge ausweist.
- **N-5 — Hard Rule 3.3 (Move und Rewrite getrennt); geprüft, ohne Befund.**
  `git show --stat -M 08cdaae` und `29e9aed` zeigen je **1 file changed, 0 insertions(+),
  0 deletions(-)** mit erkanntem Rename; `05964ad` berührt nur
  `docs/plan/planning/done/slice-122-d-check-pin-v0650.md` (3+/3−), `1edbae4` nur `d-check.mk`.
  Kein Commit mischt Move und Inhalt.
- **N-6 — Hard Rule 3.8 (Architect-Eigentum); geprüft, ohne Befund.** Kein Commit dieses Slice
  berührt `AGENTS.md` oder `harness/conventions.md`. `MR-027` ist in einem eigenen
  Architect-Commit entstanden (`aa32e1f`, **eine** Datei: `harness/conventions.md`), vor dem
  Beginn-Trigger. Die Reihenfolge-Abhängigkeit, die der Plan §1 beschreibt, ist eingehalten.
- **N-7 — Hard Rule 3.5 / `LH-QA-01`; geprüft, ohne Befund.** Kein Gate gelockert, kein Modul
  aktiviert oder deaktiviert: `.d-check.yml`, `Makefile` und `internal/emit/` sind unverändert
  (`git show --stat 1edbae4` → eine Datei). Jedes im Kopf genannte Ziel existiert:
  `docs-check` (`d-check.mk:62`), `gates` (`Makefile:299`), `regelwerk-check` (`Makefile:169`),
  `doc-structure` (`d-check.mk:104`).
- **N-8 — Ebenen-Trennung Dogfood/emittiert; geprüft, ohne Befund.** Der emittierte Kopf ist ein
  eigener, fünfzeiliger Text (`internal/emit/emit.go:41-45`, Konstante `adopterHeader`) und
  trägt keine der geänderten Aussagen — weder zu `sources`/`structure` noch zu Marker-Zahlen.
  Der Commit erzeugt dort keine Drift.
- **N-9 — Doppel-Fundorte derselben Aussage; geprüft, ohne Befund.** Die präzisierte
  `sources`-Aussage steht an keiner zweiten lebenden Stelle in der alten, unqualifizierten Form:
  `.d-check.yml:70-77` und `harness/conventions.md:595` (`MR-013`) führen bereits die genaue
  Fassung (*„Aktiviert **nur** via `make regelwerk-check`"*); `harness/README.md` und `AGENTS.md`
  nennen `sources` nicht. Es bleibt also kein zweiter Ort stehen, den die Korrektur ausgelassen
  hätte.
- **N-10 — Link-Abgleich nach dem Move; geprüft, ohne Befund.**
  `git grep -n 'slice-128' -- '*.md'` außerhalb der Plandatei liefert genau die drei Zeilen in
  `docs/plan/planning/done/slice-122-d-check-pin-v0650.md:355-357`, alle drei auf
  `../in-progress/…` gezogen; kein Verweis auf den alten `open/`-Pfad ist übrig.
- **N-11 — Hard Rule 3.9; geprüft, ohne Befund.** Der Diff führt keine Host-Toolchain ein; die
  Kommandos im Kopf sind `make`-Ziele, `git` und `docker`. Alle Sonden dieses Reports liefen
  über `docker run --network none` gegen Kopien außerhalb des Repos (`git archive`), keine
  Host-Toolchain in der Befehlsposition.
- **N-12 — `make mutate`; nicht gefahren (Auflage), Protokoll übernommen.** Frisch grün über
  `1edbae4`: **198 ok, 0 Befund(e)**, 748,31 s. Der Diff berührt keinen Zahn und keine
  bewachte Stelle — er ändert einen Kommentarblock in einer Datei, die kein Fall in
  `test/mutations/` adressiert.
- **N-13 — Kodierungs-Stil (ASCII-Transliteration neben Umlauten im selben Block); geprüft,
  kein Finding.** Der neue Text schreibt `faehrt`/`gruen`/`waechst`, der bestehende Kopf
  `heißt`/`verfügbar`/`Zeilenreferenz-Prüfung`.
  `git grep -in 'umlaut\|ASCII' -- AGENTS.md harness/conventions.md harness/README.md CLAUDE.md '.harness/skills/*.md'`
  ist leer — kein Konventions-Anker, also nach Reviewer-Skill §Anti-Pattern kein Finding.

## Kategorie-Summary

| Kategorie | Anzahl | IDs |
|---|---|---|
| HIGH | 1 | HIGH-1 |
| MEDIUM | 3 | MEDIUM-1, MEDIUM-2, MEDIUM-3 |
| LOW | 3 | LOW-1, LOW-2, LOW-3 |
| INFO | 5 | INFO-1 … INFO-5 |
| Negativbefunde | 13 | N-1 … N-13 |

**Steering-Loop-Signal (Reviewer-Skill §Kontext-Eskalation).** MEDIUM-2, MEDIUM-3 und LOW-1
tragen **dieselbe** Klasse: *ein Kommando steht neben einer Aussage und schneidet ihren
Gegenstand nicht* — einmal, weil es in der Shell nicht läuft; einmal, weil es je Datei statt
über alle Zeilen zählt; einmal, weil es Hunks statt Handgriffe zählt. Das ist die dritte
Wiederholung in einem Lauf und zugleich exakt die Klasse, die der Kopf in denselben Zeilen
formuliert (*„ein Kommando neben einer Zahl belegt sie erst, wenn das Kommando den Gegenstand
schneidet"*). Nach `MR-025` §Auflösungs-Trigger ist die Klasse bewusst ohne Wächter; dieser
Lauf ist ein Datenpunkt dafür, dass der Rollen-Wechsel sie trägt — hier hat er sie gefunden,
und zwar in dem Artefakt, das sie aufschreibt.

## Verdikt

**Blockierend.** Ein HIGH und drei MEDIUM. Der HIGH ist ein Verstoß gegen Hard Rule §3.7 in
Zeilen, die dieser Commit neu geschrieben hat, und liegt damit innerhalb des Cutoffs, den die
Regel selbst zieht. Die drei MEDIUM betreffen sämtlich Beleg-Kommandos, die ihren Gegenstand
nicht schneiden — bei einem Slice, dessen erklärter Zweck genau diese Eigenschaft ist, sind sie
nicht als Doku-Drift abzustufen.

**Nicht blockierend und ausdrücklich festgehalten:** die Kernentscheidung des Slice — die drei
Zahlen zu **streichen** statt zu korrigieren und durch ein wiederkehrend geprüftes Kriterium zu
ersetzen — trägt (N-1), ihr Grund ist unabhängig nachgemessen (N-2), und die Zuordnung der vier
Handgriffe zu ihren Hunks stimmt (N-4). Die Befunde liegen in der Ausführung, nicht im Entwurf.

**Kein Rollen-Konflikt.** `d-check.mk` ist kein Architect-Artefakt nach §3.8; alle Findings
liegen in Artefakten, die die Implementation selbst schreibt. Der Konflikt-Pfad aus Modul 8 ist
nicht einschlägig.
