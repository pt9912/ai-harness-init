# Review-Report — slice-020: regelwerk-check auf d-check `sources` (Tool statt Skript)

## Kopf-Metadaten

- **Rolle:** Unabhängiger Reviewer (Modul 10), frischer Kontext.
- **Reviewer-Skill:** `.harness/skills/reviewer.md` v1.2.0.
- **Datum:** 2026-07-19.
- **Gegenstand / Diff:** Commit `b1ff1cb` (`slice-020: regelwerk-check auf d-check sources umgestellt (MR-013)`).
- **Plan:** `docs/plan/planning/in-progress/slice-020-dcheck-sources.md`.
- **Betroffene `LH-*`:** LH-QA-01 (keine halluzinierten Gates), LH-QA-02 (Reproduzierbarkeit), LH-QA-03 (minimale Abhängigkeiten).
- **Referenzierte ADRs:** ADR-0003 (Docker-only, kein Host-`curl`/Toolchain).
- **Konventionen:** MR-013 (neu), MR-012 (Pin v0.51.1, Vorbedingung), MR-007 (kanonischer `BASELINE_ZIP_SHA256`-Pin).
- **Hard Rules:** AGENTS.md §3 (3.1, 3.3, 3.5).
- **Vorherige Findings am Modul:** slice-019-review (0 HIGH/0 MEDIUM), slice-021-review/-verify (0/0; „`sources` nur verfügbar, nicht aktiviert → kein Phantom-Gate"). Wiederkehrendes Muster: Kopplungspunkte müssen erzwungen sein; `sources` ist Netz und darf nicht in `gates` fallen. Beides hier adressiert.
- **Änderungsumfang:** `M .d-check.yml`, `M Makefile`, `M docs/plan/.../slice-020-dcheck-sources.md`, `M harness/conventions.md`, `A test/sources-pin.bats`.

**Unabhängig ausgeführt (nicht nur nachgelesen):** `make docs-check` (netzlos), Kopplungs-`bats` inkl. zweier Fehlinjektionen, `unpack`-Gegenprobe (Netz), `make regelwerk-check` (Netz), Hash-/Referenz-Greps, Rename-Detection. Alle Mutationen revertiert; Arbeitsbaum am Ende sauber (`git status --porcelain` leer).

---

## Findings

**Keine.** Kein HIGH-, MEDIUM-, LOW- oder INFO-Finding. Alle sieben geprüften Kern-Achsen sind belegt grün (siehe Negativbefunde).

---

## Negativbefunde (geprüft, ohne Befund)

### 1. `sources` NICHT in `modules:` → netzlos bleibt (LH-QA-01-Kern-Eigenschaft) — geprüft, ok

- `.d-check.yml:12` `modules:` = `[links, anchors, ids, matrix, codepaths, spans]` — enthält **kein** `sources`. Der `sources:`-Block (Zeile 71-74) ist opt-in.
- **Gemessen:** `make docs-check` (`docker run --network none … $(DCHECK_REF)`) → `71 Datei(en) geprüft, 0 Befund(e)`, Exit 0. **Kein** `source-unreachable`. Da unter `--network none` ein aktiviertes `sources` zwingend `source-unreachable` produzieren würde, beweist dessen Ausbleiben, dass `sources` im netzlosen Gate **nicht feuert** — obwohl der Block in der Config steht. Der aktuell gepinnte d-check (v0.51.1) behandelt `sources` also auch bei vorhandener Config als opt-in. Offline-grün unberührt.

### 2. `unpack: none` korrekt (gemessen, nicht behauptet) — geprüft, ok

- **Gegenprobe (Netz):** `.d-check.yml` temporär auf `unpack: zip` (gleicher Roh-Byte-Hash), `docker run … --enable sources` → `.d-check.yml:72 … source-drift`, `1 Befund(e)`, Exit 1. Mit `unpack: none` → `0 Befund(e)`, Exit 0 (Achse 4). Bestätigt die MR-013/Plan-Setzung: `BASELINE_ZIP_SHA256` ist ein Roh-Byte-Hash und passt **nur** zu `unpack: none`; `unpack: zip` hasht ein Content-Manifest (anderer Hash-Raum). Die Modus-Wahl ist gemessen belegt (LH-QA-02). `.d-check.yml` danach revertiert.

### 3. Kopplungstest `test/sources-pin.bats` hat ZÄHNE (fail-closed) — geprüft, ok

- Baseline (unverändert): beide Tests grün.
- **Fehlinjektion sha256** (`.d-check.yml`-`sources`-`sha256`, ein Zeichen `1`→`2`): Test 1 (`sources-sha256 == Makefile BASELINE_ZIP_SHA256`) → `not ok`, Exit 1. Revert.
- **Fehlinjektion Tag** (`.d-check.yml`-`url` `v3.5.0`→`v3.5.1`): Test 2 (`sources-url trägt BASELINE_TAG`) → `not ok`, Exit 1. Revert.
- Beide Divergenz-Richtungen werden gefangen (der `Makefile`-Pin ist der kanonische Anker; weicht die `.d-check.yml`-Kopie ab, wird der Test rot). Fehlerrichtung ist zusätzlich fail-**closed**: fragile `grep`/`sed`-Extraktion würde bei Formatabweichung höchstens falsch-**rot**, nie falsch-grün. Kein Falsch-Grün-Pfad gefunden. Läuft in `gates` (via `make test`, `--network none`, reiner Datei-Vergleich).

### 4. `make regelwerk-check` funktioniert (auf `sources` isoliert, Netz) — geprüft, ok

- `docker run … $(DCHECK_REF) --enable sources --disable links --disable anchors --disable ids --disable matrix --disable codepaths --disable spans` (MIT Netz) → `0 Befund(e)`, Exit 0 gegen das echte v3.5.0-Asset. Alle sechs Default-Doku-Module disabled, nur `sources` aktiv → korrekt isoliert. Recipe fail-closed: die `docker run`-Zeile trägt kein `@`/`-`, ein Nonzero-Exit bricht das Target (in Achse 2 verifiziert: Drift → Exit 1).

### 5. Zwei-Pin-Kopplung sauber (keine dritte Drift-Fläche) — geprüft, ok

- `git grep` des Hashes `123e3383…`: genau **zwei LIVE-Pins** — `Makefile:34` (`BASELINE_ZIP_SHA256`, kanonisch, MR-007 unberührt) und `.d-check.yml:73` (Duplikat, per Test gekoppelt). Die drei weiteren Treffer sind **eingefrorene Historie** (`docs/plan/planning/done/slice-019-…` und `docs/reviews/2026-07-19-slice-019-verify.md`) — keine gepflegten Pins, keine Drift-Fläche. MR-007 (`Makefile` als kanonische Quelle) bleibt unangetastet.

### 6. Name behalten (kein Rename); frozen MR-Historie unverändert; MR-013-Anker/IDs — geprüft, ok

- `git show b1ff1cb --name-status`: `regelwerk-check` bleibt derselbe Target-Name; keine Umbenennung. `--find-renames`: keine Renames, einzige Neuanlage ist `test/sources-pin.bats`.
- conventions.md-Hunk ist **rein additiv** (nur MR-013 hinzugefügt, Zeilen 498-525); keine bestehende MR-Prosa geändert/entfernt. Die ~15 `regelwerk-check`-Referenzen in `done/`-Slices beschreiben bewusst den Bash-Stand ihrer Zeit (MR-013 explizit: „frozen MR-Historie beschreibt weiter den Bash-Stand ihrer Zeit") — keine stale-Doc-Findings.
- MR-013-Anker `#mr-013--regelwerk-check-auf-d-check-sources-tool-statt-skript` löst gegen die Überschrift auf; die `ids`/`anchors`/`links`-Module sind in `docs-check` aktiv und liefern 0 Befunde über 71 Dateien → alle MR-/LH-/ADR-Referenzen im Diff sind verlinkt und die Anker resolvbar.

### 7. Hard Rules + ADR-0003 — geprüft, ok

- **3.3 (Move + Inhaltsänderung = zwei Commits):** `b1ff1cb` enthält **keinen** `git mv` (Slice bleibt in `in-progress/`, 4×M + 1×A). Der Move nach `done/` ist die separate Closure-Operation. Nicht verletzt.
- **3.1 (keine halluzinierten Gates / kein Netz in gates):** `gates:` = `baseline-verify docs-check lint build test shell-lint record-gates` — kein `regelwerk-check`, kein `sources`. Kein neuer Gate-Name in AGENTS.md §4 / harness/README.md (Diff berührt beide nicht). `docs-check` netzlos 0 Befunde. Nicht verletzt.
- **3.5 (Gates nicht ohne ADR lockern):** keine Schwellen-Senkung; im Gegenteil wird `gates` durch den fail-closed Kopplungstest **verschärft**. Nicht verletzt.
- **ADR-0003 (Docker-only, kein Host-`curl`):** der frühere Bash-Körper (`curl`+`sha256sum`) ist entfernt; `regelwerk-check` ist jetzt `docker run … d-check`. Das war der Kern-Punkt des Slice — ADR-0003-Konformität für dieses Target verbessert. Read-only-Mount (`-v "$(CURDIR):/repo:ro"`) → vendored Baum unberührt.

### 8. Nebenbetrachtung Doku-Konsistenz — geprüft, ok

- `Makefile:32` („regelwerk-check vergleicht Upstream gegen diesen Pin") und die `baseline-freshness`-Kommentar-Analogie (`Makefile:96`, „ob veraltet oder Fetch-Fehler sagt die echo-Meldung, wie bei regelwerk-check") bleiben inhaltlich zutreffend: das neue `regelwerk-check` folgt weiter dem Muster „Exit 0 = OK, !=0 = Alarm; Meldung nennt die Ursache" (d-check emittiert `source-drift`/`source-unreachable`). Kein Doku-Drift.

---

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 0 |

---

## Verdikt

**Merge NICHT blockiert.** Kein HIGH-/MEDIUM-Finding (auch kein LOW/INFO). Der Diff setzt den Plan präzise um: `regelwerk-check`-Körper von Bash (`curl`+`sha256sum`) auf `docker run … --enable sources` (auf `sources` isoliert, Netz, Maintenance/CI), `sources`-Block in `.d-check.yml` mit `unpack: none` (gemessen: Roh-Byte-Hash = `BASELINE_ZIP_SHA256`, 0 Drift; `unpack: zip` gegengeprobt → `source-drift`), **nicht** in `modules:` (netzloser `docs-check` bleibt 0 Befunde ohne `source-unreachable`). Die Zwei-Pin-Kopplung ist per `test/sources-pin.bats` **fail-closed in `gates`** erzwungen — beide Fehlinjektionen (sha256, Tag) machen den Test rot; ein Falsch-Grün-Pfad wurde nicht gefunden. Name/Interface stabil (kein Rename), MR-Historie rein additiv, ADR-0003 (Host-`curl` raus) erfüllt, Hard Rules 3.1/3.3/3.5 gewahrt. Alle Belege wurden unabhängig auf demselben Arbeitsbaum gefahren; `.d-check.yml` am Ende sauber zurückgesetzt.

*Hinweis (nicht Reviewer-Rolle): DoD-Abhakung und Gate-Lauf-Bestätigung obliegen der getrennten Verifikation.*
