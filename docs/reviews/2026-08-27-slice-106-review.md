# Review-Report: slice-106 (Code) — 2026-08-27

**Review-Art:** **Code**, erste Runde — geprüft wird der Diff gegen den Slice-Plan, die
referenzierten ADRs und die Hard Rules (Modul 10 §Drei Review-Arten). Kein vorheriger
Review-Lauf zu diesem Slice.

**Gegenstand:** gestaged, nicht committet. `git diff HEAD --stat` → **7 Dateien, 402
insertions(+), 4 deletions(-)**: 2×`M` (`harness/README.md`, `harness/tools/full-smoke.sh`),
5×`A` (`harness/tools/full-smoke-ausgang.sh`, `test/full-smoke-ausgang.bats`,
`test/mutations/187-ausgang-ohne-muster.sh`, `test/mutations/188-ausgang-immer-leitung.sh`,
`test/mutations/189-emittierter-pin-nicht-aufloesbar.sh`). Kein untrackter Fund daneben
(`git status --porcelain=v1` zeigt ausschließlich `M`/`A`, keine `??`-Zeile). HEAD `c4a0c03`
bestätigt (`git rev-parse HEAD`).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0
**Modell:** claude-sonnet-5 · **Datum:** 2026-08-27

**Eingangs-Kontext:**

- **Diff:** volltext gelesen (alle 7 Dateien), plus `git diff HEAD -- <Datei>` je Datei zur
  Bestätigung, welche Zeilen wirklich neu sind (relevant für mehrere Befunde unten, s. F-2/F-3).
- **Slice-Plan:**
  [slice-106](../plan/planning/done/slice-106-rotes-ci-traegt-seinen-ausgang.md)
  (Stand `in-progress/`, vollständig gelesen: Ziel/Bestand/Klassen §1, DoD (1)–(3) §2,
  Plan-Tabelle §3, Trigger §4, Closure-Trigger §5, Risiken §6, Sub-Area §8).
- **ADRs, gelesen als Constraint, nicht angefasst:**
  [ADR-0003](../plan/adr/0003-go-native-binaries.md) (Docker-only — der Grund, aus dem der
  Sensor überhaupt fremde Hosts befragt), [ADR-0015](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md)
  Festlegung 1 (Hard Rules gehören dem Architect — der Plan weist ihre Wirkung nur aus).
- **Hard Rules:** `AGENTS.md` §3.1 (keine halluzinierten Gates), §3.2 (Lint-Suppression-Verbot),
  §3.3 (Move/Inhalt getrennt — hier nicht anwendbar, kein Commit im Diff-Bereich), §3.5
  (Gates nicht ohne ADR lockern — der Grund, warum LEITUNG/BAUM denselben Exit-Code tragen),
  §3.6 (Zusage ohne rot gesehenes Gegenbeispiel), §3.7 (ein Kommentar beschreibt, was da ist),
  §3.9 (Docker-only, neu seit heute).
- **Vorherige Findings am gleichen Modul:** `docs/reviews/` enthält keinen früheren Lauf zu
  `full-smoke.sh`/`full-smoke-ausgang.sh` unter diesem Namen; der nächstverwandte
  (`2026-08-26-slice-099-review.md` F-6) betraf einen anderen Abschnitt derselben Datei
  (`leser_und_aufraeumen_im_ziel`) und ist hier nicht einschlägig.

---

## Selbst gefahren (nichts aus Plan/Auftrag übernommen)

Alle Zahlen unten sind mit eigenem Kommando erhoben, nicht aus dem Auftrag oder dem Plan
übernommen (MR-025).

| Zahl/Aussage | Kommando | Ergebnis |
|---|---|---|
| `einordnen`-Aufrufe in `full-smoke.sh` | `grep -c 'einordnen "' harness/tools/full-smoke.sh` | **24** |
| Werkzeug-Aufrufe (`$tmpbin/ai-harness-init`) | `grep -c 'tmpbin/ai-harness-init"' harness/tools/full-smoke.sh` | **15** |
| davon eingeordnet | manuell gegen die 15 Fundstellen geprüft | **2** (Zeile 181, 774) |
| Docker-Aufrufpunkte im Go-Binary | `grep -rn 'exec.Command("docker"' internal/ cmd/` | **1** (`internal/emit/emit.go:156`, `printMK`) |
| `make build GO_VERSION=9.99.9-gibt-es-nicht` | selbst gefahren | **Exit 0**, 1,29 s, Resolution `#4 DONE 0.0s` (Digest-Pin greift, Tag irrelevant) |
| Externe Bild-Referenzen im CI-Job 97824094857 | `gh api .../jobs/97824094857/logs \| grep -ohE '(docker\.io\|ghcr\.io)/…' \| grep -v … \| sort -u \| wc -l` | **6** (deckt sich mit dem Plan) |
| Protokollzeilen desselben Jobs | `wc -l` über dasselbe Log | **6700** (deckt sich mit dem Kopf von `full-smoke-ausgang.sh`) |
| `502 Bad Gateway` im selben Log | `grep -c '502 Bad Gateway'` | **4** |
| `unexpected status from [A-Z]+ request` im selben Log | `grep -cE …` | **4** |
| Kontextblock-Marker `(^\|[[:space:]])> ` im selben Log | `grep -cE …` | **2** |
| `make comment-claims` | `make comment-claims` | grün, **46 Datei(en), 0 Befund(e)** |
| `make shell-lint` | `make shell-lint` | grün, Exit 0 |
| `make docs-check` | `make docs-check` | grün, **406 Datei(en), 0 Befund(e)** |
| `make test-bats` | `make test-bats` | grün, **160 Tests**, davon 7 neue `ausgang:`-Fälle |
| `make full-smoke` (unmutiert, dieser Diff) | `make full-smoke` | grün, Exit 0, **1 m 25,9 s** |
| `make gates` (voller Stack) | `make gates` | grün, Exit 0, **32,8 s** (warmer Cache) |
| Mutation 187 gegen isolierte Kopie | `rsync` + `bash test/mutations/187-*.sh` + `docker run … bats … test/full-smoke-ausgang.bats` | **4 von 7 rot** (genau die LEITUNG-Fälle) |
| Mutation 188 gegen isolierte Kopie | dieselbe Methode | **2 von 7 rot** (genau die BAUM-Fälle) |
| Mutation 189 gegen isolierte Kopie, echter `make full-smoke` | dieselbe Methode, `make full-smoke` in der Kopie | **Exit ≠ 0**, meldet `AUSGANG LEITUNG: make -j gates im Ziel (--lang go)`, Beleg Muster 1, **10,5 s** (bricht früh ab) |

Mutation 187/188/189 wurden **nicht** über `harness/tools/mutate.sh` (Kosten 1402 s laut Auftrag,
vorher anzukündigen), sondern manuell über eine isolierte `rsync`-Kopie außerhalb des Repos
nachgebaut — dieselbe Mutation, derselbe Sensor (`test-bats` bzw. `full-smoke`), ohne die
Vorlauf-/Fingerabdruck-Maschinerie des Treibers. Das beantwortet dieselbe Frage (verliert der
Wächter seine Zähne?), günstiger. Der Host-Baum blieb unberührt (Kopien liegen unter
`/tmp/.../scratchpad/`, danach entfernt).

---

## Findings

### F-1 — Der Plan-eigene Rot-Nachweis für DoD (2) misst nichts; der Diff selbst weicht davon korrekt ab, ohne den Plan nachzuziehen

- **Kategorie:** MEDIUM
- **Quelle:** Plan §2 DoD (2) ("Rot: ein Lauf, dessen gepinntes Bild nicht auflösbar ist …") +
  `AGENTS.md` §3.6 (eine Zusage ist erst fertig, wenn ihr Rot-Beleg trägt)
- **Pfad:** Plan-Text (unverändert, nicht Teil dieses Diffs) gegen `Dockerfile:14`
  (`FROM golang:${GO_VERSION}@sha256:65b6f280bf050ec5af12716857e8ea8439d694dbba8f31ceeb7630670071f2bb`)
  und `internal/gen/golang.go:877-882` (emittiertes Ziel-Dockerfile, **ohne** Digest: „Digest
  bewusst weggelassen, damit GO_VERSION ein echter Knopf bleibt")
- **Befund:** DoD (2) nennt als Rot-Nachweis die überschreibbare `GO_VERSION`, „nachgestellt über
  … `grep -n 'GO_VERSION' harness/tools/full-smoke.sh` → Zeilen 21 und 141" — das ist der
  Host-Artefakt-Bau (`make artifact`/`make build`). Dieses Repos eigenes `Dockerfile` pinnt den
  `deps`-Layer per **Digest**, nicht per Tag; ich habe das selbst gefahren:
  `make build GO_VERSION=9.99.9-gibt-es-nicht` → **Exit 0** in 1,29 s, BuildKit löst die
  Referenz über den Digest auf und ignoriert den unsinnigen Tag-Text vollständig (`#4 [internal]
  load metadata for … DONE 0.0s`, keine Fehlermeldung). Der im Plan benannte Rot-Nachweis
  **kann strukturell nie rot werden** — das ist genau die Klasse, die der Plan selbst in DoD (2)
  vorwegnimmt („Vor dem Anspruch auf Vollständigkeit zu prüfen: diese Variable trifft den
  Host-Artefakt-Bau, nicht den Bau im emittierten Repo, an dem der gemessene Vorfall hing").
  Der Diff **umgeht** dieses defekte Rezept, ohne es zu korrigieren: `test/mutations/189` setzt
  stattdessen am **emittierten** Ziel-Dockerfile an (`internal/gen/golang.go`), das laut eigenem
  Kommentar bewusst **ohne** Digest bleibt — genau dort greift ein kaputter Tag wirklich. Ich habe
  das Ende-zu-Ende nachgestellt (s. Tabelle oben): `make full-smoke` in der mutierten Kopie endet
  mit `AUSGANG LEITUNG: make -j gates im Ziel (--lang go)`. DoD (2) ist damit inhaltlich real
  erfüllt — aber der Plan-Text selbst benennt weiterhin das nicht-tragende Kommando als seinen
  Rot-Nachweis, ohne Anmerkung. Ein Verifier, der DoD (2) wörtlich mit dem im Plan genannten
  Kommando prüft, bekäme ein irreführendes Bild (dauerhaftes Exit 0, ohne dass das über die
  eigentliche Zusage etwas aussagt).
- **Verifizierbar:** ja — `make build GO_VERSION=9.99.9-gibt-es-nicht` bleibt Exit 0 (Gegenprobe
  zum defekten Plan-Rezept); `bash test/mutations/189-emittierter-pin-nicht-aufloesbar.sh` gefolgt
  von `make full-smoke` in einer isolierten Kopie zeigt den tatsächlich tragenden Weg.

### F-2 — Die „ABDECKUNG, gemessen statt angenommen"-Zusage nennt nur eine von zwei Ausnahmeklassen

- **Kategorie:** MEDIUM
- **Quelle:** `MR-025` (eine Zahl/Aussage steht neben dem Kommando, das sie trägt) + `AGENTS.md`
  §3.7 (ein Kommentar beschreibt, was da ist)
- **Pfad:** `harness/tools/full-smoke.sh:20-30` (Kopf-Kommentar zu `einordnen()`) und wortgleich
  `harness/README.md` (Nicht-Gate-Verify-Absatz, Satz „Wofür die Unterscheidung gilt, ist gemessen
  statt angenommen …")
- **Befund:** Der Satz „eingeordnet wird jeder Abschnitt, dessen Ausgabe dieser Lauf in eine
  Variable einfaengt" nennt als einzige Ausnahme „die uebrigen Werkzeug-Aufrufe" (13 von 15
  `$tmpbin/ai-harness-init`-Aufrufen, korrekt belegt über die zwei genannten `grep`-Kommandos).
  Tatsächlich gibt es **fünf weitere** Abschnitte, die ihre Ausgabe ebenfalls in eine Variable
  einfangen (`… || …_rc=$?`-Muster), aber **nicht** `einordnen` aufrufen und in keiner der beiden
  Textstellen als Ausnahme genannt sind: `kette_out` (Zeile 309, `make -n gates`), `clean_out`
  (Zeile 377, `make span-clean`), `span_out` (Zeile 505, Erfassungs-Hook-Wrapper), `leer_out`
  (Zeile 548, derselbe Wrapper ohne Träger), `dryrun_out` (Zeile 1032, `make -n gates` am
  Root-Modul). Praktisch ist das harmlos — alle fünf sind strukturell nie Docker-/
  Registry-berührend (`make -n` ist ein Trockenlauf, `span-clean`/der Hook-Wrapper rufen reine
  Go-Logik) —, aber die Zusage „gemessen statt angenommen" hält einer wörtlichen Prüfung nicht
  stand: `grep -c 'einordnen "'` (24) gegen die tatsächliche Zahl output-erfassender Abschnitte
  ist **29**, nicht 24 + (die genannten 13 Ausnahmen). Ein künftiger Umbau, der einem dieser fünf
  Abschnitte eine Docker-Abhängigkeit hinzufügt, würde nicht auffallen, weil die Doku bereits heute
  eine Vollständigkeit suggeriert, die nicht vorliegt.
- **Verifizierbar:** ja — `grep -noE '^\s*[a-z_]+="\$\(.*\)"\s*\|\|\s*[a-z_]+_rc2?=\$\?'
  harness/tools/full-smoke.sh | wc -l` → 29, gegen `grep -c 'einordnen "' harness/tools/full-smoke.sh`
  → 24, Differenz 5 statt der dokumentierten 13-nur-Ausnahme.

### F-3 — 23 von 24 `einordnen`-Verdrahtungspunkten sind gegen ein stilles Entfernen unbewacht

- **Kategorie:** MEDIUM
- **Quelle:** `AGENTS.md` §3.6 (rot gesehenes Gegenbeispiel) + Reviewer-Skill-Anker
  „Reproduzierbarkeits-Risiko" (`LH-QA-02`) — hier: die Verlässlichkeit der neuen Diagnose selbst
- **Pfad:** `harness/tools/full-smoke.sh` (alle 24 `einordnen`-Aufrufstellen) gegen
  `test/mutations/189-emittierter-pin-nicht-aufloesbar.sh`
- **Befund:** Von den 24 Stellen, an denen `full-smoke.sh` nach einem Fehlschlag `einordnen`
  aufruft, prüft **genau eine** (die `make -j gates im Ziel (--lang go)`-Stelle, Zeile 206) mit
  einem echten End-to-End-Lauf, dass die Verdrahtung wirklich greift (`test/mutations/189`, von
  mir selbst nachgestellt, s. Tabelle oben). `test/mutations/187` und `188` prüfen den
  Klassifikator (`full-smoke-ausgang.sh`) **für sich**, über Ausschnitten — sie laufen nie durch
  `full-smoke.sh` selbst und können daher nicht erkennen, wenn ein `einordnen`-Aufruf aus einem
  der anderen 23 Blöcke verschwindet, während die zugehörige `echo "FEHLER — …"; exit 1`-Zeile
  stehen bleibt. Der zugrundeliegende Gate-Fall bliebe in diesem Szenario weiterhin korrekt rot
  (Exit 1 unverändert) — nur die **Diagnose**, die der ganze Slice liefert, verschwände lautlos,
  ohne dass ein Sensor das meldet. Der Kopf von `test/mutations/189` legt diese Grenze selbst
  offen („test/mutations/187 und 188 messen den Einordner fuer sich; ob seine Ausgabe im Lauf
  ankommt, entscheidet die Verdrahtung, und die gibt es nur hier") — der Befund ist also nicht
  versteckt, aber er bleibt bestehen: 23 der 24 Verdrahtungspunkte haben keinen rot gesehenen
  Beleg, nur den einen, der als erster in der Ablauf-Reihenfolge liegt.
- **Verifizierbar:** ja — `einordnen "make a-check-apps-hex (Arch-Gate-Zahn, go)" "$teeth_out"`
  (Zeile 872) probeweise entfernen und `test/mutations/187`, `188`, `189` sowie `make test-bats`
  laufen lassen: keiner der drei würde rot.

### F-4 — Zwei neue Artefakte fehlen in der Plan-§3-Tabelle

- **Kategorie:** LOW
- **Quelle:** Modul 5 §Plan (vor Code) — „Maintainability"
- **Pfad:** Plan §3 (Datei/Komponente-Tabelle) gegen `git status --porcelain=v1`
- **Befund:** Die Plan-Tabelle listet `harness/tools/full-smoke.sh` (update) und
  `test/mutations/` (neu) als betroffene Artefakte. Zwei tatsächlich neue Dateien fehlen als
  eigene Zeile: `harness/tools/full-smoke-ausgang.sh` (100 Zeilen, der eigentliche
  Klassifikator — kein Bestandteil von „`full-smoke.sh` update") und
  `test/full-smoke-ausgang.bats` (161 Zeilen, sieben neue Tests — nicht von der generischen
  Zeile „`test/mutations/` | neu" gedeckt, die nur die Mutations-Fälle meint). Beide Dateien
  sind inhaltlich sauber innerhalb des im Ziel-Absatz (§1 „Ebene") beschriebenen Vorhabens; es
  fehlt nur die Zeile in der Vor-Code-Liste selbst.
- **Verifizierbar:** ja — Abgleich `git status --porcelain=v1` gegen Plan §3, rein textuell.

---

## Negativbefunde

- **geprüft, ohne Befund — zwei Ausgänge, ein Exit-Code.** Alle 24 `einordnen`-Aufrufstellen enden
  unmittelbar danach mit `exit 1` (`grep -n -A1 'einordnen "' harness/tools/full-smoke.sh` zeigt
  in jedem der 24 Blöcke `exit 1` als Folgezeile); `full-smoke-ausgang.sh` selbst endet in beiden
  Zweigen (LEITUNG via explizites `exit 0`, BAUM implizit über die letzte erfolgreiche Zeile) mit
  0 — der Aufrufer, nicht der Klassifikator, entscheidet den Exit-Code. Ein eigener Code für
  LEITUNG existiert nicht; das deckt sich mit `AGENTS.md` §3.5 (kein impliziter Freifahrtschein).
- **geprüft, ohne Befund — die BAUM-Formulierung bleibt bei „wird zugerechnet", nie „ist".**
  `full-smoke-ausgang.sh:100` und der Kopf-Kommentar (Zeilen 21-23) sagen dasselbe wörtlich und
  explizit: „Gemessen ist die ABWESENHEIT der … Muster … nicht die Ursache." `harness/README.md`
  übernimmt denselben Wortlaut. Keine der drei Stellen behauptet Kausalität.
- **geprüft, ohne Befund — der Rückfall bei unbekanntem Text ist BAUM, nie LEITUNG.** Der
  Klassifikator prüft ausschließlich auf Anwesenheit eines der vier Muster (`if [ -n "$treffer"
  ]`); ohne Treffer folgt zwingend der BAUM-Zweig. Ein künftiger Wortlautwechsel bei BuildKit
  /Docker-Daemon macht den Sensor **unschärfer** (verpasst LEITUNG-Fälle), nie **beschönigender**
  (nie fälschlich LEITUNG). Das Verhalten ist so im Kopf-Kommentar dokumentiert und strukturell
  bestätigt.
- **geprüft, ohne Befund — „das Werkzeug hat genau einen Docker-Aufrufpunkt" trägt.** Einziger
  `exec.Command("docker", …)`-Aufruf im gesamten Go-Code ist `printMK` in
  `internal/emit/emit.go:156`, tool-neutral für d-check **und** a-check verwendet (je ein
  fester, digest-gepinnter `DefaultDigest`/`DefaultArchDigest`, unabhängig von `--arch`-Wert oder
  Sprache — `internal/emit/emit.go:27-30`, `internal/emit/archgate.go:21`). Da `DocGate`
  konvergent bei **jedem** Init/`add-lang`-Lauf neu schreibt (nicht nur beim ersten), aber Docker
  ein bereits lokal vorliegendes digest-gepinntes Image ohne Netzzugriff verwendet, ist nur der
  jeweils **erste** Aufruf je Referenz (d-check, a-check) einem echten Pull-Risiko ausgesetzt —
  genau die zwei Stellen, die `einordnen` bekommen (Zeile 181, 774). Damit ist die Begründung für
  die 13-von-15-Lücke bei den Werkzeug-Aufrufen (im Unterschied zu F-2, die eine andere
  Abschnitts-Klasse betrifft) tragfähig.
- **geprüft, ohne Befund — 6 von 6 externen Bild-Referenzen liegen hinter einem eingeordneten
  Abschnitt.** Ich habe das CI-Protokoll des im Plan genannten Laufs (Job `97824094857`) selbst
  gezogen und die Referenzliste neu erhoben (s. Tabelle oben): `docker.io/docker/dockerfile:1.7`,
  `docker.io/golangci/golangci-lint:v2.13.1`, `docker.io/library/golang:1.27.0`,
  `docker.io/library/ubuntu:26.04`, `ghcr.io/pt9912/a-check@…`, `ghcr.io/pt9912/d-check@…`. Jede
  der sechs wird ausschließlich innerhalb eines `make -j gates`/`make a-check-*`/`make lint-*`
  /`make build-*`-Aufrufs (22 davon `einordnen`-gedeckt) oder eines der zwei `einordnen`-gedeckten
  Werkzeug-Aufrufe (d-check/a-check-Pull) angefordert — kein struktureller Blindpunkt für diese
  sechs Referenzen.
- **geprüft, ohne Befund — `make full-smoke` bleibt mit diesem Diff grün.** Selbst gefahren, Exit
  0, 1 m 25,9 s (deckt sich der Größenordnung nach mit dem im Plan zitierten Vorlauf-Wert von
  90 s aus `mutate.sh`s Kopf-Kommentar). Kein `AUSGANG`-Text erscheint im Grün-Pfad (erwartungs-
  gemäß, da nur Fehlschlag-Zweige `einordnen` rufen).
- **geprüft, ohne Befund — `make gates` bleibt mit diesem Diff grün.** Selbst gefahren
  (`baseline-verify docs-check lint build test shell-lint ci-lint comment-claims host-bin
  span-check record-gates`), Exit 0, 32,8 s bei warmem Cache; kein `not ok`/`FAIL` in der
  vollständigen Ausgabe.
- **geprüft, ohne Befund — `test/mutations/187`/`188` fangen genau die Zeilen, die sie sollen.**
  Über eine isolierte `rsync`-Kopie manuell nachgestellt (Details s. Tabelle oben): 187 entfernt
  alle vier Muster und rötet genau die vier LEITUNG-Bats-Fälle, nicht die zwei BAUM-Fälle; 188
  macht LEITUNG bedingungslos und rötet genau die zwei BAUM-Fälle, nicht die vier LEITUNG-Fälle.
  Beide Richtungen sind damit real geprüft, nicht nur behauptet.
- **geprüft, ohne Befund — keine Lint-Suppression, keine Host-Toolchain.** `grep -rn 'shellcheck
  disable\|nolint'` über allen sechs geänderten/neuen Dateien → leer (§3.2). Alle eigenen Läufe
  ausschließlich über `make`-Targets bzw. direkte, unveränderte `docker run`-Aufrufe aus dem
  Makefile selbst kopiert (§3.9); kein `go`/`pip`/`npm`/`perl` in Befehlsposition verwendet.
- **geprüft, ohne Befund — keine neuen/verschobenen §3.x-Referenzen, `MR-026` nicht berührt.**
  `git diff HEAD -- harness/tools/full-smoke.sh harness/README.md | grep '§3\.'` → leer; alle
  neun `AGENTS.md §3.x`-Nennungen in `full-smoke.sh` (Zeilen 113, 255, 268, 853 ff.) sind
  vorbestehender, unveränderter Text und zeigen korrekt auf **dieses** Repos eigene Nummerierung
  (§3.2, §3.6) — nicht auf die Vorlagen-Nummerierung. Der Diff fügt keine neue Hard-Rule-Referenz
  hinzu, `MR-026`s Adressierungs-Problem (`.claude/hooks/pretooluse-command-guard.sh`) liegt
  außerhalb dieses Diffs.
- **geprüft, ohne Befund — `comment-claims` in diesem Diff nicht ausgelöst, aber auch nicht
  geprüft.** 0 CLAIM-Trigger-Wörter (`garantiert|stellt sicher|bewacht|belegt|sorgt
  dafuer|verhindert`, case-insensitiv) in irgendeiner hinzugefügten Zeile dieses Diffs oder in den
  drei komplett neuen Dateien — das grüne `46/0` bestätigt hier folglich nichts über die
  Robustheit des Gates, weil nichts in diesem Diff seine Schwachstellen berührt. Die drei im
  Auftrag benannten strukturellen Blindstellen von `harness/tools/comment-claims.sh` (nicht Teil
  dieses Diffs, aber im Prüfbereich `harness/tools/*.sh` seiner selbst) sind beim Lesen bestätigt:
  (a) keine Groß-/Kleinschreibungs-Normalisierung (kein `IGNORECASE`/`tolower()` im awk-Skript,
  Zeile 31/60), (b) das Negations-Fenster `[^.]{0,12}` erkennt nur `claim → nicht/kein/nie`
  **vorwärts** und wirkt zeilenweise, nicht instanzgebunden — ein zweiter, negierter Claim auf
  derselben Zeile unterdrückt auch einen ersten, unnegierten, (c) der Roh-String-Zustand
  (Backtick-Parität) wird über **jede** Zeile (Code **und** Kommentar) desselben Prüfbereichs
  fortgeschrieben, obwohl seine Begründung nur Go-Raw-String-Literale nennt.
  `harness/tools/full-smoke.sh:116` demonstriert das an bestehendem, nicht von diesem Diff
  berührtem Code: eine reine Shell-Zuweisung eines einzelnen Backtick-Zeichens an eine Variable
  (kein Kommentar) kippt denselben Zustand, obwohl Bash keine Go-Raw-Strings kennt. INFO, nicht
  MEDIUM/HIGH, weil außerhalb dieses Diffs und von ihm nicht ausgelöst.
- **geprüft, ohne Befund — Mutations-Nummerierung korrekt fortgeführt.** Höchste bereits
  vergebene Nummer zu Beginn dieses Diffs ist 186 (`ls -1 test/mutations/*.sh | sed -n
  's#.*/\([0-9]*\)-.*#\1#p' | sort -n | tail -1` gegen den unveränderten `HEAD`-Bestand), nicht
  die im Plan zitierte 171 vom 2026-08-26 (zwischenzeitlich durch andere Arbeit gewachsen). Die
  neuen Dateien setzen korrekt bei 187 fort — kein Kollisionsrisiko.
- **geprüft, ohne Befund — §3.7-Kommentarstil.** `git diff HEAD | grep '^+' | grep -inE
  'fr(ue\|ü)her\|zuvor stand\|w(ue\|ü)rde\|h(ae\|ä)tte'` findet einen Treffer
  (`test/mutations/187:16`, „Ein voller full-smoke-Lauf wuerde dieselbe Aussage treffen und ueber
  eine Minute kosten") — ein Kostenvergleich zur Begründung der gewählten Sensor-Stufe, keine
  Beschreibung abwesenden/entfernten Texts. Deckt sich mit demselben etablierten Stil in
  `mutate.sh`s eigenem Kopf-Kommentar; kein Befund.
- **geprüft, ohne Befund — Plan-Zeilen §3 „unverändert" stimmen.** `AGENTS.md`,
  `.github/workflows/ci.yml`, `docs/plan/adr/`, `internal/`, `docs/plan/planning/done/`,
  `docs/plan/planning/in-progress/roadmap.md` — keine dieser Dateien steht in `git status
  --porcelain=v1`. Der Diff hält sich an die im Plan vorab getroffene Abgrenzung.

---

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 3 (F-1, F-2, F-3) |
| LOW | 1 (F-4) |
| INFO | 0 (die comment-claims-Beobachtung ist ein Negativbefund, kein Finding — außerhalb des Diffs) |

## Verdikt

**Nicht formal frei — drei MEDIUM vor Merge/Closure zu klären, kein HIGH.** Die Kernmechanik
selbst ist verifiziert korrekt: `full-smoke.sh` bleibt im Grün-Pfad unverändert grün (selbst
gefahren), die neue Unterscheidung funktioniert Ende-zu-Ende für den gemessenen Vorfall
(`test/mutations/189`, von mir gegen eine echte, isolierte `make full-smoke`-Kopie bestätigt:
`AUSGANG LEITUNG` erscheint korrekt, mit Beleg), und die zwei bats-Mutationen (187/188) fangen
tatsächlich genau die Richtung, die sie sollen. Keiner der drei MEDIUM-Befunde stellt diese
Kernaussage infrage — F-1 ist ein Defekt im **Plan-Text** (nicht im Diff: der Diff weicht davon
korrekt ab, zieht die Korrektur aber nicht in den Plan nach), F-2 und F-3 sind Präzisions-/
Abdeckungslücken der **eigenen Zusagen** des neuen Sensors selbst (welche Abschnitte sind wirklich
eingeordnet; wie viele Verdrahtungspunkte haben einen rot gesehenen Beleg) — beide praktisch
risikoarm (die betroffenen Abschnitte sind entweder strukturell nie Docker-berührend, oder der
zugrundeliegende Gate-Fall bleibt korrekt rot, nur die neue Diagnose könnte lautlos verschwinden).
F-4 ist reine Plan-Buchführung. Empfehlung: F-1 (Plan-Text nachziehen oder als bekannte
Diskrepanz im Closure-Eintrag vermerken) und F-3 (die Lücke im Closure-Eintrag als
Steering-Loop-Kandidat benennen, nicht stillschweigend lassen) vor Abschluss klären; F-2 und F-4
sind schnelle Textkorrekturen.
