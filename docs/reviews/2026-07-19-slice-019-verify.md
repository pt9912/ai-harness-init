# Verifikations-Report — slice-019 (Re-Baseline v3.1.0 → v3.5.0, Kurs-Welle 32)

- **Rolle:** Unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Datum:** 2026-07-19
- **Slice:** `docs/plan/planning/in-progress/slice-019-rebaseline-v350.md`
- **Impl-Commit:** `fa41121` (`slice-019: Re-Baseline v3.1.0->v3.5.0 vendoren + Kopplungspunkte (MR-007)`)
- **Baseline-Pin:** `BASELINE_TAG=v3.5.0`, `BASELINE_ZIP_SHA256=123e3383261102e6be6465e1f4bade08a474c00edc4fff89f5c4b11bd640f8ff`

## Gesamt-Verdikt: BESTÄTIGT (0 VIOLATED)

Alle acht inhaltlichen DoD-Zeilen aus §2 **CONFIRMED** — jede unabhängig belegt,
mehrere selbst reproduziert (Provenienz-`curl`, `sha256sum -c`, `SHA256SUMS`-Regeneration,
`make baseline-verify`, `make gates`, Modul-10-Diff, skel-Byte-Vergleich). Die einzige
nicht-abgehakte DoD-Klausel (**Closure-Notiz** mit Steering-Loop-Eintrag) ist **prozessual
nachgelagert** (Closure erfolgt *nach* der Verifikation; Slice liegt noch in `in-progress/`) —
**kein** Verstoß. **Eine** LOW-Beobachtung (DoD-6, slice-019 Zeile 6) ist notiert, aber
gate-neutral und durch §6 ausdrücklich als „Zeitbezug" gedeckt.

---

## DoD-Zeilen (§2) — einzeln

### DoD-1 — Vendored Baum ersetzt (42 Dateien, `SHA256SUMS`, v3.1.0 weg, Setzung 4/2) → **CONFIRMED (selbst reproduziert)**

- **Nur v3.5.0 vorhanden:** `.harness/baseline/` enthält **ausschließlich** `v3.5.0/`; kein
  `v3.1.0/` mehr (Setzung 4 „ein Tag zur Zeit" — im Diff `fa41121` als Delete-old/Add-new belegt).
- **42 Dateien:** `find …/regelwerk -type f` = **21**, `…/templates -type f` = **21** = **42**
  (voller Baum inkl. `SHA256SUMS` = 43) — exakt der v3.1.0-Bestand, Content-Bump statt Datei-Zuwachs.
- **`SHA256SUMS` (Setzung 2) selbst nachgerechnet:** **42 Zeilen**, Datei selbst ausgenommen
  (`grep -c SHA256SUMS` = 0), Pfade relativ zu `<tag>/`. Frisch regeneriert
  (`find regelwerk templates -type f | LC_ALL=C sort | xargs sha256sum`) → **byte-identisch** zur
  committeten Datei. `sha256sum -c SHA256SUMS` = **Exit 0**, alle 42 `OK`.
- **`make baseline-verify` selbst gefahren:** `v3.5.0 OK — 42 Dateien (Integritaet + Vollstaendigkeit,
  netzlos)`, **Exit 0**.
- **„ZIP-sha256 vor dem Entpacken verifiziert":** prozessuale Behauptung — substanziiert durch die
  intakte Provenienz-Kette (DoD-2) **plus** die matchende `SHA256SUMS`-Regeneration (der vendored
  Baum entspricht bit-genau dem, was aus dem gepinnten ZIP kommt).

### DoD-2 — Provenienz + Integrität gepinnt (Setzung 1) → **CONFIRMED (Herkunft selbst gemessen)**

- **`Makefile`:** `BASELINE_TAG ?= v3.5.0` (Z. 25), `BASELINE_ZIP_SHA256 ?= 123e3383…f8ff` (Z. 34),
  `BASELINE_URL` = `…/releases/download/$(BASELINE_TAG)/lab-regelwerk.zip` (Z. 33).
- **Herkunft unabhängig gemessen (stärkster Beleg):** `curl -fsSL
  https://github.com/pt9912/ai-harness-course/releases/download/v3.5.0/lab-regelwerk.zip | sha256sum`
  → **124 486 Bytes**, sha256 = `123e3383261102e6be6465e1f4bade08a474c00edc4fff89f5c4b11bd640f8ff`
  == **Pin**. Das reale, publizierte Release-Asset deckt sich exakt mit dem gepinnten Wert —
  Provenienz-Anker (offizielle URL) und Integritäts-Anker (`SHA256SUMS`) beide geführt (Setzung 1).

### DoD-3 — Kopplungspunkt Fetch → **CONFIRMED**

- `internal/fetch/fetch.go`: `const DefaultTag = "v3.5.0"` (Z. 26).
- **Coupling-Test vorhanden & grün:** `TestDefaultTag_MatchesBaseline` (fetch_test.go:132) liest
  `BASELINE_TAG` aus dem `Makefile` und vergleicht — feuert `make test` rot bei Drift. `make test`
  meldet `ok github.com/pt9912/ai-harness-init/internal/fetch` (im `make gates`-Lauf, s. DoD-„gates").
  ⇒ `DefaultTag == BASELINE_TAG == v3.5.0`.

### DoD-4 — Kopplungspunkt Doku → **CONFIRMED**

- `harness/conventions.md` §Baseline: vendored Tag **`v3.5.0`** (Z. 11–12), Regelwerks-Stand
  **„Kurs-Welle 32 · 2026-07-19"** (Z. 13), kanonische Kurs-URL auf **`tree/v3.5.0/kurs/de`**
  gepinnt (Z. 20). Re-Baseline-Historie ergänzt („auf `v3.5.0`: 2026-07-19 (slice-019)", Z. 16).
- **Deckt sich mit der Baseline:** `.harness/baseline/v3.5.0/regelwerk/README.md:3` = **„Stand:
  Kurs-Welle 32 · 2026-07-19."** — identische Welle/Datum.
- **Historische MR-Bodies eingefroren:** die verbliebenen `v3.1.0`/`v3.0.0`-Nennungen (conventions.md
  Z. 16, Z. 275) sind Zeitbezug in MR-004/006/007-Historie — korrekt **nicht** umgeschrieben.

### DoD-5 — Emit-Embed (drift-test-gesteuert) → **CONFIRMED (Byte-Vergleich + bats grün)**

- `internal/emit/skel/` bettet **15** Dateien ein (`find` = 15). **Alle 15 byte-identisch** zu
  `.harness/baseline/v3.5.0/templates/…` (`cmp -s` je Datei, 0 Mismatches).
- **12 der 15** eingebetteten Templates haben sich v3.1.0→v3.5.0 geändert (Byte-Diff) → der
  Gleichheit-Wächter *musste* feuern; Re-Sync ist vollständig eingezogen.
- **`test/skel-drift.bats` grün** (im `make gates`-Lauf selbst gesehen):
  `ok 51 skel-Embed == vendored Baseline (Gleichheit, kein lokaler Drift)`,
  `ok 52 skel bettet ALLE in-scope-Templates der Baseline ein (Vollstaendigkeit)`,
  `ok 53 skel bettet die Set-Index-README NICHT ein`. Die Vollständigkeit-Achse feuerte
  erwartungsgemäß **nicht** (Dateibestand unverändert). Kein Blind-Sync.

### DoD-6 — slice-019-Template-Reconciliation → **CONFIRMED (strukturell)** · LOW-Beobachtung

- **Struktur-Divergenz selbst gemessen:** `git show HEAD~1:.harness/baseline/v3.1.0/templates/docs/plan/planning/slice.template.md`
  gegen die v3.5.0-Fassung. **Identisch bis auf** die Kurs-`blob/`-Link-Tags (`v3.1.0`→`v3.5.0`) in
  5 Zeilen (10/63/92/107/111). **Kein** Überschriften-/Feld-/Sektions-Unterschied → keine
  strukturelle Übernahme nötig; slice-019 ist strukturell konform (deckt sich mit der
  Commit-Aussage `fa41121`).
- **LOW-Beobachtung (gate-neutral, §6-gedeckt):** slice-019 Zeile **6** (der Lifecycle-Link im
  Slice-Kopf, aus der v3.1.0-Vorlage kopiert) zeigt weiter auf `blob/**v3.1.0**/kurs/…`.
  §6 nennt „slice-019 selbst über die slice.template-Reconciliation" als Beispiel eines Docs, dessen
  Kurs-Link **mitzieht** — hier ist er *nicht* mitgezogen. Das ist **kein VIOLATED**: §6 räumt
  ausdrücklich ein „sonst bleiben sie Zeitbezug" ein, der Link ist gültig (Tag `v3.1.0` existiert
  upstream), **kein** Gate prüft externe URLs, der **einzige** kanonische Pin (conventions.md §Baseline,
  DoD-4) ist gebumpt. Die Zeilen 88/121 tragen `blob/v3.1.0` **zu Recht** (Meta-Prosa, die *über* die
  v3.1.0-Links spricht). Hinweis an Planner: Zeile 6 optional auf `v3.5.0` ziehen, wenn der Slice bei
  der Closure ohnehin angefasst wird.

### DoD-7 — Kopplungspunkt reviewer.md (Stilles-Grün-Klasse) → **CONFIRMED (Begründung substanziiert)**

- `.harness/skills/reviewer.md`: **Version 1.2.0** · Datum 2026-07-19 · Baseline-Label
  **„Agents-Regelwerk v3.5.0 (Kurs-Welle 32), Modul 10 §Ziel-Form"** (Z. 3–5); „fünf
  **v3.5.0**-Pflicht-Punkte" (Z. 15).
- **Begründung „§Ziel-Form substanziell unverändert" unabhängig belegt:** `git show
  HEAD~1:…/v3.1.0/regelwerk/modul-10-review-harness.md` gegen die v3.5.0-Fassung → **8/8
  Überschriften identisch**, gleiche 103 Zeilen; **einziger** inhaltlicher Diff: die Quell-URL-Notiz
  (Tag-Bump) + **ein** Link-Label bekam ein `templates/`-Präfix (Z. 50). Das deckt sich **wörtlich**
  mit der Versionierungs-Notiz in reviewer.md („Überschriften identisch, nur ein Link-Label bekam ein
  `templates/`-Präfix"). Fünf Punkte / Output-Schema / Kategorien-Semantik **unverändert** →
  Versionierung (nicht Überschreiben) korrekt, Begründung tragfähig.

### DoD-8 — README.md:26 + Kurs-Links → **CONFIRMED**

- **README.md Zeile 26** (harter Literal-Pfad, kein generisches `<tag>`): „Templates liegen committet
  vendored unter `.harness/baseline/**v3.5.0**/`" — gebumpt (war die tote v3.1.0-Wurzel).
- **Kurs-Links-Entscheidung dokumentiert:** Plan §6 Bullet „Kurs-Prozess-Links (`blob/v3.1.0/kurs/…`)
  — bewusste Entscheidung": done/-Slices eingefroren, live Docs tragen die Links als *auxiliare*
  Prozess-Referenz (nicht als zweiten Pin), kanonisch ist allein conventions.md §Baseline. Kein
  repo-weiter Zwangs-Bump. Vollständig und konsistent mit MR-007/MR-008.

### DoD-„`make gates` grün (inkl. baseline-verify)" → **CONFIRMED (selbst gefahren, Exit 0)**

`make gates` (warmer Cache, nichts parallel) = **Exit 0**. Sequenzielle Prereq-Kette
`baseline-verify docs-check lint build test shell-lint record-gates` — Exit 0 impliziert **alle** grün:

- **baseline-verify:** `v3.5.0 OK — 42 Dateien (netzlos)`
- **docs-check:** grün (kein Befund; Kette hätte sonst abgebrochen)
- **lint / build:** grün
- **test:** `go test ./...` → `ok cmd/ai-harness-init`, `ok internal/emit`, `ok internal/fetch`
  (⇒ `TestDefaultTag_MatchesBaseline` grün); bats bis `ok 53` inkl. skel-drift 51/52/53
- **shell-lint:** shellcheck über `.claude/hooks/*.sh` + `harness/tools/*.sh`, clean
- **record-gates:** Nachweis geschrieben (`.harness/state/gates-passed.diffsha`, gitignored)

### DoD-„Closure-Notiz mit Steering-Loop-Lerneintrag" → **prozessual offen (kein Verstoß)**

§7 Closure-Notiz ist noch leer (`<!-- Erst nach Abschluss füllen. -->`), DoD-Checkboxen ungehakt —
**erwartungsgemäß**: Closure ist der Schritt *nach* der Verifikation (Slice in `in-progress/`; Task
„Closure + Move nach done/" offen). **Hinweis an Planner:** §7 mit Steering-Loop-Lerneintrag füllen
und DoD abhaken vor `git mv` nach `done/`.

---

## ADR- / Hard-Rule-Konformität (unabhängig geprüft)

- **ADR-0001 (Fetch vom gepinnten Kurs-Tag):** Der Picker pinnt den Tag aus conventions.md §Baseline;
  `fetch.DefaultTag = v3.5.0` == `BASELINE_TAG` == §Baseline `v3.5.0`, per go-Test gekoppelt.
  Folgepflicht (conventions.md §Baseline hält den Tag) erfüllt. **Konform.**
- **Hard Rule 3.3 (git mv + Inhalt = zwei Commits):** Die slice-**Datei**-Moves sind **eigene**
  Commits mit sauberer Rename-Detection: `52cfd78` (Rename v340→v350, `R100`, getrennt vom
  Content-Retarget `2ecf8cf`) und `0b6c6da` (Move open→in-progress, `R100`). Der **Baseline-Replace**
  (`fa41121`) ist **eine logische MR-007-Ersetzung** des ganzen Baums (Content-Replace, kein
  Rename+Edit-Splitting nötig — 40 Baseline-Dateien als Rename-mit-Änderung, 2 Delete + 2 Add wo die
  Similarity unter die Schwelle fiel). **Konform.**
- **Hard Rule 3.1 / LH-QA-01 (keine halluzinierten Gates):** Alle in `make gates` genannten Gates
  laufen auf diesem Checkout grün; kein neuer Gate-Name in AGENTS.md §4 / harness/README.md / Makefile.
  **Konform.**
- **Hard Rule 3.5 (Gates nicht ohne ADR lockern):** Keine Schwellen-Senkung / Modul-Deaktivierung im
  Diff — reiner Content-/Pin-Bump. **Konform.**
- **Hard Rule 3.4 (ADRs immutable):** Kein ADR im Diff geändert. **Konform (N/A).**
- **MR-007 Setzung 1–4:** Provenienz≠Integrität (beide geführt, DoD-2), `SHA256SUMS`-Umfang (Setzung 2,
  selbst regeneriert), Vollständigkeits-Check (Setzung 3, `baseline-verify` grün), ein-Tag-zur-Zeit
  (Setzung 4, nur v3.5.0). **Alle vier eingehalten.**

## Beobachtungen (nicht abnahme-relevant)

1. **Content-Bump-Framing verifiziert:** **21/21** regelwerk-Module und **15/21** Templates differieren
   v3.1.0→v3.5.0 (eigener Byte-Diff des aus `HEAD~1` extrahierten Baums) — exakt die §1-Zahlen; belegt,
   warum das Emit-Embed (12 betroffen) nachzuziehen war.
2. **DoD-6 Zeile 6** (slice-019-Lifecycle-Link auf `blob/v3.1.0`): s. DoD-6, LOW/gate-neutral.

## Tree-Zustand

`git status --short` zeigt nach Abschluss nur untracked Reports: `docs/reviews/2026-07-19-slice-019-review.md`
(vom Reviewer, bereits bei Verify-Beginn vorhanden) und dieser Verify-Report. **Keine** getrackten
Änderungen durch den Verify-Lauf (`record-gates` schreibt nur ins gitignorierte `.harness/state/`;
der Provenienz-`curl` lief in eine temp-Datei außerhalb des Repos). Tree sauber auf `HEAD` (`fa41121`).
