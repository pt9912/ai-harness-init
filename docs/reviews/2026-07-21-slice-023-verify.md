# Verifikation — slice-023 (Go-Skelett-Generator, deterministisch)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths` / ids-`exempt-paths`) —
> bare IDs/Pfade hier ok. Bewusst ohne Markdown-Links, damit `links`/`anchors` nichts findet.

## Kopf-Metadaten

- **Rolle:** unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Gegenstand:** DoD-Behauptung von slice-023 (8 Punkte, §2) gegen realen Zustand + Spec (LH-FA-04/LH-QA-01/LH-QA-02) + Plan + ADR/Hard Rules. Der Verifier bestätigt DoD+Spec+Plan; der Reviewer (getrenntes Artefakt, 2026-07-21-slice-023-review.md) prüfte gegen Plan+ADR.
- **Slice:** docs/plan/planning/in-progress/slice-023-skelett-generator.md (noch in-progress/, DoD-Boxen offen — Verify VOR Closure).
- **Impl-Commits:** a8f94cf (reiner Move open->in-progress, 0/0) · 08e50e3 (Generator statt Fetch: internal/gen neu, internal/fetch/fetch.go gelöscht, cmd verdrahtet, Mutationen 15-17) · 6853ccc (Verfeinerung: Pins entdoppelt, reiche .golangci.yml, SKEL_GO_VERSION-Knopf, make smoke Schritt 5 E2E, Mutationen 18-20) · b86806c (Review-LOW-Fixes F-1/F-2). Review-Report 52c9441. HEAD=b86806c, tracked Tree clean.
- **Berührte Dateien (447750a..b86806c):** cmd/ai-harness-init/main.go (+Test), internal/gen/{gen.go,golang.go,gen_test.go} (neu), internal/fetch/fetch.go (gelöscht, 160 Zeilen) + fetch_test.go + baseline.go, harness/tools/smoke.sh, test/mutations/15-20, docs-Link welle-02, docs/reviews/2026-07-21-slice-023-review.md. KEINE ADR-Datei, KEINE Dogfood-Gate-Datei (.golangci.yml/Dockerfile/Makefile/*.mk/.d-check.yml/.github/).
- **Selbst ausgeführt (belegt, nicht nur nachgelesen):**
  - `make gates` SELBST -> **Exit 0**; alle Gates real (baseline-verify, docs-check, lint, build, test, shell-lint, ci-lint, record-gates). `go test ./...` alle 4 Pakete ok inkl. internal/gen. git status vor UND nach leer.
  - `make mutate` SELBST -> **Exit 0, 20 ok / 0 Befund**; git status vor UND nach leer (mutate.sh restauriert). Die 6 neuen Wächter (15-20) färbten je ihren benannten Test rot (siehe DoD-Tabelle). Grün-Vorlauf fuhr `make test` + `make ci-lint` + `make smoke` real grün VOR der ersten Mutation.
  - `make smoke` SELBST (standalone, Host-Docker + Netz) -> **Exit 0**; Schritt 5 (`make -C .harness/skeleton gates` = lint/build/test des GENERIERTEN Skeletts) real grün.
  - **Reale Binary-Probe** (artifact-Stage extrahiert): `--lang rust` -> "unbekannte Sprache "rust"; verfuegbar: go", **Exit 2**, kein .harness/skeleton angelegt (fail-fast vor Write); fehlendes `--lang` -> Exit 2 + Usage.
  - `git diff 447750a..b86806c` (Namens- + Pfad-Diffs für ADR/Gate-Dateien/Suppression), statische Determinismus-Belege in internal/gen/gen.go (sort.Strings vor Write; statische Konstanten), `grep fetch.Skeleton` (0 Treffer).

## DoD-Verdikt je Zeile

### DoD-1 — LH-FA-04 (Generator): `--lang go` erzeugt das Skelett aus dem Layout-Profil, Test referenziert — **CONFIRMED**

gen.Generate (gen.go:41) schlägt lang in profiles() nach ("go" -> goProfile) und schreibt das Profil. goProfile (golang.go:28) emittiert GENAU fünf Dateien: go.mod, Dockerfile, Makefile, .golangci.yml, cmd/app/main.go. TestGenerate_GoProfile (gen_test.go:30) prüft den vollen Ist-Bestand (`want := {.golangci.yml, Dockerfile, Makefile, cmd/app/main.go, go.mod}`) — §3.6-„vollständiger Ist-Bestand", kein Namens-Proxy; Mutation 16 (Datei aus dem Profil) rötet ihn (selbst gesehen). **Real geprüft:** make smoke Schritt 2/3 generierte das Skelett in ein frisches Ziel-Repo, .harness/skeleton/Makefile lag da. Die Anforderung ist Generator (LH-FA-04 §7 v0.7.0), nicht mehr Fetch/Picker — Titel-Anker historisch, korrekt so gewürdigt.

### DoD-2 — LH-QA-02 (Determinismus): zwei Läufe gleiche Eingabe -> byte-identisch — **CONFIRMED**

**Konstruktion:** Der Profil-Inhalt ist vollständig STATISCH (Go-Konstanten goMain/goDockerfileTmpl/goMakefileTmpl/goGolangci; kein Zeitstempel, keine Map-Iteration im Datei-INHALT). Generate schreibt in sortierter Reihenfolge (`sort.Strings(rels)`, gen.go:51) — kein Map-Iterations-Leak in die Schreib-Ordnung. **SKEL_GO_VERSION deterministisch:** der Default ist ein gepinnter const DefaultGoVersion = "1.26.4" (golang.go:11), der Override ist ein vom Nutzer GENANNTER Wert (envOr, main.go:130) — kein Netz, kein Datum, kein "latest"-Lookup (der ist bewusst ein Folge-Slice, main.go:128-129). **„byte-identisch" belegt:** TestGenerate_Deterministic (gen_test.go:44) generiert zweimal und vergleicht den VOLLEN Datei-Satz byte-weise (`if a != b`), nicht nur „existiert" — genau die vom Plan §6 verlangte Prüfung „zwei volle Läufe, nicht nur Datei existiert". Per Konstruktion existiert kein rot-färbendes sed-Gegenbeispiel (Review-F-3, INFO); der Wächter ist bewusst unbewacht in gen.go benannt — kein Defekt, sondern eine korrekt deklarierte Grenze des mutate-Sensors.

### DoD-3 — LH-QA-01 (nur lauffähige Targets): das generierte Makefile behauptet nur lauffähige Targets — **CONFIRMED (make smoke Schritt 5 SELBST gefahren)**

Zwei-Schichten-Beleg, beide selbst gefahren:
- **Statisch:** TestGenerate_MakefileTargetsMatchStages (gen_test.go:63) koppelt jedes `--target <X>` im Makefile strukturell an eine `AS <X>`-Stage im Dockerfile (fail bei `len(targets)==0` -> zahnlos ausgeschlossen). Mutation 15 (`AS test`->`testx`) rötet ihn (selbst gesehen). Das schließt „Target ohne Stage" = halluziniertes Gate.
- **Real (der neue Kern-Beleg):** `make smoke` SELBST -> Exit 0; Schritt 5 fuhr `make -C .harness/skeleton gates` (lint/build/test) im frisch generierten Ziel-Repo REAL über Host-Docker -> „smoke: OK — Skelett generiert + eigene Go-Gates gruen". Damit ist belegt, dass die kuratiert-reiche .golangci.yml + main.go + Dockerfile + go.mod ZUSAMMENPASSEN und das generierte Skelett sich grün lintet/baut/testet. (Schritt 4 meldet 5 d-check-Befunde im emittierten Template-Satz — erwartet und dokumentiert: 0-Befunde-out-of-the-box ist LH-FA-01/slice-005, nicht dieser Slice; kein DoD-023-Bruch.)

### DoD-4 — ADR-0003 (Docker-only): generiertes Skelett Docker-only, keine Host-Toolchain-Annahme — **CONFIRMED**

golang.go: das Dockerfile trägt die Stages deps/test/lint/build; das Makefile ruft ausschließlich `docker build --target <stage>` (test/lint/build/gates). Keine Host-`go`-/Host-golangci-Annahme. Real belegt: make smoke Schritt 5 fuhr die Skelett-Gates rein über docker (der generierte Makefile-Stack ruft docker build). Konform zu ADR-0003 (Docker-only-Build, kein Host-`go`).

### DoD-5 — sprach-agnostisch (ein Profil je Sprache), go zuerst, die übrigen fünf ohne Umbau — **CONFIRMED**

profiles() (gen.go:79) ist `map[string]func(goVersion string) map[string]string` mit heute einem Eintrag ("go" -> goProfile). Eine neue Sprache ist ein neuer Map-Eintrag + Profil-Builder, kein Umbau von Generate/SupportedLangs/UnknownLangError-Mechanik. Die Reduktion 6->1 ist Plan-DoD-konform („go ist das erste, die übrigen fünf aus LH-FA-04 folgen ohne Umbau der Mechanik", §2/§3); LH-FA-04-Vollumfang (python/kotlin/java/csharp/cpp) ist ausdrücklich Folge-Slice-Sache, kein Slice-023-Defekt.

### DoD-6 — Fetch abgelöst + `--lang`-Validierung übernommen — **CONFIRMED (real geprüft)**

internal/fetch/fetch.go ist gelöscht (160 Zeilen weg im Diff); `grep fetch.Skeleton` über das Repo = 0 Treffer (auch kein toter Kommentar mehr — Review-F-2 in b86806c behoben, main_test.go:185 nennt jetzt „gen.Generate (Phase 2)" statt des gelöschten Symbols). Die --lang-Validierung wanderte vom Fetch zum Generator: gen.UnknownLangError (gen.go:25) + langExitCode (main.go:238) bilden unbekannte Sprache auf Exit 2 ab, sonst 1. **Real geprüft:** die extrahierte Binary lief `--lang rust` -> „unbekannte Sprache "rust"; verfuegbar: go", Exit 2, und legte trotz Fehler KEIN .harness/skeleton an (fail-fast VOR dem ersten Write, main.go:130-133). Mutation 17 (Validierung tot) rötet TestGenerate_UnknownLang (selbst gesehen). Die Validierung ist nicht ersatzlos verschwunden.

### DoD-7 — `make gates` grün — **CONFIRMED (selbst gefahren, Exit 0)**

`make gates` SELBST einmal gefahren -> **GATES_EXIT=0**. Alle Gates real: baseline-verify (netzlos), docs-check (d-check per Digest), lint (golangci-lint), build (cross-compile), test (bats + `go test ./...`: cmd/emit/fetch/**gen** alle ok), shell-lint (shellcheck), ci-lint (actionlint), record-gates. git status vor UND nach dem Lauf leer.

### DoD-8 — Closure-Notiz mit Steering-Loop-Lerneintrag — **CONFIRMED (korrekter Lifecycle-Zustand vor Closure)**

§7 des Slice ist noch das leere Template (`<!-- Erst nach Abschluss füllen. -->`); der Slice liegt in in-progress/, DoD-Boxen offen. Das ist ein POST-Closure/Planner-Artefakt: Verify läuft VOR der Closure. Kein Defekt zum Verify-Zeitpunkt (identisch zum Lifecycle in slice-025-verify DoD-8). Nur bestätigt — die substanzielle Zusage (Steering-Loop-Eintrag) wird beim done/-Move ergänzt.

## Hard Rules / ADR

- **§3.1 (keine halluzinierten Gates):** CONFIRMED. Kein neues Gate-Target im Dogfood-Makefile (Diff berührt Makefile nicht); die Mutationen 15-20 speisen `make mutate` (Nicht-Gate). Das GENERIERTE Makefile behauptet nur Target=Stage-gekoppelte Gates (DoD-3, smoke Schritt 5 real).
- **§3.2 (keine Lint-Suppression im Diff):** CONFIRMED. Im Diff über Produktivcode/Skripte (cmd/internal/harness/test/*.go) 0 Treffer für `//nolint`/`# shellcheck disable`/`d-check:ignore`. Der einzige `d-check:ignore` im Range liegt im Review-Report (doc-gate-exempt); die emittierte .golangci.yml ist Enable-Config, keine Inline-Suppression.
- **§3.3 (Move rein vom Inhalt getrennt):** CONFIRMED. a8f94cf = 100%-reiner Rename {open => in-progress} (0 insertions / 0 deletions, `git show --stat`); 08e50e3/6853ccc/b86806c tragen den Inhalt. Der welle-02-Link-Nachzug liegt im Inhalts-Commit.
- **§3.4 (keine ADR-Mutation):** CONFIRMED. `git diff --name-only 447750a..b86806c -- docs/plan/adr/**` = leer.
- **§3.5 (keine Gate-Lockerung ohne ADR):** CONFIRMED. Dogfood-.golangci.yml/Dockerfile/Makefile unberührt im Diff. Die emittierte, kuratiert-leichtere Config (ohne repo-eigenes forbidigo/gomodguard) ist Emitter-Design für ein FREMDES Ziel-Repo, keine Senkung eines Dogfood-Gates.
- **§3.6 (keine Zusage weiter als der Code):** CONFIRMED. Jeder gelistete Wächter (15-20) hat im selbst gefahrenen mutate-Lauf Zähne gezeigt; der einzige nicht-mutations-bewachte Wächter (TestGenerate_Deterministic) ist per Konstruktion nicht rot-färbbar und ehrlich als solcher benannt. Der Tag-ohne-Digest-Tradeoff (golang.go) ist als bewusste Wahl benannt, nicht als „gepinnt = Digest" überzugesagt.
- **ADR-0005-Konformität:** CONFIRMED. Das Skelett wechselt auf die Herkunftsklasse „Deterministisch generiert, Tool-als-Quelle" (ADR-0005-Tabelle Zeile 2); Fetch bleibt für Regelwerk+Templates+Baseline (main.go Phase 2 fetch.Baseline unberührt). Kein neues ADR nötig — der Slice implementiert die von ADR-0005 (Accepted) bereits getroffene Entscheidung; „keine ADR im Slice" ist korrekt.
- **ADR-0003-Konformität:** CONFIRMED (siehe DoD-4).

### F-4-Würdigung (architecture.md-Drift) — kein DoD-/Spec-Verstoß, legitime deferred Reconciliation

architecture.md §1 (Flowchart-Knoten `Template- und Skelett-Fetcher`, `Sprachskelett-Picker`), §2 (Schichten-Tabelle „Fetcher … Skelett … holen", „Picker … verdrahten") und §4 (Sequenz „Templates + Skelett holen (gepinnt)") beschreiben das Skelett WEITER als gefetcht/gepickt — real geprüft, die Datei liegt NICHT im Diff. ADR-0005 (Accepted) trägt „Schärft: architecture.md §Komponenten". **Würdigung:** Dies ist KEINE DoD-Verletzung — keiner der 8 DoD-Punkte verlangt die architecture.md-Reconciliation; der Slice-Scope ist ausdrücklich „erzeugt das Skelett, verdrahtet es nicht" (§6) und auf die vier Plan-Dateien begrenzt. Es ist auch KEIN Spec-Verstoß im Sinne von Modul 11: architecture.md ist rank-2-Spec, der ADR-0005 (rank-1 nach Source Precedence) bei Konflikt VORGEHT — und ADR-0005 hat die Klasse bereits auf „Generator" umgestellt. Die Prosa-Divergenz ist eine bewusst getrennte Doku-Reconciliation (Muster MR-005 „Offen — Reconciliation" / slice-017), die der Architect klärt (Modul 11: „Reviewer/Verifier prüft gegen veralteten Plan … Architect klärt"). **Empfehlung (kein Blocker):** architecture.md §1/§2/§4 in einem Folge-/Doku-Slice auf das Generator-Modell nachziehen, damit die rank-2-Spec nicht rottet. F-5 (Tag-Pin ohne Digest) ist mit LH-QA-02 („Tag/Digest — kein floating main") konform: Tag-Pinning erfüllt die Zusage; der bewusste Digest-Verzicht hält den GO_VERSION-Knopf wirksam und ist dokumentiert — kein DoD-Bruch.

## Gesamt-Verdikt

**8/8 DoD-Zeilen CONFIRMED — 0 VIOLATED.** Zwei Nuancen, beide kein Blocker:

1. **DoD-8 (Closure-Notiz):** zum Verify-Zeitpunkt bewusst leer (Post-Closure-Planner-Schritt; Slice in in-progress/) — kein Defekt, sondern der korrekte Lifecycle-Zustand vor der Closure.
2. **F-4 (architecture.md-Drift):** rank-2-Spec beschreibt das Skelett weiter als Fetcher/Picker; ADR-0005 (rank-1) geht vor und hat die Klasse bereits auf Generator umgestellt. Legitime deferred Doku-Reconciliation (Architect), keine DoD-/Spec-Verletzung dieses Slice.

Zentrale Sensor-Belege selbst gefahren: `make gates` = Exit 0 (alle Gates real, go test inkl. internal/gen ok, Baum clean); `make mutate` = Exit 0, 20 ok / 0 Befund, die 6 neuen Wächter 15-20 je ihren benannten Test rot gesehen; `make smoke` = Exit 0, Schritt 5 (generierte Go-Gates lint/build/test real grün im frischen Ziel-Repo); reale Binary-Probe `--lang rust` -> Exit 2 + Profil-Liste, kein Teil-Write. Hard Rules §3.1-§3.6, ADR-0003 und ADR-0005 konform.

## Empfehlung

**Verifikation bestanden — Closure freigegeben.** Beim done/-Move: (1) die 8 DoD-Boxen abhaken, (2) die Closure-Notiz (§7) mit dem Steering-Loop-Eintrag füllen (die Lehre: Skelett von Fetch auf deterministischen Generator umgestellt, Determinismus per statischer Konstruktion + sortierter Schreibordnung statt Test-allein, SKEL_GO_VERSION als deterministischer Knopf, smoke Schritt 5 als realer Target-Lauffähigkeits-Beleg). Getrennt (kein Blocker für diese Closure): architecture.md §1/§2/§4 auf das Generator-Modell nachziehen (F-4, Architect/Doku-Slice). Alle drei Sensor-Läufe (gates/mutate/smoke) sind in DIESER Umgebung real grün belegt, nicht nur berichtet; Baum am Ende clean.
