# Review-Report: slice-094 (Code) — 2026-08-25

**Review-Art:** **Code**, erste Runde — geprüft wird der Diff gegen den Slice-Plan, gegen die
referenzierte Accepted-ADR (nur gelesen, nicht angefasst) und gegen die Hard Rules
(Modul 10 §Drei Review-Arten). Kein vorheriger Review-Lauf zu diesem Slice.

**Gegenstand:** `9bed2d7` — **ein** Commit, **21 Dateien**
(`git show 9bed2d7 --stat` → `354 insertions(+), 218 deletions(-)`). Baum sauber vor und nach
jeder eigenen Probe (`git status --short` → leer; zwei versehentliche Seiteneffekte einer eigenen
Sonde wurden noch in derselben Runde entdeckt und per `git checkout --` bzw.
`git worktree remove` zurückgenommen, siehe unten).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0
**Modell:** claude-sonnet-5 · **Datum:** 2026-08-25

**Eingangs-Kontext:**

- **Diff:** `9bed2d7`, `git show 9bed2d7` im Volltext, plus gezielte `git diff -M<pct>`-Läufe für
  die Rename-Erkennung
- **Slice-Plan:**
  [slice-094](../plan/planning/in-progress/slice-094-ein-programm-ein-einstiegspunkt.md)
  (Stand `in-progress/`, vollständig gelesen)
- **ADR, gelesen als Constraint, nicht angefasst:**
  [ADR-0022](../plan/adr/0022-erfassungsschicht-traeger-aus-dem-produkt-binaer.md) (Accepted,
  Festlegung 2 + Folgepflichten 1, 2, 3, 8) — im Diff nicht berührt
  (`git diff --stat 9bed2d7^ 9bed2d7 -- docs/plan/adr/` leer); ergänzend
  [ADR-0011](../plan/adr/0011-telemetrie-erfassung-policy.md) Festlegung 4+6,
  [ADR-0015](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md) (Rollen-Eigentum an
  Norm-Artefakten, zur Einordnung der gemeldeten Grenzüberschreitung)
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §3.1, §3.2, §3.3, §3.4, §3.5, §3.6, §3.7, §3.8
- **Konventionen:** [`harness/conventions.md`](../../harness/conventions.md) MR-005, MR-025
- **Vorherige Findings am gleichen Modul:** keine zu `cmd/ai-harness-init/` oder
  `harness/tools/span-check.sh` in dieser Fassung; slice-093-Review (23.08.) betraf
  `harness/tools/mutate.sh` — anderer Gegenstand
- **Regelwerk `v3.5.2`:** Index gelesen; `modul-10-review-harness.md` und `modul-08-agentenrollen.md`
  on-demand

---

## Selbst gefahren (nichts aus Commit-Message oder Übergabe übernommen)

| Kommando / Probe | Ergebnis | Behauptung | Urteil |
|---|---|---|---|
| `make test-go` | Exit 0, sieben Pakete `ok` | Commit: „sieben Go-Pakete ok" | **stimmt**, selbst gefahren |
| `make gates` (frischer Lauf, Docker) | Exit 0; `d-check: 370 Datei(en) geprüft, 0 Befund(e)`; `golangci-lint: 0 issues.`; bats `144` `ok`; `comment-claims: 40 Datei(en) geprueft, 0 Befund(e)`; `span-check: Traeger vorhanden, span-emit hat einen Span geschrieben, Ablageort git-ignoriert` | Commit: „make gates Exit 0 (d-check 370/0, lint 0 issues, bats 144 ok, sieben Go-Pakete ok, comment-claims 40/0, span-check gruen)" | **stimmt wörtlich**, selbst gefahren |
| `git diff -M50% 9bed2d7^ 9bed2d7 --summary` | `span-emit/main.go`→`ai-harness-init/span_emit.go` als **rename (55%)**, `main_test.go` als **rename (62%)**; `span-report/main.go`/`main_test.go` dagegen als **create+delete** (keine Rename-Zeile) | — (nicht behauptet, eigene Prüfung von Hard Rule 3.3) | **Befund F-1** — siehe unten |
| `git diff -M1% 9bed2d7^ 9bed2d7 --summary` | dieselben vier Dateien jetzt mit ihrer tatsächlichen Ähnlichkeit: `span-report/main.go→span_report.go` **(22%)**, `main_test.go→span_report_test.go` **(45%)** — beide unter der Default-Schwelle 50% | — | bestätigt F-1 quantitativ |
| `git log --follow --oneline -- cmd/ai-harness-init/span_report.go` | **nur** `9bed2d7` | — | bestätigt F-1: Historie zu `edf739d`/`0f41911` (slice-066) ist ab hier nicht mehr auffindbar |
| `git log --follow --oneline -- cmd/ai-harness-init/span_emit.go` | `9bed2d7`, `03e171d`, `f017941`, `f88feed`, `01fe699` (bis slice-059 zurück) | — | Kontrastprobe: für die Datei, deren Ähnlichkeit über der Schwelle liegt, funktioniert `--follow` wie erwartet |
| `grep -c 'internal/span' test/mutations/*.sh \| wc -l` / dieselbe Zeile für `internal/report` | **25** / **10** | Plan DoD (2): „25 plus 10 Mutations-Fälle zeigen auf Pfade, die der Umbau bewegt" | Zahlen **bestätigt** — aber s. u., ihre Richtung war falsch |
| `git diff --stat 9bed2d7^ 9bed2d7 -- test/mutations/` | genau `107-…sh` und `112-…sh` geändert, `154-…sh` neu — **keine** weitere Mutationsdatei berührt | Commit: „Bewegt haben sich genau ZWEI Fälle" | **stimmt** |
| `git diff 9bed2d7^ 9bed2d7 -- internal/span/span.go` | nur ein Kommentar-Satz geändert, Pfad unverändert | Commit: „internal/span und internal/report sind bereits interne Pakete … ein Umzug wäre Arbeit ohne Gegenstand" | **stimmt** |
| `git diff --stat 9bed2d7^ 9bed2d7 -- internal/report/` | leer | dito | **stimmt** |
| Isolierter Lauf von `test/mutations/107-span-klemme-entfernt.sh` in einem `git worktree` + `docker build --target test` | `--- FAIL: TestClampSurvivesBrokenPayload`, `Schreiber endete nicht mit 0: exit status 2` | Commit: „fehlender Träger … färben span-check rot" (107/112 re-verankert und funktionsfähig) | **stimmt, mechanisch reproduziert** |
| Isolierter Lauf von `test/mutations/154-unterkommando-routing-vertauscht.sh` in einem `git worktree` + `docker build --target test` | `--- FAIL: TestClampSurvivesBrokenPayload` **und** `--- FAIL: TestEmitWritesSpanFromHook` (stdout trägt die `span-report`-Bilanz statt leer zu bleiben) | Commit: „faerbt TestClampSurvivesBrokenPayload und TestEmitWritesSpanFromHook rot" | **stimmt wörtlich, mechanisch reproduziert** |
| `grep -niE 'kein[e]? subkommando\|eigenes binary' Dockerfile Makefile cmd/ internal/ harness/` | **0** Treffer | Plan DoD (3): vier Zeilen in zwei Dateien sollen verschwinden | **bestätigt, vollständig entfernt** |
| Diff-Lesung der zwei Planner-Artefakte (`slice-094`-Plan, `slice-071`) Zeile für Zeile | 5 von 6 Link→Inline-Code-Umwandlungen textgleich; **1 von 6** ändert den sichtbaren Text (`cmd/span-report` → `cmd/span-report/main.go`) | Commit: „ausschliesslich die Link-FORM repariert … Text wortgleich" | **Befund F-2** — siehe unten |
| `grep -rn "span-emit\|span-report" Dockerfile Makefile .claude/settings.json harness/tools/*.sh` | alle verbleibenden Vorkommen konsistent mit dem neuen Layout (`ai-harness-init span-emit`, `$(HOST_BIN)`) | — | geprüft, kein Befund |
| `sed -n '227-249' AGENTS.md` gegen `## 3. Harte Regeln` (Z. 51–227) | die geänderten Zeilen (236–237) liegen in §4 Quality Gates, nicht in §3 | Commit: „Hard Rules aus Paragraph 3 sind unberuehrt (3.8)" | **stimmt** |
| `git show 9bed2d7 \| grep '^+' \| grep -iE 'nolint\|shellcheck disable\|d-check:ignore'` | leer | — (§3.2) | geprüft, kein Befund |

**Zwei eigene Seiteneffekte, sofort korrigiert:** eine erste Isolations-Sonde für Fall 154 lief
versehentlich gegen den echten Arbeitsbaum statt gegen ein `git worktree` (Ursache: ein
Hintergrund-Aufruf vor dem `cd` in die Kopie) und hinterließ eine einzeilige Mutation in
`cmd/ai-harness-init/main.go`; erkannt über `git status --short`, sofort mit
`git checkout -- cmd/ai-harness-init/main.go` zurückgenommen (`git status --short` danach leer).
Alle folgenden Proben liefen ausschließlich in `git worktree add --detach`-Kopien außerhalb des
Arbeitsbaums, mit `docker build --target test` statt Host-`go` (der PreToolUse-Guard blockt
Host-Toolchains ohnehin, §3.1 dieses Repos). Beide Kopien wurden per
`git worktree remove --force` entfernt; `git status --short` und `git log --oneline -1` (→
`9bed2d7`) am Ende bestätigen den Ausgangszustand.

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills.

### F-1 — Hard Rule 3.3 verletzt: `cmd/span-report` verliert seine Git-Historie beim Umzug

- `kategorie`: **HIGH**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.3 („git mv + Inhaltsänderung = zwei Commits — Move
  und Rewrite getrennt committen, sonst fällt die Rename-Detection unter die
  Similarity-Schwelle")
- `pfad`: `cmd/span-report/main.go` → `cmd/ai-harness-init/span_report.go`,
  `cmd/span-report/main_test.go` → `cmd/ai-harness-init/span_report_test.go` (im selben Commit
  `9bed2d7` verschoben **und** umgeschrieben)
- `befund`: Für die vier verschobenen Dateien dieses Commits behandelt Git zwei als Rename
  (`cmd/span-emit/main.go`→`span_emit.go` 55 %, `main_test.go`→`span_emit_test.go` 62 %,
  `git diff -M50% 9bed2d7^ 9bed2d7 --summary`), die anderen zwei nicht: bei der Default-Schwelle
  50 % erscheinen `cmd/span-report/main.go`→`span_report.go` und `main_test.go`→
  `span_report_test.go` als **`delete mode` + `create mode`**, nicht als `rename`. Gemessen mit
  `git diff -M1% 9bed2d7^ 9bed2d7 --summary` liegt ihre tatsächliche Ähnlichkeit bei **22 %** bzw.
  **45 %** — beide unter der Schwelle, exakt die Situation, vor der die Hard Rule warnt. Das ist
  kein theoretisches Risiko: `git log --follow -- cmd/ai-harness-init/span_report.go` findet
  **nur** `9bed2d7` und bricht dort ab; dieselbe Abfrage für die Schwester-Datei
  `cmd/ai-harness-init/span_emit.go` (deren Ähnlichkeit über der Schwelle lag) trägt die Historie
  bis `01fe699` (slice-059) korrekt weiter. Die ursprüngliche Autorschaft, Begründung und
  Review-Historie von `span-report` (`edf739d`/`0f41911`, slice-066: „Token-Bilanz je Rolle") ist
  ab diesem Commit über den neuen Pfad **nicht mehr auffindbar**.
- `verifizierbar`: **ja** — `git diff -M1% 9bed2d7^ 9bed2d7 --summary` und
  `git log --follow -- cmd/ai-harness-init/span_report.go` reproduzieren den Befund direkt, ohne
  Docker/Netz.

### F-2 — Selbstbericht zur Link-Reparatur ungenau: eine von sechs Stellen ändert sichtbaren Text

- `kategorie`: **MEDIUM**
- `quelle`: Rollen-Grenze (Modul 8) / Selbstbericht-Genauigkeit — kein Hard-Rule-Verstoß, da kein
  Artefakt namentlich Implementer von Planner-Slice-Dokumenten ausschließt
  ([ADR-0015](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md) begrenzt sich ausdrücklich
  auf `AGENTS.md` §3 und den Adaptions-Block, „über die übrigen Norm-Artefakte trifft diese ADR
  keine Aussage")
- `pfad`: [slice-071](../plan/planning/open/slice-071-bilanz-nennt-ihren-bestand.md), DoD (1)
  „Rot:"-Zeile
- `befund`: Der Commit behauptet, die sechs docs-check-brechenden Links seien „ausschliesslich" in
  der **Form** repariert worden (Link → Inline-Code, „Text wortgleich, keine Aussage geaendert").
  Gemessen an `9bed2d7^` gegen `9bed2d7`: fünf der sechs Stellen halten das exakt ein. Die sechste
  (slice-071 DoD (1), „Rot:"-Zeile) ändert den sichtbaren Text zusätzlich zur Link-Entfernung — aus
  dem Link-Label `cmd/span-report` (Ziel: `.../main.go`) wird der Inline-Code
  `` `cmd/span-report/main.go` ``. Der neue Text ist präziser (er nennt jetzt die tatsächliche
  Zieldatei), aber er ist nicht das Label, das vorher stand — die Selbstauskunft „Text wortgleich"
  trifft auf diese eine von sechs Stellen nicht zu. Der Auftrag verlangt ausdrücklich, das am Diff
  zu prüfen statt an der Behauptung; genau hier weicht die Behauptung vom Diff ab.
- `verifizierbar`: **ja** — `git diff 9bed2d7^ 9bed2d7 --
  docs/plan/planning/open/slice-071-bilanz-nennt-ihren-bestand.md` zeigt die Zeile direkt.

## Negativbefunde

- **geprüft, ohne Befund — die Klemme als erste Anweisung (Prüfpunkt 2, ADR-0011 F4+F6).** Der
  Unterkommando-Zweig ist tatsächlich die erste Anweisung in `main()`. Kein `init()` existiert in
  `cmd/` oder `internal/` (`grep -rn '^func init(' cmd/ internal/` → leer), keine
  paket-globale `var`-Deklaration mit Seiteneffekt (`grep -n '^var ' cmd/ai-harness-init/*.go` →
  nur `const`-Strings). Es gibt also keinen Pfad, auf dem etwas vor der Klemme fehlschlagen könnte
  oder `span-emit`/`span-report` durch `run()` liefen — bestätigt durch den expliziten
  `run()`-Kommentar „GRENZE: … erreichen diese Funktion nicht" und durch drei eigene isolierte
  Mutationsläufe (107, 112, 154), die alle am erwarteten Test rot wurden.
- **geprüft, ohne Befund — AGENTS.md §4 und spec/spezifikation.md §5 (Prüfpunkt 3).** Die
  `AGENTS.md`-Änderung liegt vollständig in §4 (Quality-Gates-Tabelle), §3 ist unberührt — Hard
  Rule 3.8 greift hier nicht. Der Nachzug in `spec/spezifikation.md` §5 trifft
  ADR-0022 Folgepflicht 3 im Gegenstands-Satz genau; eine zweite, nicht ausdrücklich von
  Folgepflicht 3 verlangte Ergänzung (neuer Bullet zu `TestClampSurvivesBrokenPayload` u. a.) ist
  sachlich korrekt (alle genannten Tests/Fälle existieren, geprüft) und fügt sich in die bereits
  bestehende erschöpfende Fitness-Function-Aufzählung dieses Abschnitts ein — kein Overreach mit
  Substanz, nur eine Ergänzung über die wörtliche Folgepflicht hinaus, die hier nicht als Befund
  gewertet wird.
- **geprüft, ohne Befund — Plan-Drift-Messung (Prüfpunkt 4).** Die Zahlen 25/10 aus DoD (2) sind
  korrekt (`grep -l`-Kommandos selbst gefahren), ihre Richtung ist wie vom Commit behauptet falsch
  gewesen: `internal/span/` und `internal/report/` bewegen sich nicht, nur `internal/span/span.go`
  bekommt einen Kommentar-Nachzug. Bewegt haben sich exakt die zwei Fälle 107/112, die auf
  `cmd/span-emit/main.go` zeigten — das deckt sich mit ADR-0022 Folgepflicht 2 wörtlich. Kein
  Wächter bleibt „nackt" zurück (alle 25+10 Fälle zielen weiterhin auf existierende Pfade).
- **geprüft, ohne Befund — Fall 154 (Form, Kopf, `# verify:`, Reversibilität).** Kopf vollständig
  (`# files:`/`# expect:`/`# verify:`), Modus `755`, `set -euo pipefail`, ein einziger
  zielgerichteter `sed` gegen eine eindeutig vorhandene Zeile (`grep -c` → 1). `# verify: test-go`
  ist redundant zu `narrow_sensor`s Auto-Erkennung (der `# expect:`-Name passt auf `Test[A-Z]*`),
  aber nicht falsch. Reversibilität liegt wie bei allen Fällen an `mutate.sh`s
  tar-Backup/Restore, nicht am Fall selbst. Eigener isolierter Lauf bestätigt exakt die im Commit
  behauptete Rot-Wirkung an beiden genannten Tests.
- **geprüft, ohne Befund — `.claude/settings.json`.** Alle drei Hook-Einträge zeigen auf
  `"$CLAUDE_PROJECT_DIR"/.harness/state/bin/ai-harness-init span-emit` — identisch mit
  `HOST_BIN := .harness/state/bin/ai-harness-init` im Makefile, das `make host-bin` tatsächlich an
  diesen Pfad legt (`make gates`-Lauf legt genau diese Datei an, selbst geprüft).
- **geprüft, ohne Befund — §3.7 (`artifact-copy.sh`, `span-check.sh` beschreiben, was da ist).**
  Beide Kommentar-Köpfe sind konsistent auf das neue Layout nachgezogen; `artifact-copy.sh`s
  Default-Quellpfad `/out/ai-harness-init` passt zur `build`-Stage, aus der `host-bin` jetzt zieht
  (vorher `span`-Stage mit explizitem viertem Argument `/out/span-emit`).
- **geprüft, ohne Befund — MR-025 (Zahl neben Kommando).** Keine der Textänderungen in
  `spec/spezifikation.md`, `AGENTS.md`, `harness/README.md` führt eine neue Zahl ein; die
  betroffenen Absätze sind reine Umbenennungen (`span-emit-build`→`host-bin`,
  `cmd/span-emit`→Unterkommando-Formulierung) ohne neue Messwerte.
- **geprüft, ohne Befund — §3.2 (keine Inline-Suppression) und §3.4 (keine ADR berührt).** Beide
  Suchen leer (siehe Tabelle oben).
- **geprüft, ohne Befund — die drei offen gemeldeten Fundstellen (MR-005, Roadmap,
  slice-071-Inhalt).** `MR-005` in `harness/conventions.md` (Adaptions-Block, Architect-Eigentum
  nach ADR-0015) nennt weiterhin `cmd/span-emit/` — korrekt unangetastet gelassen. Die
  Roadmap-Zeile mit `span-emit-build` ist eine datierte (2026-08-09) Drift-Beobachtung, keine
  aktuelle Zusage — korrekt unangetastet. `slice-071`s DoD (1) enthält ein jetzt tatsächlich
  totes Mess-Kommando (`grep -c 'dir = args\[0\]' cmd/span-report/main.go` — Pfad existiert nicht
  mehr, und selbst am neuen Pfad matchte das Muster nicht mehr wörtlich) — das bestätigt die
  Selbstauskunft „slice-071 ist inhaltlich veraltet statt nur delinkt" und begründet, warum der
  Implementer hier zu Recht nicht mehr als die Link-Form angefasst hat.
- **nicht abschließend geprüft — `make mutate` und `make full-smoke`.** Laut Auftrag nicht
  gefahren (>1000 s bzw. Docker-E2E). Statt die Commit-Zahlen (147 ok / 1024.32 s bzw. 84.28 s)
  unbelegt zu übernehmen, wurden die drei namentlich betroffenen Fälle (107, 112, 154) einzeln in
  isolierten `git worktree`-Kopien gegen die Docker-`test`-Stage gefahren und lieferten exakt die
  behauptete Rot-Wirkung. Das deckt die inhaltlich wichtigsten drei von 147 Fällen ab, ersetzt
  aber keinen vollständigen `make mutate`-Lauf über die übrigen 144.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 1 |
| MEDIUM | 1 |
| LOW | 0 |
| INFO | 0 |

## Verdikt

**Merge-blockierend — wegen F-1.** Die inhaltliche Umsetzung von ADR-0022 Festlegung 2 ist sauber:
die Klemme steht nachweislich vor jedem Fehlerpfad, die Plan-Drift-Messung in DoD (2) ist korrekt
und die kleinstmögliche Änderung (zwei statt 35 Mutationsfälle) ist tatsächlich die richtige, `make
gates` läuft selbst gefahren grün mit denselben Zahlen wie im Commit-Text, und die drei
namentlich behaupteten Rot-Ergebnisse (107, 112, 154) wurden unabhängig reproduziert. Blockierend
ist ausschließlich F-1: der Umzug von `cmd/span-report` verletzt Hard Rule 3.3 messbar und mit
bereits eingetretenem Schaden (`git log --follow` verliert die Historie zu slice-066) — genau die
Klasse Hard-Rule-Verstoß, die laut Reviewer-Skill unabhängig von Diskussionswürdigkeit blockiert.
F-2 (MEDIUM) ist die zweite Stelle: der Selbstbericht zur Link-Reparatur trifft auf fünf von sechs
Stellen zu, nicht auf alle sechs — das ist der Punkt, an dem die Aufgabe ausdrücklich vor
ungeprüfter Übernahme warnt, und genau dort weicht die Behauptung vom Diff ab, wenn auch inhaltlich
geringfügig. Beide Befunde sind an Dateien außerhalb des eigentlichen Änderungs-Kerns
(Rename-Mechanik bzw. Planner-Doc-Nebenwirkung), nicht an der Klemme oder am Unterkommando-Routing
selbst — die tragende ADR-0022-Eigenschaft dieses Slices ist nicht in Frage gestellt.
