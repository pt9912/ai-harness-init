# Review-Report: ADR-0035, ADR-0068, ADR-0069 — Konsistenz-Review vor dem Accept — 2026-09-26

**Review-Art:** Konsistenz-Review dreier `Proposed`-ADRs (Modul 8 §Rollen-Regeln: *„Architect schreibt; Reviewer prüft auf Konsistenz"*; die drei Acceptance-Trigger der Dateien verlangen diese Runde). Geprüft wird gegen die Baseline, die Hard Rules, die genannten `Accepted`-ADRs und den Code, über den die ADRs Aussagen machen. Kein Diff-Review gegen einen Slice-Plan, keine DoD (das ist der Verifier).

**Gegenstand:** `ADR-0035` (geändert: Fitness-Zeilen, Festlegung 5, Acceptance-Trigger), `ADR-0068` (Schärfung von `ADR-0064` Festlegung 3), `ADR-0069` (neu), der ADR-Index, dazu die zwei Architect-Verdikte des 2026-09-26 (Quelle der Entscheidungen; Zeitdokumente, hier nur als Kennung genannt, `AGENTS.md` §3.11). HEAD `7e17bd0a`, Baum sauber bei Beginn.

**Skill:** `.harness/skills/reviewer.md` @ Version 2.0.0 (2026-09-13) · **Modell:** Sonnet 5 · **Datum:** 2026-09-26

**Eingangs-Kontext:** die drei ADRs komplett · `ADR-0064` Festlegung 3 · `ADR-0066` · `ADR-0040` Festlegungen 1 bis 3 · `MR-050` (Zitat gemessen) · `harness/tools/mutate.sh` (`main`, `clear_belief`, `finalize_belief`, `report_partial`, `report_key`) · `harness/sensors/mutate.md` · `test/mutate-driver.bats` (Teillauf-Blöcke) · `harness/tools/tap-nachzug-nutzlast.sh` · `test/tap-nachzug.bats` · `docs/user/releasing.md` Schritt 7 · Baseline `modul-06-roadmap.md` §Das Beobachtungs-Register und §Wellen-Closure-Prozedur, `observation.template.md` · `.d-check.yml` · das Register `docs/plan/planning/observations/BEO-ALL/`. Die Aussagen der Verdikte und der ADRs waren Behauptung; Zahlen, Rot-Belege und Code-Aussagen sind selbst gemessen und gefahren.

**Eigene Läufe dieses Laufs** — ausschließlich in Scratchpad-Kopien (`tar` des Baums ohne `.harness/state`, **mit** `.git`: die Isolationskopie von `make mutate` verlangt `.git`); kein Schreibzugriff im Repo-Baum, kein Tap-Zugriff, kein voller `make mutate`, keine Host-Toolchain, `bats` im gepinnten Bild des Makefiles (`docker run --rm --network none -v <Kopie>:/code:ro …`).

| Lauf | Ergebnis |
|---|---|
| **Echter Teillauf**, `make mutate MUTATE_JOBS=1 MUTATE_CASES=10-ci-workflow-syntax`, in der Kopie; Slot **vorher** von Hand angelegt (`fake-key-A` + zweite Zeile, 18 Byte) | `MUTATE_CASES waehlt 1 von 446 Faellen — Teillauf.`, `1 ok, 0 Befund(e)`, `TEILLAUF 1 von 446 — kein Beleg (der Beleg-Slot bleibt unberuehrt).`, `Pruefgegenstand 2027be3c…`, Exit 0, 9,6 s. Slot **nachher**: vorhanden, `cmp` byte-gleich |
| derselbe Teillauf, Slot = der ausgegebene Schlüssel (Beleg zum **aktuellen** Prüfgegenstand) | der Teillauf fährt (kein `Kein Fall-Lauf`), Slot nachher byte-gleich. Danach ein **unerzwungener voller** `make mutate`: `Beleg fuer Pruefgegenstand 2027be3c… liegt vor … Kein Fall-Lauf.`, Exit 0 — der Rest aus Festlegung 5 ist mechanisch belegt |
| Zustand der Datei `.harness/state/mutate-passed.key` **im Repo** | vor und nach diesem Lauf: **nicht vorhanden** (`ls .harness/state/` ohne Eintrag); der Repo-Slot wurde nie berührt |
| `test/mutate-driver.bats --filter Teillauf`, unverändert | `1..5`, alle `ok` |
| M1: `[ -n "$partial" ] || clear_belief` ohne Bedingung (Zeile 1649, `diff` genau eine Zeile) | `not ok 4` (*„… laesst einen stehenden Beleg byte-gleich stehen"*), 1, 2, 3, 5 `ok` |
| M2: `report_partial` + `finalize_belief "$key"` (Zeile 1529, genau eine Zeile) | `not ok 3` (*„… schreibt den Beleg-Slot nie"*) und `not ok 4`; 1, 2, 5 `ok` |
| M3 (nicht in der ADR): `[ -z "$partial" ] &&` aus der Bedingung des Übersprungs (Zeile 1629) | `not ok 2` (*„… faehrt trotz stehendem Beleg zum aktuellen Schluessel"*), Rest `ok` |
| `test/tap-nachzug.bats --filter 'sync (abgelehnt\|teilerfolg)'`, unverändert | `1..2`, beide `ok` |
| M4: `404 \| 422 \| 429) … Tap unverändert` vor dem `*)`-Zweig von `schreibe()` | `not ok 1` (*sync abgelehnt*), `ok 2` |
| M5: Bedingung `[ "$geschrieben" = ja ]` in `nicht_lesbar()` aus | `ok 1`, `not ok 2` (*sync teilerfolg*) |
| Register-Schleife aus `ADR-0069` §Kontext, gegen das Register des HEAD | `4` Verzeichnisse ohne `evidence/*.md`, `180` Verzeichnisse; Abschnitts-Schleife: `abschnitt=62 mit_beleg=58` — beide Zahlen **stimmen** |
| dieselbe Schleife in einer Kopie des Registers mit Sonde `p1/observation.md` (Abschnitt *Benannt, nicht gezählt*) | ohne `evidence/`: `5` · leeres `evidence/`: `5` · nur `.gitkeep`: `5` · mit `slice-x.md`: `4` — die Rot-Aussage der ADR gilt |
| `grep -n '^modules:' .d-check.yml` | `modules: [links, anchors, ids, matrix, codepaths, spans, planning, targets, structure]` (Zeile 29) |
| `ls test/mutations/*.sh \| wc -l` · `grep -l '^# verify: full-smoke' … \| wc -l` | `446` · `19` |

---

## Findings

Kein HIGH in einer der drei Dateien.

---

## ADR-0035 — Beleg statt Lauf und die Bezugsmenge des Schlüssels

### MEDIUM

**R-35-1** — `kategorie`: MEDIUM · `quelle`: `ADR-0035` Festlegung 1 und 2, `AGENTS.md` §3.5, `LH-QA-02` · `pfad`: `docs/plan/adr/0035-beleg-statt-lauf-und-die-bezugsmenge-des-schluessels.md:149-169` (Festlegung 5) gegen `:95-103` (Festlegung 1) und `:105-115` (Festlegung 2) · `befund`: Festlegung 5 begründet das Stehenlassen des Slots nach einem roten Teillauf damit, dass *„ein Befund an der Infrastruktur … in der Exit-Form von einem Befund des Falls nicht zu unterscheiden ist"*; dieselbe Unschärfe gilt für einen roten **vollen** Lauf (Zwei-Läufe-Regel im Sensor-Dokument: *„Rot aus dem falschen Grund"* wird als `BEFUND` gemeldet, Exit 1), und der volle Lauf löscht den Slot trotzdem (`clear_belief`, Zeile 1649). Der Unterscheider ist damit allein der Preis des Nachsehens, nicht die Unterscheidbarkeit. Festlegung 1 nennt als Senkung *„derselbe Eingabe grün, wo sie rot sagte"* und begründet die Zulässigkeit des Übersprungs mit *dieselbe Eingabe, dasselbe Verdikt*; nach einem roten Teillauf über unverändertem Schlüssel gibt der nächste unerzwungene volle Aufruf `Kein Fall-Lauf`, Exit 0 aus (gefahren, Läufe 2 und 3 oben) — über derselben Eingabe steht ein rotes Ergebnis neben einem grünen Exit. Festlegung 5 benennt das als Rest mit Ausschalter (`MUTATE_FORCE`); ob dieser Rest unter Festlegung 1 (keine Senkung) oder unter Festlegung 2 (Default fail-closed, *„Deckung wird gezeigt"*) fällt, sagt sie nicht; die Übersprung-Meldung selbst nennt den roten Teillauf nicht. Die Lockerungsfrage (`AGENTS.md` §3.5) ist für den **Übersprung** sauber beantwortet, für den **Rest aus Festlegung 5** offen. · `verifizierbar`: ja — Teillauf mit Befund plus stehender Slot: `test/mutate-driver.bats` *„… laesst einen stehenden Beleg byte-gleich stehen"*, dann `make mutate` ohne `MUTATE_FORCE` (gefahren mit grünem Teillauf; die Slot-Logik ist unabhängig vom Ausgang). · `klasse`: *Begründung einer Ausnahme im Slot-Umgang trifft den Vergleichsfall ebenso; Unterscheider ist Kosten, nicht Sache*

### LOW

**R-35-2** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.6 (gemessene Aussage über einen Ausschnitt, den es nicht mehr gibt), `MR-025` · `pfad`: `0035-…md:55-61` und `:247-250` · `befund`: Die Kommando-Ausgaben, die *„die Lage, gemessen"* tragen, stimmen am Tag des Accept nicht mehr: `grep -c 'außerhalb von \`make gates\` stehen' AGENTS.md` liefert `0` (die ADR nennt `1`); `grep -m1 '^modules:' .d-check.yml` führt `links, anchors, ids, matrix, codepaths, spans` — der Ist-Stand ist `…, spans, planning, targets, structure`. Der zweite Satz steht in einem Absatz, den der 2026-09-26-Lauf selbst angefasst hat (*„Ebenso den Rest aus Festlegung 5 …"*). Die Zahlen der Menge (`247`, `6 von 247`) tragen `keine Erwartungswerte` und sind dadurch gedeckt (Ist: `446`, `19`); die zwei genannten nicht. Die Schlussfolgerung *„kein Modul liest ein Shell-Skript oder Rezept"* ist damit ungestützt durch das genannte Kommando; `targets` liest das Makefile. · `verifizierbar`: ja (die zwei Kommandos). · `klasse`: *Kommando neben Aussage zeigt nach dem Tag der Messung ein anderes Ergebnis, ohne Kennzeichnung als datiert*

**R-35-3** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.6 · `pfad`: `0035-…md:235` (Fitness-Zeile Teillauf) und `:237-241` (*Rot gesehen*) · `befund`: Festlegung 5 sagt drei Dinge zu — übergeht nie, schreibt nie, löscht nie. Die Fitness-Zeile nennt zwei Fälle (*schreibt … nie*, *… byte-gleich stehen*), das *Rot gesehen* deckt zwei Schwächungen. Das *übergeht … nie* ist von einem dritten Fall gebunden (*„ein Teillauf faehrt trotz stehendem Beleg zum aktuellen Schluessel"*; M3 färbt genau diesen Fall rot), steht aber weder in der Tabelle noch im Rot-Beleg. Die Zusage hat einen Zahn, die ADR nennt ihn nicht. · `verifizierbar`: ja (M3). · `klasse`: *Zusage einer Festlegung ohne ihre Fitness-Zeile, Zahn vorhanden*

**R-35-4** — `kategorie`: LOW · `quelle`: Baseline `modul-04-adrs.md` (Form), `AGENTS.md` §3.7 (Zeitform, sinngemäß) · `pfad`: `0035-…md:154-169` · `befund`: Die Abwägung zu Festlegung 5 (*„(ii) … wäre die falsche Aussage"*, *„(i) … entwertet einen Beleg"*) steht innerhalb der Festlegung, nicht in §Verglichene Alternativen (Tabelle A bis D endet bei Festlegung 4). Die Konjunktive über verworfene Fassungen stehen damit außerhalb der Stelle, die die Form dafür vorsieht. Darstellung. · `verifizierbar`: nein · `klasse`: *Alternativen-Vergleich außerhalb der Alternativen-Sektion*

### INFO

**R-35-5** — Fitness-Zeile 3 sagt *„der Schreibpunkt liegt hinter der Bedingung, unter der `main()` seinen Exit-Status bildet"* (Zeile 234, unverändert seit 2026-09-04). `finalize_belief` steht in `main()` **vor** der Zeile `[ "$fail_count" -eq 0 ]` (Zeilen 1828, 1830); gemeint ist erkennbar *„gesteuert von `fail_count`"*. Das Verhalten ist gebunden (`test/mutate-driver.bats`, Block *„ein voller Lauf mit Befund nennt den Pruefgegenstand-Schluessel und hinterlaesst keinen Beleg"*, gegen den echten `main()`). Wortlaut, kein Verhaltensbefund.

**R-35-6** — Die Rot-Belege der ADR (M1, M2) sind reproduziert und nachvollziehbar (Tabelle oben; Ausgabe der `not ok`-Namen gelesen, nicht nur Exit). Der echte Teillauf bestätigt: Slot vorher/nachher byte-gleich, mit und ohne passendem Schlüssel; der Repo-Slot wurde nie angelegt. `finalize_belief` und `clear_belief` haben in `mutate.sh` genau zwei bzw. einen Aufrufort (`grep -n`: Zeilen 1649 und 1828), den Teillauf ruft keiner.

**R-35-7** — `slice-180` in der Geschichte-Zeile vom 2026-09-04 ist Bestand vor dem Cutoff von §3.11 und trägt selbst den Vermerk *„Kennung ohne Adresse"*; keine Pfad-Adresse eines beweglichen Artefakts in der ADR (`grep -n 'docs/plan/planning' 0035-…md` ohne Treffer; `docs/reviews/` ist eine stehende Ablage).

### Geprüft, ohne Befund

`MR-050`-Zitat (*„Der Griff steht dort, wo ein Cache-Treffer …"*, `grep -c` → 1) und Anker/Links (Gate unten) · Acceptance-Trigger gegen `ADR-0040` Festlegungen 1 (Kennung, nicht Pfad-Link) und 3 (Trigger solange `Proposed` gesetzt) · Bezug-Liste gegen Index-Zeile (`ADR-0040` beidseitig ergänzt) · Widerspruchsfreiheit Festlegung 5 gegen 1 bis 4 im Wortlaut (bis auf R-35-1) · `MR-003`: `working-tree-hash.sh` bleibt Träger des Gate-Nachweises, Festlegung 3 schließt es nur als Schlüssel dieses Sensors aus — kein Konflikt · Fitness-Zeile *voller Lauf* gegen `main()`: der volle Lauf löscht in Zeile 1649 und schreibt oder löscht in `finalize_belief` (1828) — die Aussage trägt · Norm-Fassung *„Ein voller Lauf … keinen Beleg"* gegen Kopf `mutate.sh:64-65` (*„ein Lauf mit Befund hinterlaesst KEINEN Beleg"*, ohne *voller*; Werkzeug-Kommentar, nicht ADR — im Verdikt schon benannt).

**Accept-Empfehlung ADR-0035: ja nach Korrektur.** Die fünf Festlegungen tragen gegen den gefahrenen Code, die zwei Rot-Belege sind reproduziert. R-35-1 ist die Frage, die der Auftraggeber vor dem Accept beantwortet haben will: ob der Rest aus Festlegung 5 unter Festlegung 1 oder 2 steht. Die Beantwortung ist ein Satz und keine Umkonstruktion; R-35-2 bis R-35-4 sind Darstellung und hindern die Annahme nach dem Wortlaut des Triggers nicht.

---

## ADR-0068 — Der Nachzug nennt den Zustand des Tap nur, soweit die Antwort ihn trägt

### MEDIUM

**R-68-1** — `kategorie`: MEDIUM · `quelle`: `ADR-0068` Festlegung 1 und 3, `AGENTS.md` §3.6 · `pfad`: `docs/plan/adr/0068-…md:101-113` (Grundsatz), `:126-135` (Festlegung 3), Titel, `:159-162` (Positiv) gegen `harness/tools/tap-nachzug-nutzlast.sh:137` und `:63-77` · `befund`: Titel und Festlegung 3 sagen *„nach einem vollzogenen Schreiben sagt **jede** Meldung, dass geschrieben ist"*, und nach Festlegung 3 gilt das, wenn die Nachkontrolle *„weder gleich (Exit 0) noch Unterschied (Exit 1)"* ist. Der Code hält das nur für `nicht_lesbar()` (Lesen scheitert). Nach dem Schreibaufruf (`geschrieben=ja`) gibt es zwei weitere Wege zu Exit 2: `gleich()` mit `cmp` Exit ≥ 2 meldet *„der Vergleich lief nicht (cmp Exit N) — es wurde nichts verglichen"* (Zeile 137), und der `beende`-Zweig meldet für jeden Nutzlast-Fehler *„interner Fehler der Nutzlast … — es wurde nichts verglichen"* (Zeilen 63-77); ein Signal nach dem Schreiben (`trap 'exit 2' HUP INT TERM`) meldet gar nichts. Beide Meldungen sagen *„nichts verglichen"* nach vollzogenem Schreiben, ohne den Vollzug zu nennen — genau die Form, die Festlegung 3 als Fehlform benennt (*„liest ein Bediener als ‚nichts geschehen'"*) und die Festlegung 1 (Bullet 3: *„nichts verglichen … der Lauf endete vor dem Schreibaufruf"*) ausschließt. Die Fitness-Zeile bindet nur Lesecodes 404, 401, 403, 429, 500 und *keine Antwort* (`sync teilerfolg`); für die zwei anderen Wege existiert kein Fall. Die Aussage der ADR ist weiter als das, was Code und Fälle halten. Der Ausgang bleibt sicher (Exit 2, nie *unverändert*); falsch ist die Zusage *jede*. · `verifizierbar`: ja — Lesen des Codes (Zeilen 63-77, 137); ein Fall, der `cmp` nach dem Schreiben scheitern lässt, färbt den Wortlaut der ADR rot (nicht gefahren: Stub für `cmp` nicht vorhanden). · `klasse`: *Zusage „jede Meldung" über eine Zweig-Menge, die nur einer der Zweige hält*

### LOW

**R-68-2** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.6 (Aussage über eine Häufigkeit ohne Messung) · `pfad`: `0068-…md:148` (Alternative C) · `befund`: *„ein abgelaufenes Token (401) ist der häufigste Ausgang"* ist eine Häufigkeitsbehauptung; es gab keinen realen Nachzug (`0068-…md:164`: *„bis zum ersten realen Nachzug ungemessen"*). Die Alternative C wird auch mit ihr verworfen. Die Setzung von 401, 403 und 409 ist als Annahme benannt (Festlegung 2, §Grenze); die Häufigkeit ist es nicht. · `verifizierbar`: nein · `klasse`: *Häufigkeit als Begründung ohne Messung, in einer Ablehnung*

**R-68-3** — `kategorie`: LOW · `quelle`: `ADR-0068` §Was die Wahl trägt (`:82-93`) · `befund`: Die Asymmetrie *„die Enge kostet einen lesenden Aufruf, eine zu weite Zusage kostet eine Meldung, der der Bediener glaubt"* trägt für Status **außerhalb** der Menge. Für 401, 403 und 409 gilt dieselbe Ungemessenheit (*„am realen Tap ungemessen"*), und dort ist die falsche Aussage *„Tap unverändert"* möglich, wenn die Annahme bricht. Die ADR benennt das (Festlegung 2, §Grenze Punkt 1), wiederholt es aber nicht im Satz *„nie eine falsche Zustandsaussage"* (`:219-220`), der für die Menge gilt und für ihren Rand nicht. Wortlaut. · `verifizierbar`: nein · `klasse`: *Sicherheits-Zusage mit Reichweite nur für einen Teil der Fälle*

### INFO

**R-68-4** — Die zwei Rot-Belege sind reproduziert (M4 färbt `sync abgelehnt`, M5 färbt `sync teilerfolg`, jeweils genau ein `not ok`; unverändert `1..2` grün). Die Zuordnung 401, 403, 409 → *unverändert*, jeder andere Status und `000` → *ungewiss*, 200 → `return 0`, entspricht `schreibe()` (Zeilen 251-256) und `docs/user/releasing.md` Schritt 7 (Zeilen 137-142).

**R-68-5** — Konsistenz mit `ADR-0064` Festlegung 3 und `ADR-0066`: kein Widerspruch. `ADR-0064` nennt Ursachen (*Anmeldung, Schutz des Branches, Konflikt*), keine Statuscodes, und regelt *„ohne Antwort … ungewiss"*; die Antwort außerhalb beider Fälle ist dort offen — `ADR-0068` füllt sie, ohne einen Satz abzulösen. Die Schärfung ohne `Supersedes` trägt die Form von Baseline `modul-04-adrs.md` (*„mit explizitem Verweis auf die … geschärfte Vorgängerin"*), die Klassen 0, 1 und 2 und die Zeile `tap-sync: Exit 2` bleiben (Fall prüft sie mit). Die Nutzlast endet mit den Status 0, 10 und 2 (`beende` bildet jeden anderen auf 2 ab). Statuszeilen der Bezug-Liste gegen die Dateien: `ADR-0064`, `0066`, `0040` `Accepted`, `ADR-0062` `Proposed` — stimmen.

### „Ungewiss" ist die sichere Richtung — Prüfung

`ungewiss` behauptet nichts über den Zustand und nennt `make tap-check`. Kann sie je fälschlich *unverändert* behaupten? Nein — die Meldung enthält das Wort nicht (Fall prüft `nirgends 'unverändert'`, M4 färbt ihn rot). Kann *„Tap unverändert"* fälschlich stehen? Nur, wenn ein Vollzug mit 401, 403 oder 409 antwortet — die benannte, ungemessene Annahme (Festlegung 2); kein Befund gegen die Richtung, aber die Grenze der Zusage (R-68-3).

### Geprüft, ohne Befund

Meldung *Ausgang ungewiss* nennt `make tap-check TAG=<tag>` (Code Zeilen 255-256) · 201 und 204 → *ungewiss* (Fall enthält beide) · kein vierter Nutzlast-Status · kein `Supersedes`, Index von `ADR-0064` unverändert (Folgepflicht 2) · keine Slice-Kennung und kein Pfad eines beweglichen Artefakts in der ADR (`grep -n 'slice-' 0068-…md` ohne Treffer; `test/tap-nachzug.bats` und `harness/tools/…` sind ortsfest) · Acceptance-Trigger gegen `ADR-0040` · Index-Zeile gegen Bezug-Liste (6 von 6).

**Accept-Empfehlung ADR-0068: ja nach Korrektur.** Substanz (Menge als Setzung, Grundsatz, Schärfung ohne Ablösung) trägt und ist gegen den Code belegt. R-68-1 ist eine Zusage, die weiter reicht als Code und Fitness; sie ist vor dem Accept zu klären (Wortlaut der ADR oder Träger im Code, das entscheidet nicht der Reviewer). Ohne Klärung friert die ADR *„jede Meldung"* ein.

---

## ADR-0069 — Beleglose Register-Verzeichnisse sind ein Befund der Paarung, keine Ausnahme

### MEDIUM

**R-69-1** — `kategorie`: MEDIUM · `quelle`: `ADR-0069` Festlegung 2, Baseline `modul-06-roadmap.md` §Das Beobachtungs-Register und Closure-Schritt 3, `AGENTS.md` §3.5 · `pfad`: `docs/plan/adr/0069-…md:104-115` (Festlegung 2), insbesondere `:109-111` · `befund`: Festlegung 2 beginnt mit *„kennt keine Ausnahme"*, sagt dann für die Paarung **als Closure-Schritt**, sie prüfe *„nach dem Baseline-Wortlaut — ‚die gerade entstandenen Einträge'"*; ein Bestands-Verzeichnis, das die Closure weder angelegt noch berührt hat, sei dort *„nicht ihr Gegenstand"*. Der Baseline-Satz (`modul-06-roadmap.md`, Closure-Schritt 3) begründet den **Zeitpunkt** (*„erst jetzt, weil sie die gerade entstandenen Einträge prüfen; in Schritt 2 gäbe es sie noch nicht"*); der Umfang der Register-Paarung (c) steht im selben Modul universal (*„jede Registerzeile trägt mindestens einen Beleg"*, *„ob **jedes** Verzeichnis ein nicht leeres `evidence/` hat"*). Die Lesart engt den Closure-Schritt auf berührte Einträge ein — das Ergebnis der *„Ausnahme in der Closure-Notiz"*, die die ADR (`:64-67`) beenden will: eine Closure kann Hälfte 2 als getragen abhaken, ohne die vier Verzeichnisse zu nennen. Das Verdikt weist dem Planner die Gegenform zu (*„N Verzeichnisse ohne Beleg, namentlich … nicht als getragen behauptet"*), die ADR sagt es für die Bestandsprüfung (`:112-115`), nicht für den Closure-Schritt. Die ADR nennt sich *„die Baseline gilt wörtlich"* (`:95`); an dieser Stelle ist sie eine Lesart mit Einschränkung. · `verifizierbar`: ja — Baseline-Wortlaut (`grep -n 'jede Registerzeile\|gerade entstandenen' .harness/baseline/v6.9.0/regelwerk/modul-06-roadmap.md`) gegen Festlegung 2. · `klasse`: *Zeitpunkt-Begründung der Baseline als Umfangs-Einschränkung gelesen*

**R-69-2** — `kategorie`: MEDIUM · `quelle`: `AGENTS.md` §3.6 (gemessene Aussage), Kontext-Disziplin *grep ist keine Messung* · `pfad`: `docs/plan/adr/0069-…md:62-67` und `:187-189` (Fitness Function: *„ein Wächter existiert nicht — kein Modul der Doku-Gate-Konfiguration liest das Register"*) · `befund`: Der Beleg, den die ADR nennt (`grep -m1 '^modules:' .d-check.yml` führt `links, anchors, ids, matrix, codepaths, spans`), zeigt heute `[links, anchors, ids, matrix, codepaths, spans, planning, targets, structure]` — die ADR ist vom selben Tag, die Ausgabe stimmt nicht. Derselbe Text führt in §Was hier nicht entschieden ist (`:143-145`), ob `planning.observations` die zweite Hälfte hält, sei **ungemessen**. Die Aussage *„kein Modul liest das Register"* und *„ungemessen, ob ein Modul es liest"* stehen im selben Dokument; die erste stützt das Kommando nicht mehr. Die Konfiguration trägt unter `planning:` keinen `observations`-Schlüssel (`.d-check.yml:58-65`) — das trüge die Aussage, ist aber nicht das, was die ADR als Beleg nennt. · `verifizierbar`: ja (das Kommando; `sed -n 58,66p .d-check.yml`). · `klasse`: *Kommando-Ausgabe im Beleg passt nicht zur Aussage und nicht zum Ist-Stand*

### LOW

**R-69-3** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.6 · `pfad`: `0069-…md:62-63` · `befund`: *„jedes nennt sein Vorkommen unter ‚Benannt, nicht gezählt' und sagt selbst, dass die Paarung für es rot ist"* trifft für zwei von vier zu: nur `ci-rennt-gegen-die-publikation-des-gepinnten-releases` und `planungs-bestand-waechst-schneller-als-er-abgebaut-wird` sagen es (in `state.md`); `cpp-skelett-erfuellt-die-messmethode-von-lh-qa-02-nicht` und `einstiegs-datei-weicht-von-der-pflichtgliederung-ab` tragen den Abschnitt und kein Wort zur Paarung (`grep -rn 'Paarung'` über die vier Verzeichnisse: zwei Treffer, beide `state.md`). Zwei tragen kein `evidence/`, zwei ein leeres. · `verifizierbar`: ja. · `klasse`: *Aussage über „jedes" von N, an zwei von N wahr*

**R-69-4** — `kategorie`: LOW · `quelle`: Baseline `observation.template.md`, `AGENTS.md` §3.6 · `pfad`: `0069-…md:153` (Alternative C), `:98-102` · `befund`: Alternative C (*beleglose Verzeichnisse verbieten*) wird mit *„die Baseline sagt das Gegenteil (‚gehört trotzdem in den Eintrag')"* verworfen. Dieser Satz setzt einen bestehenden Eintrag voraus. Das vendored Template sagt daneben: *„Erfinde keine Belege: Ein Verzeichnis entsteht beim ERSTauftreten einer echten Beobachtung"* und *„Weglassen, wenn keine"* für den Abschnitt — also der Ort des **Erst**-Vorkommens ohne Vorgang ist in der Baseline nicht das, was der zitierte Satz sagt. Die Lesart (a) trägt mit dem Template-Satz gut (der Abschnitt ist Teil des Eintrag-Rumpfs jeder `observation.md`); die ADR nennt ihn nicht und begründet Alternative C mit dem schwächeren Satz. · `verifizierbar`: ja (`sed -n 25,62p .harness/baseline/v6.9.0/templates/docs/plan/planning/observation.template.md`). · `klasse`: *tragender Baseline-Satz nicht gelesen, Ablehnung stützt sich auf den schwächeren*

**R-69-5** — `kategorie`: LOW · `quelle`: `ADR-0069` Festlegung 4, Re-Evaluierungs-Trigger 1 · `pfad`: `0069-…md:125-131` und `:206-210` · `befund`: Festlegung 4 antwortet auf *„ein Beleg je Closure zählte Closures statt Wiederholungen"* — dieser Satz steht wörtlich in `planungs-bestand-waechst-schneller-als-er-abgebaut-wird/observation.md` (dort als Begründung, keinen Beleg zu tragen; `state.md` verweist für die Entscheidung auf die Lesart). Die ADR nennt den Eintrag nicht, sagt aber, der Bestand bleibe stehen (`:139-140`). Der Eintrag behält damit einen unveränderlichen Rumpf (`observation.md`), der der Festlegung widerspricht, und Trigger 1 (*„dessen `observation.md` das selbst sagt und für das kein Vorgang genannt ist"*) ist an diesem Verzeichnis wörtlich erfüllbar — ob er auslöst, hängt am Planner-Urteil *„keinen Vorgang treffen wird"*, das der Eintrag verneint (*„nahezu jede Closure"*). Der Trigger-Audit der nächsten Closure liest damit einen offenen Fall, den die ADR nicht adressiert. · `verifizierbar`: ja (`sed -n '/^## Benannt/,$p' …/planungs-bestand-…/observation.md`). · `klasse`: *Festlegung widerspricht dem Rumpf eines unveränderlichen Bestandseintrags, ohne ihn zu nennen*

### INFO

**R-69-6** — Gemessen und **bestätigt**: `4` von `180` Verzeichnissen ohne `evidence/*.md`; `62` mit Abschnitt, davon `58` mit Beleg; die Rot-Aussage der Fitness-Zeile (Sonde: `5`, leeres `evidence/`: `5`, nur `.gitkeep`: `5`, mit `slice-x.md`: `4`) gilt. Jede Zahl steht neben ihrem Kommando und ist als Stand am Tag des Entscheids gekennzeichnet (`MR-025`, *„keine Erwartungswerte"*). Die Schleife zählt Dateien `evidence/*.md`, das Baseline-Kriterium sagt *„nicht leeres `evidence/`"* — die ADR ist damit strenger (Folgepflicht 2), nicht laxer; das ist eine Verschärfung.

**R-69-7** — `MR-000`: Die Wahl (a) ist eine **Lesart**, keine Abweichung — tragfähig: Die Baseline führt die Beleg-Hälfte unbedingt; das Template legt den Abschnitt als Teil jedes Eintrags an; ein Eintrag ohne Beleg trägt das Rot der Paarung, statt ihm eine Ausnahme zu geben. Ein Adaptions-Eintrag wäre erst mit (b) fällig — die ADR sagt das (`:169-173`). Kein Widerspruch zu `ADR-0034` Festlegung 3 (Kennung ist Pfad; die Verzeichnis-Form bleibt) und `ADR-0049` (Ausgangs-Frage nicht berührt; `grep` in beiden nach *ohne Beleg* / *leeres* ohne Treffer, also keine gegenteilige Aussage). `ADR-0034`, `0049`, `0040`, `0015` sind `Accepted`, wie die Bezug-Liste sagt.

**R-69-8** — Folgen für die Wächter-Arbeit (`register-paarung-ohne-gate-modul` und der Wächter über der 3×-Schwelle): Die ADR koppelt die Verdrahtbarkeit eines Wächters für Hälfte 2 an die **Tilgung** des Bestands durch Belege (Trigger 2, *„nicht durch eine Ausnahmeliste"*) und sagt zugleich, ein Verzeichnis, dessen Klasse keinen Vorgang trifft, habe **unbegrenzt** Rot (`:166-168`) und einen Trigger für die Folge-ADR (Trigger 1). Der akzeptierte Negativ-Punkt hat damit einen Re-Evaluierungs-Trigger, benannt mit Beobachter (*„kein Sensor … der Planner urteilt"*); die Lücke ist benannt, nicht geschlossen — zulässig, siehe R-69-5 für die eine offene Adresse. Ein Wächter über der 3×-Schwelle bleibt von der ADR unberührt (beleglose Verzeichnisse stehen bei 0).

### Geprüft, ohne Befund

Form (Kontext, Entscheidung, Verglichene Alternativen, Konsequenzen, Fitness Function, Re-Evaluierungs-Trigger, Acceptance-Trigger, Geschichte) vollständig · Index-Zeile gegen Bezug-Liste (7 von 7) · Acceptance-Trigger gegen `ADR-0040` (Kennung des Verdikts, kein Pfad-Link; `docs/reviews/` als Ablage) · keine Slice-Kennung im Text (`grep -n 'slice-'`: nur der Sonden-Dateiname `slice-x.md` in der Rot-Beleg-Beschreibung) · Cutoff-Satz (*„ab dieser Entscheidung, kein Nachrüsten"*) · Festlegung 3 (*„Ein Beleg wird nicht erfunden"*) gegen Baseline *„Der Beleg ist formgebunden"* · Zuständigkeit: die ADR schreibt nichts in `AGENTS.md` §3 und keinen `MR` (`AGENTS.md` §3.8, `ADR-0015`).

**Accept-Empfehlung ADR-0069: ja nach Korrektur.** Die Lesart trägt gegen den Baseline-Wortlaut, und die zwei gemessenen Zahlen sind richtig. R-69-1 ist eine Substanz-Frage am Umfang des Closure-Schritts (eine Einschränkung, die die ADR an anderer Stelle selbst verwirft); R-69-2 ist ein Beleg, der nicht mehr zur Aussage gehört. Beide vor dem Accept zu beantworten; R-69-3 bis R-69-5 sind Wortlaut und Adresse.

---

## Summary

| ADR | HIGH | MEDIUM | LOW | INFO | Accept-Empfehlung |
|---|---|---|---|---|---|
| ADR-0035 | 0 | 1 (R-35-1) | 3 (R-35-2 bis R-35-4) | 3 | ja nach Korrektur |
| ADR-0068 | 0 | 1 (R-68-1) | 2 (R-68-2, R-68-3) | 2 | ja nach Korrektur |
| ADR-0069 | 0 | 2 (R-69-1, R-69-2) | 3 (R-69-3 bis R-69-5) | 3 | ja nach Korrektur |

Index `docs/plan/adr/README.md`: Zeilen für `ADR-0068` und `ADR-0069` (Status `Proposed`, Bezug-Listen 6 von 6 und 7 von 7) und der `ADR-0035`-Bezug auf `ADR-0040` stimmen mit den Dateien; kein Befund. Anker und Links lösen auf — Nachweis `make docs-check` im Gate-Lauf dieses Commits.

## Übergaben

| An | Was | Artefakt |
|---|---|---|
| Architect | `ADR-0035` R-35-1 (Rest aus Festlegung 5 gegen Festlegung 1 und 2 einordnen), R-35-2 bis R-35-4 | dieser Report |
| Architect | `ADR-0068` R-68-1 (Reichweite von *„jede Meldung"*: Wortlaut oder Träger im Code; die Wahl trifft nicht der Reviewer), R-68-2, R-68-3 | dieser Report |
| Architect | `ADR-0069` R-69-1 (Umfang des Closure-Schritts), R-69-2 (Beleg der Aussage *„kein Modul liest das Register"*), R-69-3 bis R-69-5 | dieser Report |
| Auftraggeber | Accept je ADR — die Empfehlung steht oben; die Entscheidung ist seine | dieser Report |
| Planner (nach Accept, Slice-Closure §7) | Finding-Klassen dieser Runde ins Beobachtungs-Register einordnen, soweit ein Vorgang sie trägt (`klasse`-Felder oben) | dieser Report |

## Negativbefund-Zeilen (Skill: pro betrachtetem Bereich)

- **Verstoß gegen aktive ADR / Hard Rule** — geprüft, ohne Befund (kein Widerspruch zu `ADR-0064`, `0066`, `0034`, `0049`, `0040`; `AGENTS.md` §3.4 nicht berührt — keine `Accepted`-Datei geändert).
- **Gate-Lockerung ohne ADR** — geprüft, ohne Befund; die drei Dateien sind selbst ADRs im Status `Proposed`. Die §3.5-Frage zu `ADR-0035` steht als R-35-1.
- **Stilles-Grün-Pfad** — R-35-1 (benannter Rest), sonst ohne Befund.
- **Halluziniertes Gate** — geprüft, ohne Befund (`make mutate`, `MUTATE_CASES`, `MUTATE_FORCE`, `make tap-check` existieren).
- **Referenz auf superseded ADR** — geprüft, ohne Befund (alle Bezug-ADRs `Accepted` bzw. wie ausgewiesen `Proposed`).
- **Norm nur im Template-Kommentar** — nicht einschlägig.
- **Kommentar trägt keine Klasse** — nicht einschlägig (kein Code, keine Konfiguration geändert).
- **Zustandsfeld trägt Chronik** — Index-Statuszellen tragen nur `Proposed`; ohne Befund. Die Geschichte-Tabellen der ADRs sind deren Ereignis-Register.
- **Eigenschaft statt Adresse in eingefrorenen Artefakten (`AGENTS.md` §3.11)** — geprüft, ohne Befund (Verdikte und Reports als Kennung, nicht als Pfad-Link; `slice-180` in `ADR-0035` ist Bestand).
- **Kennungs-Form** — `ADR-NNNN` als Link, `MR-`/`LH-` als Link; Gate unten.
