# Verifikation — slice-025 (Bootstrap-Kette per Pre-Flight absichern + L3/L4)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths` / ids-`exempt-paths`) —
> bare IDs/Pfade hier ok. Bewusst ohne Markdown-Links, damit `links`/`anchors` nichts findet.

## Kopf-Metadaten

- **Rolle:** unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Gegenstand:** DoD-Behauptung von slice-025 (8 Punkte) gegen realen Zustand + Spec + Plan + ADR/Hard Rules. Der Verifier bestätigt DoD+Spec+Plan; der Reviewer (getrenntes Artefakt, 2026-07-21-slice-025-review.md) prüfte gegen Plan+ADR.
- **Slice:** `docs/plan/planning/in-progress/slice-025-bootstrap-preflight.md` (noch in-progress/, DoD-Boxen offen — Verify VOR Closure).
- **Impl-Commits:** `5c6a44a` (reiner Move open→in-progress, 0/0) · `68040f1` (Inhalt + Tests + 4 Mutationen + 3 Link-Reparaturen). Review-Report `4fe143c`. HEAD=`4fe143c`, tracked Tree clean.
- **Berührte Dateien (68040f1):** `cmd/ai-harness-init/main.go` (+Test), `internal/fetch/baseline.go` (+Test), `internal/emit/templates.go` (+Test), `test/mutations/11-14`, 3 Doc-Links (done/022a, done/022b, welle-02). KEINE Gate-Datei (Makefile/*.mk/.d-check.yml/.github/), KEINE ADR.
- **Selbst ausgeführt (belegt, nicht nur nachgelesen):**
  - `make gates` SELBST → **Exit 0**; git status vor UND nach leer.
  - `make mutate` SELBST → **Exit 0, 14 ok / 0 Befund**; git status vor UND nach leer (mutate.sh restauriert). Cases 11-14 färbten je ihren benannten Test rot.
  - Grün-Vorlauf des mutate-Laufs fuhr `make test` + `make ci-lint` + `make smoke` real grün (Vorbedingung, sonst Abbruch) — damit ist `make smoke` in DIESER Umgebung als grün belegt, nicht nur berichtet.
  - `git diff --stat 7df00c5..68040f1` + gezielte Pfad-Diffs (Gate-Dateien, ADR, Doc-Links).
  - Statische Reihenfolge-Belege in `internal/fetch/baseline.go` (readCapped vor sha256-Pin; defer nach MkdirTemp).

## DoD-Verdikt je Zeile

### DoD-1 — Entwurfs-Entscheidung getroffen und begründet (ggf. ADR) — **CONFIRMED**

Gewählt: **Pre-Flight** (Vorbedingungen prüfen), NICHT Staging→Commit (Kette atomar). Belegt im Package-Doc `main.go:8-14` und in der EHRLICHEN GRENZE `main.go:147-150`, mit Begründung (billig, deckt den häufigsten Fall; die Atomaritäts-Alternative bewusst verworfen). Damit ist DoD-1 (Entscheidung getroffen UND begründet, §6) erfüllt.

„Keine ADR" ist tragfähig: Pre-Flight ist additive Orchestrierungs-Härtung in der CLI-Schicht (architecture.md §2 „CLI = Orchestrierung"; die Schritte „bleiben, was sie sind", Plan §3). Er ändert **keine** Herkunftsklasse und **nicht** das Fetch/Generate-Modell aus ADR-0005 — die vier Klassen der ADR-0005-Tabelle sind unberührt. Er senkt keinen Gate (§3.5 gilt für Senkungen), führt keine externe Abhängigkeit ein und ändert weder den Komponenten-Graphen noch die grobe Sequenz (architecture.md §4 kennt keine feingranulare Emit-Reihenfolge). Die DoD-1-Bedingung „nur falls Architektur-Wirkung → ADR" ist korrekt zu „keine ADR" ausgewertet. Staging→Commit HÄTTE Architektur-Wirkung gehabt (Staging-Schicht, geändertes Schreibmodell jedes Emit-Schritts, --force-Interaktion) — genau darum ist die verworfene Alternative die mit ADR-Pflicht, nicht die gewählte.

### DoD-2 — LH-FA-01 „wie vorgefunden", JE SCHRITT — **CONFIRMED (Substanz + Spec + Plan §6); DoD-Wortlaut Zeile 39 ist die Lücke, nicht der Code**

Dies ist der F-1-Kern, an den Verifier delegiert. Reconciliation:

**Was der Code hält (belegt, tragend):** Der Pre-Flight fasst die Kollision je Schritt VOR dem ersten Write:
- Phase 1 prüft beide Fetch-Ziele (`.harness/skeleton`, `.harness/baseline/<tag>`) — `main.go:110-115`. `TestRun_FetchKollisionSchreibtNichts` belegt: kollidiert die Baseline, wird auch das Skelett NICHT abgelegt. Mutation 14 rötet ihn (selbst gesehen).
- Phase 3 prüft ALLE Emit-Ziele (Verifier, `.d-check.yml`, `d-check.mk` + alle Template-Ziele aus der gefetchten Baseline) — `main.go:132-142` + `emitTargets`. `TestRun_EmitKollisionSchreibtKeinEmit` belegt: kollidiert EIN Template-Ziel (AGENTS.md), schreibt KEIN Emit-Schritt. Mutation 12 rötet ihn (selbst gesehen).
- Dass die Pre-Flight-Menge == die Emit-Schreib-Menge ist (kein Ziel entfällt), sichert `TestTemplateTargets_SpiegeltDenEmittiertenSatz` — genau der §3.6-„vollständiger Ist-Bestand"-Test, kein Namens-Proxy. Damit ist die motivierende Klasse — ein SPÄTER Emit-Schritt scheitert und lässt FRÜHERE Emit-Ergebnisse als Waisen liegen (§1-Herkunft, viermal wiederholt) — geschlossen und je Schritt belegt.

**Die Spannung (nur Wortlaut vs. Verhalten):** DoD-Zeile 39 sagt absolut „hinterlässt das Zielrepo in dem Zustand, in dem er es vorfand — belegt je Schritt". Geliefert wird das nur für die Pre-Flight-KOLLISIONS-Klasse. Eine Emit-Kollision (Phase 3) und ein Runtime-Abbruch während Fetch/Docker lassen das in Phase 2 gefetchte `.harness/` bewusst liegen — `TestRun_EmitKollisionSchreibtKeinEmit:205-209` ASSERTIERT das Bestehenbleiben der Baseline als „retry-freundlich". Streng-wörtlich ist das Repo dann NICHT „wie vorgefunden".

**Verdikt (Fällung):** bewusst-sanktionierte, korrekt eng gefasste Verengung — **kein Code-Defekt**. Plan §6 sanktioniert sie ausdrücklich: „(a) Pre-Flight … deckt den häufigsten Fall, aber **nicht** den Abbruch *während* eines Netz-Fetchs" und „Teil-Erfolg ist nicht immer falsch: … ein bereits vendored Regelwerk vielleicht nicht. Das Modell muss sagen, was es zurückrollt und was nicht — pauschal ‚alles oder nichts' kann teuer sein (erneuter Netz-Fetch beim Retry)." Der Code tut genau das: er benennt die Grenze ehrlich (Package-Doc + EHRLICHE GRENZE, §3.6-konform), verspricht nirgends weiter als er hält, und der gelassene Rest ist eine VALIDE vollständige Baseline (kein halbes Artefakt zum Handaufräumen), die ein Retry wiederverwendet. Gegen die vertragliche Spec (LH-FA-01 Boundary-AC „kein Überschreiben ohne --force") ist das Verhalten konform.

Die Lücke liegt allein im DoD-CHECKBOX-Wortlaut Zeile 39, der breiter formuliert ist als Plan §6 autorisiert und der Code liefert. Das ist die Klasse, die NUR der Verifier fängt (Modul 11: „der Plan hat eine DoD-Lücke … Architect klärt — nicht ‚wir nehmen das mildere Ergebnis'"). **Empfehlung:** die Closure-Notiz/DoD-Zeile 39 auf das Gelieferte einengen (Pre-Flight-Kollisionen je Schritt; Fetch-Rest bei Runtime-Abbruch bewusst retry-freundlich) — eine Doc-Reconciliation, kein Code-Fix, kein Blocker.

### DoD-3 — Der Test ist rot gesehen worden — **CONFIRMED (selbst gefahren)**

`make mutate` SELBST gefahren → **Exit 0, 14 ok / 0 Befund**. Die 4 neuen Wächter färbten je ihren benannten Test rot:

```
mutate: ok  11-baseline-groessen-schranke  -> TestBaseline_AssetTooLarge rot
mutate: ok  12-preflight-emit              -> TestRun_EmitKollisionSchreibtKeinEmit rot
mutate: ok  13-baseline-defer-tmp          -> TestBaseline_IncompleteBundle rot
mutate: ok  14-preflight-fetch             -> TestRun_FetchKollisionSchreibtNichts rot
```

Der mutate-Treiber prüft fail-closed vier Bedingungen (Mutation greift, Sensor wird rot, benannter Wächter steht in der Fehlschlag-Ausgabe) — „rot aus dem falschen Grund" ist ausgeschlossen. Der Grün-Vorlauf (test/ci-lint/smoke grün VOR der ersten Mutation) belegt zusätzlich, dass die Rötung von DER Mutation kommt, nicht von einem schon roten Baum. git status vor UND nach dem Lauf leer (mutate.sh restauriert vollständig). Der frühere reportPreflight-Silent-Green (Print vom Abbruch getrennt) ist geschlossen: Mutation 12 (`… && false`) entfernt Abbruch UND Meldung gemeinsam → rot.

### DoD-4 — slice-022a L3 (Temp-Rest) aufgelöst — **CONFIRMED**

`baseline.go:172` `defer func() { _ = os.RemoveAll(tmp) }()` steht direkt nach `os.MkdirTemp` (`:158`) und räumt jeden Go-Fehlerpfad. `TestBaseline_IncompleteBundle` (Abbruch NACH MkdirTemp, unvollständiges Bundle) → `assertEmptyDir(dest)` prüft, dass weder `<tag>`- noch ein `.baseline-*`-Rest liegt. Mutation 13 (defer→no-op) rötet ihn (selbst gesehen). Das verbleibende Prozess-Tod-/Stat→Rename-Fenster ist EHRLICH benannt statt verschwiegen (§3.6): `baseline.go:163-171` (SIGKILL zwischen MkdirTemp und Rename → punkt-präfigierter `.baseline-*`-Rest, vom „$base"/*/-Glob des Verifiers übersehen, MR-007 Setzung 4) und `placeBaseline:183-194` (Stat→Rename-Race benigne, weil os.Rename auf nicht-leeres Ziel mit ENOTEMPTY scheitert — kein stilles Überschreiben; Schließen verlangte ein Lock über den ganzen Bootstrap, bei WIP=1 nicht kostenwert). Beides als AKZEPTIERTER Race begründet, nicht stillschweigend gelassen — genau die von DoD-4 verlangte Ehrlichkeit.

### DoD-5 — slice-022a L4 (Größen-Schranke vor dem Pin) aufgelöst — **CONFIRMED**

`readCapped(rc, maxBaselineBytes)` (`baseline.go:136`) läuft VOR dem sha256-Pin (`:144`) — statisch belegt (Zeilenordnung). `maxBaselineBytes = 8<<20` (8 MiB; reales Asset ~241 KB, ~34-fach Reserve), `readCapped` liest `limit+1` und meldet `AssetTooLargeError` bei Überschreitung statt still abzuschneiden (`:398-409`). `AssetTooLargeError` ist ein eigener Typ (via errors.As vom `SHA256Mismatch` trennbar) — ein Abwehr-Befund (LH-QA-03), kein Reproduzierbarkeits-Befund. `TestBaseline_AssetTooLarge` (8 MiB+1) prüft `errors.As` + `Max` + `assertEmptyDir`. Mutation 11 (`>limit` → `>limit+(1<<62)`) → Body kommt durch → SHA256Mismatch statt AssetTooLargeError → Test rot (selbst gesehen).

### DoD-6 — LH-QA-01 offline-grün, kein neues Gate — **CONFIRMED**

`git diff 7df00c5..68040f1` berührt KEINE Gate-Datei: Makefile / *.mk / .d-check.yml / .github/ / .a-check.yml sind leer im Path-Diff (selbst geprüft). Kein neuer Gate-Name in AGENTS.md §4. Der selbst gefahrene `make gates`-Lauf lief offline-grün (`baseline-verify` netzlos, `docs-check` `--network none`). Die neuen `mutations/11-14` speisen den Nicht-Gate-Sensor `make mutate`, nicht `make gates`. Kein halluziniertes Gate.

### DoD-7 — `make gates` grün — **CONFIRMED (selbst gefahren, Exit 0)**

`make gates` SELBST einmal gefahren → **GATES_EXIT=0**. Alle Gates real: `baseline-verify` (v3.5.0 OK — 42 Dateien, netzlos), `docs-check` (d-check @sha256:fede…, 91 Dateien / 0 Befunde, `--network none`), `lint` (golangci-lint v2.12.2 → 0 issues, Dockerfile-lint-Stage), `build` (cross-compile), `test` (bats `1..71` alle ok + `go test ./...`: cmd/emit/fetch alle ok), `shell-lint` (shellcheck), `ci-lint` (actionlint), `record-gates`. git status vor UND nach leer — `record-gates` mutiert keine getrackte Datei.

### DoD-8 — Closure-Notiz mit Steering-Loop-Lerneintrag — **CONFIRMED (korrekter Lifecycle-Zustand vor Closure)**

§7 des Slice ist noch das leere Template (`<!-- Erst nach Abschluss füllen. -->`). Das ist ein POST-Closure/Planner-Artefakt: Verify läuft VOR der Closure, der Slice liegt noch in `in-progress/`, DoD-Boxen offen. Kein Defekt zum Verify-Zeitpunkt (identisch zum Lifecycle in slice-021-verify DoD-7). Die substanzielle Zusage — die KLASSE erklären: warum drei Zuweisungen (004a→004b/005) nicht trugen und was der vierte Anlauf anders macht — wird beim `done/`-Move ergänzt und ist dann Gegenstand der Closure, nicht dieses Verify.

## Hard Rules / ADR

- **§3.1 (keine halluzinierten Gates):** CONFIRMED. Kein neuer Gate-Name; `make gates` offline-grün; die Mutationen speisen `make mutate` (Nicht-Gate).
- **§3.2 (keine Lint-Suppression im Diff):** CONFIRMED. `git diff 7df00c5..68040f1` enthält kein `//nolint`, kein `# shellcheck disable`, kein `d-check:ignore` (selbst gegrept, 0 Treffer).
- **§3.3 (Move rein vom Inhalt getrennt):** CONFIRMED. `5c6a44a` = 100 %-reiner Rename open→in-progress (0 Insertions / 0 Deletions, `git show --stat`); `68040f1` trägt den Inhalt. Die 3 Link-Reparaturen (in NICHT-umbenannten Dateien) liegen korrekt im Inhalts-Commit und ziehen nur den Navigations-Pfad `../open/…`→`../in-progress/…` nach, Prosa unangetastet.
- **§3.4 (keine ADR-Mutation):** CONFIRMED. Keine Datei unter `docs/plan/adr/` im Diff-Range (selbst geprüft, leer).
- **§3.5 (keine Gate-Lockerung ohne ADR):** CONFIRMED. Additive Härtung, keine Schwellen-Senkung / Modul-Aktivierung / Strenge-Senkung.
- **§3.6 (keine Zusage weiter als der Code):** CONFIRMED. Jede Zusage ist eng auf das gefasst, was der Code hält; die ehrlichen Grenzen (Fetch-Rest bei Runtime-Abbruch, --force ohne Rollback, Stat→Rename-Race) sind benannt statt zugesagt; der selbst gefahrene mutate-Lauf belegt, dass jeder GELISTETE Wächter Zähne hat (14/14).
- **ADR-0005-Konformität:** CONFIRMED. Keine Änderung an Herkunftsklassen / Fetch-Generate-Modell; der Pre-Flight ist CLI-Orchestrierung (architecture.md §2). „Keine ADR" ist mit ADR-0005 und architecture.md §2/§4 verträglich.

## Gesamt-Verdikt

**8/8 DoD-Zeilen CONFIRMED — 0 VIOLATED.** Zwei Nuancen, beide kein Blocker:

1. **DoD-2 (F-1-Reconciliation):** Der Code liefert die je-Schritt-Absicherung für die KOLLISIONS-Klasse (Fetch- UND Emit-Ziele, je Schritt getestet, motivierende Teil-Emit-Klasse geschlossen) und ist gegen Spec (LH-FA-01) und Plan §6 konform. Der DoD-Wortlaut Zeile 39 („wie vorgefunden, je Schritt", absolut) ist die einzige Über-Zusage — Plan §6 hat den Fetch-Rest bei Runtime-Abbruch/Emit-Kollision ausdrücklich als retry-freundlich sanktioniert, und der Code benennt die Grenze ehrlich (§3.6). Verdikt: bewusst-sanktionierte, korrekt eng gefasste Verengung, KEIN Code-Defekt. Empfehlung: DoD-39/Closure-Notiz auf das Gelieferte einengen (Doc-Reconciliation).
2. **DoD-8 (Closure-Notiz):** zum Verify-Zeitpunkt bewusst leer (Post-Closure-Planner-Schritt; Slice in in-progress/) — kein Defekt, sondern der korrekte Lifecycle-Zustand vor der Closure.

Zentrale Sensor-Belege selbst gefahren: `make gates` = Exit 0 (alle Gates real, Baum clean); `make mutate` = Exit 0, 14 ok / 0 Befund, die 4 neuen Wächter (11-14) je ihren benannten Test rot gesehen; `make smoke` grün im mutate-Grün-Vorlauf. Hard Rules §3.1-§3.6 und ADR-0005 konform.

## Empfehlung

**Verifikation bestanden — Closure freigegeben.** Beim `done/`-Move: (1) die 8 DoD-Boxen abhaken, (2) die Closure-Notiz (§7) mit dem Klasse-Steering-Loop-Eintrag füllen, (3) dabei den DoD-39-Wortlaut auf das gelieferte, Plan-§6-sanktionierte Verhalten einengen (Pre-Flight-Kollisionen je Schritt; Fetch-Rest bei Runtime-Abbruch bewusst retry-freundlich). Entsperrt slice-023 mit einer abgesicherten Kette. Nicht selbst gefahren als eigenständiges Target: `make smoke` (aber im mutate-Grün-Vorlauf real grün belegt).
