# Review-Report: slice-066 (Code) — 2026-08-08

**Review-Art:** **Code** — geprüft wird der fertige Diff gegen **Plan, aktive ADRs und Hard
Rules** (Modul 10 §Drei Review-Arten). **Nicht** geprüft: die DoD-Abhakung — sie ist Gegenstand
der Verifikation (Modul 11, eigener Kontext, anderes Prüf-Artefakt).

**Gegenstand:** `origin/main..HEAD`, drei Commits: `edf739d` · `4aa910a` · `5468395`
(16 Dateien, +744/−112; selbst nachgezählt: `git diff --stat` bestätigt beides).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** claude-opus-5[1m] · **Datum:** 2026-08-08

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne diese Liste ist der Lauf
nicht reproduzierbar):

- Slice-Plan `docs/plan/planning/in-progress/slice-066-telemetrie-auswertung.md`
- aktive ADRs (alle vier Status-Zeilen selbst gelesen, alle **Accepted**):
  [`ADR-0003`](../plan/adr/0003-go-native-binaries.md),
  [`ADR-0004`](../plan/adr/0004-durchsetzungs-emission.md),
  [`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md),
  [`ADR-0012`](../plan/adr/0012-haupt-kontext-ohne-token-bilanz.md)
- `LH-*`: [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
  [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit),
  [`LH-QA-03`](../../spec/lastenheft.md#lh-qa-03--minimale-abhängigkeiten)
- [`AGENTS.md`](../../AGENTS.md) §2 Source Precedence, §3 Hard Rules (§3.1–§3.6), §4, §5
- [`spec/spezifikation.md`](../../spec/spezifikation.md#5-metriken-und-tracing-felder) §5 samt
  §Aufnahme-Regel (die zwei Formregeln)
- Regelwerk `v3.5.2`: `modul-09-implementierung.md`, `modul-10-review-harness.md`,
  Auszüge aus `modul-06-roadmap.md`, `modul-08-agentenrollen.md`,
  `modul-15-observability.md` §Token-Attributions-Regeln
- **vorherige Findings am gleichen Modul:** `docs/reviews/2026-08-08-slice-066-plan-review.md`
  (3 HIGH / 7 MEDIUM). Zwei Klassen daraus sind für diesen Lauf tragend: **F-1** *(Planner-
  Artefakt in der Implementer-Rolle geändert — HIGH)* und **F-7/F-8** *(Bezugsmenge einer Zahl
  falsch benannt bzw. nach neuer Messung nicht nachgezogen — MEDIUM, dort bereits als dritte
  Wiederholung markiert)*.

**Mess-Grundlage.** Alle Zahlen dieses Reports sind selbst erhoben, gegen den Span-Bestand
`.harness/state/spans/` im Stand **2026-08-08T~18:25Z** (6.081 Zeilen über alle `*.jsonl`,
drei Sitzungs-Ströme). Der laufende Strom wächst während der Messung; die Verhältnisse tragen
die Aussagen, nicht die Ziffern. Gefahren wurden `make help` und `make span-report`;
`make gates`/`make mutate` wurden **nicht** wiederholt (laut Auftrag grün bzw. 142 ok / 0).

---

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills. HIGH zuerst, dann absteigend.

### F-1 — Der Nenner der Splitting-Regel zählt Spans mit, die §5 selbst ausdrücklich als „kein Tool-Call" führt

- `kategorie`: **MEDIUM**
- `quelle`: [`spec/spezifikation.md`](../../spec/spezifikation.md#5-metriken-und-tracing-felder)
  §5 — die neue Festlegung *„im Verhältnis ihrer **Tool-Calls**"* (`spec/spezifikation.md:407-409`)
  gegen denselben Abschnitt zu `SubagentStart`: *„**Kein** `tool_name` und **kein** `tool_use_id`
  — es ist kein Tool-Call"* (`spec/spezifikation.md:304-305`) · Reviewer-Skill §MEDIUM
  (*Spec-Treue-Lücke einer Messmethode*)
- `pfad`: `internal/report/report.go:120-122`
- `befund`: `verarbeite` erhöht `toolCalls[s.AgentRole]` für **jede** Zeile mit gefüllter Rolle,
  ohne Filter auf `event` oder `tool`; ein `SubagentStart`-Span trägt eine abgeleitete Rolle
  (gemessen: `agent_type: reviewer` → `agent_role: reviewer`) und liegt im Strom des
  **gestarteten** Agenten, wandert also in dessen Nenner. Selbst gemessen: 6 `SubagentStart`-Spans
  im Bestand, davon **4 mit gefüllter Rolle** (2× `reviewer`, 2× `planner`), gegen **4.667**
  rollen-tragende Spans insgesamt.
- `failure-szenario`: Jeder Spawn erhöht den Nenner der **gespawnten** Rolle um 1, ohne dass ein
  Tool-Call stattgefunden hätte. Häufig gestartete Rollen bekommen dadurch dauerhaft einen zu
  großen Anteil des Sammelpostens zugeteilt — in einer Zeile, die die Ausgabe wörtlich mit
  *„anteilig nach Tool-Calls verteilt"* beschriftet. Heute 4/4.667 = 0,09 %; die Größe wächst mit
  jedem Spawn, und kein Test bindet den Nenner an Tool-Calls.
- `verifizierbar`: **ja, aber kein Gate** — `make span-report` gegen
  `grep -c '"event":"SubagentStart"' .harness/state/spans/*.jsonl` und die Zahl der Spans mit
  nicht-leerem `agent_role`; `make gates` und `make mutate` sind hierfür blind (kein Fall in
  `test/mutations/` adressiert die Zusammensetzung des Nenners).

### F-2 — Im von §5 geregelten Fall „keine Rolle trägt Tool-Calls" behauptet die Ausgabe eine Verteilung, die nicht stattgefunden hat, und nennt den Bestand leer, obwohl er Zähler trägt

- `kategorie`: **MEDIUM**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 (*„die Zusage auf das einschränken, was der Code
  hält"*) · [`ADR-0012`](../plan/adr/0012-haupt-kontext-ohne-token-bilanz.md) §Festlegung 2
  (die Ausgabe sagt, worüber sie rechnet) ·
  [`spec/spezifikation.md`](../../spec/spezifikation.md#5-metriken-und-tracing-felder) §5
  Prüfreihenfolge Punkt 2 (*wie groß der aufgeteilte Anteil war*)
- `pfad`: `internal/report/report.go:216-218` und `internal/report/report.go:229-230`
- `befund`: Trägt kein Span eine Rolle, bleibt `summeCalls == 0`; die Rollen-Liste ist leer,
  `Gesamt` ist 0 und der Sammelposten fällt **ganz** aus der Bilanz. Die Ausgabe druckt dann
  gleichzeitig *„Der Bestand ist leer oder ohne Zaehler"*, *„Sammelposten: N Token **anteilig
  nach Tool-Calls verteilt** (0.00 %)"* und *„Abdeckung: 1 von 1 Agent-Laeufen trugen Zaehler"*.
  Selbst gefahren gegen einen zweizeiligen Bestand (`docker run` auf dem von
  `make span-report` gebauten Image): 150 Token gemessen, 0 verteilt, Bestand als leer gemeldet.
- `failure-szenario`: Nach `make span-clean` läuft eine Sitzung, in der nur rollenlose
  Subagenten-Typen starten. Der Bericht meldet dem Leser einen leeren Bestand, obwohl er Zähler
  trägt, und behauptet in der Zeile darunter eine Verteilung, die die Festlegung für genau diesen
  Fall ausschließt. Beide Sätze sind einzeln bewacht (140/141), keiner der Wächter sieht diesen
  Zustand.
- `verifizierbar`: **ja, aber kein Gate** — reproduzierbar mit dem `report`-Image über einem
  Bestand aus einem `Agent`-Span mit Zählern ohne `spawned_role` plus einem Span mit leerem
  `agent_role`; `make gates`/`make mutate` decken den Zustand nicht ab.

### F-3 — Plan-Begründung und Lieferung beziffern denselben Sammelposten in zwei verschiedenen Größen; nur die Ausgabe sagt, in welcher

- `kategorie`: **MEDIUM**
- `quelle`: Slice-Plan DoD (1) (*„**Input- und Output-Token** summiert je Rolle"*) gegen Frage A
  Punkt (4) desselben Plans · `modul-15-observability.md` §Token-Attributions-Regeln
  (*„Summiere Input- und Output-Token pro `agent.role`"*) · Reviewer-Skill §MEDIUM
  (*Spec-Treue-Lücke einer Messmethode*) · wiederkehrende Klasse: Plan-Review F-7/F-8
- `pfad`: `docs/plan/planning/in-progress/slice-066-telemetrie-auswertung.md:141` (Frage A,
  Punkt 4) gegen `internal/report/report.go:132-139`
- `befund`: Frage A (4) beziffert den Sammelposten mit *„21.953 Token, rund 0,16 % der erfassten
  Summe"*. Selbst nachgerechnet über denselben Bestand: 21.953 ist der `total_tokens`-Wert jenes
  Spans, und 0,16 % ist sein Anteil an der `total_tokens`-Summe (13.978.516). Die gelieferte
  Bilanz summiert `input_tokens + output_tokens` — dort ist derselbe Span **227 Token** und
  **0,07 %** von 319.388. Die Ausgabe benennt ihre Größe in Zeile 2; der Plan benennt seine an
  keiner Stelle.
- `failure-szenario`: Der Verifier hält `make span-report` (*„Sammelposten: 227 Token … 0.07 %"*)
  gegen die Plan-Begründung (*„0,16 %"*), findet einen Faktor 2,3 und meldet entweder eine
  Abweichung, die keine ist, oder verlangt die Umstellung auf `total_tokens` — womit DoD (1) und
  die Modul-15-Regel gebrochen wären. Die **gelieferte** Seite ist hier die konforme.
- `verifizierbar`: **ja, aber kein Gate** — `make span-report` gegen eine `total_tokens`-Summe
  über `.harness/state/spans/*.jsonl`; kein Sensor liest Plan-Zahlen.

### F-4 — Ein Planner-Artefakt ist im Implementations-Lauf geändert worden; dieselbe Klasse trug im Plan-Review dieses Slice ein HIGH

- `kategorie`: **MEDIUM**
- `quelle`: Regelwerk `modul-08-agentenrollen.md` §Rollen-Sequenz (*„keine Rolle springt
  rückwärts in eine vorhergehende, ohne Übergabe-Artefakt"*) · `modul-06-roadmap.md`
  (Roadmap als Planner-Artefakt) · [`AGENTS.md`](../../AGENTS.md) §2 (Roadmap = Rang 5 der
  Source Precedence) · Präzedenz: `docs/reviews/2026-08-08-slice-066-plan-review.md` F-1 (HIGH)
- `pfad`: `docs/plan/planning/in-progress/roadmap.md:46` (Commit `4aa910a`)
- `befund`: `4aa910a` fügt der Kandidaten-Tabelle der Roadmap eine vollständige Zeile samt
  Aufwands-Einstufung **M** hinzu. Die Zeile schlägt die Umstrukturierung von
  `spec/spezifikation.md` §5 vor — desselben Abschnitts, den derselbe Lauf in `edf739d` geändert
  hat. Ein Übergabe-Artefakt an den Planner existiert nicht; die Rollen-Zuordnung ist am Artefakt
  selbst nicht ablesbar (der Commit trägt wie alle früheren Roadmap-Commits ein `roadmap:`-Präfix)
  und stammt aus der Gegenstands-Definition dieses Reviews.
- `failure-szenario`: Der nächste Wellen-Schnitt liest die Kandidaten-Tabelle als die
  priorisierte Liste des Planners und kann nicht erkennen, dass Zeile und Aufwands-Einstufung
  aus dem implementierenden Kontext stammen — also nie gegen WIP-Limit, Wellen-Umfang und die
  übrigen Kandidaten abgewogen wurden. Die Einstufung ist damit eine Selbst-Zuweisung, die im
  Ergebnis nicht sichtbar ist.
- `verifizierbar`: **nein, kein Gate** — `doc-planning` (d-check-Modul `planning`) ist in
  `.d-check.yml` `modules:` nicht aktiviert; `git log --format='%h %s' origin/main..HEAD` zeigt
  den Commit, nicht die Rolle.

### F-5 — §5 erlaubt in einem Satz, was seine eigene bindende Prüfreihenfolge als Pflicht setzt

- `kategorie`: **LOW**
- `quelle`: [`spec/spezifikation.md`](../../spec/spezifikation.md#5-metriken-und-tracing-felder)
  §5, Prüfreihenfolge **Punkt 1** (*„**Pflicht:** … am Ende liegt jedes Token auf einer der
  realen Rollen, nicht auf *unbekannt*"*)
- `pfad`: `spec/spezifikation.md:409-411` gegen `spec/spezifikation.md:435-436`
- `befund`: Die neue Festlegung schließt mit *„Trägt keine Rolle Tool-Calls, bleibt er
  **unverteilt**"*. Punkt 1 derselben Sektion erklärt die vollständige Verteilung zur Pflicht.
  Der Ausnahmefall wird nicht als Ausnahme markiert, und Punkt 1 bleibt unverändert stehen.
- `failure-szenario`: Der nächste Auswerter über demselben Bestand
  (`docs/plan/planning/open/slice-071-cache-zaehler-getrennt.md` übernimmt die Antworten dieses
  Slice ausdrücklich) liest zwei Anweisungen, die sich im Grenzfall widersprechen, und
  entscheidet ihn erneut — an einer zweiten Stelle, mit einem zweiten Ergebnis.
- `verifizierbar`: **nein, kein Gate** — `make docs-check` prüft Links, Anker, Kennungen,
  Matrix, Codepfade und Spans, keine Aussagen.

### F-6 — Die Ganzzahl-Division lässt Token aus der Bilanz fallen; die Ausgabe nennt sie trotzdem als verteilt

- `kategorie`: **LOW**
- `quelle`: [`spec/spezifikation.md`](../../spec/spezifikation.md#5-metriken-und-tracing-felder)
  §5 Prüfreihenfolge Punkt 1 · [`AGENTS.md`](../../AGENTS.md) §3.6
- `pfad`: `internal/report/report.go:175` und `internal/report/report.go:229-230`
- `befund`: `sammelposten * toolCalls[n] / summeCalls` schneidet je Rolle ab. Selbst
  nachgerechnet aus dem realen `make span-report`-Lauf: die Zeile meldet **227 Token … verteilt**,
  die Summe der Zuteilungen ist **225** (66 + 66 + 55 + 21 + 17); 2 Token liegen auf keiner Rolle
  und fehlen in `Gesamt`. Der Fehlbetrag ist durch (Rollenzahl − 1) beschränkt.
- `failure-szenario`: Ein Leser, der die Rollen-Zeilen aufsummiert und mit der genannten
  Sammelposten-Zahl gegenrechnet, findet eine Differenz und sucht sie im Bestand statt in der
  Rechnung. Die Zusage *„verteilt"* reicht in dieser Zeile weiter als der Code.
- `verifizierbar`: **ja, ohne Gate** — `make span-report`, Rollen-Zeilen aufsummieren und gegen
  die Sammelposten-Zeile halten.

### F-7 — Die Makefile-Kürzung entfernt an vier Stellen die Begründung einer Bau-Eigenschaft, nicht nur Entstehungs-Prosa

- `kategorie`: **LOW**
- `quelle`: Maintainability · [`AGENTS.md`](../../AGENTS.md) §3.6 (eine Eigenschaft, deren
  Begründung verschwindet, verliert ihren Feedforward-Quadranten)
- `pfad`: `Makefile:110` (`smoke`), `Makefile:146` (`baseline-verify`), `Makefile:156`
  (`regelwerk-check`), `Makefile:166` (`baseline-freshness`)
- `befund`: Ersatzlos entfallen sind (jeweils selbst im `git diff` nachgelesen) die Sätze
  *„Logik in `harness/tools/`, damit `shell-lint` sie deckt"* an **drei** Zielen, dazu
  *„Fetch↔Vergleich getrennt (hermetisch testbar)"* bei `baseline-freshness` und
  *„auf `sources` isoliert (die Doku-Module deckt `docs-check` ab)"* bei `regelwerk-check` — die
  einzige Erklärung der sechs `--disable`-Flags in dessen Rezept. Die übrige Kürzung ist geprüft
  und trägt: Docker-only-, `LH-QA-01`-, `LH-QA-02`-, `MR-003`-, `MR-007`- und
  `ADR-0011`-Festlegung-3-Bezüge sowie die Reihenfolge-Begründung von `gates` sind erhalten,
  und die Zusagen zu `smoke`/`span-emit-build` stehen zusätzlich in
  [`AGENTS.md`](../../AGENTS.md) §4 bzw. im `Dockerfile`.
- `failure-szenario`: Ein späterer Wartungsschritt zieht die Logik eines dieser Ziele als
  mehrzeiligen Shell-Block zurück ins Rezept oder streicht die `--disable`-Flags. Beides ist ohne
  die entfernten Sätze nicht mehr als Rückschritt erkennbar — `make shell-lint` sieht nur
  `harness/tools/*.sh`, den Makefile-Rezeptkörper deckt kein Linter.
- `verifizierbar`: **nein, kein Gate** — `make comment-claims` prüft vier Pfad-Muster und
  schließt den `Makefile` dauerhaft aus ([`AGENTS.md`](../../AGENTS.md) §4).

### F-8 — Zwei Zahlenangaben im Commit-Text von `edf739d` treffen den Ist-Stand nicht

- `kategorie`: **LOW**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 (die Commit-Message ist dort ausdrücklich als
  Zusage-Form genannt)
- `pfad`: Commit-Message `edf739d`, Absatz *„IM SELBEN Makefile-Diff …"*
- `befund`: (a) *„277 → 252 Zeilen"* — selbst gezählt: `git show origin/main:Makefile | wc -l`
  = **270**, `wc -l < Makefile` = **254**. (b) *„alle 39 Ziele hiessen ‚Makefile'"* — die alte
  `help`-Pipeline selbst nachgefahren: vor der Änderung standen **38** Einträge, davon **27** mit
  dem Präfix `Makefile` und **11** mit `d-check.mk`; die 39 ist der Stand **nach** dem neuen
  Ziel. Der Defekt und seine Behebung sind korrekt beschrieben, die zwei Mengenangaben nicht.
- `failure-szenario`: Die Commit-Message ist der einzige Träger dieser Messung; wer sie später
  als Beleg für den Umfang der Kürzung heranzieht, zitiert eine Zahl, die der Baum widerlegt.
- `verifizierbar`: **ja, ohne Gate** — die zwei `wc -l` oben; für (b) die alte Pipeline gegen
  `git show origin/main:Makefile` und `git show origin/main:d-check.mk`.

### F-9 — Zwei Angaben der neuen Roadmap-Zeile sind falsch bzw. innerhalb desselben Commit-Bereichs veraltet

- `kategorie`: **LOW**
- `quelle`: Maintainability · [`AGENTS.md`](../../AGENTS.md) §3.4 (Immutabilitäts-Datum einer
  Accepted-ADR)
- `pfad`: `docs/plan/planning/in-progress/roadmap.md:46`
- `befund`: (a) *„auf §5 zeigt `ADR-0012`, seit **2026-08-08** Accepted und immutabel"* — die
  Geschichte-Tabelle der ADR datiert die Annahme auf **2026-08-03**, und
  `git log -S'**Status:** Accepted'` weist sie `5851e83` vom **2026-08-03** zu. (b) *„138 reale
  Vorkommen"* des Mutations-Fall-Formats — bei `4aa910a` selbst gezählt: 138 Dateien unter
  `test/mutations/`; bei `HEAD` sind es **142**, verschoben durch den nächsten Commit desselben
  Bereichs (`5468395`). Die übrigen Mengenangaben der Zeile sind geprüft und stimmen:
  696 Zeilen · 629 in §5 · 90,4 % · 31 `test/`-Vorkommen in §5 · `codepaths.roots` =
  `[spec, docs, harness]`.
- `failure-szenario`: Wer aus (a) das Immutabilitäts-Fenster ableitet, hält fünf Tage
  Änderungshistorie für zulässig, die es nach §3.4 nicht war; (b) lässt einen späteren Leser die
  ganze Zeile nachmessen und finden, dass die erste geprüfte Zahl nicht stimmt.
- `verifizierbar`: **ja, ohne Gate** — `grep 'Accepted' docs/plan/adr/0012-*.md` und
  `git ls-tree -r --name-only HEAD test/mutations/ | wc -l`.

### F-10 — Fall 145 färbt den Wächter von 144 mit rot; tragbar, aber der Fall-Kommentar nennt nicht den Zweig, der wirklich auslöst

- `kategorie`: **INFO**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 · `harness/tools/mutate.sh` §FAIL-CLOSED
  Bedingung 4 (*„der ERWARTETE Wächter steht in der Fehlschlag-Ausgabe"* — Zusatz-Rot ist
  zulässig)
- `pfad`: `test/mutations/145-report-rollenlose-im-nenner.sh:15-16` gegen
  `internal/report/report_test.go:90-94`
- `befund`: Die Mutation ersetzt `if s.AgentRole != ""` durch `if true` und legt damit **zwei**
  Eigenschaften um: die rollenlosen Calls kommen in den Nenner **und** es entsteht eine Rolle mit
  leerem Namen. `TestAggregiere_SammelpostenWirdAnteiligVerteilt` fällt an seiner
  `r.Name == ""`-Zusicherung, also am zweiten Effekt — der Kommentar begründet das Zusatz-Rot
  mit *„auch dessen Bestand traegt rollenlose Calls"*. Beide Wächter behalten ihren
  ausschließlichen Fall (140–146 selbst gelesen: kein zweiter Fall nennt einen der beiden), die
  Spezifität ist also nicht verloren.
- `failure-szenario`: Wird die `r.Name == ""`-Zusicherung aus 144s Test entfernt, verschwindet
  das Zusatz-Rot; der Kommentar von 145 beschreibt dann einen Zustand, den es nicht mehr gibt,
  und `make mutate` bleibt grün — die Fehl-Erklärung ist unbewacht.
- `verifizierbar`: **ja** — `make mutate` (Einzelzeilen je Fall im Log).

### F-11 — `span-report` ist das einzige `span-*`-Ziel ohne dokumentierten Ort

- `kategorie`: **INFO**
- `quelle`: Maintainability · [`AGENTS.md`](../../AGENTS.md) §4 (§Nicht-Gate-Verify nennt
  `smoke`, `full-smoke`, `mutate`, `regelwerk-check`, `baseline-freshness`)
- `pfad`: `Makefile:243` · Prüfbereich der Vollständigkeitsaussage: `grep -rn 'span-report'`
  über den ganzen Baum ohne `.git/`
- `befund`: `span-report` kommt in genau drei Dateien vor — `Makefile`, `Dockerfile`,
  `cmd/span-report/main.go`. `span-emit-build` und `span-check` stehen in
  [`AGENTS.md`](../../AGENTS.md) §4 und [`harness/README.md`](../../harness/README.md) §Sensors,
  `span-clean` zweimal in `spec/spezifikation.md` §5; für `span-report` gibt es keinen solchen
  Ort. Ein Gate ist dadurch **nicht** halluziniert (§3.1 betrifft genannte Gates, und
  `span-report` wird nirgends als Gate genannt).
- `failure-szenario`: Ein Leser der beiden Tabellen kennt das Ziel nicht und wertet den
  Span-Bestand von Hand aus.
- `verifizierbar`: **nein, kein Gate** — der d-check-Modul `targets` (Doku↔Makefile) ist in
  `.d-check.yml` `modules:` nicht aktiviert; die Roadmap führt genau diese Lücke als Kandidat.

### F-12 — Ein fehlender Ablageort ist von einem leeren nicht unterscheidbar

- `kategorie`: **INFO**
- `quelle`: [`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md) Festlegung 1 Punkt 4
  (*leer und als leer erkennbar*, nicht geraten)
- `pfad`: `internal/report/report.go:66-70` · `cmd/span-report/main.go:21`
- `befund`: `filepath.Glob` über ein nicht existierendes Verzeichnis liefert kein Ergebnis und
  keinen Fehler; die Auswertung druckt dieselbe wohlgeformte leere Bilanz wie über einem real
  leeren Bestand. Unter `make span-report` maskiert das vorangestellte `mkdir -p` den Fall; ein
  direkter Container-Lauf mit falschem Mount tut es nicht. Die Annahme *„der Ablageort
  existiert"* steht nirgends.
- `failure-szenario`: Ein vertippter Mount-Pfad liefert einen plausiblen leeren Bericht statt
  eines Fehlers.
- `verifizierbar`: **ja** — `docker run … -v <falscher-pfad>:/spans:ro ai-harness-init:report`.

## Negativbefunde

- geprüft, ohne Befund: **Hard Rule §3.2 (Suppression-Verbot).** `grep -rn 'nolint\|shellcheck
  disable'` über `*.go`, `*.sh`, `Dockerfile`, `Makefile` ohne `.git/` und ohne
  `internal/emit/templates/` → **0 Treffer**; auch `git diff origin/main..HEAD -- '*.go' | grep
  nolint` ist leer. Das zwischenzeitlich gesetzte `//nolint` ist im Endstand nicht vorhanden.
- geprüft, ohne Befund: **`ADR-0003` (Docker-only).** Der Bau läuft ausschließlich über
  `docker build --target report` im per Digest gepinnten `deps`-Image mit `--build-arg
  GO_VERSION`; der Lauf über `docker run --rm --network none` mit `:ro`-Mount. Kein
  Host-`go`/`pip`/`npm` im Diff, `go.mod`/`go.sum` unverändert (`LH-QA-03`).
- geprüft, ohne Befund: **Dockerfile-Stage-Reihenfolge.** Alle sieben `docker build`-Aufrufe in
  `Makefile` und `harness/tools/*.sh` nennen ein `--target`; die neue letzte Stage `report`
  ändert daher keinen Default-Build. Kein `artifact-copy`, kein Host-Binary — konsistent mit der
  Plan-Zeile und der Stage-Begründung.
- geprüft, ohne Befund: **Formregeln von `spec/spezifikation.md`** (§Aufnahme-Regel). Der neue
  bindende Text (`spec/spezifikation.md:407-423`) trägt **keine** ADR-, MR-, `LH-`- oder
  `slice-`-Kennung, enthält keinen Link und zeigt nirgends abwärts; er verweist ausschließlich
  dokument-intern (*„Punkt 1/2 der Prüfreihenfolge unten"*). Die Abschnittsnummern sind
  unverändert.
- geprüft, ohne Befund: **inhaltliche Deckung Festlegung ↔ `verteile()`.** Verteilt wird auf die
  Vereinigung der Rollen aus `direkt` und `toolCalls`, beide Schlüsselmengen sind
  konstruktionsbedingt nicht-leer; `summeCalls` summiert nur über diese Menge, rollenlose Calls
  bleiben also aus dem Nenner. Die Sortierung führt die größte Rolle auf Position 0, was die
  „Größte Rolle"-Zeile trägt. (Die zwei Randfälle stehen als F-2 und F-6.)
- geprüft, ohne Befund: **Modul 15 §Token-Attributions-Regeln, Kern.** Summiert werden `input +
  output` (nicht `total_tokens`) wie vom Modul verlangt; die größte Rolle steht als **Zahl und
  Prozentsatz**; der Sammelposten wird verteilt und **nicht** als eigene Zeile geführt
  (Prüfreihenfolge Punkt 3) — im realen Lauf und im Test `…SammelpostenWirdAnteiligVerteilt`
  einzeln nachgesehen.
- geprüft, ohne Befund: **`ADR-0012` Fitness Function.** Beide Tabellenzeilen sind eingelöst:
  `TestSchreibe_NennerStehtDrin` prüft **beide** Hälften der Nenner-Aussage
  (`Subagenten-Laeufe` **und** `nicht ueber den Lauf`), `test/mutations/140` entfernt genau diese
  Angabe. Die drei Größen (Nenner · Sammelposten-Anteil · Abdeckung) stehen getrennt und sind
  einzeln bewacht — keine ist mit einer anderen zusammengelegt.
- geprüft, ohne Befund: **`ADR-0011`.** Die Auswertung liest ausschließlich Spans (`*.jsonl`
  unter dem Ablageort), netzlos, read-only; kein Transkript, keine Quelle außerhalb des Repos.
  Der Re-Evaluierungs-Trigger *„nach dem ersten Auswertungs-Slice erfasst kein Werkzeug über den
  Default hinaus"* greift nicht — der Emitter erfasst `path`/`bytes`/`sha256_16`/`program`/`argc`.
- geprüft, ohne Befund: **superseded ADRs.** Alle im Diff referenzierten ADRs (0003, 0004, 0011,
  0012) tragen Status **Accepted**; Statuszeilen einzeln gelesen.
- geprüft, ohne Befund: **`gates`-Kette und Hard Rule §3.5.** Die `gates:`-Prerequisitenliste ist
  im Diff nur Kontext, byte-identisch; kein Gate entfernt, keine Schwelle gesenkt, `span-report`
  nicht eingehängt.
- geprüft, ohne Befund: **`make help`.** Real gefahren: 39 Einträge, korrekte Zielnamen, C-Sortierung
  (`test` vor `test-bats`), Spaltenbreite 18 deckt den längsten Namen (`baseline-freshness`,
  `freshness-golangci`). Die `-h`-Begründung im Kommentar ist zutreffend — ohne sie trennt `awk`
  am Doppelpunkt des Dateinamen-Präfixes.
- geprüft, ohne Befund: **Mutations-Muster 140–146.** Jedes `sed`-Muster selbst gegen
  `internal/report/report.go` gezählt: **je genau ein Treffer**, keine Über-Reichweite. Jeder Fall
  nennt einen `# expect:`-Wächter, der im Testkorpus existiert.
- geprüft, ohne Befund: **`harness/tools/extract-agent-call.awk`.** Der korrigierte Kopfkommentar
  trifft zu — die Abdeckungszahl zählt innerhalb derselben Quelle und sieht einen Lauf ohne Span
  nicht; es ist eine Negativaussage, die keinen Sensor verlangt. `.awk` liegt ohnehin dauerhaft
  außerhalb des `comment-claims`-Prüfbereichs.
- geprüft, ohne Befund: **Hard Rules §3.1, §3.3, §3.4.** Kein Gate behauptet
  (`span-report` ist an allen drei genannten Orten kein Gate); kein `git mv` im Bereich; keine
  Datei unter `docs/plan/adr/` berührt.
- geprüft, ohne Befund: **Plan-Tabelle ↔ Diff.** Alle sechs Zeilen der Änderungs-Tabelle sind
  gedeckt; die `.claude/settings.json`-Zeile ist außerhalb dieses Bereichs in `faa3631`
  eingelöst (selbst nachgesehen, Verdrahtung vorhanden). Der Arbeitsbaum ist sauber
  (`git status --porcelain` leer).
- geprüft, ohne Befund: **Sicherheitsfläche der neuen Ausgabe.** Der Bericht druckt
  ausschließlich Rollennamen, Summen, Prozentsätze, Sitzungszahl und Zeitgrenzen — keine Pfade,
  Kommandos, Fingerabdrücke oder Payload-Reste. Keine neue Leak-Fläche gegenüber `ADR-0011`.
- **nicht geprüft** (und deshalb nicht behauptet): die Behauptung der neuen Roadmap-Zeile über
  *drei einander widersprechende Exit-Code-Familien*. Verifiziert ist nur, dass die
  Freshness-Familie `0/1/2` im `Makefile` und in `harness/tools/baseline-freshness.sh` so steht
  und dass die Hooks dieses Repos durchweg `exit 0` verwenden; die Vollständigkeit der drei
  Familien wurde nicht gemessen.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 4 |
| LOW | 5 |
| INFO | 3 |

## Verdikt

**NICHT KONFORM** — merge-blockierend: **ja**, wegen der vier MEDIUM (F-1 bis F-4). HIGH: keines.

Die Substanz des Slice trägt: die Aggregation setzt die Modul-15-Regel richtig um, die
Festlegung steht formkonform im Technik-Stratum, die Fitness Function aus
[`ADR-0012`](../plan/adr/0012-haupt-kontext-ohne-token-bilanz.md) ist eingelöst, und die sieben
Mutations-Fälle greifen nachweislich je genau einmal. Blockierend sind zwei
Mess-/Aussage-Defekte an der Ausgabe (F-1, F-2), eine Größen-Diskrepanz zwischen Plan-Begründung
und Lieferung, bei der die **gelieferte** Seite die konforme ist (F-3), und eine Rollen-Grenze,
deren Klasse im Plan-Review desselben Slice bereits als HIGH stand (F-4) — nach Reviewer-Skill
§Kontext-Eskalation wird eine wiederkehrende Klasse geschärft, nicht abgeschwächt.

**Steering-Loop-Signal.** F-3 ist die dritte gemessene Instanz der Klasse *„eine Zahl ohne ihre
Bezugsgröße"* in diesem Slice (Plan-Review F-7 und F-8 waren die ersten beiden). Sie wandert
mit: erst im Plan, jetzt zwischen Plan und Ausgabe. Nach Modul 10 §Pflege gehört an dieser
Stelle eine geschärfte Regel oder ein Sensor, nicht ein drittes Einzel-Finding.

**Übergabe:** Findings gehen an die Implementation; F-3 hat seine Ursache im Plan und damit die
Rückkante Review → Plan (Planner). Der Report ersetzt keine Verifikation — DoD- und
Spec-Konformität prüft der Verifier separat (Modul 11).
