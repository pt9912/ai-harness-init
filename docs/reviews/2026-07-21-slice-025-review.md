# Review-Report: slice-025 (Bootstrap-Pre-Flight) — 2026-07-21

**Review-Art:** Code — der fertige Diff gegen Plan + Konventionen (AGENTS.md,
Hard Rules), Modul 10 §Drei Review-Arten.

**Gegenstand:** slice-025 · `git diff 7df00c5..68040f1` (zwei Commits: `5c6a44a`
reiner Move `open/`→`in-progress/`, `68040f1` Inhalt + Tests + 4 Mutationen +
3 Link-Reparaturen).

**Skill:** `.harness/skills/reviewer.md` @ 1.2.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** Claude Opus 4.8 (1M context) · **Datum:** 2026-07-21

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne
diese Liste ist der Lauf nicht reproduzierbar):

- Slice-Plan: `docs/plan/planning/in-progress/slice-025-bootstrap-preflight.md`
  (§2 DoD, §3 Plan-Tabelle, §6 Risiken/Entwurfs-Entscheidung)
- Aktive ADR: ADR-0005 (Ziel-Repo-Distribution — Fetch+Generate); Slice schreibt
  bewusst KEINE neue ADR
- Berührte `LH-*`-IDs: LH-FA-01, LH-QA-01, LH-QA-03 (`spec/lastenheft.md`)
- `AGENTS.md` §3 (Hard Rules 3.1–3.6), `harness/conventions.md` (MR-007/008/009/001)
- Vorherige Findings am gleichen Modul: `docs/reviews/2026-07-20-slice-022a-review.md`
  (+re-review), `docs/reviews/2026-07-20-slice-022b-review.md` (+re-review) —
  022a L3 (Temp-Rest), L4 (unbegrenzter Puffer), I1 (Teil-Bootstrap); 022b F-3
  (templatesDir ohne Zusicherung). slice-025 löst genau diese auf.

---

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills.

### F-1 — DoD-Prosa „in dem Zustand, in dem er es vorfand" ist strenger als das gelieferte (bewusst gewählte) Verhalten

- `kategorie`: INFO
- `quelle`: LH-FA-01 (Plan §2 DoD)
- `pfad`: `docs/plan/planning/in-progress/slice-025-bootstrap-preflight.md:39`
  vs. `cmd/ai-harness-init/main.go:95-145` (bootstrap: Phase-2/3-Trennung +
  EHRLICHE GRENZE), Package-Doc `main.go:1-16`
- `befund`: Die DoD-Zeile sagt absolut zu, ein an *irgendeinem* Schritt
  gescheiterter Bootstrap „hinterlässt das Zielrepo in dem Zustand, in dem er
  es vorfand". Geliefert wird das nur für Phase-1-Kollisionen (Fetch); eine
  Phase-3-Emit-Kollision und ein Runtime-Abbruch während Fetch/Docker lassen das
  gefetchte `.harness/` bewusst liegen (Package-Doc „schreibt der *jeweilige*
  Block NICHTS" + EHRLICHE GRENZE; `TestRun_EmitKollisionSchreibtKeinEmit`
  *asserted* das Bestehenbleiben der Baseline als „retry-freundlich"). Der Code
  überverspricht **nicht** — jede Code-Zusage ist eng gefasst, und Plan §6 hat
  die retry-freundliche Verengung ausdrücklich sanktioniert. Die Spannung liegt
  allein zwischen dem DoD-Häkchen-Wortlaut und der Semantik.
- `verifizierbar`: nein — Design-/Abgrenzungs-Zusage, kein Gate; die Reconciliation
  des DoD-Wortlauts ist Sache des Verifiers (getrenntes Prüf-Artefakt, Modul 11),
  nicht dieses Reviews.

### F-2 — Eingefrorene done/-Prosa „unverändert offen" ist nach dem Lifecycle-Move veraltet

- `kategorie`: INFO
- `quelle`: Maintainability (Doku-Drift)
- `pfad`: `docs/plan/planning/done/slice-022b-embed-raus.md:144`
- `befund`: Die Zeile lautet weiter „slice-025 — unverändert offen", während
  slice-025 jetzt in `in-progress/` liegt; der Diff hat korrekt nur den
  Navigations-Pfad (`../open/…`→`../in-progress/…`) nachgezogen und die Prosa
  eines eingefrorenen done/-Slices unangetastet gelassen. Das Wort „offen"
  beschreibt damit einen historischen Stand und ist won't-fix (Frozen-Doc).
- `verifizierbar`: nein — die Aussage ist Prosa, kein referenzierbarer Pfad;
  `make docs-check` prüft nur den (reparierten) Link, nicht das umgebende Wort.

## Negativbefunde

- geprüft, ohne Befund: die vier neuen Mutationen `test/mutations/11-14` — jede
  ist tragend (11: gekapptes `>limit`→Body kommt durch→`SHA256Mismatch` statt
  `AssetTooLargeError`, rot; 13: `defer`-No-op→`.baseline-*`-Rest→`assertEmptyDir`
  rot; 14: Fetch-Pre-Flight-Liste leer→`.harness/skeleton` liegt trotz Kollision,
  rot). Der vom Implementer berichtete frühere Stille-Grün-Fall ist geschlossen:
  Print UND Return liegen im selben `if`-Block (`main.go:138-141`, ebenso der
  Phase-1-Block), und Mutation
  12 rötet `TestRun_EmitKollisionSchreibtKeinEmit` über **zwei unabhängige**
  Assertionen (fehlt Docker → „existiert bereits" bleibt aus; ist Docker da →
  `d-check.mk` würde geschrieben) — kein Silent-Green-Restpfad.
- geprüft, ohne Befund: `cmd/ai-harness-init/main.go` — 4-Phasen-Orchestrierung
  (`bootstrap`/`preflightAbsent`/`emitTargets`); Pre-Flight-Print an den Abbruch
  gebunden; `emitTargets` deckt alle drei Emit-Ziele (`BaselineVerifyPath`,
  `.d-check.yml`, `d-check.mk`) + Template-Ziele; keine Netz-Operation eingeführt.
- geprüft, ohne Befund: `internal/fetch/baseline.go` — L4 `readCapped`/
  `maxBaselineBytes` (8 MiB) schrankt VOR dem sha256-Pin, meldet Überschreitung
  als typisierten `AssetTooLargeError` statt still abzuschneiden; L3 `defer`
  räumt jeden Go-Fehlerpfad, das Prozess-Tod- und das Stat→Rename-Fenster sind
  ehrlich als benigne/akzeptiert benannt (dot-präfigiertes `.baseline-*`, vom
  Verifier-Glob übersehen — MR-007 Setzung 4).
- geprüft, ohne Befund: `internal/emit/templates.go` — neues `TemplateTargets`
  spiegelt via `TestTemplateTargets_SpiegeltDenEmittiertenSatz` GENAU den von
  `emit.Templates` geschriebenen Satz und lässt `checkRoot` mitlaufen
  (`…_MisrootedRejected`) — eine falsch gewurzelte Baseline fällt so VOR dem
  Docker-Lauf auf; Pre-Flight und Emit sehen dieselbe Ziel-Menge.
- geprüft, ohne Befund: „keine ADR"-Einstufung — konsistent mit ADR-0005
  (keine Änderung an Herkunftsklassen/Fetch-Generate-Modell) und
  `architecture.md` §2 (CLI = „Orchestrierung"; der Pre-Flight ist genau das)
  und §4 (die grobe Sequenz kennt keine feingranulare Emit-Reihenfolge, die der
  Phase-4-Umbau DocGate→Verifier verletzte). Additive Härtung → kein §3.5
  (Gate-Lockerung braucht ADR, gilt für Senkungen).
- geprüft, ohne Befund: Hard Rule 3.3 — `5c6a44a` ist reiner Move (0/0, Rename
  erkannt), `68040f1` trägt den Inhalt; die Link-Reparaturen in NICHT-umbenannten
  Dateien liegen korrekt im Inhalts-Commit.
- geprüft, ohne Befund: Hard Rule 3.1 / LH-QA-01 — `Makefile`/`d-check.mk`/
  `.d-check.yml`/`.github/` unberührt; kein neues Gate behauptet; die neuen
  `mutations/11-14` speisen den Nicht-Gate-Sensor `make mutate`; neue Codepfade
  sind lokal (offline-grün unberührt).
- geprüft, ohne Befund: Hard Rule 3.2 — kein `//nolint` / `# shellcheck disable`
  im Diff.
- geprüft, ohne Befund: Link-Reparaturen in done/022a, done/022b, welle-02 —
  regelkonform und für `make docs-check` (codepaths/links) NÖTIG, da das
  Ziel-File von `open/` nach `in-progress/` wanderte; Prosa unangetastet
  (Rest-Drift siehe F-2).
- geprüft, ohne Befund: neue öffentliche Verträge (`TemplateTargets`,
  `AssetTooLargeError`, `preflightAbsent`) haben je Positiv- UND Negativtest
  (`TestPreflightAbsent`, `TestBaseline_AssetTooLarge`, die beiden
  `TestTemplateTargets_*`).

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 2 |

## Verdikt

**Merge-blockierend:** nein — keine HIGH/MEDIUM. Die vier repo-spezifischen
HIGH-Anker (Stilles-Grün im Mutationssatz, halluziniertes Gate, aktive-ADR-/
Hard-Rule-Verstoß, Gate-Lockerung ohne ADR) wurden gezielt geprüft und treffen
nicht zu: alle vier Mutationen sind tragend, der frühere `reportPreflight`-
Silent-Green ist geschlossen, `make gates`/CI sind unberührt (offline-grün), die
„keine ADR"-Einstufung ist mit ADR-0005/architecture.md/§3.5 verträglich, und die
„ehrlichen Grenzen" (Fetch-Rest bei Runtime-Abbruch, --force ohne Rollback,
Stat→Rename-Race) sind eng auf das gefasst, was der Code hält (§3.6). Die zwei
INFO-Punkte sind kein Blocker: F-1 ist eine bewusst-sanktionierte (Plan §6)
Verengung, deren DoD-Wortlaut der Verifier reconciled; F-2 ist won't-fix
Frozen-Doc-Historie.

**Übergabe:** Findings gehen an die Implementation. Der Report ersetzt keine
Verifikation — DoD-/Spec-Konformität (u. a. „rot gesehen", je-Schritt-Abdeckung,
realer `make gates`/`make mutate`/`make smoke`-Lauf) prüft der Verifier separat
(Modul 11).
