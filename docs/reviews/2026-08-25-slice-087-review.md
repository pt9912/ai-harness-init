# Review-Report: slice-087 (Code) — 2026-08-25

**Review-Art:** **Code**, erste Runde — geprüft wird der Diff gegen den Slice-Plan, gegen die
referenzierte Accepted-ADR (nur gelesen, nicht angefasst) und gegen die Hard Rules
(Modul 10 §Drei Review-Arten). Kein vorheriger Review-Lauf zu diesem Slice.

**Gegenstand:** `b484e3a` — **ein** Commit, **vier** Dateien (`git show b484e3a --stat` →
`internal/emit/templates.go` +127/−3, `internal/emit/readme.go` +11/−1,
`internal/emit/emitteddocs_test.go` neu (230 Zeilen, 3 Tests), Modus `755`,
`test/mutations/153-ziel-anspruch-nicht-neutralisiert.sh` neu, 14 Zeilen — `4 files changed,
379 insertions(+), 3 deletions(-)`). Baum sauber vor und nach jeder eigenen Probe
(`git status --short` → leer, `HEAD` = `b484e3a`).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0
**Modell:** claude-sonnet-5 · **Datum:** 2026-08-25

**Eingangs-Kontext:**

- **Diff:** `b484e3a`, `git show b484e3a` im Volltext
- **Slice-Plan:**
  [slice-087](../plan/planning/in-progress/slice-087-emittierte-doku-tische-init-invariant.md)
  (Stand `in-progress/`, vollständig gelesen)
- **ADR, gelesen als Constraint, nicht angefasst:**
  [ADR-0020](../plan/adr/0020-emittierte-modul-15-regeln.md) — **Accepted** (der Plankopf
  zitiert sie noch als *Proposed*; das ist der Stand vom Datum der Plan-Abfassung
  16.08., nicht der heutige — der Diff selbst berührt keine ADR-Datei,
  `git diff --stat b484e3a~1 b484e3a -- docs/plan/adr/` → leer)
- **`LH-*`:** [LH-QA-01](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
  [LH-FA-01](../../spec/lastenheft.md#lh-fa-01--repo-bootstrappen),
  [LH-FA-02](../../spec/lastenheft.md#lh-fa-02--zweiklassige-template-ablage-f3)
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §3.2, §3.4, §3.6, §3.7
- **Konventionen:** [`harness/conventions.md`](../../harness/conventions.md) MR-008, MR-017,
  MR-025 (Geltungsbereich)
- **Vorherige Findings am gleichen Modul:** keine — erste Review-Runde zu
  `internal/emit/templates.go`/`readme.go` seit ihrer letzten inhaltlichen Änderung; kein
  Treffer auf „slice-087" in `docs/reviews/` vor diesem Lauf.
- **Regelwerk `v3.5.2`:** Index gelesen; `modul-10-review-harness.md` on-demand

---

## Selbst gefahren (nichts aus Commit-Message oder Übergabe übernommen)

| Kommando / Probe | Ergebnis | Behauptung | Urteil |
|---|---|---|---|
| `find .harness/baseline/v3.5.2/templates -name '*.template.md' \| xargs grep -noE 'make [a-z][a-z0-9-]*\*?'` (28 Treffer) + dieselbe Suche über `internal/emit/templates/commands/*.md` | **jede** der 28+2 Fundstellen ist entweder ein realer Ziel-Name (`test`, `lint`, `arch-check`, `coverage-gate`, `coverage-gate-critical`, `gates`, `ci`, `fullbuild`, `help`, `verify-closure-notes`) oder die Muster-Nennung `verify-*` | Commit/Kommentar: „der vendored Satz v3.5.2 trägt kein Wort, das `make ` + Kleinwort ist, ohne Ziel-Nennung oder Muster zu sein" | **stimmt, an der Quelle nachgemessen — kein Gegenbeispiel gefunden.** Einzige Auffälligkeit: `AGENTS.template.md:89` (`**Richtig:** <z.B. `make test`>`) ist eine Fülle-Beispiel-Zeile innerhalb einer `<z.B. …>`-Platzhalter-Klammer, keine Tabellenzeile — sie wird trotzdem neutralisiert (`test` ∉ `InitInvariantTargets()`), was die Zeile zu `<z.B. `<make-target>`>` verstümmelt. Kein Hallo­zinations-Risiko (war vorher schon Platzhaltertext), aber der einzige Fund, der zeigt, dass „alle unkritisch" grobkörniger gemeint ist als „alle sind Tabellenansprüche" — siehe F-1 |
| `grep -n "z.B\..*make\|make.*z.B\." .harness/baseline/v3.5.2/templates/**/*.template.md` | genau **eine** Stelle (`AGENTS.template.md:89`) | — | bestätigt: einziges Prosa-Beispiel im ganzen Satz |
| Nachrechnung „20 Nennungen von 9 Zielen" für die zwei Gate-Tabellen (`AGENTS.template.md` + `harness/README.template.md`) | AGENTS: 9 Zeilen (inkl. `:89`), README: 11 Zeilen → **20** gesamt, **9** distinkte Namen | [ADR-0020](../plan/adr/0020-emittierte-modul-15-regeln.md) Festlegung 4(e), verbatim gemessen | **stimmt exakt** — die ADR-Zählung schließt `:89` bereits als gewöhnliche „Nennung" ein, es ist also **kein unentdeckter** Fund, sondern von der bindenden Quelle selbst mitgezählt (relativiert den obigen Punkt zu INFO statt LOW, s. F-1) |
| Lesung `internal/emit/makefile.go`, `baseline.go` (Zeilen 30-40), `emit.go` (Zeilen 55-69), `enforce.go` (`enforceFiles()`, `templates/enforce/enforce.mk`) | `aggregatorMakefile` → `gates:`, `help:`, `record-gates:`; `baselineMk` → `baseline-verify:` + `GATE_CHECKS += baseline-verify`; `docGateMk` → `GATE_CHECKS += docs-check`; `enforce.mk` (einziges `.mk` in `enforceFiles()`) → `record-gates:` (Dublette, per `seen`-Map dedupliziert) | `InitInvariantTargets()` liest exakt `[baseline-verify docs-check gates help record-gates]` aus denselben Konstanten, die auch geschrieben werden | **stimmt** — Einzel-Quelle für Schreiben und Lesen (kein Drift-Risiko zwischen Emit und Wächter) |
| `make gates` (frischer Lauf, selbst gefahren) | Exit 0; `d-check: 368 Datei(en) geprüft, 0 Befund(e)`; `comment-claims: 40 Datei(en) geprüft, 0 Befund(e)`; shellcheck über `test/mutations/*.sh` (schließt `153-…sh` ein) lautlos/grün; span-check grün | Commit: „make gates Exit 0 (d-check 368/0, comment-claims 40/0, span-check grün)" | **stimmt, selbst gefahren, Zahlen exakt reproduziert** |
| `make test-go` (frischer Lauf, selbst gefahren) | Exit 0, alle 9 Pakete `ok`, inkl. `internal/emit` | Commit: „make test-go Exit 0" | **stimmt, selbst gefahren** |
| Isolierte Kopie: `sed -i 's/if g\[2\] != "" \|\| known\[g\[1\]\] {/if g[2] == "" \|\| known[g[1]] {/' internal/emit/templates.go` gegen eine Wegwerf-Kopie außerhalb des Repos (`/tmp/…/mutate-check`) | genau **eine** Zeile geändert, `diff` bestätigt Ziel-Zeile 432 | Fall 153: die Mutation trifft exakt die Stern-Bedingung | **stimmt, mechanisch reproduziert.** Semantisch nachvollzogen: nach der Mutation überleben **alle sternlosen** Ansprüche unverändert (`g[2]==""` macht den Satz immer wahr) und **die Stern-Nennung** `make verify-*` wird stattdessen fälschlich neutralisiert (`g[1]="verify-"` ist nicht in `known`) — exakt das im Fallkopf beschriebene Fehlerbild, das `TestEmittierteDokumente_NurInitInvarianteZiele` über die vier ansprechbaren Fixture-Dokumente (`lint`/`fullbuild`/`ci`/`verify-closure-notes` bleiben stehen) rot färben muss |
| Prüfung `harness/tools/mutate.sh` `run_case` (Zeilen ~294-300): `( cd "$WORK" && bash "$case_file" 2>&1 )` | **jeder** Fall — auch 153 — läuft ausschließlich in der isolierten Kopie `$WORK`, nie im Host-Baum `$REPO`; alle bestehenden Fälle (`152`, `91`-`99` etc.) nutzen dieselben relativen Pfade | Implementer-Bericht: ein manueller Testlauf mit relativen Pfaden habe versehentlich den echten Arbeitsbaum mutiert, wurde zurückgenommen | **Falle liegt nicht im neuen Fall und nicht im Treiber** — `run_case` `cd`t immer zuerst in `$WORK`; ein Vorfall dieser Art kann nur bei direktem Aufruf des Fall-Skripts außerhalb `make mutate` entstehen (z. B. `bash test/mutations/153-…sh` vom Repo-Root). `git status --short` bei Session-Start ist leer — die Rücknahme ist vollständig. Kein Diff-Befund, strukturelles Merkmal aller 146 Fälle, nicht neu eingeführt |
| Lesung `internal/emit/emitteddocs_test.go` — die drei Leerlauf-Sperren (`unerfuellbar == 0`, `dokumente == 0`, `erlaubte == 0`) | (1) prüft die **Quelle** vor dem Emit auf mindestens einen unzulässigen Anspruch — verhindert grünen Test bei leerer Fixture; (2) prüft den emittierten Baum auf mindestens ein `.md` — verhindert grünen Test bei leer laufendem Emit; (3) prüft mindestens einen **erlaubten** Anspruch im Ergebnis — verhindert grünen Test bei einem Scanner, der nichts findet, oder einer Neutralisierung, die auch gültige Ansprüche mitreißt | DoD (2): „drei Leerlauf-Sperren" | **stimmt, alle drei mechanisch nachvollzogen; keine der drei ist redundant zu einer anderen** |
| `claimSet()`-Fixture (`internal/emit/emitteddocs_test.go:56-79`) gegen `readme.go`-Verdrahtung | `project-readme.template.md` trägt in der Fixture `` `make ci` `` (nicht init-invariant) — würde die Neutralisierung in `RootReadme()` entfernt, bliebe dieser Anspruch im Emit stehen und der Test würde rot | Plan-Abweichung: Neutralisierung zusätzlich in `readme.go` | **stimmt — die Abweichung ist von der bestehenden Test-Suite bereits scharf bewacht**, nicht nur vom (nicht dediziert für `readme.go` geschriebenen) Mutationsfall 153: eine Regression an der `RootReadme`-Verdrahtung selbst fiele bei jedem `make test-go`/`make gates` auf, unabhängig von `make mutate` |
| `grep -rn "nolint\|#nosec\|shellcheck disable" internal/emit/templates.go internal/emit/readme.go internal/emit/emitteddocs_test.go test/mutations/153-…sh` | 0 Treffer | §3.2 (keine Inline-Suppression) | **stimmt** |
| `git diff --stat b484e3a~1 b484e3a -- spec/ .harness/baseline/ harness/tools/full-smoke.sh` | leer | Auftrag: „stehende Prüfung — unberührt" | **stimmt** |
| `.dockerignore` Zeile 2/5 | `.harness` explizit ausgeschlossen | Boundary-Kommentar: „Der Wächter läuft über einer Fixture, nicht über der realen vendored Baseline" | **stimmt, Grenze real vorhanden und ehrlich benannt** |

**Nicht selbst gefahren, aus der Übergabe übernommen (Anweisung: zu teuer für diese Runde):**
`make mutate` (Implementer: 146 ok, 0 Befund(e), Fall 153 rötet
`TestEmittierteDokumente_NurInitInvarianteZiele`) und `make full-smoke` (Implementer: Exit 0 über
beide Bootstrap-Varianten). Für `make mutate` stützt die eigene Sonde oben (mechanisch nachvollzogene
Semantik der Mutation) die Behauptung durchgehend; für `make full-smoke` liegt keine eigene
Herleitung vor — das ist der tragende Vorbehalt dieses Reports.

---

## Findings

### F-1 — Die "12 Formen, alle unkritisch"-Zusammenfassung benennt nicht, dass eine der Fundstellen keine Tabellen-, sondern eine Fülle-Beispiel-Zeile ist

- `kategorie`: **INFO**
- `quelle`: Maintainability — keine Hard-Rule- oder ADR-Verletzung: ADR-0020 Festlegung 4(e)
  zählt `AGENTS.template.md:89` selbst als gewöhnliche „Nennung" in ihrer bindenden 20/9-Zählung
  mit, die Behandlung ist also mit der Accepted-ADR konsistent, nicht mit ihr im Widerspruch
- `pfad`: `.harness/baseline/v3.5.2/templates/AGENTS.template.md:89` (vendored, unverändert),
  Wirkung über `internal/emit/templates.go:417-436` (`NeutralizeMakeClaims`)
- `befund`: Die Zeile `**Richtig:** <z.B. \`make test\`>` ist ein Fülle-Beispiel innerhalb einer
  `<z.B. …>`-Adopter-Platzhalterklammer im Docker-only-Hard-Rule-Abschnitt, keine
  Tabellenzeile und kein Ziel-Anspruch über den bootstrappten Bestand. Die breite Regel
  (bewusst: „auch eines im Fließtext", siehe Kommentar bei `makeClaimPattern`) neutralisiert sie
  trotzdem, weil `test` ∉ `InitInvariantTargets()` — das Ergebnis ist ein verschachtelter
  Platzhalter `<z.B. \`<make-target>\`>`. Funktional harmlos (LH-QA-01 ist vorher wie nachher
  nicht berührt, da die Zeile nie eine Behauptung über den Zielrepo war), aber die
  Commit-Zusammenfassung „12 Formen, alle unkritisch, kein Kollateralschaden" differenziert nicht
  zwischen echten Ziel-Ansprüchen und dieser einen Fülle-Beispiel-Zeile.
- `verifizierbar`: **ja** — ein frischer Bootstrap ohne `--lang` zeigt die verschachtelte Form in
  Abschnitt 3.1 der emittierten `AGENTS.md`.

---

### F-2 — Plan §3 „Dateien"-Tabelle nennt `readme.go` nicht, obwohl Plan-DoD (2) es selbst als drittes/zweites Ziel des Wächters benennt

- `kategorie`: **LOW**
- `quelle`: Maintainability (Plan-interne Konsistenz) — kein Code-Defekt: die Umsetzung ist
  fachlich korrekt und die Begründung im Commit trägt (`readme.go` liest aus derselben
  vendored, unveränderlichen Vorlage, DoD (1) zählt die Root-README ausdrücklich zum
  Dokument-Satz)
- `pfad`: [slice-087 §3](../plan/planning/in-progress/slice-087-emittierte-doku-tische-init-invariant.md)
  „Dateien"-Tabelle vs. §2 DoD (2), dritter Absatz
- `befund`: DoD (2) benennt explizit **drei** Emitter, deren Vereinigung den zu bewachenden
  Dokument-Satz bildet — „die Root-README kommt aus `internal/emit/readme.go`
  (`RootReadmePath` …)". Die „Dateien"-Tabelle in §3 listet für den DoD-(1)-Umsetzungsteil aber
  nur `internal/emit/templates.go` als `update`; `readme.go` taucht dort nicht auf. Die
  tatsächliche Umsetzung wich davon ab (mit im Commit-Text sauber benannter Begründung) — die
  Abweichung ist damit gedeckt, aber die Tabelle selbst widerspricht dem eigenen DoD-Absatz zwei
  Zeilen darüber im selben Dokument.
- `verifizierbar`: **nein** — kein Gate liest die Dateien-Tabelle gegen den DoD-Text ab; nur
  eine erneute Plan-Lesung zeigt die Diskrepanz.

---

## Negativbefunde

- **geprüft, ohne Befund — Zuschnitt des Commits.** Ein Commit, vier Dateien, keine ADR, kein
  `spec/`, kein `.harness/baseline/`, kein `harness/tools/full-smoke.sh` im Diff — genau der
  im Auftrag benannte Zuschnitt.
- **geprüft, ohne Befund — §3.2 (keine Inline-Suppression).** Kein Treffer in den vier
  Dateien; `make gates` (inkl. shellcheck über `test/mutations/*.sh`) selbst gefahren, Exit 0.
- **geprüft, ohne Befund — §3.4 (vendored Baseline unveränderlich).** `.harness/baseline/**`
  nicht im Diff; die Neutralisierung greift ausschließlich emit-seitig, wie DoD (1) verlangt.
- **geprüft, ohne Befund — §3.6 (Zusage mit rot gesehenem Gegenbeispiel).** Der Mutationsfall
  153 wurde in einer eigenen isolierten Kopie mechanisch nachvollzogen; die Semantik der
  Mutation (sternlose Ansprüche überleben, die Stern-Nennung wird fälschlich neutralisiert)
  erzeugt genau das im Fallkopf beschriebene Fehlerbild und trifft die vier ansprechbaren
  Fixture-Dokumente von `TestEmittierteDokumente_NurInitInvarianteZiele`. Der volle
  `make mutate`-Lauf selbst wurde gemäß Auftrag nicht gefahren (siehe Vorbehalt).
- **geprüft, ohne Befund — §3.7 (Kommentar beschreibt, was da ist).** Die drei benannten
  Grenzen (Fixture statt reale Baseline; `InitInvariantTargets()` enger als die reale Menge,
  weil `d-check.mk` erst zur Bootstrap-Zeit entsteht; die bewusst breite Erfassungsregel) sind
  im Code durchgehend indikativ über den heutigen Zustand formuliert, keine trägt eine
  Review-Befund-Kennung oder Konjunktiv-Prosa über eine verworfene Alternative
  (`git show b484e3a | grep '^+' | grep -iE 'befund|review-|N-[0-9]|F-[0-9]'` → leer).
- **geprüft, ohne Befund — Punkt 1 des Auftrags (Erfassungsregel trägt nicht zu viel).** Über
  den gesamten vendored `v3.5.2`-Templatesatz plus die drei Workflow-Commands gemessen: jede
  `make `+Kleinwort-Stelle ist entweder ein realer Ziel-Name oder die Muster-Nennung
  `verify-*`. Eine einzige Stelle (`AGENTS.template.md:89`) ist eine Fülle-Beispiel-Zeile statt
  einer Tabellenzeile — funktional folgenlos, siehe F-1 (INFO, kein Blocker).
- **geprüft, ohne Befund — Punkt 2 des Auftrags (Ziel-Menge korrekt gelesen, fail-closed).**
  `InitInvariantTargets()` liest exakt `[baseline-verify docs-check gates help record-gates]`
  aus denselben Konstanten, die Emit auch schreibt (Einzel-Quelle, kein Drift-Risiko). Die
  benannte Enge (kein `d-check.mk`) wirkt tatsächlich fail-closed: sie kann nur zu **zusätzlicher**
  Neutralisierung führen (ein künftiger `doc-*`-Anspruch), nie dazu, dass ein ungültiger Anspruch
  unentdeckt bleibt — konsistent mit MR-017 und mit ADR-0020 Festlegung 4(e), die dieselbe engere
  Menge als „heute ohne Wirkung" benennt.
- **geprüft, ohne Befund — Punkt 3 des Auftrags (Wächter misst die Eigenschaft, drei
  Leerlauf-Sperren wirken).** Alle drei Sperren mechanisch nachvollzogen, keine redundant;
  der Scanner (`claimsIn`, Split-basiert) ist strukturell unabhängig von der
  Emit-Regexp implementiert.
- **geprüft, ohne Befund — Punkt 4 des Auftrags (Plan-Abweichung `readme.go`).** Fachlich
  gedeckt und durch die bestehende Testsuite (nicht nur durch Mutation) scharf bewacht — siehe
  aber F-2 zur Plan-internen Inkonsistenz (Tabelle vs. DoD-Text).
- **geprüft, ohne Befund — Punkt 5 des Auftrags (Mutationsfall, Reversibilität, Falle).** Form
  und Kopf decken sich mit den Nachbarfällen (`# files:`/`# expect:` einzeilig, `755`,
  `set -euo pipefail`, ein zielgerichteter `sed`, Nummerierung `153` lückenlos auf `152`
  folgend); kein `# verify:` nötig, da `narrow_sensor` aus `Test[A-Z]…` korrekt `test-go` wählt.
  Reversibilität trägt `mutate.sh`s tar-Backup/Restore, nicht das Fall-Skript selbst. Die vom
  Implementer berichtete versehentliche Host-Mutation ist strukturell **nicht** im neuen Fall
  oder im Treiber verankert (`run_case` `cd`t immer zuerst in die isolierte Kopie) — sie kann nur
  bei direktem Aufruf außerhalb `make mutate` entstehen, ein Merkmal, das alle 146 Fälle teilen.
- **geprüft, ohne Befund — MR-025 im Code-Kommentar.** Keiner der neuen Kommentare trägt eine
  konkrete Zahl ohne Kommando; die „12 Formen"-Zahl steht ausschließlich in der Commit-Message
  (kein lebendes Markdown-Artefakt im Sinn des MR-025-Geltungsbereichs), der Code-Kommentar
  selbst formuliert die Behauptung qualitativ, ohne Zahl.
- **nicht abschließend geprüft — `make mutate` und `make full-smoke` selbst.** Gemäß Auftrag
  nicht gefahren. Für `make mutate` stützt die eigene isolierte Reproduktion der Mutations-Semantik
  die Behauptung stark; für `make full-smoke` liegt keine eigene Herleitung vor.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 1 |
| INFO | 1 |

## Verdikt

**Frei — nicht merge-blockierend.** Beide Findings sind nicht-blockierend: F-1 (INFO) bestätigt,
dass die bewusst breite Erfassungsregel genau eine Fülle-Beispiel-Zeile mitreißt, was
funktional folgenlos ist und von der Accepted-ADR-0020-Zählung bereits mitgetragen wird; F-2
(LOW) ist eine Plan-interne Inkonsistenz (Dateien-Tabelle vs. eigener DoD-Text), kein
Code-Defekt — die tatsächliche `readme.go`-Erweiterung ist fachlich richtig begründet und durch
die reguläre Testsuite scharf bewacht, nicht nur durch den einen Mutationsfall. Kein
`AGENTS.md`-§3.2/§3.4/§3.6/§3.7-Verstoß, keine ADR berührt oder verletzt, `make gates` und
`make test-go` selbst gefahren und grün, Zahlen aus der Commit-Message (`d-check 368/0`,
`comment-claims 40/0`) exakt reproduziert.

**Tragender Vorbehalt für den Verifier:** `make mutate` und `make full-smoke` selbst sind in
dieser Runde nicht gelaufen (Kostenauflage der Aufgabe). Die Mutations-Semantik wurde durch eine
eigene isolierte Sonde bestätigt (kein Ersatz für den vollen Lauf); für `make full-smoke` liegt
keine eigene Herleitung vor — der Verifier sollte beide Läufe mit ihrer Ausgabe selbst sehen.

**Übergabe:** F-1 (INFO) ist reine Beobachtung, keine Handlungsaufforderung. F-2 (LOW) gehört an
den Planner (Dateien-Tabelle nachziehen) — kein Code-Defekt im Sinne der Rückkante
Review → Implementation.
