# Verifikation slice-086 — trägt ein per `updatedInput` erzwungener Vordergrund die Zähler?

**Rolle:** Verification (Modul 11). **Datum:** 2026-08-22. **Autor:** ai-harness-init-Team (pt9912).

**Gegenstand:** [`docs/plan/planning/in-progress/slice-086-vordergrund-per-updatedinput.md`](../plan/planning/done/slice-086-vordergrund-per-updatedinput.md)
§2, die **vollständige** DoD, gegen den Baum bei `2acd074` (`git status --porcelain` leer, vor und
nach jedem Lauf; `git worktree list` → nur `main @ 2acd074`). Der Liefergegenstand ist **ein
Zeitdokument**: [`docs/reviews/2026-08-21-updatedinput-messung.md`](2026-08-21-updatedinput-messung.md),
362 Zeilen (`wc -l`), in diesem Kontext vollständig gelesen. **Kein Code, kein Gate:** die
Vereinigung aller zehn Slice-Commits berührt acht Dateien, alle unter `docs/` — gemessen, s. §2.

**Was diese Rolle prüft:** ob das Gebaute die DoD und die Plan-Zusagen erfüllt — nicht, ob es das
Richtige ist (Validator), und nicht den Diff gegen Plan, ADR und Hard Rules (Reviewer; drei Runden,
§5). Die Aussagen der schreibenden Rollen sind hier **Eingabe**, nicht Ergebnis; jede Zahl unten ist
in diesem Kontext selbst erhoben.

**Ergebnis: DoD BESTÄTIGT bis auf eine Teilzusage in DoD (1) — die Permission-Lage des Repos ist
NICHT als unverändert bestätigt** (§3.1.5). Die Messung selbst, ihr negatives Ergebnis, die
Rücknahme der Sonde, die Nicht-Aufzeichnung samt rot gesehenem Gegenbeispiel und die Übergabe an den
Architect sind bestätigt; alle Zahlen des Zeitdokuments reproduzieren. Eine Plan-vs-Ist-Differenz in
der Gegenrichtung (§4.2) und sechs Beobachtungen ohne DoD-Rang (§6) stehen unten.

---

## 1. Läufe — Kommando und Ergebnis

Alle selbst gefahren, gepinnte Images, Docker-only, nur `make`-Targets. Exit-Code getrennt erhoben
(`make … > log 2>&1; echo "EXIT=$?" >> log`), nicht durch eine Pipe verschluckt.

```
$ make gates                                  -> EXIT=0   (zweimal gefahren, identisch)
    baseline-verify: v3.5.2 OK — 42 Dateien (Integritaet + Vollstaendigkeit, netzlos)
    d-check: 329 Datei(en) geprüft, 0 Befund(e)
    golangci-lint / go build / go test -count=1 / shellcheck / actionlint   ohne Befund
    1..143   — 143 ok, 0 not ok                (bats; grep -c '^not ok' -> 0)
    comment-claims: 40 Datei(en) geprueft, 0 Befund(e)
    span-check: Emitter vorhanden, ein Span geschrieben, Ablageort git-ignoriert

$ make mutate                                 -> EXIT=0
    mutate: Gruen-Vorlauf make test
    mutate: Gruen-Vorlauf make ci-lint
    mutate: Gruen-Vorlauf make smoke           <- make smoke laeuft hier mit
    mutate: 143 Faelle
    mutate: 143 ok, 0 Befund(e)
    (danach: git status --porcelain -> leer, git rev-parse HEAD -> 2acd074 unveraendert)
```

**Warum `make gates` zweimal.** Der erste Lauf fiel in ein Fenster, in dem ein fremder Git-Worktree
(paralleler Architect-Lauf, nicht Teil dieses Slice) unterhalb von `.claude/` lag. Der Lauf war
davon **nicht** betroffen — `d-check: 329 Datei(en)` ist die Zahl des sauberen Baums, nicht die des
mitgescannten Worktrees. Der zweite Lauf ist gegen den nachweislich sauberen Baum gefahren
(`git worktree list` und `git status --porcelain` **vor** dem Lauf erhoben und mitprotokolliert) und
liefert dieselben fünf Zeilen und denselben Exit-Code. Dieser Report trägt den zweiten.

**`make full-smoke` ist nicht selbst gefahren — begründet, nicht weggelassen.** Der Prüfbereich des
Voll-E2E-Smoke ist der Emissions-Pfad (`internal/emit`, `internal/gen`, `internal/wire`,
`harness/tools/full-smoke.sh`). Gemessen ist er in diesem Slice unberührt:

```
$ git diff --name-only f93f08d..2acd074 | grep -v '^docs/' | wc -l        -> 0
$ git diff --stat 31a7908..2acd074                                        -> 4 Dateien,
                                                        alle unter docs/reviews/, +1090/-12
```

CI-Lauf `32514535860` auf `31a7908` ist in **gates, smoke, mutate, full-smoke** grün
(`gh run view 32514535860 --json conclusion,headSha,jobs` → `"conclusion":"success"`,
`"headSha":"31a7908…"`, vier Jobs je `success`). Da `31a7908..2acd074` **ausschließlich** vier
Dateien unter `docs/reviews/` bewegt, ist jede vom Emissions-Pfad gelesene Datei zwischen dem
CI-Stand und `2acd074` byte-gleich; der `full-smoke`-Beleg trägt damit auf `2acd074` weiter.
`make smoke` ist trotzdem lokal gelaufen — als Grün-Vorlauf von `make mutate` (Ausgabe oben).

**Warum `make mutate` selbst gefahren wurde, obwohl der Prüfbereich leer ist.** Die Schnittmenge
zwischen den Slice-Pfaden und den `# files:`-Zielen der 143 Mutations-Fälle ist **leer** — der Slice
hätte den Lauf nach der Prüfbereichs-Logik nicht gebraucht:

```
$ comm -12 <(for c in $(git log --format=%H f93f08d..2acd074); do \
               git show --pretty=format: --name-only "$c"; done | sed '/^$/d' | sort -u) \
           <(grep -rhn '^# files:' test/mutations/ | sed 's/^[0-9]*:# files: *//' \
               | tr ' ' '\n' | sed '/^$/d' | sort -u) | wc -l
0
```

Die DoD nennt `make mutate` aber **ausdrücklich** als eigenen Punkt. Ein Argument ist keine Messung;
der Lauf ersetzt die Übertragung durch einen Befund und kostete 24 Minuten.

## 2. Was der Slice bewegt hat — gemessen

```
$ git log --oneline f93f08d..2acd074 | wc -l                              -> 10 Commits
$ git diff --stat=200 f93f08d..2acd074
 docs/plan/carveouts/CO-002-token-achse-je-rolle.md                          |   6 +-
 docs/plan/planning/done/slice-062-emittierte-modul-15-regeln.md             |   2 +-
 docs/plan/planning/{open => in-progress}/slice-086-vordergrund-per-…\.md    |   4 +-
 docs/plan/planning/welle-09-modul-15-konformitaet.md                        |   2 +-
 docs/reviews/2026-08-21-updatedinput-messung.md                             | 362 +++
 docs/reviews/2026-08-22-slice-086-bestaetigungsrunde.md                     | 304 +++
 docs/reviews/2026-08-22-slice-086-review.md                                 | 428 +++
 docs/reviews/2026-08-22-slice-086-verdikt-runde.md                          | 242 +++
 8 files changed, 1343 insertions(+), 7 deletions(-)

$ git diff --name-only f93f08d..2acd074 -- Makefile .d-check.yml spec test internal cmd harness .claude
                                                                          -> leer
```

**Nichts Gate-Wirksames ist entstanden.** Kein `Makefile`, keine `.d-check.yml`, kein `spec/`,
kein `test/`, kein `internal/`, kein `cmd/`, kein `harness/`, kein `.claude/`. Der Slice behauptet
keinen Wächter ([`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)),
und es ist keiner entstanden.

## 3. DoD-Punkte einzeln

### 3.1 DoD (1) — Sonde gebaut, gefahren, zurückgenommen; Prompt nicht angefasst

**BESTÄTIGT in vier von fünf Teilzusagen; die fünfte NICHT BESTÄTIGT.**

#### 3.1.1 Hook-Form (`"matcher": "Agent"`, `permissionDecision: "ask"`, `updatedInput` = Eingabe + `run_in_background: false`) — BESTÄTIGT

Der Sondentext steht verbatim in [`…-updatedinput-messung.md`](2026-08-21-updatedinput-messung.md)
§2 (`:41-116`). Ich habe ihn **aus dem Dokument rekonstruiert** — genau die Eigenschaft, die die
Plan-Tabelle §3 der Sonde zuschreibt (*„ihr Text steht im Zeitdokument … und ist von dort
reproduzierbar"*) — und außerhalb des Repos gegen die Fixture aus §3 (`:124`) gefahren:

```
$ sed -n '42,115p' docs/reviews/2026-08-21-updatedinput-messung.md > sonde.sh   (74 Zeilen)
$ sed -n '124p'    docs/reviews/2026-08-21-updatedinput-messung.md > fixture.json
$ bash ./sonde.sh < fixture.json          -> Exit 0, 522 Zeichen Ausgabe
```

Die Ausgabe trägt `"permissionDecision":"ask"` und ein `updatedInput`, dessen letztes Feld
`"run_in_background":false` ist.

#### 3.1.2 Gebaut in `bash` + `awk` ([`LH-QA-03`](../../spec/lastenheft.md#lh-qa-03--minimale-abhängigkeiten)) — BESTÄTIGT

```
$ grep -nE '\b(jq|node|python|perl|curl)\b' sonde.sh      -> leer (Exit 1)
```

Der Rumpf ist ein einziges `LC_ALL=C awk '…'` in einem `bash`-Skript; kein Encoder, kein Binär.
Der Splice ist byte-treu, und das ist die Mechanik hinter der Nicht-Aufzeichnung — nachgemessen mit
den drei Checks aus §4 des Zeitdokuments, bash-Parameter-Expansion statt Parser:

```
CHECK1 Marker durchgereicht: OK
CHECK2 Splice am Ende:       OK
CHECK3 Byte-Identitaet:      OK (304 Bytes durchgereicht)
```

**Alle drei reproduzieren, einschließlich der Zahl 304** — sie ist nicht übernommen, sondern
erhoben (`cmp -s`, `wc -c` über den aus der Sonden-Ausgabe zurückgeschnittenen `tool_input`-Bereich
gegen den der Fixture). Zusätzlich geprüft, was das Dokument in §2 als Kommentar zusagt: bei
unbalancierter Payload schweigt die Sonde und gibt 0 zurück (`ausgabe_bytes=0`, `exit=0`) — der
fail-safe-Zweig hält.

#### 3.1.3 Nicht-Aufzeichnung mit rot gesehenem Gegenbeispiel ([`AGENTS.md`](../../AGENTS.md) §3.6) — BESTÄTIGT

Das ist die eine §3.6-Zusage dieses Slice, und sie ist **in diesem Kontext neu rot gesehen**, nicht
aus dem Dokument geglaubt. In einem isolierten Verzeichnis außerhalb des Repos, Datei-Inventar vor
und nach dem Lauf:

```
$ bash ./sonde.sh < fixture.json > /dev/null
$ diff before.list after.list                                       -> leer (keine neue Datei)
$ grep -rlF 'SONDE-MARKER-7c1de4b2a90f' --exclude=fixture.json .     -> leer, Exit 1

  Gegenbeispiel (schreibende Variante, `cat > ./sonde-schreibend.log`):
$ grep -rlF 'SONDE-MARKER-7c1de4b2a90f' --exclude=fixture.json .     -> sonde-schreibend.log
                                                                        Exit 0  ** ROT **
```

Dieselbe Suche findet die schreibende Variante und findet die echte Sonde nicht. Die
Kontroll-Fassung aus §6 (`:252-267`) ist gleich geprüft: 376 Zeichen Ausgabe, **keine** neue Datei,
Marker nirgends geschrieben; sie reicht die Eingabe erwartungsgemäß **nicht** durch (der
Fixture-Marker erscheint in ihrer Ausgabe nicht) — genau die Wirkung, die §6 (`:270-275`)
ausspricht.

Im Repo selbst gilt die Fund-Freiheit weiterhin, und sie gilt in der Form, die §4 des Dokuments
ausdrücklich benennt (ab dem Commit des Dokuments ist die Suche per Konstruktion in den **Zitaten**
fündig):

```
$ grep -rlF 'SONDE-MARKER-7c1de4b2a90f' --exclude-dir=.git .
docs/reviews/2026-08-22-slice-086-review.md
docs/reviews/2026-08-21-updatedinput-messung.md                     (Exit 0 — zwei Zitate)
$ … | grep -v 'docs/reviews/'                                       -> leer, Exit 1
```

Dasselbe Bild für `SONDE-KONTROLLE-9d4b`: zwei Träger, dieselben zwei Dateien, gefiltert leer. Die
Zahl **2** ist auf `2acd074` unverändert, obwohl seit `31a7908` zwei weitere Review-Reports
dazugekommen sind — die Zitier-Disziplin der Runden 2 und 3 hält.

#### 3.1.4 Rücknahme: Hook, Fixture, Verdrahtung, `git status` — BESTÄTIGT

```
$ git diff f93f08d..2acd074 -- .claude/settings.json | wc -l         -> 0
$ ls .claude/hooks/     -> pretooluse-agent-guard.sh, pretooluse-command-guard.sh,
                           stop-require-gates.sh   (drei committete, keine Sonde, keine Fixture)
$ git status --porcelain                                            -> leer
$ git grep -ln 'updatedinput-sonde\|sonde-fixture-agent-payload' -- . | grep -v '^docs/reviews/'
                                                                    -> leer (Exit 1)
```

Die `PreToolUse`-Liste in `.claude/settings.json` führt zwei Matcher (`Bash` → Command-Guard,
`Agent` → Agent-Guard); der dritte Eintrag der Messung ist weg. Nichts unter `test/`.

#### 3.1.5 „die Permission-Lage des Repos ist dieselbe wie vorher" — **NICHT BESTÄTIGT**

Der Plan lehnt eine Änderung der Permission-Lage in §1 ausdrücklich ab (*„`allow` überspränge das
Permission-System — eine Senkung der Durchsetzung, die nach `AGENTS.md` §3.5 in eine ADR gehört und
nicht in einen Messaufbau"*), und DoD (1) sagt die Unverändertheit zu. Der Sensor, den die DoD dafür
anbietet, ist `git status`. **Er kann diese Zusage strukturell nicht tragen:**

```
$ git check-ignore -v .claude/settings.local.json
/home/db/.config/git/ignore:1:**/.claude/settings.local.json   .claude/settings.local.json
$ git log --oneline --all -- .claude/settings.local.json | wc -l              -> 0
```

Die Datei, die die repo-lokale Permission-Lage trägt, ist gitignoriert und war nie versioniert —
`git status` ist auf sie blind, und ein leeres `git status` sagt über sie nichts. Was sie enthält
und wann sie geschrieben wurde, ist dagegen messbar:

```
$ grep -c '^      "' .claude/settings.local.json                             -> 106 Einträge
$ grep -n '"Agent"' .claude/settings.local.json                              -> 109:      "Agent"
$ stat -c '%y' .claude/settings.local.json      -> 2026-08-21 16:43:31.840785509 +0200
$ date +'%Z %z'                                 -> CEST +0200
```

Der **letzte** Eintrag der `permissions.allow`-Liste ist `"Agent"` — ein pauschales Allow für das
Agenten-Werkzeug, also für **jeden** Subagenten-Aufruf in diesem Repo. Die Datei wurde zuletzt am
Messtag um 16:43:31.84 Ortszeit geschrieben. Der `PostToolUse`-Span des **ersten** Mess-Aufrufs
dieses Slice (Lauf 0) trägt `"ts":"2026-08-21T14:43:31Z"` — bei `+0200` dieselbe Sekunde, 0,84 s
davor:

```
$ grep -h '"tool_use_id":"toolu_016F6282frqweYSfy7ZmKNef"' \
    .harness/state/spans/a2195604_396a_4398_8c2e_ac13d666f74b.jsonl
{"seq":65,"ts":"2026-08-21T14:43:31Z", … "slice":["slice-086-vordergrund-per-updatedinput"] …}
```

Die Einträge vor `"Agent"` (`Bash(time make mutate)`, `Bash(nohup make mutate)`,
`Bash(pkill -TERM -f "harness/tools/mutate.sh")`, `Bash(docker system *)`) gehören sichtbar zu
früherer Arbeit; `"Agent"` steht als jüngster am Ende einer Liste, an die neue Grants angehängt
werden.

**Was ich nicht sagen kann und deshalb nicht sage:** dass dieser Eintrag durch diesen Slice
entstanden ist. Die Datei hat keine Historie, also gibt es keinen Vorher-Stand, gegen den ich
messen könnte — und genau das ist der Befund. **Was ich sage:** die Zusage *„die Permission-Lage des
Repos ist dieselbe wie vorher"* hat **keinen Sensor hinter sich**, das Zeitdokument erwähnt
`.claude/settings.local.json` an keiner Stelle (`grep -n 'settings.local' docs/reviews/2026-08-21-updatedinput-messung.md`
→ leer), und die einzige messbare Spur zeigt in die Gegenrichtung: ein pauschales `Agent`-Allow,
geschrieben in der Sekunde des ersten Mess-Aufrufs, das auf `2acd074` unverändert steht. Eine
Zusage, die breiter ist als ihr Sensor, ist unbelegt — hier ist sie breiter als **jeder** Sensor,
den der Slice anbietet.

**Rang:** Das ist eine DoD-Verletzung im Sinne von Modul 11 — für `make gates`, für `make mutate`
und für alle drei Review-Runden unsichtbar, weil alle drei gegen etwas anderes prüfen. Sie kippt
das **Messergebnis nicht**: ein `Agent`-Allow erklärt keinen fehlenden Zähler, und der `ask`-Dialog
aus Lauf 1 ist im Zeitdokument als erschienen berichtet. Sie trifft die **Rücknahme-Zusage**, nicht
die Beobachtung.

### 3.2 DoD (2) — die eine Beobachtung, am Span gelesen — BESTÄTIGT

**Ort und Form.** Das Zeitdokument liegt unter `docs/reviews/`, trägt Datum im Namen, und jedes
Span-Zitat steht mit seinem Kommando (§5, `:172-241`; §6, `:284-290`).

**Die fünf Zahlen des Bestands reproduzieren, jede mit ihrem Kommando:**

```
$ grep -h '"tool":"Agent"' .harness/state/spans/*.jsonl | grep -c '"ts":"2026-08-21'      -> 6
$ … | grep '"ts":"2026-08-21' | grep -c -E 'spawned_role|input_tokens'                    -> 0
$ grep -h 'spawned_role' .harness/state/spans/*.jsonl | wc -l                             -> 89
$ … | grep input_tokens | grep output_tokens | grep cache_creation_input_tokens \
      | grep -c cache_read_input_tokens                                                   -> 89
$ … | grep -o '"ts":"[^"]*"' | sort | tail -1                          -> "2026-08-09T18:32:20Z"
```

Sechs `Agent`-Spans am Messtag, **keiner** mit Rolle oder Zähler; 89 Zeilen im Bestand tragen
`spawned_role` und dieselben 89 alle vier Zähler — der Emitter **kann** beides, die jüngste solche
Zeile ist zwölf Tage älter als die Messung.

**Die drei zitierten Span-Zeilen sind byte-gleich mit dem Bestand.** Über `tool_use_id` gelesen
(nicht über `seq` — die Warnung des Dokuments in `:174-176` ist berechtigt): Lauf 0
(`toolu_016F6282…`, `duration_ms:3`, `result_bytes:386`), Lauf 1 (`toolu_0181irqR…`, `seq:1`,
`result_bytes:457`), Kontroll-Lauf (`toolu_015bN4AL…`, `seq:2`, `result_bytes:360`). Über die
beiden Zeilen des Stroms `d3ef8106…`:

```
$ grep -h '"tool":"Agent"' .harness/state/spans/d3ef8106_bc2d_4a6e_8bd0_72c91c4b813d.jsonl \
    | grep -c -E 'spawned_role|input_tokens|output_tokens|cache_creation_input_tokens|cache_read_input_tokens'
0
```

Auch der Vorlauf (`af347d77…`, `duration_ms:14`, `result_bytes:437`) stimmt mit `:238-241` überein.

**Der Ausgang ist negativ und wird so notiert** — §7 (`:306-311`) sagt es ohne Abschwächung, und
§1 (`:9-16`) trägt es als Kopfzeile. Der Bericht aus `make span-report` steht als Grenze daneben
(`:319-320`), nicht an Stelle der Span-Lektüre — wie DoD (2) verlangt.

**Kontroll-Beobachtung.** Sie ist im Dokument berichtet (§6, `:277-283`: Tool-Zeile
`Explore(SONDE-KONTROLLE-9d4b)`, Fertigmeldung unter dem Marker-Namen) und ausdrücklich als
**Sicht**-Beobachtung ausgewiesen, deren Träger im Repo fehlt (§6 `:292-302`, §7 Grenze 4
`:321-328`). **Das ist im Repo nicht nachprüfbar, und das Dokument sagt genau das selbst** — es
schiebt den Beleg auf keinen Ersatz-Träger. Ich bestätige die **Form** der Zusage (die
Kontroll-Beobachtung hängt in derselben Zeile wie die Messung, wie DoD (2) verlangt, und wird als
unbelegbar ausgewiesen statt als belegt behauptet), **nicht** die Beobachtung selbst — dafür gibt es
in diesem Repo keinen zulässigen Sensor, und das Sitzungs-Transkript ist als Quelle ausgeschlossen
(`spec/spezifikation.md` §5 Abweichung 1, `:387-388`; [ADR-0012](../plan/adr/0012-haupt-kontext-ohne-token-bilanz.md)
Alternative D; [ADR-0019](../plan/adr/0019-agent-guard-prueft-die-aufrufform.md) Annahme (c)). Es
ist in diesem Lauf weder gelesen noch gezählt worden.

Das Zitat aus Abweichung 1 ist verbatim geprüft und deckt sich; der in Runde 3 als LOW-1 gemeldete
Pointer ist in `2acd074` korrekt gezogen:

```
$ sed -n '555p' spec/spezifikation.md   -> "6. **Der Haupt-Kontext hat keine Zahl …" (Beginn Abw. 6)
$ sed -n '569p' spec/spezifikation.md   -> "Transkript ist als Quelle ausgeschlossen (Abweichung 1: …"
$ sed -n '296,297p' docs/reviews/2026-08-21-updatedinput-messung.md
   → nennt "Abweichung 1: …" und "Abweichung 6"                       — beide Zeiger tragen
```

### 3.3 DoD (3) — die Übergabe ist ausgestellt, für den eingetretenen Ausgang — BESTÄTIGT

§8 des Zeitdokuments (`:341-362`) stellt den **negativen** Zweig aus, und zwar in der Form, die die
DoD verlangt — als benannter Auftrag, nicht als Befund:

- **Zwei Trigger im fremden Vertrag bleiben** (`:343-346`) — der dritte Weg liegt nicht mehr in
  unserer Hand.
- **Modul-7-Frage 2 kippt für `CO-002` auf *Nein*** (`:347-348`).
- **`CO-002` ist in eine Folge-ADR zu überführen** (`:348-350`), mit der ausdrücklichen
  Rollen-Trennung: *„Der Status-Wechsel und der `git mv` des Carveouts gehören dem Implementer des
  Folge-Schnitts, die ADR dem Architect"* (`:350-352`) — deckungsgleich mit DoD (3) und mit
  [`AGENTS.md`](../../AGENTS.md) §3.8.
- **Zwei Punkte, die die Folge-ADR zu tragen hat** (`:354-362`): der Rang zwischen
  [ADR-0011](../plan/adr/0011-telemetrie-erfassung-policy.md) Festlegung 3 (*kein Beleg-Status* des
  Spans) und ADR-0019 Festlegung 4 / `CO-002` (Span als Ablese-Ort), sowie die prinzipielle
  Unbelegbarkeit der Kontroll-Beobachtung.

**Was §8 richtig NICHT tut:** es entscheidet den Rang nicht, ändert keine ADR, keinen Carveout-Status
und keine Hard Rule. Gemessen: `git diff --name-only f93f08d..2acd074 -- docs/plan/adr/` ist leer,
und die einzige Änderung an `CO-002` ist eine Pfad-Reparatur (§4.2).

Die DoD verlangt hier das **Ausstellen** der Übergabe, nicht ihren Eingang beim Architect — der ist
Closure-Trigger-Sache des Planners (§5 des Plans) und in diesem Report bewusst nicht bewertet.

### 3.4 DoD (4) — `make gates` grün, `make mutate` ohne Befund — BESTÄTIGT

Beide selbst gefahren, beide Exit 0 (§1). `make mutate`: **143 ok, 0 Befund(e)**.

### 3.5 DoD (5) — Doku-Update, falls ein öffentlicher Vertrag berührt ist — BESTÄTIGT

Die Bedingung tritt nicht ein, und das ist gemessen, nicht angenommen:

```
$ git diff --name-only f93f08d..2acd074 -- spec/ README.md AGENTS.md harness/conventions.md \
    docs/plan/adr/ | wc -l                                                  -> 0
```

Kein Lastenheft, keine Spezifikation, keine Architektur-Sicht, kein ADR, kein Adaptions-Block, kein
README.

### 3.6 DoD (6) — Closure-Notiz mit Steering-Loop-Lerneintrag — OFFEN

`docs/plan/planning/in-progress/slice-086-…md` §7 trägt unverändert
`<!-- Erst nach Abschluss füllen. -->`. Das ist **keine Differenz**, sondern der noch nicht
vollzogene Schritt: die Closure-Notiz ist per Konstruktion der Closure-Akt selbst und gehört dem
Planner. Ein Lerneintrag bietet sich an und liegt schon geschrieben vor — der Nebenbefund aus §7 des
Zeitdokuments (`:337-339`): die Hook-**Liste** einer Sitzung friert beim Session-Start ein, der
Hook-**Befehl** wird bei jedem Feuern frisch von Platte gelesen.

## 4. Plan vs. Ist — in beide Richtungen

### 4.1 Plan → Ist: die Tabelle *Berührte Dateien* (Plan §3)

| Plan-Zeile | Plan sagt | Ist (gemessen) | Urteil |
|---|---|---|---|
| `.claude/hooks/` | temporär, nicht committet | drei committete Hooks, keine Sonde, keine Fixture | **deckt** |
| `.claude/settings.json` | temporär, nicht committet | `git diff f93f08d..2acd074` → 0 Zeilen | **deckt** |
| `docs/reviews/<datum>-updatedinput-messung.md` | neu | vorhanden, 362 Zeilen, vier Commits | **deckt** |
| Fixture, **nicht** unter `test/` | temporär, nicht committet | `git grep` auf Sonden-/Fixture-Namen außerhalb `docs/reviews/` leer | **deckt** |
| `Makefile`, `.d-check.yml`, `spec/`, `test/` | **unverändert** | `git diff --name-only … -- Makefile .d-check.yml spec test` leer | **deckt** |

Alle fünf Zeilen decken. Kein Geplantes fehlt.

### 4.2 Ist → Plan: drei Klassen Gebautes, das der Plan nicht führt

**(a) Drei Reviewer-Reports** (`2026-08-22-slice-086-review.md`, `…-bestaetigungsrunde.md`,
`…-verdikt-runde.md`, zusammen 974 Zeilen). Die Plan-Tabelle führt sie nicht. **Keine Differenz:**
sie sind Artefakte der Modul-10-Rolle, und der Closure-Trigger des Plans (§5) verlangt sie
ausdrücklich (*„Review konform (Modul 10) mit ausgestelltem Verdikt"*). Prozess-Mechanik, kein
ungeplanter Bau.

**(b) Die Link-Reconciliation `3f27b7c`** — `CO-002`, `welle-09`, `done/slice-062`. Der Review-Befund
LOW-3 (R1) hat sie gemeldet; der Verdikt-Report weist sie dem Planner zu. **Mein Urteil:
Lifecycle-Mechanik in der Sache, Plan-Lücke im Text.**

```
$ git show 3f27b7c --numstat | tail -4
3  3  docs/plan/carveouts/CO-002-token-achse-je-rolle.md
1  1  docs/plan/planning/done/slice-062-emittierte-modul-15-regeln.md
2  2  docs/plan/planning/in-progress/slice-086-vordergrund-per-updatedinput.md
1  1  docs/plan/planning/welle-09-modul-15-konformitaet.md
```

Sieben Zeilen rein, sieben raus; jede geänderte Zeile ersetzt ausschließlich das Pfadsegment
`open/` durch `in-progress/`. Keine Aussage ist bewegt, kein Status, kein Trigger. Der `git mv` aus
`c874ae9` erzwingt sie: ohne sie wäre `make docs-check` rot, denn die `docs/reviews/**`-Ausnahmen
aus `.d-check.yml` decken diese drei Dateien nicht. Damit ist §6 des Plans (*„Nicht in diesem
Slice: … jede Änderung an `CO-002` selbst"*) **dem Sinn nach gewahrt** — geändert ist die Adresse,
nicht die Sache.

**Die Plan-Lücke bleibt trotzdem, und sie ist präziser als der R1-Befund sie fasst:** der Plan
**kennt** die Link-Reconciliation — sein Closure-Trigger §5 schreibt sie für den Move nach `done/`
ausdrücklich vor (*„`git mv` nach `done/` in eigenem Move-Commit, eingehende Links im Zug danach"*).
Für den **Eintritts**-Move (`open` → `next` → `in-progress`, Trigger §4) sagt er nichts, und die
Tabelle §3 führt die betroffenen Dateien nicht. Der Slice hat den Nachzug also richtig gemacht und
zweimal gebraucht, während der Plan ihn nur einmal vorsieht. Das ist ein **Plan-Defekt, kein
Implementations-Defekt** — Adressat ist der Planner, und die Klasse trifft jeden wellenlosen Slice,
der durch drei Verzeichnisse wandert, nicht nur diesen.

**(c) `.claude/settings.local.json`** — s. §3.1.5. Gebaut-aber-nicht-geplant im strengsten Sinn: die
Datei ist im Plan an keiner Stelle genannt, sie trägt die Permission-Lage, die der Plan explizit
unverändert lassen wollte, und sie wurde am Messtag geschrieben. Das ist die eine Richtung, in der
Plan und Ist wirklich auseinandergehen.

## 5. Was ich vom Review übernehme und was ich nachgemessen habe

Drei Runden, drei Reports, zunehmend eng: `ec687cb` (blockiert, 4 MEDIUM / 4 LOW / 1 INFO),
`3588e97` (blockiert, 1 HIGH / 3 LOW), `74ac235` (**frei**, 1 LOW / 1 INFO).

**Die blockierende Menge der letzten blockierenden Runde ist im Dokument geschlossen** — selbst
nachgemessen, nicht vom Verdikt übernommen:

```
$ grep -n -i 'transkript' docs/reviews/2026-08-21-updatedinput-messung.md
296: … ist **als Quelle ausgeschlossen** (`spec/spezifikation.md` §5 Abweichung 1: …
359: … das Transkript ist als Quelle ausgeschlossen (§6, §7 …)
```

Zwei Treffer, **beide** benennen das Transkript als ausgeschlossene Quelle. Keine Zahl daraus, kein
Pfad, keine Zähl-Formel. HIGH-1 (R2) ist damit im geprüften Artefakt aufgelöst.

**Die beiden Findings aus Runde 3 sind in `2acd074` gezogen** — auch das nachgemessen:

```
$ git show 2acd074 | grep -E '^[+-][^+-]'
- … Abweichung 5; ADR-0012          + … Abweichung 6; ADR-0012              (LOW-1)
- … nachweislich übernommenen …     + … übernommenen … (per Sicht am Dialog belegt, §6)  (INFO-1)
- … nachweislich ersetzt wurde …    + … ersetzt wurde (per Sicht am Dialog belegt, §6)   (INFO-1)
$ grep -n 'nachweislich' docs/reviews/2026-08-21-updatedinput-messung.md   -> leer (Exit 1)
```

LOW-1 stimmt sachlich (§3.2, `sed -n '555p;569p'`), und beide sind behoben.

**Ein Punkt, den der Plan ausdrücklich an diese Rolle abgibt** (§6, vierter Risiko-Punkt: *„Zwei
Hooks auf demselben Matcher … ist an dieser Stelle gelesen, nicht gemessen — es gehört an die
DoD-Verifikation"*). **Gemessen, am Code:**

```
$ grep -n 'permissionDecision\|exit 0' .claude/hooks/pretooluse-agent-guard.sh
48:    "permissionDecision": "deny",      (einzige Ausgabeform)
59/61/68/79:  emit_deny …; exit 0          (vier fail-closed-Zweige)
81: exit 0                                 (Pass-Fall)
```

Der Agent-Guard kennt genau **zwei** Verhalten: `deny` mit Grund, oder stumm `exit 0`. Er gibt weder
`allow` noch `ask` und kein `updatedInput` aus. Im Pass-Fall kann er die `ask`-Entscheidung der
Sonde deshalb nicht überschreiben — es gibt keine konkurrierende Entscheidung, nur eine. Die
Kopplung ist damit **gemessen** und nicht mehr nur aus einer Vorrangregel gelesen; die
Laufzeit-Beobachtung aus Lauf 1 (der `ask`-Dialog erschien mit der Sonden-Begründung) deckt sich
damit. **Bestätigt.**

## 6. Beobachtungen ohne DoD-Rang

1. **`origin/main` ist während dieser Verifikation nachgezogen worden.** Beim Beginn der Läufe
   stand es auf `31a7908` (`git rev-parse HEAD origin/main` → `2acd074…` / `31a7908…`), also sechs
   Commits hinter `HEAD`; der vom Implementer angeführte CI-Lauf `32514535860` gehört zu diesem
   älteren Stand. Am Ende der Läufe steht `origin/main` auf `2acd074`
   (`git for-each-ref refs/remotes` → `refs/remotes/origin/main 2acd074`), und ein CI-Lauf auf
   `2acd074` ist angelegt und **läuft noch** (`gh run list` → `32554104446`, `"status":"in_progress"`,
   `createdAt 2026-08-22T05:20:52Z`). Sein Ausgang ist nicht Teil dieses Reports; dass er existiert,
   schließt die [`MR-014`](../../harness/conventions.md#mr-014--ci-auf-frischem-klon-github-actions)-Lücke,
   die zu Beginn bestand. Keine DoD-Frage — die DoD verlangt `make gates`, und der ist hier lokal
   auf `2acd074` gefahren (§1).
2. **Die DoD-Checkboxen im Plan sind alle ungesetzt** (`grep -n '^- \[' …` → sechsmal `- [ ]`),
   während `done/`-Slices sie gesetzt führen (`slice-062`: 4× `[x]`, 0× `[ ]`; `slice-066`: 6× / 0×).
   Der Abhak-Akt steht also noch aus. Adressat: Planner beim `git mv` nach `done/`.
3. **Der nächtliche `upstream-drift`-Lauf ist rot** (`gh run list` → `32545626760`, `failure`, auf
   `31a7908`). Fremder Gegenstand — Netz-Sensor gegen den Upstream, hängt an der offenen
   `ADR-0018`-Runde und an welle-10, nicht an diesem Slice.
4. **Der Rang `ADR-0011` Festlegung 3 vs. `ADR-0019` Festlegung 4 / `CO-002`** ist im Zeitdokument
   benannt (§7 letzte Grenze, `:329-335`) und nicht entschieden — korrekt so. Er geht mit §8 an den
   Architect und ist kein offener Posten dieses Slice.
5. **Die Transkript-Zahlen im Reviewer-Report `3588e97` (`:61-63`)** bleiben im Repo stehen. Der
   Reviewer hat das in Runde 3 selbst offengelegt und dem Auftraggeber/Architect zugewiesen. Ich
   habe die Stelle **nicht gelesen und nicht gezählt**; ich verweise nur auf die Offenlegung. Es ist
   kein Posten der slice-086-DoD, aber er verschwindet mit dem Closure nicht von selbst.
6. **`.claude/settings.local.json` wird von einem *globalen* Ignore ausgeschlossen**
   (`/home/db/.config/git/ignore:1`), nicht von der `.gitignore` des Repos. Auf einem Checkout ohne
   diese Nutzer-Konfiguration wäre die Datei sichtbar und `git status` **nicht** leer. Das berührt
   die Nachvollziehbarkeit von §3.1.5 auf einer anderen Maschine.

## 7. Ergebnis

**DoD (2), (3), (4), (5) BESTÄTIGT · DoD (1) BESTÄTIGT bis auf die Teilzusage „Permission-Lage
unverändert", die NICHT BESTÄTIGT ist (§3.1.5) · DoD (6) offen als Closure-Akt · eine
Plan-vs-Ist-Differenz in der Gegenrichtung (§4.2c), eine Plan-Lücke beim Eintritts-Move (§4.2b).**
