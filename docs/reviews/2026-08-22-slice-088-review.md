# Review-Report: slice-088 — 2026-08-22

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `codepaths.exempt-paths`, MR-011 `ids.exempt-paths`)
> — bare IDs und Pfade stehen hier ohne Link-Pflicht.

**Review-Art:** **Code** — der fertige Diff gegen Plan + Konventionen (AGENTS.md, Hard Rules),
nicht gegen Spec/ADR (Plan-Review) und nicht gegen die Architektur (Design-Review).

**Gegenstand:** Commit-Range `f961b4e..1f3f6e6` (drei Commits: `d239099` Implementer ·
`d678de7` Architect · `1f3f6e6` Implementer-Fix), zusätzlich die vier Lifecycle-/Link-Commits
desselben Slice davor (`3ea7a64`, `6766cf2`, `6551545`, `f961b4e`) auf Hard Rule 3.3.
HEAD = `1f3f6e6`, Arbeitsbaum sauber (`git status --porcelain | wc -l` → **0**).

**Skill:** `.harness/skills/reviewer.md` @ **1.4.0** (Baseline v3.5.2, Kurs-Welle 34) · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** claude-opus-5[1m] · **Datum:** 2026-08-22

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne diese Liste ist der Lauf nicht
reproduzierbar; die fünf v3.5.2-Pflicht-Punkte plus die Repo-Ergänzung Slice-Plan):

1. **Diff/Commit-Range:** `f961b4e..1f3f6e6` (`git diff --stat` → `Makefile` 2 ±, `d-check.mk`
   29 ±, `harness/conventions.md` 48 ±, `internal/emit/emit.go` 4 ±).
2. **Slice-Plan:** `docs/plan/planning/in-progress/slice-088-dcheck-pin-v0620.md` (vollständig
   gelesen: §1 Ziel, §2 DoD mit den je Punkt genannten Rot-Kommandos, §3 Plan inkl. der Tabelle
   *Berührte Dateien*, §4 Trigger, §5 Closure-Trigger, §6 Risiken, §8 Sub-Area-Modus).
3. **Berührte `LH-*`:** `LH-QA-02` (Reproduzierbarkeit/Pin), `LH-QA-01` (keine halluzinierten
   Gates), `LH-QA-03` (minimale Abhängigkeiten, Docker-only) — `spec/lastenheft.md`.
4. **Aktive Adaptions-Einträge / ADRs:** `MR-009` (Trockenlauf-Pflicht), `MR-010` (die vier
   Handgriffe; Setzung 2: nur `docs-check` ist behaupteter Gate), `MR-011`, `MR-012`, der neue
   `MR-024` — alle in `harness/conventions.md`. `ADR-0003`/`ADR-0004` nur mittelbar berührt
   (Docker-only-Build, Durchsetzungs-Emission); **keine** ADR-Datei liegt im Range
   (`git show --stat` je Commit).
5. **Hard Rules:** `AGENTS.md` §3.1–§3.8, mit Schwerpunkt §3.1, §3.3, §3.5, §3.6, §3.7, §3.8.
6. **Vorherige Findings am gleichen Modul:** `docs/reviews/2026-07-19-slice-021-review.md`
   (Pin v0.50.0 → v0.51.1, 0 Findings — dessen sieben Prüf-Achsen sind hier wiederholt),
   `docs/reviews/2026-07-18-slice-016-impl-review.md` (MR-009-Zeit),
   `docs/reviews/2026-08-22-slice-086-{review,bestaetigungsrunde,verdikt-runde}.md` (drei Runden
   desselben Tages; Klassen *„ein Beleg, den das Kommando im Dokument nicht (mehr) herstellt"*
   und *„eine Zusage reicht nur so weit wie ihr Sensor"* — der Slice-Plan beruft sich in §2
   ausdrücklich auf diese Lehre).

**Regelwerk on-demand gelesen:** `.harness/baseline/v3.5.2/regelwerk/modul-10-review-harness.md`
(§Drei Review-Arten, §Finding-Kategorien, §Ziel-Form Reviewer-Skill, §Reviewer berichtet auch,
was er nicht gefunden hat) — nicht der ganze Baum.

**Selbst gefahrene Läufe** (jede Zahl unten steht mit ihrem Kommando; Docker-only, Lese-Läufe
per `docker run`, Gates nur als `make`-Target):

| Kommando | Ergebnis |
|---|---|
| `docker inspect --format '{{join .RepoDigests ","}}' ghcr.io/pt9912/d-check:v0.62.0` | `ghcr.io/pt9912/d-check@sha256:3996a593b9cb71aa3bcb4f3ddf8f637e7409db31b3a2dac7eedc28d65814cacf`, Exit 0 |
| `docker buildx imagetools inspect ghcr.io/pt9912/d-check:v0.62.0` | `Digest: sha256:3996a593…5814cacf`, Exit 0 |
| `gh release view v0.62.0 -R pt9912/d-check` | Body: `Digest-Pin: ghcr.io/pt9912/d-check@sha256:3996a593…5814cacf`, published 2026-08-21 |
| `docker run --rm …@sha256:3996a593… --print-mk` | 68 Zeilen, Exit 0 |
| `diff -u <roh-print-mk> d-check.mk` | genau vier Hunk-Gruppen = die vier MR-010-Handgriffe + Digest-Pin, sonst nichts |
| `docker run --rm --network none -v "$PWD:/repo:ro" …@sha256:fede3d02…` (v0.51.1) | `d-check: 333 Datei(en) geprüft, 0 Befund(e)`, Exit 0 |
| `docker run --rm --network none -v "$PWD:/repo:ro" …@sha256:3996a593…` (v0.62.0) | `d-check: 333 Datei(en) geprüft, 0 Befund(e)`, Exit 0 |
| `diff <v0.51.1-Ausgabe> <v0.62.0-Ausgabe>` | leer, Exit 0 |
| `make docs-check` | `333 Datei(en) geprüft, 0 Befund(e)`, Exit 0 (gegen `d-check@sha256:3996a593…`) |
| `make freshness-dcheck` | `d-check: aktuell — gepinnt und latest sind beide v0.62.0.`, Exit 0 |
| `make doc-help` | zwölf Targets gelistet, `docs-check` **und** `doc-structure` dabei, Exit 0 |
| `make doc-structure` / `doc-planning` / `doc-tracked` / `doc-targets` | je `333 Datei(en) geprüft, 0 Befund(e)`, Exit 0 |
| `grep -c structure .d-check.yml` | `0` (Exit 1) |
| `grep -rn 'v0\.51\.1\|fede3d02' --exclude-dir=.git --exclude-dir=docs --exclude-dir=.harness .` | **9 Treffer**, Exit 0 — siehe MEDIUM-1 |
| `git ls-tree -r --name-only v0.5{5,6,7}.0 \| grep -c rules/structure.go` (lokaler d-check-Klon) | `0` / `0` / `1` |

**Nicht gefahren, mit Grund:** `make gates` / `make smoke` / `make full-smoke` / `make mutate` —
`gates` schreibt über `record-gates` einen Zustandsstempel und kollidierte mit dem laufenden
Slice; dieselbe Grenze zog der Reviewer in `docs/reviews/2026-07-19-slice-021-review.md`. Die
Bestätigung dieser Läufe ist ohnehin Verifikations-Territorium (Modul 11). Die für den Review
tragenden Teilaussagen sind stattdessen direkt gemessen (Digest-Kopplung, `docs-check`,
Trockenlauf, Regenerations-Diff).

---

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills (`kategorie · quelle · pfad · befund ·
failure-szenario · verifizierbar`). HIGH zuerst, dann absteigend. Kein Lösungsvorschlag im
Befund-Feld.

### HIGH-1 — Der neu geschriebene `d-check.mk`-Kopf sagt über sechs Recipes etwas, das für fünf gilt

- `kategorie`: **HIGH**
- `quelle`: `AGENTS.md` §3.7 (Hard Rule — *„Ein Kommentar beschreibt, was da ist"*), gestützt von
  §3.6 (eine Zusage in einem Doc-Kommentar); Reviewer-Skill §HIGH (*Verstoß gegen eine Hard Rule*)
  und §Kontext-Eskalation (dieselbe Beobachtung im Gate-Pfad steigt eine Stufe)
- `pfad`: `d-check.mk:11-12`
- `befund`: Der Kopf sagt: *„die sechs fokussierten advisory-Recipes disablen alle drei (verbatim
  vom Tool)"*. Gemessen an derselben Datei: `grep -c -- '--enable' d-check.mk` → **6**;
  `grep -- '--enable' d-check.mk | grep -c -- '--disable structure'` → **5**. Das sechste
  fokussierte Recipe ist `doc-structure` (`d-check.mk:68-70`) — es **enabled** `structure` und
  disabled von den drei opt-in-Modulen nur `citations` und `sources`. Auch der Zusatz *„verbatim
  vom Tool"* trifft die Zahl nicht: die rohe `--print-mk`-Ausgabe von v0.62.0 trägt
  `--disable structure` in fünf, nicht in sechs Recipes. Die Vorfassung (*„die fünf fokussierten
  advisory-Recipes disablen beide"*) war zutreffend; der Satz ist in `d239099` geschrieben worden
  und liegt damit innerhalb des §3.7-Cutoffs (*„Gebunden ist der Kommentar, der geschrieben oder
  geändert wird"*). Der Architect-Text zum selben Sachverhalt ist an dieser Stelle präzise
  (`harness/conventions.md:1137-1138`: *„je ein zusätzliches `--disable structure` in den
  **bestehenden** fokussierten advisory-Recipes"*) — die Ungenauigkeit steht allein im Kopf des
  Gate-Fragments.
- `failure-szenario`: Ein späterer Lauf beantwortet die Frage *„läuft in diesem Repo irgendwo das
  Modul `structure`?"* aus dem Kopf des Gate-Fragments statt aus den Recipes — der Kopf ist genau
  dafür da und ist der einzige Ort, der über die fokussierten Recipes quantifiziert. Er liest
  „alle sechs disablen es" und schließt: nirgends. Tatsächlich fährt `make doc-structure` das
  Modul (selbst gemessen: `--enable structure …`, Exit 0). Wer auf dieser Basis eine
  `structure`-Adoption oder ihre Abgrenzung schneidet, plant gegen einen Ist-Zustand, den es
  nicht gibt; die Klasse ist dieselbe, die §3.7 mit *„beschreibt abwesenden Text"* verbietet, nur
  in der Gegenrichtung — beschrieben wird ein Recipe-Verhalten, das nicht existiert.
- `verifizierbar`: **nein, kein Gate.** `make comment-claims` prüft vier Pfad-Muster
  (`internal/**/*.go`, `cmd/**/*.go`, `harness/tools/*.sh`, `.claude/hooks/*.sh` — `AGENTS.md` §4);
  `d-check.mk` liegt dauerhaft außerhalb, und der Gate prüft ohnehin nur, ob ein *genannter Sensor
  existiert*, nicht, worüber ein Kommentar spricht (`AGENTS.md` §3.7 sagt das selbst). Die zwei
  `grep -c`-Läufe oben belegen den Ist-Zustand; rot färbt ihn heute nichts.

### MEDIUM-1 — Die Rest-Suche, die DoD (1) als Rot-Kommando führt, kann nicht leer werden; drei der fünf Pin-Orte bleiben ohne Sensor

- `kategorie`: **MEDIUM**
- `quelle`: `AGENTS.md` §3.6 (*„Keine Zusage ohne rot gesehenes Gegenbeispiel"* — der DoD-Punkt ist
  ausdrücklich als Zusage-Träger genannt) · `LH-QA-02` · Reviewer-Skill §MEDIUM
  (*Reproduzierbarkeits-Risiko*, *Spec-Treue-Lücke einer Messmethode*)
- `pfad`: `docs/plan/planning/in-progress/slice-088-dcheck-pin-v0620.md:64-70` (DoD (1),
  Rot-Kommando)
- `befund`: DoD (1) nennt als eines von zwei Rot-Kommandos
  `grep -rn 'v0\.51\.1\|fede3d02' --exclude-dir=.git --exclude-dir=docs --exclude-dir=.harness .`
  → **leer**. Am HEAD `1f3f6e6` gefahren liefert dasselbe Kommando **neun** Treffer, Exit 0:
  `test/component-freshness.bats:34` und `:39` (Fixture-Strings — der Plan selbst weist sie in §3
  als *„gemessen kein Pin-Ort"* aus), `harness/conventions.md:531,536,537,543,545` (der
  eingefrorene `MR-012`-Rumpf) und `harness/conventions.md:1116,1134,1136` (die *„von"*-Nennungen
  im **neuen** `MR-024`). Die Formulierung ist damit nicht „unerfüllt", sondern **zu eng
  geschrieben**: zwei der drei Trefferklassen bestanden schon, als der Plan geschrieben wurde, und
  die dritte entsteht **notwendig** aus der Arbeit, die derselbe DoD-Punkt verlangt — ein Eintrag,
  der einen Sprung *von* v0.51.1 beschreibt, muss v0.51.1 nennen. Das Kommando kann folglich unter
  keiner korrekten Ausführung des Slice leer werden. Das zweite Rot-Kommando desselben Punktes
  (`make test` über `TestDefaultImage_MatchesCanonical`/`TestDefaultDigest_MatchesCanonical`)
  trägt, aber nur die Kopplung `internal/emit/emit.go` ↔ `d-check.mk` — gemessen: `mkVar` liest
  `DCHECK_IMAGE`/`DCHECK_DIGEST` aus `../../d-check.mk` (`internal/emit/emit_test.go:27-47`). Für
  den `Makefile`-Kommentar, die §Baseline-Zeile und den MR-Eintrag existiert kein Sensor; die
  Zusage *„an allen fünf Orten — gleichzeitig"* ist damit für **zwei von fünf** Orten gedeckt.
- `failure-szenario`: Beim nächsten Pin-Sprung (der Auflösungs-Trigger von `MR-024` ist
  *permanent*) wird die Rest-Suche als bekannt-verrauscht übergangen. `d-check.mk` und `emit.go`
  wandern, `make gates` bleibt grün — und die §Baseline-Zeile in `harness/conventions.md` behauptet
  weiter eine Image-Version, die nicht mehr läuft. Genau diesen Ausgang benennt der Plan in §6
  („wer ihn ganz vergisst, hinterlässt eine §Baseline-Zeile, die eine Version behauptet, die nicht
  mehr läuft") — ohne dass ein Kommando ihn färbt.
- `verifizierbar`: **teilweise.** `make test` bestätigt zwei der fünf Orte (Emitter-Kopplung); die
  drei übrigen bestätigt kein Gate-Lauf. Die neun Treffer oben sind mit dem Kommando aus dem Plan
  reproduzierbar.
- `rollen-verweis`: Der DoD-Text ist ein **Planner**-Artefakt (Modul 5). Der Implementer hat den
  Befund in `d239099` benannt und an den Planner übergeben; hier ist er als Finding festgehalten,
  nicht als Plan-Änderung.

### MEDIUM-2 — `MR-010` §Setzung 2 zählt und benennt die advisory-Targets abschließend; der Diff macht die Liste unvollständig

- `kategorie`: **MEDIUM** (LOW-Anker *Doku-Drift* plus §Kontext-Eskalation: die Aussage ist der
  `LH-QA-01`-Grenzziehung im Gate-Pfad)
- `quelle`: `LH-QA-01` (*„behauptet" ≠ „vorhanden"*) · `MR-010` §Setzung 2 · `AGENTS.md` §3.8
  (Adaptions-Block = Architect-Artefakt)
- `pfad`: `harness/conventions.md:465-467` (Setzung 2) und `harness/conventions.md:456`
  (§Adaption (c))
- `befund`: `MR-010` §Setzung 2 sagt im Präsens: *„`d-check.mk` bringt zehn
  advisory/opt-in-Targets mit"* und **zählt sie einzeln auf**
  (`doc-trace`/`doc-complete`/`doc-doctor`/`doc-repair`/`doc-immutable`/`doc-commits`/
  `doc-planning`/`doc-tracked`/`doc-targets`/`doc-help`). Nach dem Diff sind es **elf**: gemessen
  `grep -cE '^docs?-[a-z-]+:' d-check.mk` → **12** Targets, davon `docs-check` das einzige
  behauptete Gate; `make doc-help` listet alle zwölf, `doc-structure` darunter. `doc-structure`
  steht in keiner der beiden Stellen. Dieselbe Klasse an `:456`: *„das **volle** Target-Set (elf
  Targets)"* — heute zwölf; der Upstream-CHANGELOG nennt den Übergang selbst („zwölftes
  `--print-mk`-Target `doc-structure`", `[0.57.0]`), und `MR-024:1136-1137` schreibt „elf → zwölf
  Targets", ohne dass `MR-010` nachgezogen wurde. `MR-010` ist **aktiv** (Auflösungs-Trigger:
  permanent), nicht nach `MR-020` auf Kopf und Zeiger zurückgeführt — der Rumpf ist also kein
  eingefrorener Zeitbezug wie der von `MR-012`.
- `failure-szenario`: Ein späterer Lauf prüft die `LH-QA-01`-Frage *„welche Targets dieses
  Fragments sind verfügbar-aber-nicht-behauptet?"* gegen die abschließende Liste in Setzung 2 und
  findet `doc-structure` dort nicht. Die zwei möglichen Schlüsse sind beide falsch: entweder gilt
  es als *behauptetes* Gate (dann fehlt es in `AGENTS.md` §4 / `harness/README.md` §Sensors und
  der Lauf trägt eine Phantom-Gate-Meldung ein), oder es gilt als unbekanntes Target und wird
  entfernt — obwohl es verbatim vom Tool kommt und beim nächsten `--print-mk` zurückkehrt.
- `verifizierbar`: **nein.** Das Modul `targets` (Deklarations-Konsistenz Doku ↔ Build-Targets)
  ist in `.d-check.yml` nicht konfiguriert; selbst gemessen: `make doc-targets` → `333 Datei(en)
  geprüft, 0 Befund(e)`, Exit 0, also über leerem Prüfbereich grün. `make docs-check` fährt
  `targets` gar nicht (`modules: [links, anchors, ids, matrix, codepaths, spans]`).
- `rollen-verweis`: `harness/conventions.md` schreibt nach `AGENTS.md` §3.8 der **Architect**.

### MEDIUM-3 — Die §3.5-Antwort ruht auf einem 0-→-0-Trockenlauf, der eine Lockerung nicht sehen kann

- `kategorie`: **MEDIUM** (Reviewer-Skill §MEDIUM: *Spec-Treue-Lücke einer Messmethode*)
- `quelle`: `AGENTS.md` §3.5 (*Gates nicht ohne ADR lockern*) · `AGENTS.md` §3.6 (Zusagen in
  **Commit-Messages** sind gebunden) · `MR-009` §Trockenlauf
- `pfad`: Commit-Message `d678de7` (*„der Sprung aktiviert kein Modul und senkt keine Strenge
  (AGENTS.md 3.5 nicht beruehrt)"*) mit `harness/conventions.md:1132-1141` als Beleg-Stelle
- `befund`: Der einzige angeführte Beleg ist der byte-gleiche Trockenlauf — v0.51.1 und v0.62.0
  liefern über demselben Baum `333 Datei(en) geprüft, 0 Befund(e)` (selbst reproduziert, `diff`
  leer). Über einer 0-Befund-Basis ist dieses Ergebnis in der **Lockerungs**-Richtung
  informationsleer: eine weggefallene Befundklasse erzeugt dieselbe Ausgabe wie eine unveränderte.
  `MR-024` selbst ist an dieser Stelle vorsichtig (es nennt zwei **Zugänge** in aktiven Modulen —
  `spans`/`fence-unclosed` v0.53.0 und `links`/`resolve-from` v0.60.0 — und schreibt die Inertheit
  dem Lauf zu); die weitergehende Aussage *„senkt keine Strenge"* steht in der Commit-Message ohne
  eigenen Beleg. Selbst gemessen am lokalen d-check-Klon über die Spanne `[0.52.0]`…`[0.62.0]`:
  der CHANGELOG benennt Lockerungen ausdrücklich — *„`closure-note-boilerplate` findet weniger …
  es ist eine **Lockerung**"* und *„Auch das findet weniger"* (`[0.56.0]`), *„Wer hier Befunde
  verliert, verliert Fehlmessungen"* (`[0.53.0]`). Die beiden `[0.56.0]`-Fälle liegen in
  `planning.closure`, das dieses Repo nicht aktiviert; der `[0.53.0]`-Fall ist dagegen eine
  Änderung der **geteilten** Fence-Lexik *„an allen fünf Konsumenten"*, und `spans` ist einer
  davon (`internal/hexagon/core/rules/spans.go` im v0.62.0-Baum) **und** in
  `.d-check.yml:18` aktiv. Für dieses aktive Modul nennt weder der CHANGELOG noch der Diff die
  Richtung.
- `failure-szenario`: Der Auflösungs-Trigger von `MR-024` ist permanent — der nächste Sprung
  wiederholt das Muster. Eine d-check-Version schwächt eine Bedingung in `links`, `anchors` oder
  `codepaths`; der Trockenlauf liefert weiterhin `N/0` gegen `N/0`, die Differenz ist leer, und der
  Sprung wird als *„keine Strenge gesenkt"* eingetragen. Das Repo verliert Gate-Strenge ohne den
  ADR, den §3.5 dafür verlangt — und der Beleg, der das hätte zeigen müssen, ist strukturell blind
  dafür.
- `verifizierbar`: **nein.** Kein Gate-Lauf im Repo vergleicht Befund**klassen** zweier
  d-check-Versionen; die Ausgabe trägt nur Datei- und Befundzahl. Die CHANGELOG-Zitate oben sind
  gegen den lokalen Klon `/Development/d-check` reproduzierbar
  (`awk '/^## \[0\.62\.0\]/{f=1} /^## \[0\.51\.1\]/{f=0} f' CHANGELOG.md`).

### LOW-1 — Der Slice-Plan datiert `structure` weiterhin auf v0.62.0, nachdem der Diff dieselbe Aussage an zwei anderen Orten korrigiert hat

- `kategorie`: **LOW** (Reviewer-Skill §LOW: *Doku-Drift, veraltete Beispiele*)
- `quelle`: `LH-QA-02` (die Mess-Version gehört zur Reproduzierbarkeit) · Muster aus `MR-023`
  (*die Mess-Version nennen*)
- `pfad`: `docs/plan/planning/in-progress/slice-088-dcheck-pin-v0620.md:42-43`
- `befund`: Der Plan schreibt *„Was der Slice ausdrücklich NICHT tut: das **neue** Modul
  `structure` aktivieren. v0.62.0 bringt es als 20. Modul samt eigenem advisory-Target mit."*
  Gemessen am lokalen d-check-Klon: `git ls-tree -r --name-only v0.56.0 | grep -c
  rules/structure.go` → **0**, dasselbe für `v0.57.0` → **1**; der CHANGELOG-Block `[0.57.0]` vom
  2026-08-15 nennt *„Modul `structure`, das 20. Regelmodul"* und *„zwölftes `--print-mk`-Target
  `doc-structure`"*. Genau diese Korrektur ist im Range zweimal vollzogen worden — im
  Architect-Eintrag (`harness/conventions.md:1128-1131`: *„Ausgeliefert ist das Modul samt Target
  seit **v0.57.0**"*) und im Kopf des Gate-Fragments (`1f3f6e6`: `v0.62.0` → `v0.57.0`). Der
  dritte Fundort derselben Aussage ist unverändert geblieben.
- `failure-szenario`: Der Plan ist die Eingabe der Verifikation und der Closure-Notiz. Wird die
  Version von dort in den Closure-Beleg oder in einen Folge-Slice (`structure`-Adoption)
  übernommen, wandert die widerlegte Datierung in ein Zeitdokument, das danach nicht mehr
  angefasst wird — und die zwei korrigierten Fundorte stehen gegen einen dritten, der es nicht
  ist.
- `verifizierbar`: **nein.** Kein Modul des Doku-Gates prüft Versions-Aussagen über ein fremdes
  Werkzeug; `make docs-check` ist grün.
- `rollen-verweis`: Der Slice-Plan ist ein **Planner**-Artefakt — hier gemeldet, nicht geändert.

### LOW-2 — DoD (3) nennt `330 Datei(en)` als erwartete Ausgabe; gemessen werden `333`

- `kategorie`: **LOW**
- `quelle`: `MR-009` (Trockenlauf mit ausgewiesener Differenz) · Reviewer-Skill §LOW
- `pfad`: `docs/plan/planning/in-progress/slice-088-dcheck-pin-v0620.md:94` und `:112`
- `befund`: Der Plan führt zweimal *„Erwartet und am 2026-08-22 vorab gemessen: `d-check: 330
  Datei(en) geprüft, 0 Befund(e)`"*. Selbst gefahren am HEAD `1f3f6e6`, beide Images per Digest,
  `--network none`: je `d-check: 333 Datei(en) geprüft, 0 Befund(e)`, Exit 0 je getrennt erhoben,
  `diff` der beiden Ausgaben leer. Die Abweichung ist erklärbar — zwischen der Vorab-Messung und
  dem Slice sind Dateien hinzugekommen (`251e9ca` schneidet slice-088 und slice-089, `d95ed83`
  legt eine ADR an) —, aber der Plan führt die Zahl als *erwartete Ausgabe*, nicht als
  Zeitstempel-Notiz. Die **tragende** Aussage des Punktes (0-Befund-Differenz, Exit 0 beidseitig)
  ist unabhängig reproduziert und hält.
- `failure-szenario`: Die Verifikation (Modul 11) hakt DoD (3) gegen den Wortlaut ab und findet
  `333` statt `330`. Entweder wird der Punkt fälschlich als nicht erfüllt geführt, oder die
  Abweichung wird stillschweigend akzeptiert — und die Gewohnheit, eine ausgewiesene Zahl nicht
  ernst zu nehmen, trifft beim nächsten Mal eine, die zählt.
- `verifizierbar`: **ja** — `docker run --rm --network none -v "$PWD:/repo:ro"
  ghcr.io/pt9912/d-check@sha256:3996a593… ` liefert die Zahl direkt; `make docs-check` ebenso.
- `rollen-verweis`: **Planner**-Artefakt.

### LOW-3 — `d-check.mk` verwies auf `MR-024`, bevor es `MR-024` gab; kein Sensor sieht das

- `kategorie`: **LOW**
- `quelle`: `AGENTS.md` §5 (Requirement-/ADR-IDs referenzieren) · `MR-011`/`.d-check.yml`
  `ids.link-policy: always`
- `pfad`: `d-check.mk:2` in Commit `d239099`
- `befund`: `d239099` (09:27) schreibt in den Kopf *„adaptiert (MR-010/MR-011/MR-012/MR-024)"*.
  `MR-024` entsteht erst in `d678de7` (11:01). Zwischen beiden Commits trägt der Baum eine
  Kennung ohne Ziel. Gemessen, dass **kein** Sensor das erfasst: `.d-check.yml:32-35` macht
  `MR-\d{3}` mit `link-policy: always` linkpflichtig — die vier `MR-`-Nennungen in `d-check.mk:2`
  sind bare IDs ohne Link und haben nie einen Befund erzeugt; `make docs-check` ist am HEAD grün
  (`333 Datei(en), 0 Befund(e)`). Das `ids`-Modul greift auf `.mk`-Dateien also nicht zu. Am HEAD
  löst der Verweis auf; die Lücke ist die Sensorik, nicht der Endzustand.
- `failure-szenario`: `git revert d678de7` (oder ein Bisect, der auf `d239099` landet) hinterlässt
  ein Gate-Fragment, dessen Kopf eine Adaptions-Kennung nennt, die es nicht gibt. `make docs-check`
  bleibt grün, weil die Datei außerhalb des `ids`-Prüfbereichs liegt — ein toter Verweis genau in
  der Datei, die die Referenz-Integrität des Repos durchsetzt.
- `verifizierbar`: **nein.** Der Nachweis der Nicht-Abdeckung ist der grüne `docs-check`-Lauf über
  bare IDs in `d-check.mk`; rot färbt den Fall nichts.

### INFO-1 — Die Eigenschaft „`structure` ist nicht aktiviert" hat im Repo keinen Träger

- `kategorie`: **INFO** (dokumentationswürdige, undokumentierte Annahme)
- `quelle`: `LH-QA-01` · `AGENTS.md` §3.6 (*eine Zusage reicht nur so weit wie ihr Sensor* —
  die Lehre, auf die der Plan sich in §2 beruft)
- `pfad`: `docs/plan/planning/in-progress/slice-088-dcheck-pin-v0620.md:85-86` (DoD (2),
  Rot-Kommando) und `.d-check.yml:18`
- `befund`: DoD (2) nennt als Rot-Kommando `grep -c 'structure' .d-check.yml` → **0**. Der
  genannte Wert ist der **grüne** Fall, nicht der rote: gemessen liefert das Kommando `0` bei
  Exit 1, und rot wäre eine Zahl > 0. Das ist als Prüfkommando brauchbar, aber es ist ein
  Handlauf, kein Sensor: gemessen trägt kein Test die `modules:`-Liste der Dogfood-`.d-check.yml`
  — `grep -rn 'd-check.yml' test/` findet nur `test/sources-pin.bats` (koppelt den `sources`-Pin
  an `BASELINE_ZIP_SHA256`, nicht die Modul-Liste), und die einzigen Modul-Listen-Assertionen im
  Repo (`internal/emit/emit_test.go:17`, `internal/emit/templates_test.go:51`) betreffen die
  **emittierte** Starter-Config, nicht die eigene. Das zweite Rot-Kommando desselben Punktes
  (`grep -n 'docs-check' d-check.mk` → nicht leer) deckt von den vier `MR-010`-Handgriffen nur den
  ersten ab; die `doc-help`-Grep-Weitung und den adaptierten Kopfkommentar deckt es nicht.
- `failure-szenario`: Ein späterer Slice aktiviert `structure` versuchsweise („kurz probieren",
  vom Plan §6 ausdrücklich untersagt) und vergisst, es zurückzunehmen. Nichts wird rot: `make
  gates` fährt `docs-check` mit der geänderten Liste und meldet entweder eine Befundflut (sichtbar)
  oder — bei leerem Regelsatz — `N Datei(en) geprüft, 0 Befund(e)`, Exit 0 über einem Modul, das
  keine Datei geöffnet hat. Die zweite Variante ist der Phantom-Gate-Fall, den `LH-QA-01`
  ausschließt, und sie ist am Gate-Ausgang nicht von Grün zu unterscheiden.
- `verifizierbar`: **nein.** Kein Gate-Lauf im Repo bestätigt oder widerlegt die Modul-Liste;
  belegt ist nur der Ist-Wert (`grep -c structure .d-check.yml` → 0).
- `rollen-verweis`: Die Sensor-Frage gehört in einen eigenen Schnitt (**Planner**); die DoD-Formulierung
  ist **Planner**-Artefakt.

---

## Negativbefunde

Eine Zeile pro betrachtetem Bereich — sonst ist „keine Findings" nicht von „nicht geprüft" zu
unterscheiden.

- **geprüft, ohne Befund — Digest-Provenienz, dreifach belegt (`LH-QA-02`, DoD (1)):** alle drei
  im Plan genannten Quellen liefern **denselben** Wert. `docker inspect --format '{{join
  .RepoDigests ","}}' ghcr.io/pt9912/d-check:v0.62.0` → `…@sha256:3996a593b9cb71aa3bcb4f3ddf8f637e
  7409db31b3a2dac7eedc28d65814cacf`; `docker buildx imagetools inspect …:v0.62.0` → `Digest:
  sha256:3996a593…5814cacf` (Registry-Manifest); `gh release view v0.62.0 -R pt9912/d-check` →
  Body `Digest-Pin: ghcr.io/pt9912/d-check@sha256:3996a593…5814cacf`. `d-check.mk:17` und
  `internal/emit/emit.go:34` pinnen exakt diesen Wert. Drei unabhängige Quellen, keine Abweichung.
- **geprüft, ohne Befund — `d-check.mk` ist faithful aus `--print-mk` v0.62.0 erzeugt (DoD (2),
  `MR-010`):** `docker run --rm …@sha256:3996a593… --print-mk` (68 Zeilen, Exit 0) gegen die
  committete Datei liefert **ausschließlich** die vier dokumentierten Handgriffe plus den
  Digest-Pin: adaptierter Kopfkommentar (`:1-15`), `DCHECK_DIGEST ?=` → gepinnt (`:17`),
  `.PHONY: doc-check`/`doc-check:` → `docs-check` samt Hilfetext (`:28-29`), `doc-help`-Grep
  `'^doc-[a-z-]+:` → `'^docs?-[a-z-]+:` samt Hilfetext (`:73-74`). Alle sechs fokussierten
  Recipes, `doc-structure` eingeschlossen, stehen im Diff als **unveränderter Kontext** — die
  `--disable structure`-Flags stammen verbatim vom Tool, nicht aus der Hand. `DCHECK_IMAGE` liefert
  das Tool selbst bereits als `:v0.62.0`.
- **geprüft, ohne Befund — Pflicht-Trockenlauf unabhängig reproduziert (DoD (3), `MR-009`):** beide
  Images per **Digest**, `--network none`, Exit getrennt erhoben, gegen den HEAD-Baum mit
  unveränderter `.d-check.yml`: v0.51.1 (`@sha256:fede3d02…`) → `d-check: 333 Datei(en) geprüft,
  0 Befund(e)`, Exit 0; v0.62.0 (`@sha256:3996a593…`) → identisch, Exit 0; `diff` der Ausgaben
  leer. **Zum Baum-Unterschied:** der Implementer maß gegen `f961b4e`, ich gegen `1f3f6e6`. Die
  drei dazwischenliegenden Commits **ändern** Dateien, sie **legen keine an** (`git diff --stat
  f961b4e..1f3f6e6` → vier bestehende Dateien) — die Datei-Zahl bleibt deshalb `333`, und die
  Aussage „0-Befund-Differenz über elf Minors" ist durch den Baum-Wechsel nicht berührt, sondern
  über einem **zweiten**, größeren Korpus zusätzlich bestätigt.
- **geprüft, ohne Befund — `structure` verfügbar, nicht aktiviert (`LH-QA-01`):** `grep -c
  structure .d-check.yml` → **0** (Exit 1); `.d-check.yml:18` führt unverändert `modules: [links,
  anchors, ids, matrix, codepaths, spans]`; `.d-check.yml` liegt nicht im Diff (`git diff --stat
  f961b4e..1f3f6e6` nennt vier Dateien, keine davon `.d-check.yml`).
- **geprüft, ohne Befund — kein neuer Gate-Name (`LH-QA-01`, `AGENTS.md` §3.1):** `AGENTS.md` §4
  und `harness/README.md` §Sensors sind im Range **nicht berührt** und führen `doc-structure`
  nicht; `docs-check` bleibt das einzige behauptete d-check-Target. `d-check.mk:3-4` sagt das
  weiterhin selbst (*„als EINZIGES Target in `make gates` + AGENTS/README behauptet"*).
- **geprüft, ohne Befund — `doc-structure` ist kein neuer Phantom-Gate-Fall gegenüber dem
  Bestand:** selbst gemessen laufen `make doc-structure`, `make doc-planning`, `make doc-tracked`
  und `make doc-targets` **alle vier** mit `333 Datei(en) geprüft, 0 Befund(e)`, Exit 0 über
  unkonfigurierten Modulen. `doc-structure` verhält sich damit exakt wie die bestehenden
  advisory-Targets, die `MR-010` §Setzung 2 als *„verfügbar, aber nicht als Gate behauptet"*
  deklariert — der Diff fügt dieser Klasse ein Mitglied hinzu, keine neue Klasse. (Dass die
  Deklaration selbst unvollständig wird, ist MEDIUM-2.)
- **geprüft, ohne Befund — Emitter-Kopplung (Tier-1-Drift, `LH-QA-02`):** `internal/emit/emit.go:33-34`
  ist byte-gleich zu `d-check.mk:16-17` (`ghcr.io/pt9912/d-check:v0.62.0` /
  `sha256:3996a593…5814cacf`, je durch direkten Textvergleich der beiden Dateien geprüft).
  `TestDefaultImage_MatchesCanonical`/`TestDefaultDigest_MatchesCanonical`
  (`internal/emit/emit_test.go:27-47`) lesen `../../d-check.mk` über `mkVar` und erzwingen
  Gleichheit plus `sha256:`-Präfix — der Wächter greift auf die kanonische Quelle zu, nicht auf
  eine Kopie.
- **geprüft, ohne Befund — die v0.46.0-Fixture `internal/emit/testdata/raw-print-mk.txt` muss
  nicht mitziehen; gemessen, nicht vermutet:** `AdaptMK` (`internal/emit/emit.go:122-149`) ist
  **fail-closed** auf allen vier Ankern. Gegen die frische v0.62.0-Rohausgabe gemessen sind alle
  vier vorhanden: `DCHECK_IMAGE ?=` → 1 Treffer, `.PHONY: doc-check` → 1, `^DCHECK_DIGEST \?=$`
  (leere Zeile) → 1, `'^doc-[a-z-]+:` → 1. Der Adapter greift also auf der neuen Ausgabe
  unverändert. Der Strukturvergleich der `.PHONY`-Zeilen zeigt genau **eine** Differenz
  (`11` → `12`, ergänzt `doc-structure`) — kein Format-Bruch. Die Fixture ist bewusst hermetisch:
  ihr Doc-Kommentar (`internal/emit/emit_test.go:69-71`) nennt die Version selbst
  (*„testdata/raw-print-mk.txt, v0.46.0"*), sie prüft die **Handgriffe**, nicht den Pin, und die
  Live-Format-Achse deckt `AdaptMK`s harter Abbruch plus `make full-smoke`.
- **geprüft, ohne Befund — emittierte Ebene bleibt unberührt:** `internal/emit/templates/d-check.yml`
  liegt nicht im Diff und führt weiter `modules: [links, anchors]` (`MR-017`, fail-closed);
  `TestDCheckConfig_Minimal` (`internal/emit/emit_test.go:12-25`) hält das fest.
  `grep -rn 'doc-trace\|doc-targets\|doc-help\|advisory' internal/emit/templates/` → **keine
  Treffer**: keine emittierte Vorlage zählt Targets auf, die durch `doc-structure` stale würde.
  `adopterHeader` (`internal/emit/emit.go:41-45`) ist versions-agnostisch (*„advisory
  doc-*-Targets verbatim"*) und bleibt zutreffend.
- **geprüft, ohne Befund — Hard Rule 3.3 (Move ≠ Inhalt), beide Moves:** `git show --numstat
  --find-renames=100% 3ea7a64` → `0 0 docs/plan/planning/{open => next}/slice-088-…md`;
  `… 6551545` → `0 0 …/{next => in-progress}/…`. Beide sind reine 100%-Renames ohne eine einzige
  Inhaltszeile. Der Link-Zug liegt in **eigenen** Folge-Commits (`6766cf2`, `f961b4e`), die
  ausschließlich `docs/plan/planning/done/slice-086-…md` berühren und dort **nur** das
  Pfad-Segment (`../open/` → `../next/` → `../in-progress/`) tauschen — der Diff zeigt je drei
  Zeilen, in denen sonst kein Zeichen abweicht. Plan §5 („der Link-Zug gehört zu BEIDEN Moves")
  ist damit im Commit-Zuschnitt sichtbar erfüllt.
- **geprüft, ohne Befund — Hard Rule 3.8 (Adaptions-Block schreibt der Architect):**
  `git show --stat d678de7` → **genau eine** Datei, `harness/conventions.md`, 47 +/1 −; kein
  zweites Artefakt, keine ADR, kein Code. Die Message beginnt mit *„Rolle Architect: …"* und nennt
  die Rolle damit ablesbar. Umgekehrt berühren die zwei Implementer-Commits `harness/conventions.md`
  **nicht** (`d239099`: `Makefile`, `d-check.mk`, `internal/emit/emit.go`; `1f3f6e6`: nur
  `d-check.mk`). Der Zuschnitt entspricht §3.8 in beide Richtungen. Der neue `MR-024`-Rumpf ist
  rein **additiv** ans Ende des Blocks gehängt; die §Baseline-Zeile ist der einzige Eingriff in
  Bestehendes, und die Rümpfe von `MR-009`/`MR-010`/`MR-011`/`MR-012` sind unberührt (Kontextzeilen
  im Diff).
- **geprüft, ohne Befund — Hard Rule 3.4 (ADRs immutable):** keine Datei unter `docs/plan/adr/`
  liegt im Range (`git diff --stat f961b4e..1f3f6e6` → vier Dateien, keine ADR). `MR-024` stellt
  ausdrücklich fest, dass kein Folge-ADR entsteht, und die Begründung (kein Modul aktiviert, keine
  Strenge gesenkt) ist an der Modul-Liste unabhängig nachvollziehbar — zur Beleg-Frage s. MEDIUM-3.
- **geprüft, ohne Befund — Hard Rule 3.2 (Lint-Suppression):** kein `//nolint`, kein
  `# shellcheck disable` im Diff. Die `--disable <modul>`-Flags sind d-check-Modul-Schalter aus der
  Tool-Ausgabe, keine Lint-Suppression.
- **geprüft, ohne Befund — Hard Rule 3.7 an den zwei übrigen Kommentaren:** Der `Makefile`-Kommentar
  (`Makefile:39-41`) beschreibt die Ableitung im Indikativ und führt das Tag als Beispiel, das mit
  `DCHECK_IMAGE` (`d-check.mk:16`) übereinstimmt — geprüft, korrekt. Der Doc-Kommentar an
  `DefaultImage`/`DefaultDigest` (`internal/emit/emit.go:27-31`) ist versions-agnostisch, nennt die
  Kopplung (*„treibt den docker-Lauf UND das emittierte DCHECK_DIGEST"*) und die Grenze
  (Env-Override, *„Digest sticht Tag"*) — zwei der fünf zulässigen Klassen, keine Historie, kein
  Konjunktiv über Verworfenes. Beide unauffällig; der Befund HIGH-1 betrifft ausschließlich den
  dritten Kommentar.
- **geprüft, ohne Befund — `d-check.mk:14-15` (die verkürzte Neu-Erzeugungs-Anleitung) liegt
  außerhalb des §3.7-Cutoffs:** die Zeilen nennen nur zwei der vier Handgriffe, stehen aber im
  Diff als **unveränderter Kontext**. §3.7 bindet den Kommentar, der geschrieben oder geändert
  wird; *„der Bestand ist kein Arbeitsauftrag"*. Kein Finding, hier nur benannt, damit „nicht
  geprüft" ausgeschlossen ist.
- **geprüft, ohne Befund — `LH-QA-03` (Docker-only):** der Diff führt keinen Host-Toolchain-Aufruf
  ein; alle Recipes in `d-check.mk` laufen als `docker run --rm --network none … :ro`, der Mount
  bleibt read-only, `--network none` steht auf **jedem** Recipe, das einen Container startet
  (gezählt: `grep -c 'network none' d-check.mk` → **11** — elf der zwölf Targets fahren
  `docker run`, das zwölfte (`doc-help`) ist ein reiner `grep` über `MAKEFILE_LIST`). Der Review
  selbst ist Docker-only gefahren.
- **geprüft, ohne Befund — der auslösende Sensor ist bedient:** `make freshness-dcheck` → *„d-check:
  aktuell — gepinnt und latest sind beide v0.62.0."*, Exit 0. Der Eintritts-Trigger aus Plan §4
  (*„freshness-dcheck meldet VERALTET"*) ist damit aufgelöst.
- **geprüft, ohne Befund — Strenge-Zugänge in aktiven Modulen sind eine Verschärfung, nicht eine
  Lockerung:** `spans` hat mit `[0.53.0]` die Klasse `fence-unclosed` bekommen, und der
  CHANGELOG benennt den Anlass als *„ausgelieferter stiller Grün-Pfad"* — hinter einem offenen
  Fence übersprang die Vorverarbeitung den Rest, *„und Gates meldeten grün, ohne geprüft zu
  haben (reproduziert gegen das veröffentlichte Image v0.52.0)"*. Dieses Repo stand auf v0.51.1
  und war derselben Klasse ausgesetzt. Der Sprung schließt sie, und der Trockenlauf zeigt, dass
  der Korpus sie nicht auslöst (`333/0` unter v0.62.0). Das ist die `LH-QA-01`-relevante
  **Verbesserung** dieses Diffs; §3.5 gilt für Senkungen und ist davon nicht berührt.
- **geprüft, ohne Befund — die vier Lifecycle-/Link-Commits tragen keine Inhaltsänderung am
  Slice-Plan:** `git log --oneline --follow --numstat -- docs/plan/planning/in-progress/slice-088-dcheck-pin-v0620.md`
  zeigt die Datei im betrachteten Abschnitt in genau zwei Commits — `3ea7a64` und `6551545`, je
  `0 0`. Die einzigen Inhaltsänderungen liegen **vor** dem Abschnitt (`251e9ca` legt sie mit 228
  Zeilen an, `fce7f65` zieht 3 Link-Zeilen nach). Der Plan ist zwischen `open/` und
  `in-progress/` byte-identisch geblieben; LOW-1 und LOW-2 betreffen seinen **Inhalt seit dem
  Planner-Lauf**, nicht eine Änderung im Range.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 1 |
| MEDIUM | 3 |
| LOW | 3 |
| INFO | 1 |

**Klassen-Beobachtung (Skill §Kontext-Eskalation).** MEDIUM-1, MEDIUM-3 und INFO-1 sind
**dieselbe Klasse** wie die drei MEDIUM aus `docs/reviews/2026-08-22-slice-086-review.md`:
*ein Beleg, den das Kommando im Dokument nicht (mehr) herstellt* — bzw. ihr Zwilling *eine Zusage
reicht nur so weit wie ihr Sensor*. Die Wiederkehr am **Folgetag** und an einem **anderen
Gegenstand** (dort ein Mess-Slice, hier ein Pin-Slice) ist das Signal, das der Skill meint: die
Klasse ist nicht an ein Artefakt gebunden, sondern an die Gewohnheit, ein Rot-Kommando zu
**nennen**, ohne es einmal rot gesehen zu haben. Bemerkenswert ist, dass der Plan sich in §2
ausdrücklich auf genau diese Lehre beruft (*„Jeder Punkt nennt das Kommando, das ihn rot färbt —
eine Zusage reicht nur so weit wie ihr Sensor"*) und trotzdem in zwei von drei Punkten ein
Kommando nennt, das den behaupteten Dienst nicht leistet. Benannt, nicht gelöst — Träger und
Sensor sind nicht die Rolle des Reviewers.

## Verdikt

**Merge-blockierend: ja.**

**Begründung.** Ein **HIGH** (Verstoß gegen die Hard Rule `AGENTS.md` §3.7 im Kopf des
Gate-Fragments) und **drei MEDIUM** stehen offen; HIGH und MEDIUM blockieren nach Skill §Ablage
typischerweise, und keiner der vier trägt einen Grund, davon abzuweichen. HIGH-1 ist die schmalste
Reparatur des Satzes und zugleich die einzige, die eine Hard Rule betrifft — sie ist innerhalb
des §3.7-Cutoffs entstanden, in **diesem** Diff, und ist am Ist-Zustand der Datei mit zwei
`grep -c`-Läufen widerlegbar oder bestätigbar.

**Was ausdrücklich NICHT blockiert und belegt trägt:** die Substanz des Slice. Der Digest ist
dreifach unabhängig belegt und an allen vier lebenden Orten identisch; `d-check.mk` ist faithful
aus `--print-mk` v0.62.0 erzeugt und ausschließlich an den vier `MR-010`-Stellen adaptiert (Diff
gegen die frische Tool-Ausgabe selbst gefahren); der Pflicht-Trockenlauf ist unabhängig
reproduziert und liefert eine **echte** 0-Befund-Differenz über elf Minors; `structure` ist
verfügbar und **nicht** aktiviert, ohne dass ein neuer Gate-Name entsteht; die Emitter-Kopplung
hält; die Rollen-Trennung nach §3.8 ist im Commit-Zuschnitt sauber vollzogen, und der
Architect-Lauf hat mit der `v0.57.0`-Korrektur einen echten Messfehler des Auftrags gefunden.
Drei der vier blockierenden Findings betreffen **Beleg-Ketten und Register-Text**, nicht den Pin.

**Rollen-Zuordnung der Findings** (für die Übergabe, nicht als Aufgabenliste):

| Finding | schreibende Rolle des betroffenen Artefakts |
|---|---|
| HIGH-1 (`d-check.mk`-Kopf) | Implementer |
| MEDIUM-1 (DoD (1) Rot-Kommando) | Planner |
| MEDIUM-2 (`MR-010` §Setzung 2) | **Architect** (`AGENTS.md` §3.8) |
| MEDIUM-3 (§3.5-Beleg) | **Architect** |
| LOW-1 / LOW-2 (Plan §1, DoD (3)) | Planner |
| LOW-3 (`MR-024`-Verweis vor Existenz) | Implementer / Prozess |
| INFO-1 (Sensor für die Modul-Liste) | Planner (eigener Schnitt) |

**Übergabe.** Findings gehen an die Implementation; die Plan-Findings sind die Rückkante
Review → Plan. Der Report ersetzt **keine** Verifikation — DoD-Abhakung und die Bestätigung von
`make gates` / `make smoke` / `make full-smoke` / `make mutate` sind Modul-11-Territorium mit
anderem Prüf-Artefakt und anderem Eingabe-Kontext; sie sind hier bewusst nicht vorgenommen.
