# Review-Report: slice-105 (Code) — 2026-08-27

**Review-Art:** **Code**, erste Runde — geprüft wird der Diff gegen den Slice-Plan, die
referenzierten Anforderungen/ADRs und die Hard Rules (Modul 10 §Drei Review-Arten). Kein
vorheriger Review-Lauf zu diesem Slice.

**Gegenstand:** der **committete** Diff. `git rev-parse HEAD` → `82d2ed70fce6acc7a7b99293163570e1864088d1`;
`git status --porcelain=v1` → leer (sauberer Arbeitsbaum). `git show --stat 82d2ed7` →
**9 Dateien, 955 insertions(+), 23 deletions(-)**: `M Makefile`, `M harness/README.md`,
`M harness/tools/mutate.sh` (+635), `M test/mutate-driver.bats` (+272),
`A test/mutations/191..195`.

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0
**Modell:** claude-opus-5[1m] · **Datum:** 2026-08-27

**Eingangs-Kontext:**

- **Diff:** volltext gelesen (alle 9 Dateien), dazu `harness/tools/mutate.sh` in Gänze
  (1159 Zeilen) und die Vorfassung (`git show 82d2ed7^:harness/tools/mutate.sh`, 566 Zeilen)
  zum Abgleich der Aufruf-Kontexte (tragend für F-2).
- **Slice-Plan:**
  [slice-105](../plan/planning/in-progress/slice-105-mutate-messen-dann-teilen.md),
  vollständig gelesen (520 Zeilen): §1 Ziel/Bestand/Modell/Auflage, §2 DoD (1)–(3),
  §3 Plan-Tabelle + offene Fragen A/B/C, §4 Trigger und Reihenfolge-Bedingung,
  §5 Closure-Trigger, §6 Risiken, §8 Sub-Area.
- **Anforderungen, gelesen als Constraint:**
  [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
  (Analogie-Bezug des Plans: eine parallele Fassung, die still grün meldet),
  [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit) (dieselbe Fall-Menge,
  dasselbe Verdikt, gleich über wie viele Worker),
  [`LH-QA-03`](../../spec/lastenheft.md#lh-qa-03--minimale-abhängigkeiten) (bash/coreutils —
  der Grund für den `mkdir`-Mutex).
- **ADRs:** der Diff referenziert **keine** ADR-ID
  (`git show 82d2ed7 | grep -E '^\+' | grep -oE 'ADR-[0-9]{4}'` → leer); der Plan referenziert
  [`ADR-0015`](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md), Status **Accepted**
  (`grep -iEm3 'Status' docs/plan/adr/0015-*.md`). Keine superseded ADR im Spiel.
- **Hard Rules:** `AGENTS.md` §3.1, §3.2, §3.3, §3.5, §3.6, §3.7, §3.8, §3.9 — jede unten mit
  eigenem Kommando geprüft (Findings bzw. Negativbefunde).
- **Adaptionen:** [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  (Geltungsbereich selbst nachgelesen: **lebende repo-eigene Markdown-Artefakte**;
  `harness/README.md` liegt darin, `docs/reviews/**` ausdrücklich nicht),
  [`MR-014`](../../harness/conventions.md#mr-014--ci-auf-frischem-klon-github-actions)
  (der Pro-Push-Auslöser, an dem die Worker-Zahl hängt).
- **Vorherige Findings am gleichen Modul:** `grep -rl 'mutate' docs/reviews/*.md` liefert ältere
  Läufe zu `mutate.sh` (u. a. `2026-08-25-slice-097-verify.md`); die dort behandelten Klassen
  (Isolation, `failure_form`, Grün-Vorlauf) sind hier nicht wiederholt aufgetreten — die
  zugehörigen Wächter stehen unverändert und sind unten als Negativbefund geführt.

---

## Selbst gefahren — nichts aus Commit-Message, Plan oder Auftrag übernommen

[`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
Setzung 1: jede Zahl unten trägt das Kommando, das genau sie ausgibt, und es ist gefahren.
Die Zeitwerte sind Maschinen- und Cache-Zustände, **keine** Erwartungswerte.

| Zahl / Aussage | Kommando | Ergebnis |
|---|---|---|
| HEAD, Arbeitsbaum | `git rev-parse HEAD`; `git status --porcelain=v1` | `82d2ed7`, leer |
| Fall-Bestand jetzt / vorher | `ls -1 test/mutations/*.sh \| wc -l`; `git ls-tree -r --name-only 82d2ed7^ test/mutations/ \| grep -c '\.sh$'` | **188** / **183** (+5) |
| `mutate.sh`-Zeilen jetzt / vorher | `wc -l < harness/tools/mutate.sh`; `git show 82d2ed7^:harness/tools/mutate.sh \| wc -l` | **1159** / **566** |
| Kommentarzeilen in `mutate.sh` | `grep -cE '^[[:space:]]*#' harness/tools/mutate.sh` | **523** (45 % der Datei) |
| neue `@test` im Diff | `git show 82d2ed7 -- test/mutate-driver.bats \| grep -c '^+@test'` | **14** (Commit-Message sagt 13 — F-11) |
| `@test` gesamt jetzt / vorher | `grep -c '^@test' test/mutate-driver.bats`; `git show 82d2ed7^:test/mutate-driver.bats \| grep -c '^@test'` | **35** / **21** |
| `make shell-lint` | `make shell-lint` | grün, Exit 0 |
| `make comment-claims` | `make comment-claims` | grün, **46 Datei(en), 0 Befund(e)** |
| `make docs-check` | `make docs-check` | grün, **411 Datei(en), 0 Befund(e)** |
| `make test-bats` | `make test-bats` | **176 ok, 0 not ok**, davon **35** `driver:`-Fälle |
| Trockenlauf-Plan je Modus | `for m in test test-go test-bats smoke full-smoke ci-lint; do env -u MAKEFLAGS -u MAKELEVEL -u MFLAGS make --no-print-directory -n "$m"; done` | `test`/`test-go`/`test-bats`/`ci-lint` = nur `docker build`/`docker run`; `smoke`/`full-smoke` = `bash harness/tools/*.sh` |
| Statische Sensor-Zuordnung (Rezept aus Plan §3) | Schleife über `test/mutations/*.sh`: eigener `# verify:`-Kopf, sonst `narrow_sensor` über `# expect:`; `\| sort \| uniq -c` | **134** `test-go` · **46** `test-bats` · **6** `full-smoke` · **1** `smoke` · **1** `ci-lint` (Summe 188) |
| Fälle mit `Makefile` als Mutations-Ziel | `grep -l '^# files:.*Makefile' test/mutations/*.sh \| wc -l` | **4** (78–81, alle am `release-artifacts`/`build`-Rezept) |
| Isolations-Größe je Kopie / je Lauf | `du -sh /tmp/tmp.N3FNeHi39I/w*/repo`; `du -sh /tmp/tmp.N3FNeHi39I` | **50 M** je Kopie, **201 M** je Lauf (N=4) |
| Greifen die fünf neuen Mutationen? | je Fall: frische Kopie, `(cd $S && bash test/mutations/<n>-*.sh)`, dann `diff` gegen das Original | alle **5** greifen, jede an genau der beabsichtigten Zeile (915 / 431+776 / 848 / 702 / 683-684) |
| Färbt jede neue Mutation ihren benannten Wächter rot? | isolierte `tar`-Kopie von `harness/tools`+`test`, Mutation, dann `docker run --rm --network none -v $S:/code:ro -w /code bats/bats@sha256:e8f1… test/mutate-driver.bats` | **je genau ein zusätzliches `not ok`**, und zwar der im `# expect:` benannte Fall (33 / 34 / 29 / 23 / 35). Baseline derselben Kopie: **1** vorbestehendes `not ok 7` (mein Ausschnitt trägt kein `.git`) |
| `set -e` in getestetem Kontext | eigenes Sonden-Skript: `f` mit `false` im Rumpf, einmal direkt gerufen, einmal als `f \|\| rc=$?`; dazu verschachtelt über zwei Funktionsebenen und eine `{ }`-Gruppe | direkt: **Abbruch**; als `f \|\| rc=$?`: **läuft weiter**, auch verschachtelt |
| Sonde am echten Treiber | `worker_main 7 '' light` mit schreibgeschütztem `$ISO_ROOT/w7` (→ `mkdir -p "$TMPDIR"` scheitert) | Worker läuft **über den Fehler hinweg** weiter, schreibt `draws.7`, endet erst an `green_prerun` |
| Sonde an `merge_report` | 3-Fall-Fixture, `status.2` einmal abgeschnitten (`2\tfall-2\tBEF`), einmal leer; `merge_report 3 >datei` | beide Male **`befunde=0 ok=3`** und **„Vollstaendigkeit — 3 von 3 Fall-Dateien mit Ergebnis, jede Fall-ID genau einmal gezogen."** |
| Sonde an `report_times` | dasselbe Fixture mit leerer `status.2`; `report_times 3` | Kopfzeile **„ueber 3 von 3 Fall-Dateien"**, darunter `test-go n=2` und „Zeit je Fall, absteigend (alle 3)" mit **2** Zeilen |
| Sonde am Zug-Protokoll | dasselbe Fixture, `draws.1` = `1\n3\n` bzw. `1\n3\n99\n` | beide Male **`befunde=0`** und die Bestätigungszeile „jede Fall-ID genau einmal gezogen" |

**Der `make mutate`-Lauf ist nicht meiner** — ich habe ihn nicht gestartet (Auflage 1) und lese
sein Protokoll als **fremde Messung** unter `/tmp/claude-1000/…/scratchpad/mutate-verify.log`
(590 Zeilen). Daraus mit eigenem Kommando gezogen:

| Größe | Kommando über dem Protokoll | Ergebnis |
|---|---|---|
| Verdikt / Wanduhr / Worker | `tail -5`; `sed -n '1,5p'` | **188 ok, 0 Befund(e)**, `MUTATE_SECONDS=526.26`, **4** Worker, 4 Kopien |
| serielle Spur | `sed -n '1p'` | `smoke full-smoke` — deckt sich mit meiner eigenen `make -n`-Messung |
| Zeit je Sensor | `sed -n '/Zeit je Sensor/,/Gruen-Vorlaeufe/p'` | `test-go` 1196,6 s / **64,0 %** (n=134) · `test-bats` 578,6 s / **31,0 %** (n=46) · `full-smoke` 90,5 s / **4,8 %** (n=6) · `smoke` 2,3 s · `ci-lint` 0,7 s |
| Mittel je Sensor | dieselbe Stelle | `full-smoke` **15,09** · `test-bats` **12,58** · `test-go` **8,93** · `smoke` **2,30** · `ci-lint` **0,65** |
| Streuung **innerhalb** eines Sensors | `sed -n '/Zeit je Fall, absteigend/,/untere Schranke/p' \| awk` (min/max je Modus) | `full-smoke` 1,95 → 55,31 s = **28,4×** · `test-go` 2,2× · `test-bats` 1,3× |
| Grün-Vorläufe | `sed -n '/Gruen-Vorlaeufe/,/Zeit je Fall/p' \| awk` | **9** Einträge, Summe **175,32 s**; Worker 1 nur `full-smoke` + `smoke` |
| Fälle je Worker | `grep -oE '^mutate: \[w[0-9]\]' \| sort \| uniq -c` | w1 **41**, w2/w3/w4 je **49** (Summe 188) |

---

## Findings

### F-1 — Die Vollständigkeits-Achsen zählen **Dateien**, nicht **Urteile**: eine Statusdatei ohne lesbares Verdikt wird als OK gezählt, und der Lauf bestätigt daneben seine Vollständigkeit

- **Kategorie:** HIGH
- **Quelle:** Plan §1 („Die Auflage, die nicht verhandelbar ist" — *„Der zusammengeführte Bericht
  belegt, dass er jeden Fall gefahren hat … fail-closed bei Abweichung"*) + `AGENTS.md` §3.6 +
  [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6) (Analogie
  auf der Dogfood-Ebene, wie der Plan sie selbst zieht)
- **Pfad:** `harness/tools/mutate.sh:840-846` (`merge_report`, Verdikt-Auswertung),
  `harness/tools/mutate.sh:914-918` (`report_times`, Nenner-Prüfung),
  `harness/tools/mutate.sh:891` (Bestätigungszeile)
- **Befund:** `merge_report` liest die Statuszeile mit
  `IFS=$'\t' read -r _ _ st _ _ <<<"$st_line"` und entscheidet mit
  `if [ "$st" = "BEFUND" ]; then fail_count++ ; else pass_count++ ; fi`. Jeder Wert, der **nicht**
  exakt `BEFUND` ist — auch der leere —, zählt als bestanden. `gelaufen` wird davor bedingungslos
  erhöht, sobald die **Datei existiert**; `report_times` misst seinen Nenner mit
  `n="${#status_files[@]}"`, also ebenfalls über die Datei-Anzahl. Gegen den echten Treiber
  gefahren (Fixture aus drei Fällen, `status.2` einmal auf `2\tfall-2\tBEF` abgeschnitten, einmal
  leer): beide Male **`befunde=0 ok=3`** und in derselben Ausgabe
  `mutate: Vollstaendigkeit — 3 von 3 Fall-Dateien mit Ergebnis, jede Fall-ID genau einmal gezogen.`
  `report_times` gibt im selben Zustand eine Bilanz aus, deren Kopf
  `Zeit je Sensor ueber 3 von 3 Fall-Dateien` sagt, während die Aggregation darunter `n=2` führt
  und die Liste „Zeit je Fall, absteigend (alle 3)" **zwei** Zeilen enthält — genau die
  „Bilanz über einer Teilmenge", die DoD (1) und `test/mutations/191` ausschließen sollen; die
  Schranke greift nicht, weil sie eine andere Größe misst als die, über die die Bilanz rechnet.
  Der Kanal ist **neu mit diesem Diff**: vor `82d2ed7` führte `run_case` den Zähler im selben
  Prozess (`git show 82d2ed7^:harness/tools/mutate.sh:544` → `run_case "$c"` direkt in `main`),
  es gab keine Statusdatei und damit kein Parsen. Auslöser ist ein Schreibfehler ohne
  Prozess-Tod (volle Platte, Quota) — stirbt der Worker, fängt ihn die getrennte `wait`-Achse
  (`mutate.sh:1112-1124`). Die Reichweite ist damit schmal, die Richtung aber fail-**open** in
  genau dem Bericht, dessen Slice-Auflage lautet: *vollständig oder rot, nie still grün*.
- **Verifizierbar:** ja — Fixture wie oben (`status.$i`, `case.$i.log`, `draws.1` für drei Fälle),
  `status.2` leeren, dann `merge_report 3` und `report_times 3` über dem gesourcten Treiber:
  `fail_count` bleibt 0, die Bestätigungszeile erscheint.

### F-2 — `set -e` ist im gesamten Worker-Rumpf ausgesetzt; der Diff hat den Schutz, den Zeile 97 zusagt, aus ~70 neuen Zeilen und aus `run_case` entfernt

- **Kategorie:** MEDIUM
- **Quelle:** `AGENTS.md` §3.6 (der Kopf von `mutate.sh` führt „FAIL-CLOSED, fuenf Bedingungen"
  als seine Zusage) + Reviewer-Anker „Reproduzierbarkeits-Risiko"
  ([`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit))
- **Pfad:** `harness/tools/mutate.sh:97` (`set -euo pipefail`) gegen
  `harness/tools/mutate.sh:1098` und `:1102` (`worker_main "$j" … || rc=$?`), Wirkung auf
  `mutate.sh:733-806` (`worker_main`) und `:339-475` (`run_case`, gerufen aus `:789`)
- **Befund:** Bash setzt `set -e` für die gesamte Ausführung einer Funktion aus, die in einem
  getesteten Kontext steht — und die Aussetzung reicht in verschachtelte Funktionsaufrufe und
  `{ }`-Gruppen hinein. Beides mit eigenen Sonden gemessen: ein `false` im Rumpf einer direkt
  gerufenen Funktion bricht ab, dasselbe `false` bei Aufruf als `f || rc=$?` nicht — auch nicht
  über zwei Funktionsebenen und eine `{ }`-Gruppe hinweg. Der Diff ruft `worker_main`
  ausschließlich in dieser Form, also läuft der komplette Worker-Rumpf **einschließlich
  `run_case`** ohne `-e`. Vor dem Diff stand `run_case "$c"` in `main`s Schleife und `main "$@"`
  ungetestet am Dateiende — dort war `-e` aktiv. Am echten Treiber nachgestellt: mit
  schreibgeschütztem `$ISO_ROOT/w7` scheitert `mkdir -p "$TMPDIR"` (`mutate.sh:747`) mit
  „Keine Berechtigung", der Worker **läuft darüber hinweg**, setzt seinen `trap`, zieht seinen
  Fall aus der Warteschlange und schreibt `draws.7`; erst der Folgefehler in `green_prerun`
  beendet ihn. Die zwei Ketten, die ich weiterverfolgt habe, enden heute noch rot
  (`prepare_prerun_log` bzw. der Schreibversuch auf `/verify.log` bei leerem `$BACKUP`) — der
  **Schutz** ist damit nicht mehr `set -e`, sondern der Umstand, dass ein Folgeschritt scheitert.
  Zeile 97 und der „FAIL-CLOSED"-Block des Kopfes sagen etwas anderes zu.
- **Verifizierbar:** ja — `worker_main 7 '' light` über einer Kopie mit `chmod a-w` auf
  `$ISO_ROOT/w7` gegen einen `make`-Stub laufen lassen: der Worker passiert das gescheiterte
  `mkdir -p` und legt `draws.7` an.

### F-3 — Der Plan verlangt den Abgleich von Modell und Messung **vor** der Teilung; die Messung widerlegt die Grundlage der Frage-A-Antwort, und der Diff hält das nirgends fest

- **Kategorie:** MEDIUM
- **Quelle:** Plan §1 (*„wer sie baut, prüft zuerst, ob die gemessene Verteilung mit dieser
  modellierten übereinstimmt; tut sie es nicht, ist eine der beiden falsch, und das ist ein
  Befund vor jeder Teilung"*) und §3 (dieselbe Auflage) + `AGENTS.md` §3.6
- **Pfad:** Plan §1 (Tabelle „Zeit-Verteilung" und der Absatz „Vier von 176 Fällen tragen ein
  Viertel der Zeit") gegen `harness/tools/mutate.sh:621-636` (`mode_rank`) und die
  `report_times`-Ausgabe des Laufs
- **Befund:** Der Plan modelliert `test-go` 51 %, `full-smoke` 25 %, `test-bats` 23 % und stützt
  seine Antwort auf Frage A auf den Satz *„ein `full-smoke`-Fall kostet so viel wie sechzehn
  `test-go`-Fälle"*. Aus dem Lauf-Protokoll gezogen: `test-go` **64,0 %**, `test-bats` **31,0 %**,
  `full-smoke` **4,8 %**; die Mittel sind `full-smoke` **15,09 s** gegen `test-go` **8,93 s** —
  Faktor **1,7**, nicht 16. Der Grund ist im Treiber-Kopf bereits benannt (ein mutierter Lauf
  bricht am getroffenen Wächter ab), das Modell rechnete mit dem Preis eines **grünen** Laufs.
  Zweitens trägt `mode_rank` in ihrem eigenen Kommentar die Begründung *„Gleiche Modi liegen dabei
  von selbst beieinander, weil die Kosten je SENSOR konstant sind"*; gemessen streuen die Kosten
  **innerhalb** von `full-smoke` von 1,95 s bis 55,31 s = **28,4×** (`test-go` 2,2×, `test-bats`
  1,3×). Der Halbsatz, der „nach Stückpreis sortieren = nach Sensor gruppieren" trägt, ist damit
  für den teuersten Modus falsch. Die Auswirkung ist heute gering (die 6 `full-smoke`-Fälle liegen
  in der seriellen Spur), der **verlangte Abgleich** ist aber unterblieben: der Plan wurde vom
  Commit nicht angefasst (`git log --oneline -- docs/plan/planning/*/slice-105-*.md` zeigt seit
  dem `in-progress`-Move keine Inhaltsänderung), und weder Commit-Message noch Code halten die
  Abweichung fest. Der **andere** Abgleich, den §3 verlangt (statische gegen Laufzeit-Zuordnung
  der Sensoren), trägt dagegen — s. Negativbefunde.
- **Verifizierbar:** ja — `sed -n '/Zeit je Sensor/,/Gruen-Vorlaeufe/p'` über einem
  `make mutate`-Protokoll gegen Plan §1 Tabelle „Zeit-Verteilung"; und
  `sed -n '/Zeit je Fall, absteigend/,/untere Schranke/p' | grep 'full-smoke$'` gegen die
  „Kosten je Sensor konstant"-Begründung in `mutate.sh:621-636`.

### F-4 — Frage B und Frage C sind nirgends im Plan beantwortet; die Begründungen liegen im Makefile-Kommentar, den der Closure-Trigger nicht meint

- **Kategorie:** MEDIUM
- **Quelle:** Plan §5 Closure-Trigger (*„Frage A, B und C sind mit ihrer Begründung **im Plan**
  beantwortet"*)
- **Pfad:** Plan §3 Fragen-Tabelle (Zeilen zu B und C) gegen `Makefile:121-129` und
  `harness/tools/mutate.sh:117-124`
- **Befund:** `grep -n 'Frage A\|Frage B\|Frage C' docs/plan/planning/in-progress/slice-105-*.md`
  findet Frage A einmal **beantwortet** (§1, Zeile 101: *„Damit ist Frage A entschieden …"*) und
  B und C nur an ihrer **Frage**-Stelle (§3, Zeilen 384 und 367) — kein Antwortsatz an irgendeiner
  Stelle der Datei. Entschieden sind sie im Code: B über `JOBS="${MUTATE_JOBS:-4}"`
  (`mutate.sh:124`) plus den Durchreicher `MUTATE_JOBS='$(MUTATE_JOBS)'` (`Makefile:129`), C über
  den Makefile-Kommentar *„die CI ruft `make mutate` bar … sie fährt damit genau dieselbe
  Aufteilung wie ein lokaler Lauf ohne Vorgabe"* — nachgeprüft: `.github/workflows/ci.yml:72` ist
  `- run: make mutate`, ohne Vorgabe, also `JOBS=4`. Beide Antworten sind sachlich tragfähig; sie
  stehen nur nicht dort, wo der Closure-Trigger sie verlangt, und der Plan bleibt ein Dokument,
  das drei offene Fragen führt, während zwei davon entschieden sind. Für C kommt hinzu, dass die
  Frage ausdrücklich die **Umgebung** adressiert (*„Vier Runner mit je einem Kern verhalten sich
  anders als ein Host"*) und die feste Vorgabe 4 auf keinem CI-Runner gemessen wurde — der
  `runs-on: ubuntu-24.04`-Job fährt seit diesem Commit vier gleichzeitige Worker, ohne dass eine
  Messung dazu existiert.
- **Verifizierbar:** ja — rein textuell: `grep -n 'Frage [ABC]' docs/plan/planning/in-progress/slice-105-*.md`
  gegen `git log --oneline -- docs/plan/planning/*/slice-105-*.md` (nur Move-Commits).

### F-5 — Der `prepare_isolation`-Kommentar sagt weiterhin „EINMAL pro Lauf", während derselbe Diff sie in eine `JOBS`-Schleife setzt — und dieselbe Aussage 130 Zeilen darüber korrekt gezogen hat

- **Kategorie:** MEDIUM
- **Quelle:** `AGENTS.md` §3.7 (ein Kommentar beschreibt, was da ist)
- **Pfad:** `harness/tools/mutate.sh:203` gegen `harness/tools/mutate.sh:1069-1074`;
  zweite Stelle `harness/tools/mutate.sh:103`
- **Befund:** Zeile 203 lautet unverändert *„prepare_isolation kopiert den Host-Baum EINMAL pro
  Lauf an diesen Ort."* Der Diff ruft die Funktion in
  `for ((j = 1; j <= JOBS; j++)); do … WORK="$(prepare_isolation "$ISO_ROOT/w$j")"; done`
  (Zeilen 1069-1074), im Default also viermal — das Lauf-Protokoll bestätigt es mit
  `mutate: 4 isolierte Kopie(n)`. Dass es sich um ein Übersehen und nicht um eine Bestands-Frage
  handelt, ist am Diff selbst ablesbar: er hat **dieselbe** Formulierung im Datei-Kopf gezogen —
  `git show 82d2ed7 -- harness/tools/mutate.sh | grep -nE '^[-+].*EINMAL pro Lauf'` zeigt genau
  eine `-`-Zeile (*„# ISOLATION: der Baum wird EINMAL pro Lauf …"*, ersetzt durch *„JE WORKER"*)
  und keine `+`-Zeile. Eine Fundstelle gezogen, die zweite stehen gelassen. Zeile 103 trägt
  dieselbe Klasse: *„WORK … Bis prepare_isolation() sie anlegt, ist er leer"* — im Worker wird
  `WORK` jetzt direkt gesetzt (`mutate.sh:736`, `WORK="$ISO_ROOT/w$id/repo"`), ohne dass
  `prepare_isolation` daran beteiligt wäre. Die Stelle beschreibt die tragende
  Isolations-Mechanik; wer sie ändert, liest sie zuerst.
- **Verifizierbar:** ja — `grep -n 'EINMAL pro Lauf' harness/tools/mutate.sh` (→ 203) gegen
  `sed -n '1069,1074p' harness/tools/mutate.sh`; und
  `git show 82d2ed7 -- harness/tools/mutate.sh | grep -nE '^[-+].*EINMAL pro Lauf'`.

### F-6 — `harness/README.md` sagt „jeder Worker fährt den Grün-Vorlauf **jedes** Modus in seiner Kopie"; gemessen fährt jeder Worker nur die Modi, die er zieht

- **Kategorie:** MEDIUM
- **Quelle:** `AGENTS.md` §3.7 + [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)
  (der Satz ist die **Begründung** dafür, warum die Worker-Zahl keine Verdikt-Stellschraube ist)
- **Pfad:** `harness/README.md:70` (Nicht-Gate-Verify-Absatz, neuer Satz) gegen
  `harness/tools/mutate.sh:769-784` (fauler Vorlauf in `worker_main`)
- **Befund:** Der Treiber-Kopf formuliert die Eigenschaft korrekt und mit dem tragenden Zusatz:
  *„jeder Worker faehrt den Gruen-Vorlauf jedes Modus in SEINER Kopie (green_prerun, **faul**)"*
  (`mutate.sh:89-92`), und der Code setzt genau das um — der Vorlauf läuft erst, wenn der Worker
  den ersten Fall dieses Modus zieht (`if [ "$mode" != "-" ] && ! grep -qF " $mode " <<<" $seen "`).
  Der README-Satz übernimmt die Formulierung **ohne** „faul" und behauptet damit eine stärkere
  Eigenschaft, als der Code hält. Aus dem Lauf-Protokoll gezogen
  (`sed -n '/Gruen-Vorlaeufe/,/Zeit je Fall/p' | awk 'NF==3'`): **9** Vorlauf-Einträge über 4
  Worker und 5 Modi — Worker 1 hat ausschließlich `full-smoke` und `smoke`, kein `test-bats`;
  Worker 2 und 3 haben kein `ci-lint`. Kein einziger Worker hat den Vorlauf **jedes** Modus
  gefahren. Die wahre, schwächere Aussage („jeder Worker belegt jeden Modus, den er fährt, in
  seiner eigenen Kopie") trägt die `LH-QA-02`-Folgerung genauso — sie steht nur nicht im README.
- **Verifizierbar:** ja — `sed -n '/Gruen-Vorlaeufe/,/Zeit je Fall/p' <mutate-log> | awk 'NF==3'`
  gegen den Satz in `harness/README.md:70`.

### F-7 — `mode_rank` benennt `report_times` als ihren Sensor; dessen erste echte Lesung widerspricht ihr, und die Funktion wurde nicht nachgezogen

- **Kategorie:** LOW
- **Quelle:** `AGENTS.md` §3.6 (eine Zusage, die ihren Sensor nennt, ist an dessen Lesung
  gebunden)
- **Pfad:** `harness/tools/mutate.sh:621-625` (*„Weicht die dortige Reihenfolge von dieser ab,
  gehoert diese Funktion nachgezogen"*) gegen `harness/tools/mutate.sh:626-636`
- **Befund:** `mode_rank` ordnet `full-smoke`(1) · `test`(2) · `smoke`(3) · `test-bats`(4) ·
  `test-go`(5) · `ci-lint`(6). Gemessene Mittel aus dem Lauf: `full-smoke` 15,09 · `test-bats`
  12,58 · `test-go` 8,93 · `smoke` 2,30 · `ci-lint` 0,65 — `smoke` steht im Rang **vor**
  `test-bats` und `test-go`, gemessen aber dahinter. Die Zusage im Kommentar ist damit beim ersten
  echten Lauf ausgelöst und nicht eingelöst. Folgenlos ist es heute, weil `smoke` in der seriellen
  Spur liegt und dort nur gegen `full-smoke` sortiert wird (Rang 1 vor 3 = auch gemessen richtig)
  — aber die Zusage liest kein Gate, sie hängt allein am nächsten Leser.
- **Verifizierbar:** ja — die `mittel=`-Spalte aus `report_times` gegen die Reihenfolge in
  `mode_rank` halten; kein Sensor tut das.

### F-8 — „8,0 MB" ist die einzige Zahl, die der Treiber je zur Isolation nannte; der Diff vervierfacht den Gegenstand und lässt die Zahl stehen

- **Kategorie:** LOW
- **Quelle:** `AGENTS.md` §3.7 (Geltungsbereich: Skripte dieses Repos) — der Zahlen-Teil von
  [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  greift **nicht**, sein Geltungsbereich sind lebende Markdown-Artefakte (selbst nachgelesen)
- **Pfad:** `harness/tools/mutate.sh:208` (Satzende *„… ist im Repo ohnehin die
  Sicherungs-Mechanik (LH-QA-03). 8,0 MB."*)
- **Befund:** Eigen gemessen an den Kopien des laufenden Sensors:
  `du -sh /tmp/tmp.N3FNeHi39I/w*/repo` → **50 M** je Kopie, `du -sh /tmp/tmp.N3FNeHi39I` →
  **201 M** je Lauf; `du -sh --exclude=.harness/state /Development/KI/ai-harness-init` → **50 M**,
  davon `du -sh .git` → **39 M**. Die Zahl war schon vor diesem Diff um Faktor ~6 veraltet — das
  fällt unter den §3.7-Cutoff („der Bestand ist kein Arbeitsauftrag"). Neu ist, dass der Diff die
  Ressource, über die sie spricht, **vervierfacht**: aus einer Kopie werden `JOBS`. Die Zahl steht
  ohne Kommando im Text und ohne Hinweis, dass sie eine Kopie meint und nicht den Lauf; sie ist
  damit die einzige Größenangabe im Treiber und in beide Richtungen irreführend.
- **Verifizierbar:** ja — `du -sh --exclude=.harness/state <repo>` gegen die Zahl in Zeile 208;
  je Lauf mal `JOBS`.

### F-9 — Der Diff setzt neue Entstehungs-Prosa über einen Entwurf, den dieses Repo nie getragen hat, und eine neue, nicht auflösbare Befund-Kennung

- **Kategorie:** LOW
- **Quelle:** `AGENTS.md` §3.7 (zweite Falsch-Klasse: *„beschreibt abwesenden Text … die vorige
  hält `git`"*; Herkunft gehört in **ein** auflösbares Feld)
- **Pfad:** `harness/tools/mutate.sh:655-660`, `test/mutate-driver.bats` (zwei Stellen im neuen
  Abschnitt „Die serielle Spur"); `harness/tools/mutate.sh:740` (`(Review F-1)`)
- **Befund:** Drei neue Stellen erzählen die Geschichte eines Vorgängers: *„Genau daran ist der
  erste Entwurf zerbrochen: er suchte `artifact-copy.sh` eine Ebene tiefer über
  `^[[:space:]]*make ` …"*, *„Der Vorgaenger dieses Kriteriums las die Treiberskripte nach
  `make <ziel>`-Zeilen ab …"* und *„Genau diese Gestalt-Unabhaengigkeit fehlte dem Vorgaenger."*
  Dieser Vorgänger existiert in der Historie dieses Repos nicht:
  `git log --oneline -S 'reads_back_tag' -- .` → **0** Treffer; er liegt allein auf dem lokalen
  Branch `prototype/mutate-parallel` (`git branch --list 'prototype/*'`). §3.7s Entlastung
  („die vorige hält `git`") greift hier also gerade nicht — der Kommentar ist die einzige Ablage,
  und er wird von jedem Lauf mitgelesen. Die tragende **Abgrenzung** steht bereits im Satz davor
  (*„DAS KRITERIUM IST DIE DELEGATION, NICHT DIE ZEILENFORM"*) und kommt ohne die Erzählung aus.
  Daneben fügt der Diff genau eine neue Befund-Kennung in einen Kommentar ein (`mutate.sh:740`,
  *„… liesse SEINE Seds im cwd des Treibers laufen (Review F-1)"*) — „Review F-1" nennt keine
  Runde und keinen Report und ist damit nicht auflösbar; §3.7 verlangt für Herkunft **ein**
  auflösbares Feld (`LH-*`, `ADR-*`). Der Cutoff schützt den Bestand, nicht die neue Zeile.
- **Verifizierbar:** ja —
  `git show 82d2ed7 | grep -E '^\+' | grep -icE 'erste[rn]? Entwurf|Vorgaenger'` → 3;
  `git log --oneline -S 'reads_back_tag' -- . | wc -l` → 0;
  `git show 82d2ed7 | grep -E '^\+' | grep -c 'Review F-1'` → 1.

### F-10 — Drei Ungenauigkeiten in der Ausgabe des Laufs selbst

- **Kategorie:** LOW
- **Quelle:** Plan §2 DoD (1) (*„er nennt seinen Nenner"*) + `AGENTS.md` §3.7
- **Pfad:** `harness/tools/mutate.sh:920` und `:925` (`anteil=`), `:1085-1090` gegen `:927-930`
  (Vorwärm-Lauf), `:1092` (Kopfzeile)
- **Befund:** (a) `anteil=%5.1f%%` rechnet gegen `g` = Summe der **Fall-Dauern**; die Kopfzeile
  darüber nennt als Nenner nur die Fall-**Zahl** („ueber 188 von 188 Fall-Dateien"). Die Zeitbasis
  steht erst am Ende der Ausgabe („Fall-Arbeit gesamt 1868.7 s"), gegen eine Wanduhr von 526,26 s
  — wer `anteil=64,0 %` als Anteil am Lauf liest, liegt um Faktor 3,5 daneben. (b) Der
  Vorwärm-Lauf (`green_prerun test-go` in `main`, Zeile 1088) taucht in „Gruen-Vorlaeufe (Worker,
  Modus, Sekunden) — der **Preis der Aufteilung**" nicht auf: die Tabelle speist sich aus
  `prerun.times.*`, die nur `worker_main` schreibt. Gemessen 9 Einträge über 4 Worker; Worker 1
  hat **keinen** `test-go`-Eintrag, obwohl sein Vorlauf gefahren wurde — er ist der Vorwärm-Lauf,
  und er existiert ausweislich seines eigenen Kommentars genau deshalb, weil es mehrere Worker
  gibt. (c) Zeile 1092 meldet „dynamisch aus **einer** gemeinsamen Warteschlange", während Zeile 1
  desselben Protokolls „serielle Spur fuer: smoke full-smoke" sagt und der Code zwei Schlangen
  anlegt, von denen eine nur Worker 1 leert.
- **Verifizierbar:** ja — Summe der `summe=`-Spalte (1868,7 s) gegen `MUTATE_SECONDS` (526,26 s);
  `sed -n '/Gruen-Vorlaeufe/,/Zeit je Fall/p' | awk 'NF==3 && $1==1'` gegen `mutate.sh:1088`.

### F-11 — Die Commit-Message nennt „13 neue bats-Faelle"; es sind 14

- **Kategorie:** LOW
- **Quelle:** `AGENTS.md` §3.6 (eine Commit-Message ist eine Zusage) +
  [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  §Begründung (Commit-Messages sind als Fundort ausdrücklich genannt)
- **Pfad:** Commit-Message `82d2ed7`, vorletzter Absatz
- **Befund:** `git show 82d2ed7 -- test/mutate-driver.bats | grep -c '^+@test'` → **14**;
  gegengeprüft über den Bestand: `grep -c '^@test' test/mutate-driver.bats` → 35 gegen
  `git show 82d2ed7^:test/mutate-driver.bats | grep -c '^@test'` → 21, Differenz ebenfalls 14.
  Die beiden anderen Zahlen desselben Absatzes tragen („Fuenf Faelle 191-195":
  `ls test/mutations/19[1-5]*.sh | wc -l` → 5; „Bestand 188":
  `ls -1 test/mutations/*.sh | wc -l` → 188). Die Message ist immutabel; der Befund gehört in die
  Closure-Notiz, nicht in einen Rewrite.
- **Verifizierbar:** ja — die zwei Kommandos oben.

---

## Negativbefunde

- **geprüft, ohne Befund — alle fünf neuen Mutationen greifen und färben genau ihren benannten
  Wächter rot.** Je Fall eine frische Kopie von `harness/tools`+`test` per `tar`, Mutation
  angewandt, `diff` gegen das Original: jede trifft genau die beabsichtigte Zeile (191 → `:915`
  `-ne`→`-gt`; 192 → `:431` **und** `:776` `abort_run`→`:`; 193 → `:848` `0`→`999999`;
  194 → `:702` `return 1`→`continue`; 195 → `:683-684`, beide Abwehrlinien). Anschließend
  `docker run --rm --network none … bats/bats@sha256:e8f1… test/mutate-driver.bats` über der
  mutierten Kopie: **je genau ein zusätzliches `not ok`**, und zwar der im `# expect:` benannte
  Fall (33 / 34 / 29 / 23 / 35). Die Baseline derselben Kopie trägt genau **ein** vorbestehendes
  `not ok 7` („die Kopie traegt den Sensor-Bedarf inklusive .git") — ein Artefakt meiner Methode
  (mein Ausschnitt enthält kein `.git`), nicht der Mutationen. Das ist der §3.6-Nachweis für die
  fünf neuen Zähne, unabhängig vom `make mutate`-Lauf erbracht.
- **geprüft, ohne Befund — der Plan-§3-Abgleich „statische gegen Laufzeit-Sensor-Zuordnung"
  trägt.** Die statische Zuordnung mit dem im Plan beschriebenen Rezept nachgebaut (je Fall der
  eigene `# verify:`-Kopf, sonst `narrow_sensor` über `# expect:`): **134** `test-go` · **46**
  `test-bats` · **6** `full-smoke` · **1** `smoke` · **1** `ci-lint`. Die `n=`-Spalte der
  `report_times`-Ausgabe des Laufs nennt dieselben fünf Zahlen. Von den zwei Abgleichen, die der
  Plan vor der Teilung verlangt, hält dieser; der andere ist F-3.
- **geprüft, ohne Befund — die serielle Spur trifft heute genau die zwei gefährlichen Modi, und
  das Kriterium ist gestalt-unabhängig.** Eigene Trockenläufe je Modus
  (`env -u MAKEFLAGS -u MAKELEVEL -u MFLAGS make --no-print-directory -n <m>`): `test`,
  `test-go`, `test-bats` und `ci-lint` bestehen ausschließlich aus `docker build`/`docker run`;
  `smoke` und `full-smoke` biegen in `bash harness/tools/*.sh` ab. Das Lauf-Protokoll meldet in
  Zeile 1 genau `serielle Spur fuer: smoke full-smoke`. `harness/tools/artifact-copy.sh` — die
  Stelle, die das Binär mit `docker create`/`docker cp` **aus** dem Tag holt — hängt an
  `make artifact`, das nur über diese zwei Skripte erreichbar ist. Kein leichter Modus baut den
  Tag `ai-harness-init:build`; die leichten Modi taggen `ai-harness-init:test` bzw. ziehen ein
  digest-gepinntes Fremdbild.
- **geprüft, ohne Befund — die Klassifikation ist gegen den heutigen Fall-Bestand robust, und die
  im Kommentar benannte Grenze ist eine gemessene Aussage.** `plan_self_contained` misst einmal je
  Lauf über dem Host-Baum; die Grenze steht als solche im Kommentar (`mutate.sh:648-652`,
  *„Kein Fall in test/mutations/ tut das"*). Die Mengenaussage nachgemessen:
  `grep -l '^# files:.*Makefile' test/mutations/*.sh | wc -l` → **4** (78–81); alle vier treffen
  ausschließlich die Rezepte von `release-artifacts` bzw. `build` — kein `# verify:`-Modus ändert
  dadurch seinen Plan. Die Grenze ist damit **benannt und heute leer**, nicht nur benannt. Ein
  Wächter dagegen existiert nicht; das ist im Kommentar ebenfalls gesagt („wer den ersten
  schreibt, faellt hierunter").
- **geprüft, ohne Befund — §3.5: die Teilung ist keine Schwellen-Senkung.** Vorher fuhr
  `green_prerun` **einmal** je Modus in **einer** Kopie
  (`git show 82d2ed7^:harness/tools/mutate.sh:531-538`: `mode_list=(test)` plus die expliziten
  `# verify:`-Modi), danach liefen alle Fälle in derselben Kopie mit `restore` dazwischen. Jetzt
  belegt jeder Worker jeden Modus, den er zieht, in **seiner** Kopie, bevor dessen erster Fall
  läuft — gemessen 9 Worker-Vorläufe plus den Vorwärm-Lauf gegen zuvor 4 Modus-Läufe. Jede
  Mutation ist damit weiterhin von einem grün gesehenen Vorlauf desselben Sensors in derselben
  Kopie gedeckt; die Aussage des Vorlaufs wird nicht über Kopien hinweg geglaubt. Plan §4s
  Rückführung `in-progress` → `open` (ADR-pflichtige Senkung) hat keinen Gegenstand.
- **geprüft, ohne Befund — §3.8: der Commit fasst kein Architect-Artefakt an.**
  `git show --pretty=format: --name-only 82d2ed7 | grep -cE '^(AGENTS|harness/conventions)\.md$|^docs/plan/adr/'`
  → **0**. Dass `AGENTS.md` trotz Plan-§3-Zeile („update") unberührt bleibt, ist sachlich richtig
  und kein Übersehen: die Plan-Prämisse („beide beschreiben `make mutate` als **einen** Lauf gegen
  **eine** isolierte Kopie") trifft auf `AGENTS.md` nicht zu — `grep -n 'isoliert' AGENTS.md` →
  leer, und §4 verweist die Ziel-Beschreibungen ausdrücklich an `harness/README.md` („Eine Aussage
  hat einen Ort"). Der Diff weicht hier von der Plan-Tabelle ab und hat recht damit.
- **geprüft, ohne Befund — §3.2 und §3.3.**
  `git show 82d2ed7 | grep -cE '^\+.*(nolint|shellcheck disable)'` → **0**;
  `git show --stat -M 82d2ed7 | grep -c '=>'` → **0** (kein Rename im Commit, die Regel greift
  nicht). `make shell-lint` grün über allen fünf neuen `test/mutations/*.sh` — sie liegen im
  Prüfbereich des Targets.
- **geprüft, ohne Befund — §3.9 in meinem eigenen Lauf.** Nur `make`-Ziele, `git`, `grep`, `sed`,
  `awk`, `tar`, `du`, `bash` und `docker run` mit exakt den digest-gepinnten Images, die das
  Makefile selbst nennt. Keine Host-Toolchain und kein Host-Paketmanager in Befehlsposition.
  `make mutate` habe ich nicht gestartet (Auflage 1); die Zahlen aus dem fremden Lauf sind als
  fremd ausgewiesen.
- **geprüft, ohne Befund — §3.1: kein halluziniertes Gate hinzugekommen.** Der Diff fügt kein
  `make`-Ziel hinzu (`git show 82d2ed7 -- Makefile | grep -cE '^\+[a-z-]+:'` → 0); `mutate` bleibt
  außerhalb von `make gates`, und `harness/README.md` führt es weiterhin unter
  „Nicht-Gate-Verify". Die neue Zeit-Aufschlüsselung ist im Code selbst als Messung markiert
  („MESSUNG, kein Urteil ueber einen Waechter … ein Schwellenwert darueber waere rot ohne Befund
  und gruen ohne Deckung (LH-QA-01)") und färbt nur ihre eigene Vollständigkeit — konsistent mit
  Plan §5 („Ausdrücklich nicht Teil des Closure-Triggers: eine Laufzeit-Schwelle").
- **geprüft, ohne Befund — der Mutex ist portabel und ohne Fremdabhängigkeit
  ([`LH-QA-03`](../../spec/lastenheft.md#lh-qa-03--minimale-abhängigkeiten)).** `mkdir`/`rmdir`
  statt `flock`, `awk` statt Gleitkomma-Arithmetik, keine neuen Fremdwerkzeuge im Diff
  (`git show 82d2ed7 | grep -cE '^\+.*(jq |python|node )'` → 0). `queue_take` gibt den Mutex frei,
  **bevor** der Fall läuft (`rmdir` in `mutate.sh:589`, vor dem `printf` des Eintrags); die
  kritische Sektion umfasst drei Kommandos.
- **geprüft, ohne Befund — der Timeout am Warteschlangen-Mutex ist fail-closed, und die
  Bemessungs-Begründung widerspricht sich nicht.** `QUEUE_LOCK_TRIES=6000` × `sleep 0.05` ≈ 300 s;
  stirbt ein Halter, kehrt jeder Wartende mit Status 2 zurück, `worker_main` reicht ihn durch
  (`*) return "$rc" ;;`), die `wait`-Achse in `main` macht daraus einen Befund. Die Meldung leitet
  ihre Sekundenzahl aus dem Versuchszähler ab (`$((tries / 20))`) und ist damit eine
  **Untergrenze** der echten Wartezeit — sie sagt selbst, dass die Zeit gemessen ist und nicht ihr
  Grund. Kein Finding; erwähnenswert bleibt der Preis: bei einem toten Halter stehen `JOBS-1`
  Worker fünf Minuten, bevor der Lauf rot wird.
- **geprüft, ohne Befund — der Weg aus `run_case` bei Isolations-Bruch stimmt.** `abort_run` steht
  **vor** dem eigenen `exit 1` (`mutate.sh:430-437`), das `exit` verlässt die Worker-Subshell,
  bevor sie ihre `worker.$j.done`-Marke schreibt, und `main` unterscheidet in der `wait`-Schleife
  genau diese zwei Fälle mit zwei verschiedenen Meldungen. Die Abbruch-Flagge wird von
  `queue_take` vor jedem Zug gelesen (Status 3), so dass die übrigen Worker nicht weiterziehen —
  vom bats-Fall „nach abort_run zieht kein Worker mehr einen Fall" gedeckt, und
  `test/mutations/192` ist der Zahn dazu (von mir rot gesehen).
- **geprüft, ohne Befund — `is_heavy_mode`/`used_modes` haben kein Teilstring-Problem.** Beide
  arbeiten über raumbegrenzte Listen (`grep -qF " $1 "` gegen `" smoke full-smoke "`);
  `" smoke "` trifft `"full-smoke"` nicht, weil dessen Präfix ein `-` und kein Leerzeichen ist.
  Das Lauf-Protokoll bestätigt beide Modi getrennt in der Spur-Zeile.
- **geprüft, ohne Befund — `MUTATE_JOBS` ist fail-closed und der Default-Durchgriff stimmt.**
  `Makefile:129` reicht `MUTATE_JOBS='$(MUTATE_JOBS)'` durch, bei nicht gesetzter Variable also
  den **leeren** String; `JOBS="${MUTATE_JOBS:-4}"` (`:-`, nicht `-`) fängt das und liefert 4.
  Nicht-numerische oder leere Vorgaben laufen in den `case`-Zweig `JOBS=0` und dort in einen
  Abbruch mit eigener Meldung, **bevor** irgendetwas kopiert wird — nicht erst in der
  Zusammenführung. `.github/workflows/ci.yml:72` ruft `make mutate` bar, die CI fährt also
  denselben Default; die Aussage des Makefile-Kommentars dazu trägt.
- **geprüft, ohne Befund — `comment-claims` ist grün, sagt hier aber nichts.**
  `make comment-claims` → 46 Datei(en), 0 Befund(e). Das Gate prüft, ob eine
  Kommentar-**Behauptung** ihren Sensor nennt und ob der genannte Test existiert — nicht,
  **worüber** ein Kommentar spricht; `AGENTS.md` §3.7 sagt das selbst („Ein Wächter existiert
  nicht"). F-5, F-6, F-8, F-9 und F-10 liegen strukturell außerhalb seines Prüfbereichs bzw.
  seiner Frage; sein Grün widerlegt keinen davon.
- **geprüft, ohne Befund — kein Residuum, keine Doku-Drift im Gate-Sinn.**
  `git status --porcelain=v1` vor und nach meinen Sonden leer (alle Kopien lagen unter dem
  Scratchpad und sind entfernt); `make docs-check` grün (411 Datei(en), 0 Befund(e));
  `make test-bats` grün (176 ok, 0 not ok). `grep -n 'mutate' harness/README.md` findet genau zwei
  Stellen; die relevante ist gezogen.

---

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 1 (F-1) |
| MEDIUM | 5 (F-2, F-3, F-4, F-5, F-6) |
| LOW | 5 (F-7, F-8, F-9, F-10, F-11) |
| INFO | 0 |

## Verdikt

**Nicht formal frei — ein HIGH und fünf MEDIUM vor Merge/Closure zu klären.**

Die Kernmechanik ist geprüft und trägt: die Spur-Zuordnung ist gestalt-unabhängig fail-closed
(eigene Trockenläufe je Modus decken sich mit der Spur-Zeile des Laufs), die zwei Modi, deren
Urteil an einem geteilten Tag hängt, laufen nachweislich seriell, jeder Worker belegt jeden von
ihm gefahrenen Sensor in seiner eigenen Kopie, und alle fünf neuen Mutationen färben genau ihren
benannten Wächter rot — unabhängig vom `make mutate`-Lauf nachgestellt. §3.1, §3.2, §3.3, §3.5,
§3.8 und §3.9 sind eingehalten; die Teilung ist keine Schwellen-Senkung und braucht kein ADR.

Der **HIGH** (F-1) ist kein Defekt der Aufteilung, sondern ihrer Buchführung: die drei
Vollständigkeits-Achsen und die Zeit-Bilanz messen die **Existenz von Dateien**, nicht die
**Lesbarkeit von Urteilen**, und die Auswertung ist an dieser Stelle fail-open — eine leere oder
abgeschnittene Statusdatei wird als OK gezählt, während derselbe Lauf „Vollstaendigkeit … jede
Fall-ID genau einmal gezogen" bestätigt und `report_times` eine Bilanz über einer Teilmenge unter
einem Vollständigkeits-Kopf ausgibt. Das ist genau die Klasse, die Plan §1 als nicht verhandelbar
führt; der Auslöser (Schreibfehler ohne Prozess-Tod) ist schmal, die Richtung des Fehlers ist es
nicht. F-2 ist die strukturelle Entsprechung: der Diff hat `set -e` aus dem gesamten Worker-Rumpf
und aus `run_case` genommen, ohne dass das irgendwo steht — heute fangen Folgefehler die zwei
Ketten, die ich verfolgt habe, aber Zeile 97 sagt weiter etwas anderes zu.

F-3 und F-4 sind Plan-Rückkopplungen, die der Commit schuldig bleibt: der ausdrücklich verlangte
Abgleich Modell/Messung ist unterblieben, obwohl die Messung die Grundlage der Frage-A-Antwort
widerlegt (16× → 1,7×; „Preis je Sensor konstant" → 28,4× Streuung innerhalb `full-smoke`), und
Frage B und C sind im Code entschieden, aber nicht dort beantwortet, wo der Closure-Trigger es
verlangt. F-5 und F-6 sind Kommentar- und Doku-Aussagen, die der Diff selbst falsch gemacht hat —
bei F-5 hat derselbe Diff dieselbe Formulierung 130 Zeilen weiter oben korrekt gezogen und die
zweite Fundstelle übersehen.

Die fünf LOW sind Präzisions- und Buchführungsposten (Rang-Zusage, veraltete Größenangabe,
Entstehungs-Prosa, Ausgabe-Nenner, eine Zahl in der Commit-Message) und blockieren nichts.
