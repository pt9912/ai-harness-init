# Code-Review — slice-021 (d-check-Pin v0.50.0 → v0.51.1, `sources` verfügbar)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths`) — bare IDs/Pfade hier ok.

## Kopf-Metadaten

- **Gegenstand:** slice-021 — d-check-Pin **v0.50.0 → v0.51.1** (macht das opt-in-Modul
  `sources`, das 19. Modul, *verfügbar*; aktiviert es NICHT). `d-check.mk` neu aus
  `--print-mk`, Emitter-Pin nachgezogen, neuer MR-012, §Baseline auf v0.51.1.
- **Diff/Commit-Range:** `git show c0e4242` (ein Content-Commit). Vorangehend `786b899`
  (reiner Move open→in-progress). HEAD = `c0e4242`, Arbeitskopie clean.
- **Betroffene Dateien (c0e4242):** `d-check.mk`, `internal/emit/emit.go`,
  `harness/conventions.md`. `.d-check.yml` bewusst NICHT berührt (Plan §3).
- **Rolle:** unabhängiger Reviewer (Modul 10), frischer Kontext — Code nicht selbst geschrieben.
- **Pflicht-Kontext (v3.5.0):** Diff-Commit · `LH-QA-01`/`LH-QA-02` (Bezug im Slice) ·
  aktive ADRs (keine referenziert/berührt) · Hard Rules AGENTS.md §3 · vorherige Findings am
  gleichen Modul (Doc-Gate/d-check-Pin: slice-015 INFO-1, MR-009/010/011) · Slice-Plan
  `docs/plan/planning/in-progress/slice-021-dcheck-pin-v0511.md`. **Nicht** erhalten: DoD-Abhakung
  (Verifikations-Territorium, Modul 11).
- **Ausgeführt (belegt, nicht nur nachgelesen):**
  - `docker buildx imagetools inspect ghcr.io/pt9912/d-check:v0.51.1` → Digest
    `sha256:fede3d02…f7a2d03c` (Registry-Beleg).
  - `docker images --digests` → lokaler RepoDigest v0.51.1 identisch `sha256:fede3d02…f7a2d03c`.
  - `docker run --rm --network none …@sha256:fede3d02… --print-mk` → Regenerations-Vergleich
    gegen committetes `d-check.mk`.
  - `make docs-check` (v0.51.1, netzlos) → **69 Datei(en), 0 Befund(e), Exit 0**.
  - `make docs-check DCHECK_DIGEST=sha256:9b80…94364` (v0.50.0-Override, netzlos) → **69 Datei(en),
    0 Befund(e), Exit 0** (Trockenlauf-Diff unabhängig reproduziert).
  - `make test` → grün (`internal/emit` inkl. `TestDefault…_MatchesCanonical`).
  - **Nicht** gelaufen: `make gates` (Implementer belegt grün; `record-gates` würde kollidieren).

## Findings

Keine HIGH-, MEDIUM-, LOW- oder INFO-Findings. Alle sieben Prüf-Achsen unabhängig belegt (s.
Negativbefunde).

## Negativbefunde (geprüft, ohne Befund)

- **Digest-Provenienz = v0.51.1 (mehrfach belegt):** `imagetools inspect …:v0.51.1` →
  `sha256:fede3d027b2ebc1dd8534460853e57b67cc7a9a182cad2e2138c8eebf7a2d03c` (Registry-Manifest);
  lokaler RepoDigest (`docker images --digests`) byte-identisch; `d-check.mk` und `emit.go` pinnen
  exakt diesen Wert. Zwei voneinander unabhängige Quellen (Registry + lokaler Pull-Beleg) bestätigen,
  dass der gepinnte Digest wirklich v0.51.1 ist (LH-QA-02). Der dritte Plan-Beleg (d-check-`version.md`/
  Handbuch) liegt außerhalb dieses Repos; die zwei docker-basierten Belege genügen für die Zuordnung.
- **`d-check.mk`-Regeneration faithful:** `--print-mk` (aus `@sha256:fede3d02…`) vs. committetes
  `d-check.mk` unterscheidet sich in **exakt** den vier dokumentierten MR-010-Adaptionen —
  adaptierter Kopfkommentar; `DCHECK_DIGEST`-Pin (Tool emittiert die Zeile leer); `doc-check`→
  `docs-check` (Target + Hilfetext); `doc-help`-Grep `^docs?-` — und **nichts** sonst. Der
  Roh-Output emittiert selbst `DCHECK_IMAGE ?= …:v0.51.1`.
- **`--disable sources` verbatim vom Tool:** Im Roh-vs-Committed-Diff erscheinen die fünf
  fokussierten advisory-Recipes (`doc-immutable`/`doc-commits`/`doc-planning`/`doc-tracked`/
  `doc-targets`) als **unveränderter Kontext** — der Roh-`--print-mk` trägt `--disable sources`
  selbst, je 5/5, unmittelbar hinter `--disable citations`. Nicht handgeschrieben. Das
  Befund-Gate-Recipe (`docs-check`) sowie `doc-trace`/`doc-complete`/`doc-doctor`/`doc-repair`
  tragen **kein** `--disable sources` (wie erwartet — nur die fokussierten Recipes disablen alle
  Nicht-Ziel-Module).
- **`sources` NICHT aktiviert (LH-QA-01):** `.d-check.yml` `modules: [links, anchors, ids, matrix,
  codepaths, spans]` enthält **kein** `sources` (und kein `citations`); `grep sources .d-check.yml`
  leer. Der Bump macht das Netz-Modul nur *verfügbar*. Kein Phantom-Gate. `.d-check.yml` ist im Diff
  unberührt (Plan §3 „Nicht hier"). Konform.
- **Inert (0-Befund-Differenz, gemessen nicht angenommen):** `make docs-check` liefert unter v0.51.1
  **wie** unter dem v0.50.0-Override je **69 Dateien / 0 Befunde / Exit 0**. Die Behauptung
  „byte-identisch ohne aktives `sources`" ist am realen Repo-Korpus reproduziert, nicht aus dem
  Handbuch übernommen.
- **Emitter-Kopplung (Tier-1-Drift):** `emit.DefaultImage`/`DefaultDigest` (`emit.go:33-34`) sind
  byte-gleich zu `d-check.mk` `DCHECK_IMAGE`/`DCHECK_DIGEST` (`v0.51.1` / `sha256:fede3d02…`).
  `TestDefaultImage_MatchesCanonical` / `TestDefaultDigest_MatchesCanonical` (`emit_test.go:30-47`)
  lesen die kanonische Quelle `../../d-check.mk` und erzwingen Gleichheit + `sha256:`-Präfix;
  `make test` grün. Konform.
- **Emittierte Starter-Config minimal:** `internal/emit/templates/d-check.yml` im Diff unberührt;
  `TestDCheckConfig_Minimal` (grün) hält `modules: [links, anchors]` fest — kein `sources`,
  kein `citations` im Zielrepo (Emitter ≠ Dogfood). Korrekt.
- **MR-012-Konsistenz / Anker:** Beide `MR-012`-Vorkommen sind Anker-Links (kein bare ID); der
  Referenz-Anker `#mr-012--d-check-pin-v0511-sources-verfügbar` löst auf die Überschrift `### MR-012
  — d-check-Pin v0.51.1 (sources verfügbar)` auf (der grüne `docs-check`-Lauf mit aktivem
  `anchors`+`ids`-Modul bestätigt Auflösbarkeit sämtlicher IDs/Anker im Diff). Struktur analog
  MR-009/010/011 (Datum · Geltungsbereich · Adaption · Trockenlauf · Emitter · Auflösungs-Trigger).
- **Historische MR-Bodies eingefroren (Hard Rule 3.4-Geist):** `conventions.md` hat in `c0e4242`
  exakt zwei Hunks — die §Baseline-Zeile (`v0.50.0`→`v0.51.1`, +MR-012-Link) und ein **rein additiver**
  Anhang der MR-012-Sektion nach dem unveränderten MR-011-Body. MR-009/010/011-Bodies unberührt
  (Kontext-Zeilen im Diff bestätigen). Keine ADR-Datei im Range.
- **Hard Rule 3.3 (Move getrennt vom Inhalt):** `786b899` ist ein **100%-reiner Rename**
  (`git show --stat --find-renames` → `{open => in-progress}`, 0 Inhalts-Zeilen); der Content-Rewrite
  liegt getrennt in `c0e4242`. Der Pin-Bump ist genau **ein** Content-Commit. Konform.
- **Hard Rule 3.1 (keine halluzinierten Gates):** kein neuer Gate-Name — `AGENTS.md` §4 und
  `harness/README.md` im Range **nicht** berührt; `d-check.mk`-Kopfkommentar benennt weiterhin
  `docs-check` als einziges behauptetes Target. `docs-check` läuft grün über 69 Dateien.
- **Hard Rule 3.5 (Gate-Lockerung nur per ADR):** keine Schwellen-Senkung / Modul-Deaktivierung —
  der Pin-Bump ist versionsneutral (0-Befund-Diff), die fünf `--disable sources` betreffen nur die
  **advisory**-Recipes und stammen **verbatim vom Tool** (kein Repo-Loosening am `docs-check`-Gate).
  Kein ADR nötig.
- **Hard Rule 3.2 (Lint-Suppression):** kein `//nolint`, kein `# shellcheck disable` im Diff;
  `--disable sources` ist ein d-check-Modul-Flag (Tool-Recipe), keine Lint-Suppression.

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 0 |

## Verdikt

**Merge NICHT blockiert.** Kein HIGH/MEDIUM-Finding (auch kein LOW/INFO). Der Diff ist gegen Plan,
ADR-Lage (keine berührt) und Hard Rules (§3.1–3.5) konform: faithful `--print-mk`-Regeneration mit
ausschließlich den vier dokumentierten MR-010-Adaptionen, `--disable sources` verbatim vom Tool
in genau den fünf fokussierten advisory-Recipes, Digest unabhängig (Registry + lokaler RepoDigest)
als v0.51.1 belegt, Emitter-Pin korrekt gekoppelt und die emittierte Config bewusst minimal,
`sources` **verfügbar aber nicht aktiviert** (kein Phantom-Gate, kein neuer Gate-Name), Inertheit
am realen Korpus **gemessen** (69/0 unter beiden Pins), Move sauber vom Inhalt getrennt, historische
MR-Bodies eingefroren, MR-012-Anker aufgelöst.

**Hinweis Rollen-Trennung:** DoD-Abhakung und die abschließende Gate-Lauf-Bestätigung
(`make gates`-Grün, Closure-Beleg des Pflicht-Trockenlaufs) sind Verifikations-Territorium
(Modul 11), hier bewusst nicht abgehakt.
