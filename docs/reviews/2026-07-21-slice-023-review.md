# Review-Report: slice-023 (Go-Skelett-Generator) — 2026-07-21

**Review-Art:** Code — geprüft wird der Diff gegen den Slice-Plan, die
referenzierten aktiven ADRs und die Hard Rules (Modul 10 §Drei Review-Arten).
Nicht geprüft: DoD-/Spec-Abhakung (Verifier, Modul 11).

**Gegenstand:** slice-023 / `447750a..6853ccc` (HEAD `6853ccc`). Drei Commits:
`a8f94cf` (reiner Move open→in-progress, Hard Rule 3.3) · `08e50e3` (Generator
statt Fetch: `internal/gen` neu, `internal/fetch/fetch.go` gelöscht, cmd
verdrahtet, Mutationen 15–17) · `6853ccc` (Verfeinerung: Pins entdoppelt,
kuratiert-reiche `.golangci.yml`, `SKEL_GO_VERSION`-Knopf, `make smoke`-E2E,
Mutationen 18–20).

**Skill:** `.harness/skills/reviewer.md` @ 1.2.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** Opus 4.8 (1M-Kontext), `claude-opus-4-8[1m]` · **Datum:** 2026-07-21

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde — ohne
diese Liste ist der Lauf nicht reproduzierbar):

- Slice-Plan: [`docs/plan/planning/in-progress/slice-023-skelett-generator.md`](../plan/planning/in-progress/slice-023-skelett-generator.md) (§2 DoD, §3 Plan, §6 Risiken)
- Aktive ADRs: [`ADR-0005`](../plan/adr/0005-ziel-repo-distribution.md) (Tool-als-Quelle), [`ADR-0003`](../plan/adr/0003-go-native-binaries.md) (Docker-only). Kein neues ADR im Slice.
- Berührte IDs: [`LH-FA-04`](../../spec/lastenheft.md#lh-fa-04--sprachskelett-picker-f4), [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6), [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit), [`LH-QA-03`](../../spec/lastenheft.md#lh-qa-03--minimale-abhängigkeiten)
- [`AGENTS.md`](../../AGENTS.md) §3 (Hard Rules 3.1–3.6); [`harness/conventions.md`](../../harness/conventions.md) MR-007
- Vorherige Findings am gleichen Modul: [`2026-07-21-slice-025-review.md`](2026-07-21-slice-025-review.md) + [`-verify.md`](2026-07-21-slice-025-verify.md) (dieselbe cmd/bootstrap-Fläche, dieselben §3.6-Muster)

---

## Findings

Jedes Finding folgt dem **§Output-Schema des Reviewer-Skills** — der
verbindlichen Single Source of Truth. Die Felder unten sind nur gespiegelt;
bei Abweichung gilt der Skill bzw. dessen Quelle
[Kurs Modul 10 §Output-Schema](https://github.com/pt9912/ai-harness-course/blob/v3.5.0/kurs/de/04-qualitaet/modul-10-review-harness.md#worked-example-eine-reviewer-skill-datei-schreiben).

### F-1 — smoke.sh-Kopf sagt weiterhin „fetcht das Skelett per Netz"

- `kategorie`: LOW
- `quelle`: Maintainability (Doku-Drift)
- `pfad`: `harness/tools/smoke.sh:3`
- `befund`: Der Kopfkommentar behauptet, die Binary „ruft selbst `docker run <d-check>` und **fetcht das Skelett per Netz**" — nach slice-023 generiert die Binary das Skelett lokal/netzlos (`gen.Generate`), per Netz kommt nur noch die Baseline. Derselbe Diff korrigiert die Belege-Liste direkt darunter (Schritt 2: „Sprachskelett-Generierung … lokal/deterministisch, kein Netz"), lässt den Kopf aber stehen — die Datei widerspricht sich selbst.
- `verifizierbar`: nein — kein Gate prüft die semantische Aktualität eines Shell-Kommentars (`shell-lint`/`docs-check` erfassen das nicht); nur durch Lesen.

### F-2 — Test-Kommentar nennt das gelöschte `fetch.Skeleton` als Phase-2-Schritt

- `kategorie`: LOW
- `quelle`: Maintainability (Doku-Drift)
- `pfad`: `cmd/ai-harness-init/main_test.go:185`
- `befund`: Das ROT-Gegenbeispiel von `TestRun_FetchKollisionSchreibtNichts` erklärt „ohne Phase 1 liefe **fetch.Skeleton (Phase 2)** zuerst und legte das Skelett ab". `fetch.Skeleton` ist mit diesem Slice gelöscht; der Phase-2-Schritt ist jetzt `gen.Generate`. Die Test-Logik hält weiter, der benannte Symbolname ist tot. Datei wurde in diesem Diff editiert (Fixture/Exit-Code umgestellt), der Kommentar aber nicht nachgezogen.
- `verifizierbar`: nein — der Compiler prüft Kommentar-Symbole nicht; nur durch Lesen.

### F-3 — Determinismus-Wächter (`TestGenerate_Deterministic`) ist unbewacht (kein `make mutate`-Zahn)

- `kategorie`: INFO
- `quelle`: [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit) / Hard Rule 3.6
- `pfad`: `internal/gen/gen_test.go:44`
- `befund`: Der Test vergleicht zwei volle `Generate`-Läufe byte-weise und wäre ein echter Regressions-Wächter gegen eine künftig eingeschleuste Zeitstempel-/Map-Iterations-Nichtdeterminismus-Quelle. Da der Profil-Inhalt aber vollständig statisch (Konstanten) und die Schreibreihenfolge sortiert ist, gibt es **keine** neutralisierende sed-Mutation, die ihn heute rot färbt — er trägt daher keinen Fall in `test/mutations/` (§3.6: „unbewacht"). Bewusst und in `gen.go` benannt („die Konstruktion garantiert es, der Test belegt es"); keine Aktion erwartet.
- `verifizierbar`: nein — per Konstruktion existiert kein rot-färbendes Gegenbeispiel für `make mutate`.

### F-4 — architecture.md §1/§2/§4 benennen die Skelett-Klasse weiter als „Fetcher"/„Picker"

- `kategorie`: INFO
- `quelle`: [`ADR-0005`](../plan/adr/0005-ziel-repo-distribution.md) („Schärft: architecture.md §Komponenten")
- `pfad`: `spec/architecture.md:15`
- `befund`: Flowchart-Knoten (`Template- und Skelett-Fetcher`, `Sprachskelett-Picker`), Schichten-Tabelle und Sequenz beschreiben das Skelett weiter als gefetcht/gepickt; ADR-0005 (Accepted) stellt die Skelett-Klasse auf „Generator" um und trägt genau diese Datei als „Schärft"-Ziel. `architecture.md` liegt **nicht** im Diff; der Implementer hat die rank-2-Spec bewusst nicht im Code-Slice editiert (Muster wie MR-005 „Offen — Reconciliation" / slice-017). Doku-Reconciliation bleibt ein getrennter Folgeschritt.
- `verifizierbar`: nein — `docs-check` bleibt grün (kein toter Link/Anker); die Divergenz ist prosa-semantisch.

### F-5 — Generiertes Skelett pinnt Images per Tag ohne Digest

- `kategorie`: INFO
- `quelle`: [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)
- `pfad`: `internal/gen/golang.go:73`
- `befund`: `Dockerfile`/`Makefile` des Skeletts pinnen `golang:<ver>` und `golangci-lint:<ver>` per **Tag** (kein floating `main`/`latest`), bewusst ohne Digest, damit der `GO_VERSION`-ARG/`SKEL_GO_VERSION`-Knopf ein echter Knopf bleibt. LH-QA-02 verlangt „auf **Tag/Digest** gepinnt — kein floating main"; Tag-Pinning erfüllt das. Das Dogfood-Repo selbst pinnt zusätzlich per Digest (`golang:…@sha256:…`) — die Asymmetrie ist ein dokumentierter Tradeoff (`golang.go`: „wer Digest-Pinning will, hängt @sha256:… an"), keine Schwächung gegen die Zusage.
- `verifizierbar`: nein — Konformität ist per Spec-Lesung entschieden; kein Gate misst „Tag vs. Digest".

## Negativbefunde

- geprüft, ohne Befund: `internal/gen/gen.go` + `golang.go` — `Generate` ist rein (kein Netz/Datum/Global), schreibt in sortierter Reihenfolge; `profiles()` als Funktion statt Paket-Variable (gochecknoglobals-konform); `UnknownLangError`-Rolle korrekt vom gelöschten Fetch übernommen.
- geprüft, ohne Befund: **Mutationen 15/16/17/18/20** treffen ihren benannten Wächter mit Zähnen — 15 (`AS test`→`testx`) rötet nur `…MakefileTargetsMatchStages`; 16 (Datei aus dem Profil) nur den Ist-Bestand-Test `…GoProfile`; 17 (Validierung tot) rötet `…UnknownLang` (via nil-Builder-Panik); 18 (Pin-Drift) nur `…PinsMatchRepo`; 20 (goVersion nicht gefädelt) `…GoVersionThreaded`. Kein stilles Grün des benannten Guards.
- geprüft, ohne Befund: **Mutation 19** (`SKEL_GO_VERSION`-Override ignoriert) rötet `TestRun_SkelGoVersionOverride`; Verdrahtung env→generiertes Dockerfile belegt.
- geprüft, ohne Befund: **LH-QA-01 halluziniertes Gate** — `TestGenerate_MakefileTargetsMatchStages` koppelt jedes `--target <X>` strukturell an eine `AS <X>`-Stage (fail bei `len(targets)==0`), und `make smoke` Schritt 5 fährt `make -C .harness/skeleton gates` (lint/build/test) **real**, Exit-Code-gated (`|| exit 1`) — kein stiller Grün-Pfad.
- geprüft, ohne Befund: **LH-QA-02 Pin-Kopplung** — `TestGoProfile_PinsMatchRepo` koppelt Generator-Default an Repo-`Dockerfile`/`go.mod` (real gemessen: `GO_VERSION=1.26.4`, `GOLANGCI_LINT_VERSION=v2.12.2`, `go 1.26`); Drift ist durch Mut-18 rot vorgeführt.
- geprüft, ohne Befund: **`internal/fetch/fetch.go`-Löschung** — `writeFile`, `DefaultTag`, das Package-Doc und der `AssetFetch`-Kommentar sind sauber nach `baseline.go` gewandert (nur ein Package-Doc bleibt); keine toten **Code**-Referenzen auf `fetch.Skeleton`/`TarballFetch`/`DownloadTarball`/`supportedLangs` (nur die zwei Kommentar-Reste F-1/F-2).
- geprüft, ohne Befund: `cmd/ai-harness-init/main.go` — Phase 2 verdrahtet `gen.Generate` statt Fetch; `langExitCode` bildet unbekannte Sprache auf Exit 2, sonst 1; `sources`-Struct trägt nur noch die Baseline; die Folge-Slices (Web-`latest`-Lookup, go-freshness) sind im Kommentar benannt, nicht still fallengelassen.
- geprüft, ohne Befund: **Scope** `cmd/app/main.go` über die vier vom Plan benannten Dateien — von LH-FA-04/ADR-0005 („Verzeichnis-Layout plus Skelett-Dateien … / …") gedeckt und lasttragend: die generierte `build`-Stage baut `./cmd/app`, ohne die Datei liefe `make gates` im Skelett nicht. Kein Scope-Creep.
- geprüft, ohne Befund: **Sprachumfang 6→1** — die Reduktion (nur `go`) ist Plan-DoD („go ist das erste, die übrigen fünf folgen ohne Umbau der Mechanik"); LH-FA-04-Vollumfang ist Verifier-/Folge-Slice-Sache, kein Code-Defekt.
- geprüft, ohne Befund: **ADR-0003 Docker-only** — generiertes Skelett ist Docker-only (Go-Gates als `Dockerfile`-Stages, `Makefile` ruft `docker build --target`); keine Host-Toolchain-Annahme.
- geprüft, ohne Befund: **Hard Rule 3.2** — kein `//nolint` / `# shellcheck disable` im Diff (die emittierte `.golangci.yml` ist Enable-Config, keine Inline-Suppression).
- geprüft, ohne Befund: **Hard Rule 3.3** — `a8f94cf` ist der reine Move, `08e50e3`/`6853ccc` der Inhalt; getrennt committet.
- geprüft, ohne Befund: **Hard Rule 3.5** — das Dogfood-`.golangci.yml` ist unverändert; die kuratiert-leichtere **emittierte** Config (ohne repo-eigenes `forbidigo`/`gomodguard`) ist Emitter-Design, keine Lockerung eines Dogfood-Gates.
- geprüft, ohne Befund: **`make gates` offline-grün** — neue Codepfade sind rein lokal, kein neues Gate-Target im Dogfood-`Makefile`; die Mutationen 15–20 speisen `make mutate` (Nicht-Gate); `make smoke` Schritt 5 ist Tier-2.
- geprüft, ohne Befund: `welle-02`-Link `open/`→`in-progress/` — nötig für `docs-check` (codepaths/links) nach dem Move `a8f94cf`; Prosa unangetastet.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 2 |
| INFO | 3 |

## Verdikt

**Merge-blockierend:** nein — 0 HIGH, 0 MEDIUM. Die vier repo-spezifischen
HIGH-Anker wurden gezielt geprüft und treffen nicht zu: (1) **Stilles-Grün im
Mutationssatz** — alle sechs Mutationen 15–20 röten ihren benannten Wächter mit
Zähnen; der einzige nicht-mutations-bewachte Wächter (`TestGenerate_Deterministic`,
F-3) ist per Konstruktion nicht rot-färbbar und bewusst so benannt. (2)
**Halluziniertes Gate (LH-QA-01)** — der Target↔Stage-Kopplungstest deckt es
strukturell, `make smoke` Schritt 5 fährt die generierten Go-Gates real,
Exit-Code-gated. (3) **Aktive-ADR-/Hard-Rule-Verstoß** — ADR-0003 (Docker-only)
und ADR-0005 (Tool-als-Quelle) gewahrt, Hard Rules 3.2/3.3/3.5 sauber. (4)
**Gate-Lockerung ohne ADR** — das Dogfood-Gate ist unberührt, `make gates`
bleibt offline-grün. Die zwei LOW (F-1/F-2) sind Doku-Drift-Reste derselben
fetch→generate-Migration in editierten Dateien; die drei INFO sind bewusste,
dokumentierte Designnotizen (Determinismus per Konstruktion, deferred
architecture.md-Reconciliation, Tag-Pin-Tradeoff) — keine blockiert.

**Übergabe:** Findings gehen an die Implementation. Der Report ersetzt keine
Verifikation — DoD-/Spec-Konformität (u. a. „rot gesehen", je-Schritt-Abdeckung,
realer `make gates`/`make mutate`/`make smoke`-Lauf, LH-FA-04-Vollumfang) prüft
der Verifier separat (Modul 11; anderes Prüf-Artefakt, anderer Eingabe-Kontext).
