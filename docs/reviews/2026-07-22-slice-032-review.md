# Review — slice-032 (Command-Guard emittieren: BLOCKED-Set je `--lang`)

**Rolle:** Unabhängiger Reviewer (Modul 10, frischer Kontext — kein Selbst-Review).
**Datum:** 2026-07-22.
**Prüfgegenstand:** Implementierungs-Commit `35947bc` (12 Dateien, +404/-50). Die Plan-Cut- und
Lifecycle-Move-Commits (`b3f41e7`, `28c2cb0`) sind Planner-/Lifecycle-Artefakte (nicht Teil des Code-Reviews).
**Reviewer-Skill:** `.harness/skills/reviewer.md` v1.2.0.

**Pflicht-Kontext gelesen:** Diff · `spec/lastenheft.md`-Bezüge (LH-FA-06 Guard-Teil, LH-QA-03) ·
`docs/plan/adr/0006-durchsetzung-commands-tool-als-quelle.md` (aktiv) ·
`docs/plan/adr/0004-durchsetzungs-emission.md` (aktiv, Guard-Bauart bash+awk / BLOCKED-Set je `--lang`) ·
`AGENTS.md` §3 (3.1/3.5/3.6) · `harness/conventions.md`-Bezug (MR-005 emittiertes `tools/harness/`-Layout) ·
vorherige Findings (`docs/reviews/2026-07-22-slice-031-review.md` als Muster, insb. N-4 Layout, N-5 Verbatim,
N-9 Chmod/Vorpass, N-10 Sensor, N-11 Pre-Flight-Kopplung; slice-030 Grenz-Selbstwiderspruch) · Slice-Plan
`docs/plan/planning/in-progress/slice-032-command-guard-emit.md`.

**Verglichene Dogfood-Originale (Zeile für Zeile):** `.claude/hooks/pretooluse-command-guard.sh`,
`harness/tools/extract-command.awk`, `.claude/settings.json`. Kopplung: `internal/gen/gen.go:66` (`SupportedLangs`),
`cmd/ai-harness-init/main.go:131/194/237` (gen-Validierung vor Enforce; Pre-Flight).

**Gate-Läufe durch den Reviewer:** keine teuren Läufe (rollenkonform — `make gates`/`smoke`/`full-smoke`/
`mutate` sind Sache der Verifikation); Analyse gegen den ruhenden Baum + Diff-Inspektion.

---

## Findings

Keine HIGH- oder MEDIUM-Findings.

### L-1 · LOW — stale Doc-Kommentar behauptet noch „settings.json verdrahtet NUR den Stop-Hook"
- **kategorie:** LOW
- **quelle:** Maintainability / Doku-Drift (slice-032-Grenzverschiebung, Plan §6)
- **pfad:** `internal/emit/enforce.go:36-39`
- **befund:** Der Doc-Kommentar über `enforceFiles()` sagt weiterhin wörtlich: „settings.json verdrahtet NUR
  den Stop-Hook — der PreToolUse-Guard ist slice-032 (er kommt mit seinem Skript, sonst liefe im Ziel ein Hook
  auf ein fehlendes Skript)." Das ist mit slice-032 **überholt**: die Funktion listet jetzt selbst den Guard
  (`enforce.go:50-51`) und die emittierte `settings.json` **verdrahtet** den `PreToolUse`-Block (Diff
  `settings.json:3-13`). Der Kommentar widerspricht damit dem Code, den er dokumentiert — genau die Klasse
  „zurückgebliebener Grenz-Widerspruch", die der Plan §6 (slice-031-Kopplung) und slice-030s Selbstwiderspruch
  benennen. Nicht-funktional (Kommentar), aber irreführend für die nächste Sitzung.
- **verifizierbar:** nein (Kommentar; kein Gate färbt Prosa-Drift — Sicht-Prüfung).

### L-2 · LOW — emittierter Guard trägt den Werkzeug-Namen `ai-harness-init` + Emit-Substitutions-Prosa
- **kategorie:** LOW
- **quelle:** ADR-0006 §Entscheidung 1 „generische Fassung … adaptierbare Marker" / Genericisierung (Plan-Zeile
  „ai-harness-init/MR-spezifische Refs neutralisiert")
- **pfad:** `internal/emit/templates/enforce/pretooluse-command-guard.sh:41-42`
- **befund:** Die BLOCKED-Kopfzeile trägt im emittierten Skript den Kommentar „…die Toolchain der Ziel-Sprache
  (je `--lang` **von ai-harness-init eingesetzt**; `@@BLOCKED_SET@@` wird beim Emit ersetzt)." Im gebooteten
  Ziel ist beides tot: der Platzhalter ist da bereits substituiert (kein `@@…@@` mehr sichtbar), und
  `ai-harness-init` ist der **Quell**-Werkzeugname, nicht das Adopter-Repo. Der Plan (§3-Tabelle) fordert
  explizit „ai-harness-init/MR-spezifische Refs neutralisiert" — an dieser Zeile blieb der Werkzeugname stehen
  (alle anderen Refs, MR-002/003, ADR-0003/0004, wurden korrekt entfernt; s. N-1). Einziger Fund im
  `grep`-Durchlauf über Guard/awk/settings.json.
- **verifizierbar:** teilweise (Grep `ai-harness-init` im emittierten Guard trifft Zeile 42; kein Gate erzwingt
  die Neutralisierung — Sicht-Prüfung).

---

## Negativbefunde (geprüft, ohne blockierenden Befund)

### N-1 · Verbatim-Treue Guard-Skript — nur awk-Pfad + BLOCKED-Zeile + Refs geändert
- **quelle:** Plan §6 (Scan-Logik darf nicht driften, `test/guard.bats`), AGENTS §3.6
- **pfad:** `internal/emit/templates/enforce/pretooluse-command-guard.sh` gegen `.claude/hooks/pretooluse-command-guard.sh`
- **befund:** Zeilenweiser Abgleich: `strip_quotes`, `scan` (Segment-Trenner `&& & || | ; $( \` ( \r`, Präfix-/
  Assignment-/Brace-Skip, `head=${head##*/}`, `in_set BLOCKED`, `SHELLS`-Sub-Shell-Rekursion Tiefe ≤3,
  Flag-Bündel `-[a-z]*c[a-z]*`), `in_set`, die `input=$(cat)`/`awk -f`/`rc`-Zweige — **byte-identisch** zum
  Dogfood. Es unterscheiden sich **nur**: (a) der awk-Pfad `../../tools/harness/` statt `../../harness/tools/`
  (MR-005, s. N-3), (b) `BLOCKED="@@BLOCKED_SET@@"` statt der literalen Menge (s. N-4), (c) die
  neutralisierten Header-/`emit_block`-Refs (ADR-0003/0004, MR-002/003 entfernt; „ai-harness-init" →
  „This repository"/„dieses Repo", außer der L-2-Zeile). **Kein** Logik-Drift in der Scan-/Quote-/Sub-Shell-
  Mechanik.
- **verifizierbar:** ja (`make shell-lint` deckt `templates/enforce/*.sh`; `make full-smoke` läuft die Logik real).

### N-2 · Verbatim-Treue awk-Extraktor — Scan-Logik byte-identisch, nur Kommentar-Refs + Umlaute
- **quelle:** Plan §3/§6 (awk verbatim, sprach-agnostisch)
- **pfad:** `internal/emit/templates/enforce/extract-command.awk` gegen `harness/tools/extract-command.awk`
- **befund:** Der gesamte Scanner (Zeilen 12-81: Tiefen-/Key-Stack, `instr`/`esc`/`hadu`, `\u`-Genau-4-Hex-
  Fail-closed, `tool_input`→`command`-Tiefe-2-Pfad, `sawobj`/`depth`-Balance-Checks, `exit 3`-Zweige) ist
  **byte-identisch**. Abweichungen nur im Kopf-Kommentar: entfernte `ADR-0004`/`LH-FA-06`-Refs und ASCII-
  isierte Umlaute (`Vollständigkeit`→`Vollstaendigkeit`, `entschärft`→`entschaerft`, `zählt`→`zaehlt`) — reine
  Kommentar-Kosmetik, **keine** Verhaltensänderung. Die ASCII-Isierung ist gegenüber dem Guard-Skript
  konsistent (das schon durchgehend `ue`/`ae` nutzt).
- **verifizierbar:** ja (`make full-smoke` — der emittierte awk parst die Hook-JSON real).

### N-3 · MR-005: emittiertes Layout `tools/harness/` durchgängig (Guard-Ref, awk-Ziel, Test)
- **quelle:** MR-005 (emittiertes `tools/harness/` ≠ lokales `harness/tools/`)
- **pfad:** `pretooluse-command-guard.sh:30`, `enforce.go:51`, `enforce_test.go` (`TestEnforce_GuardBashAwkOnly`)
- **befund:** Der emittierte Guard löst `extractor="$here/../../tools/harness/extract-command.awk"` auf; `$here`
  = `.claude/hooks/`, `../../` = Repo-Root → `<root>/tools/harness/extract-command.awk`. Der Emit legt den
  Extraktor exakt dorthin (`enforceFiles` dst `tools/harness/extract-command.awk`). `TestEnforce_GuardBashAwkOnly`
  guardet **beides**: der Guard **enthält** `tools/harness/extract-command.awk` **und** **nicht** das lokale
  `harness/tools/extract-command.awk` (rot, sobald das lokale Layout durchsickert). Kein Layout-Drift.
- **verifizierbar:** ja (`make test`; `make full-smoke` löst den Pfad real via `BASH_SOURCE` auf).

### N-4 · Substitution `@@BLOCKED_SET@@` — nur im Guard, kein Platzhalter im Ziel, robust bei Unknown-Lang
- **quelle:** Plan §6 (Substitution neu; kein `@@` im Ziel), Robustheit
- **pfad:** `enforce.go:55-57` (`guardDst`), `enforce.go:119-121` (`bytes.ReplaceAll`), `enforce_test.go` (`TestEnforce_GuardBlockedSetPerLang`)
- **befund:** `Enforce` substituiert `@@BLOCKED_SET@@` **nur**, wenn `f.dst == guardDst`; der const `guardDst`
  ist byte-gleich zum `enforceFiles`-Eintrag (drifteten sie, bliebe der Platzhalter → `TestEnforce_GuardBlocked
  SetPerLang` rot, da es aus der WIRKLICH geschriebenen Datei liest, nicht dem Embed). `bytes.ReplaceAll` ist
  korrekt (genau **eine** Fundstelle im Template, Zeile 43; ReplaceAll deckt sie und wäre auch bei mehreren
  sicher). Robustheit bei unbekanntem `lang`: `blockedSet` liefert dann die **universelle** Menge (kein Panic,
  keine leere Zeichenkette). Zusätzlich läuft der Emit **nach** der gen-Validierung (`main.go:131` `gen.Generate`
  → Exit 2 bei Unknown-Lang, **vor** `main.go:194` `emit.Enforce`), sodass `lang` an der Enforce-Stelle stets
  ein bekanntes Profil ist. Zwei Sensoren decken den Platzhalter-Rest: `TestEnforce_GuardBlockedSetPerLang`
  (Go) **und** die smoke.sh-Grep-Gegenprobe (`@@BLOCKED_SET@@` darf nicht im Ziel stehen).
- **verifizierbar:** ja (`make test` + `make smoke`).

### N-5 · Lang-Kopplung: `TestBlockedSet_CoversAllGenProfiles` greift echt
- **quelle:** Plan §6 (stiller Lang-Drift), ADR-0006 (BLOCKED-Set je `--lang`)
- **pfad:** `enforce.go:63-81` (`blockedSet`/`blockedByLang`/`BlockedSetForLang`), `enforce_test.go` (`TestBlockedSet_CoversAllGenProfiles`), `gen.go:66`
- **befund:** Der Wächter bildet `universalOnly := BlockedSetForLang("___unbekannt___")` (== reine universelle
  Menge, da `blockedByLang` nur `go` kennt) und verlangt für **jedes** `gen.SupportedLangs()`-Profil
  `BlockedSetForLang(lang) != universalOnly`. Bekäme `gen` ein Profil ohne `blockedByLang`-Eintrag, gäbe
  `blockedSet` für dieses Profil exakt `universalOnly` zurück → Test rot. Der Wächter greift also real (die
  stille Lücke „Sprach-Toolchain läuft ungehindert" wird rot). Aktuell ist `SupportedLangs()` = `{go}`, das
  einen Eintrag hat → grün. Mutation 34 belegt die Zähne (s. N-6).
- **verifizierbar:** ja (`make test`; `make mutate` Fall 34).

### N-6 · Sensor-Integrität: 32 umgewidmet, 34/35 neu — echte Mutationen, färben den GENANNTEN Wächter
- **quelle:** AGENTS §3.6 (rot gesehenes Gegenbeispiel), slice-026-Klasse
- **pfad:** `test/mutations/32-enforce-settings-wires-guard.sh` (32-…stop-only.sh gelöscht), `34-…lang-kopplung.sh`, `35-…guard-substitution.sh`
- **befund:** (a) **32 umgewidmet** (nicht nur ergänzt): der alte `32-enforce-settings-stop-only.sh`
  (`# expect: TestEnforce_SettingsStopOnly`) ist **gelöscht**; der neue Fall `sed 's#pretooluse-command-guard.sh#
  guard-DISABLED.sh#'` in `settings.json` erwartet `TestEnforce_SettingsWiresBothHooks` (verliert den Guard-
  Verweis → rot). (b) **34**: löscht die `"go": …`-Zeile aus `blockedByLang` → `blockedSet("go") == universalOnly`
  → `TestBlockedSet_CoversAllGenProfiles` rot; kompiliert weiter (leere-map-Literal gültig). (c) **35**: patcht
  `[]byte("@@BLOCKED_SET@@")`→`[]byte("@@NOMATCH@@")` in `enforce.go` → Platzhalter bleibt im Guard → `TestEnforce_
  GuardBlockedSetPerLang` rot; kompiliert weiter (`bytes` bleibt genutzt). Alle drei tragen `# files:`/`# expect:`
  im Parser-Format, lassen `# verify:` weg (Default `test` → Fehlschlag-Muster `--- FAIL:`) und **ändern real**
  ihre gelistete Datei. Jede färbt den **genannten** Wächter. Kein zahnloser Fall.
- **verifizierbar:** ja (`make mutate` — 32/34/35 rot gesehen).

### N-7 · full-smoke prüft Guard-VERHALTEN (BLOCKED-Set + awk-Pfad), nicht nur Präsenz
- **quelle:** LH-FA-06/LH-QA-03 (real belegt, nicht behauptet), Plan-DoD §2
- **pfad:** `harness/tools/full-smoke.sh:87-107`
- **befund:** full-smoke füttert den emittierten Guard mit `{"tool_input":{"command":"go build ./..."}}` →
  erwartet `"decision": "block"`; und mit `{"…":"make test"}` → erwartet **keine** Ausgabe. Das ist stärker als
  Präsenz: (i) blockt „go build", ist das go-BLOCKED-Set real substituiert (ein zurückgebliebener Platzhalter
  ließe „go" durch → rot); (ii) der **Pass**-Fall fängt zusätzlich einen kaputten awk-Pfad ab — löste der Guard
  den Extraktor nicht auf (falscher `tools/harness/`-Pfad oder fehlendes awk), liefe er **fail-closed** und
  blockte **auch** „make test" → `pass_out` nicht leer → rot. Damit decken die zwei Proben BLOCKED-Set **und**
  awk-Pfad **und** bash+awk-Genügsamkeit im real gebooteten Ziel. Kein „Stempel-da"-Schwachtest.
- **verifizierbar:** ja (`make full-smoke`).

### N-8 · `TestEnforce_GuardBashAwkOnly`: Verzicht auf node/jq-Wort-Grep begründet, keine Lücke
- **quelle:** LH-QA-03 (kein node/jq), AGENTS §3.6 (Test misst Eigenschaft, nicht Implementierung)
- **pfad:** `enforce_test.go` (`TestEnforce_GuardBashAwkOnly`), `full-smoke.sh:87-107`
- **befund:** Ein negativer String-Grep auf `"jq"`/`"node"` wäre **bruchig** — beide Wörter stehen im
  erklärenden „KEIN node/jq"-Kommentar des Guards (Zeilen 4/8), ein Negativ-Grep würde dort false-positive
  auslösen. Der Test setzt stattdessen positive Struktur-Anker (`awk -f` vorhanden; awk-Pfad `tools/harness/`
  richtig, lokaler Pfad **nicht**), die **verbindliche** Abhängigkeits-Zusage trägt der **behaviorale**
  full-smoke-Lauf (der Guard läuft dort real mit bash + awk; wäre jq/node nötig, bräche er). Die Arbeitsteilung
  (struktureller Anker + behavioraler Beweis) ist die korrekte §3.6-Lesart — nicht eine grün-versteckende
  Lücke. Der Kommentar im Test benennt diese Begründung explizit.
- **verifizierbar:** ja (`make test` + `make full-smoke`).

### N-9 · settings.json: beide Hooks verdrahtet, PreToolUse byte-gleich zum Dogfood, gültiges JSON
- **quelle:** Plan §2-DoD (settings verdrahtet PreToolUse), ADR-0006
- **pfad:** `internal/emit/templates/enforce/settings.json:3-13`, `enforce_test.go` (`TestEnforce_SettingsWiresBothHooks`), `smoke.sh:80-93`
- **befund:** Die emittierte `settings.json` trägt jetzt `PreToolUse` (Matcher `Bash`, Command
  `bash "$CLAUDE_PROJECT_DIR"/.claude/hooks/pretooluse-command-guard.sh`) **neben** dem Stop-Block — der
  PreToolUse-Block ist **byte-identisch** zum Dogfood (`.claude/settings.json:3-11`). Gültiges JSON (Klammer-/
  Komma-Balance geprüft). Der Test dreht sauber um: `TestEnforce_SettingsStopOnly` ist **gelöscht**,
  `TestEnforce_SettingsWiresBothHooks` verlangt jetzt **alle fünf** Anker (`"Stop"`, `stop-require-gates.sh`,
  `PreToolUse`, `"matcher": "Bash"`, `pretooluse-command-guard.sh`). Auch smoke.sh ist umgekehrt: die
  slice-031-Negativprobe („PreToolUse darf NICHT da sein", „Guard darf NICHT emittiert sein") ist ersetzt durch
  Positiv-Proben (PreToolUse **muss** da sein; Guard/awk-Präsenz; `@@BLOCKED_SET@@` darf **nicht** zurückbleiben).
- **verifizierbar:** ja (`make test` + `make smoke`).

### N-10 · Grenz-Widersprüche gesucht: Test/Smoke sauber gedreht — einzige Ausnahme L-1
- **quelle:** Plan §6 (slice-031-Kopplung; kein zurückgebliebener „kein PreToolUse")
- **pfad:** Grep `SettingsStopOnly|Stop-only|NUR den Stop|Guard ist slice-032|NOCH NICHT emittiert|kein PreToolUse` über `internal/ cmd/ harness/ test/`
- **befund:** Der Grep trifft drei Stellen: `test/mutations/32-…:7` und `enforce_test.go:74` benennen die
  slice-031-Grenze **historisch-korrekt** („war Stop-only … slice-032 verdrahtet BEIDE" / „ist mit slice-032
  aufgehoben") — keine Widersprüche. Die **einzige** zurückgebliebene Falschaussage ist der Doc-Kommentar
  `enforce.go:38` (→ L-1). Kein Test/Smoke behauptet mehr „kein PreToolUse"; die alte Negativ-Mutation
  (`32-enforce-settings-stop-only.sh`) ist gelöscht. Grenzverschiebung ist bis auf L-1 vollständig.
- **verifizierbar:** ja (Grep + `make test`/`make smoke`/`make mutate`).

### N-11 · ADR-0006 + Hard Rules: Tool-als-Quelle (go:embed), bash+awk, Chmod, Vorpass, Pre-Flight-Kopplung
- **quelle:** ADR-0006 §Entscheidung 1 + Guard-Bauart, slice-022a L2, slice-025, slice-028
- **pfad:** `enforce.go:24` (`//go:embed all:templates/enforce`), `enforce.go:100-134` (`Enforce`), `main.go:237` (`EnforcePaths` im Pre-Flight)
- **befund:** Guard + awk liegen als eingebettete Quelle unter `templates/enforce/` (`embed.FS`) — **Tool-als-
  Quelle**, nicht gefetcht (ADR-0006 §Entscheidung 1); die Bauart bleibt bash + awk, kein node/jq/OCI (ADR-0006
  §Guard-Bauart unverändert / LH-QA-03). **Chmod-nach-Write**: der Guard steht mit 0o755 in `enforceFiles` und
  bekommt `os.Chmod` **nach** `WriteFile` (L2-Lehre — via `--force` über eine 0644-Datei bliebe der Inhalt sonst
  in nicht-ausführbarer Datei); `TestEnforce_ScriptsExecutable` deckt den Guard jetzt mit. **Kollisions-Vorpass**:
  der `!force`-Vorpass läuft über **alle 7** Ziele (inkl. Guard + awk) → kein Teil-Emit. **Pre-Flight-Kopplung**:
  `EnforcePaths()` leitet aus derselben `enforceFiles()`-Liste ab und wird in `main.go:237` in den Phase-3-
  Pre-Flight gehängt → Guard + awk fallen bei Kollision **vor** dem Emit auf; `TestEnforce_EmitsAllMechanicFiles`
  koppelt `EnforcePaths` gegen den realen Emit-Bestand (want-Liste um beide neuen Ziele erweitert). Determinismus:
  statischer Inhalt + reine `--lang`-Substitution (ADR-0006 §Konsequenzen / Fitness-Function).
- **verifizierbar:** ja (`make test` — `TestEnforce_EmitsAllMechanicFiles`/`_ScriptsExecutable`/`_NoOverwriteWithoutForce`; `make build`).

### N-12 · cp-Disziplin Slice-Plan (MR-008) — Template-Struktur erhalten (Oberfläche)
- **quelle:** MR-008 (cp aus vendored Template)
- **pfad:** `docs/plan/planning/in-progress/slice-032-command-guard-emit.md`
- **befund:** Der Plan trägt die instruktiven Template-Kommentarblöcke (§2-DoD, §3, §4-Trigger mit Kurs-Links,
  §5/§7/§8) und die Standard-Abschnittsfolge des vendored `slice.template.md` — konsistent mit cp-aus-Template +
  Ausfüllen. Der Plan-Cut selbst ist ein Planner-Commit (außerhalb des strikten Code-Review-Scopes); die volle
  Byte-Treue ist Planner-/Verifikations-Artefakt.
- **verifizierbar:** teilweise (Struktur-Inspektion).

---

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 2 (L-1, L-2) |
| INFO | 0 |
| Negativbefunde | 12 (N-1 … N-12) |

---

## Verdikt

**KONFORM — keine Blocker.** Alle Prüf-Achsen aus dem Auftrag bestanden: ADR-0006-Konformität (Guard + awk als
Tool-als-Quelle via `go:embed all:templates/enforce`, Bauart bash+awk ohne node/jq, `blockedSet`/`blockedByLang`
je `--lang`, settings.json verdrahtet PreToolUse+Stop); **Verbatim-Treue** der Scan-/Quote-/Sub-Shell-Logik
byte-identisch gegen die Dogfood-Originale (nur awk-Pfad, BLOCKED-Zeile und neutralisierte Refs geändert — der
`test/guard.bats`-erprobte Kern driftet nicht); **MR-005** (`tools/harness/`) durchgängig in Guard-Referenz,
awk-Zielpfad und Test; **Substitution** korrekt und robust (`@@BLOCKED_SET@@` nur im Guard, `guardDst`==Eintrag,
kein Platzhalter im Ziel, Unknown-Lang → universelle Menge, Emit läuft nach gen-Validierung); **Lang-Kopplung**
greift echt (`TestBlockedSet_CoversAllGenProfiles` färbt ein Profil ohne BLOCKED-Set rot, Mutation 34 belegt);
**Grenzverschiebung** slice-031→032 sauber gedreht (Test/Smoke/Mutation 32 umgeschrieben, alte Stop-only-Mutation
gelöscht); **full-smoke** prüft Guard-Verhalten (BLOCKED-Set **und** awk-Pfad via Fail-closed-Fang im Pass-Fall),
nicht nur Präsenz; der `node/jq`-Wort-Grep-Verzicht in `TestEnforce_GuardBashAwkOnly` ist begründet (positive
Anker + behavioraler full-smoke), keine Lücke; Chmod-nach-Write, Kollisions-Vorpass und Pre-Flight-Kopplung
(EnforcePaths inkl. Guard+awk) sind sauber.

Zwei **LOW** (nicht-blockierend, Doku-/Genericisierungs-Drift): **L-1** ein stale Doc-Kommentar
(`enforce.go:38`) behauptet weiter „settings.json verdrahtet NUR den Stop-Hook … Guard ist slice-032",
widerspricht dem Code, den er dokumentiert; **L-2** der emittierte Guard (`…-command-guard.sh:42`) trägt noch
den Werkzeug-Namen `ai-harness-init` + eine Emit-Substitutions-Prosa, die im Ziel tot ist (der Plan forderte die
Neutralisierung dieser Refs). Beide sind Kommentar-Drift ohne Verhaltensfolge — für einen `open/`-Folgepunkt oder
eine Nachzieh-Zeile geeignet, kein Merge-Blocker.

**Offen für die Verifikation** (nicht Reviewer-Rolle): `make gates` grün · `make full-smoke` (realer Guard-
Verhaltenslauf N-7) · `make mutate` (realer Rot-Lauf 32/34/35, N-6) · `make smoke`/`make shell-lint` (N-3/N-9) ·
DoD-Abhakung.
