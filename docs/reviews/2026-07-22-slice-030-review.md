# Review — slice-030 (Reviewer-/Closure-Skill emittieren)

**Rolle:** Unabhängiger Reviewer (Modul 10, frischer Kontext — kein Selbst-Review).
**Datum:** 2026-07-22.
**Prüfgegenstand:** `git diff 17bce9d..HEAD` (3 Commits: `096a1c7` Move, `552bf32` Move-Churn, `a4a7bf7` Feature).
**Reviewer-Skill:** `.harness/skills/reviewer.md` v1.2.0.

**Pflicht-Kontext gelesen:** Diff · `spec/lastenheft.md` (LH-FA-06, LH-QA-01) ·
`docs/plan/adr/0006-durchsetzung-commands-tool-als-quelle.md` (aktiv) ·
`AGENTS.md` §3 (3.1/3.6) · vorherige Findings (`docs/reviews/`, insb. slice-028 Zielmengen-Kopplung,
slice-022b F-1 inerte Tests) · Slice-Plan `docs/plan/planning/in-progress/slice-030-*.md`.

**Gate-Lauf durch den Reviewer:** `make test` — grün (alle 5 Pakete `ok`). Mutation 30 im
Scratch-Klon appliziert (patcht, kompiliert), danach restauriert; smoke/full-smoke NICHT gefahren
(teuer, Verifikations-Rolle).

---

## Findings

Keine HIGH-, MEDIUM- oder LOW-Findings.

---

## Negativbefunde (geprüft, ohne blockierenden Befund)

### N-1 · inScope-Änderung: Skills fallen korrekt auf `default: true`, als Singletons
- **quelle:** LH-FA-06 (Skill-Teil)
- **pfad:** `internal/emit/templates.go:137-152`
- **befund:** Der `.harness/skills/`-Ausschluss-Case ist entfernt; die beiden Skill-Vorlagen
  fallen auf `default: true`. Sie stehen weder in `isRecurring` (`templates.go:21`) noch in
  `isDerivativeIndex` (`templates.go:38`) → `planTemplates` emittiert sie als transformierte
  Singletons (`StripHintBlock` + `stampName`), nicht co-located/recurring. `strings.HasPrefix`
  bleibt im Paket genutzt (`StripHintBlock`, `templates.go:343/352`) → kompiliert. `make test`
  grün belegt es.
- **verifizierbar:** ja (`make test` — `TestTemplates_Layout`, `TestTemplates_EmittierterBestandVollstaendig`).

### N-2 · Zielmengen-Kopplung: alle drei Achsen bewegt, keine zurückgeblieben
- **quelle:** slice-028-Klasse (Fixture↔Emit↔Pre-Flight)
- **pfad:** `internal/emit/templates_test.go:235-254`, `test/courseset-fixture.bats:71-84`,
  `internal/emit/templates_test.go:60-91`
- **befund:** (a) `TestTemplates_EmittierterBestandVollstaendig` want = 16 (10 Singletons +
  6 .gitkeep, Voll-Baum-Vergleich, exakt). (b) `courseset-fixture.bats` „genau 17 in-scope";
  gegen den realen vendored Satz nachgerechnet: `.harness/baseline/v3.5.0/templates/` trägt 18
  `*.template.md`, minus `project-readme` = 17 in-scope → 10 Singletons + 2 derivative Indexe +
  5 recurring = 17, konsistent. (c) `TestTemplateTargets_SpiegeltDenEmittiertenSatz` vergleicht
  `TemplateTargets` gegen einen realen `Templates`-Lauf (`written`) — automatisch gekoppelt, keine
  eigene Zahl, die zurückbleiben könnte. Keine Achse falsch-grün.
- **verifizierbar:** ja (`make test` + `make gates` fährt `courseset-fixture.bats`).

### N-3 · smoke.sh Positiv/Negativ konsistent (kein Selbstwiderspruch)
- **quelle:** LH-QA-01 / MR (smoke)
- **pfad:** `harness/tools/smoke.sh:64` (Positiv), `:77-80` (Negativ)
- **befund:** `.harness/skills/reviewer.md` ist in den Positiv-Loop (muss existieren) aufgenommen
  UND aus dem Negativ-Loop (out-of-scope) entfernt — nicht mehr in beiden. Der zuvor real rote
  Selbstwiderspruch ist aufgelöst. `reviewer.md` ist ein Repräsentant; die exakte Voll-Menge
  bewacht `TestTemplates_EmittierterBestandVollstaendig` (in `make gates`).
- **verifizierbar:** ja (`make smoke` — Tier 2).

### N-4 · Mutation 30 kompiliert, greift, benennt den real fallenden Test
- **quelle:** AGENTS §3.6 (Gegenbeispiel rot)
- **pfad:** `test/mutations/30-skills-nicht-emittiert.sh`
- **befund:** Der `sed` patcht `templates.go:144` sauber (im Scratch verifiziert: Zeile wird zu
  `case rel == "project-readme.template.md" || strings.HasPrefix(rel, ".harness/skills/"):`).
  `strings.HasPrefix`-Count steigt auf 3 → `strings` bleibt importiert/genutzt → kompiliert (Rot
  ist ein Test-Rot, kein Build-Fehler). Der Rückfall entfernt die 2 Skills aus dem emittierten
  Bestand → `TestTemplates_EmittierterBestandVollstaendig` wird rot; `# expect:` benennt genau
  diesen Test, und die Fehlschlag-Form `--- FAIL:` (aus `failure_form "test"`) matcht ihn. `# files:`
  nennt die mutierte Datei. Mutiert die Zusage „Skills emittiert", nicht eine Nebensache.
- **verifizierbar:** ja (`make mutate`).

### N-5 · ADR-0006-Konformität: Skills bleiben Fetch (nicht tool-generiert)
- **quelle:** ADR-0006 (aktiv)
- **pfad:** `internal/emit/templates.go:147-149`, `test/courseset-fixture.bats:46-51`
- **befund:** Die Skills werden aus dem gefetchten/vendored Kurs-Template-Satz (`courseSet()` bzw.
  real `.harness/baseline/<tag>/templates/`) via Templates-Emit kopiert — kein tool-generierter
  Inhalt. Das deckt sich mit ADR-0006 §Entscheidung 2 („Fetch/Picker (unverändert) — der
  Reviewer-/Closure-Skill") und §Konsequenzen („der Reviewer-Skill bleibt Fetch"). Der inScope-
  Kommentar sagt es explizit („er bleibt Fetch, ADR-0006").
- **verifizierbar:** ja (Diff-Inspektion; kein Gate nötig).

### N-6 · Gate-Neutralität im Ziel: durch emittierte `.d-check.yml` belegt (nicht nur behauptet)
- **quelle:** AGENTS §3.6 (kein stilles Grün) / LH-QA-01
- **pfad:** `internal/emit/templates/d-check.yml:9`
- **befund:** Die emittierte `.d-check.yml` trägt `scan.ignore: ["**/*.template.md", ".tmp/**",
  ".harness/**"]` — `.harness/**` schließt die emittierten Skills vom Doc-Gate aus. Die Neutralität
  ist damit design-/config-belegt (nicht bloß behauptet). Die reale 0-Befunde-Bestätigung durch
  `make smoke` (Tier-2 `docs-check` gegen das emittierte Repo, `smoke.sh:87-89`) bzw. `make full-smoke`
  ist Sache der Verifikation — der Reviewer fährt diese teuren Läufe rollenkonform nicht.
- **verifizierbar:** ja (Verifikation: `make smoke`/`make full-smoke`, 0 docs-check-Befunde).

### N-7 · Plan-Treue: Diff == Plan §3
- **quelle:** Slice-Plan §3
- **pfad:** `docs/plan/planning/in-progress/slice-030-*.md:41-46`
- **befund:** Der Diff berührt genau die vier geplanten Achsen: `templates.go` (inScope-Refactor),
  `templates_test.go` + `courseset-fixture.bats` (Zielmenge), `smoke.sh` (Skill-Präsenz), neuer
  Mutations-Wächter. Die zwei Move-Commits (`096a1c7` open→in-progress, `552bf32` Inbound-Link in
  `welle-04-*.md`) sind reine Lifecycle-Moves/Churn (AGENTS §3.3). Kein Streuartefakt, keine
  ungeplante Fläche. Keine stale Alt-Zählung (8/14/15) außerhalb der done-Slices/Reviews gefunden.
- **verifizierbar:** ja (Diff-Inspektion).

---

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 0 |
| Negativbefunde | 7 (N-1 … N-7) |

---

## Verdikt

**KONFORM — keine Blocker.** Alle sechs Prüf-Achsen bestanden: die inScope-Änderung emittiert die
beiden Skills korrekt als Singletons und kompiliert (`make test` grün); die Zielmengen-Kopplung
(Emit-Test 16 · Fixture 17 in-scope · Pre-Flight auto-gekoppelt) ist über alle drei Achsen konsistent
und gegen den realen vendored Satz nachgerechnet; smoke.sh Positiv/Negativ sind widerspruchsfrei;
Mutation 30 patcht/kompiliert/färbt den benannten Test rot; die Skills bleiben ADR-0006-konform Fetch;
die Gate-Neutralität ist durch die emittierte `.d-check.yml` (`.harness/**` ignoriert) belegt. Offen
für die **Verifikation** (nicht Reviewer-Rolle): `make smoke`/`make full-smoke` als reale 0-Befunde-
Bestätigung von N-6, sowie `make mutate` als realer Rot-Lauf von N-4.
