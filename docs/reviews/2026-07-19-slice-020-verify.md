# Verifikation — slice-020 (regelwerk-check auf d-check `sources` umgestellt)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths` / ids-`exempt-paths`) —
> bare IDs/Pfade hier ok. Bewusst ohne Markdown-Links, damit `links`/`anchors` nichts findet.

## Kopf-Metadaten

- **Rolle:** unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Gegenstand:** DoD-Behauptung von slice-020 (5 Punkte) gegen realen Zustand + Plan-vs-Code + ADR/Hard Rules.
- **Slice:** `docs/plan/planning/in-progress/slice-020-dcheck-sources.md` (noch in-progress/, DoD-Boxen offen — Verify vor Closure).
- **Impl-Commit:** `b1ff1cb` (ein Content-Commit); vorangehend `bb41443` (reiner Move open→in-progress, 0 ins/0 del). HEAD=`b1ff1cb`, tracked Tree clean.
- **Berührte Dateien (b1ff1cb):** `.d-check.yml`, `Makefile`, `test/sources-pin.bats` (neu), `harness/conventions.md`, der Slice selbst. `AGENTS.md` / `harness/README.md` bewusst NICHT berührt (kein neuer Gate-Name).
- **Selbst ausgeführt (belegt, nicht nur nachgelesen):**
  - `make docs-check` (netzlos, `--network none`) → 72 Datei(en), 0 Befund(e), Exit 0.
  - Zahn-Test Kopplung: `.d-check.yml`-`sha256` 1 Zeichen verfälscht → `make test` → **Test 54 `not ok`**, Exit 2; revert; `git diff` leer.
  - `make gates` SELBST einmal (Cache warm), nichts parallel → Exit 0 (inkl. Kopplungs-bats 54/55 grün).
  - `make regelwerk-check` (Netz) → 72 Datei(en), 0 Befund(e), Exit 0 — 0 Drift gegen das echte v3.5.0-Asset.
  - Zahn-Test sources-Sensor: `.d-check.yml`-`sha256` verfälscht → `make regelwerk-check` → `source-drift` (1 Befund, Exit 2); revert.
  - `git show b1ff1cb -- Makefile harness/conventions.md` (Body-Diff), `git show bb41443 --stat` (reiner Rename).

## DoD-Verdikt je Zeile

### DoD-1 — Vorbedingung d-check ⩾ v0.51.0 gepinnt — **CONFIRMED**

`d-check.mk` (`DCHECK_IMAGE ?= …:v0.51.1`, `DCHECK_DIGEST ?= sha256:fede3d027b2ebc1dd8534460853e57b67cc7a9a182cad2e2138c8eebf7a2d03c`, Z. 15-16)
und `internal/emit/emit.go` (`DefaultImage`/`DefaultDigest`, Z. 33-34) pinnen v0.51.1 ⩾ v0.51.0. Diesen Pin liefert
slice-021 (MR-012), das bereits in done/ liegt (Commit `ebeba5d`). Empirischer Beleg, dass `sources` an diesem Pin
tatsächlich existiert und nicht nur behauptet ist: der verfälschte-Pin-Lauf feuerte einen echten `source-drift`-Befund
(ein nicht existentes Modul erzeugte einen Config-/Enable-Fehler, keinen Drift-Befund). Vorbedingung erfüllt.

### DoD-2 — `.d-check.yml` `sources`-Block (unpack: none), NICHT in `modules:`; docs-check netzlos 0 Befunde — **CONFIRMED**

`.d-check.yml` Z. 71-74: `sources:` mit `url: …/v3.5.0/lab-regelwerk.zip`, `sha256: 123e3383…f8ff` (== `BASELINE_ZIP_SHA256`),
`unpack: none`. `modules:` (Z. 12) = `[links, anchors, ids, matrix, codepaths, spans]` — `sources` **nicht** enthalten.
`make docs-check` netzlos (`--network none`) → **72 Datei(en), 0 Befund(e), Exit 0**, KEIN `source-unreachable`: `sources`
ist im Gate inert (nicht in `modules:`, kein `--enable` im docs-check-Recipe). Der Netzausschluss des Gates bleibt
unberührt (LH-QA-01). `unpack: none` = Roh-Byte-Hash-Raum, deckungsgleich mit dem kanonischen Makefile-Pin (MR-007).

### DoD-3 — `regelwerk-check`-Körper ersetzt (Bash raus, Name/Interface bleiben); Netz-Grenze; Netz-Lauf 0 Drift — **CONFIRMED**

`git show b1ff1cb -- Makefile`: der komplette Bash-Körper (`mktemp`/`curl -fsSL`/`sha256sum -c`) ist **entfernt** und ersetzt
durch `docker run --rm -v … $(DCHECK_REF) --enable sources --disable links … --disable spans` (auf `sources` isoliert; die
sechs `modules:`-Doku-Prüfer sind disabled). Kein `curl`/`sha256sum` mehr im Target → Host-Toolchain raus, Docker-only
(ADR-0003). Target-Name unverändert `regelwerk-check`. **Nicht** in `gates` (Z. 108: `baseline-verify docs-check lint build
test shell-lint record-gates` — `regelwerk-check` fehlt; grep bestätigt 0 Treffer). `make regelwerk-check` (Netz, kein
`--network none`) → **72 Datei(en), 0 Befund(e), Exit 0** — 0 Drift gegen das reale Asset. Zahn-Beleg: verfälschter Pin →
`.d-check.yml:72 … source-drift`, 1 Befund, Exit 2 → der Netz-Sensor holt/hasht real, kein stilles Grün.

### DoD-4 — Zwei-Pin-Kopplung, fail-closed in `gates` — **CONFIRMED**

`test/sources-pin.bats` koppelt (a) `sources`-`sha256` in `.d-check.yml` == `Makefile` `BASELINE_ZIP_SHA256` und (b)
`sources`-`url` trägt `BASELINE_TAG`. Netzlos (reiner Datei-Vergleich), läuft in `make gates` (via `make test` → bats
über `test/`). **Zähne selbst verifiziert:** `.d-check.yml`-`sha256` 1 Zeichen (`1→2`) verfälscht → `make test` →
`not ok 54 sources-sha256 in .d-check.yml == Makefile BASELINE_ZIP_SHA256 (Kopplung, MR-013)` (test file
`test/sources-pin.bats`, line 22), Exit 2. Nach Revert `git diff` leer. `BASELINE_ZIP_SHA256` lebt kanonisch im
`Makefile` (Z. 34), MR-007 Setzung 1 unberührt; `.d-check.yml` trägt die Duplikat-Kopie, die der Test fail-closed
klammert. `baseline-freshness` (Tag-Achse) und `baseline-verify` (lokale Kopie) im Impl-Commit **nicht** berührt (andere Achsen).

### DoD-5 — `make gates` grün (netzlos, inkl. Kopplungstest); `make regelwerk-check` (Netz) grün; MR-013; Closure-Notiz — **CONFIRMED (Closure-Notiz PENDING, erwartungsgemäß)**

`make gates` SELBST (Cache warm, nichts parallel) → **Exit 0**: `baseline-verify` + `docs-check` (0 Befunde, netzlos) +
`lint` + `build` + `test` (Go-Tests `cmd`/`internal/emit`/`internal/fetch` grün + bats 55/55 inkl. Kopplung 54/55) +
`shell-lint` + `record-gates`. `make regelwerk-check` (Netz) grün (s. DoD-3). MR-013 vorhanden (s. DoD-6).
**Closure-Notiz (§7) + Steering-Loop-Eintrag noch leer** — das ist der Post-Verify-Closure-Schritt (§5: „… + Verifikation
+ Closure-Notiz → nach done/"), korrekt aufgeschoben; kein VIOLATED, aber offen bis zum Move nach done/.

## Zusatz-Checks (über die DoD-Zeilen hinaus)

### DoD-6-Substanz — MR-013 + Doku, frozen MR-Historie unverändert — **CONFIRMED**

`git show b1ff1cb -- harness/conventions.md`: **rein additiv** — ein Hunk, der `### MR-013 …` vor `## Modus-Deklaration`
anhängt. MR-004/006/007 (und alle übrigen) byte-unverändert; keine Umschrift der HISTORIE-Blöcke, kein Rename-Churn.
MR-013-Anker löst auf: `make docs-check` (Module `anchors` + `ids` link-policy always) grün über den Slice, der MR-013
zweimal verlinkt (`#mr-013--regelwerk-check-auf-d-check-sources-tool-statt-skript`). Target-Name `regelwerk-check`
unverändert → die ~15 Referenzen (README/AGENTS/Makefile/conventions) churnen nicht.

### DoD-7 — ADR-0003 / Hard Rules — **CONFIRMED**

- **ADR-0003 Docker-only:** Host-`curl`/`sha256sum` aus `regelwerk-check` entfernt; das Target ruft jetzt nur `docker run`. Docker-only-Linie gewahrt.
- **HR 3.1 (keine halluzinierten/Netz-Gates):** `regelwerk-check` (Netz) ist **nicht** in `gates`; `gates` blieb offline-grün (`--network none` auf docs-check, netzlose bats). Kein neuer Gate-Name in AGENTS §4 / README §Sensors (Impl-Commit berührt beide nicht) — deckt Plan §3.
- **HR 3.3 (Move + Rewrite = zwei Commits):** `bb41443` = reiner Rename `{open => in-progress}` (0 ins/0 del); der Content-Rewrite ist `b1ff1cb`. Getrennt.
- **HR 3.5 (Gate nicht ohne ADR lockern):** Es wurde **verschärft**, nicht gelockert — `gates` gewinnt den fail-closed Kopplungstest (54/55); `modules:` unverändert, kein Modul deaktiviert, keine Schwelle gesenkt. Kein ADR nötig.

## Tree-Hygiene

`git diff` leer, `git status --short` zeigt nur diese untrackte Report-Datei (+ den Reviewer-Report). Beide Verfälschungen
(`.d-check.yml`-`sha256`) revertiert; `.d-check.yml` und Tree byte-identisch zu HEAD `b1ff1cb`.

## Gesamt-Verdikt

**Alle 5 DoD-Zeilen CONFIRMED. 0 VIOLATED.** Einzige Offenheit: die Closure-Notiz (§7) + Steering-Loop-Eintrag sind
erwartungsgemäß noch leer (Post-Verify-Closure-Schritt vor dem Move nach done/) — kein Verstoß, aber vor dem Move zu
füllen. Substanziell (Config, Target, Kopplung, Doku, Hard Rules) ist der Slice verifiziert; die Sensor-Zähne (Kopplung
netzlos + `sources` im Netz) sind unabhängig belegt, nicht nur nachgelesen.
