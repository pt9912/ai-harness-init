# Review-Report: slice-005 (Root-README emittieren) — 2026-07-21

**Review-Art:** Code — Diff gegen Plan + Hard Rules (`AGENTS.md` §3).
DoD-/Spec-Konformität ist bewusst NICHT geprüft (Verifier-Rolle, Modul 11).

**Gegenstand:** slice-005 · Commits `4f81ccb` (reiner Move open→in-progress) +
`d5a7f07` (Inhalt) · Diff-Range `f6613e2..d5a7f07`.

**Skill:** `.harness/skills/reviewer.md` @ v1.2.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad) -->
**Modell:** claude-opus-4-8[1m] · **Datum:** 2026-07-21

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde):

- Slice-Plan: [slice-005](../plan/planning/done/slice-005-root-readme.md)
- Kontext-ADR: [ADR-0005](../plan/adr/0005-ziel-repo-distribution.md) (Herkunftsklassen; slice-005 berührt KEINE ADR — Emit-Schritt)
- Hard Rules: [AGENTS.md](../../AGENTS.md) §3 (3.1/3.2/3.3/3.6)
- Konventionen: [harness/conventions.md](../../harness/conventions.md) (`MR-014` CI/smoke, `MR-008` cp-Disziplin)
- Betroffene IDs: `LH-FA-05`, `LH-FA-01`, `LH-QA-01`
- Vorherige Findings am gleichen Modul: keine slice-005-Vorläufe (Erst-Review dieses Slice).

---

## Findings

### F-1 — Emit-Logik + Tier-1-Tests liegen in `internal/emit/`, nicht wie im Plan-Table unter `cmd/`

- `kategorie`: INFO
- `quelle`: Slice-Plan §3 (Plan-vor-Code-Tabelle)
- `pfad`: `internal/emit/readme.go:1`, `internal/emit/readme_test.go:1`, `cmd/ai-harness-init/main_test.go:260`
- `befund`: Die §3-Tabelle nennt `cmd/ai-harness-init` (update) und
  `cmd/ai-harness-init/readme_test.go` (neu). Die Implementation legt die
  Emit-Funktion `emit.RootReadme` + die Tier-1-Tests stattdessen in
  `internal/emit/readme.go` / `readme_test.go` (dieselbe Paket-Lage wie
  `DocGate`/`Templates`/`BaselineVerify`) und ergänzt nur den Pre-Flight-Test
  `TestRun_ReadmeKollisionSchreibtKeinEmit` in `cmd/…/main_test.go`. Der
  Plan-Intent (README-Emit + Stempelung + Test) ist vollständig gedeckt, kein
  Scope-Verlust; die Datei-Lage folgt dem etablierten emit-Paket-Muster statt
  dem Plan-Wortlaut. Reine Plan-vs-Impl-Ablage-Abweichung, kein Defekt.
- `verifizierbar`: nein — Ablage-Konvention, kein Gate prüft sie.

### F-2 — `projectReadmeSet()` (Tier-1-Fixture) hat keinen Drift-Wächter gegen die reale Vorlage

- `kategorie`: INFO
- `quelle`: `LH-QA-01` / AGENTS §3.6 (Zusage vs. Abdeckung)
- `pfad`: `internal/emit/readme_test.go:14-26`
- `befund`: Die neue dedizierte Fixture `projectReadmeSet()` bildet
  `project-readme.template.md` nach, ist aber — anders als `courseSet()`, das
  `test/courseset-fixture.bats` gegen den realen vendored Satz koppelt — durch
  KEINEN Test an die reale Vorlage gebunden. `TestRootReadme_LinksGateSicher`
  belegt damit die Emit-Transformation (Strip + Stempel erhält Gate-Sicherheit),
  NICHT dass die reale Kurs-Vorlage gate-sicher ist. Driftet die reale Vorlage
  (Link auf ein nicht-co-emittiertes Ziel, Umbau des Hinweis-Blocks), fängt das
  KEIN offline-`make gates`-Gate — nur `make smoke` (Schritt 4, real docs-check;
  nicht in `gates`, aber pro Push via CI/`MR-014`). Die Test-Kommentare
  (`readme_test.go:22-24`) labeln diese Tier-1/Tier-2-Teilung ehrlich; die reale
  Achse ist damit bewacht (smoke/CI), nur nicht offline. Konsistent mit dem
  etablierten Tier-Modell des Repos — dokumentierte Grenze, kein stilles Grün.
- `verifizierbar`: ja — `make smoke` (Tier-2) röte bei realer Link-Drift; ein
  offline `make gates` täte es nicht.

## Negativbefunde

- geprüft, ohne Befund: **Hard Rule 3.3** — `4f81ccb` ist ein reiner Rename
  (`similarity 100%`, `git show`: 0 Inhaltsänderung), getrennt vom Inhalts-Commit
  `d5a7f07`. Der Inhalts-Commit trägt die Lifecycle-Move-Link-Reparaturen mit,
  bewegt aber die Slice-Datei selbst nicht.
- geprüft, ohne Befund: **Gate-Sicherheit / `LH-QA-01` (der kritische Punkt)** —
  die REALE Vorlage `.harness/baseline/v3.5.0/templates/project-readme.template.md`
  trägt nach `StripHintBlock` exakt drei Markdown-Links (`AGENTS.md`,
  `harness/README.md`, `spec/lastenheft.md`, Zeile 45–46), alle drei co-emittiert
  (`AGENTS.template.md`→`AGENTS.md`, `harness/README.template.md`→`harness/README.md`,
  `spec/lastenheft.template.md`→`spec/lastenheft.md`; alle in `inScope`). KEIN Link
  auf ein fehlendes Ziel. Der einzige externe `https://`-Kurs-URL (Zeile 8) lebt
  IM Hinweis-Block; `StripHintBlock` findet den Blockquote positions-unabhängig
  (im realen Template steht er NACH der H1, Zeile 3–8), entfernt ihn samt
  Folge-Leerzeile und lässt die gestempelte H1 stehen. Die „gate-sicher"-Zusicherung
  ist real gedeckt, nicht nur behauptet.
- geprüft, ohne Befund: **`emit.RootReadme`-Korrektheit** — spiegelt die
  Singleton-Transformation sauber (`stampName(StripHintBlock(...), name)`, identisch
  zu `planTemplates`); `name==""` lässt den Platzhalter stehen (Content-Urteil);
  Quelle-fehlt liefert einen umschlossenen Fehler (`… lesen: %w`). Eine
  fehlgewurzelte Baseline fällt bereits in Phase 3 (`checkRoot` via `TemplateTargets`)
  und Phase-4-`Templates` auf, VOR dem `RootReadme`-Read.
- geprüft, ohne Befund: **Force-Boundary** — `RootReadme` prüft ohne `force` per
  `os.Stat` und bricht bei vorhandener README ab (`… existiert bereits`), gleiches
  Muster wie `DocGate`/`Templates`; `TestRootReadme_ForceBoundary` belegt beide
  Zweige (ohne force unverändert, mit force überschrieben).
- geprüft, ohne Befund: **slice-025-Garantie** — `README.md` liegt via
  `emit.RootReadmePath` im Phase-3-Pre-Flight (`emitTargets`, `main.go:221`); der
  `RootReadme`-Write liegt in Phase 4 NACH `Templates` (`main.go:182`). Eine
  README-Kollision bricht damit VOR jedem Emit-Write ab; `TestRun_ReadmeKollisionSchreibtKeinEmit`
  belegt es (Exit 1 + „README.md existiert bereits" + Verifier nicht geschrieben).
  Die Phase-4-Teil-Bootstrap-„ehrliche Grenze" (`main.go:158-161`, Runtime-Abbruch
  nach Teil-Writes) ist unverändert und nicht neu durch slice-005 — nicht re-erhoben.
- geprüft, ohne Befund: **Hard Rule 3.6 (Mutationen 24/25)** — beide vom
  glob-`mutate.sh` (`$CASES_DIR/*.sh`) auto-entdeckt (25/25), gültige
  `# files:`/`# expect:`-Köpfe, kein `# verify:` (defaultet korrekt auf `test` —
  beide Wächter sind Go-Unit-Tests). `24` ersetzt `StripHintBlock(string(content))`
  → `string(content)`: der Hinweis-Block überlebt → `TestRootReadme_StampStrip` rot
  (über „Template-Hinweis"/„https://"). `25` streicht `, emit.RootReadmePath` aus
  `emitTargets`: die README-Kollision entgeht dem Phase-3-Pre-Flight → Phase 4
  erreicht `DocGate` (docker, läuft VOR `RootReadme`) und scheitert mit
  NICHT-passender Meldung → `TestRun_ReadmeKollisionSchreibtKeinEmit` rot über die
  stderr-Prüfung. Beide sed-Muster treffen reale Zeilen und kompilieren; `25` ist
  strukturgleich zur belegten Mutation `23` (slice-004b).
- geprüft, ohne Befund: **Smoke-Claim „0 hinzugefügt, 2 aufgelöst (5→3)"** —
  plausibel und statisch bestätigt: die drei ausgehenden README-Links lösen auf
  (0 neue Befunde); zwei EINGEHENDE Links auf das Root-README existierten schon
  (`AGENTS.template.md:70` `](README.md)`, `harness/README.template.md:37`
  `](../README.md)`) und waren vor slice-005 tot → mit dem emittierten `README.md`
  lösen genau 2 auf. smoke Schritt 3 prüft zusätzlich die reine Existenz am Ziel-Root.
- geprüft, ohne Befund: **Hard Rules 3.1/3.2** — kein halluziniertes Gate
  (das emittierte README verweist nur auf co-emittierte Ziele + Inline-Code-Pfade;
  die „Gates:"-Zeile ist ein `<Platzhalter>`, kein Link); keine Lint-Suppression
  (`//nolint`/`shellcheck disable`) in `readme.go`/`smoke.sh`. Kein ADR berührt
  (3.4/3.5 nicht einschlägig).
- geprüft, ohne Befund: **Lifecycle-Move-Links (4)** — `welle-03`:
  `open/`→`in-progress/slice-005-…` (slice-005 jetzt in `in-progress/`); slice-005
  ×3: `slice-024-…`→`../open/slice-024-…` (slice-024 real in `open/` verifiziert).
  Alle vier lösen relativ korrekt auf; `links`-Modul deckt sie (nicht
  `docs/reviews/`-exempt). Der pre-existente `roadmap`-Link (`slice-005:9`,
  `../in-progress/roadmap.md`) ist post-Move redundant, aber nicht gebrochen und
  nicht Teil des Diffs.
- geprüft, ohne Befund: **Plan-Scope** — der Diff deckt den §3-Intent (README-Emit
  + Stempelung + Test) plus smoke Schritt 3, Mutationen 24/25 und die vier
  Lifecycle-Links; kein Scope-Creep, kein Merge, kein fehlender Plan-Punkt (die
  Ablage-Abweichung ist F-1).

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 2 |

## Verdikt

**Merge-blockierend:** nein — keine HIGH/MEDIUM. F-1 ist eine benigne
Plan-vs-Impl-Ablage-Abweichung (emit-Paket-Muster statt Plan-Wortlaut, kein
Scope-Verlust), F-2 die ehrlich gelabelte Tier-1/Tier-2-Teilung (reale Achse
durch smoke/CI bewacht). Der Diff ist **konform** zu Plan und Hard Rules: der
Move (`4f81ccb`) ist rein 3.3-getrennt, die Mutationen 24/25 färben real rot
(3.6), die Gate-Sicherheit der REALEN Vorlage ist verifiziert (drei Links
co-emittiert, externer URL im gestrippten Block, `LH-QA-01`), und die
slice-025-Garantie „Kollision → kein Teil-Bootstrap" hält für das
Root-README-Ziel.

**Übergabe:** Findings gehen an die Implementation. Der Report ersetzt keine
Verifikation — DoD-/Spec-Konformität prüft der Verifier separat (Modul 11).
