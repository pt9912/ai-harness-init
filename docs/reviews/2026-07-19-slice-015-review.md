# Code-Review — slice-015 (Zitat-Verifikation via d-check `codepaths.check-lines`)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths`) — bare IDs/Pfade hier ok.

## Kopf-Metadaten

- **Gegenstand:** slice-015 re-scope + Implementierung — d-check-Pin v0.46.0 → v0.50.0,
  `codepaths.check-lines: true`, Emitter-Pin nachgezogen, neuer MR-011, `d-check.mk` neu
  aus `--print-mk`.
- **Diff/Commit-Range:** `git diff 792ab10..0f87af4` (drei Commits: `929721d` re-scope,
  `dd0bff6` move open→in-progress, `0f87af4` impl). HEAD = `0f87af4`, Arbeitskopie clean.
- **Betroffene Dateien:** `.d-check.yml`, `d-check.mk`, `internal/emit/emit.go`,
  `harness/conventions.md`, `docs/plan/planning/in-progress/slice-015-zitat-sensor.md`.
- **Rolle:** unabhängiger Reviewer (Modul 10), frischer Kontext — Code nicht selbst geschrieben.
- **Pflicht-Kontext (v3.1.0):** Diff-Range · `LH-QA-01`/`LH-QA-02`/`LH-QA-03` (Bezug im Slice)
  · aktive ADRs (keine referenziert/berührt) · Hard Rules AGENTS.md §3 · vorherige
  Findings am gleichen Modul (Doc-Gate/d-check-Pin: slice-014 M1/M2/R1-L1/L3, MR-009/MR-010)
  · Slice-Plan `docs/plan/planning/in-progress/slice-015-zitat-sensor.md`.
- **Ausgeführt (read-only):** `make docs-check` (v0.50.0, netzlos) → **62 Datei(en), 0 Befund(e),
  Exit 0**; `docker run … @sha256:9b80…94364 --print-mk` (Regenerations-Vergleich);
  `docker buildx imagetools inspect` + `docker image inspect` (Digest-Belege). **Nicht**
  gelaufen: `make gates` (Implementer belegt grün; `record-gates` würde kollidieren) und
  `make test` (statt dessen die Koppel-Tests statisch gelesen).

## Findings

### INFO-1 — Plan-Defekt-Rücksprung mit dem Impl-Commit gebündelt

- **kategorie:** INFO
- **quelle:** Maintainability / Modul 9 (Plan-Rücksprung)
- **pfad:** `docs/plan/planning/in-progress/slice-015-zitat-sensor.md:62` (+ Tabellen-Zeile
  `:90`) — eingebracht in `0f87af4`
- **befund:** Die fehlende DoD-Zeile + Tabellen-Zeile für `internal/emit/emit.go` wurden
  zusammen mit der Implementierung (`0f87af4`) nachgezogen, nicht als vorangehender
  Doc-only-Schritt. Im Commit-Text ausdrücklich als „Plan-Defekt-Rücksprung, Modul 9"
  offengelegt. **Kein** Hard-Rule-Verstoß (3.3 betrifft nur `git mv`+Inhalt; hier kein Move),
  und der emittierte Pin ist über den Koppel-Test ohnehin erzwungen — der „Doc-führt"-
  Audit-Trail ist lediglich in einem Commit verdichtet statt zeitlich vorgelagert.
- **verifizierbar:** nein (Prozess-Beobachtung, kein Gate). **Nicht merge-blockierend.**

## Negativbefunde (geprüft, ohne Befund)

- **Hard Rule 3.3 (Move getrennt vom Inhalt):** `dd0bff6` ist ein **100%-reiner Rename**
  (`git show --summary` → `rename … (100%)`, 0 Inhalts-Zeilen); die Content-Rewrites liegen
  getrennt in `929721d` (open/) und `0f87af4` (impl). Konform.
- **Hard Rule 3.4 (ADR-Immutabilität):** keine Datei unter `docs/plan/adr/` im Range berührt;
  historische MR-009/MR-010-Bodies bleiben **eingefroren** auf v0.46.0/v0.10.0
  (`conventions.md:353,394`) — nur §Baseline + neuer MR-011 tragen v0.50.0. Korrekte
  Immutable-Historie-Disziplin.
- **Hard Rule 3.5 (Gate-Lockerung nur per ADR):** `check-lines: true` ist **additive
  Härtung** (fügt eine Prüfung hinzu), keine Schwellen-Senkung/Modul-Deaktivierung. Kein
  ADR nötig (MR-001-Philosophie: Gate-*Anheben* → Steering-Loop). Die fünf `--disable
  citations` betreffen nur die **advisory**-Recipes und stammen **verbatim vom Tool**
  (kein Repo-Loosening am `docs-check`-Gate).
- **Hard Rule 3.1 (keine halluzinierten Gates):** `check-lines` reitet auf dem **aktiven,
  nicht-leeren** `codepaths`-Modul (`roots: [spec, docs, harness]`) und ist **kein** neuer
  Gate-Name — `AGENTS.md` §4 und `harness/README.md` §Sensors sind im Range **nicht
  berührt** (`git diff --name-only` leer). `docs-check` läuft grün über 62 Dateien. Die
  Nicht-Aktivierung des `citations`-Moduls (0 `d-check:cite`-Direktiven → nie feuerndes
  Gate) ist stichhaltig begründet. Konform.
- **Hard Rule 3.2 (Lint-Suppression):** kein `//nolint`, kein `# shellcheck disable` im
  Diff; `--disable citations` ist ein d-check-Modul-Flag (Tool-Recipe), keine Lint-Suppression.
- **`d-check.mk`-Regeneration faithful:** `--print-mk` (aus `@sha256:9b80…94364`) vs.
  committetes `d-check.mk` unterscheidet sich in **exakt** den vier dokumentierten
  MR-010-Adaptionen (adaptierter Kopfkommentar; `DCHECK_DIGEST`-Pin; `doc-check`→`docs-check`
  Target+Hilfetext; `doc-help`-Grep `docs?-`) und **nichts** sonst. Die fünf
  `--disable citations` erscheinen im Roh-vs-Committed-Diff als **unveränderter Kontext**
  (verbatim, je 5/5). Roh-Output emittiert selbst `DCHECK_IMAGE ?= …:v0.50.0`.
- **Digest = v0.50.0 (dreifach belegt):** lokaler RepoDigest (`docker image inspect` →
  `@sha256:9b80…94364`) · `imagetools inspect ghcr.io/pt9912/d-check:v0.50.0` → identischer
  Digest · Tool-Selbstauskunft (`--print-mk`-Default `:v0.50.0`). Übereinstimmend.
- **Emitter-Kopplung:** `emit.DefaultImage`/`DefaultDigest` (`emit.go:33-34`) sind byte-gleich
  zu `d-check.mk` `DCHECK_IMAGE`/`DCHECK_DIGEST`; `TestDefaultImage_MatchesCanonical` /
  `TestDefaultDigest_MatchesCanonical` (`emit_test.go:30-45`) lesen die kanonische Quelle
  `../../d-check.mk` und erzwingen Gleichheit + `sha256:`-Präfix. Konform.
- **Emittierte Starter-Config minimal:** `internal/emit/templates/d-check.yml` bleibt
  `modules: [links, anchors]`, `codepaths` auskommentiert → **kein** `check-lines` im
  Zielrepo (Emitter ≠ Dogfood). Korrekt.
- **MR-011-Konsistenz:** alle IDs als Anker-Links (kein bare ID); Anker
  `#mr-011--zitat-verifikation-via-d-check-adoptiert-check-lines` löst auf die Überschrift
  auf; Struktur analog MR-009/MR-010 (Datum · Geltungsbereich · Adaption · Trockenlauf ·
  Auflösungs-Trigger). Der grüne `docs-check`-Lauf (ids/matrix/links aktiv) bestätigt die
  Auflösbarkeit sämtlicher IDs/Anker/Pfade im Diff.
- **Kein neuer Gate-Name behauptet:** weder `AGENTS.md` §4 noch `harness/README.md` §Sensors
  im Range verändert; `d-check.mk`-Kopfkommentar benennt weiterhin `docs-check` als einziges
  behauptetes Target.

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 1 |

## Verdikt

**Merge NICHT blockiert.** Kein HIGH/MEDIUM-Finding. Der Diff ist gegen Plan, Hard Rules
(§3.1–3.5) und den MR-Block konform: faithful `--print-mk`-Regeneration mit ausschließlich
den vier dokumentierten MR-010-Adaptionen, Digest dreifach als v0.50.0 belegt, Emitter-Pin
korrekt gekoppelt und die emittierte Config bewusst minimal, `check-lines` als additive
Härtung auf nicht-leerem `codepaths` (kein halluziniertes Gate, kein neuer Gate-Name),
`citations`-Aufschub stichhaltig begründet, Move sauber vom Inhalt getrennt. Das einzige
Finding (INFO-1) ist eine offengelegte, nicht-blockierende Prozess-Notiz.

**Hinweis Rollen-Trennung:** DoD-Abhakung und Gate-Lauf-Bestätigung (u. a. der
Pin-inert-Trockenlauf (a) ohne `check-lines`, `make gates`-Grün, die zwei live
zeilen-geprüften Frozen-Refs) sind Verifikations-Territorium (Modul 11), hier bewusst nicht
abgehakt.
