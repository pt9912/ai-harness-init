# Impl-Review — slice-019: Re-Baseline v3.1.0 → v3.5.0 (Kurs-Welle 26→32)

## Kopf-Metadaten

- **Gegenstand:** slice-019 — committet vendored Baseline `v3.1.0` → `v3.5.0` (MR-007).
- **Implementierungs-Commit:** `fa41121` (`slice-019: Re-Baseline v3.1.0->v3.5.0 vendoren + Kopplungspunkte (MR-007)`).
- **Rolle:** unabhängiger Reviewer (Modul 10), frischer Kontext — Code nicht selbst geschrieben.
- **Prüf-Vertrag:** Diff **gegen Plan + ADR + Hard Rules** (`.harness/skills/reviewer.md`). **Nicht** geprüft: DoD-Abhakung (Verifier-Sache).
- **Slice-Plan:** `docs/plan/planning/in-progress/slice-019-rebaseline-v350.md`.
- **Bezug:** `LH-QA-01`, `LH-QA-02`, `LH-QA-03`, `LH-FA-04`, `MR-007` (Setzungen 1/2/4), `MR-008`; Hard Rules 3.1/3.3/3.5.
- **Datum:** 2026-07-19.
- **Fremd-Blob-Regel:** der vendored Baum wurde auf **Integrität + Provenienz** geprüft, **nicht** Zeile für Zeile (Plan §6, Setzung MR-007).

## Selbst gefahrene Verifikation (Evidenz)

| Prüfung | Kommando / Methode | Ergebnis |
|---|---|---|
| **Provenienz (Setzung 1)** | `curl` Release-Asset → `sha256sum` | `123e3383…f8ff` == `Makefile BASELINE_ZIP_SHA256` ✅ |
| Release echt (kein Draft/Prerelease) | `gh api …/releases/tags/v3.5.0` | `draft=false`, `prerelease=false`, `published 2026-07-19T11:03:31Z`, Asset-`digest` == Pin ✅ |
| **Integrität (Setzung 2)** | `make baseline-verify` | `v3.5.0 OK — 42 Dateien (Integritaet + Vollstaendigkeit, netzlos)` ✅ |
| SHA256SUMS-Format | `head`/`wc`/`grep` | 42 Zeilen, Pfade **relativ** zu `<tag>/` (`regelwerk/…`), Datei selbst **nicht** gelistet ✅ |
| SHA256SUMS-Konsistenz | `cd <tag> && LC_ALL=C sha256sum -c SHA256SUMS` | exit 0, alle 42 `: OK` ✅ |
| Baum == ZIP-Inhalt | ZIP nach `/tmp` entpackt, `diff -rq` regelwerk + templates | beide **identical** (byte-gleich) ✅ |
| ZIP-Struktur | `find` | `regelwerk/` + `templates/` top-level, 21+21 = 42 Dateien ✅ |
| **Setzung 4 (ein Tag)** | `ls .harness/baseline/` | nur `v3.5.0/` vorhanden, `v3.1.0/` weg ✅; bats-Test 11 erzwingt „zwei `<tag>` → rot" |
| Kopplung `BASELINE_TAG` | Makefile-Diff | `v3.1.0` → `v3.5.0` ✅ |
| Kopplung `fetch.DefaultTag` | `make test` (Go `internal/fetch`) | `ok` → `TestDefaultTag_MatchesBaseline` grün ⇒ `DefaultTag == BASELINE_TAG == v3.5.0` ✅ |
| Kopplung conventions.md §Baseline | Diff + Cross-Check | Tag/URL/„Welle 32 · 2026-07-19"; Wert deckt sich mit `regelwerk/README.md:3` „Stand: Kurs-Welle 32 · 2026-07-19" ✅ |
| Kopplung README.md:26 | Diff | Literal-Pfad `.harness/baseline/v3.1.0/` → `v3.5.0/` ✅ |
| **Emit-Embed** | 15 `internal/emit/skel`-Dateien einzeln `diff` gegen `…/templates/<rel>` | alle 15 **byte-identisch**; bats 51 (Gleichheit) / 52 (Vollständigkeit) / 53 (Set-Index nicht eingebettet) grün ✅ |
| reviewer.md-Claim | `diff -u` modul-10 `v3.1.0`↔`v3.5.0` | 103↔103 Zeilen; **nur 2 Änderungen**: `Quelle:`-URL-Bump + **ein** Link-Label `templates/`-Präfix (Link-Ziel unverändert) ⇒ Claim „substanziell unverändert" **belegt** ✅ |
| `make test` gesamt | Docker-`test`-Stage | Go 3/3 `ok`; alle bats `ok` (Guard, Inject, Encode, Verify, Freshness, skel-Drift) — kein `not ok` ✅ |

## Findings

### HIGH

Keine.

### MEDIUM

Keine.

### LOW

Keine.

### INFO

- **INFO-1 · Maintainability · `internal/fetch/fetch_test.go:50-58,64,76,94,97,119`.**
  Die Tests übergeben `"v3.1.0"` als `tag`-Argument an `fetch.Skeleton(…)` und
  verwenden `ai-harness-course-3.1.0/…`-Tarball-Präfixe in den Fixtures. Diese
  Literale sind **funktional inert**: der Fetcher ist gemockt (`fixtureFetch`, Signatur
  `func(_ context.Context, _ string)`) und ignoriert den Tag; die reale Kopplung an die
  Baseline leistet allein `TestDefaultTag_MatchesBaseline` (liest `BASELINE_TAG` aus dem
  Makefile). **Kein Failure-Szenario** — `make test` ist grün, kein Gate hängt daran.
  Latente Lese-Drift (ein Leser könnte die Tests fälschlich als v3.1.0-gepinnt lesen);
  nicht im Plan-Scope (Plan §3 nannte nur `DefaultTag`). Dokumentiert, nicht blockierend.
  `verifizierbar`: nein (kein Gate bestätigt/widerlegt es).

- **INFO-2 · MR-007 / Plan §6 · `docs/plan/planning/in-progress/roadmap.md:7`,
  `open/slice-004b-skeleton-wire.md:6`, `open/slice-005-root-readme.md:5`,
  `welle-01-offline-kern.md:25,33`, `welle-02-fetch-und-readme.md:35`.**
  Diese **live** Planungs-Docs tragen weiterhin `blob/v3.1.0/kurs/…`-Prozess-Links
  bzw. „v3.1.0-Konvention", während die Baseline auf v3.5.0 steht. Der Plan behandelt
  genau diese Klasse **explizit** (§3 Vollständigkeits-Analyse, §6 „Kurs-Prozess-Links"):
  kanonischer Kurs-Pin ist **allein** `conventions.md §Baseline`; live Docs führen die
  Links als *auxiliare* Referenz und ziehen nur mit, wenn sie ohnehin angefasst werden —
  **kein** repo-weiter Zwangs-Bump (das wäre die Wartungsfalle aus slice-015 §6). Also
  **bewusste, dokumentierte Won't-Fix-Setzung**, kein übersehener Scope. Bei nächster
  Berührung ziehen sie mit. `verifizierbar`: nein (kein ID-Muster, kein Gate).

## Negativbefunde (geprüft, ohne Befund)

- **Provenienz (Setzung 1):** Release-Asset frisch gezogen, sha256 == Pin; Release ist
  echtes v3.5.0 (kein Draft/Prerelease), GitHub-Asset-Digest deckt sich. Kein Befund.
- **Integrität (Setzung 2):** `make baseline-verify` grün; `sha256sum -c` alle 42 OK;
  SHA256SUMS-Format korrekt (relativ zu `<tag>/`, `LC_ALL=C`, Datei selbst aus); Baum
  byte-gleich zum ZIP-Inhalt (`diff -rq` leer). Kein Befund.
- **Setzung 4 (ein Tag):** nur `v3.5.0/` präsent, `v3.1.0/` entfernt; bats-Test 11
  erzwingt die Setzung. Kein Befund.
- **Kopplungspunkte:** `BASELINE_TAG`, `fetch.DefaultTag` (Tier-1-Drift-Test grün),
  `conventions.md §Baseline` (Tag/URL/Welle-Datum, cross-checked gegen regelwerk-README),
  `README.md:26`, Emit-Embed (15/15 byte-gleich, skel-Drift 51/52/53 grün) — alle
  vollständig und konsistent nachgezogen. Kein Befund.
- **reviewer.md (Stilles-Grün-Klasse, kein Gate fängt es):** Label-Bump auf v3.5.0
  korrekt; Behauptung „Modul 10 §Ziel-Form substanziell unverändert" **unabhängig gegen
  die v3.1.0-Fassung geprüft** und bestätigt (nur Quelle-URL + ein Link-Label); die fünf
  Pflicht-Punkte / Output-Schema / Kategorien inhaltlich unverändert; Versionierung
  1.1.0→1.2.0 additiv (Historie erhalten). Kein Befund.
- **Stille Drift (Rest-`v3.1.0`):** übrige Referenzen sind entweder **eingefroren
  historisch** korrekt (`done/`-Slices, MR-Body `conventions.md:275`, Timeline
  `conventions.md:16`, Changelog-Kommentar `reviewer.md:9`) oder die INFO-2-Klasse. Keine
  gehört fälschlich un-gebumpt in den Scope. Kein Befund.
- **Hard Rule 3.1 (keine halluzinierten Gates):** kein neues Gate behauptet;
  `baseline-verify`/`test` real und grün auf diesem Checkout. Kein Befund.
- **Hard Rule 3.3 (git mv + Rewrite = zwei Commits):** die Baseline-Ersetzung ist
  **eine** logische MR-007-Operation (alter Tag-Baum gelöscht, neuer angelegt), **kein**
  git-mv+Edit einer Quelldatei. Die Rename-Detection hat trotz Content-Change **gegriffen**
  (93–99 % Similarity je Modul; die 2 stark geänderten Dateien — `SHA256SUMS`,
  `templates/Makefile` — sauber als delete+create) — **kein** Absturz unter die
  Similarity-Schwelle, kein Historieverlust. Die Slice-Datei-Moves sind laut Auftrag eigene
  Commits. Kein Verstoß.
- **Hard Rule 3.5 (Gate-Lockerung nur per ADR):** keine Schwelle gesenkt, keine
  Modul-Aktivierung zurückgenommen. Kein Befund.
- **ADR-Treue:** keine aktive ADR verletzt; `ADR-0001` (Skelett-Distribution) / `ADR-0003`
  (Docker-only) unberührt bzw. eingehalten (nur `make`-Targets gefahren). Kein Befund.

## Kategorie-Summary

| Kategorie | Anzahl | Blockierend? |
|---|---|---|
| HIGH | 0 | — |
| MEDIUM | 0 | — |
| LOW | 0 | — |
| INFO | 2 | nein (beide dokumentiert / bewusst) |

## Verdikt

**KONFORM — nicht merge-blockierend.** Provenienz (Setzung 1), Integrität (Setzung 2)
und Ein-Tag-Regel (Setzung 4) sind unabhängig verifiziert; alle sechs Kopplungspunkte
(`BASELINE_TAG`, `fetch.DefaultTag`, `conventions.md §Baseline`, `README.md:26`,
Emit-Embed, `reviewer.md`) sind vollständig und konsistent nachgezogen; der reviewer.md-
„substanziell unverändert"-Claim ist gegen die alte Modul-10-Fassung belegt. Kein HIGH/
MEDIUM/LOW. Die zwei INFO-Punkte sind bewusste, im Plan (§6) verankerte bzw. funktional
inerte Beobachtungen ohne Failure-Szenario. `make gates` bewusst nicht gefahren
(record-gates-Kollision) — DoD-Gate-Nachweis ist Verifier-Sache.
