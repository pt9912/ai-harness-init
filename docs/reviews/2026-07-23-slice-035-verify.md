# Verifikation — slice-035 (CLI-Phasierung, `--lang` optional)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths` / ids-`exempt-paths`) —
> bare IDs/Pfade hier ok. Bewusst ohne Markdown-Links, damit `links`/`anchors` nichts findet.

## Kopf-Metadaten

- **Rolle:** unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Gegenstand:** DoD-Behauptung von slice-035 (6 prüfbare Punkte, §2) gegen realen Zustand + Spec + Plan + ADR-0007 (Entscheidung 1 + Fitness Functions) + Byte-Determinismus. Der Reviewer (getrenntes Artefakt, 2026-07-23-slice-035-review.md) prüfte gegen Plan+ADR+Hard-Rules (0 HIGH/MEDIUM, 1 INFO); der Verifier bestätigt DoD+Spec.
- **Slice:** docs/plan/planning/in-progress/slice-035-cli-phasierung.md (noch in-progress/, DoD-Boxen offen — Verify VOR Closure).
- **Impl-Commit:** 37d2930 (14 Dateien, +357/−220). HEAD=4debc2d (Review-Report darüber). tracked Tree clean VOR und NACH allen Läufen (git status leer, auch nach mutate).
- **Selbst ausgeführt (belegt, nicht nur nachgelesen), SEQUENZIELL (mutate hält Lock, kein Parallel-Lauf):**
  - `make gates` SELBST → **Exit 0** (go test aller 5 Pakete ok — cmd/emit/fetch/gen/wire; shellcheck + actionlint ok). git status leer davor.
  - `make full-smoke` SELBST → **Exit 0**; 4 OK-Zeilen inkl. der doc-only-Zeile (unten DoD-3/4).
  - `make mutate` SELBST → **Exit 0, 38 ok / 0 Befund(e)**; git status leer davor UND danach. Grün-Vorlauf fuhr `make test` + `make ci-lint` + `make smoke` real grün.
  - Realer sprachloser Bootstrap in ein frisches tmp-Repo SELBST gefahren (Fragment-Set inspiziert, unten DoD-2/3).
  - Statische Belege: internal/emit/makefile.go, internal/gen/golang.go, internal/wire/wire.go, cmd/ai-harness-init/main.go, harness/tools/full-smoke.sh, spec/architecture.md §4.1.

## DoD-Verdikt je Zeile

### DoD-1 — `--lang` optional: fehlt es, KEIN Exit 2, sprachloser Bootstrap (LH-FA-01) — **CONFIRMED**

Im Code: cmd/ai-harness-init/main.go:74-91 — der frühere `--lang`-Refuse-Block (Exit 2 bei fehlendem `--lang`) ist entfernt; run() reicht direkt an bootstrap durch, `hasLang := lang != ""` (main.go:111) verzweigt sprachlos. Netzloser Wächter: TestRun_SprachlosKeinExit2 (main_test.go:108-128) fährt `run([]string{}, …)` OHNE `--lang`, assertiert `code != 2` und `code == 1` (Phase-3-Kollision via .d-check.yml), UND dass kein `.harness/skeleton` angelegt wird. §3.6-Zahn: `make mutate` Fall 41 (`sed 's/hasLang := lang != ""/hasLang := true/'`) → sprachlos liefe gen.Generate("") → UnknownLangError → Exit 2 → **TestRun_SprachlosKeinExit2 rot gesehen** (mutate 41 ok). Realer sprachloser Lauf: `ai-harness-init --name x` (ohne `--lang`) → Exit 0, Ausgabe „sprach-agnostisch (doc-only Gate)". Kein Exit 2. Der Wächter prüft die Eigenschaft (Exit-Code-Klasse), kein Impl-Detail — kann unter der benannten Mutation rot werden und tat es.

### DoD-2 — Aggregator aus Init-Emitter (`emit.Makefile`), IMMER; `gen.goProfile` trägt KEINE Root-`Makefile` mehr (LH-FA-04) — **CONFIRMED**

Const-Move vollständig: `aggregatorMakefile` lebt NUR in internal/emit/makefile.go:22-45; internal/gen/golang.go:32-40 `goProfile` liefert exakt {go.mod, Dockerfile, harness/mk/go.mk, .golangci.yml, cmd/app/main.go} — KEIN `"Makefile"`-Schlüssel. emit.Makefile (makefile.go:51) wird in emitAll IMMER gerufen (main.go:219), vor dem `hasLang`-Zweig für wire.Place. Wächter am Ziel: TestGenerate_GoProfile (gen_test.go:30) nagelt den Datei-Satz OHNE Makefile fest; TestGenerate_GoMkFragment (gen_test.go:98-100) assertiert explizit, dass gen KEINE Root-Makefile erzeugt; TestPlace_PlacesVerbatim (wire_test.go:78-80) assertiert, dass wire keine Root-Makefile platziert. Kein Wächter an toter gen-Ausgabe (die slice-034-F-1-Klasse): TestGenerate_AggregatorHasOrderEdge ist ersatzlos entfernt, der Ordnungskanten-Wächter lebt als TestMakefile_HasOrderEdge in internal/emit/makefile_test.go am emit-Const. Real belegt: im sprachlosen Ziel liegt die Root-`Makefile` (Aggregator) OHNE go.mod/Skelett; im `--lang go`-Ziel dieselbe Aggregator-Makefile + zusätzlich das Skelett — beide Läufe grün (full-smoke Exit 0).

### DoD-3 — `make full-smoke` doc-only: sprachlos `make gates` grün (docs-check + baseline-verify + record-gates, KEIN lint/build/test, kein Skelett) (LH-QA-01) — **CONFIRMED**

full-smoke SELBST → Exit 0. Der doc-only-Block (harness/tools/full-smoke.sh:122-157) bootstrappt ein zweites tmp-Repo OHNE `--lang`, fährt `make -j gates` und belegt ehrlich: (a) Exit 0; (b) die sprach-agnostischen Marker `geprüft` + `Integritaet + Vollstaendigkeit` PRESENT (real gemessen: „baseline-verify: v3.5.0 OK — 42 Dateien" + „d-check: 12 Datei(en) geprüft, 0 Befund(e)" im Ziel-Output); (c) KEIN `--target (lint|build|test)` im Output ABSENT-assertiert; (d) kein Skelett-Artefakt (go.mod/cmd/app/main.go/harness/mk/go.mk/Dockerfile) am Ziel. Selbst gefahrener sprachloser Bootstrap bestätigt das Fragment-Set unter harness/mk/: exakt {baseline.mk, doc-gate.mk, enforce.mk} — KEIN go.mk. Der Glob `include harness/mk/*.mk` matcht also sprachlos nur die drei sprach-agnostischen Fragmente → GATE_CHECKS = baseline-verify + docs-check, record-gates via enforce.mk-Rezept; kein lint/build/test. Kein halluziniertes Code-Gate (ABSENT-Assertion + leerer Glob-Anteil), kein stiller Verlust (die zwei Marker PRESENT-assertiert). OK-Zeile real: „full-smoke: OK — sprachloser Init (ohne --lang) faehrt make -j gates doc-only gruen (docs-check + baseline-verify, KEIN Code-Gate, kein Skelett) — --lang optional (slice-035/LH-FA-01)." Deckt ADR-0007 Fitness Function „Doc-only-Gate".

### DoD-4 — `make full-smoke` mit `--lang go` bleibt grün (One-Shot, wie slice-034) — **CONFIRMED**

full-smoke Block 2/3+3/3 (full-smoke.sh:35-120) bootstrappt mit `--lang go --name full-smoke`, fährt `make -j gates` und findet alle fünf Silent-Subset-Marker (`--target lint`, `--target build`, `--target test`, `geprüft`, `Integritaet + Vollstaendigkeit` — real im Output gesehen) + den geschlossenen Gate-Nachweis-Kreis (record-gates-Stempel == frische working-tree-hash-Berechnung, .harness/.gitignore greift) + den greifenden Command-Guard (go build geblockt, make test durchgelassen). Exit 0. Der One-Shot ist unverändert grün; die Relocation hat ihn nicht gebrochen.

### DoD-5 — `make gates` grün (Dogfood) — **CONFIRMED**

`make gates` SELBST → Exit 0. go test aller 5 Pakete ok (cmd/emit/fetch/gen/wire), shellcheck + actionlint ok. Der Dogfood-Makefile behält seine eigene Nicht-Aggregator-Form (kein Selbst-Umbau). Kein Silent-Grün-Pfad beobachtet.

### DoD-6 — Doku architecture.md §1/§4.1 (Init sprachlos) trägt es; conventions.md-MR nötig? — **CONFIRMED (Doku trägt; keine MR nötig, Begründung tragfähig)**

spec/architecture.md §1 (Zeilen 12-16: „ein sprach-agnostischer Init legt die Harness … an, … doc-only-Gate; Sprache … kein Init-Argument") + §4.1 („Init — sprach-agnostisch", Sequenz mit „ai-harness-init --name X [ohne Sprache]" → „Gate-Fragmente + Aggregator, Guard mit Boden" → „make gates grün auf reinen Docs") tragen den sprachlosen Init konsistent mit ADR-0007 Entscheidung 1 (bereits als ADR-0007-Nachzug emittiert). `make docs-check` grün. **Keine conventions.md-MR nötig:** die conventions.md-MRs dokumentieren, wo der DOGFOOD-Repo von der Ziel-Form abweicht (MR-005 harness/tools-Layout, MR-010 d-check.mk-Fragment). slice-035 verschiebt eine INTERNE Emitter-Zuständigkeit (welches Go-Paket die Root-Makefile schreibt: gen→emit) — kein Dogfood-Layout-Bruch, keine neue Repo-Abweichung. MR-010 beschreibt weiterhin korrekt das d-check.mk-Fragment (unberührt) und behauptet NICHT, dass gen den Aggregator trägt. Die „keine MR"-Begründung des Implementers trägt.

### Zusatz — Closure-Notiz (§7 des Slice)

Noch leer (Slice in in-progress/). Die DoD-Zeile „Closure-Notiz mit Steering-Loop-Lerneintrag" ist ein Post-`done/`-Schritt (Modul 5 Lifecycle) — erwartet offen zur Verify-Zeit, kein Verify-Blocker.

## Byte-Determinismus (LH-QA-02)

CONFIRMED. TestGenerate_Deterministic (gen_test.go:44-57) vergleicht den VOLLEN Datei-Satz byte-für-byte über zwei gen-Läufe — lief in `make test` (Teil von gates, Exit 0) UND im mutate-Grün-Vorlauf grün. Der Aggregator ist eine statische Const `aggregatorMakefile` in emit (kein Zeitstempel, keine Map-Iteration im Inhalt); emit.Makefile schreibt sie verbatim (makefile.go:61). TestMakefile_Emits (makefile_test.go:32) assertiert die emittierte Datei == AggregatorMakefile(). Keine nicht-deterministische Emit-Quelle beobachtet.

## Verifier-only-Klassen (gezielt gesucht)

- **Property-vs-Impl-Detail (§3.6-Falsch-Beispiel):** Alle vier neuen/verschobenen Wächter prüfen eine Eigenschaft und wurden unter ihrer benannten Mutation rot gesehen: 41→TestRun_SprachlosKeinExit2 (Exit-Code-Klasse), 38→TestMakefile_HasOrderEdge (Ordnungskanten-Substring `record-gates: $(GATE_CHECKS)` am emit-Const), 14→TestRun_FetchKollisionSchreibtNichts (Phase-1-Pre-Flight via fetchTargets), 23→TestRun_SkeletonKollisionSchreibtKeinEmit (Phase-3-Pre-Flight via wire.Targets). Kein zahnloser Namens-Proxy.
- **Halluziniertes Code-Gate sprachlos:** ausgeschlossen — der sprachlose Glob `include harness/mk/*.mk` matcht empirisch nur {baseline,doc-gate,enforce}.mk (selbst inspiziert); full-smoke ABSENT-assertiert `--target (lint|build|test)` + jedes Skelett-Artefakt. Kein Check kann sprachlos still fehlen: die zwei sprach-agnostischen Marker sind PRESENT-assertiert, fehlte die Ordnungskante hinge gates nur an record-gates (ohne Prereqs) und beide Marker fehlten → rot.
- **Aggregator-Relocation vollständig (slice-034-F-1-Klasse):** JA. grep-verifiziert: `aggregatorMakefile` nur in emit/makefile.go, `goProfile` ohne `"Makefile"`. Kein Test an toter gen-Ausgabe — TestGenerate_AggregatorHasOrderEdge ersatzlos entfernt, Mutation 38 zeigt auf internal/emit/makefile.go (verifiziert am Mutations-Skript). Zusätzlich prüfen TestGenerate_GoMkFragment + TestPlace_PlacesVerbatim aktiv die ABWESENHEIT einer Root-Makefile aus gen/wire.
- **Pre-Flight-Lücke durch hasLang-Konditionalisierung:** ausgeschlossen. emitTargets (main.go:253-279) nimmt emit.MakefilePath IMMER in den Phase-3-Pre-Flight (auch sprachlos); die Skelett-Ziele (wire.Targets) NUR bei hasLang — korrekt, denn sprachlos gibt es kein Skelett. Eine kollidierende Root-Makefile fällt sprachlos wie mit Sprache vor dem Emit auf. Phase-1-fetchTargets nimmt `.harness/skeleton` NUR bei hasLang (main.go:116-118) — konsistent (kein Skelett-Fetch sprachlos). Beide Pre-Flight-Klassen (14/23) unter Mutation rot gesehen. Kein Teil-Bootstrap-Durchlass im sprachlosen Pfad.
- **Mutation 22 weg + Deckung nicht mehr nötig:** JA. 22 (`22-wire-gates-guard.sh`) mutierte die wire-Makefile-Vorbedingung; diese existiert nach dem reinen-Placer-Umbau nicht mehr (wire.go trägt keine Makefile-Vorbedingung/`skeletonMakefile`-Const/`bytes`-Import). Es gibt nichts mehr zu mutieren; TestPlace_NoGatesTarget ist konsistent entfernt (die geprüfte Vorbedingung ist weg). Kein Deckungs-Loch: die verbleibende wire-Eigenschaft (reiner Placer, keine Root-Makefile) deckt TestPlace_PlacesVerbatim.
- **Review-INFO (F-1: konvergent vs. Refuse):** echter Deferral, keine heutige DoD-Lücke. emit.Makefile behält den Kollisions-Refuse (existiert bereits ohne force, makefile.go:53-60) — konsistent mit ALLEN aktuellen Emittern (alle refusen). Die Idempotenz-Klassifikation (ADR-0007 Entscheidung 3, konvergent) ist im Slice-Plan §1/§6 ausdrücklich als Folge-Slice ausgewiesen; die slice-035-DoD (§2) verlangt sie NICHT. Kein Gate deckt Idempotenz in diesem Slice (per Plan out-of-scope). Bewusst aufgeschoben, nicht defekt.

## Gesamturteil

**DoD BESTÄTIGT.** Alle sechs DoD-Punkte CONFIRMED (DoD-6 mit tragfähiger „keine MR"-Begründung). Real gemessen: `make gates` Exit 0 · `make full-smoke` Exit 0 (4 OK-Zeilen inkl. der doc-only-Zeile „sprachloser Init … doc-only gruen … KEIN Code-Gate, kein Skelett"; alle Silent-Subset-Marker im `--lang go`-Block + Stempel-Match) · `make mutate` 38 ok / 0 Befund (Fälle 41/38/14/23 je ihren benannten Wächter rot gesehen). Byte-Determinismus (LH-QA-02) durch TestGenerate_Deterministic gedeckt (grün); der Aggregator ist eine statische emit-Const ohne Zeitstempel. Fall 22 obsolet + seine Deckung nicht mehr nötig (mutierte Vorbedingung existiert nicht mehr). Die Aggregator-Relocation gen→emit ist vollständig — kein Wächter an toter gen-Ausgabe. Tree vor und nach jedem Lauf clean, keine Mutation zurückgeblieben. Das Reviewer-INFO (F-1) ist ein per Plan/ADR bewusst aufgeschobener Folge-Slice, keine heutige DoD-Verletzung.
