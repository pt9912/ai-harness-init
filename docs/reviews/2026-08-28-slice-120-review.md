# Review nach Modul 10 — slice-120 (`CO-003` wird vollzogen)

**Rolle:** Reviewer (Modul 8/10, frischer Kontext). **Datum:** 2026-08-28.
**Gegenstand:** `e312ba0^..2df3c79` (sechs Commits), HEAD `2df3c79`, Arbeitsbaum sauber
(`git status --porcelain` → leer vor Beginn dieses Laufs).

## Eingangs-Kontext (die fünf Pflicht-Punkte + Slice-Plan)

- **Diff/Range:** `e312ba0` · `dbc84ee` · `1f0a4a0` · `dbe5e50` · `48c2063` · `2df3c79`.
  **9** Dateien, **0** davon außerhalb `*.md`
  (`git diff --name-only e312ba0^..2df3c79 | grep -v '\.md$' | wc -l` → **0**),
  alle unter `docs/`.
- **`LH-*`:** der Diff referenziert **keine** Anforderungs-ID im geänderten Text; die einzige
  `LH-`-Nennung in der bewegten Datei ([`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6))
  stand dort vor dem Move und wurde nur im Pfad nachgezogen.
- **Aktive ADRs:** keine im Diff genannt
  (`git log -p e312ba0^..2df3c79 | grep -E '^\+' | grep -oE 'ADR-[0-9]{4}' | sort -u` → leer).
  Gelesen als Constraint: [`ADR-0015`](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md)
  (Accepted) — sie nennt **keinen** Carveout (`grep -ci 'carveout' …` → **0**, Exit 1).
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §3.2, §3.3, §3.4, §3.5, §3.6, §3.7, §3.8, §3.9 —
  jede unten mit eigenem Kommando.
- **Vorherige Findings am gleichen Modul:** [`2026-08-27-slice-117-review.md`](2026-08-27-slice-117-review.md),
  [`2026-08-27-slice-117-review-runde2.md`](2026-08-27-slice-117-review-runde2.md). Runde 2 führt
  bereits einen `MR-025`-Befund **über `CO-003`** („Mess-Kommando wandert nicht mit"). Dieselbe
  Klasse kehrt hier dreimal wieder — siehe §Muster.
- **Slice-Plan:** [`slice-120-co-003-wird-vollzogen.md`](../plan/planning/done/slice-120-co-003-wird-vollzogen.md),
  vollständig gelesen. **Nicht** geprüft: die DoD-Abhakung (Verifikation, getrennter Kontext).

**Auflagen dieses Laufs eingehalten:** kein `make mutate` (fremdes Protokoll gelesen, siehe
§Negativbefunde), kein `git push`, nichts committet außer diesem Report. Jeder **Check** lief über
ein `make`-Ziel im gepinnten Image — auch die Läufe über die **drei** historischen Stände
`dbc84ee`, `dbe5e50` und `1f0a4a0`, ausgepackt per `git archive` in den Scratchpad und dort mit
`make -C <scratch> docs-check` gefahren; der Host-Baum blieb dabei unberührt.
**Offengelegt, weil es dafür kein `make`-Ziel gibt:** die Pfad-Normalisierung der Ebenen-Prüfung
(§Negativbefunde, Punkt 2) lief als `python3`-Einzeiler auf dem Host. Das ist kein Check, kein
Build und kein Gate, und es fällt unter keine der in [`AGENTS.md`](../../AGENTS.md) §3.9
aufgezählten Toolchains — der PreToolUse-Guard nimmt andere Interpreter ausdrücklich aus. Es steht
hier, damit die Zusage „Docker-only" nicht mehr deckt, als sie deckt.

---

## Findings

### F-0 (bekannt, verifiziert) — `48c2063` nennt einen Move-Commit, den es nicht gibt

- **Kategorie:** MEDIUM (vom Auftraggeber vorab gemeldet; hier nur bestätigt und **eingegrenzt**)
- **Quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 (Commit-Message als Zusage)
- **Pfad:** Commit-Message `48c2063`, Zeile 3: *„Inhalts-Commit zum Move 0f8d1a1 (AGENTS 3.3 getrennt)."*
- **Befund:** `git rev-parse -q --verify 0f8d1a1^{commit}` → Exit 1; der Move ist `dbe5e50`.
- **Eigene Eingrenzung (der eigentliche Auftrag):** über **alle sechs** Messages und **alle neun**
  berührten Dateien geprüft — je 7–40-stelliger Hex-Token gegen `git rev-parse -q --verify
  "<t>^{commit}"`. **Genau ein** nicht auflösbarer Commit-Verweis, nämlich dieser. Die zehn
  „MISS"-Treffer in `docs/reviews/**` und `slice-105` sind Dezimalzahlen (PIDs, Zeitstempel,
  `1000000`), keine Hashes. Aufgelöst und korrekt: `6bbea9f` (2×), `60b438b`, `48c2063`,
  `ba5c07f`, `6020941`, `9b9866b`.
- **Verifizierbar:** ja — `git rev-parse -q --verify 0f8d1a1^{commit}; echo $?` → `1`.

### F-1 — `48c2063` sagt zu, zwei Dateien nicht anzufassen, und fasst eine davon im selben Commit an

- **Kategorie:** **HIGH**
- **Quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 — Hard Rule, die **Commit-Message** ausdrücklich
  als Zusage-Träger führt (*„Eine Zusage — Doc-Kommentar, Test-Name, DoD-Punkt, Commit-Message —
  ist erst fertig, wenn benannt ist, was passieren müsste, damit sie bricht, und das einmal rot
  gesehen wurde"*); Falsch-Beispiel derselben Form dort: *„»Byte-Gleichheit belegt `make smoke`«,
  ohne `smoke` gelesen zu haben"*
- **Pfad:** Commit-Message `48c2063` (*„Die zwei VERIFIKATIONS-Berichte bleiben unberuehrt, und
  das ist gemessen, nicht kulant"*) gegen `docs/reviews/2026-08-27-slice-117-verify.md:618`
- **Befund:** Derselbe Commit ändert `docs/reviews/2026-08-27-slice-117-verify.md`
  (`git show --stat 48c2063 | grep -c 'slice-117-verify\.md'` → **1**; `1 +-`). Die Zusage ist
  durch das `--stat` des eigenen Commits widerlegt und wurde offensichtlich nicht dagegen
  gehalten. Zwei Neben-Zusagen desselben Absatzes fallen mit: die Aufzählung *„Eingehend gezogen:
  slice-117, slice-105, slice-118, slice-120 und die zwei REVIEW-Berichte"* deckt zusammen mit den
  zwei separat genannten Posten (Index, bewegte Datei) genau **acht** der **neun** Dateien im
  `--stat` ab; die neunte ist `…verify.md` und kommt in keiner Aufzählung vor. Und *„sie nennen
  den Pfad als Inline-Code"* gilt nur für **einen**
  der zwei Berichte — `docs/reviews/2026-08-27-slice-117-verify-runde2.md` nennt den Pfad in
  **keiner** Form (`grep -c 'CO-003-mutate-ohne-zeitschranke' …verify-runde2.md` → **0**, Exit 1;
  über `…verify.md` → **1**).
- **Failure-Szenario:** Ein späterer Lauf (Verifikation, Re-Baseline-Audit, Carveout-Audit)
  rekonstruiert aus der Message, welche Zeitdokumente bewusst eingefroren blieben, schließt die
  zwei Verifikations-Berichte aus der Prüfung aus — und übersieht, dass einer von ihnen still
  umgeschrieben wurde und seither eine falsche Statusaussage trägt (F-2).
- **Verifizierbar:** ja — `git show --stat 48c2063 | grep verify`.

### F-2 — Der Eingriff in `…-verify.md` war nicht gate-erzwungen und macht die Zeile falsch

- **Kategorie:** MEDIUM
- **Quelle:** [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  §Geltungsbereich (*„`docs/reviews/**` … **Zeitdokumente**, die eine Messung zu ihrem Datum
  festhalten und darum **nicht nachgezogen werden**"*); [`.d-check.yml`](../../.d-check.yml)
  Zeile 57–59 (*„exempt-paths (datei-weit): Zeitdokumente `docs/reviews/**` **frieren den Stand
  ihres Review-Laufs ein**; Lifecycle-Pfade … veralten **per Definition**"*); Slice-Plan §3-Tabelle,
  Zeile *„`docs/reviews/2026-08-27-slice-117-verify.md` und `…-verify-runde2.md` | **unverändert**"*
- **Pfad:** `docs/reviews/2026-08-27-slice-117-verify.md:618`
- **Befund:** Die Zeile lautet seit `48c2063`
  „`docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md` steht auf **Status: Aktiv**".
  Sie ist gegen **jeden** Baum falsch: zum Datum des Berichts lag die Datei nicht unter `done/`,
  und an HEAD steht sie nicht auf *Aktiv*
  (`grep -c '^\*\*Status:\*\* Aktiv' docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md`
  → **0**, Exit 1). **Erzwungen war der Eingriff nicht:** der Zwischenlauf aus DoD (2) —
  von mir über `git archive dbe5e50 | tar -x` und `make -C … docs-check` **reproduziert**:
  `d-check: 422 Datei(en) geprüft, 72 Befund(e)` — meldet **null** Befunde in den zwei
  Verifikations-Berichten (`grep 'target-missing' … | cut -d: -f1 | sort -u` → **8** Dateien, keine
  davon `*verify*.md`). Die zwei **Review**-Berichte dagegen waren rot (**3** bzw. **2** Befunde)
  und mussten gezogen werden — das ist korrekt und deckt sich mit dem Präzedenzfall `ba5c07f`.
- **Failure-Szenario:** Wer den eingefrorenen Bericht als Beleg heranzieht — er ist die Quelle für
  B-10 der slice-117-Verifikation —, liest eine Statusaussage, die weder den damaligen noch den
  heutigen Baum beschreibt, und kann die Drift nicht mehr von einer echten Regression trennen.
  `codepaths` fängt sie strukturell nicht (`docs/reviews/**` ist ausgenommen).
- **Verifizierbar:** ja — `git show 48c2063 -- docs/reviews/2026-08-27-slice-117-verify.md` plus
  der reproduzierte Zwischenlauf über `dbe5e50`.

### F-3 — Zwei Kommandos im lebenden Slice-Plan wurden umgeschrieben, ihre Zahlen nicht

- **Kategorie:** MEDIUM
- **Quelle:** [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  Setzung 1 (*„… trägt im selben Absatz das Kommando, das **genau sie** ausgibt, und wer sie
  schreibt, hat es über dem Baum gefahren, von dem sie spricht"*). Der Slice-Plan liegt **im**
  Geltungsbereich: `docs/plan/planning/in-progress/**` ist dort **nicht** ausgenommen (draußen sind
  nur `docs/reviews/**`, `docs/plan/planning/done/**`, `.harness/baseline/**`, `*.template.md`).
- **Pfad:** `docs/plan/planning/in-progress/slice-120-co-003-wird-vollzogen.md:110` und `:205`
- **Befund:** `48c2063` zieht in beiden Zeilen den Pfad **im Kommando** nach und lässt die Zahl
  stehen.
  - `:110` — *„**40** Ziele, alle relativ"* neben
    `grep -o '](\([^)]*\))' docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md | grep -vc '](http'`;
    dasselbe Kommando an HEAD → **47**.
  - `:205` — *„nennt heute keine ID — `grep -c 'slice-120' docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md`
    → **0**"*; dasselbe Kommando an HEAD → **4**. Diese Zahl ist kein Beiwerk, sondern das
    **Bruch-Kriterium des Beginn-Triggers** (§4: *„Der Slice beginnt, wenn dasselbe Kommando ≥ 1
    liefert"*).
  Vor dem Nachziehen zeigte das Kommando auf einen nicht mehr existierenden Pfad und war damit
  sichtbar unbrauchbar; danach läuft es und liefert still einen anderen Wert.
- **Failure-Szenario:** Genau der Schaden, den `MR-025` §*Was der Schaden ist* benennt — ein Lauf,
  der die ausgewiesene Messung nachzählt, bekommt ein falsches Rot an einem korrekten Gegenstand
  und kann nicht entscheiden, ob das Kriterium falsch formuliert war oder der Baum gedriftet ist.
- **Verifizierbar:** ja — die zwei Kommandos an HEAD fahren und mit `sed -n '110p;205p' …` vergleichen.

### F-4 — Die aufgelöste Datei sagt an zwei Stellen weiter, ihr Vollzug stehe aus

- **Kategorie:** MEDIUM
- **Quelle:** Modul 7 §Ziel-Form (`.harness/baseline/v3.5.2/regelwerk/modul-07-carveouts.md`:
  *„Auflösen ohne Verschiebung ist eine zweite Lüge"* — hier die Umkehrung: Verschiebung ohne
  Auflösung im Text); `48c2063` behauptet für sich das Gegenteil (*„Ort, Index und Kopf sagen
  dasselbe"*)
- **Pfad:** `docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md:194` und `:222–229`
- **Befund:** Der Kopf (`:3`) sagt **Aufgelöst**. Zwei Stellen derselben Datei sagen weiter das
  Gegenteil:
  - `:194` — *„Der Architect-Teil ist mit dieser Prüfung erledigt, der Planner-Teil seit dem
    2026-08-28; **offen ist allein der Implementer-Teil**."* Die Tabelle darunter markiert die
    Planner-Zeile als *erledigt am 2026-08-28*, die Implementer-Zeile trägt keine solche Marke —
    obwohl genau dieser Teil der Gegenstand des Diffs ist.
  - `:222–229` — die **Geschichte**-Tabelle endet mit dem Eintrag vom 2026-08-28
    *„Modul-7-Übergang **aufgelöst** — Trigger eingetreten, **Vollzug ausstehend** und übergeben"*.
    Eine Zeile für den Vollzug selbst — das Ereignis dieses Slice — fehlt
    (`sed -n '222,229p' … | grep -c 'Vollzogen\|vollzogen'` → **0**, Exit 1).
- **Failure-Szenario:** Das nächste Carveout-Audit liest §Übergabe und §Geschichte — die zwei
  Abschnitte, die genau dafür da sind — und schließt, der Implementer-Teil sei offen; oder es
  vertraut dem Kopf und stellt fest, dass die Historie des Registers den größten Zustandswechsel
  des Carveouts nicht führt. Beide Male ist der Mechanismus für die Frage unbrauchbar, für die
  Modul 7 ihn baut.
- **Verifizierbar:** ja — `sed -n '3p;194p;222,229p' docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md`.

### F-5 — Index-Titel und Datei-Titel weichen voneinander ab

- **Kategorie:** LOW
- **Quelle:** `.harness/baseline/v3.5.2/templates/docs/plan/carveouts/README.template.md`
  (*„**Derivativ:** Quelle der Wahrheit sind die Carveout-Dateien"*); `48c2063`
  (*„Ort, Index und Kopf sagen dasselbe"*)
- **Pfad:** `docs/plan/carveouts/README.md:34` gegen
  `docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md:1`
- **Befund:** Die Index-Zeile führt den Titel in Vergangenheit (*„`make mutate` **hatte** keine
  Zeitschranke — ein hängender Worker **färbte** den Lauf nicht rot"*), die H1 der Quelle in
  Gegenwart (*„… **hat** keine Zeitschranke — … **färbt** den Lauf nicht rot"*). Die derivative
  Sicht weicht damit von ihrer Quelle ab. Bei `CO-001` und `CO-002` stimmen Index-Titel und H1
  zeichengleich überein (`head -1` je Datei gegen die Index-Zeile).
- **Failure-Szenario:** Wer den Index-Titel greppt, findet die Datei nicht; wer die H1 liest,
  liest eine Gegenwartsaussage, die der Status derselben Datei zwei Zeilen tiefer aufhebt.
- **Verifizierbar:** ja — `head -1 docs/plan/carveouts/done/CO-003-*.md; sed -n '34p' docs/plan/carveouts/README.md`.

### F-6 — Der Index-Kopf sagt weiter voraus, was dieser Slice getan hat

- **Kategorie:** LOW
- **Quelle:** Reviewer-Anker *„Doku-Drift"*; [`AGENTS.md`](../../AGENTS.md) §3.7 Cutoff dem Sinn
  nach (*„Wer eine solche Zeile ohnehin anfasst, zieht sie nach"* — die Datei ist in `48c2063`
  angefasst; der Buchstabe von §3.7 bindet Markdown nicht)
- **Pfad:** `docs/plan/carveouts/README.md:6–7`
- **Befund:** *„Aufgelöste Carveouts wandern per `git mv` nach done/ (**das Verzeichnis entsteht
  bei der ersten Auflösung**)."* Das Verzeichnis existiert seit `dbe5e50`
  (`test -d docs/plan/carveouts/done && echo da` → `da`). Die Klammer beschreibt eine Zukunft, die
  dieser Slice eingelöst hat.
- **Verifizierbar:** ja — `sed -n '6,7p' docs/plan/carveouts/README.md; ls -d docs/plan/carveouts/done`.

### F-7 — `48c2063` rechnet mit einer Zahl, die der Architect zuvor korrigiert hatte

- **Kategorie:** LOW
- **Quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 (Commit-Message als Zusage);
  Klasse aus [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  §Begründung (*„Die Fundorte liegen in Planungs-Text, in einer **Commit-Message** und in einem
  Mess-Zeitdokument"*)
- **Pfad:** Commit-Message `48c2063`, Absatz 6
- **Befund:** *„Von den **vier** offenen Checklisten-Punkten sind **zwei** gehakt (Move, Index).
  Offen bleibt **einer**."* — 4 − 2 = 2, die Message sagt 1. Der Ist-Stand am Eltern-Commit war
  **3**, nicht 4 (`git show dbe5e50:docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md |
  grep -c '^- \[ \] '` → **3**); mit 3 stimmt die Rechnung. Die 4 stammt aus dem Rot-Kommando des
  Slice-Plans, und `6bbea9f` hatte die Korrektur bereits ausdrücklich angesagt (*„sein
  DoD-(3)-Rotkommando `grep -c '^- \[ \] '` liest ueber dieser Datei 3 statt 4"*).
- **Verifizierbar:** ja — dasselbe `grep -c` über den fünf Ständen `60b438b`/`6bbea9f`/`dbe5e50`/
  `48c2063`/`2df3c79` → **4 / 3 / 3 / 1 / 0**.

### F-8 — `1f0a4a0` beruft sich auf eine Regel, die den Fall ausdrücklich nicht regelt, und zählt daneben

- **Kategorie:** LOW
- **Quelle:** [`AGENTS.md`](../../AGENTS.md) §3.8 Absatz 2 (*„**Über andere Norm-Artefakte sagt
  diese Regel nichts.** Wo eine Quelle die schreibende Rolle benennt, gilt sie unverändert; wo
  keine sie benennt, bleibt die Frage offen"*); §3.6 für die Zahl
- **Pfad:** Commit-Message `1f0a4a0`
- **Befund:** Zwei Aussagen des Begründungs-Absatzes halten der Messung nicht stand.
  1. *„Der Carveout ist Architect-Artefakt (AGENTS 3.8)"* — §3.8 bindet ausschließlich die Hard
     Rules dieser Datei und den Adaptions-Block und schließt jede Aussage über andere Artefakte
     ausdrücklich aus. `ADR-0015` nennt Carveouts nicht (`grep -ci 'carveout'` → **0**, Exit 1).
     Die tatsächlich tragende Quelle ist Modul 7 §Carveout-Audit-Slice, und die steht im
     Slice-Plan §6 — nicht in dieser Message.
  2. *„von 55 Link-Abgleich-Commits fassen **zwei** bereits Carveouts an (CO-002, CO-003)"* — die
     55 stimmen (`git log --all --format=%h --grep='Link-Abgleich' | wc -l` → **56**, minus dieser
     Commit). Die zwei nicht: von denselben 56 fassen **8** eine Datei unter
     `docs/plan/carveouts/` an, ohne diesen Commit **7**; beschränkt auf Commits mit
     „Link-Abgleich" in der **Betreffzeile** sind es **5** (`ba5c07f`, `8e612bd`, `cdfb7df`,
     `ba6c68d`, `fce7f65`). „Zwei" trifft nur die Zahl der **Carveouts**, nicht die der Commits,
     über die der Satz spricht.
- **Nicht widerlegt:** *„ADRs dagegen nie"* — von denselben 56 Commits berührt **keiner** eine
  Datei unter `docs/plan/adr/` (Schleife über alle 56, leere Ausgabe). Die Präzedenz trägt also;
  nur ihre Herleitung ist falsch beziffert und falsch belegt.
- **Verifizierbar:** ja — die drei genannten `git log`-Schleifen.

### F-9 — Vier `d-check:ignore`-Marker führen einen Grund, den dieser Slice aufgehoben hat

- **Kategorie:** LOW
- **Quelle:** Reviewer-Anker *„latente Wartungsfalle"*; `.claude/commands/implement-slice.md:31-32`
  (der Marker ist für die *geplante*, noch nicht existierende Datei gedacht)
- **Pfad:** `docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md:205`;
  `docs/plan/planning/in-progress/slice-120-co-003-wird-vollzogen.md:80`, `:105`, `:235`
- **Befund:** Alle vier tragen den Text
  `<!-- d-check:ignore (done/ entsteht erst bei erster Carveout-Auflösung) -->`. Das Verzeichnis
  existiert seit `dbe5e50`; der genannte Grund ist damit aufgehoben, die Unterdrückung bleibt
  bestehen. Sie ist heute wirkungslos — aber sie unterdrückt nicht mehr das, was sie zu
  unterdrücken vorgibt, und deckt ab sofort jede künftige Falschschreibung derselben Zeile
  stillschweigend mit. Denselben Wortlaut führen **drei** weitere Marker außerhalb dieses Diffs
  (`docs/plan/carveouts/CO-001-bats-shell-lint.md:86`,
  `docs/plan/planning/open/slice-113-co-001-ist-faellig.md:46`,
  `docs/plan/planning/done/welle-12-results.md:312`); sie sind hier nur als Fundort genannt, nicht
  als Befund — der letzte liegt in einem Zeitdokument und wird per Definition nicht nachgezogen.
- **Verifizierbar:** ja — `grep -rn 'd-check:ignore (done/' --include='*.md' docs/ | wc -l` → **7**
  ohne diesen Report (**9** mit ihm, da er den Marker zweimal zitiert); `ls -d docs/plan/carveouts/done`.

### F-10 — Die neue *Aufgelöst*-Tabelle verliert die Spalte, die `CO-003` seine erste Aussage nennt

- **Kategorie:** LOW
- **Quelle:** Reviewer-Anker *„Doku-Drift"*;
  `docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md:21`
  (*„**Betroffenes Gate: keines — und das ist die erste Aussage dieses Carveouts.**"*)
- **Pfad:** `docs/plan/carveouts/README.md:32` (Kopfzeile der neu angelegten Tabelle)
- **Befund:** Die drei Abschnitte des Index führen drei verschiedene Spaltensätze:
  *Aktiv* = `ID | Titel | Betroffenes Gate | Angelegt`; *Permanent* = `ID | Titel | Übergeführt in
  | Übergeführt am`; *Aufgelöst* (neu) = `ID | Titel | Angelegt | Aufgelöst | Wodurch`. Die
  Vorlage gibt für *Aufgelöst* keine Form vor (`README.template.md` führt dort `(noch keine)`), es
  liegt also kein Template-Verstoß vor. Beim Verschieben der Zeile fiel jedoch die einzige
  Angabe weg, die `CO-003` von jedem anderen Eintrag unterscheidet — *„keines (`make mutate` ist
  Nicht-Gate-Verify)"*; sie stand vor `48c2063` in der *Aktiv*-Zeile und steht in keiner Zeile
  mehr. Die Spalte *Angelegt* ist der einzige Übertrag aus dem alten Satz.
- **Failure-Szenario:** Ein Freshness-/Carveout-Audit, das den Index nach *„welcher Carveout senkte
  welches Gate"* liest, findet für aufgelöste Einträge keine Antwort mehr und muss jede Datei
  einzeln öffnen — der Grund, aus dem der Index derivativ geführt wird, entfällt für den Abschnitt.
- **Verifizierbar:** ja — `sed -n '14,16p;26,28p;32,34p' docs/plan/carveouts/README.md`.

### F-11 — Der Implementer schreibt in `docs/plan/planning/**`, das die Übergabe dem Planner zuweist

- **Kategorie:** INFO
- **Quelle:** `docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md:198`
  (Architect-Text, `6bbea9f`: *„Schnitte und `docs/plan/planning/**` gehören dem Planner"*);
  [`AGENTS.md`](../../AGENTS.md) §3.8 Absatz 2 (*„wo keine [Quelle] sie benennt, bleibt die Frage
  offen"*)
- **Pfad:** `1f0a4a0` (**eine** Datei) und `48c2063` (**vier** Dateien) unter `docs/plan/planning/`
- **Befund:** Der Slice arbeitet unter der Annahme, dass Link- und Kommando-Pflege in
  Planner-Dateien vom Implementer-Auftrag *„`git mv` und Config-Updates"* gedeckt ist. Die Annahme
  ist plausibel und in der Übergabe-Tabelle des Carveouts halb ausgesprochen (*„der Link-Abgleich
  fasst Dateien an, die anderen Rollen gehören"*), aber **nirgends als Regel niedergelegt** —
  und sie trägt in `48c2063` über den reinen Link hinaus bis in ein Mess-Kommando (F-3).
  §3.8 lässt die Frage ausdrücklich offen; ein Sensor existiert für sie nicht
  (`.d-check.yml` führt `links, anchors, ids, matrix, codepaths, spans`, keines liest Commits).
  **Kein Verstoß** — eine dokumentationswürdige, undokumentierte Annahme.
- **Verifizierbar:** nein (kein Gate-Lauf bestätigt oder widerlegt sie) — belegbar allein durch
  `git show --stat 1f0a4a0 48c2063`.

---

## Muster (Steering-Loop-Signal)

Drei der zwölf Positionen — F-0, F-1, F-7 — sind **dieselbe Klasse in derselben Commit-Message**
(`48c2063`): eine überprüfbare Tatsachenbehauptung, die niemand vor dem Commit gegen ihr eigenes
Kommando gehalten hat. F-3 und F-8 tragen die Klasse in den Slice-Plan und in `1f0a4a0`, und die
Runde-2-Review zu slice-117 hat sie am selben Gegenstand (`CO-003`) schon einmal gemeldet. Nach
Reviewer-Skill §Kontext-Eskalation ist damit die Schwelle erreicht, ab der nicht mehr der einzelne
Befund, sondern die fehlende **Trägerschaft** der Punkt ist — dieselbe Diagnose, die `MR-025`
§Begründung für sich selbst stellt. Ein Sensor über Commit-Messages existiert nicht und ist hier
**nicht** als Lösung vorgeschlagen; die Beobachtung gehört in die Closure-Notiz.

Bemerkenswert daneben: die **handwerklich schwierigen** Teile des Slice sind fehlerfrei. Die
Ebenen-Transformation, der Zwischenlauf und die Zahlen-Splits halten jeder Nachmessung stand
(§Negativbefunde). Was bricht, sind ausnahmslos Behauptungen **über** die Arbeit, nicht die Arbeit.

---

## Negativbefunde (geprüft, ohne Befund)

- **§3.3 (Move und Inhalt getrennt) — zweimal erfüllt.** Alle drei Moves sind reine Renames:
  `git show --raw --find-renames` liefert für `e312ba0`, `dbc84ee` und `dbe5e50` je **R100** bei
  identischem Blob-Hash (`4f17cff` bzw. `d280f39`) und `0 insertions, 0 deletions`. Die
  Inhalts-Commits `1f0a4a0` (vor dem Carveout-Move) und `48c2063`/`2df3c79` (danach) sind davon
  getrennt.
- **Die Ebenen-Regel `sed 's|](\.\./|](../../|g'` stimmt für **jedes** Ziel — und es gibt keines,
  das die Ebene nicht brauchte.** Über den Stand vor dem Move (`1f0a4a0`) trägt die Datei **44**
  relative Ziele (`grep -o '](\([^)]*\))' … | grep -vc '](http'` → **45**, davon eines ein
  Artefakt: das Suchmuster selbst steht als Code-Span im Text). Sie zerfallen in **39** mit
  `../`-Präfix (**27**× `../`, **3**× `../../`, **9**× `../../../` — deckungsgleich mit der
  Message) und **5** Geschwister-Links (4× `CO-001-bats-shell-lint.md`, 1× `README.md`). Jedes der
  39 zeigte aus `docs/plan/carveouts/` **hinaus**; keines löste nach dem Move ohne die zusätzliche
  Ebene korrekt auf. Entscheidend und separat geprüft: die Transformation ist **pfad-erhaltend** —
  ich habe jedes Ziel vorher aus `docs/plan/carveouts/` und nachher aus
  `docs/plan/carveouts/done/` normalisiert aufgelöst; die Multimenge der aufgelösten
  absoluten Pfade ist identisch bis auf **+1** `slice-117` und **+1** `slice-113`, die der
  Inhalts-Commit neu hinzufügt. Kein Ziel zeigt nach dem Move auf eine *andere* existierende
  Datei — der Fall, den `docs-check` grün durchließe.
- **Der Zwischenlauf aus DoD (2) ist echt und exakt so groß, wie die Message sagt.** Über
  `git archive dbe5e50 | tar -x -C <scratch>` und `make -C <scratch> docs-check` reproduziert:
  `422 Datei(en) geprüft, 72 Befund(e)`, Exit 1. Aufteilung nachgezählt: **44** in der bewegten
  Datei, **28** eingehend über **7** weitere Dateien (`slice-120` 8, `slice-117` 7, `slice-105` 6,
  `review` 3, `review-runde2` 2, `slice-118` 1, `carveouts/README.md` 1) — **8** Dateien gesamt.
  Die Gegenprobe über dem unbewegten Baum (`1f0a4a0`) ist ebenfalls selbst gefahren und grün —
  `422 Datei(en) geprüft, 0 Befund(e)`, Exit 0. Die Reihenfolge trägt also, wie DoD (2) sie
  begründet: erst der Move, dann der rote Lauf, dann das Nachziehen.
- **Der Link-Abgleich nach den zwei Lifecycle-Moves ist ebenso reproduziert.** Über `dbc84ee`:
  `422 Datei(en) geprüft, 10 Befund(e)` — 4 eingehend in `CO-003`, 6 ausgehend (5× `slice-113`,
  1× `slice-118`), genau die Aufteilung aus der Message von `1f0a4a0`.
- **Der Zug an den zwei Review-Berichten war gate-erzwungen, nicht Kulanz.** Sie tragen die
  Markdown-Link-Form (3 bzw. 2 Vorkommen) und waren im Zwischenlauf rot. Gemessen über
  [`.d-check.yml`](../../.d-check.yml): `docs/reviews/**` steht in **vier** `exempt-paths`-Listen
  — den drei `ids`-Mustern (Zeilen 27, 31, 35) und `codepaths` (Zeile 60). `links` und `anchors`
  tragen **keine**; sie greifen also unverändert in Zeitdokumente hinein. Anmerkung ohne
  Befund-Rang: der Plan §1 sagt, die zwei Berichte trügen *„je einen Link"* — gemessen waren es
  **3** und **2**; die Folgerung (sie werden gezogen) bleibt richtig.
- **§3.8 — der Zugriff des Implementers auf `CO-003` ist gedeckt.** §3.8 bindet nur Hard Rules und
  Adaptions-Block und sagt über andere Norm-Artefakte ausdrücklich nichts; `ADR-0015` nennt
  Carveouts nicht; Modul 7 §Carveout-Audit-Slice weist zu — Zitat **verbatim** gegen den vendored
  Baum geprüft: *„Implementer führt `git mv` und Config-Updates aus"*
  (`.harness/baseline/v3.5.2/regelwerk/modul-07-carveouts.md`). Slice-Plan §2 DoD (3) und §6
  benennen die Deckung. Die Präzedenz trägt ebenfalls (**5** frühere Link-Abgleich-Commits fassen
  Carveouts an, **0** fassen ADRs an) — nur ihre Bezifferung in `1f0a4a0` nicht (F-8).
  **Kein §3.8-Befund.**
- **§3.4 — keine ADR berührt.** `git diff --name-only e312ba0^..2df3c79 | grep -c '^docs/plan/adr/'`
  → **0**, Exit 1.
- **§3.5 / LH-QA-01 — keine Gate-Lockerung, kein halluziniertes Gate.** Weder `Makefile` noch
  `.d-check.yml` noch `.github/` sind im Diff (0 Nicht-`.md`-Dateien); es gab auch nichts
  auszubauen — `git grep -c 'CO-00' -- Makefile .d-check.yml d-check.mk .github/` → Exit 1.
- **§3.2 — keine Lint-Suppression.** Keine Go-/Shell-Datei im Diff.
- **§3.9 — der Diff enthält keine Host-Toolchain-Nutzung.** Die sechs Commits ändern ausschließlich
  Markdown; es gibt nichts, was ein Host-Lauf hätte erzeugen können. Für die Werkzeug-Wahl dieses
  Reviews selbst siehe die Offenlegung im Kopf.
- **§3.7 — kein Kommentar in Code/Konfiguration/Skript berührt.** Der Geltungsbereich der Regel
  (Code, Konfiguration, Skripte) ist im Diff leer; F-6 und F-9 sind darum bewusst **nicht** als
  §3.7-Verstoß geführt.
- **Die drei Sensor-Zahlen aus `2df3c79` halten der Gegenprobe stand.** `make docs-check` an HEAD:
  `422 Datei(en) geprüft, 0 Befund(e)`, Exit 0 — selbst gefahren. `make comment-claims` an HEAD:
  `46 Datei(en) geprueft, 0 Befund(e)`, Exit 0 — selbst gefahren. `make mutate` **nicht** gefahren
  (Auflage); das übergebene Protokoll trägt `mutate: 198 ok, 0 Befund(e)` und
  `MUTATE_SECONDS=788.76` und deckt sich mit der Message — als **fremde Messung** gelesen, nicht
  als eigene. Die Begründung des Hakens ist unabhängig nachgemessen: **45** eindeutige
  `# files:`-Ziele in `test/mutations/`, davon **0** unter `docs/`
  (`sed -n 's/^# files: //p' test/mutations/*.sh | tr ' ' '\n' | sort -u | grep -c '^docs/'`), bei
  **198** Fällen. Der Haken entwertet die Läufe also tatsächlich nicht.
- **Die Verifikations-Checkliste ist vollständig gehakt.** `grep -c '^- \[ \] '` → **0**,
  `grep -c '^- \[x\] '` → **7**. Die Zählung selbst ist damit richtig; ob jeder Haken trägt, prüft
  die Verifikation.
- **Die DoD-Kästchen des Slice-Plans sind unangetastet.** Drei `- [ ]` an `:153`, `:162`, `:173` —
  der Implementer hat sich keinen Punkt selbst abgehakt.
- **`make gates` über dem Baum, der diesen Report trägt: EXIT=0** — selbst gefahren, nach dem
  Schreiben. Die Datei-Zahl des Doku-Gates wandert dabei mit dem Bestand und ist **kein**
  Erwartungswert: **422** über `2df3c79`, **424** mit diesem Report **und** dem parallel
  entstandenen Verifikations-Report im Arbeitsbaum. Befunde in beiden Läufen: **0**.
- **Kein weiterer toter Commit-Verweis.** Siehe F-0: über sechs Messages und neun Dateien geprüft,
  genau ein Treffer.

---

## Kategorie-Summary

| Kategorie | Anzahl | IDs |
|---|---|---|
| HIGH | **1** | F-1 |
| MEDIUM | **4** | F-0 (vorab bekannt), F-2, F-3, F-4 |
| LOW | **6** | F-5, F-6, F-7, F-8, F-9, F-10 |
| INFO | **1** | F-11 |

**12** Positionen gesamt, davon **1** (F-0) vom Auftraggeber vorab gemeldet und hier nur bestätigt
und eingegrenzt.

## Verdikt

**Blockiert.** Ein HIGH (F-1) und vier MEDIUM (F-0, F-2, F-3, F-4) stehen offen; HIGH und MEDIUM
blockieren nach Reviewer-Skill §Ablage typischerweise, und keiner der fünf trägt hier einen Grund,
davon abzuweichen.

Zwei Einordnungen, damit das Verdikt nicht mehr behauptet, als es prüft:

1. **Die Sache ist richtig gebaut.** Der Move, die Ebenen-Transformation über 44 Ziele, der rot
   gesehene Zwischenlauf und die Aufteilung 44/28 halten jeder unabhängigen Nachmessung stand.
   Kein Befund betrifft den Zustand des Baums an HEAD gegenüber dem, was der Slice tun sollte —
   `make docs-check` ist grün, und das zu Recht.
2. **Was blockiert, sind Aussagen über die Arbeit.** Eine Zusage, die ihr eigenes `--stat`
   widerlegt (F-1); ein eingefrorenes Zeitdokument, das ohne Gate-Zwang aufgetaut wurde und
   seither eine gegen jeden Baum falsche Zeile trägt (F-2); zwei Mess-Kommandos, die nach dem
   Nachziehen andere Werte liefern als die Zahl daneben (F-3); und ein aufgelöster Carveout, der
   an zwei Stellen weiter sagt, sein Vollzug stehe aus (F-4).

**Nicht Gegenstand dieses Reviews:** die Abhakung von DoD (1)–(3) und die Bestätigung der
Gate-Läufe — beides gehört in die Verifikation nach Modul 11, die parallel läuft.
