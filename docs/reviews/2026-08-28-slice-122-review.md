# Code-Review slice-122 — der d-check-Pin auf v0.65.0

**Rolle:** Reviewer (Modul 10) · **Datum:** 2026-08-28 · **Gegenstand:** HEAD `3ce4ea3`,
Arbeitsbaum sauber · **Runde:** 1

## Kopf-Metadaten

| Feld | Wert |
|---|---|
| Commit-Range | `7d6202b..3ce4ea3` — `2ea9412` (`open`→`next`), `a813969` (`next`→`in-progress`), `c323735` (Link-Abgleich), `3ce4ea3` (Pin) |
| Umsetzungs-Diff | `Makefile` 1 Zeile, `d-check.mk` 4 Zeilen, `internal/emit/emit.go` 2 Zeilen (7+/7−) |
| Slice-Plan | `docs/plan/planning/in-progress/slice-122-d-check-pin-v0650.md` |
| Betroffene Anforderungen | `LH-QA-01`, `LH-QA-02`, `LH-QA-03` |
| Referenzierte Adaptions-Einträge | `MR-009`, `MR-010`, `MR-012`, `MR-016`, `MR-017`, `MR-024` |
| Geprüfte Hard Rules | 3.2, 3.3, 3.5, 3.6, 3.7, 3.8, 3.9 |
| Vorherige Findings am gleichen Modul | `docs/reviews/2026-08-22-slice-088-review.md`, `-bestaetigungsrunde.md`, `-verdikt-runde.md` (Vorgänger-Sprung v0.51.1→v0.62.0) |
| Nicht geprüft (fremde Rolle) | DoD-Abhakung, Gate-Lauf-Bestätigung — Modul 11 |

**Nachbar-Kontext.** Der Vorgänger-Sprung wurde dreimal reviewt und trug in Runde 1 einen
MEDIUM derselben Klasse, die hier wiederkehrt: *„Der einzige angeführte Beleg ist der
byte-gleiche Trockenlauf"* (`docs/reviews/2026-08-22-slice-088-review.md:191`). Die Antwort
darauf war die Strenge-Bilanz an der Quell-Differenz, die `MR-024` seither als
Auflösungs-Trigger festschreibt. Dieser Slice nimmt sie ernst und zieht sie — dieser Report
prüft, ob sie trägt.

## Findings

### HIGH-1 — Die Gegenprobe-Zeile „barer Marker → v0.65.0 unterdrückt" ist am Bestand widerlegt; die Form-Achse der Verengung ist ungeprüft geblieben

- **kategorie:** HIGH
- **quelle:** `AGENTS.md` §3.6 · `MR-024` §Auflösungs-Trigger
- **pfad:** Commit `3ce4ea3`, Message-Block *„DoD (3)"* (die drei Zeilen der Gegenprobe);
  Bezug `docs/plan/planning/in-progress/slice-122-d-check-pin-v0650.md:141-147`
- **befund:** Die Message führt als Messergebnis
  *„barer Marker dahinter → v0.62.0 unterdrueckt, v0.65.0 unterdrueckt"*. Gegen dieselben zwei
  Digests über einer eigens gebauten Sonde außerhalb des Repos gemessen, gilt das Gegenteil:
  ein `d-check:ignore` **ohne** HTML-Kommentar-Form unterdrückt unter `v0.65.0` **nicht** mehr,
  weder bei `codepaths` noch bei `ids`. Die Sonde trägt acht Zeilen (vier je Modul: barer
  Marker, Marker in Inline-Code, ohne Marker, in Kommentar-Form); `v0.62.0` meldet **2**
  Befunde (nur die beiden Kontrollen), `v0.65.0` meldet **6** — dazu kommen genau die zwei
  baren und die zwei Inline-Code-Zeilen. Die Zahlen der Message (`1` gegen `2` über drei
  Zeilen) sind nur erreichbar, wenn die als „bar" ausgewiesene Zeile die **Kommentar**-Form
  trug; die Form-Achse — im Slice-Plan §1 Messung 4 als d-checks slice-159 selbst benannt und
  im d-check-CHANGELOG `[0.64.0]` wörtlich als *„Eine **blanke** Erwähnung in Prosa wirkt dort
  nicht mehr"* geführt — ist damit in keiner Richtung gemessen worden, obwohl die DoD sie mit
  *„bzw. in barer Form"* ausdrücklich verlangt.
- **verifizierbar:** ja. Sonde: eine Datei unter `docs/` mit je einem toten Inline-Code-Pfad
  bzw. einer unverlinkten `MR-\d{3}`-Kennung und den vier Marker-Lagen, Config
  `modules: [ids, codepaths]`; dann

  ```
  docker run --rm --network none -v "<sonde>:/repo:ro" \
    ghcr.io/pt9912/d-check@sha256:3996a593b9cb71aa3bcb4f3ddf8f637e7409db31b3a2dac7eedc28d65814cacf
  docker run --rm --network none -v "<sonde>:/repo:ro" \
    ghcr.io/pt9912/d-check@sha256:5ea03abe7918381c68203d8ac078a78d0d4ab91b5478e87c66b5a7b4fda41288
  ```

  → `3 Datei(en) geprüft, 2 Befund(e)` gegen `3 Datei(en) geprüft, 6 Befund(e)`. Die
  Befundmenge von `v0.62.0` ist echte Teilmenge der von `v0.65.0`.
- **Warum HIGH.** Der Befund liegt im Gate-Pfad — die Marker-Semantik **ist** das Ventil des
  Doku-Gates —, und die Kontext-Eskalation des Reviewer-Skills hebt eine Beobachtung dort eine
  Stufe. Dazu trifft er die Falsch-Klasse aus §3.6 („benennen, was wirklich deckt"): die
  Message weist eine Messung als Beleg für einen Fall aus, den sie nicht gefahren hat. Die
  Message **ist** in diesem Slice das Übergabe-Artefakt an den Architect (Plan §3, Zeile
  `harness/conventions.md`); der `MR`-Eintrag entstünde aus einer Zeile, die am Bestand fällt.

### MEDIUM-1 — Für `ids` liegt keine Richtungs-Messung vor; die Bilanz stützt sich dort auf `numstat` plus CHANGELOG, und `MR-024` schließt den CHANGELOG als tragendes Bein aus

- **kategorie:** MEDIUM
- **quelle:** `MR-024` §Auflösungs-Trigger und §*Welches der zwei Beine die Bilanz trägt* ·
  `AGENTS.md` §3.5
- **pfad:** `docs/plan/planning/in-progress/slice-122-d-check-pin-v0650.md:90-108`
  (Messung 3 und 4); Commit `3ce4ea3` (Message nennt `ids` an keiner Stelle)
- **befund:** `MR-024` verlangt beim Re-Pin die Strenge-Bilanz *„an der Quell-Differenz der
  Regeldateien"* und weist die CHANGELOG-Aufzählung ausdrücklich als **bestätigend, nicht
  tragend** aus. Für `codepaths` liefert der Slice beides — `numstat` und eine konstruierte
  Gegenprobe. Für `ids` liefert er nur den `numstat`
  (`git diff --numstat v0.62.0..v0.65.0 -- internal/hexagon/core/rules/ids.go` → `35 11`) und
  die CHANGELOG-Zuordnung. Ein `numstat` trägt keine Richtung: über die Vorgänger-Spanne konnte
  `MR-024` aus *„null entfernte Zeilen"* auf „rein additiv" schließen, hier sind es **elf**
  entfernte. Damit steht die §3.5-Antwort für ein aktives Modul im Repo-Bestand auf genau dem
  Bein, das `MR-024` als nicht tragend deklariert.
- **verifizierbar:** ja. Selbst gefahren und **entlastend** ausgegangen — die Richtung ist
  Verengung, an zwei unabhängigen Stellen:
  (a) `git diff v0.62.0..v0.65.0 -- internal/hexagon/core/rules/ids.go` am lokalen d-check-Klon
  zeigt, dass die entfernten elf Zeilen der alte `markerLines`-Rumpf sind
  (`strings.Contains(pl.raw, ignoreMarker)`), ersetzt durch zwei zusätzliche Bedingungen
  (gestrippter Text **und** `commentMarkerRe`); die Funktion ist ab `v0.65.0` die **geteilte**
  Antwort für `codepaths` **und** beide `ids`-Pfade.
  (b) Die Sonde aus HIGH-1 zeigt für `ids` dasselbe Bild wie für `codepaths` (`MR-901`/`MR-902`
  unter `v0.62.0` unterdrückt, unter `v0.65.0` gemeldet).
  Der Befund ist damit **nicht**, dass eine Senkung vorliegt, sondern dass der Repo-Bestand die
  Aussage nicht trägt, aus der der `MR`-Eintrag entstehen soll.

### MEDIUM-2 — `MR-025`s nicht-permanenter Auflösungs-Trigger feuert mit genau diesem Pin-Sprung und ist in Plan, Commit und Übergabe nicht genannt

- **kategorie:** MEDIUM
- **quelle:** `MR-025` §Auflösungs-Trigger
- **pfad:** `docs/plan/planning/in-progress/slice-122-d-check-pin-v0650.md:38-57`
  (Bezug-Block); Commit `3ce4ea3`
- **befund:** `MR-025` ist der einzige Eintrag des Adaptions-Blocks mit einem **nicht
  permanenten** Auflösungs-Trigger: *„fällig beim nächsten d-check-Pin-Sprung … Fällig ist dann
  eine **Entscheidung**, keine Erinnerung, und es sind drei"* — (a) eigener hermetischer
  Prüfer, (b) Adoption eines Form-tragenden d-check-Moduls, (c) bewusste Permanenz. Er nennt
  dabei den auslösenden Lauf beim Namen (*„der Slice, der den Pin zieht, schlägt diesen Block
  ohnehin auf"*) und hängt zwei Vorfrage-Messungen daran. Der Slice zieht den Pin und nennt
  `MR-025` an keiner Stelle.
- **verifizierbar:** ja.
  `grep -c 'MR-025' docs/plan/planning/in-progress/slice-122-d-check-pin-v0650.md` → **0**;
  `git log -1 --format=%B 3ce4ea3 | grep -c 'MR-025'` → **0**;
  `grep -o 'MR-0[0-9][0-9]' <plan> | sort -u` → `MR-009 MR-010 MR-012 MR-016 MR-017 MR-024`.
  Die zwei Vorfragen sind hier beantwortet und ändern die Entscheidungslage nicht:
  `git -C <d-check-klon> grep -ln 'os/exec' v0.65.0 -- 'internal/*.go' 'internal/**/*.go' 'cmd/**/*.go' ':!*_test.go'`
  → leer, Exit 1 (Weg (b) bleibt zu); `grep -c 'structure' .d-check.yml` → **0**. Die
  Entscheidung ist also billig — sie ist nur nirgends getroffen und nirgends geführt.

### MEDIUM-3 — Der §Baseline-Block behauptet weiterhin `Image v0.62.0`, und der Auftrag, der das auflöst, existiert nur als Plan-Tabellenzelle

- **kategorie:** MEDIUM
- **quelle:** `MR-024` §Geltungsbereich (nennt die §Baseline-Version) · `AGENTS.md` §3.8
- **pfad:** `harness/conventions.md:14`
- **befund:** Der §Baseline-Block des Adaptions-Registers sagt
  *„**d-check:** Image v0.62.0 (Digest in d-check.mk, …)"*, während `d-check.mk:18`
  `v0.65.0` führt. Dass der Slice die Datei nicht anfasst, ist nach §3.8 richtig und im Plan §3
  begründet. Der Befund ist die **Trägerschaft** der Folgepflicht: sie steht ausschließlich in
  einer Zelle des Plans, `§7 Closure-Notiz` ist leer, und der Plan wandert nach `done/` — in
  ein Zeitdokument, das kein Lauf wieder aufschlägt. Kein Modul der aktiven Sechs liest
  Versions-Aussagen (`versions` und `pins` sind nicht in `modules:`), also meldet auch kein
  Gate den Widerspruch.
- **verifizierbar:** ja. `grep -n 'Image v0.62.0' harness/conventions.md` → **1** Treffer
  (`:14`) gegen `grep -n 'DCHECK_IMAGE ?=' d-check.mk` → `v0.65.0`. Ergänzend:
  `grep -c 'structure\|versions\|pins' .d-check.yml` bestätigt, dass kein Versions-Modul aktiv
  ist.

### MEDIUM-4 — Die zwei Kopplungstests, auf denen DoD (1) ihr Rot erzeugt, haben keinen Fall in `test/mutations/` — nach §3.6 sind sie unbewacht

- **kategorie:** MEDIUM (LOW-Anker *latente Wartungsfalle*, eine Stufe gehoben: Gate-Pfad)
- **quelle:** `AGENTS.md` §3.6 §Feedback (*„wer keinen Fall in `test/mutations/` hat, ist
  unbewacht"*)
- **pfad:** `internal/emit/emit_test.go:30` und `:42`
  (`TestDefaultDigest_MatchesCanonical`, `TestDefaultImage_MatchesCanonical`);
  `test/mutations/` (Abwesenheit)
- **befund:** Die Kopplung `internal/emit/emit.go:33-34` ↔ `d-check.mk:18-19` ist der Wächter,
  an dem DoD (1) ihr Rot holt und der die Tier-1-Drift des emittierten Pins verhindert. Für
  drei Geschwister derselben Klasse führt das Repo je einen Mutations-Fall
  (`01-baseline-pin-kopplung.sh` → `TestDefaultBaselineSHA256_MatchesMakefile`,
  `18-gen-pin-drift.sh` → `TestGoProfile_PinsMatchRepo`, `66-archgate-pin.sh` →
  `TestAdaptArchMK_PinsProducingRef`). Für den d-check-Pin führt es keinen. Die Lücke ist
  älter als dieser Slice — sie hat drei Pin-Sprünge überlebt —, aber dieser Slice ist der, der
  seine zentrale Zusage auf ihr abstützt.
- **verifizierbar:** ja. `grep -rn 'MatchesCanonical' test/mutations/` → leer, Exit 1;
  `grep -rln 'DefaultDigest\|DefaultImage\|DCHECK_DIGEST\|DCHECK_IMAGE' test/mutations/` →
  leer. Versagens-Szenario: wird die Vergleichs-Zeile in `emit_test.go` auf `HasPrefix`
  aufgeweicht, bleibt `make test` grün und `make mutate` stumm (kein gelisteter Fall); der
  nächste Pin-Sprung ließe `emit.DefaultDigest` still stehen, und das emittierte Repo
  bootstrappte sein Gate mit einem anderen Image als dieses Repo fährt (`LH-QA-02`).

### LOW-1 — `d-check.mk:2` zieht die Version auf `v0.65.0` und lässt den Rang-Zeiger auf `MR-024` stehen, der den `v0.62.0`-Sprung trägt

- **kategorie:** LOW
- **quelle:** `AGENTS.md` §3.7 (Rang-Zeiger-Klasse; Cutoff greift, weil genau diese Zeile
  geändert wurde)
- **pfad:** `d-check.mk:2`
- **befund:** Die Zeile lautet nach dem Commit
  *„Abgeleitet aus `d-check --print-mk` (v0.65.0) und adaptiert
  (MR-010/MR-011/MR-012/MR-024)"*. Die aufgezählten Einträge sind die, die den jeweiligen
  Sprung tragen — `MR-011` den auf v0.50.0, `MR-012` den auf v0.51.1, `MR-024` den auf
  v0.62.0. Für `v0.65.0` steht in der Liste keiner, und `MR-024` trägt im Titel eine andere
  Version. Der Nachzug ist in keinem Artefakt als Folgepflicht geführt: `d-check.mk` gehört
  **nicht** zu den zwei Architect-Artefakten aus §3.8, die Plan-Zeile deckt nur
  `harness/conventions.md`.
- **verifizierbar:** ja. `sed -n '2p' d-check.mk` gegen
  `grep -n '^### MR-024' harness/conventions.md` → `MR-024 — d-check-Pin v0.62.0`.

### INFO-1 — Die Adaptions-Fixture ist die v0.46.0-Rohausgabe; sie ist kein stiller Grün-Pfad, steht aber in keinem Re-Pin-Trigger

- **kategorie:** INFO
- **quelle:** `MR-010` §Auflösungs-Trigger · Maintainability
- **pfad:** `internal/emit/testdata/raw-print-mk.txt`
- **befund:** `TestAdaptMK_Fixture` prüft die vier `MR-010`-Handgriffe gegen eine eingefrorene
  `--print-mk`-Ausgabe von v0.46.0 (**64** Zeilen gegen heute **68**). Der emittierte Pfad
  adaptiert dagegen die **Live**-Ausgabe des gepinnten Images (`emit.go:101` → `AdaptMK`).
  Kein Befund daraus: `AdaptMK` bricht auf allen vier Handgriffen hart ab
  (`emit.go:139-146`), ein Formatwechsel fällt also laut. Die Fixture veraltet trotzdem
  stillschweigend mit jedem Pin, und `MR-010`s Auflösungs-Trigger nennt sie nicht.
- **verifizierbar:** ja. `wc -l < internal/emit/testdata/raw-print-mk.txt` → **64**;
  `docker run --rm <v0.65.0-digest> --print-mk | wc -l` → **68**.

### INFO-2 — Zwei Aussagen des Kopfkommentars sind unpräzise, beide von diesem Commit unberührt

- **kategorie:** INFO
- **quelle:** `AGENTS.md` §3.7 §Cutoff (*„der Bestand ist kein Arbeitsauftrag"*)
- **pfad:** `d-check.mk:9-11` und `:16-17`
- **befund:** (a) *„Die opt-in-Module `citations` …, `sources` … und `structure` … sind NICHT
  aktiviert"* — für `sources` gilt das nur für `make gates`; `make regelwerk-check` fährt es mit
  `--enable sources` (`MR-013`). (b) Die Neu-Erzeugungs-Zeile nennt zwei der vier
  `MR-010`-Handgriffe (Rename, Digest) und lässt Kopfkommentar und `doc-help`-Grep aus. Beide
  Zeilen hat dieser Commit nicht angefasst; der §3.7-Cutoff bindet sie nicht. Genannt, damit
  der nächste Lauf, der die Zeilen ohnehin anfasst, sie mitnimmt.
- **verifizierbar:** ja. `grep -n 'enable sources' Makefile d-check.mk` gegen `d-check.mk:9-11`.

## Prüfung der übergebenen Behauptungen

Keine ist als Beleg übernommen; jede ist selbst gefahren (`MR-025`).

| Behauptung | Verdikt | Meine Messung |
|---|---|---|
| Digest `sha256:5ea03abe…41288` dreifach identisch | **bestätigt** | `docker image inspect --format '{{index .RepoDigests 0}}' ghcr.io/pt9912/d-check:v0.65.0` → `…@sha256:5ea03abe…41288`; `docker buildx imagetools inspect …:v0.65.0` → `Digest: sha256:5ea03abe…41288`; `gh release view v0.65.0 --repo pt9912/d-check --json tagName,publishedAt,body` → Body `Digest-Pin: …@sha256:5ea03abe…41288`, `publishedAt 2026-08-28T03:37:33Z`. Alle drei zeichengleich mit `d-check.mk:19` und `emit.go:34` |
| DoD-(1)-Rot gesehen | **plausibilisiert, nicht selbst gefahren** | Nicht reproduziert (der Baum bleibt unangetastet, und der Gate-Lauf ist Modul 11). Geprüft ist stattdessen, dass die zitierten Meldungen echt sind: `emit_test.go:36` trägt `"emit.DefaultDigest %q != kanonische Pin-Quelle %q (Drift)"`, `:45` trägt `"emit.DefaultImage %q != kanonische Quelle %q (Tag-Drift)"` — beide Formatstrings decken den zitierten Wortlaut, und beide Tests lesen `d-check.mk` über `mkVar` (`:31`, `:43`), die Kopplung ist real |
| `--print-mk` beider Versionen: je 68 Zeilen, genau eine Zeile Unterschied | **bestätigt** | `docker run --rm <digest> --print-mk` beidseits, `wc -l` → **68**/**68**; `diff` → ein Hunk, `DCHECK_IMAGE ?= …:v0.62.0` gegen `…:v0.65.0`. Zusätzlich `grep -cE '^docs?-[a-z-]+:'` → **12**/**12**, und **12** im gelebten `d-check.mk` — die Aufzählung in `MR-010` Setzung 2 bleibt gedeckt |
| Trockenlauf beidseits `432 Datei(en), 0 Befund(e)` | **bestätigt** | `git archive HEAD` in eine Kopie außerhalb des Repos, dann `docker run --rm --network none -v "<kopie>:/repo:ro" <digest>` je Digest → beide `d-check: 432 Datei(en) geprüft, 0 Befund(e)`, Exit 0, `diff` der Ausgaben leer |
| Gegenprobe: Marker in Inline-Code unterdrückt unter v0.62.0, nicht unter v0.65.0 | **bestätigt** | Sonde aus HIGH-1, Zeilen C2/I2 |
| Gegenprobe: barer Marker unter beiden unterdrückt | **widerlegt** | Siehe HIGH-1 |
| keine lebende `v0.62.0`-Angabe mehr | **widerlegt in der Reichweite** | Die Behauptung trägt über die vier gegreppten Muster (`*.mk`, `Makefile`, `*.go`, `*.yml`) — dort ist sie richtig. Markdown ist nicht im Grep, und `git grep -n 'v0\.62\.0' -- ':!.harness/baseline'` findet in `harness/conventions.md:14` eine **lebende, normative** Angabe über den heutigen Pin (MEDIUM-3). Die übrigen Markdown-Treffer sind Zeitbezüge (`done/`, `docs/reviews/**`, ADR-0021, `test/full-smoke-ausgang.bats:104` als aufgezeichnete Ausgabe) und tragen zu Recht die alte Version |

## Negativbefunde (geprüft, ohne Befund)

- **Re-Adaption des Fragments (`MR-010` Setzung 1, vier Handgriffe).** `diff` der frischen
  `v0.65.0`-Rohausgabe gegen `d-check.mk` zeigt **genau vier** Abweichungen: Kopfkommentar,
  `DCHECK_DIGEST`-Pin, `doc-check`→`docs-check` (Target **und** Hilfetext), `doc-help`-Grep auf
  `docs?-`. Kein advisory-Recipe ist berührt, alle behalten `--network none` und den `:ro`-Mount.
- **`MR-010` Setzung 2, Target-Aufzählung.** 12 Targets im Fragment beider Versionen und im
  gelebten `d-check.mk`; `docs-check` ist das einzige d-check-Target in `make gates`
  (`grep -n '^gates:' Makefile`). Kein halluziniertes Gate (`LH-QA-01`).
- **Die Richtung des Sprungs an den aktiven Modulen — beide Richtungen gemessen.** Der
  Trockenlauf ist über einer 0-Befund-Basis in der Gegenrichtung leer; darum die
  Ventil-aus-Gegenprobe: alle `d-check:ignore` in einer Kopie von HEAD entwertet, dann beide
  Digests. Ergebnis `432 Datei(en) geprüft, **26** Befund(e)` unter **beiden** Versionen, und
  `diff <(sort v0.62.0-Ausgabe) <(sort v0.65.0-Ausgabe)` ist **leer** — identische Befundmenge,
  identische Zeilen. Über diesem Korpus ändert der Sprung an keinem aktiven Modul etwas, in
  keiner Richtung. Das ist der Beleg, den `MR-024` §*Was dieser Lauf trägt — und was nicht*
  vermisst; er fehlt im Slice und ist hier nachgeholt.
- **Die übrigen aktiven Regeldateien.** `git diff --numstat v0.62.0..v0.65.0 -- internal/hexagon/core/rules/`
  am lokalen d-check-Klon führt `links.go`, `anchors.go`, `matrix.go`, `spans.go` **nicht** —
  keine Zeile bewegt. Die Plan-Aussage stimmt.
- **Geteilte Vorverarbeitung.** `markdown.go` (+15/−5) berührt allein `diagramFenceLines` und
  einen neuen Typ für `diagrams`; `sections.go` (+41/−1) allein `SectionHeadings` für
  `structure`; `run.go` (+30/−19) allein die `versions`-Paarauflösung und eine zusätzliche
  fail-closed-Prüfung für `diagrams.defined-in`. Keines der drei Module ist in `modules:`.
- **Lockerungs-Vokabular über die Spanne.** `awk '/^## \[0\.65\.0\]/,/^## \[0\.62\.0\]/' CHANGELOG.md | grep -niE 'lockerung|findet weniger|weniger streng|entschärf|nicht mehr gemeldet|aufgehoben'`
  am Klon → **zwei** Treffer, beide „Falsch-Rot" in der Beschreibung der **benannten Grenzen**
  der Verengung (mehr Befunde, nicht weniger). Upstream deklariert slice-159 selbst als
  *„monoton verengend: sie kann keinen neuen stillen Grün-Pfad erzeugen"*. Damit ist §3.5 in
  der Sache erfüllt — **Anheben**, kein ADR nötig (`MR-001`: Anheben → Steering-Loop).
- **Modul-Inventar.** `git ls-tree -r --name-only <tag> -- internal/hexagon/core/rules/` ohne
  `_test`: **28** Dateien je Tag, Namensliste identisch; die `--disable`-Listen beider
  Fragmente nennen dieselben 19 Modulnamen. Der Kopfkommentar-Satz über die opt-in-Module
  18./19./20. bleibt vollständig — es gibt kein 21.
- **Kopfkommentar-Zahlen.** `grep -c -- '--enable' <v0.65.0-Fragment>` → **6**, davon **5** mit
  allen drei `--disable citations|sources|structure`; das sechste ist `doc-structure`. Die
  Formulierung *„von den sechs … disablen FUENF alle drei"* hält.
- **Marker-Fläche im Bestand.** `git grep -n 'd-check:ignore' -- ':!.harness/baseline' | wc -l`
  → **238**; davon `grep -cE '<!--[^>]*d-check:ignore'` → **172** in Kommentar-Form, **66**
  nicht. Die 66 sind sämtlich **Erwähnungen** (in Backticks oder in einem zitierten
  `grep`-Kommando) — durchgesehen, keine gesetzte Direktive darunter. Die Verengung kostet
  diesen Baum nichts; die Ventil-aus-Messung oben belegt es unabhängig.
- **Hard Rule 3.3.** `2ea9412` und `a813969` sind reine Moves (`0 insertions(+), 0 deletions(-)`),
  der Link-Abgleich `c323735` und die Inhaltsänderung `3ce4ea3` liegen je in eigenen Commits.
- **Hard Rule 3.2.** `git show 3ce4ea3 | grep -E '^\+.*(nolint|shellcheck disable|d-check:ignore)'`
  → leer.
- **Hard Rule 3.8.** Kein Commit des Slice berührt `AGENTS.md` §3 oder `harness/conventions.md`
  (`git diff --name-only 7d6202b..3ce4ea3`). Die Trennung ist eingehalten; die offene Flanke ist
  die Trägerschaft der Folgepflicht (MEDIUM-3), nicht die Trennung selbst.
- **Hard Rule 3.9.** Der Diff bringt keine Host-Toolchain; die `d-check.mk`-Rezepte bleiben
  ausschließlich `docker run` mit `--network none` und `:ro`.
- **`MR-017` / Emitter-Ebene.** `git diff --name-only 3ce4ea3~1 3ce4ea3 -- internal/emit/templates/`
  → leer. Die emittierte Starter-Config bleibt `modules: [links, anchors]` mit
  auskommentiertem `ids`/`codepaths`; die Marker-Verengung ist im emittierten Repo damit inert.
- **Makefile-Kommentar und Tag-Achse.** `Makefile:40` beschreibt weiter genau die Ableitung, die
  darunter steht; `make freshness-dcheck` → *„d-check: aktuell — gepinnt und latest sind beide
  v0.65.0."*, Exit 0.
- **`LH-QA-02` gegen den neuen Registry-Spiegel.** `[0.65.0]` führt einen Docker-Hub-Spiegel ein
  und weist ausdrücklich aus, dass Digests **registry-lokal** sind. Der Pin nennt
  `ghcr.io/pt9912/d-check@…` und wird von GHCR gezogen — konsistent, kein Auflösungs-Risiko.
- **`make mutate`.** Nicht von mir gefahren (Lock belegt); das übergebene Protokoll endet mit
  `mutate: 198 ok, 0 Befund(e)`, `MUTATE_SECONDS=768.40`, keine `BEFUND`/`FAIL`/`not ok`-Zeile.
  Kein bestehender Zahn ist stumpf geworden — MEDIUM-4 spricht über einen **fehlenden**, nicht
  über einen stumpfen.
- **Link-Abgleich `c323735`.** Vier Verweise aus `welle-13` von `open/` auf `in-progress/`
  gezogen; der grüne `links`/`anchors`-Lauf über HEAD (432/0) deckt, dass alle vier auflösen.

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 1 |
| MEDIUM | 4 |
| LOW | 1 |
| INFO | 2 |

## Verdikt

**Blockierend.** Ein HIGH und vier MEDIUM; nach der Ablage-Regel des Reviewer-Skills blockieren
beide Kategorien typischerweise, und hier gibt es keinen Grund für eine Ausnahme: HIGH-1 und
MEDIUM-1 betreffen dieselbe Aussage, auf die der Slice seinen Titel setzt — die Strenge-Bilanz.
Sie ist **in der Sache richtig** (der Sprung verengt an beiden bewegten Modulen und ändert über
diesem Baum nichts, beides oben gemessen), aber der Bestand trägt sie nicht so, wie `MR-024` es
verlangt, und eine ihrer drei Gegenprobe-Zeilen fällt am Bestand.

Die technische Substanz des Diffs ist sauber: der Pin ist dreifach belegt, das Fragment ist
faithful re-adaptiert, die Kopplung zum emittierten Default hält, kein Gate wird gelockert, und
die vier Commits sind korrekt geschnitten. Was blockiert, ist der **Beleg-Stand des
Übergabe-Artefakts** — der Architect-Lauf, der `MR-024` fortschreibt, würde sonst eine falsche
Zeile und eine ungedeckte Modul-Aussage übernehmen.

**Rollen-Konflikt:** keiner. Kein Finding verlangt eine Änderung an einem Architect-Artefakt
durch den Implementer; MEDIUM-3 und MEDIUM-2 benennen im Gegenteil, dass die
Architect-Übergabe unvollständig **vorbereitet** ist.

**Grenzen dieses Laufs.** `make gates`, `make test`, `make smoke`/`full-smoke` und `make mutate`
sind nicht von mir gefahren (Modul 11 bzw. belegter Lock). Die Strenge-Bilanz ist gegen einen
lokalen d-check-Klon und gegen konstruierte Sonden gemessen, nicht gegen ein Gate dieses Repos —
kein Modul hier vergleicht Befundklassen zweier d-check-Versionen; das bleibt die offene Fläche,
die schon `MR-024` benennt.
