# Verifikation: slice-adapter-und-ports-ordner-folgen-ihren-rollen-namen

**Rolle:** Verifier (Modul 11) · **Datum:** 2026-09-19 · **Kopf:** `387b5210`
**Eingabe:** Slice-Plan (`docs/plan/planning/in-progress/slice-adapter-und-ports-ordner-folgen-ihren-rollen-namen.md`) · [ADR-0060](../plan/adr/0060-adapter-und-ports-ordner-folgen-ihren-rollen-namen.md) (`Accepted`) · [ADR-0009](../plan/adr/0009-hexslice-arch-realisierung.md) mit dem Index-Zusatz an seiner Zeile. Geprüft wurde gegen Plan und ADRs — **nicht** gegen die Review-Reports (Rollen-Trennung, Modul 8).

**Sensor-Basis dieses Laufs (selbst gefahren):** `make full-smoke` → EXIT 0; `make test` → EXIT 0; `make e2e-abdeckung` → exit 0, `git diff --stat docs/user/e2e-abdeckung.md` → leer. **Nicht wiederholt** (am Kopf gemessen): `make gates` grün, `make docs-check` 1753/0. `make mutate` **nicht gefahren** (nächtliche Stufe, ~50 min) — die Zähne sind statisch gegen ihre Muster gemessen (Befund V-1).

---

## 1. DoD — Punkt für Punkt

| # | DoD-Punkt | Verdikt | Beleg |
|---|---|---|---|
| 1 | **Liefer-Punkt 1 — Adapter-Ordner** | **erfüllt** | Go: `internal/gen/golang.go:83-85` legt `internal/adapters/{driving,driven}/…` an; C++: `internal/gen/cpp.go:75-77` legt `src/adapters/{driving,driven}/…` an. Die Tests tragen die Erwartungen als geschlossene Datei-Sätze: `internal/gen/hexslice_test.go:36-48` (`internal/adapters/driving/cli/example/cli.go`, `internal/adapters/driven/{memory,notify}/…`) und `internal/gen/cpp_test.go:178-196` (`src/adapters/…`) — legt das Skeleton `inbound` an, bricht der Vergleich mit der want-Liste. Die Composition-Root-Imports folgen (`internal/gen/golang.go:468-470`, `internal/gen/cpp.go:428-430`). **Mit Vorbehalt (ADR-getragen):** der DoD-Text „die Rollen-Namen kommen aus `internal/gen/arch.go`“ ist im Baum nur als *Übereinstimmung* erfüllt, nicht als *Bindung* — die Ordner-Segmente stehen als Literale, ein Test, der sie an die Konstanten hält, existiert nicht (`grep -rn 'hexagonal-driving' internal/gen/` → nur die Konstantendefinition `internal/gen/arch.go:54`). [ADR-0060](../plan/adr/0060-adapter-und-ports-ordner-folgen-ihren-rollen-namen.md) §Kontext trägt genau das (*„Gelesen, nicht gebunden … die Bindung ist Folgepflicht 6, nicht Bestand“*) — die Zusage ist also vom ADR umgeschnitten, nicht still verengt; **Folgepflicht 6 ist offen** (Übergabe, siehe §5). |
| 2 | **Liefer-Punkt 2 — Ports-Gliederung** | **erfüllt** | `ports/{inbound,outbound}/` je Ebene: use-case-lokal (`internal/gen/golang.go:71-72`: `greet/ports/inbound/greet.go`, `greet/ports/outbound/notifier.go`), business-area (`internal/gen/golang.go:73`: `example/ports/outbound/greeting_repository.go`); C++ spiegelbildlich (`internal/gen/cpp.go:65-67`). Der Repository-Port ist outbound, der CLI-Use-Case-Port **inbound — und er liegt im Baum**: `goHexInboundPort` trägt `Command`/`Result` und das Interface `Greet` (`internal/gen/golang.go:256-277`), der treibende Adapter importiert den Port und nie die Slice (`internal/gen/golang.go:374`), die Config deklariert dafür `app→ports_inbound` und `driving_adapters→ports_inbound` und lässt `driving_adapters→app` bewusst weg (`internal/gen/golang.go:585-598`). Rote Gegenprobe: die Datei-Satz-Tests zählen die Ports-Pfade genau; die Referenz-Form deckt (`/Development/hexslice-architecture`-Referenz gelesen: der inbound-Port trägt Request-/Result-Typen plus Interface — dieselbe Form, hier `Command`/`Result`). |
| 3 | **Liefer-Punkt 3 — Arch-Gate-Config wandert mit** | **erfüllt** | Gegen die Referenz gezählt (Kommandos, Ausgaben): `awk '/const goHexArchConfig/,/^`$/' internal/gen/golang.go \| grep -c 'direction:'` → **4**, `\| grep -c '{from:'` → **6**; Referenz `/Development/hexslice-architecture/lab/examples/go/.a-check.yml`: `grep -c 'direction:'` → **4**, `grep -c '{from:'` → **6**. Sechs Layer (`domain`, `ports_inbound`, `ports_outbound`, `app`, `driving_adapters`, `driven_adapters`), `direction:` auf Port- **und** Adapter-Schichten (`internal/gen/golang.go:561/:567/:575/:579`), Port-Globs enden am Richtungs-Segment, sechs Kanten (`internal/gen/golang.go:584-589`), drei bewusst abwesende Kanten je mit Referenz-Grund als Kommentar, `driving_adapters→app` abwesend. C++ trägt dieselbe Form plus die sprach-abhängige Vererbungs-Kante: `awk '/const cppHexArchConfig/,/^`$/' internal/gen/cpp.go \| grep -c '{from:'` → **7** (`driven_adapters→ports_outbound`); die Go-Fassung trägt sie bewusst nicht — beides hält der nachgezogene Zahn `TestArchGateConfig_CppAllowsAdapterToPorts` (`internal/gen/cpp_test.go:273-289`, prüft die Kante in C++ **und** ihre Abwesenheit in Go). Rot-Beleg gegen den Gate-Test: strukturell fällt eine Datei unter keinen Glob → „Loch im Prüfbereich“ (`internal/gen/archgate_test.go:85-100`); die vier Rot-Proben der Runde 2 sind dort selbst gefahren und laut (a: Config ohne Schicht-Teilung → `TestArchGateConfig_MatchesSkeleton` rot; b2: Port-Kante entfernt → beide Richtungen rot; c: Pin auf v0.15.0 → Pin-Zahn rot mit exakter Meldung). |
| 4 | `make gates` grün | **erfüllt** | Am Kopf `387b5210` gemessen (Übergabe); in diesem Lauf `make test` EXIT 0 und `make full-smoke` EXIT 0 selbst gefahren — der engere Kreis der drei Achsen läuft in beiden. |
| 5 | Review, Report unter `docs/reviews/` | **erfüllt** | Runde 1 (`f90a6c8c`) und Runde 2 (`936b1d84`) liegen vor; Runde 2s blockierender Befund F-1 stand am **Accept-Übergang von ADR-0060**, nicht am Code-Diff, und ist seitdem geschlossen (ADR-Re-Schnitt `6228cbd5`, Accept `387b5210` auf Beleg der Accept-Nachrunde Runde 4 ohne blockierenden Befund). Der offen gebliebene MEDIUM F-2 (Handbuch) ist in §4 dieser Verifikation gezogen (V-2). |
| 6 | Doku-Update, falls öffentlicher Vertrag berührt | **nicht erfüllt (Teil)** | Nachgezogen: `spec/architecture.md:172-177` (sechs Layer, sechs Kanten, C++-Zusatz-Kante), `:218-220` (sprach-abhängige Kante), `docs/user/benutzerhandbuch.md:276` (Skelett-Pfade `driving`/`driven`). **Offen:** `docs/user/benutzerhandbuch.md:298` weist den Adopter weiterhin an, einen Port-Eintrag „unter `ports`“ nachzutragen — ein Layer, den die emittierte Config nicht mehr führt (`ports_inbound`/`ports_outbound` mit je `direction:`); ein nach dieser Anweisung nachgetragener Eintrag landet unter keinem existierenden Layer. Das ist Runde-2 F-2 (MEDIUM) und bleibt ungezogen — siehe Befund V-2. |
| 7 | Closure-Notiz, Register, Risiko-Ausgänge, Paarungen | **nicht prüfbar hier** | Der Slice steht in `in-progress/`; der Abschluss ist Planner-Arbeit nach dieser Übergabe ([AGENTS.md](../../AGENTS.md) §3.10). Der Plan-Defekt-Posten in §6 ist bereits mit Ausgang *entfallen* gezogen (gelesen). |
| 8 | `docs/user/e2e-abdeckung.md` regeneriert, falls Stufen-Deklaration ändert | **erfüllt (Bedingung leer)** | Die Stufen-**Deklarationen** (Kennung + Beschreibung) änderten sich nicht — die Commits änderten die Pfad-Checks der Stufen. `make e2e-abdeckung` → exit 0, `git diff --stat docs/user/e2e-abdeckung.md` → leer. |

**Die drei E2E-Stufen-Anker** (`harness/tools/full-smoke.sh`, vom Plan §1 benannt) stehen alle: Go-Stage prüft `apps/hex/internal/adapters/driving/cli/example/cli.go` (`:2261`), Go-Zahn fährt `make a-check-apps-hex` über einem verbotenen Import (`:2338-2341`), cpphex-Stage prüft den Area-Port an seiner neuen Stelle `apps/cpphex/src/hexagon/application/example/ports/outbound/greeting_repository.hpp` (`:2375`), C++-Zahn fährt `make a-check-apps-cpphex` mit Richtungs-Befund-Erwartung (`:2455-2463`). Der Unfall-Lauf des Implementers (cpphex-Stage am alten Area-Port-Pfad rot) ist im Fix-Commit `6c880bb7` nachgezogen; mein eigener `full-smoke`-Lauf lief über genau diese Stufen grün.

## 2. Die Verfeinerungen — gewogen

- **Verfeinerung 1** („beide Ports outbound, die inbound-Hälfte bleibt Struktur“): **übergangen, nicht still.** Der Plan behielt sie als Übergabe; die endgültige Lieferung (Verfeinerung 3) materialisiert den inbound-Port — das ist die **Erweiterung** gegen Verfeinerung 1, getragen vom re-geschnittenen [ADR-0060](../plan/adr/0060-adapter-und-ports-ordner-folgen-ihren-rollen-namen.md) Festlegung 2 (`Accepted`). Keine still verengte Lieferung — die Lieferung ist breiter als die Verfeinerung und deckt den DoD-Text.
- **Verfeinerung 3** (Referenz-Form an a-check v0.20.0): **getragen** — Kanten-Menge exakt die der Referenz (oben gezählt), Pin `v0.20.0` samt Digest, portScope-Form lebendig (Port-Globs enden am Richtungs-Segment), inbound-Port materialisiert, `driving_adapters→app` abwesend.

**Der DoD-Konflikt (Plan §2 „der CLI-Adapter-Port inbound“) — ausdrücklich beantwortet:** erfüllt. Der Konflikt zwischen DoD-Text und Verfeinerung 1 („die inbound-Hälfte der Ordner-Form bleibt Struktur“) ist normativ durch den ADR-Re-Schnitt entschieden; die DoD-Zeile wurde dabei **nicht umgeschrieben** — sie stimmt jetzt wörtlich mit dem Baum. Der Implementer hat sein eigenes Abnahmekriterium nicht geändert (§3.10 sauber): die Plan-Änderungen in `366c8837`/`34c9e446` tragen die Verfeinerungen als §3-Übergaben, der DoD-Text blieb stehen.

## 3. Skeleton- und Config-Vergleich gegen die Referenz

- **Skeleton-Dateien:** Referenz-Go-Baum `find /Development/hexslice-architecture/lab/examples/go/internal -type f | wc -l` → **21**; emittiertes Go-Skelett **15** Dateien (geschlossener Satz im Test `TestGenerate_GoHexsliceProfile_FileSet`). Struktur-Korrespondenz vollständig: `adapters/{driving,driven}` · `ports/{inbound,outbound}` je Slice · Area-Port unter `application/<area>/ports/outbound/` · `hexagon/{domain,application}`. Die Mengen unterscheiden sich wie die Beispiele (zwei Slices/`order` vs. ein Slice/`example`) — die Form deckt.
- **C++-Referenz:** die Referenz trägt **kein** cpp-Beispiel (`ls /Development/hexslice-architecture/lab/examples/` → `go`, `kotlin`); die C++-Form ist die Kotlin-Nominal-Analogie ([ADR-0060](../plan/adr/0060-adapter-und-ports-ordner-folgen-ihren-rollen-namen.md) Festlegung 4). Kotlin-Baum gelesen: `adapters/{driving/cli/order, driven/{id, memory/order, notify}}` — dieselbe Rollen-Form.
- **Config:** Go exakt die Referenz (oben gezählt: 4/6 gegen 4/6), C++ Referenz + 1 (Vererbungs-Kante).

## 4. Befunde (Negativbefund-Pflicht)

- **V-1 (MEDIUM) — drei gelistete Mutations-Fälle sind stumm.** Die Config-Re-Schnitte (`366c8837`, `34c9e446`) änderten die Literale, auf deren Muster die gelisteten Mutations-Fälle 68, 71 und 96 ihre sed fahren — alle drei sind jetzt No-Ops, `make mutate` würde dort BEFUND melden (Zahn verloren, [AGENTS.md](../../AGENTS.md) §3.6). Gemessen (`grep -c`, je exit 1):
  - `test/mutations/68-archconfig-kopplung.sh` (expect `TestArchGateConfig_MatchesSkeleton`) sucht `application/example/greet/ports/**` — `grep -c 'greet/ports/\*\*' internal/gen/golang.go internal/gen/cpp.go` → **0/0**; die Port-Globs enden jetzt am Richtungs-Segment.
  - `test/mutations/71-archgate-kante-auf-vorrat.sh` (expect `TestArchGateConfig_EdgesMatchSkeleton`, `internal/gen/archgate_test.go:142` existiert) verankert an `  - {from: ports,    to: domain}` — `grep -c '{from: ports,    to: domain}' internal/gen/golang.go` → **0**.
  - `test/mutations/96-cpp-archgate-adapterport-kante.sh` (expect `TestArchGateConfig_CppAllowsAdapterToPorts`, `internal/gen/cpp_test.go:273` existiert und hält die **neue** Kante) streicht `{from: adapters, to: ports}` — `grep -c '{from: adapters, to: ports}' internal/gen/cpp.go` → **0**.
  Die **Tests** selbst sind grün und die Rot-Proben einmal selbst gefahren (Runde 2, mit Meldung) — die gelistete Zahnpflege ist gegen die neue Config-Form nicht nachgezogen. Übergabe: die drei Fälle auf die neue Form umschneiden (68: einen Port-Glob ins Leere zeigen lassen; 71: die Kante auf Vorrat in der neuen Form — `driven_adapters→ports_outbound` in **Go**; 96: die C++-Kante `{from: driven_adapters,  to: ports_outbound}` streichen).
- **V-2 (MEDIUM) — Handbuch `:298` nennt einen Layer, den die Config nicht führt** (Runde-2 F-2, offen; Details in DoD-Zeile 6). Übergabe: Nachzug auf `ports_inbound`/`ports_outbound` mit Richtung.
- **V-3 (MEDIUM) — [ADR-0060](../plan/adr/0060-adapter-und-ports-ordner-folgen-ihren-rollen-namen.md) Folgepflicht 3 unvollständig.** Die Prosa-Stellen der Spec tragen die neue Form (`spec/architecture.md:172-177`, `:218-220`); die **`ARC-009`-Zelle** (`spec/architecture.md:97`) nennt die neuen Ordner, die Ports-Gliederung und die Port-Kante nicht — sie beschreibt `hexslice` weiter als `domain`/`application`/`ports`/`adapters` ohne Richtungs-Segmente. Die Zelle ist nicht *falsch*, aber sie erfüllt die genannte Folgepflicht nicht. Übergabe: Zelle auf die neue Form nachziehen.
- **V-4 (LOW) — Zahn-Kommentar beschreibt einen Rot-Beleg, den es nicht gibt.** `internal/gen/archgate_test.go:227-232` behauptet *„Rot-Gegenbeispiel: test/mutations setzt den Pin auf die Vorgaenger-Fassung“* — kein Fall tut das (Bestand 365 Fälle, `ls test/mutations/ | wc -l`; keiner setzt `DefaultArchImage` auf eine Vorgänger-Fassung). Der **Rot-Beleg selbst** steht: Runde 2, Rot-Probe (c), selbst gefahren mit der exakten Meldung — der einmalige Nachweis ist erbracht; der Kommentar verweist nur auf eine Listedung, die nicht existiert. Übergabe: entweder einen Mutation-Fall ergänzen oder den Kommentar auf den tatsächlichen Beleg zurücknehmen.
- **V-5 (INFO) — Fremd-Kennungen:** die vier Implementer-Commits tragen **0** Vorkommen (`git show <c> \| grep -c 'hexslice-architecture'` → je 0). Der Repo-Bestand führt die Kennung in **5** Nicht-Zeitdokument-Dateien (Lastenheft, ADR-0009, ADR-0060, zwei `done/`-Pläne) — die Lastenheft-/ADR-0009-Vorkommen stammen vom 2026-07-24 (`git log -S` → `9ce4721b`/`e41bb78b`), sind also Bestand, nicht Einführung dieses Slices; ADR-0060 nennt die Referenz als Mess-Kontext der kanonischen Quelle (Tool-als-Quelle, ADR-0005).

## 5. Plan-vs-Code-Diff (beide Richtungen)

**Geplant und gebaut:** alle sieben §3-Tabellenzeilen getroffen (`golang.go`, `cpp.go`, `hexslice_test.go`, `cpp_test.go`, `archgate_test.go`, `full-smoke.sh`; `e2e-abdeckung.md` mit leerer Bedingung unverändert).

**Gebaut, aber nicht in der §3-Tabelle** — je mit Träger im Plan:
- `internal/emit/archgate.go` (Pin `DefaultArchImage = "ghcr.io/pt9912/a-check:v0.20.0"`, `DefaultArchDigest = "sha256:e8208764b119c606c92f82722813386277a65b12812d23b6107ea7a14dc25da1"`, `internal/emit/archgate.go:24-25`) — als Absicht in §3 Verfeinerung 3 getragen („Der a-check-Pin wandert auf v0.20.0 samt Digest“), aber keine Tabellenzeile. Kein Drift, eine Buchungs-Lücke.
- `cmd/ai-harness-init/main_test.go` (`366c8837`, 2 Zeilen): die CLI-Erwartung folgt auf den `driving`-Pfad — konsequent, nicht geplant.
- Der Plan selbst (26 + 18 Zeilen in den zwei Implementer-Commits): die Verfeinerungen als deklarierte Übergabe, nicht als DoD-Umschrift (oben geprüft).

**Zuschnitt:** vier Implementer-Commits (`366c8837` Haupt-Umbau inkl. Spec/Handbuch, `34c9e446` Referenz-Config + Pin, `6c880bb7` cpphex-Stufen-Fix, `ea8556e4` Kommentar-Nachzug), alle mit der Kennung `ADR-0060`; Accept-Umschlag `387b5210` setzt Status, Accept-Zeile und den Index-Zusatz an der [ADR-0009](../plan/adr/0009-hexslice-arch-realisierung.md)-Zeile im selben Commit (`git show 387b5210 --stat` → ADR-0060 + `docs/plan/adr/README.md`, 2 Dateien).

## 6. Übergabe an den Planner

1. **Befunde V-1 bis V-4** — V-1 bis V-3 als Folge-Slice oder Closure-Auflage schneiden (Mutations-Umschnitt, Handbuch `:298`, ARC-009-Zelle); V-4 ist Kommentar-Nachzug am Zahn.
2. **ADR-0060 Folgepflicht 6** (Bindung der Ordner-Segmente an die `arch.go`-Konstanten) ist offen und in keinem Plan getragen — beim Closure-Übergang als geplanten Ausgang im Register führen.
3. **Risiken §6:** Risiko 1 (Bestand gebootstrappter Ziele trägt das alte Skelett) steht auf „weiter offen → Sichtung“ — für die Closure vorgemerkt; Risiko 2 (Kommentar-Konsistenz) ist durch den Nachzug erfüllt messbar (`ea8556e4`); Risiko 3 (C++-Namespaces) durch die Datei-Satz-Tests und den Include-Zahn gehalten; Risiko 4/5 mit Ausgang gezogen (Plan gelesen).
4. Closure-Notiz, Register-Eintrag und die drei Paarungen stehen aus (§3.10) — mein Bericht ist die Übergabe dafür, nicht der Abschluss.

## 7. Grenzen dieses Laufs

`make mutate` nicht gefahren (~50 min, nächtliche Stufe) — die Stummheit von V-1 ist statisch gemessen (Muster vorhanden/nicht vorhanden); der Lauf selbst bestätigt sie. Die Referenz wurde nur gelesen. `make gates`/`docs-check` nicht wiederholt (Kopf-Messung).