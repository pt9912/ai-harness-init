# Verifier-Report slice-038 — Idempotenz-Klassifikation (konvergent / skip-if-present)

Rolle: Verifier (Modul 11). Frischer Kontext. Commit b2973d3.
Datum: 2026-07-23.

Sensoren real gelaufen (Docker, Host):
- `make test` -> alle Go-Pakete ok (cmd/emit/fetch/gen/wire).
- `make mutate` -> **43 ok, 0 Befund(e)**; die vier neuen slice-038-Mutationen 49/50/51/52 faerben ihren Waechter rot, die obsoleten Pre-Flight-Mutationen 12/14/23/25/46 sind entfernt.
- `make full-smoke` -> gruen inkl. der neuen Idempotenz-/kein-Prune-Sektion.
- `make gates` -> Exit 0.

---

## DoD Punkt fuer Punkt

### 1. Zweiter Init-Lauf = Exit 0 (idempotent) statt Refuse — BESTAETIGT
- Beleg Code: `cmd/ai-harness-init/main.go` — `bootstrap()` hat keinen `preflightAbsent`/`emitTargets`/`addLangTargets` mehr (alle drei Funktionen geloescht, Diff L435-489); `--force` (Flag/Usage/Parsing) raus (L86-94, L69-83); Phasenkette 4->3.
- Beleg Sensor: `harness/tools/full-smoke.sh` Idempotenz-Sektion — 2. `--lang go`-Init auf gebootstrapptem Repo, `idem_rc` MUSS 0 sein; real gruen ("IDEMPOTENT (slice-038): 2. Init-Lauf Exit 0 ...").
- Beleg Test: `TestRun_AddLangIdempotent` (main_test.go:305) — zweiter `add-lang go apps/api` Exit 0, kein Refuse.
- Der fruehere Refuse ist WIRKLICH weg: kein `preflightAbsent` mehr in nicht-Test-Code (grep bestaetigt), kein `--force`.

### 2. Konvergent-Dateien kanonisch neu / Drift geheilt — BESTAETIGT
- `writeFileMode` (enforce.go) schreibt IMMER (kanonisch, Chmod nach Write). Konvergente Emitter: `Enforce`, `Makefile`, `BaselineVerify`, `DocGate` (`d-check.mk`+`doc-gate.mk`), `BlockedFragment`, `wireLang`-Code-Gate-Fragment (`os.WriteFile` direkt), `fetch.Baseline` (aside-Swap ohne force).
- Heilen sie eine VORHANDENE modifizierte Datei? JA, jeder Test praepariert erst eine `adopter-modifiziert`-Datei und prueft, dass der Re-Lauf sie ueberschreibt:
  - `TestEnforce_Convergent` (enforce_test.go:255) — record-gates.sh geheilt + Exec-Bit gesetzt.
  - `TestBaselineVerify_Convergent` (baseline_test.go:85) — geheilt + 0755.
  - `TestMakefile_Emits` (makefile_test.go:29) — zweiter Lauf nach Drift = kanonisch.
  - `TestBlockedFragment_Convergent` (enforce_test.go:150) — driftendes blocked/go geheilt.
- Beleg Sensor: full-smoke schreibt `# drift` in die Makefile und prueft, dass der 2. Lauf sie entfernt (konvergent geheilt). Real gruen.
- Mutationen 49 (Enforce->skip clobbert Konvergenz) und 52 (blocked->skip) faerben ihren Waechter rot — beide real ok.

### 3. Skip-if-present-Dateien unberuehrt — BESTAETIGT
- `writeSkipIfPresent` (enforce.go, neu) schreibt NUR bei Abwesenheit (`os.Stat` == nil -> return nil). Skip-Emitter: `Templates`, `RootReadme`, `Commands`, `DocGate` (`.d-check.yml`), `wire.Place` (Skelett-Code).
- Halten sie eine adopter-modifizierte Datei? JA:
  - `TestTemplates_SkipIfPresent` (templates_test.go:421) — Sentinel in spec/lastenheft.md ueberlebt, andere fehlende Singletons geschrieben.
  - `TestRootReadme_SkipIfPresent` (readme_test.go:104) — gewachsenes README ueberlebt.
  - `TestCommands_SkipIfPresent` (commands_test.go:82) — adaptierte plan-welle.md ueberlebt, fehlende geschrieben.
  - `TestPlace_SkipIfPresent` (wire_test.go:90) — vorhandenes go.mod ueberlebt, fehlende Skelett-Dateien platziert.
- Beleg Sensor: full-smoke haengt `# adopter-gewachsen` ans README, prueft byte-gleich nach dem 2. Lauf. Real gruen.
- Mutationen 50 (skip-Writer -> Clobber) und 51 (wire `continue`->`break`) faerben ihren Waechter rot — real ok.

### 4. Kein Prune — BESTAETIGT (behavioral real)
- `fetch.Baseline` ersetzt nur `<tag>/` (aside-Swap in `placeBaseline`), prunt keinen anderen `.harness/`-Inhalt; konvergente Writer schreiben nur emittierte Dateien, loeschen kein Verzeichnis.
- Beleg Sensor: full-smoke kein-Prune-Sektion — sprachloser 2. Init am Mono-Repo-Ziel (`tmprepo_doc` mit `harness/mk/apps-api.mk`, `harness/mk/apps-web.mk`, `tools/harness/blocked/go`, `apps/api/go.mod`), prueft alle vier ueberleben. Real gruen. `prune_rc` MUSS 0.

### 5. `--force` entfernt (Init + add-lang) — BESTAETIGT
- Init: Usage-Block ohne `--force` (L69-83), kein `fs.Bool("force", …)` (L94), `bootstrap`/`emitAll`/`wireLang`/`fetch.Baseline`/alle Emitter-Signaturen ohne `force`.
- add-lang: `addLangUsage` ohne `--force`, `runAddLang` parst nur zwei Positionsargumente (kein FlagSet mehr), `addLang`/`wireLang` ohne force. `preflightAbsent`/`emitTargets`/`addLangTargets` geloescht.
- grep ueber `cmd`+`internal` (nicht-Test): keine `force`-Variablen/-Parameter mehr, nur noch Prosa-Erwaehnungen in Kommentaren (siehe Luecken).

### 6. `blocked/<sprache>` konvergent (Review-I-1) — BESTAETIGT
- `BlockedFragment` nutzt jetzt `writeFileMode` (konvergent), nicht mehr den skip-if-present-Zweig aus slice-037. Doc-Kommentar zitiert ADR-0007 Z.100. `TestBlockedFragment_Convergent` + Mutation 52 belegen es; ein zweites `add-lang go` schreibt byte-identisch (kein Fehler, `TestRun_AddLangIdempotent`).

### 7. `make full-smoke` Idempotenz-Fitness — BESTAETIGT (real gruen, s.o. Punkte 1/3/4).

### 8. `make gates` gruen — BESTAETIGT (Exit 0).

### 9. Doku — TEILWEISE (zwei Luecken, s.u.)
- README.md L25-28: idempotent-Zeile + `--force`-Wegfall — BESTAETIGT.
- spec/lastenheft.md LH-FA-01 (L46-48): Init sprachlos, `--lang` optional, Boundary "idempotent skip-if-present/konvergent, kein `--force`" — BESTAETIGT.
- spec/architecture.md §5 (L114-137): Idempotenz-Klassifikation je Datei — BESTAETIGT.
- ABER: **architecture.md §4 L110 WIDERLEGT** — siehe Luecke A.

---

## Unabhaengige Klassifikations-Pruefung gegen ADR-0007 Entscheidung 3 (Tabelle Z.98-103)

Jede emittierte Datei gegen die von der ADR gelistete Klasse:

| Artefakt | ADR-Klasse | Code | Urteil |
|---|---|---|---|
| Enforce (record-gates/working-tree-hash/hooks/settings.json/.gitignore/enforce.mk/guard/awk) | konvergent | writeFileMode | OK |
| Makefile-Aggregator | konvergent | writeFileMode | OK |
| BaselineVerify (baseline-verify.sh + harness/mk/baseline.mk) | konvergent | writeFileMode | OK |
| DocGate `.d-check.yml` | skip-if-present | writeSkipIfPresent | OK |
| DocGate `d-check.mk` + `doc-gate.mk` | konvergent | writeFileMode | OK (gemischte Klasse korrekt getrennt) |
| `tools/harness/blocked/<sprache>` | konvergent | writeFileMode | OK |
| `harness/mk/<modul>.mk` (add-lang) | konvergent | os.WriteFile (wireLang) | OK |
| Baseline `.harness/baseline/<tag>/` | konvergent | aside-Swap | OK |
| Doc-Chain spec/*.md, docs/plan-Struktur, Roadmap | skip-if-present | writeSkipIfPresent (Templates) | OK |
| README.md | skip-if-present | writeSkipIfPresent | OK |
| Commands `.claude/commands/*.md` | skip-if-present (Default, Z.103) | writeSkipIfPresent | OK (bewusste ADR-Abweichung final bestaetigt) |
| Skelett-Code main.go/go.mod/Dockerfile/.golangci.yml | skip-if-present | wire.Place skip | OK |
| **`.harness/skills/*`** | **konvergent (Z.100)** | **skip-if-present (via emit.Templates)** | **ABWEICHUNG — Luecke B** |

DocGate gemischte Klasse ist korrekt: `.d-check.yml` skip, `.mk` konvergent — im Code zwei getrennte Writer, exakt ADR-konform.

Kein stiller Uebergriff (skip->konvergent, das clobbern wuerde) gefunden. Die eine Abweichung (Skills) geht in die SICHERE Richtung (skip statt konvergent = Drift-nicht-geheilt, NIE Clobber — der ADR-"im-Zweifel"-Default), ist aber eine explizite ADR-Tabellen-Abweichung.

---

## Luecken (beide nicht-clobbernd, vor Closure reconcilen)

### A. architecture.md §4 L110 stale — Doc widerspricht Code + ADR + eigenem §5
`spec/architecture.md:110`: „`blocked/<sprache>` ist per-Sprache **skip-if-present**" — das ist die slice-037-Klassifikation. slice-038 hebt `blocked/<sprache>` auf **konvergent** (Code `BlockedFragment` = writeFileMode; ADR-0007 Z.100; architecture.md §5 L120-121 beschreibt bereits das konvergente kein-Prune-Verhalten). Die Zeile widerspricht damit dem eigenen §5 und dem Ist-Code. Der DoD-Punkt „architecture.md pruefen" ist an dieser Stelle NICHT sauber. Fix: L110 auf „konvergent (byte-identisch je Lauf; mehrere Module teilen ein `blocked/<sprache>`)" ziehen.

### B. `.harness/skills/*` — ADR-Tabelle konvergent, Code skip-if-present
Die Reviewer-/Closure-Skills werden als gestempelte Singletons durch `emit.Templates` (planTemplates, templates.go:145) emittiert; `emit.Templates` ist mit slice-038 UNIFORM skip-if-present. ADR-0007 Z.100 listet `.harness/skills/*` aber unter **konvergent**. Folge: ein Baseline-Bump der Skills wird beim Re-Lauf NICHT geheilt (Drift bleibt) — die sichere Fehlrichtung (nie Clobber), aber eine unversoehnte Abweichung von der expliziten ADR-Tabelle. Der Slice hat die Skills stillschweigend in „Templates = skip-if-present" gefaltet, ohne die ADR-Zeile zu adressieren. Entweder Skills konvergent emittieren (getrennter Writer-Pfad) ODER die ADR/architecture bewusst reconcilen (Skills = adopter-adaptierbar wie Commands). Nicht test-/mutations-gedeckt (Verifier-only-Klasse).

Zusatzbeobachtung (kosmetisch, nicht blockierend): stale `--force`-Prosa in Kommentaren `internal/emit/templates.go:156,178` und `internal/fetch/baseline.go:19` — beschreiben das gefallene Pre-Flight/`--force`-Modell; Verhalten korrekt, nur die Kommentare drifteten.

---

## Gesamturteil

**DoD BESTAETIGT** fuer den behavioralen + Code-Kern: idempotenter 2. Init-Lauf (Exit 0), konvergentes Heilen, skip-if-present-Erhalt, kein Prune, `--force`-Entfernung, `blocked/<sprache>` konvergent — alle durch reale Sensoren belegt (`make test`/`mutate` 43-0/`full-smoke`/`gates` Exit 0). Kein clobbernder Fehl-Klassifikations-Uebergriff.

**Zwei nicht-blockierende Reconciliation-Luecken vor Welle-Closure schliessen:**
- A. architecture.md L110 stale (blocked skip-if-present -> konvergent) — trifft direkt den DoD-Punkt „architecture.md pruefen".
- B. `.harness/skills/*` Code (skip-if-present) vs. ADR-Tabelle (konvergent) — sichere Fehlrichtung, aber explizite ADR-Abweichung, unversoehnt + ungedeckt.

Beide clobbern KEINEN Adopter-Inhalt (sichere Richtung). Empfehlung: A + B beheben (oder B als bewusste ADR-Reconciliation dokumentieren), dann Closure.
