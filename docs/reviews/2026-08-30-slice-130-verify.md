# Verifikation slice-130 — jede der vier Vorlagen trägt eine Klasse, und das Bootstrap-Ergebnis ist wieder gate-sicher

**Rolle:** Verifier (Modul 11). **Datum:** 2026-08-30. **Frischer Kontext**, kein Anteil an
Implementation, Architektur, Planung oder Review dieses Slice.

**Gegenstand:** `HEAD` = `4da4f64` (`git rev-parse --short HEAD`), Arbeitsbaum sauber
(`git status --porcelain | wc -l` → **0**, vor und nach allen Läufen dieses Berichts; der
Bericht selbst ist die erste Änderung). Die Kette, vollständig statt verkürzt
(`git log --oneline 7e6eb0b~1..HEAD`): `856eb20` (Link-Abgleich nach dem Lifecycle-Move) ·
`7e6eb0b` (Implementer) · `a880c84` (`git mv` `CO-004` → `done/`) · `5cf8245` (Link-Abgleich) ·
`4d02b6e` (Review 1) · `add5f43` (Architect, §3.7-Quellen-Klausel) · `722e272` (Fix 1) ·
`a6d436c` (Architect, `ADR-0025` + `isBrownfieldOnly` ist keine Abweichung) · `92d9c50`
(Review 2) · `7ce375a` (Planner-Fix) · `4da4f64` (Implementer-Fix).

**Maßstab:**
[slice-130](../plan/planning/done/slice-130-emitter-entscheidet-jedes-neue-template.md)
§2 DoD, der Closure-Trigger §5, die Plan-Tabelle §3, die Rückführungen §4 und die Risiken §6;
dazu [`spec/lastenheft.md`](../../spec/lastenheft.md)
([`LH-FA-01`](../../spec/lastenheft.md#lh-fa-01--repo-bootstrappen),
[`LH-FA-02`](../../spec/lastenheft.md#lh-fa-02--zweiklassige-template-ablage-f3),
[`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
[`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)),
[`ADR-0005`](../plan/adr/0005-ziel-repo-distribution.md) und
[`AGENTS.md`](../../AGENTS.md) §3.2/§3.3/§3.5/§3.6/§3.7/§3.8/§3.9.

**Nicht Maßstab:** die zwei Review-Reports
([Review 1](2026-08-30-slice-130-review.md) · [Fix-Review](2026-08-30-slice-130-fix-review.md)).
Sie sind als Kontext gelesen; ihre Verdikte sind weder als Ergebnis noch als Ausschluss
übernommen (Modul 11: *„Reviewer hat Plan + ADR. Verifier hat DoD + Spec + Plan. Andere Eingabe,
andere Findings."*). Ebensowenig Maßstab sind die Commit-Messages — sie sind die **Behauptung**,
deren Bestätigung dieser Bericht ist. **Die letzten zwei Commits (`7ce375a`, `4da4f64`) haben
keinen Review gesehen**; dieser Lauf ist der erste fremde Kontext über ihnen.

**Werkzeug-Vorbehalt.** `grep` ist auf diesem Host eine Shell-Funktion, die je nach Flag an
`ugrep` weiterreicht (`type grep | head -1` → `grep ist eine Funktion.`). **Jede** Zählung und
jede Ableitung unten läuft deshalb über `command grep`
(`command grep --version | head -1` → `grep (GNU grep) 3.11`), `awk`
(`awk --version | head -1` → `GNU Awk 5.2.1, …`), `sed` (`sed --version | head -1` →
`sed (GNU sed) 4.9`) oder `git grep`. Keine Host-Toolchain in der Befehlsposition
([`AGENTS.md`](../../AGENTS.md) §3.9): jeder Sensor-Lauf geht über `make`
(`make --version | head -1` → `GNU Make 4.3`).

---

## 1. Lauf-Rahmen — welche Sensoren gelaufen sind

Alle sechs über `HEAD` = `4da4f64` bei sauberem Baum, in dieser Reihenfolge und ohne
Nebenläufigkeit zwischen den Docker-Läufen.

| Lauf | Kommando | Ergebnis |
|---|---|---|
| Mutations-Sensor | `make mutate` | Exit **0** · `mutate: 213 ok, 0 Befund(e)` |
| Repo-Gates | `make -k gates` | Exit **2** · genau **ein** rotes Ziel: `docs-check` |
| Emit-Smoke | `make smoke` | Exit **0** · `d-check: 20 Datei(en) geprüft, 0 Befund(e)` |
| Voll-E2E | `make full-smoke` | Exit **0** · **16** `full-smoke: OK`-Zeilen, **0** `FEHLER` |
| eigener Bootstrap, Nach-Stand | Träger aus `make artifact`, `--lang go --name verify` | Prüfbereich **20** `.md` |
| eigener Bootstrap, Vor-Stand `856eb20` | dasselbe über `git archive 856eb20` | Prüfbereich **23** `.md`, **2** Befunde |

Die zwei eigenen Bootstraps sind **nicht** `make smoke` mit anderem Namen: sie legen den
emittierten Baum offen, statt nur seinen Exit-Code zu lesen. Sie tragen §4.3 und §4.4 unten und
sind der Grund, warum die Nenner-Frage hier gemessen und nicht referiert ist.

### 1.1 Die Warnung ernst genommen — was ein Instrument hier taugt

Die Beauftragung verlangt, bei jeder reproduzierten Messung zu prüfen, ob ihr Instrument selbst
Gegenstand eines Befundes war. Drei Instrumente stehen hier zur Debatte, und zwei davon habe ich
**nicht** benutzt, um die Aussage zu stützen, für die sie gebaut wurden:

- **`test/courseset-fixture.bats`, dritte Achse** — der in dieser Kette neu gebaute Wächter. Er
  leitet die wiederkehrenden Vorlagen aus dem realen Satz ab und hält sie gegen den Rumpf von
  `emit.isRecurring`. Ich habe seine Ableitung **nachgebaut statt zitiert** (§3.2): eigene
  `awk`/`command grep`-Kette über denselben Satz, unabhängig davon, ob sein Extraktor stimmt.
  Beide Mengen fallen zusammen — das ist eine Bestätigung, keine Zirkularität.
- **`make smoke`** — sein Urteil ist der **Exit-Code** des emittierten `docs-check`, nicht eine
  Zahl in einem Skript. `harness/tools/smoke.sh` trägt **keinen** Erwartungswert für die
  Dateizahl (`command grep -cE '\b(19|20|23)\b' harness/tools/smoke.sh` → **0**); die `20` ist
  Ausgabe, nicht Vorgabe. Ein „Grün durch Anpassen einer Zahl" ist an dieser Stelle
  **bauartbedingt** nicht möglich.
- **`make mutate`** — der Treiber ist fail-closed in fünf Bedingungen und belegt seine
  Vollständigkeit selbst: `Vollstaendigkeit — 213 von 213 Fall-Dateien mit Ergebnis, jede
  Fall-ID genau einmal gezogen.` Er wurde **hier und heute** gefahren, nicht aus einer
  Commit-Message übernommen.

---

## 2. DoD (1) — jede der vier Vorlagen trägt eine Klasse, die Begründung steht am Code: **ERFÜLLT**

### 2.1 Die vier Ausgänge, und dass sie aus der Vorlage selbst folgen

Der Plan gibt den Ausgang ausdrücklich **nicht** vor; er verlangt die Ableitung je Vorlage aus
ihrer eigenen Aussage. Das ist der Ist-Stand am Code, und daneben die Aussage der Vorlage, gegen
die ich ihn gehalten habe:

| Vorlage | Weiche im Code | Aussage der Vorlage (verbatim aus dem vendored Satz) |
|---|---|---|
| `docs/plan/planning/welle-results.template.md` | `isRecurring` → nicht emittiert | *„Kopiere nach `docs/plan/planning/done/welle-<NN>-results.md`"* — Platzhalter im Ziel-Pfad |
| `harness/conventions/MR-NNN-titel.template.md` | `isRecurring` → nicht emittiert | *„Kopiere nach `harness/conventions/MR-<NNN>-<titel>.md` … Ein Eintrag je Datei"* |
| `docs/plan/planning/reconciliation.template.md` | `isBrownfieldOnly` → nicht emittiert | *„Vorlage für das Reconciliation-Register eines Repos im **Brownfield-Bootstrap** … Ein reines Greenfield-Repo braucht die Datei nicht"* |
| `docs/plan/planning/observations.template.md` | keine Weiche → Singleton (Voreinstellung) | *„Kopiere nach `docs/plan/planning/observations.md` (flach, neben den offenen Wellen)"* — genau ein Ziel je Repo |

Die vier Zitate sind über
`sed -n '1,10p' .harness/baseline/v5.12.0/templates/<pfad>` an der Quelle gelesen, nicht aus dem
Plan übernommen. Sie decken den Kommentar an der jeweiligen Weiche.

### 2.2 Die Belege der `isBrownfieldOnly`-Begründung — einzeln nachgelesen

Diese Weiche ist die einzige neue, und ihr Kommentar stützt sich auf vier fremde Aussagen. Alle
vier lösen auf:

- *„Der Bootstrap kennt den Modus des Zielrepos nicht — das Werkzeug hat dafür kein Flag"* —
  gemessen: `command grep -rcE 'fs\.(String|Bool|Int)\(' cmd/ai-harness-init/main.go` → **3**,
  und die drei sind `--lang`, `--name`, `--arch`. Kein Modus-Flag.
- *„`LH-FA-01` setzt in beiden Happy-Path-Kriterien ein LEERES Git-Repo voraus"* — beide
  Akzeptanzkriterien beginnen mit *„Given leeres Git-Repo"*.
- *„`modul-02-harness-bootstrap.md` legt `reconciliation.md` in BF-Schritt 8 an … die
  GF-Schritttabelle nennt die Datei überhaupt nicht"* — Schritt 8 der BF-Tabelle nennt sie
  wörtlich; über den GF-Abschnitt gemessen:
  `sed -n '122,339p' .harness/baseline/v5.12.0/regelwerk/modul-02-harness-bootstrap.md | command grep -c 'reconciliation'`
  → **0**. Der Vendor-Schritt ist BF-3 bzw. GF-2 (*„Baseline vendoren"*), also **vor** Schritt 8.
- *„die mitemittierte `docs/plan/planning/README.md` spricht dem frischen Repo die Datei ab"* —
  der Satz *„Greenfield-Repos haben die Datei nicht."* steht in
  `templates/docs/plan/planning/README.template.md`, und diese Vorlage **wird** emittiert (§4.3).
  Der Selbstwiderspruch, den die Weiche vermeidet, ist damit real und nicht konstruiert.

### 2.3 Die Grenz-Aussage gegen den Set-Index — gemessen, und sie hält

Der Kommentar behauptet, der Set-Index des vendored Satzes sei **keine geschlossene Taxonomie**.
Gemessen: der Satz führt **22** Vorlagen
(`find .harness/baseline/v5.12.0/templates -type f -name '*.template.md' | wc -l` → 22); der
Singleton-Eimer nennt **10** Namen, der Wiederkehrend-Eimer **5**
(`sed -n '77,87p' .harness/baseline/v5.12.0/templates/README.md`). **7** Vorlagen stehen damit in
keinem Eimer. Der Kommentar nennt **5** davon; die zwei ungenannten sind die Skill-Vorlagen, die
der Set-Index in einem **eigenen** Abschnitt führt
(`command grep -c '^## Skill-Dateien$' .harness/baseline/v5.12.0/templates/README.md` → **1**).
Die Aussage ist eine **Existenz**-Aussage („die Liste ist nicht geschlossen") und keine
Vollständigkeitsaussage — als solche trägt sie.

Dass die Zuordnung von `reconciliation` upstream **gezielt** gesetzt wurde, ist ebenfalls
belegbar und nicht nur behauptet: der Vor-Tausch-Satz führte im Singleton-Eimer acht Namen
(`git show c6cc56f:.harness/baseline/v3.5.2/templates/README.md | sed -n '/Ein- vs/,/^## /p'`),
`v5.12.0` fügt genau `observations` und `reconciliation` hinzu — und lässt `welle-results` und
`MR-NNN-titel` in **beiden** Eimern aus. Derselbe Autor hat dieselbe Liste angefasst und die zwei
neuen Wiederkehrenden nicht eingetragen; das stützt die Nicht-Geschlossenheit stärker als der
Kommentar selbst es tut.

### 2.4 Was ich hier **nicht** geprüft habe

Ob die Architect-Entscheidung aus `a6d436c` (*`isBrownfieldOnly` ist keine Abweichung, kein
`MR`-Eintrag, [`MR-000`](../../harness/conventions.md#mr-000--baseline-aussage) gilt fort*)
richtig ist, ist nicht Gegenstand dieses Laufs. Geprüft ist, **ob der Code sie korrekt
wiedergibt** — und das tut er: der Kommentar sagt *„Im Adaptions-Block steht zu dieser Weiche
kein Eintrag; wo keiner steht, gilt die Baseline unveraendert"*, und das ist am Ort nachlesbar
(`command grep -c 'isBrownfieldOnly\|reconciliation.template.md' harness/conventions.md` → **0**).
[`ADR-0025`](../plan/adr/0025-register-mit-gemischten-originalen.md) steht auf `Proposed` und ist
damit nicht normativ; kein Satz dieses Slice hängt an ihr.

---

## 3. DoD (2) — vier Entscheidungen rot gesehen, Ist-Bestand vollständig geprüft: **ERFÜLLT**

### 3.1 Die vier Zähne, mechanisch bestätigt

`make mutate` über `HEAD`, Exit **0**, `mutate: 213 ok, 0 Befund(e)`, dazu die
Vollständigkeits-Schranke des Treibers (`Vollstaendigkeit — 213 von 213 Fall-Dateien mit
Ergebnis, jede Fall-ID genau einmal gezogen.`). Die sechs in dieser Kette entstandenen Fälle sind
darin einzeln als `OK` protokolliert:

| Fall | Mutation | erwarteter Wächter |
|---|---|---|
| `215-welle-results-als-singleton` | `welle-results` fällt aus `isRecurring` | `TestTemplates_EmittierterBestandVollstaendig` |
| `216-mr-eintrag-als-singleton` | `MR-NNN-titel` fällt aus `isRecurring` | dito |
| `217-reconciliation-als-singleton` | `isBrownfieldOnly` trifft nicht mehr | dito |
| `218-beobachtungsregister-nicht-emittiert` | `observations` **kommt in** `isRecurring` (Gegenrichtung) | dito |
| `219-vorlagenhinweis-driftet-lautlos` | Ziel-Pfad der Quelle bekommt einen Platzhalter | dritte bats-Achse |
| `220-kopiere-satz-verstellt` | Wortstellung des Kopiere-Satzes, Pfad unverändert | dritte bats-Achse |

Jede der **vier** Entscheidungen aus DoD (1) hat damit genau einen Fall, und die vierte
(`observations` = Singleton) ist als **Gegenrichtung** gebaut — der einzige Bau, der eine
Voreinstellung überhaupt bewachen kann. Das ist der Punkt, an dem eine Nicht-Eintragung
aufhört, stillschweigend zu sein.

**Beiläufig, aber tragend:** der Grün-Vorlauf des Treibers ist fail-closed und fuhr in diesem Lauf
auch die Modi `smoke` (9.30 s) und `full-smoke` (109.22 s) grün, bevor die erste Mutation lief
(`mutate: Gruen-Vorlaeufe (Worker, Modus, Sekunden)`). DoD (3) hat damit einen zweiten,
unabhängig erzeugten Beleg.

### 3.2 Die Definition und die Liste — eigene Ableitung statt Zitat

Der Kommentar an `isRecurring` spricht eine **Definition** aus (*„die Vorlage nennt ihren
Ziel-Pfad mit einem Platzhalter darin"*), der Rumpf hält eine **Namensliste**. Ich habe die
Definition über den realen Satz selbst ausgewertet — eigene `awk`-Blockquote-Extraktion, eigene
`command grep`-Bedingung, ohne den Wächter zu benutzen — und gegen den Rumpf gehalten:

```sh
# je in-scope-Vorlage den Kopiere-Satz lesen, Platzhalter im Ziel-Pfad -> wiederkehrend
# (eigene Kette; Ergebnis in recur_real.txt)  -> 7 Basenamen
awk '/^func isRecurring\(/{f=1} f&&/^}/{f=0} f' internal/emit/templates.go \
  | command grep -oE '"[^"]+\.template\.md"' | tr -d '"' | LC_ALL=C sort   # -> 7 Namen
```

Beide Mengen sind **identisch** und umfassen `MR-NNN-titel`, `NNNN-titel`, `carveout`,
`review-report`, `slice`, `welle-results`, `welle` — **7**. Keine Vorlage liefert eine
`OHNE-ZIEL`-Zeile, der Extraktor läuft also über allen **21** in-scope-Vorlagen an
(`find … -name '*.template.md' | command grep -v '^project-readme' | wc -l` → 21). Die
Klassen-Aussage des Codes ist damit **von außen** bestätigt, nicht vom eigenen Wächter.

### 3.3 Der Vollständigkeits-Test misst wirklich vollständig

`TestTemplates_EmittierterBestandVollstaendig` vergleicht den **ganzen** emittierten Baum
(`emittedTree`, `filepath.WalkDir`) gegen `want` per String-Join — kein `os.Stat` auf geratene
Namen. `want` führt **11** Singletons und **6** `.gitkeep`; `docs/plan/planning/observations.md`
steht darin, und die Fälle 215–218 färben genau diesen Vergleich rot. Die Erwartung ist die
**Liste**, nicht eine Zahl daneben — das schließt die Klasse aus, die der Plan fürchtet.

Die Rechnung geht auf: **21** in-scope − **7** wiederkehrend − **2** derivative Indexe − **1**
modus-gebunden = **11** Singletons, und genau diese Zahl nennt der Kommentar des bats-Falls
*„der reale Satz liefert genau 21 in-scope-Templates"*.

### 3.4 `test/courseset-fixture.bats` grün, und die Zahl ist die des realen Satzes

`make test-bats` → `1..196`, **0** `not ok`
(`command grep -cE '^not ok' gates.log` → 0, im Gate-Lauf aus §5). Die Zahl **21** im Fallnamen
ist der gemessene Ist-Stand des Satzes (Kommando in §3.2). Die zwei Fälle, die
[`CO-004`](../plan/carveouts/done/CO-004-emitter-klassifikation-offen.md) als roten
Geltungsbereich führte, sind grün — und sie sind es **mit neuen Namen** („21" statt „17",
„die sieben" statt „die fuenf"), was die Datei in ihrer Geschichte selbst festhält.

---

## 4. DoD (3) — die Gate-Sicherheit ist gemessen: **ERFÜLLT**, und die Falle ist ausgeschlossen

### 4.1 Die zwei geforderten Läufe

- `make smoke` → Exit **0**, `d-check: 20 Datei(en) geprüft, 0 Befund(e)`.
- `make full-smoke` → Exit **0**, **16** Zeilen `full-smoke: OK`, **0** `FEHLER`
  (`command grep -c '^full-smoke: OK' fullsmoke.log` → 16;
  `command grep -c 'FEHLER' fullsmoke.log` → 0). Darin: `make -j gates` im emittierten Repo
  grün, in **beiden** Bootstrap-Varianten (`--lang go` und doc-only), jeweils mit
  `d-check: 20 Datei(en) geprüft, 0 Befund(e)`.

### 4.2 Die Falle, vor der der Plan selbst warnt — auf eigener Achse ausgeschlossen

Der Plan verlangt, **vor** dem Nachziehen einer Erwartung zu prüfen, welche Ursache die Befunde
trägt. Ich habe das nicht aus den Reports übernommen, sondern denselben Bootstrap über dem
**Vor-Stand** `856eb20` (= `7e6eb0b^`, `git rev-parse --short 7e6eb0b^`) gefahren und das
emittierte `docs-check` dort laufen lassen:

```
Vor-Stand 856eb20:  d-check: 23 Datei(en) geprüft, 2 Befund(e)
  docs/plan/planning/welle-results.md:67   observations.template.md   target-missing
  docs/plan/planning/welle-results.md:83   ../observations.md         target-missing
Nach-Stand 4da4f64: d-check: 20 Datei(en) geprüft, 0 Befund(e)
```

Und der Prüfbereich selbst, als Mengendifferenz statt als Zahl
(`LC_ALL=C comm` über die zwei sortierten Dateilisten):

- **nur im Vor-Stand** (aus dem Prüfbereich gefallen): `docs/plan/planning/reconciliation.md`,
  `docs/plan/planning/welle-results.md`, `harness/conventions/MR-NNN-titel.md` — **3**;
- **nur im Nach-Stand** (neu hinzugekommen): **keine**;
- **gemeinsam**: **20**.

Daraus folgt dreierlei, und keines davon ist eine Plausibilität:

1. Der Nenner fällt um **genau die drei Dateien**, deren Nicht-Emission die drei
   Nicht-Singleton-Entscheidungen dieses Slice sind. Keine vierte Datei verlässt den Prüfbereich.
2. Beide Befunde des Vor-Stands lagen in **einer** dieser drei (`welle-results.md`). Die
   verbleibenden **20** Dateien trugen also schon vor dem Slice **0** Befunde und tragen sie
   danach immer noch — es ist **kein** Befund mit dem Nenner verschwunden.
3. Die zwei anderen entfernten Dateien (`reconciliation.md`, `MR-NNN-titel.md`) trugen **0**
   Befunde; ihre Entfernung kauft kein Grün, sie folgt allein aus ihrer Klasse.

Das Grün ist damit **nicht** durch Schrumpfen erkauft: es entsteht dadurch, dass genau die eine
Datei nicht mehr emittiert wird, deren Emission selbst der Defekt war — und das ist die im Plan
vorgesehene Auflösung („Diese zwei lösen sich mit der Klassen-Entscheidung, nicht durch
Link-Arbeit"), nicht ihre Umgehung.

### 4.3 Der emittierte Bestand, direkt besichtigt

Aus meinem eigenen Bootstrap über `HEAD` (Träger aus `make artifact`, `--lang go --name verify`):

- `docs/plan/planning/observations.md` — **DA** (die Singleton-Entscheidung ist im realen Ziel
  wirksam, nicht nur in der Fixture);
- `docs/plan/planning/reconciliation.md` — **nicht da**;
- `docs/plan/planning/welle-results.md` und `docs/plan/planning/done/welle-results.md` — **nicht da**;
- `harness/conventions/MR-NNN-titel.md` — **nicht da**, und das Verzeichnis
  `harness/conventions/` entsteht gar nicht erst;
- `.template.md` außerhalb der vendored Baseline: **0**
  (`command grep -c '\.template\.md$' tree.txt` über den Baum ohne `.harness/baseline/`) — keine
  co-located Kopie, das referenzierte Modell aus
  [`ADR-0005`](../plan/adr/0005-ziel-repo-distribution.md) hält.

Der emittierte Baum umfasst **51** Dateien ohne die vendored Baseline; **20** davon liegen im
Prüfbereich des emittierten Doku-Gates (`roots: ["."]`, `ignore: **/*.template.md, .tmp/**,
.harness/**` aus `internal/emit/templates/d-check.yml`). Die **20** meiner eigenen Filterung und
die **20** aus `d-check: 20 Datei(en) geprüft` sind dieselbe Menge — die Zahl im Smoke-Output ist
damit als Prüfbereich identifiziert und nicht als beliebiger Zähler.

### 4.4 Sind `LH-FA-01` und `LH-FA-02` wirklich eingelöst?

**`LH-FA-01` — ja, und beide Akzeptanzkriterien einzeln.** `make full-smoke` fährt den Bootstrap
in ein leeres tmp-Repo und dort `make -j gates`: Exit 0 in der `--lang go`-Variante (Doc-Gate +
Code-Gates) **und** in der sprachlosen Variante (Doc-Gate). Damit sind *Happy Path (Init,
sprachlos)* und *Happy Path (One-Shot-Kurzform)* beide belegt, dazu die Boundary-Zusage
(Idempotenz, 2. Init-Lauf) aus derselben Lauf-Ausgabe.

**`LH-FA-02` — die tragende Zusage ja, der Text der Anforderung nein.** Die Zusage *„Der
emittierte Stand ist damit out-of-the-box **gate-sicher**"* ist eingelöst und zum ersten Mal seit
dem Baum-Tausch wieder gemessen: 0 Befunde über 20 Dateien, in zwei Varianten, plus der
zusätzliche Feldlisten-Zahn des Voll-Smokes, der belegt, dass das Ziel-Gate **Zähne hat** und
nicht nur schweigt. Nicht eingelöst ist die **Aufzählung** derselben Anforderung: sie nennt fünf
wiederkehrende Vorlagen, `emit.isRecurring` führt sieben, und die Disposition
*modus-gebunden* kommt in ihr nicht vor. Das ist ein Text-Rückstand in Rang 1, kein Defekt am
Code — sein Träger ist
[slice-139](../plan/planning/open/slice-139-lastenheft-deckt-die-emit-disposition.md), und der
Weg dorthin ist durch
[`MR-015`](../../harness/conventions.md#mr-015--change-request-bei-personalunion-von-auftraggeber-und-entwickler)
vorgeschrieben (*„weder ADR noch Slice dürfen `LH-*` je ändern"*). **Der Slice hat hier korrekt
gehandelt, indem er nicht gehandelt hat.**

**Was `LH-FA-02` nicht zusagt und dieser Lauf trotzdem gesehen hat:** zwei emittierte Dateien
tragen weiterhin `BEDIENHINWEIS`-Blöcke, die von sich selbst sagen, sie fielen beim Kopieren weg
(`docs/plan/planning/observations.md`, `docs/plan/planning/in-progress/roadmap.md` — gemessen im
eigenen Bootstrap, `command grep -rl 'BEDIENHINWEIS' <ziel> --include='*.md' | command grep -v '/.harness/' | wc -l`
→ **2**). Sie sind **gate-neutral** (HTML-Kommentar, kein Link) und berühren DoD (3) nicht; ihr
Träger ist
[slice-140](../plan/planning/open/slice-140-emittierter-stand-ohne-vorlagen-hilfen.md). Im
Vor-Stand waren es **3** — die dritte war `reconciliation.md`, dieser Slice hat sie beiläufig
mitgenommen.

---

## 5. DoD (4) — `make gates` ohne die `CO-004`-Ausnahme: **ERFÜLLT in der Sache**, mit Textbefund `V-1`

`make -k gates` (keep-going; ohne `-k` verdeckt der erste Abbruch die übrigen Ziele), Exit **2**,
und genau **ein** rotes Ziel:
`command grep -cE '^make(\[[0-9]+\])?: \*\*\*' gates.log` → **1**, nämlich
`make: *** [d-check.mk:66: docs-check] Fehler 1`.

- `docs-check`: `d-check: 467 Datei(en) geprüft, 1 Befund(e)`, und der eine ist
  `harness/conventions.md:1019 → ../.harness/baseline/v3.5.2/regelwerk/modul-08-agentenrollen.md#rollen-sequenz-für-einen-slice · target-missing`
  — Zeichen für Zeichen der Geltungsbereich von
  [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md), Träger
  [slice-132](../plan/planning/in-progress/slice-132-adaptions-block-ohne-totes-ziel.md). **Kein
  Befund dieses Slice.**
- Grün und selbst gesehen: `baseline-verify: v5.12.0 OK — 51 Dateien`, `lint`, `build`,
  `test-bats` (`1..196`, 0 `not ok`), `test-go`, `shell-lint`, `ci-lint`,
  `comment-claims: 46 Datei(en) geprueft, 0 Befund(e)`, `host-bin`,
  `span-check: Traeger vorhanden, span-emit hat einen Span geschrieben, Ablageort git-ignoriert`.
- **Kein roter Fall außerhalb von `CO-005`.** Die zwei Fälle, die `CO-004` ausnahm, sind grün;
  eine Konfiguration, die einen Fall ausnimmt, gibt es nicht
  (`git grep -c 'CO-004' -- test/ .d-check.yml Makefile` → kein Treffer, Exit 1).

**Der Carveout-Vollzug ist vollständig und formgerecht.** `docs/plan/carveouts/done/CO-004-…md`
liegt in `done/`; der Move ist ein **eigener** Commit (`a880c84`, `1 file changed, 0 insertions`,
Rename erkannt) und der Link-Abgleich ein zweiter (`5cf8245`) —
[`AGENTS.md`](../../AGENTS.md) §3.3 eingehalten. Die Index-Zeile steht in
`docs/plan/carveouts/README.md` unter `## Aufgelöst` mit Auflösungs-Datum und auflösendem Slice.

**Der Befund ist der DoD-Text, nicht der Baum** — siehe `V-1` in §10.

---

## 6. DoD (5) — Doku-Update, falls ein öffentlicher Vertrag berührt ist: **ERFÜLLT (kein Trigger im Slice)**

Berührt ist genau ein öffentlicher Vertrag: `LH-FA-02` (§4.4). Er ist Rang 1 und darf von einem
Slice nicht geändert werden; der Plan sieht dafür in §3 ausdrücklich *„unverändert"* und in §6
die Übergabe vor. Beides ist eingehalten und gemessen:

- `git diff --name-only 7e6eb0b^..HEAD -- spec/ | wc -l` → **0**.
- Der Träger existiert als lebendes Artefakt:
  [slice-139](../plan/planning/open/slice-139-lastenheft-deckt-die-emit-disposition.md) in
  `open/`, und er führt beide heimatlosen Posten — die Fünf-gegen-Sieben-Lücke **und**
  [`ADR-0020`](../plan/adr/0020-emittierte-modul-15-regeln.md), die dieselbe Menge nennt und
  `Accepted`, also immutabel ist.

Kein anderer öffentlicher Vertrag ist berührt:
`command grep -rn 'wiederkehrend\|isRecurring\|Singleton' harness/README.md README.md` → kein
Treffer; `spec/spezifikation.md` und `spec/architecture.md` führen die Aufzählung nicht.

---

## 7. Die vier Closure-Punkte — **noch nicht fällig**, nicht „nicht erfüllt"

Die DoD-Haken sind ungesetzt, §6 trägt noch die `<…>`-Ausgangs-Platzhalter, §7 ist leer
(`<!-- Erst nach Abschluss füllen. -->`). Das ist der **erwartete** Zustand zum
Verifikations-Zeitpunkt: Haken, §6-Ausgänge, §7 und der Lifecycle-Move setzt der **Planner**, und
der Verifier läuft davor (Präzedenz `slice-080`, `slice-133`, `slice-138`). Ich führe sie
deshalb als **noch nicht fällig** und **nicht** als unerfüllt:

| DoD-Punkt | Stand |
|---|---|
| Closure-Notiz mit Steering-Loop-Lerneintrag | noch nicht fällig (Planner) |
| Beobachtungs-/Reconciliation-Register-Item entfällt mit Grund, notiert in §7 | noch nicht fällig (Planner); die Grundlage steht: das Repo führt beide Register nicht (`ls docs/plan/planning/observations.md docs/plan/planning/reconciliation.md` → beide fehlen), und die Dogfood-Frage liegt bei `slice-137` |
| Jedes Risiko aus §6 trägt einen Ausgang | noch nicht fällig (Planner) — gemessene Eingabe in §8 |
| Die drei Paarungen (Anker · Folge-Slice · Register) | nach DoD-Text von der [welle-10](../plan/planning/welle-10-re-baseline.md)-Closure geprüft, nicht hier |

---

## 8. §6-Risiken — gemessene Eingabe für die Ausgänge des Planners

| Risiko | Gemessener Sachverhalt | Naheliegender Ausgang |
|---|---|---|
| `LH-FA-02` zählt die Wiederkehrenden namentlich auf (fünf) | **eingetreten**: zwei Entscheidungen fielen auf *wiederkehrend*, `emit.isRecurring` führt sieben (§3.2). Übergabe existiert als lebender Träger | *eingetreten* — Slice-ID [slice-139](../plan/planning/open/slice-139-lastenheft-deckt-die-emit-disposition.md) nachtragen |
| Eine „unklassifiziert, nicht emittiert"-Liste wäre der Rückfall | **entfallen**: `emit.inScope` ist über die ganze Kette unangetastet (`git diff 7e6eb0b^..HEAD -- internal/emit/templates.go \| command grep -cE '^[+-][^+-].*func inScope'` → **0**); keine Warteliste entstanden, je Vorlage wurde entschieden | *entfallen* |
| `reconciliation.template.md` ist modus-gebunden | **eingetreten**: benannte Entscheidung mit Begründung am Code (`isBrownfieldOnly`, §2.2), Zahn in `test/mutations/217` | *eingetreten* — **aber nicht mit dem Wortlaut des Plans**, s. `V-2` |
| `make full-smoke` läuft nicht offline | **entfallen**: der Lauf gelingt und ist protokolliert (Exit 0, §4.1) | *entfallen* |

---

## 9. Plan-vs-Code-Diff, beide Richtungen

### 9.1 Zugesagt und geliefert

| Plan §3 | Ist |
|---|---|
| `internal/emit/templates.go` update | geliefert: `isRecurring` +2 Namen, neue Weiche `isBrownfieldOnly`, `planTemplates` +Voreinstellungs-Kommentar |
| `internal/emit/templates_test.go` update | geliefert: `courseSet()` +4 Pfade, `want` +`observations.md`, Kommentar-Nachzug |
| `test/courseset-fixture.bats` update | geliefert: Zahlen nachgezogen **und** eine dritte Achse gebaut, die es im Plan nicht gab (§9.2) |
| `test/mutations/` ggf. neu | geliefert: **6** neue Fälle (215–220) |
| `CO-004` auflösen (`git mv` nach `done/`) | geliefert, formgerecht in zwei Commits (§5) |

### 9.2 Gebaut, ohne so in der Plan-Tabelle zu stehen

- **Die dritte bats-Achse** (*„emit.isRecurring fuehrt genau die Vorlagen mit Platzhalter im
  Ziel-Pfad"*) samt `kopiere_ziel`/`wiederkehrend_real`/`wiederkehrend_code` und den zwei Fällen
  219/220. Der Plan sagt „`test/courseset-fixture.bats` update — der Wächter, der die Frage
  gestellt hat: Fixture-Abgleich und in-scope-Zahl"; ein Wächter über die **Definition** war
  nicht zugesagt. Er ist eine Antwort auf einen Review-Befund und ist **mehr** als geplant, nicht
  anderes — er verschiebt weder eine Zusage noch eine Grenze des Slice.
- **`docs/plan/carveouts/README.md`** (Index-Zeile). Steht nicht in der §3-Tabelle, wohl aber in
  DoD (4) (*„die Index-Zeile umgehängt"*) — Plan-intern gedeckt.
- **Der Link-Abgleich `5cf8245`** über **14** Dateien, darunter vier Zeitdokumente
  (`docs/reviews/**`) und drei `done/`-Slice-Dateien. Mechanische Folge des `git mv`; ohne ihn
  wäre `docs-check` rot. Nicht im Plan, aber ohne Alternative.

### 9.3 Zugesagt und ausdrücklich **nicht** angefasst — bestätigt

- `spec/lastenheft.md`: `git diff --name-only 7e6eb0b^..HEAD -- spec/ | wc -l` → **0**.
- `internal/emit/templates/`, `.harness/skills/`:
  `git diff --name-only 7e6eb0b^..HEAD -- internal/emit/templates/ .harness/skills/ | wc -l` → **0**.
- Eine `observations.md`/`reconciliation.md` **in diesem** Repo: beide existieren nicht.
- Der gesamte Code-Eingriff bleibt in zwei Dateien:
  `git diff --name-only 7e6eb0b^..HEAD -- internal/` → `internal/emit/templates.go`,
  `internal/emit/templates_test.go`.

---

## 10. Befunde

Keiner blockiert die Closure. `V-1` bis `V-3` sind **Entscheidungsbedarf beim Planner** und
gehören in die Closure-Notiz; `V-4` bis `V-6` sind Notizen ohne Handlungszwang.

### V-1 — Entscheidungsbedarf (Planner): DoD (4) und §5 verlangen wörtlich mehr, als jede korrekte Ausführung dieses Slice liefern kann

- **quelle:** Slice-Plan §2 DoD (4), §5 Closure-Trigger · [`AGENTS.md`](../../AGENTS.md) §3.6
- **pfad:** `docs/plan/planning/in-progress/slice-130-…md` §2 und §5
- **befund:** Beide sagen *„`make gates` grün — **ohne** die Ausnahme aus `CO-004`"*. Gemessen
  ist `make -k gates` Exit **2** mit einem roten Ziel, und dessen Ursache ist
  [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md), dessen Träger
  [slice-132](../plan/planning/in-progress/slice-132-adaptions-block-ohne-totes-ziel.md) in `open/`
  liegt. Der Slice kann `CO-005` nicht auflösen; das Kriterium ist damit **unter keiner
  korrekten Ausführung** wörtlich erreichbar — genau die Falle, die der Plan in §4 für den
  Start-Trigger selbst benennt (*„Ein Kriterium, das unter keiner korrekten Ausführung dieses
  Slice grün werden kann, ist keine Abnahme, sondern eine Falle"*). Der Schwester-Slice hatte die
  tragfähige Formulierung: `slice-133` §2 schreibt *„`make gates` grün, **soweit die Welle es
  zulässt**"* und nennt beide Carveouts.
- **erfüllt ist die Sache:** kein rotes Ziel, das diesem Slice gehört; die `CO-004`-Fälle sind
  grün und ohne Konfigurations-Ausnahme.
- **verifizierbar:** ja — `make -k gates` und die Ein-Zeilen-Zählung der `***`-Zeilen.
- **klasse:** Abnahme-Kriterium weiter gefasst als der Gegenstand des Slice.

### V-2 — Entscheidungsbedarf (Planner): der Ausgangs-Text von §6 Risiko 3 zitiert eine Quelle, auf der die Code-Begründung nicht mehr ruht

- **quelle:** Slice-Plan §2/§6 · [`MR-017`](../../harness/conventions.md#mr-017--default-regel-für-emittierte-prüfbereiche-fail-closed)
- **pfad:** `docs/plan/planning/in-progress/slice-130-…md` §6, dritter Aufzählungspunkt
- **befund:** Der vorformulierte Ausgang lautet *„eingetreten: benannte Entscheidung mit
  Begründung am Code, **nach dem Fehlerbild aus `MR-017`** — laut falsch schlägt leise falsch".
  `MR-017`s eigener Geltungsbereich ist *„jede vom Tool **emittierte** Gate-Konfiguration"*; die
  Frage, **ob** eine Vorlage emittiert wird, fällt nicht darunter. Der Code hat den Verweis mit
  `4da4f64` folgerichtig gestrichen. Wer den Ausgang jetzt wörtlich abschreibt, setzt eine
  Begründung in ein lebendes Artefakt, die der Code nicht mehr trägt.
- **verifizierbar:** nein — Textkonsistenz zwischen Plan und Kommentar, kein Gate-Gegenstand.
- **klasse:** Plan-Formulierung überlebt die Quelle, auf die sie zeigt.

### V-3 — Entscheidungsbedarf (Planner): §4 und §6 decken denselben eingetretenen Sachverhalt mit verschiedenen Folgen

- **quelle:** Slice-Plan §4 (Rückführungen) und §6 (Risiko 1) ·
  [`MR-015`](../../harness/conventions.md#mr-015--change-request-bei-personalunion-von-auftraggeber-und-entwickler)
- **pfad:** `docs/plan/planning/in-progress/slice-130-…md` §4 zweiter Rückführungs-Fall vs. §6
  erster Punkt
- **befund:** §4 sagt: *„`in-progress` → `open` … wenn eine der vier Entscheidungen die
  Aufzählung in `LH-FA-02` ändern müsste … und `CO-004` bleibt bis zur Antwort bestehen"*. §6
  Risiko 1 sagt für denselben Fall: *„eingetreten: Übergabe an die schreibende Rolle des
  Lastenhefts, Slice-ID nachtragen"*. Der Sachverhalt ist eingetreten (zwei Entscheidungen auf
  *wiederkehrend*, §3.2); der Lauf folgte §6, hielt den Slice in `in-progress` und löste
  `CO-004` auf. **Der Weg ist inhaltlich richtig** — nach
  [`MR-015`](../../harness/conventions.md#mr-015--change-request-bei-personalunion-von-auftraggeber-und-entwickler)
  darf ein Slice `LH-*` **nie** ändern, weshalb §4s Bedingung *„ändern müsste"* von keinem
  Slice je erfüllbar ist und die §4-Kante damit leerläuft. Was fehlt, ist die **Notiz**: steht
  die Wahl nicht in der Closure, bleibt §4 als scheinbar übergangene Kante stehen, und der
  nächste Plan schreibt dieselbe tote Bedingung ab.
- **verifizierbar:** nein.
- **klasse:** zwei Plan-Klauseln über denselben Auslöser mit unvereinbarer Folge.

### V-4 — Notiz: zwei der vier Zeilen, die dieser Slice `TestTemplates_RecurringNichtEmittiert` hinzufügt, können unter keiner Mutation rot werden

- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 (*„Ein Test … muss die Eigenschaft messen"*)
- **pfad:** `internal/emit/templates_test.go`, Liste in `TestTemplates_RecurringNichtEmittiert`
- **befund:** Der Slice trägt vier Pfade nach: `welle-results.template.md`,
  `MR-NNN-titel.template.md` und die zwei transformierten `.md`-Formen. Die **`.template.md`**-
  Hälfte ist unter der heutigen Emit-Regel unerreichbar: `singletonTarget` erzeugt ausschließlich
  `.md`-Ziele, und kein anderer Schreibpfad existiert
  (`command grep -rn 'template\.md' internal/emit/*.go` ohne `_test.go` → nur Vergleiche,
  kein Schreiben). Die zwei Zeilen sind Über-Deckung gegen eine 0.7.0-Form, die der Code nicht
  mehr kennt — kein Zahn. **Keine DoD-Verletzung:** DoD (2) benennt den
  Vollständigkeits-Test als Träger, und der ist lebendig (§3.3); die Zähne der zwei
  Entscheidungen sitzen in `215`/`216`. Das Muster ist geerbt, nicht neu.
- **verifizierbar:** ja — eine Mutations-Sonde auf diese zwei Zeilen bliebe grün.
- **klasse:** Abwesenheits-Zusicherung auf einen Pfad, den der Code nicht schreiben kann.

### V-5 — Notiz: zwei bats-Fälle, deren Text dieser Slice geändert hat, haben keinen kuratierten Mutations-Fall

- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6
- **pfad:** `test/courseset-fixture.bats`, Fälle *„der reale Satz liefert genau 21
  in-scope-Templates"* und *„die sieben wiederkehrenden Templates existieren real"*
- **befund:** Beide Aussagen wurden in `7e6eb0b` neu gefasst (aus „17" bzw. „die fuenf"). Ihre
  rot färbende Mutation läge im **realen Satz** — und dort greift nur ein Fall-Paar:
  `command grep -l 'harness/baseline' test/mutations/*.sh` → nur `219` und `220`, beide auf die
  dritte Achse gerichtet. Die Lücke ist geerbt (die Vorgänger-Fassungen hatten sie ebenso) und
  liegt außerhalb der **vier** Entscheidungen, die DoD (2) aufzählt; `make mutate` prüft
  ausdrücklich Haltbarkeit gelisteter Wächter, nicht Entstehung neuer.
- **verifizierbar:** ja — ein Fall, der eine wiederkehrende Vorlage aus dem vendored Satz
  entfernt, färbte beide rot.
- **klasse:** geänderte Zusage ohne eigenen kuratierten Gegenbeispiel-Fall.

### V-6 — Notiz: kein einzelner Sensor prüft die vier Entscheidungen am **realen** emittierten Baum

- **quelle:** [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
- **pfad:** `harness/tools/smoke.sh`, Gegenproben-Schleife
- **befund:** `make smoke` ist der einzige Lauf, der realen Satz **und** Emit-Regel zusammen
  fährt. Seine positive Liste (`AGENTS.md`, `docs/plan/adr/.gitkeep`, `roadmap.md`,
  `.harness/skills/reviewer.md`) und seine Gegenprobe (`slice.template.md`,
  `NNNN-titel.template.md`, `adr/README.md`, `carveouts/README.md`, `README.md.md`, …) führen
  **keinen** der vier Slice-Gegenstände. Belegt sind sie über eine **Komposition**: die
  bats-Fixture-Achse hält `courseSet()`-Pfade == realen Satz, und die Emit-Entscheidung hängt
  ausschließlich am Pfad (`inScope`/`isRecurring(path.Base)`/`isDerivativeIndex`/
  `isBrownfieldOnly`) — Pfad-Gleichheit überträgt das Urteil also vollständig. Die Komposition
  trägt; **ein** Lauf, der die vier Namen im realen Ziel behauptet, existiert trotzdem nicht.
  Ich habe ihn für diesen Bericht von Hand hergestellt (§4.3).
- **verifizierbar:** ja — vier Zeilen in der bestehenden Schleife von `smoke.sh`.
- **klasse:** Deckung nur über Komposition, kein Sensor auf der Aussage selbst.

---

## 11. Negativbefunde (geprüft, ohne Befund)

- **`AGENTS.md` §3.3** (Move + Rewrite getrennt): `a880c84` ist ein reiner Rename
  (`1 file changed, 0 insertions(+), 0 deletions(-)`), `5cf8245` der Inhalts-Commit.
- **`AGENTS.md` §3.5** (Gates nicht ohne ADR lockern): keine Schwelle gesenkt; die zwei
  `CO-004`-Fälle sind **schärfer** geworden (17→21, fünf→sieben), und es kam eine dritte Achse
  hinzu. `.d-check.yml`, `Makefile`, `test/` tragen keine Kennung
  (`git grep -c 'CO-004' -- test/ .d-check.yml Makefile` → kein Treffer).
- **`AGENTS.md` §3.2** (Lint-Suppression): `make shell-lint` grün; die zwei neuen
  Mutations-Fälle umgehen `SC2016` konstruktiv (`printf '\140'`, Muster ohne Backticks) statt
  per Inline-Suppression.
- **`AGENTS.md` §3.8** (Norm-Artefakte schreibt der Architect): die zwei Architect-Commits
  (`add5f43`, `a6d436c`) berühren ausschließlich `AGENTS.md`/`harness/conventions.md` bzw.
  ADR + ADR-Index; kein Implementer-Commit dieser Kette fasst eines davon an
  (`git diff --name-only 7e6eb0b^..HEAD` je Commit geprüft).
- **§3.7-Quellen-Klausel gegen die neuen Kommentare:** die einzige Stelle mit einer Slice-Nummer
  als Erzählung im nach `add5f43` geschriebenen Diff ist *„…, an dem slice-024s Voll-Smoke die
  wiederkehrenden Vorlagen entschied"*. Sie ist **satzweise byte-identisch getragen** — derselbe
  Satz steht in `7e6eb0b` (vor der Klausel), `722e272` und `HEAD`, nur anders umbrochen
  (`git show <rev>:internal/emit/templates.go | awk '/^\/\/ isBrownfieldOnly/{f=1} f&&/^func /{f=0} f' | tr '\n' ' '`
  über die drei Stände, identisches Ergebnis). Der Fix-Review hat die Cutoff-Einheit begründet
  auf die **Aussage** gelegt; unter dieser Lesart ist die Stelle nicht gebunden. Ich stelle die
  Lesart nicht in Frage — ich melde, dass sie **tragend** ist: unter der Zeilen-Lesart wäre es
  ein Verstoß.
- **Spannung DoD (1) ↔ §3.7:** DoD (1) verlangt die Begründung am Code, §3.7 legt die Abwägung
  in die ADR. Der Fix-Review führt sie als INFO-2; sie ist eine **Planungs-/Architektur**-Frage
  und keine DoD-Verletzung — der Code erfüllt die DoD, die zum Zeitpunkt des Schnitts galt.
- **Dogfood-Ebene sauber getrennt:** das Werkzeug emittiert `observations.md`, dieses Repo führt
  keine. Der Plan nimmt die Frage ausdrücklich aus (§1, §3), der Träger ist `slice-137`. Kein
  Selbstwiderspruch im **emittierten** Stand: die mitemittierte `planning/README.md` nennt das
  Register und spricht dem Greenfield-Repo `reconciliation.md` ab — beides deckt sich mit dem,
  was emittiert wird (§4.3).
- **`emit.inScope` bleibt Regel statt Allowlist**, und keine „unklassifiziert"-Liste ist
  entstanden (§8, Risiko 2).
- **Reproduzierbarkeit ([`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)):**
  die `20`/`0` aus `make smoke` und die `20`/`0` aus dem `make -j gates` des Ziels im
  `full-smoke` stimmen mit meinem eigenen Bootstrap überein — drei unabhängig erzeugte
  Ziel-Bäume, dieselbe Menge.
- **`docs/plan/carveouts/README.md`-Zelle:** die *Wodurch*-Spalte ist eine Beleg-Spalte, keine
  `Stand`-/`Status`-Zelle; der §3.7-Zustandsfeld-Teil greift dort nicht.

---

## 12. Rückführungen nach §4 — **keine ausgelöst**

- `in-progress` → `next` (*zu groß*): der Nachweis aus DoD (3) zeigt **keinen** Fehler im
  Bootstrap-Pfad selbst — Exit 0 in beiden Smokes, 0 Befunde im Ziel, und die einzige daneben
  gefundene Auffälligkeit (`BEDIENHINWEIS`) ist gate-neutral und hat einen eigenen Träger.
- `in-progress` → `open` (*blockiert*): siehe `V-3`. Die Kante ist nach
  [`MR-015`](../../harness/conventions.md#mr-015--change-request-bei-personalunion-von-auftraggeber-und-entwickler)
  von keinem Slice erfüllbar; der gewählte Weg (§6, Übergabe mit lebendem Träger) ist der
  einzige zulässige.

---

## 13. Verdikt

**DoD-KONFORM. Der Slice kann zur Closure.**

Die drei Liefer-Punkte sind erfüllt und je einzeln auf eigener Achse nachgemessen, nicht aus den
Reports übernommen: die vier Klassen folgen aus der Aussage ihrer Vorlage und stehen am Code
(§2); jede der vier Entscheidungen hat einen Zahn, der in diesem Lauf mechanisch rot wurde, und
der Vollständigkeits-Test misst den ganzen Baum (§3); die Gate-Sicherheit ist gemessen, und die
Nenner-Falle ist durch einen eigenen Bootstrap über dem Vor-Stand **ausgeschlossen** — die drei
Dateien, die den Prüfbereich verlassen, sind exakt die drei nicht mehr emittierten, beide Befunde
lagen in einer davon, und die verbleibenden 20 Dateien waren vorher wie nachher befundfrei (§4.2).

**`LH-FA-01` ist eingelöst**, in beiden Bootstrap-Varianten. **`LH-FA-02` ist in seiner tragenden
Zusage eingelöst** — *out-of-the-box gate-sicher*, zum ersten Mal seit dem Baum-Tausch —, während
seine **Aufzählung** dem Code nachläuft; das ist ein Rang-1-Textrückstand mit eigenem Träger und
kein Mangel dieses Slice, denn ein Slice darf `LH-*` nicht ändern.

**Was blockiert, blockiert nichts:** `V-1` bis `V-3` sind drei Sätze, die in die Closure-Notiz
gehören und die der **Planner** schreibt — das Abnahme-Kriterium, das weiter greift als der
Slice; ein Risiko-Ausgang, der eine gestrichene Quelle zitiert; und zwei Plan-Klauseln über
denselben Auslöser. `V-4` bis `V-6` sind Deckungs-Notizen ohne Handlungszwang. Keiner der sechs
Befunde verlangt eine Code-Änderung.

**Der einzige rote Gate-Lauf gehört nicht diesem Slice:** `docs-check`, ein Befund, `CO-005`,
Träger `slice-132`.
