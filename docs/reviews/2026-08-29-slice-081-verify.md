# Verifikation slice-081 — Baum tauschen, Pin ziehen, Verweise nachziehen

**Rolle:** Verifier (Modul 11). **Datum:** 2026-08-29. **Frischer Kontext**, kein Anteil an
Implementation, Architektur, Planung oder Review dieses Slice.

**Gegenstand:** `HEAD` = `06a640a` (`git rev-parse --short HEAD`), Arbeitsbaum sauber
(`git status --porcelain=v1 | wc -l` → **0**, vor und nach allen Läufen dieses Berichts). Die
Commit-Kette gegen den Vor-Stand `c6cc56f`: `e4a4909` (Lifecycle-Move) · `3871da4` · `b902b60`
(Implementer) · `d72e6dd` · `d6f3ae8` (Architect) · `26aec2c` (Planner) · `2b0d741` · `eaccc4d`
(Architect) · `ff2a1dd` (Review-Ablage) · `db855a1` · `d12b938` (Planner, Fix-Runde) · `62bc866` ·
`06a640a` (Architect).

**Maßstab:** [slice-081](../plan/planning/done/slice-081-baum-tauschen-pin-ziehen.md) §2 DoD
(1)–(6) — **DoD (4) ausdrücklich in der Fassung mit drei Sensoren**, wie §5 sie hervorhebt —, der
Closure-Trigger §5, der Plan §3 und die Risiken-Ausgänge §6; dazu
[`spec/lastenheft.md`](../../spec/lastenheft.md)
([`LH-FA-01`](../../spec/lastenheft.md#lh-fa-01--repo-bootstrappen),
[`LH-FA-02`](../../spec/lastenheft.md#lh-fa-02--zweiklassige-template-ablage-f3),
[`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
[`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)) und
[`AGENTS.md`](../../AGENTS.md) §3.4, §3.5, §3.6, §3.8, §3.9.

**Abgrenzung, und sie ist tragend.** Der Reviewer hatte *Plan + ADR*, dieser Bericht hat
*DoD + Spec + Plan* — andere Eingabe, andere Befunde (Modul 11). Der Report
[`2026-08-28-slice-081-review.md`](2026-08-28-slice-081-review.md) war **Kontext**, nie Beleg: sein
Verdikt ist hier weder Ergebnis noch Ausschluss. **`make gates` grün ist hier kein Kriterium** —
§5 des Plans verwirft es ausdrücklich; das Grün gehört der Welle
([welle-10](../plan/planning/welle-10-re-baseline.md) §3). Jede Zahl unten ist in diesem Lauf neu
erhoben und trägt im selben Absatz ihr Kommando
([`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)).

**Ergebnis vorab:** DoD (1) **erfüllt**, (2) **teilweise erfüllt**, (3) **erfüllt**,
(4) **nicht erfüllt**, (5) **erfüllt**, (6) **nicht erfüllt**. Der blockierende Punkt ist **V-1**:
es gibt einen **vierten** roten Sensor dieses Slice — `make mutate` —, und er trägt keinen
benannten Ausgang. Genau das schließt DoD (4) mit seinem eigenen Satz aus: *„Jeder Befund außerhalb
dieser drei bleibt Bedingung dieses Punktes."* Der Review hat ihn nicht gefangen; sein Lauf war
untersagt (dort INFO-3), und für Review und Tests ist er unsichtbar. **Der Closure-Trigger ist
nicht erreicht.**

---

## 1. Lauf-Rahmen

Alle Läufe Docker-only ([`AGENTS.md`](../../AGENTS.md) §3.9): ausschließlich `make`-Ziele, keine
Host-Toolchain in der Befehlsposition. Sonden liefen gegen **Kopien außerhalb des Repos**
(`git ls-files -z | tar --null -T - -cf -` bzw. `git archive c6cc56f | tar -x -C …`), nie gegen den
Arbeitsbaum; er ist vor und nach jedem Lauf unverändert (Kennzahl **O** unten).

Der Bericht schreibt **nur diese Datei**. Kein Plan, keine ADR, kein Carveout, kein Code, keine
Norm-Datei ist berührt ([`AGENTS.md`](../../AGENTS.md) §3.8) — Befunde werden benannt, nicht
behoben.

### 1.1 Kennzahlen-Block, ein Lauf, ein Kommando je Zeile

| Kennung | Kommando | Ergebnis |
|---|---|---|
| A | `git rev-parse --short HEAD` | `06a640a` |
| B | `ls -1 .harness/baseline/ \| wc -l` | **1** |
| C | `wc -l < .harness/baseline/v5.12.0/SHA256SUMS` | **51** |
| D | `grep -c 'v3\.5\.2' Makefile internal/fetch/baseline.go .d-check.yml` | je **0** |
| E | `git grep -oE '\]\([^)]*\.harness/baseline/v3\.5\.2/[^)]*\)' c6cc56f -- ':!.harness/baseline' \| wc -l` | **24** |
| F | dasselbe ohne Ref (HEAD) | **2** |
| G | `grep -coE '\]\([^)]*\.harness/baseline/v5\.12\.0/[^)]*\)' spec/spezifikation.md` | **12** |
| H | `grep -m1 '^  ignore:' .d-check.yml \| grep -o '"[^"]*"' \| wc -l` | **5** |
| I | `git diff c6cc56f..HEAD -- docs/plan/adr/0013-technik-stratum-als-zielort.md \| wc -l` | **0** |
| J | `git show c6cc56f:harness/conventions.md \| grep '\.harness/baseline/v3\.5\.2/' \| grep -vc ']('` | **9** |
| K | `grep '\.harness/baseline/v3\.5\.2/' harness/conventions.md \| grep -vc ']('` | **10** |
| L | `grep -rh '\.harness/baseline/v3\.5\.2/' --include='*.md' docs/plan/adr/ \| grep -vc ']('` | **6** |
| M | `git show c6cc56f:docs/plan/adr/README.md \| wc -c` → `wc -c < docs/plan/adr/README.md` | **38 349 → 12 961** |
| N | `sed -n '104,176p' docs/plan/planning/in-progress/slice-081-…md \| grep -c '^- \[ \]'` | **6** |
| O | `git status --porcelain=v1 \| wc -l` | **0** |

---

## 2. DoD (1) — einziges `<tag>`-Verzeichnis, Pin an fünf Stellen: **ERFÜLLT**

`make baseline-verify` meldet wörtlich

```
baseline-verify: v5.12.0 OK — 51 Dateien (Integritaet + Vollstaendigkeit, netzlos)
```

bei Exit 0. Der Baum ist eindeutig (Kennzahl **B** → 1) und deckt sich mit seiner Prüfsummenliste
(**C** → 51). Die fünf mechanischen Stellen tragen sämtlich den neuen Tag und keinen alten
(**D** → je 0):

- `Makefile:26` `BASELINE_TAG ?= v5.12.0`, `Makefile:35` `BASELINE_ZIP_SHA256 ?= c91b62bd…6e04`
- `internal/fetch/baseline.go:48` `const DefaultTag = "v5.12.0"`, `:54` `const DefaultBaselineSHA256 = "c91b62bd…6e04"`
- `.d-check.yml:89` `sources[0].url … /download/v5.12.0/lab-regelwerk.zip`, `:90` derselbe sha256

Die Nicht-Scope-Klausel des Punktes (`v3.5.2`-Kurs-URLs in den Lifecycle-Köpfen bestehender Slices)
ist eingehalten: sie sind unangetastet.

## 3. DoD (2) — jeder gate-sichtbare Befund trägt den Ausgang seiner Klasse: **TEILWEISE ERFÜLLT**

### 3.1 Die gate-sichtbare Hälfte: vollständig, und die Menge deckt sich mit §1

Über dem Vor-Stand zeigten **24** Markdown-Links in den alten Baum (**E**), verteilt über sieben
Dateien:

```
git grep -oE '\]\([^)]*\.harness/baseline/v3\.5\.2/[^)]*\)' c6cc56f -- ':!.harness/baseline' \
  | cut -d: -f2 | sort | uniq -c | sort -rn
```

→ 12 `spec/spezifikation.md` · 4 `harness/conventions.md` · 3 `open/slice-114-…` ·
2 `done/slice-076-…` · 1 `docs/reviews/2026-07-26-slice-050-verification.md` ·
1 `docs/reviews/2026-07-26-slice-050-impl-review-runde-5.md` ·
1 `docs/plan/adr/0013-technik-stratum-als-zielort.md`. Jede der 24 trägt einen Ausgang:

- **12 gezogen** in `spec/spezifikation.md` (**G** → 12; alter Rest dort **0**).
- **3 von 4 gezogen** in `harness/conventions.md`; die vierte bleibt bewusst rot und trägt
  [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md).
- **3 gezogen** in `open/slice-114-…` (Planner, `26aec2c`).
- **4 Adressen entfallen** in **3** Zeitdokumenten. Der sichtbare Text bleibt Zeichen für Zeichen —
  über `git diff c6cc56f..HEAD -- docs/plan/planning/done/slice-076-…md docs/reviews/2026-07-26-slice-050-impl-review-runde-5.md docs/reviews/2026-07-26-slice-050-verification.md`
  nachgelesen: aus `[Modul 15 §Span-/Audit-Attribut-Regeln](…)` wird `Modul 15
  §Span-/Audit-Attribut-Regeln`, aus ``[`modul-10-review-harness.md`](…)`` wird
  ``` `modul-10-review-harness.md` ```. Kein Satz geändert
  ([ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md) Festlegung 4).
- **1 bleibt byte-gleich:** `docs/plan/adr/0013-…md` ist über den ganzen Bereich unverändert
  (**I** → 0), und ihre Datei ist der **einzige neue** `scan.ignore`-Eintrag. `git diff
  c6cc56f..HEAD -- .d-check.yml` zeigt genau eine hinzugefügte Glob-Zeichenkette plus
  Begründungs-Kommentar mit ADR-Zeiger; der Zensus steht bei **5** (**H**).

Am HEAD bleiben genau **2** alte Links (**F**): `harness/conventions.md:1009` (der `CO-005`-Träger)
und die eingefrorene ADR-0013 hinter `scan.ignore`. Der Gate bestätigt es:

```
make docs-check
# d-check: 447 Datei(en) geprüft, 1 Befund(e)
# harness/conventions.md:1009  ../.harness/baseline/v3.5.2/regelwerk/modul-08-agentenrollen.md#rollen-sequenz-für-einen-slice  target-missing
```

**Die Anker-Einzelprüfung ist am Ergebnis belegt, nicht nur behauptet.** Ein `sed` über den
Tag-String hätte den Gate rot gelassen — die Zieldateien heißen am neuen Stand anders
(`grundlagen-konventionen.md` existiert dort nicht mehr). Dass der Gate über diese Dateien schweigt,
ist damit ein Sensor für „Tag **und** Dateiname gezogen". Die Anker selbst deckt das aktive Modul
`anchors` (`grep -n '^modules:' .d-check.yml` → `[links, anchors, ids, matrix, codepaths, spans]`).
Stichprobe auf die **Substanz** — die kein Gate sieht — hält ebenfalls:
`grep -c "Ein Attribut ohne Incident-Frage fliegt raus" .harness/baseline/v5.12.0/regelwerk/modul-15-observability.md`
→ **1**, dito *„Korrelations-IDs zu Slice/PR/Agent-Rolle"* → **1**, `cache.hit_ratio` → **2**. Und
der eine Fall, in dem die Adresse hält und die Aussage bricht, ist gefunden und behandelt: das
Kommando aus `d6f3ae8` liefert am HEAD eine **leere** Ausgabe, also Zeichengleichheit der sechs
Rollen-Namen mit den sechs Bezeichnern:

```
diff <(grep -oE 'participant [A-Za-z]+ as [A-Za-z]+' .harness/baseline/v5.12.0/regelwerk/modul-08-agentenrollen.md \
        | awk '{print tolower($4)}' | sort -u) \
     <(grep -A1 'kanonischen Namen der Agenten-Typen' spec/spezifikation.md | grep -oE '`[a-z]+`' | tr -d '`' | sort -u)
```

Die **6** Inline-Nennungen in Accepted-ADRs sind unberührt (**L** → 6, verteilt 3/2/1 auf
ADR-0012/ADR-0014/ADR-0011); `git diff --name-only c6cc56f..HEAD -- docs/plan/adr/` nennt allein
`README.md`, keine ADR-Datei ([`AGENTS.md`](../../AGENTS.md) §3.4 gehalten).

### 3.2 Was den Punkt auf „teilweise" zieht — zwei Zusagen, die der Bestand nicht trägt

**(a) *„Die lebenden Inline-Nennungen sind ebenso gezogen"* ist messbar unwahr.** In
`harness/conventions.md` — nach §1 des Plans ausdrücklich ein Artefakt der Klasse *gezogen*
(*„lebende Norm-Artefakte"*) — stehen am HEAD **10** stumme Nennungen des alten Baums (**K**), über
dem Vor-Stand waren es **9** (**J**); die zehnte ist neu und stammt aus `MR-029` selbst.
Zeilenweise (`grep -n '\.harness/baseline/v3\.5\.2/' harness/conventions.md | grep -v ']('`):
703 · 895 · 942 · 1110 · 1125 · 1337 · 1388 · 1395 · 1583 · 1697.

Das ist **nicht** dasselbe wie „unbearbeitet". `d72e6dd` erklärt **fünf** von ihnen begründet zu
*stehen lassen* — die Commit-Message nennt sie an ihren Zeilennummern des Vor-Stands (688 · 880 ·
927 · 1110 · 1568), und diese fünf sind am HEAD die Zeilen 703 · 895 · 942 · 1125 · 1583
(`for n in 688 880 927 1110 1568; do git show c6cc56f:harness/conventions.md | sed -n "${n}p"; done`
gegen dieselben Textzeilen am HEAD). Die Begründung trägt und ist die richtige: sie zitieren eine
Vorlagen-Zeile, die es am neuen Stand nicht mehr gibt — ein Tag-Tausch hätte dort Zitate fabriziert,
genau die Klasse, gegen die [ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md) Festlegung 1
geschrieben ist. Die zehnte (1697, `MR-029`) ist eine explizit als historisch markierte
Gegenüberstellung und trägt ihre Begründung im Satz.

**Ohne jede Entscheidung dieses Slice bleiben damit vier**: Zeile 1110
([`MR-023`](../../harness/conventions.md#mr-023--die-platzierung-der-kommentar-regel-ist-keine-abweichung)),
1337 ([`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert))
und 1388/1395 ([`MR-026`](../../harness/conventions.md#mr-026--die-hard-rule-nummer-ist-eine-adresse-keine-baseline-entsprechung)).
Einen **nachträglichen** Träger haben sie: `db855a1` erweitert
[slice-082](../plan/planning/open/slice-082-adaptions-durchgang.md) Achse 1 um *„der Ausgang **und**,
ob seine Messungen gegen den gepinnten Stand noch reproduzieren"* — das deckt die Klasse. Der
**DoD-Text** wurde dabei nicht nachgezogen: er sagt weiter pauschal *gezogen*, wo der Bestand
*teils gezogen, teils begründet stehen gelassen, teils an slice-082 abgegeben* lautet. Wer den
Haken nach dem Wortlaut setzt, setzt ihn falsch.

**(b) *„und `MR-001` führt Zahl, Klassifikation und die Aufnahme-Grenze nach"* ist an `MR-001`
nicht geschehen.** Der Abschnitt trägt unverändert die alte Aussage (*„`scan.ignore` führt heute
vier Einträge, aus zwei Gründen"*, `awk '/^### .*MR-001/,/^### .*MR-002/' harness/conventions.md |
grep -n 'scan\.ignore'`). Die **Wirkung** ist hergestellt, aber an einem anderen Ort: `MR-029` trägt
den Zensus **5** mit dem Kommando, das ihn ausgibt (**H**, in `MR-029` abgedruckt und hier
nachgefahren), die neue Drei-Gründe-Klassifikation und die Aufnahme-Grenze verbatim aus
[ADR-0017](../plan/adr/0017-doku-gate-ausnahme-fuer-ein-eingefrorenes-adr.md), und verweist auf
`MR-001` als überholt, ohne ihn zu berühren. Die Begründung — Append-only-Disziplin des adoptierten
Stands — ist am Baum belegt und schlüssig. **Als DoD-Punkt gelesen ist der Satz gleichwohl nicht
erfüllt:** er nennt einen Ort, und dort steht es nicht. Das ist kein Streit über die Sache, sondern
über die Abnahme: eine DoD, die einen anderen Ort meint, muss ihn nennen.

**Randnotiz, kein eigener Befund.** Der neue `scan.ignore`-Kommentar erfüllt die
DoD-Anforderung *„mit Begründung und Zeiger auf sie im Config-Kommentar"* — er beziffert die
Modul-Menge aber mit *„über alle **fuenf** Module"* (`.d-check.yml:23`), während `modules:` sechs
führt. Der Review trägt das als LOW-1; es besteht fort.

## 4. DoD (3) — alle vier Kopplungs-Zähne rot gesehen: **ERFÜLLT**

Nicht übernommen, sondern **selbst gefahren** — über einer isolierten Kopie außerhalb des Repos,
je eine Sonde, jede danach zurückgenommen, mit Kontrolllauf.

| Sonde | Mutation | Ergebnis |
|---|---|---|
| 1 | `internal/fetch/baseline.go:48` `DefaultTag` → `v5.12.1` | `--- FAIL: TestDefaultTag_MatchesBaseline` — **einzige** FAIL-Zeile; `internal/emit`, `internal/gen`, `internal/report`, `internal/span`, `internal/wire`, `cmd/…` bleiben `ok` |
| 2 | `:54` `DefaultBaselineSHA256` `c91b…` → `d91b…` | `--- FAIL: TestDefaultBaselineSHA256_MatchesMakefile` — einzige FAIL-Zeile |
| 3 | `.d-check.yml:89` URL-Tag → `v9.9.9` | `not ok 183 sources-url …`, dabei `ok 182 sources-sha256 …` |
| 4 | `.d-check.yml:90` sha256 `c91b…` → `d91b…` | `not ok 182 sources-sha256 …`, dabei `ok 183 sources-url …` |

Kontrolllauf nach Rücknahme: `make test-go` → **0** FAIL-Zeilen; `make test-bats` → `ok 182`,
`ok 183`. **Jede Sonde färbt genau ihren Sensor, die jeweils andere Seite bleibt grün** — die halbe
Migration fällt an vier getrennten Stellen auf ([`AGENTS.md`](../../AGENTS.md) §3.6 erfüllt).

*Zum Verfahren:* die Kopie entsteht per `git ls-files -z | tar --null -T - -cf -` und trägt kein
`.git`; deshalb schlägt dort zusätzlich `not ok 114 driver: die Kopie traegt den Sensor-Bedarf
inklusive .git` fehl. Das ist ein Artefakt der Sonde, nicht des Repos: im Arbeitsbaum meldet
`make test-bats` genau **zwei** Fehlschläge (`not ok 40`, `not ok 41`) bei **189** Fällen
(`make test-bats 2>&1 | grep -cE '^(ok|not ok)'` → 189).

## 5. DoD (4) — jeder rote Sensor trägt einen benannten Ausgang: **NICHT ERFÜLLT**

Die drei benannten Sensoren halten. Der Punkt fällt an seinem eigenen Auffang-Satz.

### 5.1 Sensor 1 — `make gates`: bestätigt

`make gates` bricht nach `docs-check` mit Exit 2 ab, die zehn folgenden Ziele laufen in diesem Lauf
nie. Die Aussage *„zwei Befunde"* ist deshalb **einzeln** nachgeprüft, Ziel für Ziel, gegen die
Prerequisite-Liste `gates: baseline-verify docs-check lint build test shell-lint ci-lint
comment-claims host-bin span-check record-gates` (`grep -nE '^gates:' Makefile`):

| Ziel | Ergebnis |
|---|---|
| `baseline-verify` | Exit 0 — `v5.12.0 OK — 51 Dateien` |
| `docs-check` | Exit 2 — `447 Datei(en) geprüft, 1 Befund(e)`, und es ist genau `harness/conventions.md:1009` = [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md) |
| `lint` · `build` · `shell-lint` · `ci-lint` · `host-bin` · `span-check` | je Exit 0 |
| `comment-claims` | Exit 0 — `comment-claims: 46 Datei(en) geprueft, 0 Befund(e)` |
| `test` | `test-go` **0** FAIL-Zeilen; `test-bats` Exit 2 mit genau `not ok 40` / `not ok 41` = [`CO-004`](../plan/carveouts/CO-004-emitter-klassifikation-offen.md) |

**Innerhalb von `make gates` liegt kein dritter Rot-Zustand**, und beide Carveouts sind
extensional geschlossen (`CO-004` auf zwei namentlich zitierte bats-Fälle, `CO-005` auf **eine**
Referenz). Der Punkt ist an dieser Stelle erfüllt.

### 5.2 Sensor 2 — `make smoke` / `make full-smoke`: bestätigt, samt Aufteilung 7 + 3

```
make smoke        # d-check: 23 Datei(en) geprüft, 10 Befund(e) — Exit 2
make full-smoke   # dieselben 10 Befunde, danach:
                  # full-smoke: FEHLER — make gates im emittierten Repo ist NICHT Exit 0 (LH-FA-01 Happy-Path verletzt).
                  # Exit 2
```

Die zehn Befundzeilen sind in beiden Läufen identisch. Die Zuordnung der DoD auf **7** →
[slice-133](../plan/planning/done/slice-133-emittierter-baum-ohne-platzhalter-links.md) und **3** →
[slice-130](../plan/planning/open/slice-130-emitter-entscheidet-jedes-neue-template.md) trifft je
Befund und ist an der Vorlagen-Zeile nachprüfbar:

| # | Befundzeile im emittierten Baum | Ursache | Träger |
|---|---|---|---|
| 1 | `docs/plan/planning/in-progress/roadmap.md:19` → `../<welle-NN-titel>.md` | vorbestehende Vorlage | slice-133 |
| 2–4 | `harness/README.md:144,145,146` → `pfad` | vorbestehende Vorlage | slice-133 |
| 5–7 | `harness/conventions.md:117,129,129` → `conventions/MR-<NNN>-<titel>.md`, `conventions/done/MR-<NNN>-<titel>.md` | vorbestehende Vorlage | slice-133 |
| 8–9 | `docs/plan/planning/welle-results.md:67,83` | **neue** Vorlage `welle-results.template.md` | slice-130 |
| 10 | `harness/conventions/MR-NNN-titel.md:15` → `…/baseline/<tag>/regelwerk/…` | **neue** Vorlage `MR-NNN-titel.template.md` | slice-130 |

slice-133 §2 DoD (1) nennt die drei Dateien der Ursache A namentlich und schließt die Restmenge
ausdrücklich aus; slice-130 §1 führt die drei Befundzeilen 8–10 einzeln mit ihrer Vorlagen-Zeile
(`grep -nE 'welle-results|MR-NNN-titel' docs/plan/planning/open/slice-130-…md` → Zeilen 92–94).
**7 + 3 = 10, ohne Rest.** `CO-004` §Nicht Geltungsbereich grenzt die zehn korrekt aus dem Carveout
aus und benennt Reparatur statt Ausnahme. Der Punkt ist an dieser Stelle erfüllt.

### 5.3 Sensor 3 — die Modul-7-Pflicht von `CO-004` ist offen und benannt: bestätigt

`git grep -c 'CO-004' -- test/ .d-check.yml Makefile` → **kein Treffer** (Exit 1), dito für
`CO-005`. `CO-004` §Geltungs-Konfiguration sagt genau das — *„Es gibt heute keine … ein offener
Posten"* —, benennt den Unterschied zu `CO-005` (dort Werkzeug-Grenze, hier möglich und ungetan),
gibt die Verdrahtung an den Implementer weiter und erklärt sich zur *„Bedingung des Abnahme-Punktes
von slice-081 §2 DoD (4)"*. **Offen benannt**, nicht verschwiegen. Der Punkt ist an dieser Stelle
erfüllt.

### 5.4 Warum der Punkt trotzdem fällt — `make mutate` ist der vierte rote Sensor (**V-1**)

```
make mutate
# mutate: 198 Faelle auf 4 Worker, dynamisch aus einer gemeinsamen Warteschlange.
# mutate: BEFUND  worker-1 … worker-4   Worker kehrte mit Status 1 zurueck …
# mutate: BEFUND  vollstaendigkeit      0 von 198 Fall-Dateien haben ein Ergebnis — der Lauf ist unvollstaendig
# mutate: 0 ok, 7 Befund(e)
# Exit 2
```

**Zwei Ursachen, beide aus diesem Slice, beide in der Ausgabe wörtlich benannt:**

- Worker 1: *„ABBRUCH — der Gruen-Vorlauf `make full-smoke` ist in SEINER Kopie ohne Mutation
  rot."*
- Worker 4: *„ABBRUCH — der Gruen-Vorlauf `make test-bats` ist in SEINER Kopie ohne Mutation
  rot."*

Der `make test-go`-Vorlauf war grün; die Fall-Menge ist **198** und **0** davon haben ein Ergebnis.
Die Kausalkette ist gemessen, nicht erschlossen: `test-bats` ist im Arbeitsbaum genau an den
Fällen 40/41 rot (§4), und über dem Vor-Stand war es dort grün —
`git archive c6cc56f | tar -x -C <tmp>; make -C <tmp> test-bats` meldet als einzigen Fehlschlag
`not ok 114` (das `.git`-Artefakt der Kopie, §4), die Fälle 40/41 sind `ok`. `full-smoke` war über
demselben Stand grün. Für den Vor-Stand ist der Grün-Lauf zusätzlich aktenkundig:
`docs/plan/carveouts/done/CO-003-mutate-ohne-zeitschranke.md` §Verifikation nennt
*„`make mutate` **198 ok, 0 Befund(e)**, 788,76 s"* über `48c2063`, und
`git merge-base --is-ancestor 48c2063 c6cc56f` bestätigt, dass dieser Stand vor dem Tausch liegt.

**Damit ist `make mutate` ein roter Sensor dieses Slice.** Er trägt keinen Ausgang:

```
git grep -n -i 'mutate' -- docs/plan/planning/in-progress/slice-081-*.md docs/plan/carveouts/ \
  docs/plan/planning/welle-10-re-baseline.md docs/plan/planning/open/slice-13{0,1,2,3}-*.md
```

findet außerhalb der abgeschlossenen `CO-003`-Akte **keine** Zeile. Weder der Slice-Plan noch
`CO-004`/`CO-005` noch einer der vier Folge-Slices nennt ihn.

**Der Mechanismus des Fehlers ist derselbe, den DoD (4) an sich selbst schon einmal korrigiert
hat.** [welle-10](../plan/planning/welle-10-re-baseline.md) §3 führt — vom selben Planner
geschrieben — *„**Die drei Sensoren außerhalb der Gates** — `make smoke`, `make full-smoke`,
`make mutate`"*. Das Makefile bestätigt die Menge: `grep -nE '^[a-z][a-z0-9-]*:.*NICHT in gates'
Makefile` listet neben Maintenance-/Freshness-Zielen genau `smoke`, `full-smoke`, `mutate` als
Sensoren; `span-report` und `hook-overhead` deklarieren sich selbst als *„Bericht, kein Sensor"*
bzw. *„Messung, kein Sensor"*. **DoD (4) zählt zwei dieser drei.** Der Punkt wurde in `db855a1`
von einem auf drei Sensoren erweitert, weil `make gates` allein den Bruch nicht sah — dieselbe
Lücke ist eine Stelle weiter offen geblieben.

**Warum das nicht bloß Formalie ist.** `make mutate` ist der Sensor, den
[`AGENTS.md`](../../AGENTS.md) §3.6 als **Feedback** für die Haltbarkeit der Wächter benennt
(*„meldet jeden gelisteten Wächter, der seine Zähne verloren hat"*). Er läuft nicht teilweise — er
läuft **gar nicht**: 0 von 198 Fällen. Und er läuft pro Push:
`harness/README.md:68` — *„GitHub Actions fährt `make gates` + `make smoke` + `make mutate` auf
frischem Klon pro Push/PR"*. Seit diesem Slice ist damit die Haltbarkeits-Prüfung sämtlicher 198
Wächter dunkel, und zwar ohne dass ein Artefakt des Slice es aussprechen würde.

**Die Reparatur ist wahrscheinlich schon geschnitten, aber nicht zugeordnet.** Werden slice-133 und
slice-130 geschlossen, gehen beide Vorläufe wieder grün — der Ausgang wäre inhaltlich dieselbe
Kante. Genau das aber verlangt DoD (4) **explizit** zu schreiben, und sein Sensor 3 formuliert das
Kriterium selbst: *„Offen **benannt** erfüllt diesen Punkt, offen **verschwiegen** nicht."*

## 6. DoD (5) — Doku-Update: **ERFÜLLT**

- `harness/conventions.md` §Baseline nennt `v5.12.0` an drei Stellen: den vendored Pfad, den
  Regelwerks-Stand (*„Kurs-Welle 98 · 2026-08-26"*, mit dem Kommando daneben) und die
  Re-Baseline-Historie (*„**auf `v5.12.0`:** 2026-08-28 (slice-081)"*).
  `sed -n '/^## Baseline/,/^## /p' harness/conventions.md | grep -c 'v5\.12\.0'` → **4**.
- `docs/user/benutzerhandbuch.md:174` — die Beispiel-Abschlusszeile spiegelt `DefaultTag` und lautet
  *„Bootstrap (Baseline v5.12.0 vendored …)"*.

*Ohne Befundwert notiert:* `docs/user/benutzerhandbuch.md:341` und `:401` zeigen weiterhin
`COURSE_TAG=v3.5.2`. Beide stehen im Abschnitt *„Eine andere Kurs-Version verwenden"* — der alte
Tag ist dort das Beispiel für **einen anderen als den voreingestellten** Stand und damit korrekt.
Kein Widerspruch zur DoD.

## 7. DoD (6) — Closure-Notiz mit Steering-Loop-Lerneintrag: **NICHT ERFÜLLT**

§7 des Plans steht unverändert auf `<!-- Erst nach Abschluss füllen. -->`. Alle **6** DoD-Kästchen
sind offen (**N** → 6), und der Slice liegt weiter in `in-progress/`
(`ls docs/plan/planning/in-progress/` → `roadmap.md`, `slice-081-baum-tauschen-pin-ziehen.md`).

Das ist erwartbare Closure-Arbeit und für sich kein Fehler — es ist der Grund, warum §5 noch nicht
erreicht ist. Es steht hier, weil DoD (6) ein DoD-Punkt ist und die Abnahme ihn zählt.

## 8. Plan-vs-Code-Diff — beide Richtungen

Grundlage: `git diff --name-status c6cc56f..HEAD -- . ':!.harness/baseline'` → **29** Einträge.

### 8.1 Zugesagt, ohne (vollständige) Umsetzung

| Plan §3 sagt | Ist-Stand | Bewertung |
|---|---|---|
| *„die 16 nachziehbaren Links"* in `spec/spezifikation.md` + `harness/conventions.md` | 12 + 3 = **15** gezogen; die 16. bleibt bewusst rot unter `CO-005` | **gedeckt** — §6 führt den Ausgang; §3 ist die pauschalere Formulierung |
| *„und die lebenden Inline-Nennungen"* (dieselbe Zeile) | **10** stumme Nennungen des alten Baums bleiben in `harness/conventions.md` (**K**); 5 begründet stehen gelassen, 1 neu und selbst begründet, **4** ohne Entscheidung in diesem Slice | **Lücke im Zusage-Text** — Träger nachträglich in slice-082 Achse 1 (`db855a1`), DoD/§3 nicht nachgezogen (**V-2**) |
| *„dazu `MR-001` als Zähl- und Klassifikations-Ort der `scan.ignore`-Einträge"* | `MR-001` unverändert; die Wirkung steht in `MR-029` | **anderer Ort als zugesagt** (**V-3**) |
| `test/sessionstart.bats` — *„drei Fixture-Namen"* | `git diff c6cc56f..HEAD -- test/sessionstart.bats`: genau **3** Vorkommen `grundlagen-konventionen.md` → `grundlagen-harness-dateien.md` (Zeilen 61, 64, 100) | **erfüllt** |
| ADR-0011/0012/0013/0014/0015 *„unberührt"* | `git diff --name-only c6cc56f..HEAD -- docs/plan/adr/` → nur `README.md` | **erfüllt** |
| Zeitdokumente *„update (nur Adresse)"* | 4 Adressen, 3 Dateien, Text zeichengleich | **erfüllt** |
| `.harness/baseline/` ersetzen · `Makefile` · `internal/fetch/baseline.go` · `.d-check.yml` | siehe §2, §3.1 | **erfüllt** |

### 8.2 Umgesetzt, ohne Plan-Deckung

- **`docs/plan/adr/README.md` — der größte einzelne Posten, und er steht im Slice-Plan nirgends.**
  Vier Commits der Kette (`2b0d741`, `eaccc4d`, `62bc866`, `06a640a`) schreiben den ADR-Index um:
  **38 349 → 12 961** Zeichen (**M**), Titel-Spalte auf die H1 der Zieldatei projiziert,
  Status-Spalte entprosaisiert, ein Konventionen-Abschnitt neu gefasst.
  `grep -nEi 'adr-index|adr/README|Index der ADR' docs/plan/planning/in-progress/slice-081-…md`
  → **kein Treffer**; §3 und §6 nennen ihn nicht, DoD (5) nennt nur `harness/conventions.md` und
  `docs/user/benutzerhandbuch.md`. Die Arbeit selbst ist rollen-konform (Architect,
  [`AGENTS.md`](../../AGENTS.md) §3.8 ist nicht berührt, keine ADR-Datei angefasst) und in den
  Commit-Messages belegt — aber sie ist **Arbeit ohne Plan-Posten** innerhalb eines Slice, dessen
  DoD sich auf Baum, Pin und Verweise beschränkt (**V-5**).
- **`internal/emit/templates.go`** — eine Kommentarzeile (`v3.5.2` → `v5.12.0` in einer Zusage über
  den vendored Satz). Von `b902b60` belegt und nach §1 des Plans sachlich richtig; in §3 nicht
  aufgeführt.
- **`docs/user/benutzerhandbuch.md`** — von DoD (5) verlangt, in der §3-Tabelle nicht aufgeführt.
- **`docs/plan/planning/done/slice-080-…md`** — Link-Abgleich nach dem Lifecycle-Move (`3871da4`).
  Konvention des Hauses, in §3 nicht aufgeführt.
- **`docs/plan/carveouts/{CO-004,CO-005,README}.md`, `docs/plan/planning/{welle-10, in-progress/roadmap,
  open/slice-082..085, open/slice-114, open/slice-130..133}.md`** — Planner-Arbeit aus `26aec2c`,
  `db855a1`, `d12b938`. **Über §6 gedeckt** (jedes dieser Artefakte ist der dort benannte *Ausgang*
  eines eingetretenen Risikos), nicht über §3. Das ist die vorgesehene Mechanik und kein Befund;
  hier vollständigkeitshalber verzeichnet, weil §3 sonst als abschließende Datei-Liste gelesen wird.
- **`docs/reviews/2026-08-28-slice-081-review.md`** — Lauf-Beleg, `ff2a1dd`.

## 9. Was die Pre-completion-Behauptungen sagen und hier nicht bestätigt werden konnte

Eingang waren die DoD-Bestätigungen in den Commit-Messages der Kette. Bestätigt sind: der
Provenienz-Nachweis (`baseline-verify` grün, Pin an fünf Stellen, **D**/**H**), die vier
Kopplungs-Zähne (§4, unabhängig nachgefahren), die Verweis-Sortierung je Treffer (§3.1, inklusive
des einen Falls, in dem ein `sed` ein Zitat fabriziert hätte), die Smoke-Aufteilung 7 + 3 (§5.2)
und die drei `docs-check`-Stände `447/1` aus `db855a1`, `d12b938`, `06a640a` (hier erneut `447/1`).

**Nicht bestätigt:**

1. `b902b60`: *„NICHT GRUEN, und begruendet: make gates ist rot. **Vier** Entscheidungen sind
   offen"* — die Aufzählung der roten Sensoren endet bei `gates`. Der Übergabe-Bericht selbst liegt
   nicht im Repo; was das Repo trägt, ist DoD (4), und dort fehlt `make mutate` (**V-1**).
2. `26aec2c` / `db855a1`: *„Kein Gate-Gruen erzwungen"* — bestätigt; **aber** der Satz *„dieser Slice
   hinterlässt keinen unzugeordneten Befund"*, den DoD (4) trägt, ist unbestätigt: `make mutate`
   ist unzugeordnet (**V-1**).
3. Plan §1/§3 und DoD (2): *„die lebenden Inline-Nennungen sind ebenso gezogen"* — widerlegt
   (**K** → 10, **V-2**).
4. DoD (2): *„`MR-001` führt Zahl, Klassifikation und die Aufnahme-Grenze nach"* — an `MR-001`
   nicht geschehen (**V-3**).
5. `AGENTS.md` §3.6 über das Repo als Ganzes: dass die **gelisteten** Wächter ihre Zähne behalten
   haben, ist für diesen Stand **nicht gemessen** und wird hier nicht als grün ausgegeben — der
   einzige Sensor dafür ist `make mutate`, und er kommt nicht zum Fall (0 von 198).

## 10. Befunde

| ID | Klasse | Gegenstand | Wirkung |
|---|---|---|---|
| **V-1** | DoD-Verletzung | `make mutate` ist rot (0 von 198 Fällen, Exit 2), verursacht durch die zwei roten Vorläufe dieses Slice, und trägt in keinem Artefakt einen benannten Ausgang. DoD (4) zählt zwei der drei Nicht-Gate-Sensoren, die [welle-10](../plan/planning/welle-10-re-baseline.md) §3 selbst führt | **blockierend** — DoD (4) nicht erfüllt, an seinem eigenen Auffang-Satz |
| **V-2** | DoD-Zusage ohne Deckung | DoD (2) *„Die lebenden Inline-Nennungen sind ebenso gezogen"*; real **10** in `harness/conventions.md` (**K**), davon **4** ohne Entscheidung dieses Slice. Träger existiert seit `db855a1` in slice-082 Achse 1; der DoD-Text nennt ihn nicht | DoD (2) teilweise; Planner-Posten (Text nachziehen oder Klasse in der DoD aufnehmen) |
| **V-3** | DoD-Ort ≠ Ist-Ort | DoD (2) verlangt die Nachführung **in `MR-001`**; sie steht in `MR-029`. Sachlich richtig und begründet, als Abnahme-Kriterium unerfüllt | DoD (2) teilweise; Planner-Posten |
| **V-4** | Closure offen | §7 leer, **6** offene Kästchen (**N**), Slice in `in-progress/` | DoD (6) nicht erfüllt; §5 nicht erreicht |
| **V-5** | Plan-vs-Code | `docs/plan/adr/README.md` **38 349 → 12 961** Zeichen (**M**) über vier Commits der Kette, ohne jeden Posten im Slice-Plan | kein Regelverstoß; Zuschnitt-Befund an den Planner |
| **V-6** | Notiz | `.d-check.yml:23` *„über alle **fuenf** Module"* gegen sechs in `modules:` — bereits Review LOW-1, besteht fort und sitzt in genau dem Kommentar, den DoD (2) verlangt | Hinweis |

**Nicht Gegenstand dieses Berichts:** die Befunde des Reviews, soweit sie den Diff gegen Plan und
ADR betreffen. HIGH-1 bis HIGH-3 sind in der Fix-Runde (`db855a1`) sachlich adressiert — die
Instrumenten-Wahl *Reparatur statt drittem Carveout* ist mit Modul-7-Zitat und Rang-1-Begründung
belegt, `CO-004` führt den Ist-Zustand im Indikativ und weist die fehlende Verdrahtung als
Nicht-Vorhandenes aus. Ob das der **richtige** Weg war, ist keine Verifier-Frage; dass er
**genommen und belegt** ist, ist hier bestätigt.

## 11. Verdikt

**DoD NICHT VOLLSTÄNDIG — der Closure-Trigger ist nicht erreicht.**

| DoD | Ergebnis |
|---|---|
| (1) einziges `<tag>`-Verzeichnis, Pin an fünf Stellen | **erfüllt** |
| (2) jeder gate-sichtbare Befund trägt den Ausgang seiner Klasse | **teilweise erfüllt** (V-2, V-3) |
| (3) alle vier Kopplungs-Zähne rot gesehen | **erfüllt** (unabhängig nachgefahren) |
| (4) jeder rote Sensor trägt einen benannten Ausgang | **nicht erfüllt** (V-1) |
| (5) Doku-Update | **erfüllt** |
| (6) Closure-Notiz mit Steering-Loop-Lerneintrag | **nicht erfüllt** (V-4) |

**Der mechanische Kern des Slice hält, und zwar über den Zusagen.** Der Baum ist eindeutig, der Pin
sitzt an allen fünf Stellen, die vier Kopplungs-Zähne färben je genau ihren Sensor, alle 24
gate-sichtbaren Verweise tragen einen Klassen-Ausgang, der eine Fall, in dem ein `sed` ein Zitat
fabriziert hätte, ist gefunden und an der richtigen Stelle behandelt, und die zehn Smoke-Befunde
sind je Vorlagen-Zeile auf zwei Slices aufgeteilt, ohne Rest.

**Was blockiert, ist eine DoD-Verletzung im engen Sinn des Moduls 11: eine Differenz zwischen
DoD-Punkt und Artefakt-Stand, die weder der Reviewer noch ein Test sehen kann.** DoD (4) hat sich
in der Fix-Runde selbst von einem auf drei Sensoren erweitert, weil der Bruch durch die
Ein-Sensor-Fassung gefallen war. Die erweiterte Fassung zählt drei, wo die eigene Welle-Datei vier
Sensoren-Orte kennt — und der vierte, `make mutate`, ist rot, aus genau den zwei Ursachen, die
dieser Slice gesetzt hat, und läuft mit 0 von 198 Fällen überhaupt nicht mehr. Solange kein
Artefakt das ausspricht, ist die Bedingung *„keiner bleibt unzugeordnet"* nicht erfüllt. Der Punkt
schreibt sein eigenes Maß: **offen benannt erfüllt ihn, offen verschwiegen nicht.**

**Empfehlung an den Planner — drei Züge, alle klein:**

1. **V-1 schließen:** DoD (4) um `make mutate` als vierten Sensor ergänzen und ihm den Ausgang
   geben, den die Kausalkette ohnehin nahelegt (slice-133 + slice-130; beide Vorläufe gehen mit
   ihnen grün). Kein Carveout nötig — der Ausgang existiert, er ist nur nicht geschrieben. Danach
   ist DoD (4) erfüllt.
2. **V-2/V-3 auflösen:** den Zusage-Text von DoD (2) auf das einschränken, was der Bestand hält —
   *gezogen, wo die Zieldatei die Aussage trägt; begründet stehen gelassen, wo ein Zug ein Zitat
   fabrizierte; an slice-082 Achse 1, wo die Messung nachzuhalten ist* — und den Ort der
   `scan.ignore`-Nachführung auf `MR-029` korrigieren.
3. **V-4:** Closure-Notiz mit Steering-Loop-Lerneintrag schreiben, Haken setzen, `git mv` nach
   `done/`.

**Ein blockierender inhaltlicher Befund am Gebauten existiert nicht.** Was fehlt, ist an drei
Stellen die Deckung zwischen dem, was die DoD zusagt, und dem, was das Repo trägt — und an einer
Stelle ein Sensor, der seit diesem Slice schweigt, ohne dass jemand es aufgeschrieben hätte.
