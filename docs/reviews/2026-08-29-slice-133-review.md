# Review-Report: slice-133 (Code) — 2026-08-29

**Review-Art:** Code, erste Runde. Geprüft wird der Diff gegen den Slice-Plan, gegen die
Hard Rules und gegen die Rang-1-Zusagen, die der Plan zitiert. **Nicht** geprüft: die DoD-Abhakung
— sie ist Sache der Verifikation (Modul 11: anderer Kontext, anderes Prüf-Artefakt).

**Gegenstand:** Commit `f979b59` — sieben Dateien, `359` Einfügungen gegen `22` Löschungen
(`git show --stat f979b59 | tail -1` → `7 files changed, 359 insertions(+), 22 deletions(-)`).
Arbeitsbaum bei Review-Beginn sauber (`git status --short` → leer), `HEAD` = `f979b59`.

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0 · **Modell:** claude-opus-5[1m] · **Datum:** 2026-08-29

## Eingangs-Kontext (die fünf Pflicht-Punkte plus Plan)

1. **Diff/Commit-Range:** `f979b59`, im Volltext gelesen (`git show f979b59`).
2. **Slice-Plan:** `docs/plan/planning/done/slice-133-emittierter-baum-ohne-platzhalter-links.md`,
   vollständig gelesen (Stand `in-progress/`; der Diff berührt ihn nicht).
3. **`LH-*`:** `LH-FA-01` (Happy Path: `make gates` läuft grün), `LH-FA-02` (der emittierte Stand
   ist out-of-the-box gate-sicher, keine gate-unsicheren Platzhalter-Skelette), `LH-QA-01` (keine
   halluzinierten Gates; Messmethode ist wörtlich der Smoke-Test).
4. **Referenzierte aktive ADRs: keine.** Weder der Plan noch die Commit-Message nennen eine ADR;
   der ADR-Bestand trägt zu dieser Mechanik auch keinen Text
   (`grep -rln "Neutralisier" docs/plan/adr/*.md` → keine Ausgabe). Die tragenden Normen sind
   `MR-007`, `MR-015`, `MR-017`, `MR-025` und die Hard Rules.
5. **Hard Rules:** `AGENTS.md` §3.2, §3.3, §3.5, §3.6, §3.7, §3.8, §3.9.
6. **Vorherige Findings am gleichen Modul:** `docs/reviews/2026-08-25-slice-087-review.md`
   (`internal/emit/templates.go` + `readme.go`, dieselbe Neutralisierungs-Bauart) mit `F-1` INFO und
   `F-2` LOW — `F-2` ist genau die Klasse „`readme.go` steht nicht in der Plan-Tabelle"; sie kehrt in
   diesem Diff wieder (hier `LOW-1`). Dazu `docs/reviews/2026-08-22-slice-088-review.md` `HIGH-1`
   als Kalibrierung für §3.7 an einem Gate-Artefakt.
7. **Regelwerk:** `.harness/baseline/v5.12.0/regelwerk/README.md` (Index) gelesen,
   `modul-10-review-harness.md` on-demand.

---

## Selbst gefahren — nichts aus Commit-Message oder Übergabe übernommen

Vorbemerkung zur Werkzeug-Wahl: `grep` ist auf diesem Host als Shell-Funktion auf `ugrep`
umgebogen (`type grep` → `grep ist eine Funktion`), und `ugrep` liefert für die hier gebrauchten
ERE-Kombinationen abweichende Ergebnisse. Alle Zählungen unten laufen deshalb über
`command grep` (GNU grep 3.x) bzw. GNU awk. Keine Host-Toolchain in der Befehlsposition
(`AGENTS.md` §3.9); der einzige gefahrene Gate-Lauf geht über `make`.

Als `formen` wird unten die Pipeline bezeichnet, die der Wächter selbst führt
(`test/courseset-fixture.bats:119-128`), hier verbatim nachgebildet:

```
formen() { command grep -oE '\]\([^()[:space:]]*\)' \
  | awk '{z=substr($0,3,length($0)-3); sub(/#.*$/,"",z); if (z !~ /<[^<>]*>/) next;
          print (substr(z,1,1)=="<") ? "spitz" : "eingebettet"}' | LC_ALL=C sort -u; }
```

| Kommando / Probe | Ergebnis | Behauptung | Urteil |
|---|---|---|---|
| `find .harness/baseline/*/templates -type f -name '*.md' -exec cat {} + \| command grep -oE '\]\([^()[:space:]]*\)' \| sed 's/^](//; s/)$//; s/#.*$//' \| command grep -cE '<[^<>]*>'` | `12` | Plan §1: der reale Satz trägt Platzhalter-Pfad-Links | **stimmt** — 12 Vorkommen, davon 8 in emittierten Singletons (7 Ursache A + `harness/conventions/MR-NNN-titel.template.md`), 4 in nicht emittierten Vorlagen |
| dieselbe Pipeline, aber mit der Emit-Regexp `\[[^][]*\]\([^()[:space:]]*\)` als Matcher | `12` | `NeutralizePlaceholderLinks` erfasst die Fundstellen des realen Satzes | **stimmt, 12 von 12** — keine Fundstelle fällt heute durch Grenze 3 des Doc-Kommentars |
| `command grep -rhoF '](' --include='*.md' .harness/baseline/v5.12.0/templates \| wc -l` gegen `command grep -rhoE '\]\([^()[:space:]]*\)' --include='*.md' … \| wc -l` | `114` und `114` | `placeholderLinkPattern`-Kommentar: „das ZIEL traegt weder Klammern noch Leerraum — die Form, in der der Vorlagen-Satz seine Verweise fuehrt" | **stimmt, an der Quelle nachgemessen** |
| `formen` über `v5.12.0` (Arbeitsbaum) | `eingebettet` `spitz` | — | zwei Eimer |
| `formen` über `v3.5.2` (`git show c6cc56f:…` je Datei aus `git ls-tree -r --name-only c6cc56f`) | `eingebettet` `spitz` | Commit: „der Baum-Tausch brachte Formen mit, die die Fixture nicht kannte" | **trifft nicht zu** — der Vor-Tausch-Baum führte dieselben zwei Eimer, siehe `HIGH-1` |
| `git show c6cc56f:internal/emit/templates_test.go \| awk '/^func courseSet\(\)/ {infn=1} infn && /^}/ {infn=0} infn' \| formen \| command grep -c .` | `0`, bei `34` Rumpf-Zeilen (`… \| wc -l`) | — | die Fixture führte am Vor-Tausch-Stand keine der zwei Formen; der Parser greift trotzdem |
| dieselbe Extraktion über dem Arbeitsbaum | `eingebettet` `spitz` | Commit: die Fixture bekommt beide Formen | **stimmt** |
| `find … -exec cat {} + \| command grep -oE '\]\([^()[:space:]]*\)' \| awk '{z=substr($0,3,length($0)-3); sub(/#.*$/,"",z); if (z !~ /<[^<>]*>/) next; if (substr(z,1,1)=="<" && z !~ /^<[^<>]*>$/) print z}'` | zweimal `<NNNN>-<titel>.md` | bats-Kommentar: „`spitz` — das ganze Ziel steht in spitzen Klammern" | **trifft für 2 von 5 `spitz`-Treffern nicht zu**, siehe `HIGH-2` |
| dieselbe Pipeline ohne den zweiten Filter, mit `sort \| uniq -c` | `7 eingebettet`, `5 spitz` | — | Verteilung der zwei Eimer über den realen Satz |
| `for f in $(ls test/*.bats \| LC_ALL=C sort); do command grep -c '^@test' "$f"; done` kumuliert | `test/courseset-fixture.bats` belegt `40`–`43`; der neue Fall ist `43`, die zwei `CO-004`-Fälle sind `40` und `41` | Commit: „`ok 43`", „`not ok 40, 41`" | **stimmt, Nummerierung unabhängig nachgerechnet** |
| `command grep -c 'body = NeutralizePlaceholderLinks(body)' internal/emit/templates.go` und `… internal/emit/readme.go` | `1` und `1` | Fälle 206/208 treffen genau eine Zeile | **stimmt** |
| `command grep -c '\[<welle-NN-titel>\](\.\./<welle-NN-titel>\.md)' internal/emit/templates_test.go` | `2` | Fall 207: „Die Fixture verliert die EINGEBETTETE Platzhalter-Form" | **trifft nur zur Hälfte** — der `sed` erreicht zwei Zeilen, siehe `LOW-2` |
| `command grep -n '^@test' test/courseset-fixture.bats` | `59`, `77`, `93`, `143` | `CO-004` nennt dieselben drei Fälle mit `(Z. 53)`, `(Z. 71)`, `(Z. 87)` | **überholt durch diesen Commit**, siehe `LOW-4` |
| `command grep -oE '\]\([^()[:space:]]*\)' .harness/baseline/v5.12.0/templates/project-readme.template.md \| command grep -cE '<[^<>]*>'` | `0` bei `3` Link-Zielen insgesamt | `readme.go`-Kommentar: „Der Pointer-Abschnitt der heutigen Vorlage traegt keinen Platzhalter-Link" | **stimmt** — der Aufruf ist über dem heutigen realen Satz wirkungslos und ausdrücklich als Form-Regel begründet |
| `M=$(command grep -m1 'roadmapDoneLink =' internal/emit/templates.go \| cut -d'"' -f2); command grep -cF "$M" .harness/baseline/v5.12.0/templates/docs/plan/planning/roadmap.template.md` | `1` | `NeutralizeRoadmap` behält einen Gegenstand, und `NeutralizePlaceholderLinks` frisst ihn nicht | **stimmt** — der Marker trägt keinen Platzhalter im Pfad und läuft unverändert durch die neue Regel |
| `make comment-claims` | `comment-claims: 46 Datei(en) geprueft, 0 Befund(e)`, Exit 0 | Commit: „comment-claims (46 Datei(en), 0 Befunde)" | **stimmt, selbst gefahren** |
| `git show f979b59 \| command grep -nE '^\+.*(nolint\|shellcheck disable\|#nosec)'` | keine Ausgabe | §3.2 | **stimmt** |
| `git show --stat f979b59 -- spec/ docs/plan/adr/ AGENTS.md harness/conventions.md docs/plan/planning/ docs/plan/carveouts/` | leer | §3.8, `MR-015`: keine fremden Rollen-Artefakte, `spec/lastenheft.md` unverändert | **stimmt** |
| `git show --raw f979b59 \| command grep '^:'` | die drei neuen `test/mutations/*.sh` mit Modus `100755` | — | ausführbar, wie der Treiber es braucht |
| `cat .dockerignore` | `.git`, `.harness` | bats statt go-test, weil `.harness/` nicht im Build-Kontext liegt | **stimmt, Begründung real** |

**Nicht selbst gefahren, bewusst:** `make mutate` (Auftrag: bricht am Grün-Vorlauf ab, solange
`CO-004` steht) und `make smoke` / `make -k gates` (die Gate-Lage ist Gegenstand der Verifikation,
nicht dieses Laufs). Wo unten eine Aussage darauf beruht, steht sie als Übernahme gekennzeichnet.

---

## Findings

Schema je Finding: `kategorie` · `quelle` · `pfad` · `befund` · `failure-szenario` ·
`verifizierbar`. HIGH zuerst, dann absteigend. Kein Lösungsvorschlag im Befund.

### HIGH-1 — Die neue Inhalts-Achse liefert über dem Vor-Tausch- und dem Nach-Tausch-Baum dasselbe Urteil; sie kann den Bruch, für den sie gebaut wurde, nicht sehen

- `kategorie`: **HIGH**
- `quelle`: `AGENTS.md` §3.6 (*„Keine Zusage ohne rot gesehenes Gegenbeispiel"* — ein Test, dessen
  Name eine Eigenschaft behauptet, muss die Eigenschaft messen) und §3.7; `LH-QA-01`
  (stilles Grün in einem Gate); Slice-Plan DoD (3) (*„Ein Wächter innerhalb von `make gates` sieht
  die Klasse"*); Reviewer-Skill §HIGH (Stilles-Grün-Pfad in einem Gate) und §Kontext-Eskalation.
- `pfad`: `test/courseset-fixture.bats:110-171` (Klassifikator und `@test`), dazu die zwei Orte, die
  dieselbe Aussage tragen: `internal/emit/templates.go` im Doc-Kommentar von
  `NeutralizePlaceholderLinks` (*„dass die Fixture die Formen des REALEN Satzes ueberhaupt traegt,
  haelt test/courseset-fixture.bats fest"*) und die Commit-Message von `f979b59`
  (*„Warum genau dort: der historische Bruch IST dieser Fall — der Baum-Tausch brachte Formen mit,
  die die Fixture nicht kannte"*).
- `befund`: Die Wertemenge des Klassifikators hat genau zwei Elemente — `test/courseset-fixture.bats:125`
  führt `print (substr(ziel, 1, 1) == "<") ? "spitz" : "eingebettet"`. Über dem Vor-Tausch-Baum
  `v3.5.2` und über dem heutigen `v5.12.0` ist diese Menge identisch `{eingebettet, spitz}`
  (`formen` je Baum, Kommandos in der Tabelle oben; für `v3.5.2` gespeist aus
  `git ls-tree -r --name-only c6cc56f`). Der Baum-Tausch hat in der Sprache dieses Wächters also
  **keine** neue Form mitgebracht. Die Fixture führte am Vor-Tausch-Stand `c6cc56f` **null** Formen
  (`… | formen | command grep -c .` → `0`, bei `34` gelesenen Rumpf-Zeilen), heute **zwei**.
  Daraus folgt für den Vergleich `real ⊆ fix` dasselbe Urteil links und rechts des Tauschs: mit der
  damaligen Fixture rot **vor und nach** dem Tausch, mit der heutigen grün **vor und nach** dem
  Tausch. Ein Wächter, dessen Ausgabe sich über dem Ereignis nicht ändert, kann das Ereignis nicht
  anzeigen — die drei zitierten Stellen sagen das Gegenteil.
- `failure-szenario`: Nach diesem Commit gibt es für `ok 43` nur noch zwei Rot-Wege: der reale Satz
  verliert **jeden** `<…>`-Pfad-Link (dann feuert `[ -n "$real" ]`), oder die Fixture verliert einen
  Eimer — eine repo-seitige Regression, genau Fall `207`. Eine **Upstream**-Änderung kann ihn nicht
  mehr röten, denn jeder `<…>`-Pfad-Link fällt in einen der zwei Eimer, und beide liegen in der
  Fixture. Die zwei Klassen, die im Zielrepo wirklich einen toten Link erzeugen würden, liegen dabei
  außerhalb seiner Reichweite: eine andere Platzhalter-Schreibweise (`{{…}}`, `@@…@@`) fällt schon
  durch den `<[^<>]*>`-Filter des Klassifikators, und ein Link, dessen Text `[` oder `]` trägt oder
  dessen Ziel Leerraum bzw. Klammern trägt, wird vom Klassifikator in einen **vorhandenen** Eimer
  einsortiert — er liest allein `](ziel)` und sieht den Text-Teil nicht —, während
  `NeutralizePlaceholderLinks` ihn nach Grenze 3 des eigenen Doc-Kommentars nicht erfasst. Der
  emittierte Baum trüge dann denselben toten Link wie beim historischen Bruch, `make gates` bliebe
  grün, und gesehen hätte es wieder nur `make smoke` außerhalb der Gates. Wer DoD (3) an diesem
  Wächter abhakt, hält für gedeckt, was für die Klasse, die den Bruch verursacht hat, nicht gedeckt
  ist.
- `verifizierbar`: **ja für den Befund, nein für ein Gate.** Die zwei `formen`-Läufe über `c6cc56f`
  und den Arbeitsbaum belegen ihn mechanisch. Rot färbt ihn heute nichts: `make comment-claims`
  prüft vier Pfad-Muster (`internal/**/*.go`, `cmd/**/*.go`, `harness/tools/*.sh`,
  `.claude/hooks/*.sh`, `AGENTS.md` §4) — `test/*.bats` liegt dauerhaft außerhalb; selbst gefahren:
  `comment-claims: 46 Datei(en) geprueft, 0 Befund(e)`.

### HIGH-2 — Der Kommentar definiert `spitz` als „das ganze Ziel steht in spitzen Klammern"; der Code prüft das erste Zeichen, und der vendored Satz trägt zwei Gegenbeispiele

- `kategorie`: **HIGH**
- `quelle`: `AGENTS.md` §3.7 (Hard Rule — *„Ein Kommentar beschreibt, was da ist"*); Reviewer-Skill
  §HIGH (Verstoß gegen eine Hard Rule) und §Kontext-Eskalation (Gate-Pfad). Kalibriert an
  `docs/reviews/2026-08-22-slice-088-review.md` `HIGH-1`, wo dieselbe Form — ein im Diff
  geschriebener Kopf-Kommentar eines Gate-Artefakts, am eigenen Gegenstand widerlegbar — HIGH war.
- `pfad`: `test/courseset-fixture.bats:110-115` (Kommentar) gegen `test/courseset-fixture.bats:125`
  (Code).
- `befund`: Der Kommentar schreibt: *„`spitz` — das ganze Ziel steht in spitzen Klammern,
  `](<pfad>)`"* und begründet die Zweiteilung damit, dass *„ein Markdown-Parser die zwei verschieden
  liest"*. Der Code prüft ausschließlich `substr(ziel, 1, 1) == "<"`. Über dem Satz, auf den die
  Funktion zeigt, fallen damit `5` Ziele in den Eimer `spitz` (Pipeline mit `sort | uniq -c`,
  Tabelle oben: `7 eingebettet`, `5 spitz`), von denen `2` der Definition widersprechen — beide
  `<NNNN>-<titel>.md`, in `docs/plan/adr/README.template.md:10` und
  `docs/plan/adr/NNNN-titel.template.md:17`. Dort steht nicht das ganze Ziel in spitzen Klammern,
  sondern nur das erste Pfad-Segment; ein Markdown-Parser liest es wie jedes andere eingebettete
  Beispiel, nicht wie ein `<…>`-Destination-Literal. Kommando:
  `find .harness/baseline/*/templates -type f -name '*.md' -exec cat {} + | command grep -oE '\]\([^()[:space:]]*\)' | awk '{z=substr($0,3,length($0)-3); sub(/#.*$/,"",z); if (z !~ /<[^<>]*>/) next; if (substr(z,1,1)=="<" && z !~ /^<[^<>]*>$/) print z}'`
  → zweimal `<NNNN>-<titel>.md`.
- `failure-szenario`: Der einzige real angle-bracket-gewrappte Ziel-Typ des Satzes ist `](<pfad>)`,
  `3` Vorkommen in `harness/README.template.md:152-154` (Zeilen-Liste in der Tabelle oben). Fällt er
  upstream weg, meldet der Klassifikator wegen `<NNNN>-<titel>.md` weiterhin `spitz`, die Fixture
  passt weiter, und der Wächter bleibt grün — obwohl die Form, deren abweichendes Parsing der
  Kommentar als Grund der Zweiteilung nennt, im realen Satz nicht mehr vorkommt und der
  Emit-Testsatz über sie nichts mehr sagt. Umgekehrt liest jeder, der den Klassifikator anfasst, aus
  dem Kommentar eine Regel heraus, die der Code nicht führt, und ändert ihn gegen einen
  Ist-Zustand, den es nicht gibt.
- `verifizierbar`: **ja für den Befund, nein für ein Gate** — dasselbe Kommando; `test/*.bats` liegt
  außerhalb von `make comment-claims`, und der Gate prüft ohnehin nur, ob ein *genannter Sensor
  existiert*, nicht, worüber ein Kommentar spricht.

### MEDIUM-1 — Vier lebende Artefakte führen die 7/3-Aufteilung, die dieser Commit widerlegt hat; keine Übergabe benennt sie

- `kategorie`: **MEDIUM**
- `quelle`: `MR-025` Setzung 1 (eine Zahl mit Messwert-Rolle steht neben dem Kommando, das genau sie
  ausgibt); Reviewer-Skill §MEDIUM (Bezugs-/Abdeckungslücke), eskaliert aus Doku-Drift, weil
  `CO-004` den Geltungsbereich eines heute roten Gates festlegt und *„extensional geschlossen"* ist.
- `pfad`: `docs/plan/carveouts/done/CO-004-emitter-klassifikation-offen.md` (Kopf, Absatz
  *„Nicht Geltungsbereich"*: *„**sieben** Befunde slice-133, **drei** slice-130"*),
  `docs/plan/planning/done/slice-130-emitter-entscheidet-jedes-neue-template.md:119`,
  `docs/plan/planning/welle-10-re-baseline.md:273`,
  `docs/plan/planning/in-progress/roadmap.md:279`.
- `befund`: Der Commit nimmt `8` der `10` Befunde weg, nicht `7` — die sieben der Ursache A plus
  `harness/conventions/MR-NNN-titel.md`, das dieselbe Form trägt und von der Form-Regel miterfasst
  wird. Die vier Artefakte oben führen weiterhin die Aufteilung `7` / `3`. Die Commit-Message stellt
  die Tatsache fest (*„slice-130 traegt damit noch 2 statt 3 Befunde"*), benennt aber weder ein
  Artefakt noch einen Träger; der einzige Übergabe-Satz lautet *„DoD-Haken, Risiko-Ausgaenge und
  Closure-Notiz setzt der Planner"*. Dass der Implementer die vier Dateien **nicht** angefasst hat,
  ist richtig — es sind Planner-Artefakte —, aber die Korrektur hängt damit an keinem Ort.
- `failure-szenario`: `slice-130` startet gegen seinen Plan und `CO-004`, erwartet `3` Befunde der
  Klasse B, misst `2` und liest die Differenz als Regression oder als schon erledigt — beides führt
  zu einer falschen Klassen-Entscheidung für die vier neuen Vorlagen, und `CO-004` löst sich an einer
  Bedingung auf, die niemand mehr nachrechnen kann. Verschärfend: `CO-004` erklärt seinen
  Geltungsbereich für extensional geschlossen, seine Trägerschafts-Zeile ist damit normativ und
  nicht illustrativ.
- `verifizierbar`: **nein, kein Gate liest diese Zahlen.** Den Ist-Wert liefert `make smoke`
  (Übernahme aus der Übergabe: `23 Datei(en) geprüft, 2 Befund(e)`); die Herkunft der `8` ist am
  Diff mechanisch nachvollziehbar (die 8 emittierten Fundstellen aus der Zeilen-Liste der Tabelle
  oben, alle `12` von `12` durch die Emit-Regexp erfasst).

### LOW-1 — `internal/emit/readme.go` liegt außerhalb des Plan-Tischs §3, und die Abweichung steht nur in der Commit-Message

- `kategorie`: **LOW**
- `quelle`: Maintainability (Plan gegen Diff). Kein Code-Defekt: `LH-FA-02` zählt die
  Root-`README.md` ausdrücklich zu den Singletons, `MR-017` verlangt für emittierte Prüfbereiche die
  fail-closed-Richtung, und `docs/reviews/2026-08-25-slice-087-review.md` `F-2` hat die
  gleichgelagerte Abweichung an derselben Datei bereits als LOW eingeordnet.
- `pfad`: Plan §3 (Tabelle *„Datei / Komponente"*) gegen `internal/emit/readme.go`.
- `befund`: Die Plan-Tabelle nennt `internal/emit/templates.go`, `internal/emit/templates_test.go`
  und `test/mutations/`; `internal/emit/readme.go` steht dort nicht, und anders als in `slice-087`
  nennt auch kein DoD-Satz dieses Slice die Root-README. Plan §1 formuliert das Ziel über *„die drei
  Singleton-Artefakte"*. Der zweite Aufrufpunkt ist damit eine echte Umfangs-Erweiterung. Sie ist
  über dem heutigen realen Satz wirkungslos: `project-readme.template.md` trägt `3` Link-Ziele und
  davon `0` mit Platzhalter im Pfad
  (`command grep -oE '\]\([^()[:space:]]*\)' .harness/baseline/v5.12.0/templates/project-readme.template.md | command grep -cE '<[^<>]*>'`
  → `0`); der Kommentar an der Stelle sagt das selbst und begründet den Aufruf mit der Form statt
  mit dem heutigen Stand. Fachlich trägt das. Nur ist die Abweichung in keinem Artefakt vermerkt,
  das ein späterer Lauf liest.
- `failure-szenario`: Ein Verifier hält den Diff gegen §3 und findet eine Datei mehr, ohne eine
  Quelle, die sie deckt; die Entscheidung wird ein zweites Mal geführt, oder die Erweiterung wird
  als ungedeckt gewertet, obwohl sie es nicht ist.
- `verifizierbar`: **nein** — kein Gate liest die Plan-Tabelle gegen den Diff.

### LOW-2 — Der Kopf von Fall `207` beschreibt eine Mutation an der Fixture; der `sed` erreicht zwei Zeilen

- `kategorie`: **LOW**
- `quelle`: `AGENTS.md` §3.7 (gilt für Skripte, die dieses Repo besitzt).
- `pfad`: `test/mutations/207-fixture-ohne-eingebettete-platzhalter-form.sh:5-8` gegen
  `internal/emit/templates_test.go:30` und `:597`.
- `befund`: Der Kopf sagt *„Die Fixture verliert die EINGEBETTETE Platzhalter-Form"*. Das Muster
  kommt in der Zieldatei zweimal vor
  (`command grep -c '\[<welle-NN-titel>\](\.\./<welle-NN-titel>\.md)' internal/emit/templates_test.go`
  → `2`): einmal in `courseSet()` (Zeile `30`) und einmal als Eingabe eines Falls von
  `TestNeutralizePlaceholderLinks` (Zeile `597`). `sed -i` ohne Adresse ersetzt in beiden Zeilen; die
  Mutation ändert also zusätzlich eine Unit-Test-Erwartung, die mit der Fixture nichts zu tun hat.
  Für das Verdikt des Treibers ist das folgenlos — `narrow_sensor` wählt aus der `# expect:`-Zeile
  `test-bats`, und die Go-Stufe läuft für diesen Fall nicht —, für die Aussage *„der Delta ist genau
  das neue Rot"* ist es eine ungenannte zweite Wirkung.
- `failure-szenario`: Fällt `CO-004` und läuft der Fall künftig unter `# verify: test` (voller Satz)
  oder liest jemand den A/B-Beleg über `make test` statt `make test-bats` nach, fallen Go-Tests mit,
  deren Rot der Kopf nicht erklärt — der Befund wird dem Wächter zugeschrieben statt dem
  Mutations-Zuschnitt.
- `verifizierbar`: **ja** — das `grep -c` oben; kein Gate deckt es (`test/mutations/*.sh` liegt
  außerhalb von `make comment-claims`).

### LOW-3 — Die im Plan §3 offen gelassene Ebenen-Entscheidung ist durch Unterlassen beantwortet, nicht in Schrift

- `kategorie`: **LOW**
- `quelle`: Slice-Plan §3, Zeile `internal/emit/templates/d-check.yml` (*„offen — Entscheidung im
  Slice … der Slice begründet, welche Ebene trägt, statt beide anzufassen"*); `MR-017`.
- `pfad`: `internal/emit/templates/d-check.yml` (unverändert), Commit-Message `f979b59`.
- `befund`: Der Plan verlangt eine **Begründung**, welche Ebene die Reparatur trägt — Emitter oder
  emittierter Prüfbereich. Der Diff berührt die Datei nicht, und weder die Commit-Message noch ein
  Code-Kommentar nennt sie. Der Doc-Kommentar von `NeutralizePlaceholderLinks` begründet allein die
  Abgrenzung gegen den **Kurs** (*„der Vorlagen-Satz gehoert dem Kurs und liegt unveraenderlich
  vendored (AGENTS 3.4), die Reparatur faellt darum emit-seitig"*), nicht die gegen den emittierten
  `scan.ignore`. Die Entscheidung ist damit richtig getroffen und unbelegt.
- `failure-szenario`: Ein späterer Slice, der denselben Bruch an einer anderen Vorlagen-Klasse
  antrifft, findet keine Begründung, warum der Ausschluss nicht in den emittierten Prüfbereich
  gehört, und trifft die Abwägung neu — mit dem Risiko, dass der Adopter dann doch einen blinden
  Fleck in seinem eigenen `d-check` erbt, den `MR-017` fail-closed ausschließt.
- `verifizierbar`: **nein** — kein Gate liest den Plan.

### LOW-4 — `CO-004` zitiert Zeilennummern, die dieser Commit verschoben hat

- `kategorie`: **LOW**
- `quelle`: `MR-025` Setzung 1 (die Zahl steht neben dem Kommando, das genau sie ausgibt).
- `pfad`: `docs/plan/carveouts/done/CO-004-emitter-klassifikation-offen.md`, Kopf-Absatz
  *„Geltungsbereich"* (`(Z. 53)`, `(Z. 71)`, `(Z. 87)`) und die Ausgangs-Tabelle
  (*„die Fälle Z. 53 und Z. 71"*).
- `befund`: `CO-004` nennt das Kommando selbst — `grep -n '^@test' test/courseset-fixture.bats` —
  und stellt drei Zeilennummern daneben. Nach diesem Commit liefert dasselbe Kommando `59`, `77`,
  `93`, `143` (selbst gefahren, siehe Tabelle). Die Fall-**Nummern** `not ok 40` / `not ok 41`, die
  `CO-004` weiter unten nennt, bleiben korrekt — der neue Fall steht am Dateiende. Zweiter Fundort
  derselben Klasse, schwächer: `docs/plan/planning/open/slice-115-jeder-sensor-sagt-seinen-ausgang.md:116-117`
  zitiert `not ok 71` / `not ok 72` aus einer datierten Sonde; die Einfügung verschiebt jede
  bats-Nummer ab `44` um eins. Dort ist es ein festgehaltenes Lauf-Protokoll, nicht ein
  Erwartungswert — deshalb genannt, nicht gleichgewichtet.
- `failure-szenario`: Wer den Geltungsbereich von `CO-004` gegen `AGENTS.md` §3.5 prüfen will
  (*„extensional geschlossen"*), springt an die genannten Zeilen und landet in fremdem Text; die
  Prüfung, welche zwei Fälle gedeckt sind, läuft über den Wortlaut weiter — sie muss es nur wissen.
- `verifizierbar`: **nein, kein Gate** — `codepaths.check-lines` prüft Zeilen-Referenzen der Form
  `datei:<von>-<bis>` in Inline-Code, nicht Prosa-Angaben der Form `(Z. 53)`.

### INFO-1 — Die verbliebenen Zähne von `ok 43` liegen gegen eine Fixture-Regression, und die deckt der Go-Test nicht vollständig ab

- `kategorie`: **INFO**
- `quelle`: Maintainability (dokumentationswürdige Annahme).
- `pfad`: `test/courseset-fixture.bats:143-171`, `internal/emit/templates_test.go`
  (`TestTemplates_KeinPlatzhalterLinkImEmittiertenSatz`, Leerlauf-Sperre `inQuelle == 0`).
- `befund`: Auch unter `HIGH-1` bleibt ein Rest, der nicht redundant ist: die Leerlauf-Sperre des
  Go-Tests feuert erst, wenn die Fixture **jeden** Platzhalter-Link verliert; `ok 43` feuert schon,
  wenn sie **einen der zwei Eimer** verliert — das ist genau der Unterschied, den Fall `207`
  ausnutzt. Wer die Inhalts-Achse anfasst, entfernt mit ihr also auch diesen engeren Schutz.
- `failure-szenario`: kein eigenständiges; die Notiz verhindert, dass der Rest bei einer Nacharbeit
  unbemerkt mitgeht.
- `verifizierbar`: **ja** — Fall `207` ist die Probe, sobald `CO-004` fällt.

---

## Negativbefunde — geprüft, ohne Befund

- **Der mitgefallene Befund der Klasse B (`harness/conventions/MR-NNN-titel.md`) ist kein Übergriff
  in fremdes Gebiet.** Die Regel verfügt über keine Namen; eine Ausnahme bräuchte genau die
  Namensliste, deren Fehlen der Grund dieses Slice ist — die Begründung des Implementers trägt. Die
  Klassen-Entscheidung von `slice-130` ist unberührt: `inScope`, `isRecurring` und
  `isDerivativeIndex` sind im Diff nicht angefasst
  (`git show f979b59 -- internal/emit/templates.go | command grep -c '^[+-].*func \(inScope\|isRecurring\|isDerivativeIndex\)'`
  → `0`), die Vorlage wird also weiterhin als Singleton emittiert und ihre Klassen-Frage steht
  offen. Was `slice-130` dadurch verliert, ist ein Befund, kein Entscheidungsgegenstand — die
  Buchhaltung darüber ist `MEDIUM-1`.
- **Der Ort des DoD-(3)-Wächters ist gedeckt.** Der Plan lässt ihn ausdrücklich offen
  (*„Der Slice entscheidet je Antwort"*), und die Begründung für bats statt go-test ist real:
  `.dockerignore` schließt `.harness` aus, die go-test-Stage sieht den vendored Baum nicht.
  `test/courseset-fixture.bats` läuft über `test-bats` in `test` und damit in `gates`
  (`command grep -n '^gates:' Makefile` → `gates: baseline-verify docs-check lint build test …`).
  Der Wächter liegt also mechanisch **innerhalb** von `make gates`; was er dort misst, ist `HIGH-1`.
- **Die Reichweite der Emit-Regel über dem realen Satz ist vollständig.** Alle `12`
  Platzhalter-Pfad-Links des vendored Satzes werden von der Form der Emit-Regexp erfasst (`12` von
  `12`, beide Kommandos in der Tabelle); die Grenzen 1, 2 und 3 des Doc-Kommentars haben heute kein
  Gegenbeispiel im Satz. Die `7` Fundstellen der DoD-(1)-Dateien sind damit strukturell erreichbar.
- **Die Reihenfolge der drei Neutralisierungen ist unkritisch.** `NeutralizeMakeClaims` kann keinen
  Platzhalter in ein Link-Ziel schreiben (sein Muster verlangt ein Leerzeichen, Ziele schließen
  Leerraum aus), und `NeutralizePlaceholderLinks` lässt den `roadmapDoneLink`-Marker unangetastet
  (kein `<…>` im Pfad; Marker im realen Satz weiterhin einmal vorhanden, Kommando in der Tabelle) —
  `NeutralizeRoadmap` behält seinen Gegenstand.
- **Der neue Wiring-Test misst sich nicht selbst.** `platzhalterZiele` ist ein eigener Scanner
  (Split an `](`, zeichenweise Prüfung), nicht die Emit-Regexp; er ist **breiter** als sie und würde
  einen Link melden, den sie nicht erfasst. Zwei Leerlauf-Sperren (`inQuelle == 0`,
  `dokumente == 0`) sind vorhanden, dazu die Gegenrichtung (`<welle-NN-titel>` steht nach dem Emit
  noch da) — Risiko 4 des Plans (*„eine Zeile löschen statt entschärfen"*) ist damit gemessen.
- **Die drei Mutations-Fälle treffen die Verdrahtung, die der Aufrufer benutzt.** `206` und `208`
  löschen je genau eine Zeile in `templates.go` bzw. `readme.go` (`grep -c` je Datei → `1`); der
  erwartete Wächter ist ein Go-Test, `narrow_sensor` wählt korrekt `test-go`, und
  `--- FAIL: TestTemplates_…` erfüllt `failure_form`. `207` wählt `test-bats` und
  `not ok [0-9]+`. Kein Fall baut die Verdrahtung nach: `207` mutiert die reale Fixture-Datei, die
  auch die Go-Tests lesen, und der bats-Wächter liest dieselbe Datei über `fixture_body`.
- **Die A/B-Ersatzmessung ist in ihren nachprüfbaren Teilen konsistent.** Die Nummerierung stimmt
  unabhängig nachgerechnet (`40`/`41` = die zwei `CO-004`-Fälle, `43` = der neue), und der
  Abbruchgrund ist am Treiber belegt: `green_prerun` fährt jeden benutzten Modus einmal, `test-bats`
  ist rot, also endet der Lauf vor dem ersten Fall. Nicht nachprüfbar ohne den Lauf selbst bleibt,
  ob im Vorlauf der Kopie wirklich nur `40`/`41` rot waren; das ist eine Übernahme und als solche
  gekennzeichnet.
- **§3.2** — keine Inline-Suppression im Diff (Kommando in der Tabelle).
- **§3.3** — keine Umbenennung im Diff (`git show --raw f979b59` zeigt `M`/`A`, kein `R`).
- **§3.5** — keine Gate-Lockerung: `.d-check.yml`, `internal/emit/templates/d-check.yml` und die
  `scan.ignore`-Listen sind unberührt; der Diff **fügt** einen Fall hinzu.
- **§3.8 / `MR-015`** — der Commit berührt kein Architect- und kein Planner-Artefakt
  (`git show --stat f979b59 -- spec/ docs/plan/adr/ AGENTS.md harness/conventions.md docs/plan/planning/ docs/plan/carveouts/`
  → leer). DoD-Haken und Risiko-Ausgänge sind ausdrücklich dem Planner überlassen. `spec/lastenheft.md`
  unverändert, wie Plan §3 es festlegt.
- **§3.9** — der Diff führt keine Host-Toolchain ein; die drei neuen Fall-Skripte benutzen `bash`
  und `sed`.
- **§3.7 an den übrigen geänderten Kommentaren** — der nachgezogene Doc-Kommentar von
  `NeutralizeRoadmap` steht im Indikativ und trifft zu (`courseset-fixture.bats` gleicht vom Inhalt
  wirklich nur die Platzhalter-Pfad-Form ab, keinen Wortlaut); der `readme.go`-Kommentar ist an der
  Quelle nachgemessen richtig (`0` Platzhalter-Ziele in `project-readme.template.md`); die vier
  Grenzen im Kopf von `NeutralizePlaceholderLinks` sind Negativ-Aussagen und decken die
  Regex-Ränder, die ich geprüft habe, vollständig ab. Der Befund liegt allein bei der Zusage über
  den bats-Wächter (`HIGH-1`) und bei der `spitz`-Definition (`HIGH-2`).
- **Referenzierte ADRs** — keine; damit auch keine superseded referenziert und keine aktive
  verletzt.

---

---

## Gate-Beobachtung am Rand — nicht Gegenstand dieses Reviews

`make docs-check`, selbst gefahren nach dem Anlegen dieses Reports:
`d-check: 454 Datei(en) geprüft, 4 Befund(e)`, Exit 1. Aufgeteilt:

- `harness/conventions.md:1019` `target-missing` — der bekannte `CO-005`-Befund, Träger
  `slice-132`, vom Slice unabhängig.
- **drei** Befunde in `docs/plan/planning/open/slice-136-roadmap-traegt-die-ziel-form.md`
  (Zeilen `63`, `64`, `145`, je `codepath-missing` auf `../reconciliation.md` bzw.
  `../observations.md`). Diese Datei ist **untrackt** und war zu Beginn dieses Laufs nicht im Baum
  (`git status --short` war leer; jetzt zeigt es `?? docs/plan/planning/open/slice-136-…` und
  `?? docs/reviews/2026-08-29-slice-133-review.md`, `git ls-files` kennt sie nicht). Sie ist
  während dieses Laufs von außerhalb entstanden und gehört nicht zu `f979b59`.

Dieser Report selbst trägt **0** Befunde: `454 − 1` (dieser Report) `− 1` (die untrackte Datei)
`= 452` geprüfte Dateien und `4 − 3 = 1` Befund entsprechen genau der Lage, die die Übergabe
nennt (`452 geprüft, 1 Befund = CO-005`).

## Kategorie-Summary

| Kategorie | Anzahl | IDs |
|---|---|---|
| HIGH | 2 | `HIGH-1`, `HIGH-2` |
| MEDIUM | 1 | `MEDIUM-1` |
| LOW | 4 | `LOW-1`, `LOW-2`, `LOW-3`, `LOW-4` |
| INFO | 1 | `INFO-1` |

## Verdikt

**NICHT KONFORM — merge-blockierend: ja. Der Slice geht in dieser Fassung nicht zum Verifier.**

**Begründung.** Zwei HIGH und ein MEDIUM stehen offen; HIGH und MEDIUM blockieren nach Skill
§Ablage typischerweise, und keiner der drei trägt einen Grund, davon abzuweichen. Tragend ist
`HIGH-1`: er betrifft **genau** den Punkt, den der Verifier als Nächstes abhaken würde. DoD (3)
verlangt einen Wächter innerhalb von `make gates`, der die Klasse sieht; gebaut ist ein Wächter,
dessen Urteil über dem Ereignis, das die Klasse definiert, unverändert bleibt — über dem
Vor-Tausch- wie über dem Nach-Tausch-Baum ist seine Eingangsmenge dieselbe. Ein Verifier, der die
DoD gegen `ok 43` hält, hakt eine Zusage ab, deren Gegenbeispiel nicht rot werden kann; das ist die
Lage, gegen die §3.6 geschrieben ist. `HIGH-2` ist die schmalste Reparatur von allen und am
eigenen Gegenstand mit einem Kommando widerlegbar oder bestätigbar.

**Was ausdrücklich nicht blockiert und belegt trägt:** die Substanz der Reparatur. Die
Neutralisierung trägt die **Form** statt eines Wortlauts, sie erfasst alle `12`
Platzhalter-Pfad-Links des realen Satzes, sie lässt den Link-Text verbatim stehen (Risiko 4 des
Plans, im Test gemessen), sie liegt im Emitter statt im emittierten Prüfbereich, und ihre vier
Grenzen stehen am Code. Die Wiring-Probe hat einen unabhängigen Scanner und zwei Leerlauf-Sperren.
Der mitgefallene Klasse-B-Befund ist die richtige Konsequenz der Bauart, kein Übergriff. Die
Rollen-Trennung ist im Commit-Zuschnitt sauber vollzogen. `LOW-1` bis `LOW-4` und `INFO-1` sind
Buchhaltung und Kommentar-Genauigkeit, kein Verhalten.

**Was der Reviewer nicht entscheidet:** wie `HIGH-1` aufgelöst wird — ob der Wächter eine andere
Achse bekommt, ob DoD (3) auf die zweite zugelassene Antwort fällt (*„ausdrücklich nicht gebaut,
mit Grund und Folge-Slice"*), oder ob der Schnitt nach Plan §4 zurückgeht (*„wenn die Entscheidung
aus DoD (3) einen eigenen Wächter verlangt, der mehr ist als die Umkehrung der Neutralisierung"*).
Der Plan hält alle drei Wege offen; die Wahl gehört in die Übergabe an Planner und Implementer.
