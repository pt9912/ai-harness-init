# Review — ADR-0024, MR-031, MR-032 und die §3.7-Fortschreibung

**Datum:** 2026-08-29 · **Rolle:** Reviewer (Modul 10) · **Lauf:** 1

**Quelle der Rolle:** `modul-08-agentenrollen.md:144` (`v5.12.0`) — *„ADR-Änderung: Architect
schreibt; Reviewer prüft auf Konsistenz; Implementer liest als Constraint"*.

## Kopf-Metadaten

- **Gegenstand (Diff-Range):** uncommittete Architect-Arbeit im Arbeitsbaum am Stand `06a640a`,
  vier Artefakte: `git diff -- AGENTS.md harness/conventions.md docs/plan/adr/README.md` plus die
  untrackte Datei `docs/plan/adr/0024-derivatives-register-gehoert-der-rolle-seines-originals.md`.
  Umfang: `git diff --numstat -- AGENTS.md harness/conventions.md docs/plan/adr/README.md` →
  **45/18**, **178/88**, **1/0** (+/−). Nicht Gegenstand: die uncommittete Planner-Arbeit im
  selben Baum (`slice-081`, `welle-10`, `roadmap.md`, `slice-082`, `slice-083`, `slice-134`,
  `slice-135`).
- **Betroffene `LH-*`:** [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
  (keine behaupteten Wächter), [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)
  (jede Messung gegen den gepinnten Baum).
- **Referenzierte aktive ADRs:** [ADR-0014](../plan/adr/0014-aufgehobener-eintrag-kopf-statt-rumpf.md),
  [ADR-0015](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md),
  [ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md),
  [ADR-0017](../plan/adr/0017-doku-gate-ausnahme-fuer-ein-eingefrorenes-adr.md),
  [ADR-0023](../plan/adr/0023-verweis-beschluss-traegt-ueber-den-sprung.md).
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §3.4 (ADRs ab `Accepted` immutabel), §3.5,
  §3.6, §3.7, §3.8, §3.9 (Docker-only — dieser Lauf hat `make docs-check` und
  `make comment-claims` gefahren, keine Host-Toolchain).
- **Vorherige Findings am gleichen Modul:** `docs/reviews/2026-08-09-slice-066-bestaetigungsrunde.md`
  (B2/B3/B4 am Paar `AGENTS.md` §3.7 / `MR-022` — dreimalige Wiederholung derselben Klasse,
  Steering-Loop-Signal), `docs/reviews/2026-08-09-slice-066-verdikt-runde.md`
  (`ADR-0014`-Konformität der `MR-023`-Konstruktion, damals ohne Befund),
  `docs/reviews/2026-08-28-slice-081-review.md` (`MR-022`/`MR-023`/§3.7 als offener Posten der
  Re-Baseline).
- **Plan:** `slice-082` §2 (3) und §3 (Form-Entscheidung und Architect-Zuweisung), `slice-083` §2
  (`Ersetzt-Baseline-Regel`, Form-Vergleich der Singletons), `slice-134` §3/§6
  (Zuständigkeit für den ADR-Index) — alle drei uncommittet und nur als **zitierte Quelle** des
  Gegenstands geprüft, nicht als Gegenstand.
- **Gate-Stand dieses Laufs:** `make docs-check` → `d-check: 451 Datei(en) geprüft, 4 Befund(e)`;
  `make comment-claims` → `comment-claims: 46 Datei(en) geprueft, 0 Befund(e)`. Einordnung der
  vier: einer ist der von [`MR-030`](../../harness/conventions.md#mr-030--der-rollen-name-der-baseline-und-der-bezeichner-fallen-zusammen)
  ausdrücklich in Kauf genommene `target-missing` in `harness/conventions.md`, drei stammen aus
  `docs/plan/planning/open/slice-135-d-check-pin-v0661.md` — einer Datei, die während dieses Laufs
  im Baum erschien und nicht Gegenstand ist. **Der Gegenstand fügt keinen Befund hinzu.**

---

## Findings

### HIGH-1 — `ADR-0024` steht auf `Accepted`, ohne dass der Acceptance-Trigger stattgefunden hat

- `kategorie`: HIGH
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.4 · Baseline `v5.12.0`
  `grundlagen-bootstrap.md` §Trigger-Klassen (*„Acceptance-Trigger: Phase-Übergang via Sign-off
  (z. B. ADR Proposed → Accepted)"*, Beispielspalte *„ADR-Review-Runde abgeschlossen →
  bindend"*) · ADR-Vorlage `templates/docs/plan/adr/NNNN-titel.template.md` §Geschichte
- `pfad`: `docs/plan/adr/0024-derivatives-register-gehoert-der-rolle-seines-originals.md:3` und
  `:192-196`
- `befund`: Die Datei trägt `**Status:** Accepted` und eine Geschichte-Tabelle mit **einer**
  Zeile — `| 2026-08-29 | Accepted | Architect-Lauf; …` —, also weder eine `Proposed`-Phase noch
  ein Sign-off noch eine abgeschlossene Review-Runde als `Verweis`. Die Vorlage legt zwei Zeilen
  an (`| YYYY-MM-DD | Proposed | <Slice-Datei> |`, `| YYYY-MM-DD | Accepted | <PR-Link> |`). Die
  Begründung „Repo-Praxis, alle 24 ADRs" ist gegen den Bestand messbar falsch:
  `ls docs/plan/adr/0*.md | wc -l` → **24** Dateien,
  `grep -lE '^\| [0-9]{4}-[0-9]{2}-[0-9]{2} \| \*{0,2}Proposed' docs/plan/adr/0*.md | wc -l` →
  **19** mit `Proposed`-Zeile; die fünf ohne sind über `grep -LE` derselben Suche
  `ADR-0001`–`ADR-0004` (alle vom Bootstrap-Tag 2026-06-13) und `ADR-0024` selbst. Beide
  Zahlen wandern mit dem Bestand ([`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  Setzung 2); tragend ist, dass unter den **19** ADRs seit 2026-06-13 keine einzige ohne
  `Proposed`-Zeile steht. Die unmittelbare Folge: §3.4 sperrt die Datei ab diesem Status, und die
  drei Befunde HIGH-2, MEDIUM-1 und MEDIUM-4 dieses Reports lassen sich nicht mehr in sie
  einarbeiten, sondern nur noch durch eine Folge-ADR mit `Supersedes` — die Konsistenzprüfung,
  die `modul-08-agentenrollen.md:144` dem Reviewer zuweist, kommt zu spät, weil der Status vor
  ihr gesetzt wurde.
- `verifizierbar`: nein durch ein Gate (kein Modul aus `modules:` der `.d-check.yml` liest
  Status-Übergänge oder Commit-Folgen); ja durch die zwei Kommandos oben und durch die Präzedenz
  `git log --format='%h %s' -- docs/plan/adr/0023-verweis-beschluss-traegt-ueber-den-sprung.md`,
  deren Geschichte-Tabelle drei Zeilen führt (`Proposed` → *Überarbeitet, weiter Proposed* →
  `Accepted`, letztere als *„Entscheidung des Auftraggebers"*).

### HIGH-2 — `ADR-0024` Festlegung 1 schließt ihren eigenen Anwendungsfall aus

- `kategorie`: HIGH
- `quelle`: `ADR-0024` Festlegung 1 gegen `ADR-0024` §Entscheidung *Angewandt*
- `pfad`: `docs/plan/adr/0024-derivatives-register-gehoert-der-rolle-seines-originals.md:85-96`
  gegen `docs/plan/adr/README.md:33-74`
- `befund`: Festlegung 1 definiert *derivativ* über eine Ausschluss-Bedingung: *„Das Register
  trägt **keine eigene Aussage**, sondern Projektionen … Trägt ein Artefakt daneben eigene
  Setzungen, ist es kein derivatives Register, und diese ADR sagt über es nichts."* Der einzige
  angewandte Fall — `docs/plan/adr/README.md` — trägt eine `## Konventionen`-Sektion mit eigenen
  Setzungen, die keine Projektion eines ADR-Feldes sind, unter anderem *„Einen Zusatz trägt die
  Zelle nur, wo eine `Accepted`-ADR ihn anordnet"* und *„Der Zusatz nennt den Umfang der Revision
  und die revidierende ADR — sonst nichts."* Ihr Umfang:
  `awk '/^## Konventionen/{f=1} f' docs/plan/adr/README.md | wc -l` → **42** von
  `wc -l < docs/plan/adr/README.md` → **74** Zeilen, beide wandern
  ([`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  Setzung 2). Dasselbe gilt für das Beispiel, mit dem Festlegung 2 den offenen Fall belegt:
  `docs/plan/carveouts/README.md` trägt neben der Projektionstabelle eigene Setzungen (die
  Absätze über die Rückkehr des Verzeichnisses und über den Ort eines überführten Carveouts).
  Ein Lauf, der die Vorfrage aus Festlegung 1 stellt und den Ausschluss-Satz anwendet, kommt
  damit zum Ergebnis *„diese ADR sagt über es nichts"* — während §Konsequenzen zusagt, `slice-134`
  §6 bekomme seinen Ausgang *entfallen*.
- `verifizierbar`: nein durch ein Gate; ja durch die zwei Kommandos oben plus Lektüre von
  `docs/plan/adr/README.md:33-74`.

### HIGH-3 — Aufhebung und Rumpf-Entfernung liegen in einem Änderungssatz

- `kategorie`: HIGH
- `quelle`: [ADR-0014](../plan/adr/0014-aufgehobener-eintrag-kopf-statt-rumpf.md) Festlegung 2 (c)
  — *„Aufhebung und Entfernung sind **zwei Commits** — ein Diff, der nur löscht, ist prüfbar, ein
  vermischter macht Korrektur und Entfernung ununterscheidbar"* (aktive ADR,
  [`AGENTS.md`](../../AGENTS.md) §3.3 in der Sache)
- `pfad`: `harness/conventions.md:1059-1067` (die zwei entkernten Einträge) gegen `:1744-1911`
  (die zwei neuen)
- `befund`: Derselbe Änderungssatz fügt
  [`MR-031`](../../harness/conventions.md#mr-031--die-kommentar-regel-steht-in-der-adoptierten-baseline)
  und [`MR-032`](../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger)
  hinzu **und** löscht die Rümpfe von `MR-022` und `MR-023`
  (`git diff --numstat -- harness/conventions.md` → **178/88**, ein einziger Satz von
  Hunks). Genau diese Vermischung verbietet Festlegung 2 (c): am gemischten Diff ist nicht
  ablesbar, ob ein Satz des gelöschten Rumpfs vor der Löschung noch geändert wurde — die
  Eigenschaft, die die Append-only-Disziplin schützt. Die Präzedenz dieses Repos zeigt den
  geprüften Zuschnitt: `git show --stat 3f7908a` (*„Aufhebung: MR-021 hebt MR-018 auf, die
  Zeiger-Zeile steht im selben Commit"* — **+81/−0** in `harness/conventions.md`) und der
  Folge-Commit `fc79a67` (*„Entfernung: der Rumpf von MR-018 geht, dieser Commit loescht nur"*).
  Der Befund ist eine Eigenschaft des übergebenen Änderungssatzes; er fällt, sobald die Übergabe
  in dieser Reihenfolge zwei Commits sind.
- `verifizierbar`: nein durch ein Gate — `ADR-0014` §Fitness Function sagt selbst, ein Wächter
  über den Vollzug hätte *„ein Diff"* als Gegenbeispiel und *„kein Gate dieses Repos liest
  Diffs"*; ja durch `git show --stat` über die zwei Commits der Übergabe.

### MEDIUM-1 — `MR-032` Setzung 3 fällt beim eigenen Schreiben durch: `MR-000` bleibt ohne Marke

- `kategorie`: MEDIUM
- `quelle`: [`MR-032`](../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger)
  Setzung 3 (*„Der **ablösende** Eintrag setzt sie in derselben Änderung, in der er entsteht. Ein
  Nachfolger ohne Marke am Vorgänger ist unvollständig"*) und Setzung 4 (Fälligkeit)
- `pfad`: `harness/conventions.md:53-62` (`MR-000`, ohne Blockquote) gegen `:1908-1910`
  (`MR-032`s eigene Aufhebungs-Zeile)
- `befund`: `MR-032` hebt in seiner letzten Zeile die Blankett-Klausel von `MR-000` *„für diesen
  Punkt"* auf und nennt sie dabei wörtlich — *„keine inhaltlichen Adaptionen ggü.
  Baseline-Default"*. Das ist der Fall, den Setzung 4 als **fällig** definiert (*„wenn ein
  späterer Eintrag eine Aussage **namentlich** ablöst oder für überholt erklärt"*). `MR-000`
  trägt keine Marke: `awk '/^### MR-000/,/^### MR-001/' harness/conventions.md | grep -c '^> '`
  → **0**. Es ist auch nicht der einzige Fall — `grep -c 'Blankett-Klausel'
  harness/conventions.md` → **3** (`MR-020`, `MR-026`, `MR-032`), alle drei mit derselben
  Nicht-Anfassen-Formel *„`MR-000` bleibt unangetastet, seine übrigen Setzungen gelten fort"*,
  die `MR-032` an drei anderen Orten ausdrücklich auflöst. Beide Zahlen wandern
  ([`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  Setzung 2). Die „Löst auf"-Liste in `:1869-1883` nennt **drei** Einträge; gemessen ist die
  Klasse größer, und `MR-032` erzeugt ihren vierten Fall im selben Text. Der Ausfall trifft den
  Eintrag, den jeder Lauf zuerst liest: `MR-000` sagt unwidersprochen *„keine inhaltlichen
  Adaptionen"*, während `grep -c '^### MR-' harness/conventions.md` → **33** Einträge im Block
  stehen. Der Zensus in `:1884-1890` kann das nicht auffangen — er zählt nach eigener Aussage
  gesetzte Marken, nicht fehlende.
- `verifizierbar`: nein durch ein Gate (`MR-032` §Kein Wächter sagt das selbst); ja durch die
  drei Kommandos oben.

### MEDIUM-2 — Die `HISTORIE`→`ÜBERHOLT`-Umschrift an zwei Einträgen ist von keiner Setzung gedeckt

- `kategorie`: MEDIUM
- `quelle`: [`MR-032`](../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger)
  Setzung 1 und Setzung 2 · Baseline `v5.12.0` `grundlagen-harness-dateien.md`
  §harness/conventions.md als Konventionsspeicher (*„Einträge werden nie überschrieben"*) ·
  Eintrags-Vorlage `templates/harness/conventions/MR-NNN-titel.template.md` (*„Wer mehrere Regeln
  ersetzen will, schreibt mehrere Einträge"*)
- `pfad`: `harness/conventions.md:127` und `:217` gegen `:1846-1863`
- `befund`: An `MR-004` und `MR-006` wurde eine **bestehende** Blockquote-Zeile ersetzt
  (`> **HISTORIE — der Cache-Teil ist seit slice-011 überholt → …**` →
  `> **ÜBERHOLT: der Cache-Teil, seit slice-011 → …**`). Setzung 1 beschreibt ausschließlich das
  **Setzen** einer Zeile und schließt daneben jede Textänderung aus: *„Sonst wird am Eintrag
  nichts geändert: der Rumpf bleibt wörtlich, **kein Satz wird nachgezogen**, keine Adresse
  getauscht."* Setzung 2 begründet die Vereinbarkeit mit der Baseline mit *„Die Marke ersetzt
  keine Aussage des Eintrags"* — für diese zwei Orte trifft das nicht zu, dort wurde eine
  vorhandene Aussage über den Zustand durch eine andere ersetzt. Weder die „Löst auf"-Liste noch
  eine Setzung nennt `MR-004`/`MR-006`. Die Umschrift ist zugleich die Voraussetzung des tragenden
  Arguments in `:1884-1890`: `grep -c '^> \*\*ÜBERHOLT: ' harness/conventions.md` → **5** und
  `grep -c 'HISTORIE — ' harness/conventions.md` → **0** gelten nur, weil sie stattgefunden hat —
  ohne sie stünde der Zensus auf 3 zu 2, also genau auf der „zwei Beschriftungen"-Lage, gegen die
  der Eintrag argumentiert (beide Zahlen wandern,
  [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  Setzung 2). Dazu kommt das Pflichtfeld: `MR-032` nennt unter `Ersetzt-Baseline-Regel` **eine**
  Regel (*„Der Zustand ist die Verzeichnis-Position, kein Status-Feld"*); die zweite von der
  ausgeführten Form berührte Regel — *„Einträge werden nie überschrieben"* aus derselben
  Baseline-Sektion, in `MR-029` §*Warum ein neuer Eintrag* verbatim zitiert — steht dort nicht,
  und die Vorlage verlangt für zwei Regeln zwei Einträge.
- `verifizierbar`: nein durch ein Gate; ja durch `git diff -- harness/conventions.md` (die zwei
  Ersetzungs-Hunks bei `:127` und `:217`) und die zwei `grep -c` oben.

### MEDIUM-3 — `MR-031`s Deckungs-Aussage lässt den zweiten benannten Träger der Baseline-Regel aus

- `kategorie`: MEDIUM
- `quelle`: [`MR-031`](../../harness/conventions.md#mr-031--die-kommentar-regel-steht-in-der-adoptierten-baseline)
  §*Was über die Vorlage hinaus stehen bleibt* · Baseline `v5.12.0`
  `grundlagen-harness-dateien.md` §Was ein Kommentar trägt, Feedback-Hälfte ·
  [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
- `pfad`: `harness/conventions.md:1801-1809` und `AGENTS.md:186-188`
- `befund`: `MR-031` zählt drei Stücke auf, die über die Vorlage hinausstehen (Geltungsbereich,
  Cutoff, Wächter-Aussage), und stützt die Wächter-Hälfte auf das Zitat *„gebaut ist der Sensor
  dafür nicht"*. In der Baseline gehört dieser Satz zur Klasse **Ersetzungs-Trümmer**; für
  Zustandsfelder nennt dieselbe Sektion einen anderen und **zweiten** Träger: *„Für Zustandsfelder
  ebenso: „ist das eine Chronik?" ist ein Urteil — Träger sind das Briefing (§3.7) und der
  HIGH-Eintrag *Zustandsfeld trägt Chronik* im Reviewer-Skill."* Dieser zweite Träger existiert
  im Repo nicht: `grep -c 'Zustandsfeld' .harness/skills/reviewer.md` → **0** und
  `grep -c 'Kommentar trägt keine' .harness/skills/reviewer.md` → **0**, gegen
  `grep -c 'Zustandsfeld trägt Chronik' .harness/baseline/v5.12.0/templates/.harness/skills/reviewer.template.md`
  → **1**. Damit trägt §3.7 die Regel im Repo allein; die Deckungs-Aussage *„Eine Ergänzung ohne
  Einschränkung ist keine Adaption"* ist an dieser Stelle nicht gemessen, sondern übersprungen.
  Konkretes Versagen: der laufende Review — der einzige benannte Träger für die neue
  Zustandsfeld-Hälfte — hat für sie keinen Anker in seinem Skill, und der Skill deklariert seine
  HIGH-Liste als abschließend („eines der folgenden").
- `verifizierbar`: nein durch ein Gate (`make comment-claims` hat keine Markdown-Datei im
  Prüfbereich, `comment-claims: 46 Datei(en) geprueft, 0 Befund(e)`); ja durch die drei `grep -c`
  oben. Der Zuschnitt für den Skill selbst liegt bei `slice-083` §2, der ihn namentlich führt —
  der Befund richtet sich gegen die **Vollständigkeit der Deckungs-Aussage** in `MR-031`, nicht
  gegen den Skill.

### MEDIUM-4 — Zwei allgemeine Sätze aus dem `MR-023`-Rumpf sind weder umgezogen noch als ersatzlos verzeichnet

- `kategorie`: MEDIUM
- `quelle`: [ADR-0014](../plan/adr/0014-aufgehobener-eintrag-kopf-statt-rumpf.md) Festlegung 2 (b)
  — *„jede bindende Aussage des Rumpfs steht an einem bindenden Ort oder ist im aufhebenden
  Eintrag als *ersatzlos* mit Grund verzeichnet"*
- `pfad`: `harness/conventions.md:1064-1067` (der entkernte `MR-023`) gegen `:1744-1826`
  (`MR-031`)
- `befund`: `MR-031` begründet die vollständige Aufhebung von `MR-023` mit *„Sein Gegenstand war
  die Teil-Aufhebung von `MR-022`; mit dessen vollständiger Aufhebung hat er keinen."* Der Rumpf
  trug daneben mindestens einen Satz mit eigener, allgemeiner Bindung: *„Eine Aussage über die
  Baseline nennt darum den Tag, gegen den sie gemessen ist"* — die Regel, die `docs/reviews/2026-08-22-slice-088-review.md`
  als *„Muster aus `MR-023`"* zitiert. `MR-031` verzeichnet ihn weder als *ersatzlos mit Grund*
  noch nennt es einen bindenden Zielort. [ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md)
  Festlegung 2 fängt ihn nicht auf: sie regelt die Beleg-Form in Artefakten, *die unveränderlich
  werden*, und stellt `AGENTS.md` und `harness/conventions.md` ausdrücklich auf die änderbare
  Seite (*„Der lokale Pfad bleibt erlaubt — in änderbaren Artefakten"*). Dasselbe gilt für den
  zweiten allgemeinen Satz jenes Rumpfs, die Handhabung eines Sachfehlers (*„entweder kann der
  Punkt ersatzlos entfallen … oder an seiner Stelle muss etwas Richtiges binden — dann trägt es
  der aufhebende Eintrag. In beiden Fällen bleibt der Rumpf unangetastet"*). Failure-Szenario:
  die nächste Baseline-Aussage in einem lebenden Artefakt wird ohne Mess-Tag geschrieben — genau
  der Defekt, gegen den `MR-023` entstand — und keine geltende Quelle verbietet es mehr.
- `verifizierbar`: nein durch ein Gate (`ADR-0014` §Konsequenzen: *„die tragende Bedingung 2 (b)
  hat keinen Wächter"*); ja durch `git show HEAD:harness/conventions.md` gegen den Arbeitsbaum.

### LOW-1 — Der Zensus-Vergleich in `MR-032` behauptet mehr, als er zeigt

- `kategorie`: LOW
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 (eine Zusage nennt, was sie bräche)
- `pfad`: `harness/conventions.md:1884-1890`
- `befund`: Der Absatz stellt `grep -c '^> \*\*ÜBERHOLT: '` → **5** gegen
  `grep -c '^- \*\*Aufgehoben durch '` → **3** und schließt: *„mit zwei Beschriftungen gäbe es
  keine der beiden Zahlen."* Die zweite Zahl hängt nicht an der Beschriftung der Kopf-Marke — sie
  zählt das **andere** Instrument und bliebe bei jeder Wahl unverändert. Tragend ist nur die
  erste Hälfte des Satzes.
- `verifizierbar`: ja, durch die zwei Kommandos im Absatz selbst.

### LOW-2 — Die ausgeführte Marke hat zwei Gestalten, und zwei tragen Chronik im Zustandsfeld

- `kategorie`: LOW
- `quelle`: [`MR-032`](../../harness/conventions.md#mr-032--ein-überholter-eintrag-trägt-eine-kopf-marke-auf-seinen-nachfolger)
  Setzung 1 · [`AGENTS.md`](../../AGENTS.md) §3.7 Zustandsfeld-Hälfte
- `pfad`: `harness/conventions.md:66`, `:127`, `:217`, `:881`, `:979`
- `befund`: Setzung 1 gibt die Gestalt als *eine* Blockquote-Zeile vor und erlaubt daneben
  *„weitere Blockquote-Zeilen"*. Drei der fünf Marken (`:66`, `:881`, `:979`) hängen stattdessen
  einen zweiten Satz **auf derselben Zeile** außerhalb der Fettung an (*„Die übrigen Setzungen
  dieses Eintrags gelten fort."*), zwei (`:127`, `:217`) führen weitere Blockquote-Zeilen. Und die
  `<Reichweite>` der zwei umgeschriebenen Marken trägt mit *„seit slice-011"* ein Chronik-Element,
  während die im selben Änderungssatz eingeführte Zustandsfeld-Hälfte von §3.7 für ein
  Zustandsfeld *„den Zustand und den Beleg als auflösbaren Anker, nicht die Chronik"* verlangt.
- `verifizierbar`: nein durch ein Gate; ja durch `grep -n '^> \*\*ÜBERHOLT: ' harness/conventions.md`.

### LOW-3 — `MR-031` vertagt eine Frage, die die Eintrags-Vorlage unbedingt beantwortet

- `kategorie`: LOW
- `quelle`: Eintrags-Vorlage `templates/harness/conventions/MR-NNN-titel.template.md` — *„Ein
  Eintrag, der keine benannte Regel ersetzt, ist ein **Fork**, keine Adaption."*
- `pfad`: `harness/conventions.md:1773-1776`
- `befund`: `MR-031` trägt `Ersetzt-Baseline-Regel: keine.` und stellt die Einordnung zurück
  (*„ob ein Rückbau-Eintrag ohne benannte Regel unter das *Fork*-Kriterium fällt … entscheidet
  jener Slice"*). Die Vorlage formuliert den Satz ohne Bedingung. Das Feld ist damit ausgefüllt
  und nicht stillschweigend übergangen — die **Folge** aus seinem Wert bleibt offen.
- `verifizierbar`: ja, durch `grep -c 'Ersetzt-Baseline-Regel' harness/conventions.md` → **2**
  gegen `grep -c '^### MR-' harness/conventions.md` → **33** (beide wandern,
  [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
  Setzung 2) — die Achse selbst führt `slice-083` §2.

### INFO-1 — Zwei Pflichtfelder fehlen in beiden neuen Einträgen, blockweit ist das kein neuer Zustand

- `kategorie`: INFO
- `quelle`: Baseline `v5.12.0` `grundlagen-harness-dateien.md` §Pflichtgliederung (*„Pflichtfelder
  eines Eintrags: Datum, Geltungsbereich, `Ersetzt-Baseline-Regel`, Adaption, Begründung,
  Auflösungs-Trigger"*)
- `pfad`: `harness/conventions.md:1744-1826` und `:1827-1911`
- `befund`: `MR-031` und `MR-032` führen kein benanntes `Adaption:`- und kein `Begründung:`-Feld;
  ihre Setzungen bzw. die *Warum*-Absätze stehen an deren Stelle. Gemessen ist das der Zustand des
  Blocks, nicht seine Verschlechterung: `grep -c '^### MR-' harness/conventions.md` → **33**,
  `grep -c '^- \*\*Adaption' harness/conventions.md` → **22**,
  `grep -c '^- \*\*Begründung' harness/conventions.md` → **15** (alle drei wandern).
- `verifizierbar`: ja durch die drei Kommandos; die Achse trägt `slice-083` §2, das die
  überlebenden *wie* die unter dem neuen Stand geschriebenen Einträge einschließt.

### INFO-2 — Eine zitierte Messung eines Planner-Artefakts ist durch diesen Änderungssatz erloschen

- `kategorie`: INFO
- `quelle`: Kopplung (Planner-Artefakt, nicht Gegenstand dieses Reviews)
- `pfad`: `docs/plan/planning/open/slice-082-adaptions-durchgang.md:105-112`
- `befund`: Die DoD-Zeile führt `grep -c 'HISTORIE — ' harness/conventions.md` → *„am 2026-08-29
  **2** Einträge mit einer Kopf-Marke"* und leitet daraus ab, *„der Bestand gibt **zwei**
  Antworten statt einer"*. Nach diesem Änderungssatz liefert dasselbe Kommando **0**. Die Zahl ist
  dort als wandernd gekennzeichnet, also kein Regelbruch — die **Aussage** daneben trifft nicht
  mehr zu. Zuständig ist der Planner.
- `verifizierbar`: ja, `grep -c 'HISTORIE — ' harness/conventions.md`.

### INFO-3 — `slice-082` §2 (3) weist die Form-Entscheidung dem Durchgang zu, vollzogen hat sie dieser Lauf

- `kategorie`: INFO
- `quelle`: `slice-082` §2 (3) (*„Der Durchgang entscheidet die Form **einmal** und wendet sie auf
  den Bestand an"*) gegen `MR-032` §Wirksamkeits-Anlass (*„entschieden ist sie hier, weil dieser
  Block dem Architect gehört"*)
- `pfad`: `harness/conventions.md:1830-1834`
- `befund`: Der Eintrag nennt die Zuweisung und die Abweichung von ihr ausdrücklich; die
  Begründung über [`AGENTS.md`](../../AGENTS.md) §3.8 trägt das **Schreiben** durch den Architect,
  nicht den **Zeitpunkt** vor dem Slice. Der Posten aus `slice-082` §2 (3) ist damit vorweggenommen,
  einschließlich der Anwendung auf den Bestand. Kein Widerspruch zu einer Hard Rule; der Zuschnitt
  gehört dem Planner.
- `verifizierbar`: nein.

---

## Negativbefunde (geprüft, ohne Befund)

- **`MR-032` §Löst auf, Reichweite der drei genannten Sätze.** Alle drei Zitate stehen wörtlich in
  ihren Quellen und sind korrekt zugeordnet: `MR-021` über `MR-019` (`harness/conventions.md:1011-1013`,
  *„jener Eintrag wird dafür nicht angefasst, seine Zahl ist überholt"*), `MR-029` über `MR-001`
  (`:1570`), `MR-030` über `MR-021` (`:1697`). Die fortgeltende Hälfte ist an allen drei Orten
  **eingehalten**: `git diff -- harness/conventions.md` fügt bei `:66`, `:881`, `:979` je genau
  eine Zeile ein und ändert am Rumpf nichts. Insbesondere ist die zweite Hälfte des
  `MR-030`-Satzes — *„und sein Verweis wird nicht nachgezogen"* — respektiert: der Link auf
  `v3.5.2` in `MR-021` steht unverändert, `make docs-check` meldet ihn weiter als
  `harness/conventions.md:1015 … target-missing`.
- **Die Marke bricht keinen Anker.** Alle fünf Marken zeigen auf Index-Anker derselben Datei,
  und `make docs-check` meldet über `harness/conventions.md` genau den einen bekannten
  `target-missing`, keinen `anchor-missing`.
- **`ADR-0024` ohne `Supersedes` gegen `ADR-0015` — korrekt.** `ADR-0015` verengt ihren
  Geltungsbereich selbst (`docs/plan/adr/0015-rollen-eigentum-an-norm-artefakten.md:115-118`:
  *„Über die übrigen Norm-Artefakte trifft diese ADR **keine** Aussage … wo keine sie benennt,
  bleibt das eine offene Frage, die diese Verengung ausdrücklich stehen lässt"*). `ADR-0024`
  korrigiert und schärft daran nichts, sie füllt die offen gelassene Frage — `Supersedes` wäre
  hier falsch, `Bezug` ist die richtige Form.
- **`ADR-0024` §Kontext, alle Messungen nachgefahren.**
  `grep -rlEi 'ADR-Index|Carveout-Index' .harness/baseline/v5.12.0/regelwerk/ | wc -l` → **2**;
  `grep -rniE '(ADR|Carveout)-Index' … | grep -ciE 'architect|planner|…'` → **0**;
  `grep -c 'Planner. identifiziert die fälligen Carveouts' … modul-07-carveouts.md` → **1**;
  `git log --format='%s' -- docs/plan/adr/README.md | wc -l` → **65**, davon **26** mit
  `Rolle Architect` und **39** ohne Rollen-Präfix. Alle vier Zitate (Modul 7, Modul 8,
  §Jedes Artefakt hat einen Konsumenten, `ADR-0015`) sind verbatim.
- **`ADR-0024` §Fitness Function behauptet kein Gate.** Die Aussage *„kein Modul der heutigen
  `.d-check.yml` tut das"* ist nachgefahren: `grep -m1 '^modules:' .d-check.yml` →
  `modules: [links, anchors, ids, matrix, codepaths, spans]`. Konform mit
  [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6).
- **`ADR-0024` Form gegen die Vorlage:** `Status`, `Datum`, `Autor`, `Bezug`, `Schärft: —`
  (Prozess-ADR, von der Vorlage vorgesehen), fünf Alternativen mit Pro/Contra (gefordert sind
  drei), Konsequenzen mit Positiv/Negativ/Folgepflichten, Fitness Function, Re-Evaluierungs-Trigger
  — alle Pflichtsektionen vorhanden. Einzige Abweichung ist die Geschichte-Tabelle, siehe HIGH-1.
- **`AGENTS.md` §3.7, Deckung gegen die Vorlage.** Die drei `grep -c`-Belege in `MR-031` und in
  §3.7 reproduzieren (**1** und **1**), und die Kontroll-Schleife aus `MR-031` liefert
  `Zustandsfeld AGENTS.md=5 Vorlage=2`, `seit welle- AGENTS.md=1 Vorlage=1`,
  `grundlagen-harness-dateien AGENTS.md=2 Vorlage=1` — keine Zeile links auf **0**, wie der
  Eintrag es als tragend benennt.
- **`AGENTS.md` §3.7, der Satz *„Eine Begründung des Zustands … ist Zustand, keine Chronik"* ist
  keine Lockerung — REFUTED mit Beleg.** Er steht wörtlich in der Baseline:
  `grundlagen-harness-dateien.md` §Was ein Kommentar trägt, *Dieselbe Regel für Zustandsfelder*:
  *„eine Begründung des Zustands (*gestrichen — tritt nicht mehr auf, weil …*) ist Zustand, nicht
  Chronik."* Ebenso die Trennung Drift-Log ↔ Closure-Log. Die Ergänzung *ADR-Index* in der
  Aufzählung der Orte ist eine repo-lokale Instanz der Baseline-Klasse *Register-Zeile*, keine
  Erweiterung des Geltungsbereichs.
- **§3.7 behauptet keinen Wächter.** *„keine Markdown-Datei liegt in seinem Prüfbereich, also
  auch kein Zustandsfeld"* ist gegen den Sensor gehalten: `make comment-claims` →
  `46 Datei(en) geprueft, 0 Befund(e)`, und der Prüfbereich sind laut
  [`AGENTS.md`](../../AGENTS.md) §4 vier Pfad-Muster ohne Markdown. Die Cutoff-Hälfte sagt
  ausdrücklich, warum **keine** Zahl danebensteht. Konform mit §3.6.
- **`AGENTS.md` §3.8, der neue Zeiger auf `ADR-0024`** verdoppelt keinen ADR-Text: er nennt die
  Regel in einem Satz und verweist auf die Entscheidung, wie es `ADR-0024` §Konsequenzen
  Folgepflicht 1 verlangt, und legt keine neue Hard-Rule-Nummer an
  ([`MR-026`](../../harness/conventions.md#mr-026--die-hard-rule-nummer-ist-eine-adresse-keine-baseline-entsprechung)
  Setzung 2 bleibt unberührt).
- **`docs/plan/adr/README.md`**: die neue Zeile projiziert Titel (`# `-Überschrift ohne Präfix,
  wörtlich), Status (`Accepted`) und Bezug aus `ADR-0024` und hält die Konventionen des Index —
  kein Zusatz in der Status-Zelle, `Bezug` voll verlinkt.
- **`MR-031` §Auflösungs-Trigger / `MR-032` §Auflösungs-Trigger**: beide vorhanden und
  beobachtbar formuliert (Baseline-Stand ändert §3.7 · Migration in die Verzeichnis-Form).
- **Die zitierten Plan-Stellen existieren.** `slice-134:250` und `:253` tragen beide von
  `ADR-0024` zitierten Sätze verbatim; `slice-082` §3 weist den Norm-Text ausdrücklich einem
  Architect-Lauf zu; `slice-083` §2 trägt das *Fork*-Kriterium.
- **Kein Gegenstands-Befund im Doc-Gate.** Die vier `docs-check`-Befunde dieses Laufs stammen
  sämtlich nicht aus dem Gegenstand (einer aus `MR-030`s bewusstem Preis, drei aus einer
  Planner-Datei, die während des Laufs erschien).
- **§3.9 eingehalten:** dieser Lauf hat ausschließlich `make`-Ziele und `git`/`grep`/`awk`
  benutzt, keine Host-Toolchain.

---

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 3 |
| MEDIUM | 4 |
| LOW | 3 |
| INFO | 3 |

`grep -c '^### HIGH-' docs/reviews/2026-08-29-adr-0024-mr-031-032-review.md` und dieselbe Suche
mit `MEDIUM`/`LOW`/`INFO` geben die vier Zahlen aus.

## Verdikt

**NICHT KONFORM.** Drei HIGH blockieren, vier MEDIUM sind vor der Übergabe zu klären.

**Zu der gestellten Frage — kann `ADR-0024` auf `Accepted` bleiben?** Nein. Zwei Gründe, und der
zweite ist der schwerere:

1. Der Status wurde ohne den Übergang gesetzt, den die Baseline für ihn vorsieht (HIGH-1), und
   gegen eine Repo-Praxis, die messbar anders ist als die Begründung behauptet: **19** der **23**
   vorbestehenden ADRs tragen eine `Proposed`-Zeile, die vier ohne sie stammen alle vom
   Bootstrap-Tag.
2. Die Entscheidung trägt einen Sachbefund, der ihren einzigen Anwendungsfall aushebelt (HIGH-2).
   Bliebe sie auf `Accepted`, sperrte [`AGENTS.md`](../../AGENTS.md) §3.4 die Korrektur, und der
   Preis stiege von einer Zeile auf eine Folge-ADR — genau die Kosten-Erwägung, mit der
   [ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md) den Accept-Übergang als Träger
   begründet (*„nach der Annahme ist derselbe Satz durch §3.4 unerreichbar"*).

**Was nicht Gegenstand dieses Verdikts ist:** die DoD-Abhakung von `slice-082`/`slice-083`
(Verifier, Modul 11) und die Planner-Artefakte im selben Baum.

**Rollen-Konflikt-Pfad (Modul 8):** Widerspricht die schreibende Rolle einem der drei HIGH, wird
er nicht herabgestuft; die Sequenz läuft über Übergabe-Artefakte — Befund, Gegenmessung mit
Kommando, Entscheid. HIGH-3 fällt ohne Diskussion, sobald die Übergabe zwei Commits in der
Reihenfolge *Aufhebung* → *Entfernung* sind (Präzedenz `3f7908a` / `fc79a67`).
