# Review-Report: slice-093 (Code) — 2026-08-23

**Review-Art:** **Code**, erste Runde — geprüft wird der Diff gegen den Slice-Plan, gegen die
referenzierte Accepted-ADR (nur gelesen, nicht angefasst) und gegen die Hard Rules
(Modul 10 §Drei Review-Arten). Kein vorheriger Review-Lauf zu diesem Slice.

**Gegenstand:** `569eec7` — **ein** Commit, **drei** Dateien
(`git show 569eec7 --stat` → `harness/tools/mutate.sh` +22, `test/mutate-driver.bats` +28,
`test/mutations/152-cpp-lint-schichtfilter.sh` neu, Modus `755`, +17 — `3 files changed,
67 insertions(+)`). Baum sauber vor und nach jeder eigenen Probe
(`git status --short` → leer).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0
**Modell:** claude-sonnet-5 · **Datum:** 2026-08-23

**Eingangs-Kontext:**

- **Diff:** `569eec7`, `git show 569eec7` im Volltext
- **Slice-Plan:**
  [slice-093](../plan/planning/done/slice-093-mutations-treiber-erreicht-full-smoke.md)
  (Stand `in-progress/`, vollständig gelesen)
- **ADR, gelesen als Constraint, nicht angefasst:**
  [ADR-0022](../plan/adr/0022-erfassungsschicht-traeger-aus-dem-produkt-binaer.md) (Accepted) —
  im Diff nicht berührt (`git diff --stat 268fe2f..569eec7 -- docs/plan/adr/` leer)
- **`LH-*`:** [LH-QA-01](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §3.2, §3.4, §3.6, §3.7
- **Konventionen:** [`harness/conventions.md`](../../harness/conventions.md) MR-001, MR-016,
  MR-025 (Geltungsbereich + Setzung 1/2)
- **Vorherige Findings am gleichen Modul:** keine — erste Review-Runde zu `harness/tools/mutate.sh`
  seit den in den Kopf-Kommentaren zitierten Vorfällen (`Review-Befund NR-1/NR-2` u.a.), die zu
  diesem Diff nicht gehören
- **Regelwerk `v3.5.2`:** Index gelesen; `modul-10-review-harness.md` on-demand

---

## Selbst gefahren (nichts aus Commit-Message oder Übergabe übernommen)

| Kommando / Probe | Ergebnis | Behauptung | Urteil |
|---|---|---|---|
| `grep -rn 'HeaderFilterRegex' internal/ harness/ test/ Makefile` (Stand `569eec7`) | **4** Treffer: `cpp.go`, zwei in `full-smoke.sh`, **einer in `test/mutations/152-…sh`** | Commit: „drei Treffer, keiner in einem Test" | **an der Quelle geprüft, präzisiert** — gegen den Elternstand `268fe2f` (vor diesem Commit) liefert dasselbe Kommando **genau drei** Treffer, keinen in `test/`; der vierte Treffer heute ist die erklärende Prosa des NEUEN Falls selbst, kein konkurrierender Test. Die Design-Zeit-Messung stimmt |
| `grep -c 'full-smoke: FEHLER' harness/tools/full-smoke.sh` | **73** | DoD-Bestandsaufnahme | **stimmt** |
| Quellcode-Rekonstruktion aller 73 Stellen | jede steht unmittelbar vor `exit 1`, alle auf `>&2` | „Fehlschlag-Stellen, dazu ebenso viele exit 1" | **stimmt** (`grep -c 'exit 1'` → 90, `grep -c 'echo "full-smoke: FEHLER'` → 73 — die Differenz sind `exit 1` an anderen Prüfungen ohne dieses Präfix) |
| `grep -ni 'fehler' harness/tools/full-smoke.sh` gegen die 3 gemessenen case-insensitive Treffer eines grünen Laufs | genau **eine** Text-Konstruktion (Zeilen 444/458/459: `absichtlicher Schicht-Fehler`, zweimal reflektiert aus dem Compiler-Output plus einmal in der Ankündigungszeile `full-smoke: C++-Schicht-Zaehne belegt (Fehler in der Domain-Schicht …)`) erklärt lückenlos, woher `grep -cEi 'full-smoke.*fehler'` → 3 kommt, während `grep -cE 'full-smoke: FEHLER'` (case-sensitiv) → 0 bleibt, weil `Fehler` nirgends als `FEHLER` groß geschrieben vorkommt | DoD (2) / neuer Kommentar in `failure_form` | **stimmt, quellcode-seitig hergeleitet** — der gemessene grüne Lauf selbst wurde nicht neu gefahren (siehe Vorbehalt unten) |
| Isolierte Kopie: `sed -i "s@HeaderFilterRegex: '(^|/)src/'@HeaderFilterRegex: '^src/'@" internal/gen/cpp.go` gegen eine Wegwerf-Kopie außerhalb des Repos | genau **eine** Zeile geändert (`574c574`), exakt die im Fall benannte | Fall 152: die Mutation trifft `HeaderFilterRegex` gezielt | **stimmt**, mechanisch reproduziert |
| Lesung `internal/gen/cpp_test.go` | prüft nur, dass `.clang-tidy` **emittiert** wird, nicht ihren Inhalt (`grep -n 'HeaderFilterRegex\|clang-tidy' cpp_test.go` → zwei Treffer, beide Dateilisten, keiner Content) | „keiner in einem Test" | **stimmt** |
| Lesung `harness/tools/smoke.sh` | bootstrappt ausschließlich `--lang go` (`grep -n 'lang go\|lang cpp' smoke.sh` → nur `--lang go`) | Fallkopf: „`make smoke` bootstrappt nur `--lang go`" | **stimmt** — die C++-Schicht-Datei wird unter `make smoke` nie erzeugt, der Fall kann dort gar nicht greifen |
| Quellcode-Trace `harness/tools/full-smoke.sh:439-484` | Build-Zähne-Check (Zeilen 439-459) mutiert dieselbe Datei anders (Syntaxfehler) und nimmt zurück, bevor der Lint-Zähne-Check (461-484) beginnt; unter der `152`-Mutation bleibt `lint-apps-cpphex` grün, weil der verankerte Header-Filter den absoluten Container-Pfad nie trifft → `cpplint_rc -eq 0` → genau die im Fall zitierte FEHLER-Zeile feuert | Fall 152 trifft genau diesen Wächter, nicht früher/später | **stimmt, statisch hergeleitet** — nicht per echtem `full-smoke`-Lauf nachgefahren (Vorbehalt unten) |
| `failure_form full-smoke` Quellzeile 254 gegen `run_case`-Matching (Zeile 394: `grep -E -- "$form" "$out" \| grep -qF -- "$expect"`) | die einzige Zeile im Skript, die BEIDE Substrings trägt (`full-smoke: FEHLER` UND `die Schicht-Header werden nicht gelintet`), ist Zeile 478 | Muster + Erwartung greifen gemeinsam nur am richtigen Ort | **stimmt**, mechanisch nachgeprüft |
| Nachbau von DoD-1-Gegenprobe (Arm entfernt) am `main()`-Quelltext | der Grün-Vorlauf (`for m in test $modes`) ruft `failure_form "$m"` **vor** jedem `make $m`; ohne den `full-smoke)`-Arm liefert das den Ablehnungs-Arm (`*) return 1`) und damit `mutate: ABBRUCH — unbekannter '# verify: full-smoke' …` **vor dem ersten Fall** | DoD-1-Gegenprobe | **stimmt, statisch hergeleitet** — nicht per echtem Lauf nachgefahren (`make mutate` kostet laut Plan >900 s, Anweisung: nicht fahren) |
| Eigene Mutations-Probe an `narrow_sensor`: Standard-Zweig global auf `full-smoke` gesetzt | die vier BESTEHENDEN `narrow_sensor`-Fälle brechen dabei **mit**, weil einer von ihnen (`emittiert: eingelegter SYMLINK` → `test-bats`) genau diesen Zweig prüft | — | **Widerspruch zur ersten Simulation** — führt zur zweiten, präziseren Probe |
| Zweite, engere Probe: nur die literale Eingabe `full-smoke` wird vor den bestehenden Zweigen abgefangen (`case "$expect" in full-smoke) printf 'full-smoke'; return;; esac`) | die vier bestehenden `narrow_sensor`-Fälle bleiben **alle grün** (`test-go`, `test-bats`, `test`, `test`); der NEUE Wertebereichs-Test allein wird rot (`ausserhalb des Wertebereichs: [full-smoke] -> [full-smoke]`) | Commit: „die vier bestehenden Fälle blieben grün — der neue trägt die Eigenschaft allein" | **stimmt** — unabhängig reproduziert, und erklärt zugleich, warum die Randeingaben `full-smoke`/`smoke: FEHLER` im neuen Test stehen: sie sind genau die Klasse Eingabe, die eine solche Fehlkonstruktion aufdeckt |
| `make gates` (frischer Lauf) | Exit 0; `d-check: 359 Datei(en) geprüft, 0 Befund(e)`; bats `1..144`, `144` `ok`, `0` `not ok` (inkl. `ok 116 driver: narrow_sensor liefert NUR Werte aus {test, test-go, test-bats}`); `comment-claims: 40 Datei(en) geprüft, 0 Befund(e)`; span-check grün | Commit: „make gates Exit 0 (…, bats 144 ok, comment-claims 40/0, …)" | **stimmt**, selbst gefahren |
| `make shell-lint` | Exit 0, keine shellcheck-Befunde über `test/mutations/*.sh` (schließt die neue Datei ein) | Commit: „make shell-lint Exit 0" | **stimmt**, selbst gefahren |
| `git status --short` nach beiden Läufen | leer | Isolation/keine Seiteneffekte | **stimmt** |

**Nicht selbst gefahren, aus der Übergabe übernommen (Anweisung: zu teuer für diese Runde):**
`make mutate` (145 ok, 0 Befund(e), 1091.28 s) und `make full-smoke` (90.00 s / 136.16 s
unmutiert, 53.32 s mutiert, Exit 2). Für beide gilt: die Quellcode-Herleitung oben stützt die
behaupteten Ergebnisse durchgehend, ohne Bruchstelle gefunden zu haben — aber es ist eine
Herleitung, kein Nachlauf. Insbesondere die Docker-Cache-Streuung (90 s vs. 136 s über denselben
Baum) ist nicht nachprüfbar, ohne genau den Cache-Zustand zu reproduzieren, den der Implementer
hatte.

---

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills.

### F-1 — Plan §6 letzter Risiko-Punkt behauptet weiterhin das Gegenteil der jetzt gemessenen Richtung

- `kategorie`: **LOW**
- `quelle`: Maintainability (Doku-Drift) — kein Hard-Rule- oder MR-Verstoß, da MR-025 `.md`-Dateien
  im `in-progress/`-Stand zwar bindet, aber keine Regel verlangt, dass eine widerlegte Prognose
  rückwirkend korrigiert wird, bevor der Slice schließt
- `pfad`:
  [slice-093 §6](../plan/planning/done/slice-093-mutations-treiber-erreicht-full-smoke.md),
  letzter Punkt „Der gemessene Preis ist eine Untergrenze"
- `befund`: Der Plan sagt dort ausdrücklich voraus: „Der Fall selbst mutiert eine Quelldatei und
  erzwingt damit den Neubau … sein Lauf ist teurer." Die jetzt gemessenen Zahlen zeigen das
  Gegenteil: der mutierte Lauf (53.32 s, Exit 2) ist **billiger** als beide unmutierten
  Referenzläufe (90.00 s / 136.16 s), weil er am getroffenen Wächter mitten im Skript abbricht,
  statt es bis zum Ende zu durchlaufen. Die Korrektur steht sauber im Commit-Text und im
  Code-Kommentar von `harness/tools/mutate.sh` — beide sagen die neue Richtung ausdrücklich und im
  Indikativ. Im **Plan-Dokument selbst**, das als lebendes Artefakt im `in-progress/`-Stand liegt,
  steht die widerlegte Prognose unverändert.
- `verifizierbar`: **nein** — kein Gate liest Plan-Prosa gegen Code-Kommentare ab.
  **Versagen:** wer den Plan nach Closure liest, ohne den Commit-Text danebenzulegen, hält den
  mutierten Lauf für den teureren Fall und schätzt den Aufschlag eines künftigen
  `full-smoke`-Falls in die falsche Richtung.

---

## Negativbefunde

- **geprüft, ohne Befund — Zuschnitt des Commits.** Ein Commit, drei Dateien, keine ADR, kein
  Makefile, kein `full-smoke.sh` im Diff (`git diff --stat 268fe2f..569eec7 -- docs/plan/adr/
  Makefile harness/tools/full-smoke.sh` → leer für alle drei) — genau der im Auftrag benannte
  Zuschnitt.
- **geprüft, ohne Befund — §3.2 (kein inline `shellcheck disable`).** Kein Treffer in den drei
  Dateien; `make shell-lint` selbst gefahren, Exit 0.
- **geprüft, ohne Befund — §3.7 (Kommentar beschreibt, was da ist).** Beide neuen Kommentarblöcke
  in `mutate.sh` sind durchgehend indikativ über den gemessenen Zustand, tragen keine
  Review-Befund-Kennung und keine Konjunktiv-Prosa über verworfene Alternativen
  (`git show 569eec7 | grep '^+' | grep -iE 'befund|review-|N-[0-9]|F-[0-9]'` → leer).
- **geprüft, ohne Befund — der gewählte Wächter trägt die behauptete Eigenschaft.** Die
  `HeaderFilterRegex`-Suche ergibt genau drei Treffer am Elternstand, keinen in einem Test; der
  einzige existierende Go-Test zu `.clang-tidy` (`cpp_test.go`) prüft Emission, nicht Inhalt;
  `make smoke` bootstrappt nachweislich nur `--lang go` und kann die C++-Schicht nie erreichen.
  Die Wahl von `full-smoke` als schmalste ausreichende Stufe ist damit an der Quelle bestätigt.
- **geprüft, ohne Befund — DoD-2-Muster (Fehlschlag-Form eng genug).** Die Quellcode-Herleitung
  erklärt lückenlos, warum ein case-insensitives Muster über einem grünen Lauf 3 statt 0 Treffer
  hätte (eine einzige, absichtlich gerötete Teststelle im Skript) und warum das case-sensitive
  Muster `full-smoke: FEHLER` sie nicht mitzieht (`Fehler` wird nirgends `FEHLER` geschrieben).
- **geprüft, ohne Befund — DoD-3 (`narrow_sensor` bleibt eng, der Wertebereichstest trägt selbst
  belegt).** Eine erste eigene Mutations-Probe widersprach zunächst der Commit-Behauptung („vier
  bestehende Fälle blieben grün"); eine präzisere, dazu passende Probe (Interzeption der Eingabe
  `full-smoke`) reproduzierte die Behauptung exakt — die vier bestehenden Fälle bleiben grün, der
  neue Wertebereichstest wird allein rot. Die verdächtigen Randeingaben (`full-smoke`,
  `smoke: FEHLER`) im neuen Test erklären sich damit als genau die Eingabeklasse, gegen die er
  gebaut ist.
- **geprüft, ohne Befund — der neue Fall gegen die Kopf-Konvention.** `# files:`/`# expect:`/
  `# verify:`-Kopf einzeilig und vollständig, Modus `755`, `set -euo pipefail`, ein einziger
  zielgerichteter `sed`, Nummerierung `152` lückenlos auf `151` folgend (145 Fälle insgesamt nach
  dem Commit). Format und Prosa-Stil decken sich mit den benachbarten Fällen (`08-…`, `10-…`).
- **geprüft, ohne Befund — Reversibilität.** Die Mutation selbst muss nicht selbstinvertierend
  sein — `mutate.sh`s `tar`-Backup/Restore-Mechanik in `run_case` übernimmt die Rückstellung,
  unabhängig vom Fall-Skript; das ist dieselbe Konstruktion wie bei allen bestehenden Fällen.
- **geprüft, ohne Befund — MR-025 (Geltungsbereich + Setzung 1/2).** Formal bindet MR-025 nur
  `.md`-Dateien; `harness/tools/mutate.sh` ist `.sh` und liegt außerhalb seines gemessenen
  Geltungsbereichs — eine Konventions-Verletzung liegt daher nicht vor, auch wenn eine Zahl ohne
  Kommando dastünde. Tatsächlich tragen aber beide neuen Zahlen-Absätze ihr Kommando im selben
  Absatz und kennzeichnen die streuenden Laufzeiten ausdrücklich als das, was sie sind — keine
  feste Erwartung, sondern eine Größenordnung unter einem bestimmten Cache-Zustand. Damit folgt
  der Kopf der Sache nach der Setzung 1/2-Disziplin, ohne formal an sie gebunden zu sein.
- **geprüft, ohne Befund — §3.6 (Zusage mit rot gesehenem Gegenbeispiel).** Beide in DoD 1
  verlangten Rot-Läufe sind im Commit-Text mit ihrer jeweiligen Meldung benannt, und die
  Meldungstexte sind wörtlich aus dem Quellcode ableitbar (`report_fail`-Format in `run_case`,
  Ablehnungs-Arm-Meldung in `main()`) — keine erfundene Ausgabe.
- **nicht abschließend geprüft — die beiden teuren Läufe selbst.** `make mutate` (1091.28 s) und
  `make full-smoke` in allen drei Varianten (90.00 s / 136.16 s / 53.32 s) sind gemäß Auftrag
  **nicht** nachgefahren. Die Quellcode-Herleitung stützt jede einzelne Behauptung, ersetzt aber
  keinen echten Lauf — das ist der tragende Vorbehalt dieses Reports.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 1 |
| INFO | 0 |

## Verdikt

**Frei — nicht merge-blockierend.** F-1 ist eine Doku-Drift in einem Plan-Risiko-Punkt, kein
Gate sieht sie, und sie beschreibt nichts, was der Code oder der Commit-Text falsch machen — im
Gegenteil, beide tragen die korrigierte Richtung bereits sauber im Indikativ. Der gewählte
Wächter (`HeaderFilterRegex` in `internal/gen/cpp.go`) ist an seiner Quelle als einziger
ungetesteter Fundort bestätigt, das Fehlschlag-Muster ist eng genug hergeleitet, und der neue
`narrow_sensor`-Wertebereichstest wurde unabhängig — nach einer ersten, zunächst widersprüchlichen
eigenen Probe — mit einer präziseren Mutation nachgestellt und trägt die behauptete Eigenschaft
allein. Kein `AGENTS.md`-§3.2/§3.4/§3.6/§3.7-Verstoß, keine ADR berührt, `make gates` und
`make shell-lint` selbst gefahren und grün.

**Tragender Vorbehalt für den Verifier:** `make mutate` und `make full-smoke` selbst sind in
dieser Runde nicht gelaufen (Kostenauflage der Aufgabe). Der Verifier sollte beide DoD-1/DoD-2-Läufe
mit ihrer Ausgabe selbst sehen, bevor er sie abhakt — dieser Report liefert dafür die
Quellcode-seitige Erwartung, keinen Ersatz für den Lauf.

**Übergabe:** F-1 (LOW) gehört in die Closure-Notiz bzw. an den Planner, der §6 des Plans
nachzieht — kein Plan-Defekt im Sinne der Rückkante Review → Plan, da der Plan zum Zeitpunkt
seiner Abfassung korrekt maß, was er damals messen konnte.
