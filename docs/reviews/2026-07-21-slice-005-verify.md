# Verifikations-Report: slice-005 (Root-README emittieren) — 2026-07-21

**Art:** Verifikation (Modul 11) — DoD-/Spec-Bestätigung, **nicht** ein zweites
Code-Review. Geprüft wird jeder DoD-Punkt (§2) gegen den realen Code **und** gegen
selbst gelaufene Sensoren. Eine DoD-Verletzung ist eine Verifier-only-Klasse
(unsichtbar für Review und Tests) — „Behauptung ohne Bestätigung" ist die häufigste
Verifier-Lücke.

**Gegenstand:** slice-005 · HEAD `e045923` (slice-005-Inhalt `d5a7f07` + Code-Review
`e045923`) · Inhalts-Diff-Range `f6613e2..d5a7f07`. Kernstücke:
`internal/emit/readme.go`, `internal/emit/readme_test.go`,
`cmd/ai-harness-init/main.go`, `cmd/ai-harness-init/main_test.go`,
`harness/tools/smoke.sh`, `test/mutations/24|25`.

**Skill:** `.harness/skills/reviewer.md` @ v1.2.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad) -->
**Modell:** claude-opus-4-8[1m] · **Datum:** 2026-07-21 · **Rolle:** unabhängiger
Verifier (frischer Kontext, keine Selbst-Verifikation).

**Eingangs-Kontext** (die Verträge, gegen die verifiziert wurde):

- Slice-Plan mit DoD: [slice-005](../plan/planning/done/slice-005-root-readme.md) §2
- Kontext-ADR: [ADR-0005](../plan/adr/0005-ziel-repo-distribution.md) (Herkunftsklassen; slice-005 berührt KEINE ADR — reiner Emit-Schritt)
- Anforderungen: [spec/lastenheft.md](../../spec/lastenheft.md) — `LH-FA-05`, `LH-FA-01`, `LH-QA-01`
- Konventionen: [harness/conventions.md](../../harness/conventions.md) (`MR-014` CI/smoke, `MR-008` cp-Disziplin)
- Hard Rules: [AGENTS.md](../../AGENTS.md) §3 (3.1/3.2/3.6)
- Zur Orientierung (nicht wiederholt): [Review-Report](2026-07-21-slice-005-review.md) (konform, 0 HIGH/MEDIUM, 2 INFO)

---

## DoD-Bestätigung (§2, Punkt für Punkt)

| # | DoD-Punkt (§2) | Erfüllt? | Beleg (Code + Sensor) |
|---|---|---|---|
| 1 | `LH-FA-05` erfüllt: Root-README aus Vorlage, Projektname gestempelt | **erfüllt** | `readme.go:33-52` `RootReadme` liest `project-readme.template.md` (`rootReadmeSource`, `readme.go:15`) und schreibt `README.md` (`RootReadmePath`, `readme.go:12`) als `stampName(StripHintBlock(...), name)` (`readme.go:47`) — identische Singleton-Transformation wie `planTemplates`. `main.go:182` ruft es in Phase 4. `TestRootReadme_StampStrip` (`readme_test.go:41`) belegt Stempel gesetzt + Platzhalter weg + Hinweis-Block gestrippt. Sensor: `make smoke` Schritt 3 belegt `README.md` real am Ziel-Root (`smoke.sh:48-51`); Mutation `24` (Strip entfernt) → `TestRootReadme_StampStrip` **rot** (selbst gesehen). |
| 2 | Vorwärts-Verweise gate-sicher (kein Markdown-Link auf fehlende Ziele), `make docs-check` im Zielrepo grün (= README fügt **0** Befunde hinzu; Voll-0 ist slice-024) | **erfüllt** | Reale Vorlage `.harness/baseline/v3.5.0/templates/project-readme.template.md` trägt nach `StripHintBlock` genau drei Markdown-Links (`AGENTS.md`, `harness/README.md`, `spec/lastenheft.md`, Zeile 45-46), alle **co-emittiert** (in `emit.Templates`/`inScope`); der einzige externe `https://`-Kurs-URL lebt IM Hinweis-Block (Zeile 8), den `StripHintBlock` (`templates.go:250`) positions-unabhängig entfernt. `TestRootReadme_LinksGateSicher` (`readme_test.go:70`) prüft nach vollem Emit, dass JEDER lokale Link real existiert (`checked>0`-Guard gegen wirkungslosen Test). Sensor: `make smoke` Schritt 4 → **`d-check: 11 Datei(en) geprüft, 3 Befund(e)`** — vor slice-005 (slice-004b-Verify) waren es `10 Datei(en) / 5 Befund(e)`; die neue `README.md` fügt also **0** Befunde hinzu und löst **2** vorher tote Eingangs-Links auf (5→3). Die verbleibenden **3** sind Vorwärts-Verweise anderer Templates = slice-024. **Kein Über-Claim:** `smoke.sh:22-23,109` + `readme.go:22-27` vertagen den Voll-0-Run explizit auf slice-024. |
| 3 | Emittierter Stand smoke-fähig; Voll-E2E = slice-024, hier **nicht** behauptet | **erfüllt** | Selbst gelaufen: `make smoke` **Exit 0**. `smoke.sh` Schritt 5 fährt bewusst `lint build test` am Ziel-Root, **nicht** `make gates`; `smoke.sh:98-101,109` (HINWEIS) vertagen den vollen `make-gates`-Green-Run explizit auf slice-005/024. Schritt 4 (`3 Befund(e)`) bestätigt: 0-out-of-the-box ist noch offen — passend zur Vertagung, kein Widerspruch. |
| 4 | Go-Test: README vorhanden, gestempelt, `docs-check` grün | **erfüllt** | `TestRootReadme_StampStrip` (vorhanden + gestempelt + Strip), `TestRootReadme_LinksGateSicher` (gate-sicher = jeder lokale Link löst auf; Tier-1-Struktur-Beleg für „docs-check grün"), `TestRootReadme_ForceBoundary` (Boundary-AC), `TestRun_ReadmeKollisionSchreibtKeinEmit` (`main_test.go:267`). Alle grün im `go test ./...` von `make gates` (`internal/emit` + `cmd/...` ok). Die REALE docs-check-Achse deckt Tier 2 (`make smoke` Schritt 4) — ehrlich gelabelt in `readme_test.go:22-25` (kein stilles Grün). |
| 5 | `make gates` grün | **erfüllt** | Selbst gelaufen: **Exit 0**. `baseline-verify: v3.5.0 OK — 42 Dateien`; `d-check: 100 Datei(en) geprüft, 0 Befund(e)`; golangci-lint `0 issues.`; bats `1..71` alle ok + `go test ./...` alle ok; shell-lint + ci-lint clean. |
| 6 | Closure-Notiz mit Steering-Loop-Lerneintrag | **steht aus (planmäßig)** | Planner-Schritt **nach** dem Verifier. §7 trägt nur `<!-- Erst nach Abschluss füllen. -->`; DoD-Checkboxen §2 unmarkiert; Slice liegt noch in `in-progress/`. Korrekt offen — nicht Aufgabe des Verifiers, **kein** Verifier-Befund. |

## Spec-/ADR-Konformität

- **`LH-FA-05`** (Root-README emittieren, gate-sichere Vorwärts-Verweise): erfüllt.
  Die README entsteht aus `project-readme.template.md` (Singleton-Transformation
  Strip+Stempel), der Pointer-/Trust-Abschnitt verweist ausschließlich auf
  co-emittierte Ziele (`AGENTS.md`/`harness/README.md`/`spec/lastenheft.md`), der
  externe Kurs-URL wird mit dem Hinweis-Block entfernt. Real gedeckt (smoke Schritt 4:
  README fügt 0 Befunde hinzu), nicht nur behauptet.
- **`LH-FA-01`** (Repo bootstrappen): erfüllt für den README-Emit-Anteil. `README.md`
  landet am Ziel-Root (smoke Schritt 3); die Boundary-AC „kein Überschreiben ohne
  `--force`" hält doppelt: `RootReadme` prüft per `os.Stat` (`readme.go:35-42`, mit
  force überschreibbar) UND `README.md` liegt im Phase-3-Pre-Flight (`emitTargets`,
  `main.go:221`) — eine Kollision bricht VOR jedem Emit-Write ab (kein Teil-Bootstrap,
  slice-025). Der Voll-Happy-Path „`make gates` 0-Befunde out-of-the-box" ist per
  Plan/Spec ausdrücklich slice-024 — hier weder behauptet noch nötig.
- **`LH-QA-01`** (keine halluzinierten Gates/Verweise): erfüllt. Die emittierte README
  verlinkt nur reale, co-emittierte Ziele; die `Gates:`-Zeile der Vorlage ist ein
  `<Platzhalter>` (Plain-Text), kein Link. `make gates` im Dogfood grün (0 Befunde);
  smoke belegt am realen Ziel-Root, dass die README 0 docs-check-Befunde hinzufügt.
- **`ADR-0005`** (Herkunftsklassen): nicht berührt — reiner Emit-Schritt aus dem
  gefetchten Template-Satz (dieselbe Klasse wie `emit.Templates`); keine neue Klasse,
  kein Merge, keine Ownership-Frage.

## Sensor-Läufe (selbst gefahren, Modul 11)

| Target | Ergebnis | Kernzeile der Ausgabe |
|---|---|---|
| `make gates` | **Exit 0 — grün** | `d-check: 100 Datei(en) geprüft, 0 Befund(e)` · `baseline-verify: v3.5.0 OK — 42 Dateien` · golangci-lint `0 issues.` · bats `1..71` ok · `go test ./...` ok · shell-lint + ci-lint clean |
| `make mutate` | **Exit 0 — grün** | `25 ok, 0 Befund(e)`; die beiden slice-005-Wächter färben real rot: `24-readme-strip → TestRootReadme_StampStrip rot`, `25-readme-preflight → TestRun_ReadmeKollisionSchreibtKeinEmit rot` |
| `make smoke` | **Exit 0 — grün** | `smoke: OK …`; Schritt 3 belegt `README.md` am Ziel-Root; Schritt 4: **`d-check: 11 Datei(en) geprüft, 3 Befund(e)`** = README fügt 0 hinzu / löst 2 auf (5→3 ggü. slice-004b-Stand), 3 Rest = slice-024; HINWEIS vertagt Voll-0 explizit |

**Reale smoke-Befundzahl eingeordnet:** `3` Befunde bei `11` geprüften Dateien. Der
Implementation-Claim „5→3 (README fügt 0 hinzu, löst 2 auf)" ist damit **real
bestätigt** — die drei ausgehenden README-Links lösen auf (0 neu), zwei vorher tote
Eingangs-Links auf das Root-README lösen mit der emittierten `README.md` auf. Die drei
Rest-Befunde sind Vorwärts-Verweise anderer Templates und gehören per Plan/DoD zu
slice-024. Kein Über-Claim.

**Arbeitsbaum nach jedem Lauf:** `git status --short` leer (gates schreibt nur ins
gitignorierte `.harness/state/`; mutate restauriert via `tar` — der Endzustand war
sauber, `25 ok`; smoke arbeitet in `mktemp`-tmp). HEAD unverändert bei `e045923`. Kein
liegengebliebener Mutations-Stand.

## Verdikt

**DoD bestätigt.** Fünf der sechs DoD-Punkte sind **erfüllt**, jeder mit
Code-Beleg **und** selbst gelaufenem Sensor; die drei hermetischen/Host-Sensoren
(`gates`/`mutate`/`smoke`) sind grün, die beiden neuen README-Wächter (`24|25`)
haben ihren rot gesehenen Gegenbeispiel-Beleg (AGENTS §3.6). Die kritische Zusage
(Gate-Sicherheit der Vorwärts-Verweise, `LH-QA-01`) ist **real gemessen**: die
emittierte README fügt am Ziel-Root **0** docs-check-Befunde hinzu (`5→3`), ohne mehr
zu behaupten als gedeckt — der Voll-0-out-of-the-box-Run bleibt korrekt auf slice-024
vertagt (`smoke.sh`-HINWEIS + `readme.go`-Doc). Punkt 6 (Closure-Notiz) steht
**planmäßig aus** — der Planner-Schritt nach der Verifikation, kein Verifier-Befund.

**Übergabe:** Kein Verifier-Befund an die Implementation (Modul 8). Der Slice ist
aus DoD-/Spec-Sicht abnahmereif; offen bleibt allein die Closure-Notiz (Planner).
