# Code-Review: slice-060 DoD (2) — die V-1-Auflösung (`467e467..5cef697`)

**Rolle:** Reviewer (Modul 10, `.harness/skills/reviewer.md` v1.4.0). **Datum:** 2026-07-30.
**Autor:** ai-harness-init-Team (pt9912). Frischer Kontext — weder der Code noch einer der
Vorgänger-Reports stammt aus diesem Lauf.

## Kopf-Metadaten (die fünf Pflicht-Punkte + Slice-Plan)

| Punkt | Inhalt |
|---|---|
| **Diff / Commit-Range** | `467e467..5cef697` — `3e3079d` (B1 bekommt einen Dauer-Zahn, `MR-018` sagt, was jeder Zahn bindet) · `296d75d` (Rest-Lücke gemessen statt behauptet) · `5cef697` (Dateimodus 755). 8 Dateien, +271/−30 |
| **`LH-*`-Anforderungen** | [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6), [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit) |
| **Referenzierte aktive ADRs** | [`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md) (**Accepted**, Festlegung 1 Punkt 2/3, Festlegung 2, Folgepflicht 1), [`ADR-0003`](../plan/adr/0003-go-native-binaries.md) |
| **Hard Rules** | [`AGENTS.md`](../../AGENTS.md) §3.1 · §3.2 · §3.3 · §3.4 · §3.5 · **§3.6** |
| **Vorherige Findings am gleichen Modul** | `docs/reviews/2026-07-30-slice-060-dod2-adr-0011-architect.md` (B1–B5, Z1–Z5), `…-slice-060-dod2-review.md` (Runde 1), `…-slice-060-dod2-review-runde-2.md` (Runde 2: R2-HIGH-1, R2-MEDIUM-1, R2-LOW-1/2, R2-INFO-1/2), Verifikations-Befund **V-1** (nur als Gesprächsprotokoll, keine Datei) |
| **Slice-Plan** (Repo-Ergänzung) | `docs/plan/planning/in-progress/slice-060-rollen-achse.md` DoD (2), §3-Änderungstabelle Zeile `test/mutations/` |

**Nicht Gegenstand:** die DoD-Abhakung und die Bestätigung der Gate-Läufe (Modul 11).

### Gefahrene Sensoren — echte Ausgabe, nichts übernommen

Kein `make mutate`-Vollauf (Nutzer-Ausschluss). Stattdessen **elf** Docker-Läufe über den
gesourcten Treiber-Pfad (`harness/tools/mutate.sh` + `run_case`) gegen eine isolierte Kopie
außerhalb des Repos, mit Grün-Vor- und -Nachlauf; der Host-Baum stand vor und nach jedem
Lauf auf `5cef697` mit leerem `git status`.

```
GRUEN-VORLAUF   make test-go exit=0

(1) intakte Waechter, die vier neuen Faelle einzeln
mutate: ok      132-span-rolle-aus-argument       -> TestAgentGetsNoArgumentFields rot
mutate: ok      133-span-werkzeugachse-geweitet   -> TestOnlyAgentToolGetsResponseValues rot
mutate: ok      134-span-zaehler-praesent-leer    -> TestFailedAgentCallCapturesNothing rot
mutate: ok      135-span-agent-auf-gattungszeile  -> TestAgentGetsNoArgumentFields rot
SUMMARY: 4 ok, 0 Befund(e)

(2) B1-Zusicherungen aus TestAgentGetsNoArgumentFields gestrichen (Gruen-Vorlauf exit=0)
mutate: ok      131-span-werkzeugname-leer        -> TestAgentGetsNoArgumentFields rot
mutate: BEFUND  132-span-rolle-aus-argument       make test-go blieb GRUEN —
                                                  'TestAgentGetsNoArgumentFields' hat keine Zaehne mehr

(3) mustContain wirkungslos gemacht (Gruen-Vorlauf exit=0)
mutate: ok      123-span-ergebnis-content         -> TestNoResponseFreetextReachesSpan rot
mutate: ok      127-span-positivliste-negiert     -> TestUnlistedResponseKeyStaysOut rot

(4) Sonde: p.Spawned = extractAgentResult(v) ausgebaut
make test-go exit=2 — rot bei TestNoResponseFreetextReachesSpan,
TestUnlistedResponseKeyStaysOut, TestOnlyAgentToolGetsResponseValues,
TestSpawnedRoleIsNormalised, TestResolvedModelIsStructurallyBounded

(5) Sonde: sechs der neun Wert-Namen aus TestFailedAgentCallCapturesNothing gestrichen
mutate: ok      134-span-zaehler-praesent-leer    -> TestFailedAgentCallCapturesNothing rot

(6) Sonde: `omitempty` von `output_tokens` genommen
make test-go exit=0 — 0 x "--- FAIL:"

(7) Sonde: argc der Kommandozeile aus Fall 135
SONDE-ARGC {... "program":"gh","argc":4}

GRUEN-NACHLAUF  make test-go exit=0
```

Dazu am Host, lesend: `make comment-claims` → `38 Datei(en) geprueft, 0 Befund(e)`;
`make docs-check` → `d-check: 257 Datei(en) geprüft, 0 Befund(e)`; Auszählung von
`test/mutations/`, der `# expect:`-Köpfe, der `MR-018`-Zahlen und der Anker-Eindeutigkeit
gegen `git ls-files`.

### Die drei nachzuzählenden Angaben — eigene Zahlen

| Angabe | Fundstelle | nachgezählt | Urteil |
|---|---|---|---|
| „die **elf** Zähne oben (123–129 und 132–135)" | `harness/conventions.md:1224-1225` | 7 + 4 = **11** | **stimmt** |
| „geliefert sind **dreizehn** … 123–135" | `docs/plan/planning/in-progress/slice-060-rollen-achse.md:216` | `ls test/mutations/ \| grep -cE '^(12[3-9]\|13[0-5])-'` = **13**, alle Dateien existieren, höchste Nummer 135, Gesamtstand 131 Fälle | **stimmt** |
| „**fünf** von sieben / **vier** von sieben" | `harness/conventions.md:1108-1112` | 5 (`123-126`,`127`,`128`,`129`,`131` nennen fünf der sieben Wächter; `TestOnlyAgentToolGetsResponseValues` und `TestFailedAgentCallCapturesNothing` hatten keinen) bzw. 4 (Messung (2): mit gestrichenen B1-Zusicherungen meldet 131 weiter „ok") | **stimmt, beide** |

Die Zählfehler-Serie dieser Familie setzt sich in `MR-018` **nicht** fort. Sie setzt sich
in einem Fall-Kopf fort (LOW-1).

---

## Findings

### MEDIUM-1 — `TestFailedAgentCallCapturesNothing` prüft acht der neun Werte; `output_tokens` darf in **jeder** Span-Zeile als `null` erscheinen, ohne dass ein Gate es sieht

- **kategorie:** MEDIUM
- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 (*„Ein Test, dessen Name eine Eigenschaft
  behauptet, muss die Eigenschaft messen"*) ·
  [`MR-018`](../../harness/conventions.md#mr-018--span-schema-der-telemetrie-erfassung)
  Feldtabelle `:869` (`output_tokens` = **Optional**) ·
  [`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md) Festlegung 1 Punkt 3
- **pfad:** `harness/conventions.md:1160-1163` gegen `internal/span/response_test.go:323-326`
- **befund:** `MR-018` §Bewacht Punkt 8 schreibt dem Wächter die Zusicherung *„**kein halber
  Span**: die **neun** Werte fehlen, statt anwesend-und-ungemessen dazustehen"* zu. Die
  `mustNotContain`-Liste des Wächters (`response_test.go:323-326`) führt sieben Zeichenketten:
  `spawned_role`, `input_tokens`, `total_tokens`, `total_duration_ms`, `total_tool_use_count`,
  `model_version`, `result_bytes`. `input_tokens` deckt per Teilstring zusätzlich
  `cache_creation_input_tokens` und `cache_read_input_tokens` — **`output_tokens` deckt
  nichts davon ab**, und kein anderer Test nennt es je in einer Negativ-Prüfung
  (repo-weit gemessen: `output_tokens` steht nur in `response_test.go:80` und `:108`, beide
  `mustContain`, sowie in `response.go:28`/`:68`). Gemessen, nicht geschlossen: mit
  `json:"output_tokens"` statt `json:"output_tokens,omitempty"` meldet `make test-go`
  **Exit 0** und **null** `--- FAIL:`-Zeilen (Sonde 6). `make test-bats` ist hier ohne
  Aussage — keine bats-Datei berührt Spans. Damit ist die Neuner-Zusage der Liste an genau
  einer Stelle nicht nur breiter als ihr Zahn, sondern breiter als ihr **Wächter**.
- **failure-szenario:** Jemand vereinheitlicht die Struct-Tags in
  `internal/span/response.go:26-34` und nimmt dabei ein `omitempty` weg. Ab da trägt
  **jede** geschriebene Zeile — auch jeder `Bash`-Span — `"output_tokens":null`. Genau die
  Lesart, die `MR-018:868` und der Kopf von Fall 134 als tragend beschreiben („der
  Unterschied zwischen *unbekannt* und *nicht vorhanden*"), kippt: slice-066 sieht eine
  Messung, wo keine stattfand. `make gates` bleibt grün, `make mutate` bleibt grün, und
  `MR-018` §Bewacht sagt dem nächsten Leser, diese Eigenschaft sei neunfach bewacht.
- **verifizierbar:** ja, gefahren. `make test` bestätigt den Befund als **stilles Grün**;
  reproduzierbar mit `sed -i 's@json:"output_tokens,omitempty"@json:"output_tokens"@'` in
  einer isolierten Kopie und anschließendem `make test-go`.
- **Zur Kategorie, weil sie bestreitbar ist:** ein Argument für **HIGH** liegt vor — §3.6
  ist eine Hard Rule, und der Skill führt *„Verstoß gegen … eine Hard Rule"* unter HIGH; die
  Aussage steht in dem Artefakt, an das
  [`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md) Folgepflicht 1 die
  Feldtabelle delegiert. Dagegen: der Gate **selbst** hat keinen Stillen-Grün-Pfad, es fehlt
  ein Negativ-Assert (MEDIUM-Anker *„fehlende Negativtests bei neuem öffentlichen
  Vertrag"*), und die Vorgänger-Runden haben dieselbe Klasse konsistent als MEDIUM geführt
  (R2-MEDIUM-1). Ich bleibe bei MEDIUM; die Verdikt-Folge ist dieselbe.

### MEDIUM-2 — Der neue Kommentar an `intoSpawnedRole` nennt Fall 134 als Dauer-Sensor für die Abwesenheit von `spawned_role`; 134 mutiert ein anderes Feld

- **kategorie:** MEDIUM
- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 · Runde-2-Befund **R2-MEDIUM-1**
  (dieselbe Fehlerform: Zusicherung einem Zahn zugeschrieben, der ein anderes Feld mutiert)
- **pfad:** `internal/span/response.go:109-114`
- **befund:** Der Kommentar sagt: *„die Abwesenheit bei fehlendem Ergebnis bewachen
  `TestAgentGetsNoArgumentFields` und `TestFailedAgentCallCapturesNothing` an der
  geschriebenen Zeile, **mit den Dauer-Sensoren** `test/mutations/132-…` … **und**
  `test/mutations/134-span-zaehler-praesent-leer.sh`"*. Gegenstand des Absatzes ist
  `spawned_role`; Fall 134 mutiert `json:"input_tokens,omitempty"` und berührt
  `spawned_role` an keiner Stelle. Für den ersten der beiden Wächter ist die Zuschreibung
  seit diesem Diff korrekt (Fall 132, Messung (1)/(2)); für den **zweiten** ist sie es
  nicht. Gemessen (Sonde 5): streicht man `"spawned_role"` — zusammen mit
  `total_tokens`, `total_duration_ms`, `total_tool_use_count`, `model_version`,
  `result_bytes` — aus der `mustNotContain`-Liste in `response_test.go:324-325`, bleibt
  `make test-go` grün und Fall 134 meldet weiter **„ok"**. Die Draht-Form-Zusicherung von
  `spawned_role` in diesem Wächter hat damit keinen Dauer-Zahn, während der Kommentar
  einen nennt. `make comment-claims` fängt es nicht: Prüfung (b) validiert Testnamen,
  `test/mutations` zählt als Sensor-Token ohne Existenz- oder Inhaltsprüfung
  (`harness/tools/comment-claims.sh:36`).
- **failure-szenario:** Beim Umbau der Fehlschlag-Payload streicht jemand `"spawned_role"`
  aus der Liste in `response_test.go:324` (etwa weil `spawned_role` „ohnehin von
  `TestAgentGetsNoArgumentFields` geprüft" wird). `make mutate` bleibt ohne Befund — 134
  wird weiter über `input_tokens` rot und meldet `-> TestFailedAgentCallCapturesNothing rot`.
  Danach ist die Aussage aus `MR-018:868` („`spawned_role` ist `omitempty` und **fehlt** bei
  leerem Wert") für den Fehlschlag-Fall unbewacht, und die Zuschreibung im Code-Kommentar
  behauptet das Gegenteil. Das ist exakt R2-MEDIUM-1, eine Datei weiter — die **dritte**
  Wiederholung dieser Klasse in dieser Slice-Familie und nach dem Reviewer-Skill ein
  **Steering-Loop-Signal**.
- **verifizierbar:** ja, gefahren. `"spawned_role"` aus `response_test.go:324` entfernen
  und Fall 134 über `run_case` fahren — er meldet „ok".

### LOW-1 — Fall 135 sagt in seinem Kopf `"argc":5` zu; gemessen entsteht `"argc":4`

- **kategorie:** LOW
- **quelle:** [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit) ·
  Muster „Zahlen behauptet statt abgezählt" (sechs Vorinstanzen in dieser Familie)
- **pfad:** `test/mutations/135-span-agent-auf-gattungszeile.sh:27`
- **befund:** Der Kopf begründet die Wahl der Kommando-Gattung mit: *„Ueber die
  Kommando-Gattung entstehen `"program":"gh"` und `"argc":5`"*. Die Kommandozeile im
  `tool_input` des Wächters (`response_test.go:206`) ist
  `gh auth login --with-token TOKEN-iii999` = fünf Felder; `commandProgram`
  (`internal/span/span.go:249-263`) liefert `len(fields) - i - 1` = **4**. Gemessen über
  eine Sonde in der isolierten Kopie: `"program":"gh","argc":4`. Die tragende Aussage des
  Absatzes (beide Werte stehen auf keiner `mustNotContain`-Liste, der Wächter läuft bis zur
  Strukt-Prüfung durch) bleibt richtig — die Zahl ist es nicht.
- **failure-szenario:** Wer Fall 135 von Hand nachvollzieht, baut die erwartete Zeile aus
  dem Kopf, findet `argc 4` und muss entscheiden, ob der Anker gewandert ist oder der Kopf
  falsch zählt — an einem Fall, dessen Existenzgrund die Präzision der Zuordnung ist.
- **verifizierbar:** ja, gefahren (Sonde 7).

### LOW-2 — `MR-018` trägt vier Zeilennummern in `response_test.go` als Gegenwarts-Aussage; kein Sensor prüft sie, und die im Bericht genannte Einschränkung steht nicht dort

- **kategorie:** LOW
- **quelle:** [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit) ·
  [`MR-018`](../../harness/conventions.md#mr-018--span-schema-der-telemetrie-erfassung)
  §Auflösungs-Trigger (*„permanent"*) · Runde-2-Befund R2-LOW-1 (dieselbe Klasse:
  eingefrorene Messwerte in einer normativen Zeile)
- **pfad:** `harness/conventions.md:1121-1124`
- **befund:** Die Zeile lautet: *„**Jeder ist einzeln gefahren und färbt genau EINEN
  Wächter an genau der Zeile seiner Zusicherung** (2026-07-30, …): 132 an
  `response_test.go:209`, 133 an `:171`, 135 an `:223`, 134 an `:323`."* **Alle vier
  Nummern stimmen heute** — gemessen an der echten Fehlschlag-Ausgabe (`:209`
  `"spawned_role" steht in der Span-Zeile`, `:171` `Werkzeug "Bash" gab Ergebnis-Werte
  preis`, `:223` `` `Agent` wurde auf eine Gattungszeile abgebildet ``, `:323`
  `"input_tokens" steht in der Span-Zeile`). Zwei Beobachtungen dazu: (a) die
  Einschränkung „datierte Messung, nicht lebende Zusage" steht **nicht** im Artefakt — der
  fett gesetzte Satz steht im Präsens, datiert ist nur die Klammer; (b) es gibt keinen
  Sensor: `d-check`s `check-lines` (`.d-check.yml:53`) validiert Zeilen-Referenzen der Form
  `datei:<von>-<bis>` an **Pfaden**, und `response_test.go:209` bzw. `:171` tragen keine
  Verzeichnis-Komponente — gemessen bleibt `make docs-check` mit ihnen grün
  (257 Dateien, 0 Befunde).
- **failure-szenario:** Der nächste, der eine Zeile in `response_test.go` einfügt,
  verschiebt alle vier Nummern. `MR-018` — das Artefakt, das nach Folgepflicht 1 *„der
  nächste Leser … ohne Code finden"* muss — zeigt dann auf vier falsche Zeilen, kein Gate
  meldet es, und die Genauigkeit, die der Absatz beweisen soll, wird zum Gegenteil.
- **verifizierbar:** ja, gefahren (Messung (1) für die Nummern, `make docs-check` für das
  Fehlen des Sensors).

### INFO-1 — Die Commit-Aussage „ausführbar (Modus 755)" beschreibt keinen Zustand, der den Treiber je betroffen hätte

- **kategorie:** INFO
- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.6 (Commit-Message als Zusage) ·
  Maintainability
- **pfad:** Commit `5cef697` gegen `harness/tools/mutate.sh:301`
- **befund:** Die Betreffzeile lautet *„die vier neuen Mutations-Faelle ausfuehrbar
  (Modus 755)"*. `run_case` ruft jeden Fall als `bash "$case_file"` auf
  (`mutate.sh:301`) — das Ausführungs-Bit ist für `make mutate` ohne Wirkung; die vier
  Fälle liefen auch bei `100644`. Der Modus ist damit Konsistenz mit den übrigen 127
  Fällen, nicht eine Reparatur, und **kein** Sensor bindet ihn (kein Test und kein
  Mutations-Fall prüft die Modi in `test/mutations/`).
- **failure-szenario:** Wer die Historie liest, schließt aus der Betreffzeile, die vier
  Fälle seien bei `3e3079d` in `make mutate` nicht gelaufen — eine falsche Aussage über
  einen Commit, auf den der CI-Vollauf zeigt.
- **verifizierbar:** nein (Aussage über eine Commit-Message). Nachlesbar an `mutate.sh:301`.

---

## Negativbefunde (geprüft, ohne Befund)

**Der Befund V-1 selbst.**

- **Geprüft, ohne Befund: die vier neuen Fälle färben je genau EINEN Wächter — selbst
  ausgezählt, nicht nur der erwartete Name.** Über alle `--- FAIL:`-Zeilen der vier
  Verify-Logs (Docker druckt jede Zeile doppelt, einmal im Stream, einmal im Sammel-Log):
  132 → 1 Wächter (`TestAgentGetsNoArgumentFields`), 133 → 1 Wächter
  (`TestOnlyAgentToolGetsResponseValues`) mit seinen drei Unterfällen `Bash`/`Read`/`Write`
  (der Fall-Kopf nennt sie namentlich), 134 → 1 Wächter
  (`TestFailedAgentCallCapturesNothing`), 135 → 1 Wächter (`TestAgentGetsNoArgumentFields`).
  Kein Mehrfach-Rot über Wächter-Grenzen; die Präzedenz R2-MEDIUM-4 wiederholt sich nicht.
- **Geprüft, ohne Befund: die B1-Bindung ist zweiseitig belegt, und die Gegenprobe zeigt
  wirklich beide Richtungen.** Mit intaktem Wächter meldet 132 „ok" und fällt an
  `response_test.go:209`; mit gestrichenen B1-Zusicherungen (`"spawned_role"` im
  `mustNotContain` **und** die `s.SpawnedRole != ""`-Prüfung) meldet er **BEFUND**
  (*„make test-go blieb GRUEN — … hat keine Zaehne mehr"*), **während 131 im selben Lauf
  weiter „ok" meldet** (er fällt an `response_test.go:212`, der Gegenprobe `"tool":"Agent"`).
  Damit ist gemessen, was `MR-018:1113-1119` behauptet: 131 nennt den Wächter und bindet
  B1 nicht.
- **Geprüft, ohne Befund: Fall 132 trifft die Eigenschaft, nicht ihren heutigen Träger.**
  Die Mutation liest `subagent_type` direkt aus der Payload-Map und lässt `ToolInput`
  unangetastet. Das ist die **stärkere** Konstruktion, nicht die Umgehung: das
  Architect-Gegenbeispiel zu B1 (`…-architect.md:262-264`) ist als Payload-Aussage
  formuliert (*„eine Payload mit `tool_input.subagent_type: "reviewer"` und fehlendem
  `tool_response` erzeugt einen Span mit leerem `spawned_role`"*), nicht als Struct-Aussage;
  §3.6 verlangt genau das („der Verifier prüft nicht die Absicht, sondern das Rot"). Eine
  Mutation, die `ToolInput` um ein viertes Feld erweitert, wäre die **schwächere**: ein
  ungenutztes Struct-Feld erreicht den Span nicht und färbte nichts rot.
- **Geprüft, ohne Befund: die Normalisierung in der 132er-Mutation ist begründet und
  gemessen.** `roleFromAgentType` im Rückfall hält `TestFailedAgentCallCapturesNothing`
  (`subagent_type: "nope"`) grün — bestätigt durch die Ein-Wächter-Auszählung oben. Ein
  roher Rückfall färbte zwei Wächter und machte „132 rot" mehrdeutig.
- **Geprüft, ohne Befund: die vier `sed`-Anker greifen genau einmal.**
  `^\tp.Failed = failed(raw, p.Event)$` (1×), `toolClass(p.Tool) == classAgent` (1×),
  `json:"input_tokens,omitempty"` (1×), `^\tcase classCommand:$` (1×) — gezählt gegen die
  Zieldateien. Bedingung 2 des Treibers hat in allen Läufen gegriffen.
- **Geprüft, ohne Befund: die Neben-Zusagen der Fall-Köpfe stimmen.** 133s Behauptung, der
  einzige weitere Test mit `tool_response` an einem fremden Werkzeug sei
  `TestDurationAndResultSize` (`Bash`, JSON-**String**), ist repo-weit nachgemessen (ein
  Treffer, `span_test.go:308`). 134s Behauptung, `json:"input_tokens,omitempty"` stehe so
  nur an `InputTokens`, stimmt. 135s Begründung, warum die Kommando- und nicht die
  Datei-Gattung (sonst fiele der Wächter schon am `mustNotContain` über `/etc/shadow`),
  trägt — die gemessene Fehlschlag-Zeile ist die Strukt-Prüfung `:223`.

**Die Selbstkorrektur in `296d75d`.**

- **Geprüft, ohne Befund: die neue Fassung ist gemessen und sie stimmt — in beiden
  Hälften.** (a) *„macht man `mustContain` … wirkungslos, bleibt `make test-go` grün, und
  die Fälle 123 und 127 melden weiter »ok«"* — nachgefahren (Messung (3)): Grün-Vorlauf
  exit 0, `mutate: ok 123 …`, `mutate: ok 127 …`. (b) *„eine ganz ausfallende Erfassung
  bräche die Gegenproben und damit `make test`"* — die zurückgenommene Behauptung war
  „bliebe unbemerkt"; die Ersatz-Aussage ist nachgemessen (Sonde 4): mit ausgebautem
  `p.Spawned = extractAgentResult(v)` meldet `make test-go` **Exit 2** und fünf rote
  Wächter. Die Korrektur geht in die richtige Richtung und ist belegt, nicht gelesen.
- **Geprüft, ohne Befund: die Zurückstellung der `mustContain`-Gegenproben trägt nach
  §3.6.** Die Regel sanktioniert genau diesen Zug (*„benennen, was wirklich deckt — oder
  dass nichts deckt"*), die Lücke ist gemessen, ihre Klasse benannt („dieselbe Klasse wie
  V-1, eine Ebene tiefer"), der Preis eines Zahns benannt (er färbte mehrere Wächter auf
  einmal) und sie ist **nicht** mitgezählt. Sie steht außerdem in `MR-018`, dessen
  Auflösungs-Trigger *„permanent"* lautet — anders als bei R2-LOW-2 überlebt diese
  Zurückstellung die Slice-Closure, auch wenn `grep -rln "mustContain"
  docs/plan/planning/` leer bleibt. Es ist **nicht** dieselbe Lücke eine Ebene weiter,
  sondern ihre ehrliche Benennung.

**Die neue Form von `MR-018` §Bewacht.**

- **Geprüft, ohne Befund: alle sieben Wächter aus `response_test.go` haben heute einen
  Fall.** Auszählung der `# expect:`-Köpfe über alle 131 Fälle:
  `TestNoResponseFreetextReachesSpan` → 123/124/125/126, `TestUnlistedResponseKeyStaysOut`
  → 127, `TestOnlyAgentToolGetsResponseValues` → 133, `TestAgentGetsNoArgumentFields` →
  131/132/135, `TestSpawnedRoleIsNormalised` → 128,
  `TestResolvedModelIsStructurallyBounded` → 129, `TestFailedAgentCallCapturesNothing`
  → 134. Die 5-von-7-Lücke aus V-1 ist geschlossen.
- **Geprüft, ohne Befund: die Doppelrolle von Fall 131 ist aufgelöst, nicht verdeckt.** Die
  nummerierte Liste nennt zwölf Zähne (123–129, 131–135), die Schlusszählung spricht von
  „**elf** Zähne**n** oben (123–129 und 132–135)" und weist 130/131 ausdrücklich der
  **Voraussetzung** zu; Punkt 9 der Liste verweist selbst dorthin. Beide Zahlen sind
  intern konsistent, die Klammer ist eindeutig.
- **Geprüft, ohne Befund: Punkt 4, 5, 6, 7 und 9 sind an ihre Zusicherung gebunden.** B1
  (Punkt 4) und B2 (Punkt 5) sind einzeln rot gesehen und fallen an verschiedenen Zeilen
  desselben Wächters (`:209` gegen `:223`) — die Begründung „ein Zahn je Zusicherung ist
  hier kein Luxus" trägt. Punkt 9 (`"tool":"Agent"`) ist gemessen: 131 fällt an `:212`
  **und** `:319`, den beiden `mustContain`-Zeilen.
- **Geprüft, mit Rest-Beobachtung: Punkt 3 ist enger gebunden, als er klingt — aber der
  Fall-Kopf sagt es.** `133` weitet `== classAgent` zu `!= classNone`; die vier
  `classNone`-Unterfälle des Wächters (`Task`, `mcp__x__agent`, `BashOutput`, `agent`)
  bleiben davon unberührt und sind damit von keinem Zahn gebunden. Der Fall-Kopf benennt
  das ausdrücklich (*„der Waechter faellt also an der Ausweitung, nicht am fail-closed
  Default selbst"*); `MR-018` Punkt 3 lässt die Einschränkung weg. Kein eigener Befund —
  die Offenlegung existiert und liegt an der Stelle, an der der nächste Leser mutiert.

**Hard Rules.**

- **§3.1 geprüft, ohne Befund.** Kein neues Gate behauptet; `make docs-check`,
  `make comment-claims` am Host gefahren, beide grün mit Vollständigkeits-Zeile.
- **§3.2 geprüft, ohne Befund.** `git diff 467e467..HEAD | grep "nolint\|shellcheck
  disable"` → keine Treffer.
- **§3.3 nicht anwendbar** — kein `git mv` im Range.
- **§3.4 geprüft, ohne Befund.** Der Diff berührt `docs/plan/adr/` nicht; die ADR-Lage
  bewegt sich ausschließlich über `MR-018`, an das
  [`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md) Folgepflicht 1 delegiert.
- **§3.5 geprüft, ohne Befund.** Kein Gate entfernt, kein Schwellwert gesenkt; die
  Fallzahl steigt von 127 auf **131** (der CI-Vollauf auf `467e467` meldete `127 ok` — die
  Zahlen passen zusammen). Die einzige Entfernung im Diff ist Prosa (die
  Nebeneinanderstellung in §Bewacht), ersetzt durch eine strengere Form.
- **§3.6 Muster-Abgleich.** Die Klasse „Zusage breiter als Sensor" tritt im Delta **zweimal**
  auf (MEDIUM-1, MEDIUM-2) — nach drei Instanzen in Runde 1 und zwei in Runde 2. Beide
  zeigen diesmal auf **dieselbe** Stelle: die Zuschreibung von Fall 134 zu
  `TestFailedAgentCallCapturesNothing`. Nach dem Reviewer-Skill ist das ein
  **Steering-Loop-Signal** (Guide/Sensor nachziehen statt nur melden).

**Dogfood vs. emittiert.**

- **Geprüft, ohne Befund: es wird weiterhin nichts emittiert.** Der Diff berührt
  `internal/emit/templates/` nicht; alle Aussagen dieses Reviews gelten für den
  **Dogfood**. `ADR-0011` Festlegung 5 (das *Ob* der Emission) bleibt unberührt, B0 des
  Architect-Verdikts hält.

**Architect-Grenzen B1–B5 (als Eingabe, nicht als Erledigung — die Abhakung ist Modul 11).**

- **B1** ist seit diesem Diff an einen Dauer-Zahn gebunden (Messung (1)/(2)). **B2**
  ebenfalls (Fall 135, `:223`). **B3** unverändert gebunden (Fall 127, Runde 2 zweiseitig
  gemessen). **B4** unverändert: alle neun Werte stehen mit Incident-Frage in
  `harness/conventions.md:868-874`. **B5** unverändert gebunden (Fall 129). Keine der fünf
  Grenzen ist durch diesen Diff bewegt worden.

---

## Kategorie-Summary

| Kategorie | Anzahl | IDs |
|---|---|---|
| **HIGH** | 0 | — |
| **MEDIUM** | 2 | MEDIUM-1 · MEDIUM-2 |
| **LOW** | 2 | LOW-1 · LOW-2 |
| **INFO** | 1 | INFO-1 |

---

## Verdikt

**NICHT KONFORM** — zwei MEDIUM blockieren nach der Verdikt-Regel des Reviewer-Skills.
Kein HIGH; die Auflösung von V-1 selbst ist **vollständig und gemessen**.

**Was dieser Diff einlöst.** Die Kern-Zusage trägt: B1 hat einen Dauer-Zahn, und das ist
zweiseitig im selben Lauf belegt — 132 „ok" mit intaktem Wächter, 132 „BEFUND" mit
gestrichenen B1-Zusicherungen, **während 131 daneben weiter „ok" meldet**. Alle sieben
Wächter aus `response_test.go` haben jetzt einen Fall; die 5-von-7-Lücke ist geschlossen.
Jeder der vier neuen Fälle färbt genau einen Wächter — selbst über alle `--- FAIL:`-Zeilen
ausgezählt, nicht am erwarteten Namen abgelesen. Alle drei nachgezählten Zahlen (elf,
dreizehn, fünf/vier) stimmen; die Zählfehler-Serie setzt sich in `MR-018` nicht fort. Die
Selbstkorrektur in `296d75d` ist in beiden Hälften gemessen und richtig.

**Zu Fall 135 — Entscheidung: legitime Vervollständigung, kein Scope-Verstoß.** B2 ist eine
der fünf Grenzen, auf denen das Architect-Verdikt ruht (`…-architect.md:267-273`), sie sitzt
im **selben** Wächter wie B1, und sie war nach derselben Messung zahnlos. §3.6 kennt keine
Staffelung nach Auftragsumfang: eine Zusage ohne rot gesehenes Gegenbeispiel ist unfertig,
gleich wer sie geschnitten hat. Sie herauszulösen hieße, dieselbe Lücke in einen künftigen
Slice zu verschieben und den Wächter mit zwei von drei gebundenen Zusicherungen
zurückzulassen — genau die Halbheit, die V-1 gefunden hat. Der Fall bleibt; ein eigener
Slice wäre hier Verwaltung, kein Zuschnitt.

**Die eine Sache, die einer Closure im Weg steht.** `MR-018` §Bewacht ist in dieser Runde
von einer Nebeneinanderstellung zu einer Liste *„was ein Zahn bindet"* umgebaut worden —
und **Punkt 8 ist die eine Zeile, die diesen eigenen Anspruch nicht hält**: sie schreibt
dem Wächter und seinem Zahn die neun Werte zu, während der Wächter **acht** davon prüft
und der Zahn **einen** bindet. Der ungeprüfte neunte ist `output_tokens`, und das ist kein
Formfehler: ein `omitempty` weniger an `internal/span/response.go:28` schreibt
`"output_tokens":null` in **jede** Span-Zeile, und der gesamte Gate-Stack bleibt grün
(gemessen). Solange diese Zeile steht, sagt das Artefakt, an das
[`ADR-0011`](../plan/adr/0011-telemetrie-erfassung-policy.md) Folgepflicht 1 die Feldtabelle
delegiert, dem nächsten Leser das Gegenteil dessen, was gemessen ist — in genau dem Absatz,
der zur Reparatur dieser Fehlerform geschrieben wurde.

**Rollen-Grenze.** Dieses Artefakt prüft den Diff gegen Plan, ADR und Hard Rules; es hakt
keinen DoD-Punkt ab. Zwei Punkte gehen als **Eingabe** an die Verifikation (Modul 11), nicht
als Befund: (a) die DoD-Zeile *„`make mutate` ohne Befund"* ist auch hier nur über gesourcte
Einzelläufe belegt — mein Beleg umfasst 123, 127, 131–135 in mehreren Varianten, nicht die
131 Fälle; der CI-Vollauf auf `5cef697` lag mir nicht vor. (b) `make gates` habe ich nicht
vollständig gefahren; belegt sind `make docs-check` (257/0) und `make comment-claims` (38/0)
sowie `make test-go` (grün, Vor- und Nachlauf).
