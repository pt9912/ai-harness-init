# Review-Report: slice-035 — 2026-07-23

**Review-Art:** Code — geprüft gegen Slice-Plan + ADR-0007 + Hard Rules (AGENTS.md §3) + Konventionen (Modul 10 §Drei Review-Arten). **Nicht** die DoD (Verifier-Rolle).

**Gegenstand:** slice-035 CLI-Phasierung — Implementierungs-Commit `37d2930`.

**Skill:** `.harness/skills/reviewer.md` @ 1.2.0 · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** Claude Opus 4.8 (1M) · **Datum:** 2026-07-23

**Eingangs-Kontext** (die Verträge, gegen die geprüft wurde):

- Slice-Plan: `docs/plan/planning/in-progress/slice-035-cli-phasierung.md`
- aktive ADR: ADR-0007 (Bootstrap-Phasen; Entscheidung 1 `--lang` optional / Phasen, Entscheidung 2 Fragment-Aggregator)
- Vorgänger-Kontext: `docs/plan/planning/done/slice-034-gate-fragment-assembly.md` §6/§7 (Option-A-Deferral: Aggregator lag vorübergehend im `gen`)
- berührte `LH-*`-IDs: LH-FA-01, LH-FA-04, LH-QA-01
- `AGENTS.md` §3 (Hard Rules), `harness/conventions.md`

---

## Findings

### F-1 — Idempotenz der konvergenten Klasse (Aggregator) noch nicht umgesetzt

- `kategorie`: INFO
- `quelle`: ADR-0007 (Entscheidung 3, Artefakt-Klassifikation)
- `pfad`: `internal/emit/makefile.go:50-64`
- `befund`: `emit.Makefile` schreibt die Root-`Makefile` mit dem bisherigen Kollisions-Refuse (`existiert bereits` ohne `--force`), während ADR-0007 §3 den Aggregator der Klasse **konvergent** zuordnet (Re-Lauf schreibt kanonisch, nie clobbern-blockierend). Der Slice-Plan §1/§6 deklariert die Idempotenz-Klassifikation ausdrücklich als **Folge-Slice** (slice-036); das Verhalten ist damit konsistent mit dem aktuellen Tool-Stand (alle Emitter refusen), nicht ein Defekt dieses Slice. Rein dokumentierte, bewusst aufgeschobene Annahme.
- `verifizierbar`: nein — kein Gate deckt Idempotenz in diesem Slice (per Plan out-of-scope).

## Negativbefunde

- geprüft, ohne Befund — **Aggregator-Relocation `gen`→`emit`:** Der Const-Move ist vollständig. `aggregatorMakefile` existiert nur noch in `internal/emit/makefile.go`; `internal/gen/golang.go` trägt weder die Const noch den `"Makefile"`-Schlüssel im `goProfile`-Map (grep-verifiziert). Kein toter `gen`-Wächter: `TestGenerate_AggregatorHasOrderEdge` ist ersatzlos entfernt, der Ordnungskanten-Wächter lebt als `TestMakefile_HasOrderEdge` in `internal/emit/makefile_test.go` und prüft die Eigenschaft (`record-gates: $(GATE_CHECKS)` u. a.) am `emit`-Const — Mutation 38 zeigt jetzt korrekt auf `internal/emit/makefile.go`. Die slice-034-F-1-Klasse „Wächter am toten Code" tritt nicht auf.
- geprüft, ohne Befund — **CLI `--lang` optional + sprachloser Fluss:** `run()` hat den Exit-2-Refuse-Block entfernt (`cmd/ai-harness-init/main.go:83-89`); unbekannte Sprache/Flags liefern weiter Exit 2 (via `bootstrap`/`Parse`). `bootstrap` verzweigt sauber über `hasLang`: Phase-1-Pre-Flight nimmt `.harness/skeleton` nur mit Sprache, `gen.Generate`/`wire.Place` laufen nur mit Sprache, `emit.Makefile` immer. `emitTargets(hasLang)` nimmt `emit.MakefilePath` immer in den Pre-Flight → eine kollidierende Root-`Makefile` wird sprachlos wie mit Sprache vor dem Emit gefangen; keine Kollision rutscht durch.
- geprüft, ohne Befund — **`wire` reiner Placer:** Makefile-Vorbedingung, `skeletonMakefile`-Const und `bytes`-Import restlos entfernt; `errors` weiter benutzt (`wire.go:68`), kein toter Import. `TestPlace_NoGatesTarget` zu Recht entfernt (die geprüfte Vorbedingung existiert nicht mehr). `TestPlace_PlacesVerbatim` prüft neu, dass wire **keine** Root-`Makefile` mehr platziert.
- geprüft, ohne Befund — **doc-only-Gate (LH-QA-01):** Der neue `full-smoke`-Block (`harness/tools/full-smoke.sh:122-158`) belegt ehrlich: sprachloses `make -j gates` == Exit 0, Marker `geprüft` + `Integritaet + Vollstaendigkeit` **present**, `--target (lint|build|test)` **absent**, kein Skelett-Artefakt am Ziel. Die Marker sind identisch zu denen des bereits grünen `--lang go`-Laufs (Zeile 64) — kein neuer, unbelegter Marker.
- geprüft, ohne Befund — **`emitAll`-Extraktion:** Rein gegen die gocognit-Schwelle ausgelagert, kein Verhaltenswechsel. Erste-Fehler-gewinnt bleibt (jeder Schritt `return err`), der Fehler-Print steht einmal in `bootstrap` (`main.go:175-178`). Die Pre-Flights (Phase 1 + Phase 3 mit Print+Return-Bindung, slice-025) bleiben in `bootstrap`. Emit-Reihenfolge unverändert (DocGate zuerst); `emit.Makefile` vor `wire.Place`, ohne Konflikt (Skelett trägt keine `Makefile` mehr).
- geprüft, ohne Befund — **Mutations-Reconciliation (§3.6):** 22 entfernt (die mutierte Vorbedingung + `TestPlace_NoGatesTarget` sind beide weg — konsistent). 38 auf `emit` umgezogen, greift die relocatete Ordnungskante. 14 nachgezogen (sed trifft die neue `fetchTargets`-Variable). 23 unverändert, aber weiter gültig (sein sed-Ziel `wire.Targets(...)` steht weiter im `hasLang`-Zweig; `TestRun_SkeletonKollisionSchreibtKeinEmit` prüft jetzt eine `go.mod`- statt `Makefile`-Kollision — ein reines Skelett-Ziel, das nur `wire.Targets` fängt). 41 neu (`hasLang := true` → sprachlos läuft `gen.Generate("")` → Exit 2 → `TestRun_SprachlosKeinExit2` rot). Sprachloser Init hat damit einen **netzlosen** Wächter (41 + `TestRun_SprachlosKeinExit2`), nicht nur `full-smoke`; die run()-Refuse-Rückkehr fängt der Test direkt (leere Args → code != 2). Keine Zusage ohne rot-gesehenes Gegenbeispiel entdeckt.
- geprüft, ohne Befund — **Hard Rules:** §3.2 keine Lint-Suppression (kein `//nolint`/`disable`; gocognit per legitimem Refactor, nicht Unterdrückung). §3.3 nicht verletzt — der Const wandert in eine **neue** Datei (`makefile.go`), kein `git mv`-Rename, die Similarity-Detection ist nicht im Spiel. §3.5 Gate-Änderung (Aggregator-Relocation + doc-only-Gate) durch ADR-0007 gedeckt. §3.6 s. o.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |
| INFO | 1 |

## Verdikt

**Merge-blockierend:** nein — 0 HIGH, 0 MEDIUM. Das einzige Finding (F-1) ist INFO und beschreibt eine per Plan/ADR **bewusst aufgeschobene** Folgearbeit (Idempotenz-Klassen, slice-036), kein Defekt dieses Slice.

Die Relocation `gen`→`emit` ist sauber vollzogen: kein toter Code, kein Wächter am toten Pfad, der Ordnungskanten-Wächter samt Mutation 38 vollständig mitgezogen. Der sprachlose CLI-Fluss ist netzlos bewacht, das doc-only-Gate ehrlich belegt (Marker present + Code-Gate/Skelett absent). Gutes, chirurgisches Slice.

**Übergabe:** Findings gehen an die Implementation. Der Report ersetzt keine Verifikation — DoD-/Spec-Konformität prüft der Verifier separat (Modul 11).
