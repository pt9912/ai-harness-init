# Review-Report: slice-095 (Code) — 2026-08-25

**Review-Art:** **Code**, erste Runde — geprüft wird der Diff gegen den Slice-Plan, gegen die
referenzierten Accepted-ADRs (nur gelesen, nicht angefasst) und gegen die Hard Rules
(Modul 10 §Drei Review-Arten). Kein vorheriger Review-Lauf zu `harness/tools/comment-claims.sh`
oder zu `harness/tools/hook-overhead.sh` (Datei ist neu).

**Gegenstand:** `f29524c` — **ein** Commit, **5 Dateien**
(`git show f29524c --stat` → `303 insertions(+), 12 deletions(-)`; neu:
`harness/tools/hook-overhead.sh`, 256 Zeilen). Baum sauber vor und nach jeder eigenen Probe
(`git status --short` → leer; zwei eigene `git worktree`-Kopien für Mutations-/Rot-Proben, beide
per `git worktree remove --force` entfernt, `git worktree list` danach ohne Rest).

**Skill:** `.harness/skills/reviewer.md` @ 1.4.0
**Modell:** claude-sonnet-5 · **Datum:** 2026-08-25

**Eingangs-Kontext:**

- **Diff:** `f29524c`, `git show f29524c` im Volltext
- **Slice-Plan:**
  [slice-095](../plan/planning/done/slice-095-hook-aufschlag-gemessen.md) (Stand
  `in-progress/`, vollständig gelesen)
- **ADRs, gelesen als Constraint, nicht angefasst:**
  [ADR-0022](../plan/adr/0022-erfassungsschicht-traeger-aus-dem-produkt-binaer.md) (Accepted,
  Annahme (c), Folgepflicht 9) — im Diff nicht berührt (`git diff --stat f29524c^ f29524c --
  docs/plan/adr/` leer);
  [ADR-0011](../plan/adr/0011-telemetrie-erfassung-policy.md) (Accepted, die 50-ms-Median-Schwelle,
  Festlegung 6 stdout/Exit-Klemme)
- **Hard Rules:** [`AGENTS.md`](../../AGENTS.md) §3.2, §3.4, §3.6, §3.7, §3.8
- **Konventionen:** [`harness/conventions.md`](../../harness/conventions.md) MR-005, MR-025
- **Vorherige Findings am gleichen Modul:** [slice-094-verify](2026-08-25-slice-094-verify.md)
  V-2 (LOW) — die Positions-Behauptung am selben `main.go`-Kommentarblock hatte keinen Sensor;
  dieser Diff adressiert genau sie
- **Regelwerk `v3.5.2`:** Index gelesen; `modul-10-review-harness.md` und `modul-08-agentenrollen.md`
  on-demand

---

## Selbst gefahren (nichts aus Commit-Message oder Übergabe übernommen)

| Kommando / Probe | Ergebnis | Behauptung | Urteil |
|---|---|---|---|
| `SAMPLES=50 make hook-overhead` (realer Lauf, aktueller Träger) | Median 2.6 ms, p99 9.4 ms, max 9.4 ms | Commit: „Träger Median 2,7 bis 2,8 ms" | **im gemeldeten Rahmen**, selbst reproduziert (andere Stichprobe/Host-Last, gleiche Größenordnung) |
| `SAMPLES=1 make hook-overhead` | lief korrekt, ein Payload, ein Sample | — (Eigenprüfung der `SAMPLES=1`-Einzelaufruf-Betriebsart) | geprüft, kein Befund |
| Block „Unterkommando-Zweig" in `cmd/ai-harness-init/main.go` hinter `os.Getwd()` verschoben, in eigenem `git worktree` | `make test-go` Exit 0 (alle 7 Pakete `ok`), `make span-check` Exit 0 | Commit: „gemessen am 2026-08-25 … beide Exit 0" | **stimmt, mechanisch reproduziert** |
| `bash -c 'LC_ALL=de_DE.utf8; t0=$EPOCHREALTIME; sleep 0.05; t1=$EPOCHREALTIME; echo $(( ${t1/./} - ${t0/./} ))'` gegen dieselbe Probe mit `LC_ALL=C` | ohne `LC_ALL=C`: Ergebnis ist exakt das Mikrosekunden-Feld von `t0` (Komma-Operator-Effekt); mit `LC_ALL=C`: korrekte Differenz (~51 ms) | Kopf-Kommentar: „Auf einem de_DE-Host … liefert still das Mikrosekunden-Feld von t0" | **stimmt, mechanisch reproduziert** — `LC_ALL=C` ist tragend, kein Stil |
| `harness/tools/hook-overhead.sh` Zeile für Zeile gegen `internal/span/{span,response,emit}.go` gelesen | Payload-Rekonstruktion trifft exakt die zwei benannten Achsen (Kommando-Text, Ergebnis-Inhalt); bei `tool_name":"Agent"` läuft `extractAgentResult()` auf der Füllzeichen-Kette, scheitert am `json.Unmarshal` in ein Objekt und liefert `AgentResult{}` — **genau** das, was der Kopf-Kommentar sagt („die Positiv-Liste … greift darin auf nichts zu") | Kopf-Kommentar: zwei nachgebaute Achsen, „Positiv-Liste greift auf nichts zu" | **bestätigt**, keine dritte unbenannte Achse mit relevantem Gewicht gefunden |
| `.claude/settings.json` gegen die Messschleife | `PostToolUse`/`PostToolUseFailure`/`SubagentStart` rufen `"$CLAUDE_PROJECT_DIR"/.harness/state/bin/ai-harness-init span-emit` **direkt**, ohne `bash -c`-Hülle — dieselbe Aufrufform, die das Skript misst (`"$@" < payload`) | — (eigene Prüfung: misst das Skript wirklich den Aufschlag oder etwas daneben?) | geprüft, kein Befund — kein zusätzlicher, unberücksichtigter Wrapper-Layer |
| `git worktree add --detach … f29524c`, `git mv harness/tools/hook-overhead.sh harness/tools/hook-overhead-renamed.sh`, `make docs-check` | `d-check: 377 Datei(en) geprüft, 2 Befund(e)` — `AGENTS.md:247` und `harness/README.md:72`, je `codepath-missing` | Commit: „Vier Rot selbst gesehen: … faerbt docs-check ueber codepaths rot (zwei Fundorte)" | **stimmt wörtlich, mechanisch reproduziert** |
| dieselbe Kopie, `harness/tools/hook-overhead.sh` unverändert, stattdessen `sed -i 's/^hook-overhead:/hook-overhead-renamed-target:/' Makefile` | `make docs-check` → `0 Befund(e)`; `make comment-claims` → `41 Datei(en) geprueft, 0 Befund(e)` | — (eigene Prüfung: deckt „codepaths liefert die Deckung" wirklich DoD (2), „das Kommando existiert und ist nennbar"?) | **Befund F-2** — siehe unten |
| dieselbe Kopie, ein erfundener Sensor-Verweis `Bewacht von make nicht-existierendes-ziel-xyz` in `cmd/ai-harness-init/main.go` angehängt | `make comment-claims` → `41 Datei(en) geprueft, 0 Befund(e)` | Commit: „ein Kommentar mit einem erfundenen make-Ziel laeuft gruen durch (gemessen, Exit 0)" | **stimmt wörtlich, mechanisch reproduziert** |
| `FILES=$(git ls-files 'internal/*.go' 'internal/**/*.go' 'cmd/**/*.go' \| grep -v '_test[.]go'; git ls-files 'harness/tools/*.sh' '.claude/hooks/*.sh')` (identisch zu `Makefile:134`) → `echo "$FILES" \| wc -l` | **41** — exakt die Dateizahl, die `comment-claims` selbst meldet | — (Kontrolle: Prüfbereich korrekt nachgebildet) | bestätigt, gleicher Prüfbereich |
| über demselben `$FILES`: case-insensitives `CLAIM`-Pattern ohne Treffer im case-sensitiven Pattern, **abzüglich** case-insensitiver `NEGATION`-Treffer (dieselbe `NEGATION`-Regel wie im Sensor, `IGNORECASE=1` versus keine) | **23 Zeilen in 10 Dateien** tragen eine Claim-Formulierung ausschließlich in einer vom case-sensitiven `CLAIM`-Pattern nicht erkannten Groß-/Kleinschreibung; 17 davon in `internal/span/*.go` + `internal/report/report.go` + `cmd/ai-harness-init/span_emit.go` (dem Telemetrie-Kern) | Commit nennt die Eigenschaft, keine Reichweiten-Zahl | **Befund F-1** — siehe unten |
| `make docs-check` (unveränderter `f29524c`-Stand) | `d-check: 377 Datei(en) geprüft, 0 Befund(e)` | Commit: „d-check 377/0" | **stimmt**, selbst gefahren |
| `make comment-claims` (unveränderter `f29524c`-Stand) | `comment-claims: 41 Datei(en) geprueft, 0 Befund(e)` | Commit: „comment-claims 41/0" | **stimmt**, selbst gefahren |
| `make shell-lint` | leer/Exit 0 | Commit: „shell-lint Exit 0" | **stimmt**, selbst gefahren |
| `grep -rn "hook-overhead" internal/emit/` | leer | Commit: „gemessen, kein Vorkommen von hook-overhead in internal/emit" | **stimmt** |
| `git show f29524c \| grep -n 'd-check:ignore\|nolint\|shellcheck disable'` | leer | — (§3.2) | geprüft, kein Befund |
| `git diff --stat f29524c^ f29524c -- docs/plan/adr/` | leer | — (§3.4) | geprüft, kein Befund |
| `sed -n '227,251p' AGENTS.md` gegen `## 3. Harte Regeln`/`## 4. Quality Gates` | Zeile 247 (neu) liegt in §4, §3 endet bei Zeile 227 | Commit: impliziert reine §4-Ergänzung | **stimmt** |
| `grep -n "^hook-overhead\|hook-overhead" Makefile` gegen `gates:`-Zeile und `.PHONY`/Prerequisite-Ketten | `hook-overhead` steht nur in `.PHONY` und als eigenständiges, prerequisite-loses Target; `gates:` listet es nicht; kein anderes Target hängt von ihm ab | Commit/Plan: „NICHT in gates … in keiner Prerequisite-Kette" | **stimmt** |
| `git cat-file -t d686787` + `git show d686787:Makefile \| grep span-emit-build` | Commit existiert, `span-emit-build` war dort ein reales Target | Kopf-Kommentar: Vergleichspunkt-Bauanleitung `git worktree add … d686787 && make -C … span-emit-build` | **stimmt**, Anleitung reproduzierbar |

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills.

### F-1 — MEDIUM: die case-sensitive Behauptungs-Erkennung von `comment-claims` hat eine gemessene, nicht berichtete Reichweite von 23 Zeilen in 10 Dateien, Schwerpunkt im Telemetrie-Kern

- `kategorie`: **MEDIUM**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §3.6 (Hard Rule, deren Sensor `comment-claims.sh` ist)
- `pfad`: `harness/tools/comment-claims.sh:32` (`CLAIM='garantiert|stellt sicher|bewacht|belegt|…'`,
  case-sensitiv angewandt); betroffene Fundorte u. a. `internal/span/span.go` (4),
  `internal/span/response.go` (4), `internal/span/emit.go` (3),
  `cmd/ai-harness-init/span_emit.go` (2), `internal/report/report.go` (4),
  `harness/tools/span-check.sh` (2), `harness/tools/mutate.sh` (1),
  `harness/tools/smoke.sh` (1), `harness/tools/full-smoke.sh` (1),
  `harness/tools/start-smoke.sh` (1)
- `befund`: Der Commit benennt die Fallgrenze **qualitativ** (an einem Block gemessen: „BEWACHT
  gruen, Bewacht gruen, bewacht rot") und lässt den Mechanismus bewusst unangetastet — das ist
  eine vertretbare Grenzziehung, wird hier aber ohne Reichweiten-Zahl getroffen. Über genau dem
  Prüfbereich, den `comment-claims` selbst scannt (`git ls-files` nach `Makefile:134`, 41
  Dateien), tragen **23 Kommentarzeilen in 10 Dateien** eine Claim-Formulierung ausschließlich in
  einer Groß-/Kleinschreibung, die das case-sensitive `CLAIM`-Pattern nicht erkennt (überwiegend
  großgeschriebenes „Bewacht"/„Belegt"/„UNBEWACHT" am Satzanfang) — nach Abzug der Zeilen, die
  auch case-insensitiv unter die `NEGATION`-Ausnahme fallen (eine Zeile: „UNBEWACHT, nicht
  unbewachbar"). **17 der 23** liegen in `internal/span/*.go`, `internal/report/report.go` und
  `cmd/ai-harness-init/span_emit.go` — exakt dem Paket, dessen ADR-0011/ADR-0022-Zusagen dieses
  Gate am dringendsten tragen soll. In jedem gemessenen Fall steht der genannte Sensor (noch)
  daneben; die Klasse „Zusage ohne Sensor" bliebe aber unsichtbar, entfernte jemand künftig genau
  einen dieser Sensor-Verweise, weil `comment-claims` den Block nie als claim-tragend erkennt (der
  case-sensitive `CLAIM`-Regex trifft nicht, `block_claim` bleibt 0).
- `verifizierbar`: **ja**, ohne Docker/Netz —
  ```
  FILES=$(git ls-files 'internal/*.go' 'internal/**/*.go' 'cmd/**/*.go' | grep -v '_test[.]go'; \
          git ls-files 'harness/tools/*.sh' '.claude/hooks/*.sh')
  ci=$(echo "$FILES" | xargs awk 'BEGIN{IGNORECASE=1}{c="garantiert|stellt sicher|bewacht|belegt|sorgt dafuer|sorgt dafür|verhindert";n="(garantiert|stellt sicher|bewacht|belegt|verhindert)[^.]{0,12}(nicht|kein|nie)";if($0~c&&$0!~n)print FILENAME":"FNR}')
  cs=$(echo "$FILES" | xargs awk '{c="garantiert|stellt sicher|bewacht|belegt|sorgt dafuer|sorgt dafür|verhindert";n="(garantiert|stellt sicher|bewacht|belegt|verhindert)[^.]{0,12}(nicht|kein|nie)";if($0~c&&$0!~n)print FILENAME":"FNR}')
  comm -23 <(echo "$ci" | sort) <(echo "$cs" | sort) | wc -l   # -> 23
  ```

### F-2 — MEDIUM: die Ersatz-Deckung „codepaths in `make docs-check`" für DoD (2) deckt nur die Datei-Pfad-Achse, nicht die im Plan verlangte Kommando-Namens-Achse

- `kategorie`: **MEDIUM**
- `quelle`: Slice-Plan [slice-095](../plan/planning/done/slice-095-hook-aufschlag-gemessen.md)
  DoD (2), Rot-Klausel: „das Kommando existiert und ist nennbar" — der Commit ersetzt die
  (nachweislich wirkungslose, s. u.) `comment-claims`-Zusage des Plans durch `codepaths` in `make
  docs-check` und stellt das als vollwertigen Ersatz dar
- `pfad`: `Makefile:283` (Ziel-Name `hook-overhead`), `AGENTS.md:247`, `harness/README.md:72`
  (beide referenzieren wörtlich `` `make hook-overhead` ``)
- `befund`: Empirisch geprüft (isolierter `git worktree`, `f29524c`): Der Plan-Befund selbst
  stimmt — ein erfundener `make`-Ziel-Verweis in einem Kommentar lässt `comment-claims` grün
  („41/0", mechanisch reproduziert). Die vom Commit genannte Ersatz-Deckung über `codepaths`
  wurde ebenfalls reproduziert: Verschiebt/löscht man **die Datei**
  `harness/tools/hook-overhead.sh`, färbt `docs-check` an genau den zwei Stellen rot, die den
  Datei-Pfad zitieren (`AGENTS.md:247`, `harness/README.md:72`). Benennt man dagegen
  **ausschließlich das Makefile-Ziel** um (`hook-overhead:` → `hook-overhead-renamed-target:`,
  die Datei bleibt unverändert liegen), bleiben sowohl `make docs-check` (377/0) als auch `make
  comment-claims` (41/0) grün — obwohl `AGENTS.md`/`harness/README.md` danach ein Kommando
  behaupten, das es nicht mehr gibt. Die Ersatz-Deckung trifft also nur das Verschwinden/Umbenennen
  der referenzierten **Datei**, nicht das Verschwinden/Umbenennen des referenzierten
  **Kommandos** — genau die Eigenschaft, die DoD (2)s ursprüngliche Rot-Klausel wollte. Diese
  engere Reichweite steht nirgends benannt, anders als an der Stelle im selben Diff, an der eine
  vergleichbare Grenze (Positions-Bewachung in `main.go`) ausdrücklich als „GRENZE, benannt statt
  verschwiegen" ausgewiesen ist.
- `verifizierbar`: **ja**, ohne Docker für den ersten Schritt, mit Docker für `docs-check`:
  ```
  git worktree add --detach <baum> f29524c
  cd <baum> && sed -i 's/^hook-overhead:/hook-overhead-renamed-target:/' Makefile
  make docs-check      # -> 0 Befund(e)
  make comment-claims  # -> 0 Befund(e)
  ```

## Negativbefunde

- **geprüft, ohne Befund — trägt die Messmethode (Prüfpunkt 1)?** Das Skript misst die
  Wanduhr-Zeit eines echten Subprozess-Starts+Laufs des Trägers mit gepipetem Payload — dieselbe
  Aufrufform, die `.claude/settings.json` für den echten Hook verdrahtet (kein zusätzlicher
  `bash -c`-Wrapper wie bei `PreToolUse`/`Stop`). Kein Fork zwischen den zwei Zeitnahmen
  (`EPOCHREALTIME` als Shell-Variable statt `date`-Subprozess). Die `LC_ALL=C`-Notwendigkeit wurde
  am eigenen Host unter `de_DE.utf8` reproduziert (siehe Tabelle). Eigener Lauf
  (`SAMPLES=50 make hook-overhead`) liegt in derselben Größenordnung wie die berichtete Zahl.
- **geprüft, ohne Befund — die zwei benannten Nachbau-Achsen der Payload.** Kommando-Text und
  Ergebnis-Inhalt sind als nachgebaut benannt; am Code gegengelesen (`internal/span/span.go`,
  `response.go`): für `tool_name":"Agent"`-Spans läuft `extractAgentResult()` auf der
  Füllzeichen-Kette leer, exakt wie der Kopf-Kommentar sagt („die Positiv-Liste … greift darin auf
  nichts zu") — keine dritte, unbenannte Achse mit relevantem Gewicht gefunden. Die Zahl bleibt
  eine Untergrenze für die Payload-Achse, wie ausdrücklich vermerkt.
- **geprüft, ohne Befund — der Kommentar-Nachzug in `main.go` und `Makefile` (Prüfpunkt 4).** Die
  Positions-Behauptung war messbar falsch (bestätigt slice-094-verify V-2) und ist jetzt als
  Grenze benannt, mit selbst reproduziertem Messergebnis (`make test-go` + `make span-check` beide
  Exit 0 nach Verschieben hinter `os.Getwd()`). Der zweite zugewiesene Kommentar-Nachzug
  (`span-report`-Kopf im `Makefile`, „für einen Bericht soll niemand einen Container starten
  müssen" vs. `host-bin`-Prerequisite) ist ebenfalls sachlich nachgezogen.
- **geprüft, ohne Befund — die Grenzen des Slice (Prüfpunkt 5).** `hook-overhead` steht weder in
  der `gates:`-Prerequisite-Zeile noch hängt ein anderes Target von ihm ab (`grep -n
  'hook-overhead' Makefile`); kein CI-Workflow referenziert es. Der Skript-Kopf markiert den
  gemessenen Stand ausdrücklich als „KEIN ERWARTUNGSWERT" und stellt jede genannte Zahl neben ihr
  exaktes Kommando (`MR-025`); `AGENTS.md`/`harness/README.md` wiederholen keine der Zahlen, nur
  den Zeiger auf den Skript-Kopf.
- **geprüft, ohne Befund — stehende Prüfungen.** Keine ADR im Diff berührt (`ADR-0011`,
  `ADR-0022` bleiben Accepted, unangetastet). `AGENTS.md`-Änderung liegt vollständig in §4, §3
  unberührt (Hard Rule 3.8). Kein Emit-Pfad berührt (`grep -rn hook-overhead internal/emit/` →
  leer). Keine Inline-Suppression (`d-check:ignore`/`nolint`/`shellcheck disable`) im Diff.
- **nicht abschließend geprüft — `make mutate`.** Laut Auftrag nicht gefahren; die Zahl „147 ok"
  aus dem Commit wird als Behauptung übernommen, nicht verifiziert. Die drei im Commit namentlich
  behaupteten Rot-Proben zu diesem Diff wurden stattdessen einzeln, unabhängig und mit eigenen
  Konstruktionen reproduziert (`test/mutations/154`-Positionsprobe, `codepaths`-Doppel-Rot,
  erfundener Sensor-Verweis) — das deckt die inhaltlich wichtigsten, slice-eigenen Proben ab,
  ersetzt aber keinen vollständigen Lauf über die übrigen Fälle.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 2 |
| LOW | 0 |
| INFO | 0 |

## Verdikt

**Frei — mit zwei MEDIUM-Auflagen, die vor dem `done/`-Move (Planner-Closure) einzuarbeiten sind,
nicht das Merge dieses Diffs blockieren.** Die tragende Messung selbst — Median des
Hook-Aufschlags gegen die 50-ms-Schwelle aus `ADR-0011`, samt Vergleichspunkt gegen den getrennten
Emitter aus `d686787` — ist methodisch sauber, korrekt als „keine Gate-Zusage" verdrahtet (nicht in
`gates`, keine Prerequisite-Kette, kein Erwartungswert) und in ihrer Größenordnung selbst
reproduziert; `ADR-0022` Annahme (c) ist damit tatsächlich gemessen statt vermutet, und keiner der
drei im Commit selbst gemeldeten Zusatz-Befunde (Rot-Rezept-Fehler in DoD (2), Case-Sensitivität,
Positions-Grenze in `main.go`) ist erfunden — alle drei wurden hier unabhängig reproduziert. Beide
MEDIUM-Findings betreffen nicht diese Messung, sondern die **Genauigkeit der Selbstauskunft über
zwei bereits vom Implementer selbst aufgeworfene Nebenbefunde**: F-1 fehlt die Reichweiten-Zahl zu
einer qualitativ richtig beschriebenen Sensor-Lücke (23 Zeilen/10 Dateien, größtenteils im
Telemetrie-Kern) — der Mechanismus bewusst nicht anzufassen ist vertretbar, aber ohne diese Zahl
lässt sich „richtige Grenze" nicht beurteilen. F-2 stellt eine gemessene Teil-Deckung
(`codepaths` auf der Datei-Pfad-Achse) als vollständigen Ersatz für eine Kommando-Namens-Zusage dar,
ohne die schmalere Reichweite so zu benennen, wie es der gleiche Diff an anderer Stelle
(Positions-Grenze in `main.go`) vorbildlich tut. Beide sind mit exaktem Reproduktionsweg belegt und
sollten in die Closure-Notiz (§7 des Plans), nicht müssen sie den aktuellen Diff aufhalten.
