# Verify-Report slice-037 — add-lang-Subkommando (wiederholbar, Mono-Repo)

**Rolle:** Verifier (Modul 11). **Datum:** 2026-07-23. **Commit:** c7715e5.
**Bezug:** LH-FA-04, ADR-0007. Slice-Plan:
docs/plan/planning/in-progress/slice-037-add-lang.md (§2 DoD).

Frischer Kontext, read-only. Sensoren gelaufen (alle Docker, erlaubt):
`make test` gruen · `make mutate` 43 ok / 0 Befund · `make full-smoke` Exit 0.
`make gates` (Stempel) bewusst NICHT gelaufen (Auftrag).

---

## DoD Punkt fuer Punkt

### DoD-1 — add-lang existiert, droppt Skelett/Fragment/blocked, wiederholbar (Mono-Repo)
**BESTAETIGT.**
- Dispatch VOR dem Flag-Parsing: cmd/ai-harness-init/main.go:75-77
  (`if len(args) > 0 && args[0] == "add-lang" { return runAddLang(...) }`) →
  runAddLang (main.go:121) → addLang (main.go:157) → wireLang (main.go:220).
- addLang droppt: Skelett unter `<pfad>` (wire.Place, main.go:230), Code-Gate-Fragment
  `harness/mk/<modul>.mk` (main.go:236-240), `blocked/<sprache>` (emit.BlockedFragment,
  main.go:242).
- Wiederholbar: TestRun_AddLangRepeatable (main_test.go) ruft `add-lang go apps/api`
  dann `add-lang go apps/web`, beide Exit 0, prueft dass apps/api/go.mod, apps/web/go.mod,
  harness/mk/apps-api.mk, harness/mk/apps-web.mk, tools/harness/blocked/go alle existieren.
- Asserts greifen wirklich: TestRun_AddLangDropsModule statet die konkreten Zieldateien
  UND prueft Fragment-Inhalt (`test-apps-api:`, `--target test -t apps-api:test apps/api`,
  `GATE_CHECKS += lint-apps-api build-apps-api test-apps-api`) via strings.Contains —
  keine Tautologie. Staging-Cleanup (.harness/skeleton weg) wird mitgeprueft.
- Mutation 44 (Dispatch-Vergleich neutralisiert → add-lang faellt in Default-Init) faerbt
  TestRun_AddLangDropsModule rot — im mutate-Lauf bestaetigt (ok 44-cli-addlang-dispatch).
- Realer Beleg: full-smoke droppt apps/api + apps/web ans doc-only-Repo, beide Module
  bauen (apps-api:test, apps-web:test Images), Exit 0.

### DoD-2 — Code-Gate-Fragment `<pfad>`-aware, kollisionsfrei
**BESTAETIGT.**
- gen.CodeGateFragment(lang, path, goVersion) (internal/gen/gen.go) → goFragment
  (internal/gen/golang.go): `context == "."` → unscoped goMkFragmentTmpl (Targets
  test/lint/build, `docker build .`), sonst goScopedMkFragmentTmpl (Targets
  test-<modul>/lint-<modul>/build-<modul>, `docker build <context>`).
- Root: TestCodeGateFragment_Root prueft `GATE_CHECKS += lint build test` +
  `--target test -t $(IMAGE):test .` + KEIN eigenes `gates:`-Target.
- Subdir: TestCodeGateFragment_ScopedSubdir prueft `test-apps-api:` …,
  `--target test -t apps-api:test apps/api`, `GATE_CHECKS += lint-apps-api …` UND dass
  die unscoped Targets (`\ntest:`, `\nlint:`, `\nbuild:`, `GATE_CHECKS += lint build test`)
  NICHT auftauchen — der Kollisions-Gegenbeweis ist explizit verankert.
- Kollisionsfreiheit zwei Module gleicher Sprache: apps/api → apps-api, apps/web → apps-web
  (gen.ModuleName, slash→dash). Getrennte Fragment-Dateien (harness/mk/apps-api.mk vs
  apps-web.mk), disjunkte Target-Namen, disjunkte Image-Tags (apps-api:test / apps-web:test).
  Die geteilten `GO_VERSION ?=` / `GOLANGCI_LINT_VERSION ?=` sind `?=` (konditional →
  Doppel-Definition harmlos). full-smoke faehrt `make -j gates` mit BEIDEN Fragmenten grün,
  prueft Marker `apps/api` UND `apps/web` UND `--target lint/build/test` im Recipe-Echo —
  waere ein Target kollidiert, liefe nur ein Modul/ein Kontext → rot. Real gruen gesehen.
- Mutation 45 (context-Auswahl invertiert) faerbt TestCodeGateFragment_ScopedSubdir rot
  (bestaetigt); zusaetzlich schlaegt TestCodeGateFragment_Root fehl (Root bekaeme scoped).

### DoD-3 — emit.Enforce sprach-agnostisch; blocked/<sprache> nur aus add-lang, skip-if-present
**BESTAETIGT.**
- emit.Enforce(targetDir, force) — kein lang-Param mehr (internal/emit/enforce.go).
  EnforcePaths() no-arg. Aufrufer nachgezogen: emitAll (main.go:366), emitTargets
  (main.go:416).
- blocked/<lang> NICHT mehr in EnforcePaths: TestEnforce_EmitsAllMechanicFiles prueft
  explizit dass `blocked/` NICHT in EnforcePaths() ist UND dass ein sprachloser Enforce
  kein tools/harness/blocked-Verzeichnis anlegt.
- emit.BlockedFragment(targetDir, lang, force) skip-if-present: bei vorhandenem
  blocked/<lang> ohne force → `return nil` (kein Clobber, kein Fehler); mit force
  ueberschreiben. Ohne gen-Profil (leere/unbekannte Sprache) no-op.
- TestBlockedFragment_SkipIfPresent legt adopter-eigenes blocked/go an, ruft
  BlockedFragment ohne force → Inhalt unveraendert (Assert greift auf Byte-Gleichheit),
  dann mit force → ueberschrieben. TestBlockedFragment_Drops prueft Toolchain-Inhalt +
  sprachlos-no-op.
- Mutation 46 (skip-if-present-Zweig `return nil` → `break`) faerbt
  TestBlockedFragment_SkipIfPresent rot (bestaetigt).

### DoD-4 — `--lang`-Init rueckwaertskompatibel (Init + gemeinsamer Platzierungskern)
**BESTAETIGT (mit Praezisierungs-Hinweis).**
- Der gemeinsame Kern ist **wireLang** (Skelett-Place + Fragment + blocked), NICHT
  literal `addLang`: emitAll ruft wireLang(targetDir, skelDir, ".", lang, …) (main.go:382),
  add-lang ruft dieselbe wireLang (main.go:189). addLang umschliesst wireLang zusaetzlich
  mit Aggregator-Vorbedingung + gen.Generate + preflight — im Init-Pfad laufen Generate
  (bootstrap, hasLang-Zweig) und der Aggregator (emit.Makefile) bereits vorher, ein
  addLang-Aufruf wuerde sie doppeln. Die Extraktion auf wireLang ist damit die saubere
  Faktorisierung; die DoD-Formulierung „dieselbe addLang-Funktion" ist dem Wortlaut nach
  ungenau, die SUBSTANZ (ein geteilter Platzierungspfad, `--lang` == One-Shot von add-lang)
  haelt.
- Root-Aequivalenz belegt: TestRun_AddLangRoot (`add-lang go .`) liefert die UNSCOPED
  Fassung (harness/mk/go.mk, `GATE_CHECKS += lint build test`) — byte-identisch zum
  --lang-One-Shot. full-smoke: der erste Block (--lang go) faehrt `make -j gates`
  weiterhin grün (Rueckwaertskompat real gesehen).

### DoD-5 — make full-smoke: add-lang go <pfad> → make -j gates grün inkl. Go-Gates; Guard blockt go+pip
**BESTAETIGT.**
- harness/tools/full-smoke.sh (slice-037-Sektion): add-lang go apps/api + apps/web ans
  doc-only-Repo, prueft Datei-Drops, `make -j gates` Exit 0, Marker `--target lint/build/test`
  + `apps/api` + `apps/web`, danach Guard blockt `go build` (`"decision": "block"`).
- Lauf real: Exit 0, Abschlusszeile „add-lang WIEDERHOLBAR (Mono-Repo): apps/api + apps/web
  koexistieren, make -j gates faehrt beide modul-scoped Go-Gates, blocked/go skip-if-present,
  Guard blockt go danach". apps-api:test/lint + apps-web:test/lint Images gebaut, golangci
  0 issues. `pip`-Boden ist im slice-036-Block derselben full-smoke abgedeckt (Guard blockt
  sprachlos schon pip). record-gates strikt zuletzt via Ordnungskante (Aggregator, slice-034).

### DoD-6 — make gates grün (Dogfood)
**BESTAETIGT (indirekt; Stempel bewusst ausgelassen).**
- `make test` grün (bats + go test, alle Pakete ok). `make mutate` faehrt als Gruen-Vorlauf
  `make test` + `make ci-lint` + `make smoke` grün, danach 43 ok / 0 Befund. `make full-smoke`
  Exit 0. Der Gate-Stempel (record-gates) wurde auftragsgemaess nicht erzeugt — die
  konstituierenden Checks sind alle grün gesehen.

### DoD-7 — Doku: architecture.md §4.2 nachgezogen
**BESTAETIGT.**
- spec/architecture.md §4.2: add-lang-Sequenzdiagramm auf „Skelett nach PFAD" +
  „harness/mk/MODUL.mk (Kontext PFAD)" aktualisiert; Fliesstext ergaenzt (modul-abgeleiteter
  Fragment-Name, modul-scoped Targets, Root-Rueckwaertskompat, blocked/<sprache>
  skip-if-present, `--lang` One-Shot, emit.Enforce sprach-agnostisch). §5 auf
  `harness/mk/<modul>.mk` nachgezogen. README: add-lang/Mono-Repo-Absatz ergaenzt
  (CLI-Vertrag beruehrt → README gezogen).

### DoD-8 — Closure-Notiz mit Steering-Loop-Eintrag
**OFFEN (erwartungsgemaess).** §7 des Slice-Plans noch leer — wird beim `done/`-Move
gefuellt. Keine Verifier-Blockade (Closure-Schritt, nach diesem Verify).

---

## ADR-0007-Grenze zu slice-038 (Idempotenz-Klassifikation)
**KORREKT GEZOGEN, nicht ueberschritten.**
- slice-037 macht NUR `blocked/<sprache>` skip-if-present (emit.BlockedFragment).
- Skelett-Dateien (main.go/go.mod/Dockerfile) tragen weiter refuse-if-present: wire.Place
  (VORPASS, main.go via internal/wire/wire.go:56) + preflightAbsent (main.go:394) +
  wireLang-Fragment-Kollisionscheck (main.go:222-228) — alle ohne --force abbrechend.
- Die breitere konvergente Skelett-Idempotenz (Re-Lauf DESSELBEN Moduls) ist NICHT
  implementiert und NICHT beansprucht: full-smoke und TestRun_AddLangRepeatable nutzen
  UNTERSCHIEDLICHE Pfade (apps/api vs apps/web), triggern also nie den Same-Module-Refuse.
  Commit-Message + Plan §6 benennen die Grenze explizit („hier gilt fuer Skelett-Dateien
  noch die bestehende --force-Semantik (refuse-if-present)" → slice-038). Kein stiller
  Uebergriff.

---

## Sensor-Zusammenfassung
- make test: gruen (go test alle Pakete ok, bats ok).
- make mutate: 43 ok, 0 Befund — 44 (Dispatch)→TestRun_AddLangDropsModule,
  45 (Scoping/Kontext)→TestCodeGateFragment_ScopedSubdir,
  46 (blocked skip-if-present)→TestBlockedFragment_SkipIfPresent,
  47 (ModuleName)→TestModuleName, alle rot gesehen.
- make full-smoke: Exit 0, slice-037-Mono-Repo-Zeile bestaetigt.

## Beobachtung (nicht blockierend)
- DoD-4-Wortlaut nennt „addLang-Funktion", der reale geteilte Kern ist wireLang. Die
  Rueckwaertskompat-Garantie haelt; nur die Benennung im Plan ist ungenau. Empfehlung:
  in der Closure-Notiz kurz klarstellen (wireLang = geteilter Platzierungskern), damit
  spaetere Slices nicht am Namen `addLang` haengen.

---

## Urteil

**DoD BESTAETIGT.**

Sieben von acht DoD-Punkten bestaetigt (DoD-1..7); DoD-8 (Closure-Notiz) ist der
erwartete offene Closure-Schritt, keine Verifier-Blockade. Die ADR-0007-Grenze zu
slice-038 ist sauber gezogen und nicht ueberschritten. Alle vier neuen Mutationen (44-47)
rot gesehen, full-smoke beweist den Mono-Repo-Nachweis-Kreis real (Exit 0).
Einziger nicht-blockierender Punkt: die DoD-4-Benennung (addLang vs wireLang).
