# Review — slice-081 (Baum tauschen, Pin ziehen, Verweise nachziehen)

| Feld | Wert |
|---|---|
| **Rolle** | Reviewer (Modul 10) — frischer Kontext, getrennt von Implementation, Architektur und Planung |
| **Review-Art** | Code-/Doku-Review — fertiger Diff gegen Plan, aktive ADRs und Hard Rules. **Nicht** DoD-Abhakung (Verifier) |
| **Gegenstand** | `git diff c6cc56f..HEAD` — `e4a4909` · `3871da4` · `b902b60` (Implementer) · `d72e6dd` · `d6f3ae8` (Architect) · `26aec2c` (Planner). `HEAD` = `26aec2c` |
| **Plan** | `docs/plan/planning/done/slice-081-baum-tauschen-pin-ziehen.md` |
| **Bindende ADRs** | [ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md) (Folgepflicht 1) · [ADR-0017](../plan/adr/0017-doku-gate-ausnahme-fuer-ein-eingefrorenes-adr.md) (1 und 2) · [ADR-0023](../plan/adr/0023-verweis-beschluss-traegt-ueber-den-sprung.md) (1 und 3) — alle *Accepted* |
| **Anforderungen** | `LH-QA-01` · `LH-QA-02` · `LH-FA-01` · `LH-FA-02` · `LH-FA-09` |
| **Skill-Version** | `.harness/skills/reviewer.md` 1.4.0 — **mit Vorbehalt, s. MEDIUM-1** |
| **Modell** | Claude Opus 5 (1M context) |
| **Kontext frisch** | ja — keine Aussage aus den Berichten der drei Läufe übernommen; jede Zahl unten in dieser Sitzung neu gefahren |

**Zum Output-Schema.** Der adoptierte Stand `v5.12.0` führt in
`modul-10-review-harness.md` §Ziel-Form: Reviewer-Skill ein **sechstes** Feld
(`kategorie · quelle · pfad · befund · verifizierbar · klasse`; `klasse` = stabile
Kurz-Bezeichnung des Fehlermusters). `.harness/skills/reviewer.md` führt fünf. Dieser Report
folgt dem **adoptierten Modul**, weil der Skill selbst auf es zeigt — und macht damit MEDIUM-1
an sich selbst sichtbar.

---

## Findings

### HIGH-1 — Der Tausch bricht zwei Lastenheft-Zusagen, und kein Carveout nennt das betroffene Ziel

- **kategorie:** HIGH
- **quelle:** [`LH-FA-02`](../../spec/lastenheft.md#lh-fa-02--zweiklassige-template-ablage-f3) (*„Der emittierte Stand ist damit out-of-the-box **gate-sicher**"*), [`LH-FA-01`](../../spec/lastenheft.md#lh-fa-01--repo-bootstrappen), [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6); Baseline-Regelwerk `modul-07-carveouts.md` §Ziel-Form: Carveout
- **pfad:** `.harness/baseline/v5.12.0/templates/` (Ursache `b902b60`) · `docs/plan/carveouts/CO-004-emitter-klassifikation-offen.md` (Kopf: *betroffenes Gate*) · `docs/plan/planning/done/slice-081-baum-tauschen-pin-ziehen.md` §6
- **befund:** `make smoke` und `make full-smoke` sind nach dem Tausch rot; das emittierte Repo trägt
  **10** `target-missing` über **23** Dateien, darunter drei Ziele aus den vier **neu** vendorten
  Vorlagen (`harness/conventions/MR-NNN-titel.md:15`, `docs/plan/planning/welle-results.md:67,83`).
  Über dem Vor-Stand `c6cc56f` sind beide Läufe grün (`19 Datei(en), 0 Befund(e)`, Exit 0) — der
  Tausch ist die Ursache, nicht eine Vorlast. `full-smoke` rechnet den Fehlschlag selbst dem Baum zu
  (*„Keine der 4 gefuehrten Formen einer nicht mit 2xx beantworteten Anfrage … steht in den 194
  gelesenen Zeilen; der Fehlschlag wird dem geprueften Baum zugerechnet"*). Weder
  [`CO-004`](../plan/carveouts/CO-004-emitter-klassifikation-offen.md) (*betroffenes Gate:* `test`,
  extensional geschlossen auf zwei bats-Fälle) noch
  [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md) (*betroffenes Gate:*
  `docs-check`) nennt `smoke` oder `full-smoke`; das Wort kommt im Slice-Plan und in beiden
  Carveouts kein einziges Mal vor.
- **verifizierbar:** ja — `make smoke` (Exit 2) und `make full-smoke` (Exit 2) am HEAD gegen
  `git archive c6cc56f | tar -x -C <tmp> && cd <tmp> && make smoke && make full-smoke` (beide Exit 0),
  gleiche Maschine, gleiche gepinnte Images
- **klasse:** Vertragsbruch-ohne-Carveout

### HIGH-2 — `CO-004` führt den bereits eingetretenen Schaden im Konjunktiv und leitet daraus die Reichweite der Ausnahme ab

- **kategorie:** HIGH
- **quelle:** Baseline-Regelwerk `modul-07-carveouts.md` §Ziel-Form: Carveout (*technische Begründung*), [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
- **pfad:** `docs/plan/carveouts/CO-004-emitter-klassifikation-offen.md:41,43,49`
- **befund:** Der Carveout begründet seine Reichweite damit, dass ein Nachziehen der Fixture die
  Emission auslöste: *„die vier **würden** also als gestempelte Singletons emittiert"*, *„`welle-results`
  **trüge** am flachen Emissions-Ort `](../observations.md)`"*, *„Die Zusage aus `LH-FA-02` … **fiele**
  damit"*. `courseSet()` ist Test-Fixture (`internal/emit/templates_test.go`, per `.dockerignore`
  vom realen Baum getrennt); der Emitter liest `.harness/baseline/*/templates/` direkt, und
  `emit.inScope` ist default-true. Die vier Vorlagen werden **heute schon** emittiert — der emittierte
  Bestand wächst 19 → 23 Dateien und enthält genau die im Konjunktiv beschriebenen Verweise. Der
  Konjunktiv verlegt einen gemessenen Ist-Zustand in eine Zukunft, die von einer Entscheidung
  abhängt, die den Zustand gar nicht auslöst.
- **verifizierbar:** ja — `make smoke`; die Befundzeilen nennen `docs/plan/planning/welle-results.md`
  und `harness/conventions/MR-NNN-titel.md` im emittierten Baum
- **klasse:** Konjunktiv-über-Ist-Zustand

### HIGH-3 — `CO-004` §Geltungs-Konfiguration behauptet eine Verdrahtung, die nicht existiert

- **kategorie:** HIGH (Kontext-Eskalation: Beobachtung im Gate-Pfad)
- **quelle:** Baseline-Regelwerk `modul-07-carveouts.md` §Ziel-Form: Carveout — *„Die Gate-Konfiguration nennt die `CO-<NNN>` im Gate-Output — sonst ist die Ausnahme eine stille Senkung ohne Begründung. … sichtbar sein muss sie in jedem Fall."*; [`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)
- **pfad:** `docs/plan/carveouts/CO-004-emitter-klassifikation-offen.md:84 ff.` (Tabelle, zwei Zeilen)
- **befund:** Die Tabelle führt zwei Zeilen in `test/courseset-fixture.bats`, deren Fehlermeldung
  *„`CO-004` und den Folge-Slice"* nennen soll. `git grep -n 'CO-004\|CO-005' -- test/ .d-check.yml Makefile`
  ist leer; der rote Lauf gibt `not ok 40` / `not ok 41` ohne jede Kennung aus. Die §Verifikation-Zeile
  *„Die `CO-004`-Nennung ist aus beiden Fehlermeldungen entfernt"* kann daher nie erfüllt werden.
  [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md) behandelt dieselbe
  Modul-7-Pflicht korrekt (*„Es gibt keine. Das ist der Befund, nicht eine Auslassung"*) — die zwei
  Carveouts desselben Commits geben zur selben Pflicht Verschiedenes an.
- **verifizierbar:** ja — `make test-bats` (Ausgabe der Fälle 40/41) gegen `git grep 'CO-004' test/`
- **klasse:** Behauptete-nicht-vorhandene-Mechanik

---

### MEDIUM-1 — Der Rollen-Skill deklariert eine Baseline, die es nicht mehr gibt; sein Output-Schema stammt aus der abgelösten Modul-Fassung

- **kategorie:** MEDIUM
- **quelle:** [ADR-0023](../plan/adr/0023-verweis-beschluss-traegt-ueber-den-sprung.md) Folgepflicht 1, [`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)
- **pfad:** `.harness/skills/reviewer.md:4`, `:17-19`, `:23`, §Output-Schema (Z. 68–71)
- **befund:** Der Kopf sagt `**Baseline:** Agents-Regelwerk v3.5.2 (Kurs-Welle 34)`; der Baum trägt
  `v5.12.0` (Welle 98). Der Versions-Kommentar begründet 1.4.0 damit, `modul-10-review-harness.md`
  habe *„real **nur** seine eingebettete Quell-URL"* geändert — für den neuen Stand ist das falsch:
  das Modul führt im Output-Schema ein sechstes Feld `klasse`, ersetzt die Steering-Loop-Pflege
  (Zählung über Finding-Klasse → Slice-Closure §7 → Beobachtungs-Register, Herkunfts-Anker
  `(seit welle-<NN>)`) und setzt für §Ablage *„Der Report ist ein Lauf-Beleg, kein Wissensspeicher"*.
  Der Skill führt fünf Felder und die alte Pflege-Regel. Kein Gate sieht das. Die Arbeit ist
  `slice-085` §3 zugewiesen (*„aus Modul 10 abgeleitet; das Modul hat ein Delta"*) — dem **letzten**
  Slice der Welle; §6 des Slice-Plans zählt den Skill nicht zu den Artefakten, die den
  Zwischenzustand tragen.
- **verifizierbar:** nein — kein Modul aus `.d-check.yml` liest Skill-Metadaten; nachweisbar per
  `diff <(git show b902b60^:.harness/baseline/v3.5.2/regelwerk/modul-10-review-harness.md) .harness/baseline/v5.12.0/regelwerk/modul-10-review-harness.md`
- **klasse:** Präsens-Aussage-gegen-gepinnten-Stand

### MEDIUM-2 — Der Tausch macht eine ausgewiesene Messung in `MR-026` falsch, und ihr Kommando läuft nicht mehr

- **kategorie:** MEDIUM
- **quelle:** [ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md) Festlegung 1 (*die Verwandlung von laut nach stumm*), [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 1
- **pfad:** `harness/conventions.md:1388` (Geltungsbereich) und `:1395` (Setzung 1)
- **befund:** Geltungsbereich und Beleg-Kommando adressieren
  `.harness/baseline/v3.5.2/templates/AGENTS.template.md`, das der Tausch entfernt hat — das
  abgedruckte `comm -12 … | wc -l` liefert am HEAD `0` statt der behaupteten **1**. Gegen den
  adoptierten Stand gefahren sind es **2** deckungsgleiche Überschriften: `### 3.3 git mv + Inhaltsänderung
  = zwei Commits` **und** `### 3.7 Ein Kommentar beschreibt, was da ist`. Der zweite Treffer widerlegt
  zugleich den Vorgriff-Satz in [`AGENTS.md`](../../AGENTS.md) §3.7 / `MR-022` / `MR-023`. Der Eintrag
  steht in Inline-Code, nicht in einem Link — kein Gate meldet es. Die Klasse ist `slice-082`
  („jeder Eintrag einzeln") implizit zugewiesen; `slice-131` schließt `harness/conventions.md`
  ausdrücklich aus, `slice-132` behandelt nur die Adresse.
- **verifizierbar:** nein — nachfahrbar mit
  `comm -12 <(grep -E '^### 3\.' AGENTS.md | sort) <(grep -E '^### 3\.' .harness/baseline/v5.12.0/templates/AGENTS.template.md | sort)`
- **klasse:** Präsens-Aussage-gegen-gepinnten-Stand

### MEDIUM-3 — Vier Zahlen in lebenden Artefakten, deren danebenstehendes Kommando sie nicht liefert (Steering-Loop-Signal)

- **kategorie:** MEDIUM
- **quelle:** [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert) Setzung 1 (*„trägt im selben Absatz das Kommando, das **genau sie** ausgibt, und wer sie schreibt, hat es über dem Baum gefahren, von dem sie spricht"*)
- **pfad:** siehe Liste
- **befund:** Vier im Prüfbereich neu geschriebene Zahlen reproduzieren mit ihrem eigenen Kommando nicht:

  | Ort | behauptet | Kommando liefert |
  |---|---|---|
  | `harness/conventions.md:1792` (`MR-030`) | **254** Link-Vorkommen / **68** eindeutige Ziele | **290** / **72** |
  | `docs/plan/planning/open/slice-114-…:204-206` | **132 869** Zeichen, *„mehr als das Fünfzehnfache"* | **148 721**, Faktor 16,9 |
  | `docs/plan/planning/open/slice-131-…:73` | **11** Nennungen in **6** Dateien | **14** in **8** (mit der im Satz angekündigten `slice-114`-Ausnahme: **12** in **7**) |
  | `docs/plan/planning/open/slice-132-…:105` | **1 572** Verweise / **533** lebend | **1 589** / **550** |

  In allen vier Fällen ist die Kennzeichnung *„kein Erwartungswert"* (Setzung 2) gesetzt — Setzung 1
  ist davon unberührt: die Zahl war zum Schreibzeitpunkt über einem anderen Baum gemessen als dem,
  über dem sie steht. `slice-114` zieht daneben eine **Folgerung** aus der Ziffer, die mit ihr
  wandert. Zwei schreibende Rollen, ein Commit-Range. **Nach `.harness/skills/reviewer.md`
  §Kontext-Eskalation ist die dritte Wiederholung derselben Klasse ein Steering-Loop-Signal** — der
  Träger fehlt, nicht die Regel.
- **verifizierbar:** nein — kein Modul prüft Zahlen gegen Läufe (`MR-025` §Kein Wächter, gemessen)
- **klasse:** Zahl-ohne-lieferndes-Kommando

### MEDIUM-4 — `slice-081` trägt nach der Plan-Änderung zwei einander ausschließende Abschluss-Bedingungen

- **kategorie:** MEDIUM
- **quelle:** Baseline-Regelwerk `modul-05-planning-harness.md` §Closure- und Lerneintrag-Regeln; Maintainability
- **pfad:** `docs/plan/planning/done/slice-081-baum-tauschen-pin-ziehen.md` §2 DoD (4) gegen `:184` §5 Closure-Trigger
- **befund:** `26aec2c` ersetzt DoD (4) von *„`make gates` grün"* durch *„`make gates` läuft, und dieser
  Slice hinterlässt keinen unzugeordneten Befund"* und begründet das ausführlich. §5 Closure-Trigger
  blieb unverändert: *„DoD vollständig, `make gates` nach dem Tausch **grün**, Closure-Notiz
  geschrieben."* Der Plan verlangt damit an einer Stelle, was er an der anderen ausdrücklich
  verwirft; welche der beiden den Abschluss regiert, sagt er nicht.
- **verifizierbar:** nein — das `planning`-Modul steht nicht in `modules:` der `.d-check.yml`
- **klasse:** Plan-interner-Widerspruch

---

### LOW-1 — Der neue `scan.ignore`-Kommentar zählt fünf Module, die Konfiguration darunter führt sechs

- **kategorie:** LOW
- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.7 (*ein Kommentar beschreibt, was da ist*)
- **pfad:** `.d-check.yml:23` gegen `:29`
- **befund:** *„Der Ausschluss wirkt datei-weit ueber alle **fuenf** Module"*; `modules:` führt
  `[links, anchors, ids, matrix, codepaths, spans]` — sechs. Die Ziffer stammt aus
  [ADR-0017](../plan/adr/0017-doku-gate-ausnahme-fuer-ein-eingefrorenes-adr.md) §Konsequenzen, die
  `spans` ebenfalls nicht mitzählt (und als *Accepted* nicht korrigierbar ist);
  [`MR-029`](../../harness/conventions.md#mr-029--der-scanignore-zensus-wandert-und-sein-dritter-grund-ist-keine-scoping-aussage)
  formuliert denselben Sachverhalt im selben Slice korrekt als *„über alle aktiven Module"*.
- **verifizierbar:** nein
- **klasse:** Zahl-ohne-lieferndes-Kommando

### LOW-2 — Die zwei neuen Adaptions-Einträge tragen das Pflichtfeld des adoptierten Stands nicht

- **kategorie:** LOW
- **quelle:** Baseline-Regelwerk `grundlagen-harness-dateien.md` §harness/conventions.md als Konventionsspeicher (*„Pflichtfelder eines Eintrags: Datum, Geltungsbereich, `Ersetzt-Baseline-Regel`, Adaption, Begründung, Auflösungs-Trigger"*); `modul-02-harness-bootstrap.md` (*„neue **Pflicht**-Felder … verlangen Nacharbeit am gefüllten Artefakt — sonst behauptet das Repo eine Baseline-Konformität, die seine Artefakte nicht tragen"*)
- **pfad:** `harness/conventions.md:1636 ff.` (`MR-029`), `:1738 ff.` (`MR-030`)
- **befund:** Beide Einträge entstehen **nach** dem Tausch, also unter dem neuen Stand, und führen
  `Ersetzt-Baseline-Regel` nicht. Aus derselben Baseline-Zeile übernehmen sie die zwei **bedingten**
  Felder (*Löst auf*, *Ausgelöst durch Baseline-Stand*) — die Zeile ist also gelesen worden. Die
  Klasse ist `slice-083` zugewiesen (*„Das trifft jeden überlebenden Eintrag dieses Repos"*); diese
  zwei sind keine überlebenden, sondern neue, und vergrößern den dortigen Rückstand.
- **verifizierbar:** nein
- **klasse:** Vorlagen-Pflichtfeld-fehlt

### LOW-3 — Die zwei neuen Carveouts erscheinen in keiner Bindung-Spalte

- **kategorie:** LOW
- **quelle:** Baseline-Regelwerk `grundlagen-harness-dateien.md` §harness/README.md als Einstiegspunkt (*„die Bindung-Spalte der Tabelle (`Target | Vertrag | Bindung`) verweist auf die `CO-<NNN>`-ID"*) — wortgleich schon in `v3.5.2`
- **pfad:** `harness/README.md` §Sensors, Zeilen zu `make test` und `make docs-check`
- **befund:** Beide Ziele sind seit diesem Slice strukturell rot; ihre Bindung-Spalte führt weiter
  nur `ADR-0003`/`ADR-0004` bzw. `MR-010`. Das Muster ist **vorbestehend** (`CO-001` fehlt dort seit
  2026-07-21) und damit keine Neuerung des Tauschs — die zwei neuen Carveouts fügen ihm zwei
  Instanzen hinzu.
- **verifizierbar:** nein
- **klasse:** Register-Eintrag-fehlt

### LOW-4 — `CO-004` zitiert einen Fallnamen, den es so nicht gibt

- **kategorie:** LOW
- **quelle:** Baseline-Regelwerk `modul-07-carveouts.md` §Ziel-Form: Carveout (*Geltungsbereich*)
- **pfad:** `docs/plan/carveouts/CO-004-emitter-klassifikation-offen.md`, Kopf §Geltungsbereich
- **befund:** Der Geltungsbereich nennt *„der reale Satz liefert genau **N** in-scope-Templates"*; der
  Fall heißt `fixture: der reale Satz liefert genau 17 in-scope-Templates`. Eine extensional
  geschlossene Menge, deren Elemente sich nicht per Name auffinden lassen, ist nur der Absicht nach
  geschlossen. In derselben Aufzählung steht *„der Adaptions-Eintrag der Adaptions-Eintrag
  `MR-NNN-titel`"* doppelt.
- **verifizierbar:** ja — `grep -n '^@test' test/courseset-fixture.bats`
- **klasse:** Zitat-trifft-die-Quelle-nicht

---

### INFO-1 — `spec/spezifikation.md` wurde in der Architect-Rolle geändert; die Zuständigkeit ist offen, nicht geregelt

- **kategorie:** INFO
- **quelle:** [`AGENTS.md`](../../AGENTS.md) §3.8, [ADR-0015](../plan/adr/0015-rollen-eigentum-an-norm-artefakten.md) §Kontext
- **pfad:** `d6f3ae8`
- **befund:** §3.8 bindet nur `AGENTS.md` §3 und `harness/conventions.md`; für das Technik-Stratum
  benennt keine Quelle eine schreibende Rolle. Die Commit-Message deklariert das ausdrücklich und
  belegt die Messung über alle 26 Regelwerk-Dateien. Die Commit-Trennung ist eingehalten (`d72e6dd`
  trägt genau `harness/conventions.md`, `d6f3ae8` genau `spec/spezifikation.md`, beide nennen die
  Rolle). Kein Verstoß — festgehalten, weil die Frage mit jedem weiteren Lauf wiederkehrt.
- **verifizierbar:** nein
- **klasse:** Zuständigkeit-ohne-Quelle

### INFO-2 — `make gates` bricht bei `docs-check` ab; die Aussage „kein weiterer Rot-Zustand" ist daraus nicht ablesbar

- **kategorie:** INFO
- **quelle:** Maintainability
- **pfad:** `Makefile:299` (`gates: baseline-verify docs-check lint build test …`)
- **befund:** `make gates` endet nach `docs-check` mit Exit 2; die zehn nachfolgenden Ziele laufen nie.
  Einzeln nachgefahren sind sie grün (s. Negativbefunde). Wer die Zuordnung der roten Zustände aus
  **einem** `gates`-Lauf ableitet, hat neun Ziele nicht gesehen.
- **verifizierbar:** ja — `make gates` (Ausgabe endet bei `d-check.mk:66`)
- **klasse:** Gate-Abbruch-verdeckt-Rest

### INFO-3 — `make mutate` ist ungeprüft geblieben

- **kategorie:** INFO
- **quelle:** Auftrags-Auflage
- **pfad:** —
- **befund:** Der Lauf war ausdrücklich untersagt. Ob unter `smoke`/`full-smoke` hinaus noch ein
  Rot-Zustand liegt, ist für diese Achse **nicht** geprüft und wird hier nicht als grün ausgegeben
  ([`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)).
- **verifizierbar:** ja — `make mutate`
- **klasse:** Nicht-geprüft

---

### INFO-4 — Ein untrackter `.tmp/`-Bestand geht in den Gate-Stempel ein (außerhalb des Diffs)

- **kategorie:** INFO
- **quelle:** [`MR-003`](../../harness/conventions.md#mr-003--härtung-inhaltsbasierter-nachweis-und-sub-shell-prüfung), Maintainability
- **pfad:** `.gitignore` gegen `harness/tools/working-tree-hash.sh:14` und `.d-check.yml:28` (`scan.ignore` führt `.tmp/**`)
- **befund:** Während dieses Reviews entstand im Repo-Wurzelverzeichnis ein untrackter Bestand
  `.tmp/adrcells/*.cell` (23 Dateien) und `.tmp/idx-nopipe.md` (38 KB) — **nicht** von den Kommandos
  dieses Laufs und von keinem Skript des Repos (`grep -rn 'adrcells' .` außerhalb von `.git` ist
  leer). `harness/tools/working-tree-hash.sh` bildet den Hash über
  `git ls-files --cached --others --exclude-standard`; `.gitignore` führt `.tmp/` **nicht**, also
  zählt der Bestand mit, und `.claude/hooks/stop-require-gates.sh` verlangt zusätzlich ein leeres
  `git status --porcelain=v1`. `.d-check.yml` kennt die Konvention (`scan.ignore` führt `.tmp/**`),
  `.gitignore` nicht — dieselbe Asymmetrie, die dort für `/bin/` und `/dist/` bereits als
  Review-Befund F-5 dokumentiert ist. **Vorbestehend und außerhalb dieses Diffs**; hier notiert,
  weil ein Schreiber im Arbeitsbaum unterwegs war, während der Diff geprüft wurde. Der **getrackte**
  Baum hat sich dabei nicht bewegt (`git status --porcelain` zeigt ausschließlich `??`-Einträge,
  `HEAD` unverändert `26aec2c`) — das Review-Ergebnis ist davon unberührt.
- **verifizierbar:** ja — `bash harness/tools/working-tree-hash.sh` vor und nach einem Schreibvorgang unter `.tmp/`
- **klasse:** Arbeitsbaum-Verschmutzung-durch-Nebenprodukt

## Negativbefunde (geprüft, ohne Befund)

- **Vendored Baum und Pin.** `make baseline-verify` → `v5.12.0 OK — 51 Dateien`. Genau ein
  `<tag>`-Verzeichnis; die fünf mechanischen Stellen (`Makefile` ×2, `internal/fetch/baseline.go` ×2,
  `.d-check.yml` `sources`) tragen durchgängig `v5.12.0` und denselben sha256.
- **ADR-0016 Festlegung 1 — Sortierung selbst am Text beider Bäume geprüft.** Alle 12 in
  `spec/spezifikation.md` gezogenen Ziele einzeln gegen `v5.12.0` gehalten: Anker
  `#kernidee-modul-15`, `#span-audit-attribut-regeln`, `#token-attributions-regeln`,
  `#cache-counter-regeln`, `#rollen-sequenz-für-einen-slice` existieren; die zitierten Sätze
  (*„Ein Attribut ohne Incident-Frage fliegt raus"*, *„Korrelations-IDs zu Slice/PR/Agent-Rolle"*,
  Pflicht-Minimum, `slice.id`/`requirement.id`/`adr.id`/`agent.role`, *„Eine einzelne Metrik
  `cache.hit_ratio` reicht nicht"*, das Sammelposten-Zitat) stehen dort whitespace-normalisiert
  verbatim. `modul-15-observability.md` hat gegenüber `v3.5.2` **zwei** Änderungen, beide
  unschädlich für die zitierten Stellen.
- **Der eine Fall, in dem Adresse hält und Aussage brach, ist gefunden und behandelt.**
  `modul-08-agentenrollen.md` schreibt `participant I as Implementation` (`v3.5.2`) gegen
  `participant I as Implementer` (`v5.12.0`). Im Technik-Stratum ist der Abweichungs-Satz gestrichen
  statt der Link mechanisch gezogen (`d6f3ae8`); im Adaptions-Block bleibt der Link bewusst rot und
  trägt [`CO-005`](../plan/carveouts/CO-005-adaptions-block-datierter-beleg.md). Die sechs
  Rollen-Namen sind kleingeschrieben zeichengleich mit den sechs Bezeichnern (`diff`-Ausgabe leer,
  selbst gefahren).
- **`spec/spezifikation.md:36` — die eine Aussage, bei der ein Tausch hätte fabrizieren können.**
  *„Die Abschnittsnummern sind die der vendored Vorlage"*, auf `v5.12.0` gezogen: die Gliederung
  1–7 der Vorlage ist über beide Tags identisch. Der Zug ist wahr, nicht bloß auflösbar.
- **ADR-0016 Festlegung 4 — Zeitdokumente.** Genau **4** Adressen in **3** Dateien entfallen
  (`done/slice-076` ×2, zwei `docs/reviews/`-Kopfzeilen); der sichtbare Text steht in allen vier
  Fällen Zeichen für Zeichen. Kein Satz geändert.
- **ADR-0017 Folgepflicht 1.** Der `scan.ignore`-Eintrag ist gesetzt, extensional auf
  `docs/plan/adr/0013-technik-stratum-als-zielort.md` geschlossen, mit Begründung und
  ADR-Zeiger im Kommentar. Kein zweiter Eintrag hinzugekommen (Zensus 5, davon 4 vorbestehend).
- **ADR-0017 Folgepflicht 2 — der Ort.** Die Pflicht nennt eine **Wirkung** (Zahl, Klassifikation,
  Aufnahme-Grenze sind zu lesen) und einen Ort (`MR-001`). Die Append-only-Disziplin des
  adoptierten Stands verbietet den Edit dort ausdrücklich und benennt den Fall eigens (*„Rückbau
  ist ein neuer Eintrag, kein Edit … Die alte Zeile ist die historisch korrekte Aussage über den
  damaligen Zustand"*, `modul-02-harness-bootstrap.md`; *„Einträge werden nie überschrieben"*,
  `grundlagen-harness-dateien.md`). Beide Zitate selbst gegen den Baum gehalten, ebenso das
  `v3.5.2`-Gegenstück (`templates/harness/conventions.template.md` Z. 82–84) und die Aussage, dass
  es am neuen Stand nicht mehr existiert (`grep -rn` leer). `MR-029` trägt Zensus (**5**,
  Kommando gefahren), die neue Drei-Gründe-Klassifikation und die Grenze verbatim, verweist auf
  `MR-001` und markiert dessen Aussage als überholt, ohne sie zu berühren; die Hauspräzedenz
  (`MR-021` gegen `MR-019`) trägt. **Die Pflicht ist erfüllt** — die Wirkung ist hergestellt, der
  genannte Ort verweist auf den Nachfolger. Kein Finding.
- **ADR-0023 Festlegung 3 (`versions` verworfen).** Unabhängig nachgemessen: `--print-config` des
  gepinnten Digests `sha256:5ea03…` zeigt für `links` **genau eine** Option (`resolve-from`);
  `ignore-refs` steht unter `codepaths`, `exempt-paths` unter `ids`/`matrix`/`codepaths`/`diagrams`/
  `versions`/`structure`. Für `links` existiert keine Referenz-Ausnahme. `CO-005`s Behauptung trägt;
  seine Aufzählung der `exempt-paths`-Module ist unvollständig (vier von sechs), was die tragende
  Aussage nicht berührt.
- **`CO-005` insgesamt.** Sechs Pflicht-Header-Felder vorhanden, Geltungsbereich extensional auf
  **eine** Referenz geschlossen, Folge-Slice benannt, Auflösungs-Trigger beobachtbar und
  zweiteilig (Befund weg **und** `.d-check.yml`-Diff leer), Geltungs-Konfiguration ehrlich als
  nicht existent ausgewiesen. **Keine Gate-Senkung** — die Config bleibt unberührt, `docs-check`
  bleibt rot. `AGENTS.md` §3.5 ist nicht berührt.
- **Die vier Kopplungs-Zähne — nachgefahren, nicht übernommen.** Über einer isolierten Kopie
  außerhalb des Repos (`git ls-files -z | tar --null -T - -cf -`), je eine Sonde, danach
  zurückgenommen: (1) `DefaultTag` verbogen → nur `--- FAIL: TestDefaultTag_MatchesBaseline`;
  (2) `DefaultBaselineSHA256` verbogen → nur `--- FAIL: TestDefaultBaselineSHA256_MatchesMakefile`;
  (3) `sources`-URL-Tag verbogen → `not ok 183`, `ok 182`; (4) `sources`-sha256 verbogen →
  `not ok 182`, `ok 183`. Jede Sonde färbt genau ihren Sensor, die jeweils andere Seite bleibt grün.
  **Die Behauptung der drei Läufe ist bestätigt.**
- **§3.8 Commit-Trennung.** `d72e6dd` → genau `harness/conventions.md`; `d6f3ae8` → genau
  `spec/spezifikation.md`; beide nennen die Rolle in der Message. Kein Architect-Artefakt in einem
  Implementer- oder Planner-Commit.
- **§3.4 ADR-Immutabilität.** `git diff --name-only c6cc56f..HEAD -- docs/plan/adr/` ist leer;
  `AGENTS.md` und `CLAUDE.md` ebenso unberührt.
- **§3.7 Kommentar-Behauptung in `internal/emit/templates.go`.** *„Der vendored Satz `v5.12.0`
  traegt kein solches Wort"* — nachgemessen: alle 10 `make <wort>`-Nennungen im neuen
  Vorlagen-Satz sind ziel-förmig, kein Fließtext-Wort. Der neue Fundort
  (`welle-results.template.md`) gehört zu den vier neuen Vorlagen.
- **Restliche Gate-Ziele einzeln gefahren.** `baseline-verify`, `lint`, `build`, `test-go`,
  `shell-lint`, `ci-lint`, `comment-claims` (46 Dateien / 0 Befunde), `host-bin`, `span-check` —
  alle Exit 0. `docs-check`: **1** Befund, und es ist genau `harness/conventions.md:1009`
  (`CO-005`). `test-bats`: **2** Fehlschläge, und es sind genau die Fälle 40/41 (`CO-004`).
  **Innerhalb von `make gates` liegt kein dritter Rot-Zustand darunter** — außerhalb schon (HIGH-1).
- **`slice-114`-Nachmessungen des Planners.** Sieben von acht Zahlen reproduzieren exakt
  (7 322 · 16 161 · 8 785 · 34 · 14 083 · 6 047 · 154), die acht Pflicht-Abschnitte der Vorlage
  ebenso; die Zitate *„drei bis fünf geordnete Zeiger"*, *„Menschen-Hälfte des Einstiegs"* und
  *„≤ 3 Liefer-Punkte"* stehen verbatim in den genannten Abschnitten. Die achte Zahl ist MEDIUM-3.
- **Carveout-Index.** `docs/plan/carveouts/README.md` führt CO-004/CO-005 unter *Aktiv*, mit Gate
  und Datum; CO-002 unter *Permanent*, CO-003 unter *Aufgelöst*. Dateibestand deckt sich.
- **`3871da4` (Link-Abgleich nach dem Move).** 14 Verweise über 7 Dateien, je `+1/-1`, kein Satz
  geändert; `docs-check` danach 0 Befunde.

---

## Kategorie-Summary

| Kategorie | Anzahl | Klassen |
|---|---|---|
| HIGH | 3 | Vertragsbruch-ohne-Carveout · Konjunktiv-über-Ist-Zustand · Behauptete-nicht-vorhandene-Mechanik |
| MEDIUM | 4 | Präsens-Aussage-gegen-gepinnten-Stand (2×) · Zahl-ohne-lieferndes-Kommando · Plan-interner-Widerspruch |
| LOW | 4 | Zahl-ohne-lieferndes-Kommando · Vorlagen-Pflichtfeld-fehlt · Register-Eintrag-fehlt · Zitat-trifft-die-Quelle-nicht |
| INFO | 4 | Zuständigkeit-ohne-Quelle · Gate-Abbruch-verdeckt-Rest · Nicht-geprüft · Arbeitsbaum-Verschmutzung-durch-Nebenprodukt |

**Wiederholte Klasse in diesem Lauf:** *Zahl-ohne-lieferndes-Kommando* fünfmal (MEDIUM-3 ×4,
LOW-1), *Präsens-Aussage-gegen-gepinnten-Stand* zweimal. Beide überschreiten die
Steering-Loop-Schwelle aus `.harness/skills/reviewer.md` §Kontext-Eskalation.

## Verdikt

**NICHT KONFORM — blockiert.**

Der mechanische Kern des Slice ist sauber und über den Erwartungen: der Baum steht auf `v5.12.0`,
die fünf Pin-Stellen tragen ihn, die vier Kopplungs-Zähne sind hier unabhängig rot gesehen worden,
die Anker-Einzelprüfung hat den einen Fall gefunden, in dem ein `sed` ein Zitat fabriziert hätte,
und ADR-0017 Folgepflicht 2 ist trotz des Vorlagen-Konflikts erfüllt statt umgangen.

Was blockiert, liegt außerhalb von `make gates` und außerhalb beider Carveouts: der Tausch bricht
`LH-FA-02` **und** `LH-FA-01` im emittierten Repo — nachgewiesen gegen den Vor-Stand, auf derselben
Maschine, mit denselben Images — und der einzige Carveout, der die Ursache berührt, beschreibt den
eingetretenen Schaden im Konjunktiv und behauptet daneben eine Gate-Ausgabe, die es nicht gibt.
Ein Slice, der eine Rang-1-Zusage bricht, braucht dafür einen Posten, der den Bruch beim Namen
nennt; welchen Weg dieser Posten nimmt — eigener Carveout, Erweiterung von `CO-004`, Rückführung
nach `next` —, ist eine Planner-Entscheidung und nicht Gegenstand dieses Reports.
