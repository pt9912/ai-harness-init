# Review-Report: slice-mutate-fall-filter-und-die-belegform-vereinigung — 2026-09-26

**Review-Art:** Werkzeug-, Test-, Mutations- und Doku-Diff gegen Plan, ADR-0035 (`Proposed`, nirgends als `Accepted` behauptet) und Hard Rules (Modul 10). Nicht gegen die DoD (das ist der Verifier).

**Gegenstand:** `git diff cbd0ffdf..HEAD` (HEAD `d20ebcfc`, Baum sauber, Stempel gedeckt): `070af9f5` (`harness/tools/mutate.sh` Filter und Teillauf, sieben Wächter in `test/mutate-driver.bats`, Anker von Fall 263), `20878b5d` (Mutations-Fälle 453 bis 457), `d20ebcfc` (`harness/sensors/mutate.md`, README-Zeile) plus die Move-/Marker-Commits `d40fb021` (reiner Move, `git diff -M` 99 % ohne Inhaltsänderung im Move-Commit selbst: 0 Einfügungen), `09159a4a` (Verweis-Nachzug, 2 Zeilen im Schwester-Slice), `372c0ea2` (Ruhe-Marker der Roadmap, drei Zeilen).

**Plan-Bezug:** Slice `slice-mutate-fall-filter-und-die-belegform-vereinigung` (Ziel, §1 Abgrenzung, §3, §6 R1 bis R5, §8) — Kennung, nicht Pfad (`AGENTS.md` §3.11). **Constraint:** `ADR-0035` (`Proposed`; Fitness-Zeile *ein Lauf mit Befund hinterlässt keinen Beleg*, Festlegung 4), `MR-071`, `MR-025`, `LH-QA-02`, `AGENTS.md` §3.6, §3.7, §3.9, §3.10, §3.11.

**Skill:** `.harness/skills/reviewer.md` @ Version 2.0.0 (2026-09-13)
**Modell:** Sonnet 5 · **Datum:** 2026-09-26

**Eingangs-Kontext:** Diff · Slice-Plan · `ADR-0035` · `MR-071` · `MR-025` · `LH-QA-02` · `AGENTS.md` §3 · `harness/tools/mutate.sh` (vollständig gelesen: Kopf, `select_cases`, `report_partial`, `main()`, `clear_belief`/`finalize_belief`/`write_belief`, Trap-Handler) · `test/mutate-driver.bats` · `test/mutations/263`, `264`, `453` bis `457` · `harness/sensors/mutate.md` · `harness/README.md`. Der Implementer-Bericht war Behauptung; Anker, Fälle, Text und Läufe sind selbst gelesen und gefahren.

**Eigene Sensor-Läufe dieses Laufs** — Mutationen und bats in Scratchpad-Kopien von `git archive HEAD` (kein `.git`; alle anderen `.bats`-Dateien in der Kopie entfernt, damit nur `test/mutate-driver.bats` läuft; Docker-only im bats-Bild des Makefiles über `make test-bats`); der echte Teillauf über `make mutate`; **kein voller `make mutate`**, kein Tap-Zugriff, keine Host-Toolchain.

| Lauf | Ergebnis |
|---|---|
| Kopie unverändert, `make test-bats` | `1..64`; einziges `not ok`: `7 driver: die Kopie traegt den Sensor-Bedarf inklusive .git` (bekanntes Kopie-Artefakt, `.git` fehlt) |
| Fall 453 angewandt (`bash test/mutations/453-…sh` in der Kopie) | `diff`: genau eine Zeile eingefügt (`finalize_belief "$belief_key"` hinter `report_partial`); `not ok 62 … schreibt den Beleg-Slot nie` mit `[ ! -e …mutate-passed.key ]' failed` (der `# expect:`-Test, die Meldung ist die behauptete Ursache: der Slot wurde geschrieben) und zusätzlich `not ok 63` (`cmp: … No such file`, weil der Befund-Teillauf über `finalize_belief` den stehenden Slot löscht — dieselbe Mutation, zweiter Zeuge) |
| Fall 454 | genau eine Zeile geändert (`[ -n "$partial" ] \|\| clear_belief` → `clear_belief`); `not ok 63 … laesst einen stehenden Beleg byte-gleich stehen` mit `cmp: …/mutate-passed.key: No such file or directory` — der Slot wurde entwertet, die Ursache passt |
| Fall 455 | genau eine Zeile (`— kein Beleg (der` → `— Beleg (der`); `not ok 64 … sagt kein Beleg …` mit `false' failed` an Zeile 1248 (die eigene Meldung des Tests nennt das fehlende `kein Beleg`, sie geht auf fd 3 und steht nicht im gefilterten Lauf-Auszug) |
| Fall 456 | genau eine Zeile (`[ -z "$partial" ] && ` entfernt); `not ok 61 … faehrt trotz stehendem Beleg …` mit `[ "$(grep -cF 'Kein Fall-Lauf' <<<"$output")" -eq 0 ]' failed` — der Übersprung griff mit Filter, Ursache passt |
| Fall 457 | genau eine Zeile (`if false; then`); `not ok 58 … unbekanntem Namen …` mit `[ "$status" -eq 1 ]' failed` — der unbekannte Name ging durch |
| Fall 263 mit dem neuen Anker | `diff`: genau die Zeile `  [ -n "$partial" ] \|\| clear_belief` gelöscht; `not ok 56 … loescht einen bestehenden Beleg SOFORT …` mit `[ ! -f …mutate-passed.key ]' failed` — Fall 263 greift am neuen Stand und färbt seinen Test aus dem behaupteten Grund |
| alle 32 Fälle mit `# files:` auf `harness/tools/mutate.sh` (`grep -l '^# files:.*harness/tools/mutate\.sh' test/mutations/*.sh \| wc -l` → 32), je Fall gegen eine Kopie von `mutate.sh` angewandt | **kein** Fall lässt die Datei unverändert — außer 263 (schon nachgezogen) ist kein Anker verrutscht; die Prüfung sagt „Anker trifft", nicht „trifft genau die gemeinte Stelle" (das prüfte je Fall der Implementer bei der Anlage) |
| MR-071 für die fünf neuen Anker, `grep -c` gegen `harness/tools/mutate.sh` | 453: `^    report_partial ` → 1; 454 und 263: `^  \[ -n .* \|\| clear_belief$` → 1; 455: `— kein Beleg (der` → 1; 456: `\[ -z "[$]partial" \] && ` → 1; 457: `if \[ ! -f "[$]cases_dir/[$]name.sh" \]; then` → 1 |
| Gegenprobe 1 (455 geschwächt: `(der Slot bleibt …` statt `(der Beleg-Slot bleibt …`) | nur das Kopie-Artefakt rot, **grün** — der Test bindet `TEILLAUF 1 von 3 — kein Beleg`, nicht den Klammersatz |
| Gegenprobe 2 (456 geschwächt: nur `[ -z "${MUTATE_FORCE:-}" ] && ` entfernt) | Fall 61 bis 64 **grün**; rot allein `56` (Bestand: der Test hält die `MUTATE_FORCE`-Bedingung) — die neuen Teillauf-Tests hängen nicht an `MUTATE_FORCE` |
| Gegenprobe 3 (453 geschwächt: `finalize_belief ""`) | **nicht sauber verwertbar:** der leere Schlüssel lässt `finalize_belief` unter `set -e` mit Status 1 enden, die Tests 61 bis 64 färben sich am Status statt am Slot. Als Gegenprobe zähle ich sie nicht; die Bindung des Falls 453 belegen der Fall selbst und Test 62 (`[ ! -e … ]`, Slot fehlt vorher, existiert nach der Mutation) |
| Ersatz-Mutation „`${MUTATE_CASES:+x}` statt `${MUTATE_CASES+x}`" (leerer Wert fährt voll) | rot: `59 … gesetzt, aber leer …` mit `grep -qF 'mutate: ABBRUCH — MUTATE_CASES ist gesetzt, nennt aber keinen Fall'` |
| Ersatz-Mutation „Duplikat-Zweig (`*" $name "*)`) entfernt" | rot: `60 … doppeltem Namen …` mit der fehlenden Meldung `… nennt '01-ok' mehrfach` |
| Ersatz-Mutation „Filter wählt alle Fälle" (`chosen+=("$cf")`) | rot: `61`, `62`, `64` (`[ "$status" -eq 0 ]`, weil `02-befund` mitläuft) |
| Ersatz-Mutation „Zweig `*/*` aus `select_cases` entfernt" | **überlebt:** `1..64`, nur das Kopie-Artefakt rot (siehe R-3) |
| Ersatz-Mutation „`report_partial` nur bei grünem Lauf" (`[ "$fail_count" -ne 0 ] \|\| report_partial …`) | **überlebt:** `1..64`, nur das Kopie-Artefakt rot (siehe R-4) |
| Ersatz-Mutation „vor dem Filter-Abbruch entsteht eine Isolationskopie" (`mktemp -d` + `cp -a` vor `select_cases`) | **überlebt:** `1..64`, nur das Kopie-Artefakt rot — der EXIT-Trap `cleanup` räumt `ISO_ROOT` weg, `ls -A` sieht nichts (siehe R-2) |
| `make mutate MUTATE_JOBS=1 MUTATE_CASES=<Name>` mit `nicht-vorhanden`, leer, `10-ci-workflow-syntax 10-ci-workflow-syntax`, `../mutations/10-ci-workflow-syntax`, `*`, `-x` | je `mutate: ABBRUCH — MUTATE_CASES …` mit dem Namen (leer: `gelesen: ''`), `make` meldet `Fehler 1`, Exit **2**; nach allen sechs Läufen fehlt `.harness/state/mutate-passed.key` weiterhin, kein Lock-Rest, keine Isolations-Kopie in `/tmp` |
| **Echter Teillauf** `make mutate MUTATE_JOBS=1 MUTATE_CASES=10-ci-workflow-syntax` (Beleg-Slot vorher: nicht vorhanden; ich habe zur Messung einen Platzhalter `reviewer-dummy-slot` gelegt, sha256 `d8c70cb0…5bd20`) | Exit **0** in 11,5 s; `mutate: MUTATE_CASES waehlt 1 von 445 Faellen — Teillauf.`, `ok      10-ci-workflow-syntax … -> syntax-check rot`, `mutate: 1 ok, 0 Befund(e)`, dann wörtlich `mutate: TEILLAUF 1 von 445 — kein Beleg (der Beleg-Slot bleibt unberuehrt).`, `mutate: Pruefgegenstand 2dd84ff8…995c05`, `mutate: ok-Faelle: 10-ci-workflow-syntax `; Slot **nachher byte-gleich** (gleicher sha256), danach von mir wieder entfernt: Endzustand **nicht vorhanden**, wie vor dem Lauf |
| `make shell-lint` | ohne Befund (siehe Gate-Lauf unten) |
| `git ls-files -s test/mutations/45[3-7]*` | Modus `100644`; Bestand gemischt (`git ls-files -s test/mutations \| awk '{print $1}' \| sort \| uniq -c` → 119 mal `100644`, 326 mal `100755`), der Treiber ruft jeden Fall mit `bash <datei>` |
| Probe zur `! grep`-Falle (bats im gepinnten Bild) | `@test { ! grep -q x <<<"x"; true }` → `ok`; dieselbe Zeile als letzte → `not ok` — bestätigt: eine Negation mitten im Test schlägt nicht an |

---

## Findings

Kein HIGH.

### MEDIUM

**R-1** — `kategorie`: MEDIUM · `quelle`: `LH-QA-02`, `AGENTS.md` §3.6 (die Zusage trägt, was der Code hält) · `pfad`: `harness/sensors/mutate.md:107` (Bedingung 1), `harness/tools/mutate.sh:1529` und `:1531` (die einzigen zwei Ausgabestellen des Schlüssels neben der Übersprung-Meldung `:1620`) · `befund`: Die Vereinigungsregel verlangt als erste Bedingung, dass **beide** Läufe denselben `mutate: Pruefgegenstand <hash>` nennen, und erklärt sonst *„zwei Aussagen über zwei Bäume"*. Nur ein **Teillauf** druckt diese Zeile; ein voller Lauf, der mit Befund endet (der Hauptlauf im Anlass der Regel, `425 ok, 8 Befund(e)`), druckt keinen Schlüssel — `finalize_belief` löscht den Slot, die Übersprung-Meldung erscheint nur bei einem vorliegenden Beleg. Die Bedingung ist für den Hauptlauf mit der Ausgabe des Treibers nicht erfüllbar; wer sie liest, kann die Vereinigung nie zulässig ziehen oder muss den Schlüssel außerhalb der Regel beschaffen (etwa `isolation_key` aus dem gesourcten Treiber). Die Regel ist damit strenger, als sie klingt (sicher), aber die frühere Praxis, die sie tragen soll, ist mit ihr nicht durchführbar. · `verifizierbar`: ja — `grep -n 'Pruefgegenstand' harness/tools/mutate.sh` nennt Ausgabezeilen nur in `report_partial` und in der Übersprung-Meldung; ein voller Lauf mit Befund druckt keine davon. · `klasse`: *Regel verlangt einen Beleg-Inhalt, den die Ausgabe des Laufs, den sie betrifft, nicht trägt*

**R-2** — `kategorie`: MEDIUM · `quelle`: `AGENTS.md` §3.6 (Test-Name behauptet eine Eigenschaft, die er nicht misst), Slice-Ziel Punkt 1 *„bevor eine Isolationskopie entsteht"* · `pfad`: `test/mutate-driver.bats:1172` (Name *„… kopiert nichts"*), `:1178`, `:1190`, `:1200` (`[ -z "$(ls -A "$TL_ROOT/tmp")" ]`); `harness/tools/mutate.sh:1584-1592`; `harness/sensors/mutate.md:81-82` · `befund`: Die drei Sperren-Tests prüfen *„keine Isolationskopie"* als leeres `TMPDIR`. Der EXIT-Trap (`cleanup`) entfernt `ISO_ROOT` bei jedem Ausstieg, also ist `TMPDIR` nach dem Abbruch leer, **auch wenn** die Kopie vorher entstand. Eine Mutation, die vor dem Filter-Abbruch eine Kopie anlegt (Ersatz-Mutation oben), färbt keinen der 64 Fälle. Was die Reihenfolge tatsächlich bindet, ist allein die Slot-Probe im Unbekannt-Test (`stehender-beleg` unverändert: verhindert eine Entwertung **vor** der Prüfung); dass **keine Isolationskopie** vorher entsteht, ist in Name, Kommentar und Sensor-Doc zugesagt und von keinem Fall gehalten. · `verifizierbar`: ja — die Ersatz-Mutation ist in einer Scratchpad-Kopie grün. · `klasse`: *Zusage ohne rot färbenden Zahn: Leer-Probe nach einem Aufräum-Trap*

### LOW

**R-3** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.6 · `pfad`: `harness/tools/mutate.sh:635` (`*/* | "")`), Kommentar `:619-624` (*„ein Name mit `/` ist nie ein Fall-Name"*) · `befund`: Der Zweig `*/*` ist von keinem Fall gebunden: entfernt man ihn, bleiben alle 64 Fälle grün. Der Zweig hat einen Zweck (`../mutations/<fall>` löst gegen `$cases_dir/$name.sh` zu einer echten Datei auf); ohne ihn wählt der Filter danach 0 Fälle. Ich habe den Ablauf ohne den Zweig gefahren: kein stilles Grün — `mutate: BEFUND  vollstaendigkeit … kein einziger Worker hat ein Zug-Protokoll hinterlassen`, `0 ok, 1 Befund(e)`, `TEILLAUF 0 von 445`, Exit 1. Die Vollständigkeits-Prüfung fängt es, mit einer Meldung, die auf den Aufruf-Fehler nicht hinweist. · `verifizierbar`: ja · `klasse`: *Zusage ohne rot färbenden Zahn: Zweig durch nachgelagerte Prüfung verdeckt*

**R-4** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.6 · `pfad`: `harness/tools/mutate.sh:1520-1524` (Kommentar *„Sie steht auch bei einem Befund"*), `harness/sensors/mutate.md:94` (*„(auch bei einem Befund)"*) · `befund`: Die Zusage, dass `TEILLAUF … kein Beleg` auch bei einem Befund erscheint, hat keinen Zahn: `[ "$fail_count" -ne 0 ] || report_partial …` lässt alle 64 Fälle grün; Test 63 (Teillauf mit Befund) liest nur `BEFUND +02-befund` und den Slot, nicht die `TEILLAUF`-Zeile. Ein roter Teillauf ist als Beleg nicht zitierbar (R1 des Plans betrifft den grünen); die Zeile ist dort die Klarstellung, dass der Slot stehen bleibt. · `verifizierbar`: ja · `klasse`: *Zusage ohne rot färbenden Zahn*

**R-5** — `kategorie`: LOW · `quelle`: `AGENTS.md` §3.7 (Konjunktiv über die verworfene Alternative) · `pfad`: `harness/tools/mutate.sh:622-624` · `befund`: *„nichts davon wird uebergangen: ein still verkleinerter Lauf waere ein Gruen ueber weniger Faellen, als der Aufrufer angefragt hat"* — der erste Halbsatz ist die Zusage im Indikativ, der zweite beschreibt die verworfene Alternative im Konjunktiv (`AGENTS.md` §3.7, Falsch-Beispiel *„Ohne dieses Feld behauptete die Ausgabe …"*). Kategorie **LOW**, nicht HIGH: der Skill führt „Kommentar trägt keine der Kommentar-Klassen" unter HIGH, der Satz trägt aber die Klasse *Zusage* (der Indikativ-Teil) und der Konjunktiv-Rest ist ein einzelner Halbsatz; die Nachbar-Reports gradieren dieselbe Klasse einheitlich als LOW (`R-4` im Report zu `slice-tap-check-liest-keine-version-ist-gebunden`). Ist die Einstufung strittig, schärft der Skill sie (§Pflege). · `verifizierbar`: nein — kein Gate liest den Satz. · `klasse`: *Kommentar im Konjunktiv über die verworfene Alternative*

### INFO

**R-6** — `kategorie`: INFO → **Architect** · `quelle`: `ADR-0035` (`Proposed`) Fitness-Zeile, Slice §6 R5 · `pfad`: `harness/tools/mutate.sh:1639` (`[ -n "$partial" ] || clear_belief`), `test/mutate-driver.bats:1228-1240` · `befund`: Die Fitness-Zeile lautet *„Ein Lauf mit mindestens einem Befund hinterlässt keinen Beleg"*; der Bestandstest 55 nennt es *„(auch keinen stehengebliebenen)"*. Ein Teillauf mit Befund lässt einen stehenden Beleg stehen — ein Lauf mit Befund hinterlässt hier also einen Beleg (den des früheren vollen Laufs). Der Plan entscheidet das ausdrücklich (§1, R2) und dokumentiert es im Sensor-Doc; ob ein Teillauf ein *Lauf* im Sinne der Fitness-Zeile ist, ist eine Formulierungsfrage der ADR, kein Befund gegen den Slice. Nirgends im Diff wird die ADR als `Accepted` behauptet (`grep -n 'ADR-0035' harness/sensors/mutate.md harness/tools/mutate.sh harness/README.md` nennt sie nur als Kennung und Festlegung; `harness/README.md` nennt sie nicht). · `verifizierbar`: nein · `klasse`: *ADR-Wortlaut und Slice-Entscheidung an einer Fitness-Zeile*

**R-7** — `kategorie`: INFO → **Verifier / Planner** · `quelle`: `AGENTS.md` §3.6, Punkt (7) des Auftrags · `pfad`: `test/mutations/453` bis `457`, `test/mutate-driver.bats:1183-1204` · `befund`: Der Plan nennt drei Mutations-Fälle (schreibt, entwertet, sagt „Beleg"); geliefert sind fünf — 456 (Übersprung mit Filter) und 457 (unbekannter Name) sind zusätzlich, beide binden (siehe Tabelle). Kein Mangel. Ungelistet bleiben die Zähne für *leer*, *doppelt* und *Filter wählt alle*: die bats-Tests 59, 60, 61 bis 64 färben sich unter den drei Ersatz-Mutationen oben (gesehen), aber `make mutate` bewacht ihre **Haltbarkeit** nicht (AGENTS.md §3.6: *„wer keinen Fall in `test/mutations/` hat, ist unbewacht"*). Das ist die dokumentierte Grenze des kuratierten Satzes, keine Zusage des Slice. Die Zusage im Sensor-Doc *„Gehalten wird das von … den Fällen 453 bis 457"* ist dafür etwas weit; die Fälle halten fünf von sieben Eigenschaften, die Tests alle sieben (R-2, R-3, R-4 ausgenommen). · `verifizierbar`: ja · `klasse`: *kuratiert ist unvollständig*

**R-8** — `kategorie`: INFO (nur melden, Bestand nicht ändern) · `quelle`: `Maintainability`, `AGENTS.md` §3.6 · `pfad`: `test/mutate-driver.bats` (19 Zeilen `  ! grep`; 13 davon nicht als letzte Anweisung ihres `@test`, gezählt mit `awk`: nächste Zeile ist nicht `}`) · `befund`: Die `! grep`-Falle ist real (Probe oben): eine Negation mitten im Test schlägt nicht an. Die sieben **neuen** Tests meiden die Form vollständig — sie nutzen `[ "$(grep -c … )" -eq 0 ]` (`:1211`, `:1212`, Kommentar davor). Im Bestand bindet ein Teil der Zusagen nicht, darunter die `Kein Fall-Lauf`-Negationen bei `:1099` und `:1124` (Bestandstest zum Übersprung). Nicht geändert, nicht Gegenstand dieses Slice. · `verifizierbar`: ja · `klasse`: *Negation mitten im bats-Test bindet nicht*

**R-9** — `kategorie`: INFO · `quelle`: `Maintainability` · `pfad`: `harness/tools/mutate.sh:1529-1531`, `harness/sensors/mutate.md` · `befund`: Zwei Beobachtungen ohne Failure-Szenario im Gate-Pfad. (a) Die Zeile `mutate: ok-Faelle: 10-ci-workflow-syntax ` trägt ein Leerzeichen am Zeilenende (der `awk` hängt je Name ein Leerzeichen an). (b) `MUTATE_CASES` ist eine Umgebungsvariable: eine in der Shell stehengebliebene Variable macht aus `make mutate` einen Teillauf; er sagt das laut (`TEILLAUF … kein Beleg`, `MUTATE_CASES waehlt … Teillauf`) und schreibt den Slot nicht — sicher, aber ein Leser, der die Ausgabe nicht liest, hält einen Vollauf. Die CI ruft `make mutate` bar. · `verifizierbar`: ja · `klasse`: *Ausgabe-Kosmetik / Umgebungs-Leck, sicher gerichtet*

---

## Geprüft, ohne Befund

- **Filter-Parsing (`select_cases`, `main()`):** `IFS=$' \t\n' read -r -d '' -a` splittet auf Leerzeichen, Tab, Newline, ohne Globbing; `${MUTATE_CASES+x}` trennt *gesetzt-aber-leer* von *nicht gesetzt* (Ersatz-Mutation `:+x` färbt Test 59). Ein Name wird nur in `[ -f "$cases_dir/$name.sh" ]` (gequotet), im `case`-Muster `*" $name "*` (Variablen-Expansion, kein Muster-Metazeichen-Durchgriff über das Wort) und als fester String in `grep -qxF` gelesen; nie in `sed`, `rm`, `cp`, `eval` oder als Pfad-Argument. Sechs Aufrufe über `make` (unbekannt, leer, doppelt, `../…`, `*`, `-x`) enden mit Meldung und Exit 2, ohne Slot-, Lock- oder Kopie-Rest.
- **Exit-Codes und `set -e`:** Abbruch über `|| exit 1` vor `isolation_key`, Sofort-Entwertung und Isolation; der Lock (`mkdir "$LOCK"`) steht davor und wird vom EXIT-Trap freigegeben (kein Lock-Rest gemessen). `local -a chosen=()` und `cases=("${chosen[@]}")` sind unter `set -u` sicher, weil `select_cases` mindestens einen existierenden Namen liefert und dieselbe Glob-Liste liest. Ein Signal mitten im Teillauf löscht den Slot nicht (`on_signal` ruft `clear_belief` nicht).
- **Die drei Verzweigungen in `main()`:** Übersprung (`[ -z "$partial" ] &&`), Sofort-Entwertung (`[ -n "$partial" ] || clear_belief`), Schluss (`report_partial` statt `finalize_belief`) — je durch Mutation rot (Fälle 456, 454/263, 453). Der Filter wirkt auf die Fall-Liste; `merge_report` prüft gegen `total` (Vollständigkeitszeile `1 von 1 Fall-Dateien` am echten Lauf gelesen).
- **Neue bats-Wächter (7):** Erwartungen stammen aus Fake-Repo und Stub, nicht aus dem geprüften Gegenstand (der Stub färbt `make test` rot bei `mutiert`; `TL_KEY` kommt aus `isolation_key` des gesourcten Treibers — Test 64 bindet damit *„nennt den Schlüssel, den der Lauf berechnet"*, nicht seinen Wert, das ist der gemeinte Anspruch). Sieben Tests, sieben Mutationen rot (Tabelle), keine `! grep`-Form.
- **Fall-Köpfe und `\x24`-Konvention:** `# files:` und `# expect:` vorhanden; `# verify:` fehlt in allen fünf — der Bestand lässt es in 287 von 445 Fällen weg, `narrow_sensor` leitet `test-bats` aus dem `# expect:`-Text ab (kein `Test[A-Z]`). Kein literales `$` im einfach gequoteten Muster (`[$]`, SC2016 sauber, `make shell-lint` ohne Befund).
- **Sensor-Doc und README-Zeile:** Zustandsform, keine Chronik, keine Konjunktive über verworfene Fassungen im Doc-Text der Vereinigungsregel; Zahlen: keine im Text (`Skript 1, über make 2` ist ein Exit-Code, keine Messung); README-Zeile `make mutate` trägt `kein Gate` und die Bindung `AGENTS.md` §3.6, ohne neue `make`-Zeile. Die Grenze der Regel (Ausschnitt jedes Laufs, ADR-0035 Festlegung 4, Prosa ohne Doku-Modul, Träger ist die lesende Rolle) steht im selben Abschnitt und sagt nicht mehr zu, als Festlegung 4 trägt; **bis auf Bedingung 1 (R-1)** trägt die Regel die frühere Praxis (Vereinigung am identischen Baum als Aussage im Bericht, nie im Slot).
- **Abgrenzung §1, §3.3, §3.10, §3.11:** der Schwester-Slice in `open/` ist nur durch den Verweis-Nachzug von `09159a4a` berührt (zwei Zeilen, Pfad `next/` → `in-progress/`, Werkzeug-Mechanik von `make slice-mv`); `d40fb021` ist ein reiner Move (0 Einfügungen), Inhaltsänderung in eigenen Commits (§3.3); kein Closure-Artefakt im Diff, kein Häkchen gesetzt (§3.10); der Report nennt den Slice bei der Kennung (§3.11). ADR-0035 nirgends als `Accepted`. Docker-only: alle Läufe über `make`. Kein Verstoß gegen §3.9.
- **HIGH-Liste des Skills, Punkt für Punkt:** aktive ADR / Hard Rule — kein Verstoß (ADR-0035 `Proposed`, nicht aktiv; §3.6 siehe R-2 bis R-4 als MEDIUM/LOW, weil die Zusagen zwar zahnlos, aber nicht falsch sind: gemessen hält der Code sie); Gate-Lockerung ohne ADR — keine (der Filter ist Nachfrage, kein Gate, `make gates` unverändert); Stilles-Grün-Pfad — kein gefundener: ein leerer, unbekannter, doppelter Name und ein Filter, der 0 Fälle wählt, enden rot; halluziniertes Gate — keines (`MUTATE_CASES` ist kein Target; README-Zeile zeigt auf ein vorhandenes); superseded ADR — keine; Norm nur im Template-Kommentar — nein; Kommentar ohne Klasse — R-5 (LOW, begründet); Zustandsfeld mit Chronik — der Ruhe-Marker der Roadmap wurde entfernt (Zustand `in-progress/` belegt), keine Chronik hinzugefügt.

## Verdikt

**Kein HIGH; zwei MEDIUM vor dem Merge zu klären.** Filter und Teillauf tun am echten Lauf, was der Plan sagt: der Teillauf schreibt und löscht den Slot nie (vorher/nachher byte-gleich, Endzustand wiederhergestellt), sagt `kein Beleg` samt Schlüssel und `ok`-Fällen, und die vier Abbrüche enden fail-closed. Die fünf neuen Mutations-Fälle und der nachgezogene Anker von Fall 263 färben ihren Test aus dem behaupteten Grund rot, jeder Anker trifft im Quell-Bestand genau eine Stelle, und kein weiterer der 32 `mutate.sh`-Fälle hat seine Wirkung verloren. Zwei Lücken: die Vereinigungsregel verlangt in Bedingung 1 einen Schlüssel, den der Hauptlauf nicht ausgibt (R-1), und *„keine Isolationskopie vor dem Abbruch"* ist zugesagt, aber von keinem Fall gehalten (R-2).

**Übergabe:**

- **R-1 → Implementer** (Wortlaut der Regel in `harness/sensors/mutate.md`, oder die Ausgabe des vollen Laufs; Korrektur dort, nicht in diesem Lauf) und → **Planner**: der Plan formuliert Punkt 3 (i) als *„beide Läufe nennen denselben Prüfgegenstand-Schlüssel"*; ob das ein Auftrag an die Ausgabe des vollen Laufs ist, ist Planner-Entscheidung — der Schwester-Slice führt die Beleg-Lesbarkeit ohne Lauf, aber nicht die Ausgabe eines roten Vollaufs.
- **R-2, R-3, R-4 → Implementer** (Zähne bzw. Zusagen einschränken auf das, was der Code hält; ein Beobachter einer vor der Prüfung entstandenen Kopie liest die Ausgabe — `isolierte Kopie(n)` — statt `TMPDIR`). R-5 → Implementer (Kommentar).
- **R-6 → Architect** bei der Annahme von ADR-0035: die Fitness-Zeile *ein Lauf mit Befund hinterlässt keinen Beleg* gegen die Slice-Entscheidung *ein Teillauf mit Befund lässt den Beleg des letzten vollen Laufs stehen* formulieren (Plan §6 R5 nennt die Bedingung).
- **R-7, R-8 → Verifier / Planner** zur Kenntnis: R-8 ist Bestand (nicht geändert); Rot-Belege der Zusagen (§3.6, sicherheits-/korrektheitskritisch) trägt der Verifier nach, soweit sie oben nicht stehen: Fälle 453 bis 457 und die drei Ersatz-Mutationen sind hier rot gesehen.
- Die **Finding-Klassen** gehen in die Slice-Closure §7 und von dort in den Zähler. Dieser Report ist ein Lauf-Beleg und ersetzt keine Verifikation: DoD-Konformität und die Closure-Schritte prüft der Verifier bzw. schreibt der Planner (`AGENTS.md` §3.10).
