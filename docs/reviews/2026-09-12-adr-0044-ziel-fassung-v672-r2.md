# Review (Runde 2) — ADR-0044: Ziel-Fassung regiert den Sprung `v6.5.0` → `v6.7.2`

**Rolle:** Reviewer (frischer Kontext — kein Anteil am Architect-Lauf, keiner an Runde 1, keiner
an den zwei Runden zu [ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md)) ·
**Datum:** 2026-09-12 ·
**Gegenstand:** [ADR-0044](../plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md) (`Proposed`,
777 Zeilen) im Stand der Nacharbeit `d3a09df7` — drei Dateien: die ADR, der
[ADR-Index](../plan/adr/README.md) (Titel-Zelle) und
[`harness/conventions.md`](../../harness/conventions.md) §Baseline (−1 Folgerung).

**Vorrunde:** [`2026-09-12-adr-0044-ziel-fassung-v672.md`](2026-09-12-adr-0044-ziel-fassung-v672.md)
— 1 HIGH · 1 MEDIUM · 4 LOW · 3 INFO, Verdikt *„Beleg trägt nicht"*. Dieser Report urteilt über
den **geänderten** Zuschnitt, nicht über den von Runde 1; er ist die erneute Runde derselben
prüfenden Rolle, die
[ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md) Festlegung 2 nach
einem blockierenden Befund verlangt.

**Prüfgrundlage:** [ADR-0018](../plan/adr/0018-ziel-fassung-regiert-die-migration.md),
[ADR-0031](../plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md),
[ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md) und die teilweise
abgelöste [ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md) — die vier, die der
Acceptance-Trigger namentlich nennt; dazu
[ADR-0032](../plan/adr/0032-eingefrorene-referenz-folgt-ihrem-rumpf.md) und
[ADR-0034](../plan/adr/0034-register-verzeichnis-form-und-die-ortsfestigkeit-der-register-datei.md)
(Form-Vorbild der Teil-Ablösung), [`docs/plan/adr/README.md`](../plan/adr/README.md) §Konventionen,
[`.d-check.yml`](../../.d-check.yml) (`vcs`, `matrix`),
[`harness/sensors/adr-immutable.md`](../../harness/sensors/adr-immutable.md),
[`AGENTS.md`](../../AGENTS.md) §3.1/§3.4/§3.5/§3.7/§3.8/§3.11,
[`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert),
[`MR-033`](../../harness/conventions.md#mr-033--eine-aussage-über-die-baseline-nennt-den-tag-gegen-den-sie-gemessen-ist),
[`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
[`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit).

**Messbasis:** Arbeitsbaum auf `50907948`, sauber; Kurs-Klon
`/Development/KI/ai-harness-course` (Tags `v6.5.0`, `v6.7.1`, `v6.7.2`), zwei
`git archive`-Bäume im Scratchpad. **Kein Gate-Lauf, kein Docker-Ziel** (Anweisung des
Auftraggebers) — dieser Report trifft **keine** Aussage über Gate-Farbe. Für Link- und
Anker-Auflösung ist der Stempel des Auftraggebers die Deckung, nicht dieser Lauf:
`git merge-base --is-ancestor d3a09df7 50907948` → wahr, der geprüfte Text liegt also unter dem
grünen Stand.

---

## Findings

### LOW-1 — Eine dritte falsch gewordene Präsens-Stelle in ADR-0043 ist nicht benannt · `…-v672.md:648`

`quelle` [`AGENTS.md`](../../AGENTS.md) §3.4 ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:648` ·
`befund` Die Konsequenz-Position lautet *„**Zwei weitere Stellen** jener Datei sind durch den
bewegten Zielstand falsch geworden, ohne abgelöst zu sein"* und nennt das Kopffeld `Kopplung:`
und die Architect-Folgepflicht. Eine dritte steht in
[ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md) §Kontext: *„Der gesetzte
Zielstand ist `v6.7.1`."* (`:126`) — Präsens, heute falsch, in der eingefrorenen Datei nicht
korrigierbar. In §Was der Teil-Supersede umfasst (`:528`) ist die Zahl durch den Zusatz *„über
§Baseline"* gedeckt; in der Konsequenz-Position steht sie als Zählung der Klasse *falsch
geworden*. **Versagensbild:** Ein späterer Lauf liest die Aufzählung als vollständig und hält
`:126` für eine noch geltende Aussage über den Zielstand. **Zweites Auftreten der Klasse** aus
Runde 1 (dort LOW-3, damals das Kopffeld `Kopplung:`). ·
`verifizierbar` **nein** — kein Modul der [`.d-check.yml`](../../.d-check.yml) liest Zeitform oder
Vollständigkeit einer Aufzählung ·
`klasse` Rest in der eingefrorenen Datei unvollständig benannt

```sh
grep -n 'Der gesetzte Zielstand ist' docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md
# -> 126:eines: Der gesetzte Zielstand ist `v6.7.1`. Der Umfang des Sprungs selbst ist dagegen klein:
grep -n 'v6\.7\.1' docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md | wc -l   # -> 25 Zeilen
grep -o 'v6\.7\.1' docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md | wc -l   # -> 26 Vorkommen
```

### LOW-2 — Die wiederholte zweite Hälfte weicht vom Original ab, obwohl die ADR Deckungsgleichheit zu ihrem eigenen Defekt-Test macht · `…-v672.md:675-689`

`quelle` [ADR-0044](../plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md) §Konsequenzen,
Folgepflicht (Planner) ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:682` ·
`befund` Der Punkt wiederholt die **nicht** abgelöste zweite Hälfte von
[ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md) `:527-532` mit der Zusage
*„sie sagt dasselbe, und eine Abweichung zwischen beiden wäre ein Defekt dieser Datei"* und der
Begründung *„damit der Auftrag an einer Stelle als Ganzes lesbar bleibt"*. Gemessen weicht sie an
zwei Stellen ab: die Apposition, die die zwei Felder **identifiziert** (*„— das der
`v6.5.0`-Zeile und das der Zeile, die der Vollzug anlegt"*), fehlt; und *„Eingetragen wird **er**
nicht in diesem Punkt"* ist zu *„Eingetragen werden **sie**"* geweitet. **Versagensbild:** Der
Planner liest den Auftrag an der Stelle, an der er laut Zusage als Ganzes steht, und erfährt
nicht, welche zwei Nachweis-Felder die Kennung des Durchgangs aufnehmen — die identifizierende
Hälfte steht nur in der Datei, auf die der Punkt verweist. ·
`verifizierbar` **nein** — kein Sensor hält zwei Prosa-Fassungen derselben Aussage zusammen
(dieselbe Lage, die §Fitness Function für die ganze Regel feststellt) ·
`klasse` zweite Fassung derselben Norm-Aussage weicht bei der Anlage schon ab

```sh
sed -n '527,532p' docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md
sed -n '675,689p' docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md
grep -c 'das der `v6.5.0`-Zeile und das der Zeile, die der Vollzug anlegt' \
  docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md \
  docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md
# -> 0043: 1   0044: 0
```

### LOW-3 — Zwei Kommando-Blöcke zeigen in der Ausgabe-Position ein Urteil statt der Ausgabe · `…-v672.md:402-408`

`quelle` [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
Setzung 1 ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:408` ·
`befund` `grep -rln 'Adaptions-Durchgang' …open …next …in-progress` trägt die Ausgabe-Marke
`# -> kein Durchgangs-Slice`; real liefert das Kommando **drei** Pfade (`slice-092`, `slice-222`,
`roadmap.md`). Das Urteil ist richtig — keiner der drei ist ein Durchgangs-Slice —, steht aber an
der Stelle, an der der Block sonst Ausgabe zeigt. Dieselbe Stelle in
[ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md) `:336-337` nannte die Treffer
noch (*„ein Slice zur Träger-Inventur und die Roadmap, kein Durchgang"*). Der Block darüber zeigt
drei von fünf Treffern des `grep -n 'Delta-Nachweis'`, ohne die Kürzung zu markieren.
**Versagensbild:** Ein Lauf, der den Block reproduziert, sieht drei Treffer gegen *„kein"* und
hält entweder die Messung für veraltet oder `slice-222` für den fehlenden Durchgangs-Slice. ·
`verifizierbar` **ja** (negativ) — das Kommando ist hermetisch und läuft ohne Netz; seine Ausgabe
deckt sich nicht mit der Marke ·
`klasse` Urteil in der Ausgabe-Position eines Kommandos

```sh
grep -rln 'Adaptions-Durchgang' docs/plan/planning/open docs/plan/planning/next \
  docs/plan/planning/in-progress
# -> docs/plan/planning/open/slice-222-sensor-datei-traegt-die-form-ihrer-vorlage.md
#    docs/plan/planning/open/slice-092-traeger-inventur.md
#    docs/plan/planning/in-progress/roadmap.md
grep -n 'Delta-Nachweis' harness/conventions.md | wc -l   # -> 5 (der Block zeigt 3)
```

### LOW-4 — [ADR-0031](../plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md) wird als bindend zitiert und steht auf `Proposed` · `…-v672.md:38`, `:69`, `:668`

`quelle` [`AGENTS.md`](../../AGENTS.md) §3.4 (Bindung ab `Accepted`) ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:38` ·
`befund` Das Bezug-Feld sagt, Festlegung 2 jener Datei *„**bindet** die Buchung"*; `Kopplung:`
sagt *„Der **Ort** ist der von ADR-0031 Festlegung 2"*; §Konsequenzen ordnet an, der Vollzug werde
*„in der Drei-Teil-Form von ADR-0031 Festlegung 2"* gebucht. ADR-0031 trägt `**Status:** Proposed`
— in Datei und Index gleichlautend — und führt **keinen** Acceptance-Trigger. Keine der drei
Stellen nennt den Status, obwohl dieselbe Datei den Status sonst mitführt (§Baseline-Zeiger:
*„ADR-0044 Festlegung 1, `Proposed`"* / *„ADR-0043 Festlegung 2, `Accepted`"*). **Versagensbild:**
ADR-0044 friert mit einer Anordnung ein, deren Grundlage noch änderbar ist; ändert sich die
Drei-Teil-Form vor ihrer Annahme, zeigt die eingefrorene Datei auf eine Form, die keine
angenommene Entscheidung trägt. **Nicht mit diesem Commit entstanden** — die bereits `Accepted`e
ADR-0043 trägt dieselbe Bezugnahme; **und eine Korrektur an Runde 1**, deren Prüfgrundlage die vier
genannten ADRs als *„alle `Accepted`"* führte. ·
`verifizierbar` **nein** — kein Modul vergleicht eine Bindungs-Aussage mit dem Kopffeld der
zitierten Datei ([`docs/plan/adr/README.md`](../plan/adr/README.md) §Konventionen sagt das für die
Status-Spalte selbst) ·
`klasse` Bindungs-Aussage über eine nicht angenommene Entscheidung, Status nicht genannt

```sh
grep -m1 '^\*\*Status:\*\*' docs/plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md
# -> **Status:** Proposed
grep -c 'Acceptance-Trigger' docs/plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md  # -> 0
grep -l '^\*\*Status:\*\* Proposed' docs/plan/adr/[0-9]*.md | wc -l                                  # -> 5
```

### INFO-1 — Korrektur an Runde 1, LOW-2: die Hälfte der Annahme trägt nicht — und die Lücke ist größer als der Fundweg

`quelle` [`docs/plan/adr/README.md`](../plan/adr/README.md) §Konventionen ·
`pfad` `docs/plan/adr/README.md:15-26` ·
`befund` Runde 1 schrieb, *„ADR-0032 und ADR-0034 ordnen ebenfalls einen Zusatz an"*. Gemessen
ordnet **nur** ADR-0032 einen an (Folgepflicht 2, für die Zelle von ADR-0026); ADR-0034 führt
keine Folgepflicht auf den ADR-Index — jede ihrer vier betrifft `.d-check.yml`,
`harness/conventions.md`, den Restbreite-Wächter und die Selbstanwendung. Damit trägt die
Status-Zelle von ADR-0030 einen Zusatz, den **keine** `Accepted`-ADR anordnet — gegen den Satz
*„Einen Zusatz trägt die Zelle nur, wo eine `Accepted`-ADR ihn anordnet"*. Die neue
`Folgepflicht (Architect), eigener Vorgang` in ADR-0044 (`:692-706`) benennt genau das als offene
Messung (*„je getragener Zelle die anordnende Stelle zu bestimmen — eine eigene Messung, die
dieser Lauf nicht gefahren hat"*) und behauptet keine Deckung
([`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6)); die
Verschiebung in einen eigenen Vorgang ist damit sachlich gedeckt, nicht nur zulässig. ·
`verifizierbar` **nein** ·
`klasse` Fundmenge aus einem Fundort geschlossen

```sh
grep -n 'Folgepflicht' docs/plan/adr/0034-register-verzeichnis-form-und-die-ortsfestigkeit-der-register-datei.md
# -> 354, 359, 362, 370 — .d-check.yml · harness/conventions.md · Restbreite-Waechter · Selbstanwendung
grep -cE '^\| \[ADR-[0-9]{4}\][^|]*\| [^|]*\| Accepted \(' docs/plan/adr/README.md   # -> 5 Zellen mit Zusatz
git grep -lE '^\*\*(Revidiert \(Teil-Supersede\)|Supersedes \(Teil\)):\*\*' -- 'docs/plan/adr/0[0-9]*.md' | wc -l   # -> 6
git grep -l 'Revidiert (Teil-Supersede)' -- 'docs/plan/adr/0*.md' | wc -l                                          # -> 3
```

### INFO-2 — Der Satzstamm des geteilten Punktes ist keiner Hälfte zugeordnet · `0043-…-v671.md:523`

`quelle` Maintainability · `pfad` `docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md:523` ·
`befund` Der abgelöste Punkt beginnt mit *„zwei Slices sind zu schneiden — der"*; ADR-0044 weist
die erste Hälfte ab *„der **Baum-Tausch**"* zu und die zweite ab *„der
**Adaptions-Durchgang**"*. Der Stamm gehört zu keiner der beiden. Folgenlos, weil er unter
ADR-0044 unverändert wahr bleibt (zwei Slices: Tausch auf `v6.7.2` und Durchgang ab `v6.0.0`) —
festgehalten, weil ein Schnitt *am Gegenstand* jede Textstelle einer Seite zuordnen können muss. ·
`verifizierbar` nein · `klasse` Rest-Text ohne Seite beim Schnitt am Gegenstand

---

## Negativbefunde

1. **Richtung „zu eng": geprüft, ohne Befund.** Nach der Ablösung ordnet **keine** Stelle in
   ADR-0043 mehr `v6.7.1` an. Gemessen trägt die Datei `v6.7.1` in **25** Zeilen (26 Vorkommen),
   und jede ist gelesen: Titel (`:1`) · Kopffeld `Kopplung:` (`:48`, von ADR-0044 als falsch
   geworden benannt) · 16 Kontext-, Mess- und Zitatzeilen (`:72`–`:363`, darunter der Präsens-Satz
   `:126` → LOW-1) · §Entscheidung Festlegung 1 (`:384`, `:386` — abgelöst) · zwei Begleitsätze der
   **nicht** abgelösten Festlegung 2 (`:416`, `:418`), die den Zielwechsel inhaltlich überstehen
   (die Dateimenge des Durchgangs ist gegen `v6.7.2` dieselbe) · §Konsequenzen `:519`
   (Architect-Folgepflicht, *„im selben Commit eingelöst"* — ein abgeschlossener Akt, von ADR-0044
   benannt) · §Konsequenzen `:524` (der abgelöste Auftrag) · §Re-Evaluierungs-Trigger `:571`
   (Geltungsbereich der abgelösten Festlegung, keine Anordnung). §Fitness Function nennt `v6.7.1`
   nicht. Der **alte sha256** steht in genau einer lebenden Stelle, und die liegt im abgelösten
   Stück: `git grep -n '5d3dba9c…' -- ':!docs/reviews' ':!docs/plan/planning/done'` → ein Treffer,
   `0043-…:525`. Die Prämisse *„keine Pin-Stelle hat je `v6.7.1` getragen"* ist nachgemessen:
   `git log --oneline -S'v6.7.1' -- Makefile .d-check.yml internal/fetch/baseline.go | wc -l` → `0`;
   alle fünf Stellen tragen heute `v6.5.0` / `80684c17…`. Außerhalb der zwei ADRs nennt nur die
   Titel-Zelle des Index `v6.7.1` — und die ist nach §Konventionen der verbatim-Titel.
2. **Richtung „zu weit": geprüft, ohne Befund.** Abgelöst sind genau zwei Stücke; Festlegung 2,
   die Achsen-Korrektur, die Netto-Rechnung, die Entkräftung von *byte-gleich*, die Grenzen und
   die Re-Evaluierungs-Trigger bleiben. Die Durchgangs-Pflicht ist ausdrücklich ausgenommen
   (`:514`), und ADR-0044 Festlegung 2 **wendet** die Leseregel an, statt sie zu wiederholen: Ihr
   normativer Satz nennt kein Sprungziel (*„der letzte, für den §Baseline … einen Slice mit
   Delta-Nachweis ausweist"*, `0043-…:403-406`). Die einzige allgemeine Aussage im abgelösten
   Stück — *„(kanonisch ist das Makefile-Paar; die vier übrigen sind fail-closed daran
   gekoppelt)"* — ist im ersetzenden Punkt **verbatim** wieder da, geht also nicht verloren.
3. **Fundbarkeit der abgelösten Stelle: geprüft, ohne Befund.** Der angegebene Fundweg
   `grep -n 'Folgepflicht (Planner)' docs/plan/adr/0043-…md` liefert **genau einen** Treffer
   (`:523`); die Hälften sind an den zwei fettgesetzten Substantiven eindeutig getrennt. Eine
   Rückfrage ist nicht nötig (Ausnahme: der Satzstamm, INFO-2).
4. **MEDIUM-1 aus Runde 1: gestrichen, und der Ersatz trägt.** Das widerlegte Argument
   (*„Statuszeile … das Byte, das §3.4 einfriert"*) steht nicht mehr in der Datei
   (`grep -c 'das Byte, das' …` → `0`; `grep -c 'liest Statuszeile und Trigger-Abschnitt' …` → `0`;
   `:538` trägt den neuen Text). Alle vier Teilaussagen des neuen Arguments sind gemessen:
   `vcs.head-allow` führt **genau drei** Alternativen (`Accepted|Deprecated|Superseded by
   [ADR-NNNN](NNNN-titel.md)`), keine für *teilweise abgelöst*; das Vokabular der ADR-Vorlage
   führt ebenfalls keine (`**Status:** Proposed | Accepted | Deprecated | Superseded by ADR-NNNN`);
   `vcs.status-line` nimmt die Zeile aus dem Kern — [`adr-immutable.md`](../../harness/sensors/adr-immutable.md)
   sagt es wörtlich (*„markiert, welche Zeile diesem `head-allow` statt der vollen
   Kern-Unveränderlichkeit unterliegt"*) und führt die Link-Form als gelebten Übergang; der
   Bestand trägt sie **zweimal**
   (`grep -h '^\*\*Status:\*\* Superseded by' docs/plan/adr/[0-9]*.md | wc -l` → `2`).
5. **Selbstverteidigung in Option D und F: geprüft, ohne Befund.** Option D schließt mit *„**Nicht**
   dagegen spricht die Statuszeile: Der volle Supersede-Übergang ist in diesem Repo ausdrücklich
   zugelassen"*, Option F mit *„Die Wahl steht auf diesen zwei Gründen und braucht keinen dritten —
   die Statuszeile ist keiner (Option D)"*. Der Satz leistet, was er soll: Er entzieht der Wahl
   das widerlegte Argument, statt es abgeschwächt weiterzuführen.
6. **Das Ergebnis steht ohne das gestrichene Argument: geprüft, ohne Befund.** Beide verbliebenen
   Gründe tragen. (a) Ein voller Supersede nähme die **Leseregel** mit — ADR-0044 Festlegung 2
   erklärt sich ausdrücklich zur *Anwendung* und nicht zur zweiten Fassung, stünde danach also
   ohne geltende Grundlage da (genau der Einwand, den Option E für den umgekehrten Fall trägt).
   (b) Die `matrix.status`-Kaskade ist jetzt an ihrer Konfiguration geschnitten und trifft zu:
   `classes` = `spec-straten`, `adr`, `slice`; `status.forbidden` = `[superseded, deprecated]`;
   `exclude-sections` = `[Historie, "7. Historie", Geschichte]`; `docs/plan/adr/README.md` und
   `harness/conventions.md` fallen aus jedem `paths`-Muster. Die Mehrheits-Aussage ist nachgefahren:
   **19 von 24** lebenden Verweisen liegen in der Klasse `adr`
   (`git grep -oE '\]\([^)]*0043-…\.md[^)]*\)' -- ':!docs/reviews' ':!docs/plan/planning/done' | wc -l`
   gegen dieselbe Abfrage über `'docs/plan/adr/[0-9]*.md'` — keine Erwartungswerte).
7. **LOW-4 aus Runde 1 und die Buchung insgesamt: geprüft, ohne Befund.** Die abgeleitete Folgerung
   (*„der Durchgang schließt damit den ausstehenden `v6.5.0`-Nachweis ein"*) steht **nicht** mehr in
   §Baseline; sie steht nur noch in der ADR. Die Buchung stimmt im Übrigen: Ziel-Tag `v6.7.2`,
   Datum `2026-09-12`, *„sein Vollzug steht aus"*, Setzung dem Auftraggeber zugeschrieben;
   `Stand:` unverändert `v6.5.0` (der **adoptierte**, nicht der gesetzte Stand); die Zeile
   *„auf `v6.5.0`: 2026-09-07, Delta-Nachweis steht aus"* bleibt offen; der **Wert** `v6.0.0` steht
   nirgends in der Buchung — sie nennt nur, woraus er gelesen wird. `v6.7.1` kommt in
   `harness/conventions.md` nicht mehr vor. Die Drei-Teil-Form von ADR-0031 Festlegung 2 gilt der
   Vollzugs-Buchung, die noch nicht existiert; das sagt die ADR selbst (zum Status jener ADR →
   LOW-4).
8. **Kein unbelegter sha256: geprüft, ohne Befund.** ADR-0044 enthält **null** 64-stellige
   Hex-Folgen (`grep -cE '[0-9a-f]{64}'` → `0`) und sagt ausdrücklich *„Sie nennt keinen sha256.
   Der des `v6.7.2`-Assets wird beim Vollzug am Asset gemessen"*; der Planner-Punkt schreibt
   entsprechend *„den am Asset gemessenen sha256"*. Der einzige Hash im Prüfbereich ist der
   `v6.7.1`-Hash in ADR-0043 — im abgelösten Stück (Negativbefund 1). **Nicht** nachgemessen ist,
   ob `80684c17…` der wahre Hash des `v6.5.0`-Assets ist: Träger dafür ist `make regelwerk-check`
   über Netz, und dieser Lauf fährt kein Ziel.
9. **Zahlen und Kommandos: geprüft, ohne Befund über LOW-3 hinaus.** Unabhängig nachgefahren und
   reproduziert: Stufe (a) gepinnt `1` und Ziel-Fassung `1` (jetzt literal statt paraphrasiert);
   Stufe (b) `123`/`123` Zeilen, `diff` leer, `7` Eigenschaften, `9` Verweise in `4` Dateien;
   Delegat roh/netto `43`/`2`/`0`/`0`; Sektions-Netto `25`/`4`/`2`/`12`/`0`/`0` (Summe `43`);
   `v6.7.1`→`v6.7.2` `3 Dateien +7 −4` und je Sektion `4` (jetzt literal); Dateimenge
   `v6.0.0..v6.7.1` gegen `v6.0.0..v6.7.2` — `diff` leer; die dreizehn Suchbegriffe → `0`;
   `git grep -l 'gate.template.md' …` → vier Dateien wie annotiert. Die INFO-1-Korrektur aus
   Runde 1 (zwei paraphrasierte Kommandos) ist eingelöst, die INFO-2-Korrektur (Ebene Dogfood vs.
   emittiert) ebenfalls.
10. **Zitat-Treue ([ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md),
    [`MR-033`](../../harness/conventions.md#mr-033--eine-aussage-über-die-baseline-nennt-den-tag-gegen-den-sie-gemessen-ist)):
    geprüft, ohne Befund.** Die vier tragenden Baseline-Zitate stehen im Zielbaum verbatim, jeweils
    mit Tag, Datei und Sektion: *„Der Gate-Index steht einmal, und zwar hier"*
    (`v6.7.2`, `grundlagen-harness-dateien.md:196`), *„Der Gate-Index steht **einmal**, in
    `harness/README.md` §Sensors"* (`AGENTS.template.md:193`), *„die **Autoritäts-Doku**, und es
    gibt genau eine"* (`modul-13-quality-gates.md:97`) und die Sensor-Ziel-Form
    (`grundlagen-harness-dateien.md:252`, Auswahlkriterium `:249` byte-gleich in `v6.5.0`). Das
    neu gesetzte Stützzitat im Kopffeld (INFO-3 aus Runde 1) zeigt jetzt auf den **normativen**
    Satz von Festlegung 2 und gibt ihn wörtlich wieder.
11. **[`AGENTS.md`](../../AGENTS.md) §3.8 (Commit-Zuschnitt): geprüft, ohne Befund.** `d3a09df7`
    berührt drei Dateien — ADR, ADR-Index, `harness/conventions.md` —, alle drei
    Architect-Eigentum ([ADR-0024](../plan/adr/0024-derivatives-register-gehoert-der-rolle-seines-originals.md)
    für den Index), und die Message nennt die Rolle.
12. **Index-Zelle: geprüft, ohne Befund.** Die neue Titel-Zelle ist mit der `# `-Überschrift der
    Datei **zeichengleich** (String-Vergleich, `IDENTISCH`); der Status `Proposed` stimmt mit dem
    Kopffeld; die Zelle von ADR-0043 steht unverändert — korrekt, denn §Konventionen bindet den
    Zusatz an eine `Accepted`-ADR, und diese ist es noch nicht.
13. **Referenzierte Entscheidungen: geprüft, ein Befund (LOW-4).** Keine der zwölf verlinkten ADRs
    ist `superseded` oder `deprecated`; elf sind `Accepted`, eine (`ADR-0031`) ist `Proposed`.
14. **[ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md)
    Festlegungen 2 und 3: geprüft, ohne Befund.** Die Änderung des Acceptance-Triggers
    (Prüfgegenstand *zu eng / zu weit*) fand statt, solange die Datei `Proposed` ist — Festlegung 3
    ist eingehalten. Dieser Lauf ist eine erneute Runde derselben prüfenden Rolle in frischem
    Kontext und damit nach Festlegung 2 als Beleg zulässig; er hat weder die ADR noch die
    Nacharbeit geschrieben.
15. **[`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6):
    geprüft, ohne Befund.** §Fitness Function behauptet kein Gate, führt sechs Kandidaten mit dem
    Grund ihres Nicht-Greifens und markiert den teilweise mechanisierbaren Sensor als **nicht
    gebaut**. Die beiden neuen Nicht-Deckungs-Aussagen sind an der Konfiguration gedeckt: `matrix`
    kennt keine Teil-Ablösung, `targets` prüft gegen **eine** `authority`-Datei
    (`authority: AGENTS.md`).
16. **[`AGENTS.md`](../../AGENTS.md) §3.11 / §3.7: geprüft, ohne Befund.** `slice-222` steht als
    blanke Kennung ohne Pfad-Adresse; genannt sind sonst nur ortsfeste Verzeichnisse
    (die Lifecycle-Verzeichnisse unter `docs/plan/planning/` sowie `docs/reviews/`). Keine Zustandszelle
    trägt Chronik; die §Geschichte-Zeile nennt Anlass und Ereignis, nicht den Verlauf des Laufs.

## Was dieser Lauf **nicht** geprüft hat

- **Keinen Gate-Lauf und kein Docker-Ziel** (`make gates`, `docs-check`, `adr-immutable`,
  `baseline-verify`) — Anweisung des Auftraggebers. Über Gate-Farbe, Link- und Anker-Auflösung
  steht hier nur die Ancestry-Aussage im Kopf.
- **Nichts über Netz.** Der sha256 des `v6.7.2`-Assets ist nicht gemessen (die ADR nennt keinen);
  dass `80684c17…` zum `v6.5.0`-Asset gehört und `5d3dba9c…` zum `v6.7.1`-Asset, ist **nicht**
  nachgemessen, sondern aus dem Makefile-Pin bzw. aus ADR-0043 übernommen.
- **Die Sachfrage selbst nicht entschieden:** ob die Ziel-Fassung regieren *soll*, ist
  Architect-Urteil. Geprüft ist, ob das Kriterium aus
  [ADR-0018](../plan/adr/0018-ziel-fassung-regiert-die-migration.md) Festlegung 3 angewandt und
  die tragende Messung reproduzierbar ist.
- **Die Vollständigkeit des Delegat-Arguments** weder bestritten noch nachgewiesen: dass außer den
  vier verlinkten Zieldateien keine weitere den Freshness-Audit trägt, folgt aus dem `grep` über
  die Links des Abschnitts — eine Delegation über Prosa fände er nicht.
- **Den Wortlaut von `slice-222`** und die noch nicht geschnittenen Tausch-/Durchgangs-Slices nicht
  auf Deckung mit dieser ADR geprüft (Planner-Eigentum); festgestellt ist nur Existenz und Lage.
- **Den ADR-Bestand** nur so weit gelesen, wie Acceptance-Trigger und Findings es verlangen — kein
  vollständiger Konsistenz-Durchgang über die 44 Dateien des Bestands.
- **Die Zusatz-Anordnung je Index-Zelle** nur für die fünf Zellen mit Zusatz bestimmt (INFO-1),
  nicht die Aufzählung in §Konventionen insgesamt fortgeschrieben — das ist der eigene Vorgang,
  den die ADR benennt.

## Kategorie-Summary

**0 HIGH · 0 MEDIUM · 4 LOW · 2 INFO.**

HIGH-1 und MEDIUM-1 aus Runde 1 sind aufgelöst und gegengemessen; LOW-1 und LOW-4 jener Runde sind
eingelöst, LOW-2 ist als eigener Vorgang benannt und LOW-3 in den Text aufgenommen. Wiederkehrende
Klasse über die vier Runden dieser ADR-Reihe: **zwei Fassungen derselben Norm-Aussage** —
[`2026-09-12-adr-0043-ziel-fassung-v671.md`](2026-09-12-adr-0043-ziel-fassung-v671.md) LOW-3, Runde 1 LOW-4 (beide behoben) und hier LOW-2 (die wiederholte
Hälfte). Drittes Auftreten in derselben Reihe; ob es die Register-Schwelle erreicht, entscheidet
die Closure, nicht dieser Report.

## Verdikt

**Kein blockierender Befund. Der Beleg, den der Acceptance-Trigger von
[ADR-0044](../plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md) verlangt, trägt.**

**Der neue Zuschnitt ist in beide Richtungen geprüft und in beiden richtig.** *Zu eng* ist er
nicht: Alle 25 `v6.7.1`-Zeilen in ADR-0043 sind gelesen, und nach der Ablösung steht dort keine
Anordnung mehr, die `v6.7.1` verlangt oder den alten Hash als geltend führt — der Hash liegt in
genau der Zeile, die das zweite abgelöste Stück trägt. *Zu weit* ist er ebenso wenig: Festlegung 2
und die auf ihr beruhende Durchgangs-Pflicht sind namentlich ausgenommen, die allgemeine
Pin-Aussage des abgelösten Stücks ist verbatim gerettet, und ADR-0044 Festlegung 2 wendet die
Leseregel an, statt sie ein zweites Mal zu schreiben. Die Grenze läuft damit am Gegenstand, und
die beiden Stücke sind über einen Fundweg mit genau einem Treffer auffindbar.

Die vier LOW blockieren nicht: Keiner von ihnen ändert eine Anordnung, und drei (LOW-1, LOW-3,
LOW-4) betreffen Stellen, an denen die ADR das Richtige tut und es unvollständig oder ohne
Statusangabe belegt. LOW-2 ist die einzige, die eine Zusage der Datei an ihrem eigenen Maßstab
misst und knapp verfehlt; sie ist, solange die Datei `Proposed` ist, mit einem Halbsatz zu
schließen — das zu tun oder zu lassen, ist Architect-Sache.

**Für den Accept-Übergang:** Nach
[ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md) Festlegung 1 nennt
die Accept-Zeile diesen Report als **Kennung** — `2026-09-12-adr-0044-ziel-fassung-v672-r2` —,
nicht als Pfad-Link.
