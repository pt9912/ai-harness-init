# Review-Report: slice-086 (`f93f08d..31a7908`) — 2026-08-22

**Review-Art:** **Code** — geprüft wird der fertige Diff gegen den Slice-Plan und die
Konventionen (Modul 10 §Drei Review-Arten). Nicht geprüft: DoD-Abhakung und
Gate-Lauf-Bestätigung (Modul 10 §Anti-Pattern — das ist der Verifier, Modul 11, in getrenntem
Kontext).

**Gegenstand:** Commit-Range `f93f08d..31a7908`, vier Commits, fünf Dateien.

| Commit | Was | Umfang (`git show --stat`) |
|---|---|---|
| `6c4c321` | `open/` → `next/`, reiner Move | 1 Datei, 0 Zeilen |
| `c874ae9` | `next/` → `in-progress/`, reiner Move | 1 Datei, 0 Zeilen |
| `3f27b7c` | Link-Reconciliation nach dem Move | 4 Dateien, +7 / −7 |
| `31a7908` | das Werkstück: `docs/reviews/2026-08-21-updatedinput-messung.md` | 1 Datei, +258 |

Gesamt (`git diff --stat f93f08d..31a7908`): 5 Dateien, 265 Einfügungen, 7 Löschungen.

**Skill:** `.harness/skills/reviewer.md` @ Version 1.4.0 (`ce4b611`) · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** `claude-opus-5[1m]` · **Datum:** 2026-08-22 · **Rolle:** Reviewer (Modul 10),
frischer Kontext, Subagent `reviewer`.

**Eingangs-Kontext** (die fünf v3.5.2-Pflicht-Punkte plus Slice-Plan, Modul 10
§Eingangs-Kontext — ohne diese Liste ist der Lauf nicht reproduzierbar):

- **Diff/Commit-Range:** `f93f08d..31a7908` (HEAD `31a7908`, Arbeitsbaum sauber).
- **Slice-Plan:** `docs/plan/planning/in-progress/slice-086-vordergrund-per-updatedinput.md`
  (§1 Ziel, §2 DoD, §3 Plan vor Code, §4 Trigger, §5 Closure-Trigger, §6 Risiken).
- **`LH-*`:** `LH-QA-03` (minimale Abhängigkeiten — Sonde nur `bash` + `awk`), `LH-QA-01`
  (keine halluzinierten Gates — Beobachtung statt Wächter), `LH-QA-02` (Reproduzierbarkeit,
  als Kategorie-Anker des Skills).
- **Aktive ADRs (Status selbst gelesen, alle `Accepted`):** `ADR-0011` (Festlegung 2 Prompt,
  Festlegung 3 Zustands-Bereich), `ADR-0019` (Festlegung 3/4, §Re-Evaluierungs-Trigger),
  `ADR-0020` (nur soweit slice-086/`CO-002` genannt), `ADR-0015` (Rollen-Eigentum), `ADR-0016`
  (Präzedenz uncommitteter Messaufbau), `ADR-0012` (permanenter Pfad). Dazu der Carveout
  `docs/plan/carveouts/CO-002-token-achse-je-rolle.md`.
- **Hard Rules:** `AGENTS.md` §3.1–§3.8, im Diff einschlägig §3.3, §3.6, §3.7, §3.8.
- **Vorherige Findings am gleichen Modul:** `docs/reviews/2026-08-15-adr-0019-bestaetigungsrunde.md`,
  `docs/reviews/2026-08-15-adr-0019-bestaetigungsrunde-runde-2.md`,
  `docs/reviews/2026-08-16-adr-0019-0020-konvergenzrunde.md`; Mess-Präzedenz
  `docs/reviews/2026-08-15-agent-guard-tool-vertrag.md`.
- **Repo-Adaptionen:** `MR-016` (Welle oder nicht, Setzung 2/3), `.d-check.yml`
  (`docs/reviews/**` ist von `ids` und `codepaths` ausgenommen, **nicht** von `links`/`anchors`).

**Was in diesem Lauf nicht messbar war, und das gehört an den Anfang:** ein Subagent führt das
`Agent`-Werkzeug nicht und kann den `ask`-Dialog des Werkzeugs nicht auslösen. Alles zum
Tool-Verhalten prüft die **Beleg-Kette**, nicht das Verhalten. Der Span-Bestand liegt gitignored
und maschinenlokal; ich habe ihn read-only gelesen und nichts darunter verändert.

---

## Selbst gefahren — Kommando und Ergebnis, nichts davon übernommen

| Kommando | Ergebnis |
|---|---|
| `git status --porcelain` (vor dem Lauf) | leer |
| `git diff --stat f93f08d..31a7908` | 5 Dateien, 265 (+) / 7 (−) |
| `git show 6c4c321 --raw -M` · `git show c874ae9 --raw -M` | je `R100`, Blob `bf529b7 bf529b7` — identische Blobs, kein Inhalts-Byte bewegt |
| `git diff --name-only f93f08d..31a7908 -- Makefile .d-check.yml spec test internal cmd harness .claude` | **leer** |
| `ls .claude/hooks/` | 3 Dateien (`pretooluse-agent-guard.sh`, `pretooluse-command-guard.sh`, `stop-require-gates.sh`) — keine Sonde, keine Fixture |
| `grep -n 'matcher' .claude/settings.json` | `PreToolUse` führt zwei Matcher (`Bash`, `Agent`) mit je **einem** Kommando — kein dritter Eintrag |
| `grep -rlF 'SONDE-MARKER-7c1de4b2a90f' --exclude-dir=.git --exclude=sonde-fixture-agent-payload.json .` | **1 Treffer**, `docs/reviews/2026-08-21-updatedinput-messung.md`, Exit 0 (s. MEDIUM-2) |
| `grep -h '"tool":"Agent"' .harness/state/spans/d3ef8106*.jsonl` | 2 Zeilen, **byte-identisch** mit den Zitaten in `:178-180` und `:222-224` |
| `grep -h '"seq":65,' .harness/state/spans/a2195604*.jsonl` | 2 Zeilen; die `Agent`-Zeile ist byte-identisch mit `:160`, daneben eine `Bash`-Zeile (s. LOW-4) |
| `grep -h '"tool":"Agent"' .harness/state/spans/af347d77*.jsonl` | 1 Zeile, `duration_ms:14`, keine Rolle, keine Zähler — deckt `:182-183` |
| `grep -h '"tool":"Agent"' .harness/state/spans/*.jsonl \| grep -c '"ts":"2026-08-21'` | **6** `Agent`-Spans am Messtag; davon mit `spawned_role` oder `input_tokens`: **0** |
| `grep -h 'spawned_role' .harness/state/spans/*.jsonl \| head -2` | zwei Zeilen vom 2026-08-03 mit `spawned_role:"reviewer"` **und** allen vier Zählern — der Emitter **kann** die Achse schreiben |
| Sonde aus `:39-112` und Fixture aus `:121` extrahiert, mit `bash`+`awk` über die Fixture gefahren | wohlgeformte `ask`-Ausgabe, Exit 0; CHECK1/CHECK2/CHECK3 reproduziert |
| `printf '%s' "$ti" \| wc -c` / `wc -m` über den `tool_input`-Bereich | **304** Bytes / 303 Zeichen — die `304` in `:131` ist die **Byte**-Zahl und stimmt |
| `grep -c 'slice-086' docs/plan/planning/in-progress/roadmap.md` | **0** — `MR-016` Setzung 2 gehalten |
| `ls docs/plan/planning/in-progress/` | `roadmap.md` + genau ein Slice — WIP-Limit gehalten |
| `grep -n 'Status:' docs/plan/adr/00{11,12,15,16,19,20}-*.md` | sechsmal `Accepted` — keine superseded Referenz |
| `make docs-check` (nach dem Schreiben dieses Reports) | s. §Gate-Lauf unten |
| `git status --porcelain` (nach dem Lauf) | nur dieser Report (`??`) |

---

## Findings

Jedes Finding folgt dem §Output-Schema des Reviewer-Skills. HIGH zuerst; kein Lösungsvorschlag
im Befund.

**HIGH: keines.** Kein Gate berührt, kein Stilles-Grün-Pfad, kein halluziniertes Gate, keine
superseded ADR referenziert, keine Hard Rule verletzt (§3.3/§3.6/§3.7/§3.8 einzeln geprüft,
s. §Negativbefunde).

### MEDIUM-1 — Die tragende Beleg-Quelle des Dokuments ist die, die `ADR-0011` ausdrücklich zur Nicht-Beleg-Quelle erklärt; die zwei Stellen sind nirgends zusammengeführt

- **kategorie:** MEDIUM
- **quelle:** `ADR-0011` (**Accepted**) Festlegung 3, dritter Punkt — gegen `ADR-0019`
  (**Accepted**) Festlegung 4 und `CO-002` §Auflösungs-Trigger, die dieselbe Lektüre anordnen
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:228-232` gegen
  `docs/plan/adr/0011-telemetrie-erfassung-policy.md:172-174`
- **befund:** `ADR-0011` sagt an `:172-174`: *„**Kein Beleg-Status.** Ein Span ist kein
  Review-Gegenstand und keine Quelle für eine Zusage im Sinne von `AGENTS.md` §3.6. Was belegt
  werden muss, wird gemessen — nicht aus dem Log gelesen."* Das Zeitdokument überschreibt seinen
  Ergebnis-Abschnitt mit *„Die eine Beobachtung, **am Span gelesen**"* und trägt sie in §8 als
  Auftrag weiter. Kein Artefakt im Repo führt die zwei Stellen zusammen: `grep -rn 'Beleg-Status'
  --include='*.md' . | grep -v '^./.harness/baseline'` → 6 Treffer, alle in `ADR-0011` selbst und
  in den Proposed-Runden zu `ADR-0011`; keiner in `ADR-0019`, `CO-002`, im Slice-Plan oder in
  diesem Dokument.
- **failure-szenario:** Die Folge-ADR aus §8 ruht auf drei Span-Zeilen. `ADR-0011` hat die
  Integritäts-Anforderung an den Bestand mit genau diesem Satz gesenkt — der Bestand ist
  gitignored, maschinenlokal, ohne Prüfsumme und ohne Manipulationsschutz, und der Emitter räumt
  beim Sitzungsstart selbst darin auf. Ist eine der Zeilen falsch oder verändert, fängt das kein
  Gate ab; die permanente Überführung von `CO-002` (`AGENTS.md` §3.4: ab *Accepted* immutabel)
  stünde dann auf einer Quelle, die eine aktive ADR als Quelle ausschließt.
- **nicht beanstandet:** die Zeilen selbst. Ich habe alle drei im Bestand nachgelesen — sie sind
  byte-identisch vorhanden, und die Gegenprobe „schreibt der Emitter die Achse überhaupt?" fällt
  positiv aus (zwei Zeilen vom 2026-08-03 tragen `spawned_role` + vier Zähler). Beanstandet ist
  der ungeklärte Rang zwischen zwei aktiven ADRs, nicht die Beobachtung.
- **verifizierbar:** ja, ohne Gate-Lauf — `sed -n '172,174p'
  docs/plan/adr/0011-telemetrie-erfassung-policy.md` neben `sed -n '228,232p'
  docs/reviews/2026-08-21-updatedinput-messung.md` lesen. Rollen-Verweis: Auflösung durch den
  Architect (`AGENTS.md` §3.4/§3.8), nicht durch die Implementation.

### MEDIUM-2 — Das Kommando, mit dem das Dokument seine Nicht-Aufzeichnung belegt, liefert am Bestand jetzt einen Treffer

- **kategorie:** MEDIUM
- **quelle:** `LH-QA-02` (Reproduzierbarkeit); `AGENTS.md` §3.6 (die Zusage, die der Beleg trägt);
  `ADR-0011` Festlegung 2 (die Zusage selbst)
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:137-145`, dazu `:121` und `:138`
- **befund:** §4 nennt als Suche
  `grep -rlF 'SONDE-MARKER-7c1de4b2a90f' --exclude-dir=.git --exclude=sonde-fixture-agent-payload.json .`
  und für Lauf 3 das Ergebnis *„dieselbe Suche **ohne** die Fixture-Ausnahme über das ganze Repo —
  leer (Exit 1)"*. Selbst gefahren am 2026-08-22 auf HEAD `31a7908`: **ein Treffer**,
  `docs/reviews/2026-08-21-updatedinput-messung.md`, Exit 0 — das Dokument führt die Markierung
  zweimal selbst (`:121` in der Fixture, `:138` im Kommando). Für die Kontroll-Markierung gilt
  dasselbe: `grep -rlF 'SONDE-KONTROLLE-9d4b' --exclude-dir=.git .` → ein Treffer, dieselbe Datei.
- **failure-szenario:** DoD (1) sagt zu, dass sich die Markierung *„in keiner Datei unterhalb des
  Repos wieder[findet]"*. Wer diese Zusage mit dem im Dokument abgedruckten Kommando nachprüft,
  bekommt Exit 0 und muss zwischen *„die Sonde hat geschrieben"* und *„das Dokument zitiert sich
  selbst"* entscheiden — ohne dass das Dokument den Fall nennt. Der einzige Beleg für die
  `ADR-0011`-Festlegung-2-Zusage ist damit nicht mehr nachfahrbar, und die nächste Sonde derselben
  Bauart erbt eine Suche, die per Konstruktion fündig wird.
- **kein Abfluss:** die Markierung ist synthetisch und selbst verfasst; es steht kein fremder
  Inhalt im Repo. Beanstandet ist der Beleg, nicht die Erfassung.
- **verifizierbar:** ja, ohne Gate-Lauf — Kommando oben.

### MEDIUM-3 — Die Span-Lektüre steht ohne Kommando und ohne Ort; das Dokument nennt den Bestandspfad an keiner Stelle

- **kategorie:** MEDIUM
- **quelle:** Slice-Plan DoD (2) (`:78` — *„steht **mit ihrem Kommando** in einem Zeitdokument"*)
  und §3 (`:148` — *„Das Zeitdokument aus DoD (2) trägt Kommando, Datum und Ergebnis"*);
  `LH-QA-02`
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:157-161` · `:175-180` · `:219-224`
- **befund:** Die drei Span-Zeilen sind mit *„Span des Aufrufs …:"* bzw. *„Span (seq 1 der
  Sitzung)"* eingeleitet und roh zitiert. Das ganze Dokument enthält **genau ein** Kommando
  (`:138`); für die Span-Lektüre nennt es keines, und die Zeichenkette `state/spans` kommt darin
  nicht vor (`grep -c 'state/spans' docs/reviews/2026-08-21-updatedinput-messung.md` → **0**).
  Der Plan verlangt das Kommando in derselben Zeile, in der er die Beobachtung verlangt.
- **failure-szenario:** Der Verifier und danach der Architect sollen die eine Beobachtung
  nachvollziehen, an der der Ausgang des Slice hängt. Ohne Pfad und ohne Kommando muss jeder
  Nachprüfer den Leseweg selbst erfinden — ich habe ihn aus dem Slice-Plan (`:141-147`)
  rekonstruiert, nicht aus dem Dokument. Wächst der Bestand weiter oder räumt der Emitter einen
  Strom weg, ist aus dem Dokument nicht entscheidbar, wo die Zeilen gestanden haben; die
  Beobachtung wird von einer nachfahrbaren zu einer geglaubten.
- **nicht beanstandet:** der Inhalt der Zitate — alle drei sind byte-identisch im Bestand
  vorhanden (Kommandos in §Selbst gefahren).
- **verifizierbar:** ja, ohne Gate-Lauf.

### MEDIUM-4 — Die einzige Gegenkraft gegen den „Negativ aus der falschen Ursache" ist repo-extern, und §7 führt sie nicht unter den Grenzen

- **kategorie:** MEDIUM
- **quelle:** Slice-Plan §6 erster Punkt (`:226-231` — *„Die Kontroll-Beobachtung aus DoD (2) ist
  die einzige Gegenkraft; ohne sie wird nicht gedeutet"*); `LH-QA-02`
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:212-218` gegen `:234-241`
- **befund:** Die Übernahme von `updatedInput` ist als Prosa mit dem Zusatz *„(Screenshot-belegt)"*
  festgehalten: die Tool-Zeile habe `Explore(SONDE-KONTROLLE-9d4b)` gezeigt, der Agent sei unter
  dem Marker-Namen durchgelaufen. Im Repo liegt kein Beleg dafür, und die daneben zitierte
  Span-Zeile (`:222-224`) trägt weder `description` noch die Betriebsart, kann die Aussage also
  nicht stützen. §7 nennt drei Grenzen — Strippen vs. Ignorieren, Momentaufnahme,
  `make span-report` —; die Nicht-Nachprüfbarkeit gerade dieser Beobachtung ist keine davon.
- **failure-szenario:** Vom Hintergrund-Start ist am Span genug ablesbar (`duration_ms: 3`, keine
  Zähler); von der Übernahme nichts. Fällt sie weg, ist das verbleibende Bild exakt das, was Plan
  §6 als teuersten Fehler des Slice benennt — ein Negativ, das die eigene Sonde misst. Ein
  Verifier, der §7 als vollständige Grenzen-Liste liest, sieht diese Lücke nicht und trägt den
  Ausgang in die Folge-ADR, wo er nach `AGENTS.md` §3.4 einfriert.
- **verifizierbar:** ja, ohne Gate-Lauf — `:212-218` gegen `:222-224` und `:234-241` lesen.

### LOW-1 — Das Kopf-Ergebnis schreibt dem Splice zu, was am statischen Kontroll-`updatedInput` beobachtet wurde

- **kategorie:** LOW
- **quelle:** `AGENTS.md` §3.6 (die Zusage auf das einschränken, was gemessen ist)
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:9-13` gegen `:170-171` und `:228-232`
- **befund:** `:9-13` fasst zusammen: *„`updatedInput` wird übernommen …, aber ein
  **eingespleistes** `"run_in_background": false` erzeugt trotzdem einen Hintergrund-Start."*
  Nachweislich übernommen wurde das **statische** `updatedInput` der Kontroll-Fassung (§6), die
  nichts parst und keinen Splice enthält. Für den Splice-Lauf (§5) hält dasselbe Dokument fest:
  *„Der Dialog zeigt die rohe Eingabe nicht an; ob `updatedInput` übernommen wurde, klärt erst der
  Kontroll-Lauf (§6)."* §7 formuliert die Aussage korrekt („dessen Eingabe per `updatedInput`
  nachweislich ersetzt wurde"); der Kopf zieht die zwei Läufe zusammen.
- **failure-szenario:** Ein späterer Schnitt zitiert den Kopf als Kurzfassung — dieselbe Klasse,
  die Runde 2 als LOW-3 an der `ADR-0019`-Index-Zeile gefunden hat — und hält den Splice-Weg für
  in vivo geprüft. Geprüft ist er nur offline gegen eine synthetische Fixture (§4); ob das Werkzeug
  eine gespleißte Ausgabe annimmt, ist an keiner Stelle beobachtet.
- **verifizierbar:** ja, ohne Gate-Lauf.

### LOW-2 — Die Kontroll-Fassung ersetzt Auftrag **und** Prompt des realen Aufrufs; das Dokument gibt sie als reproduzierbares Material aus, ohne diese Wirkung zu nennen

- **kategorie:** LOW
- **quelle:** Slice-Plan DoD (1) (`:59-60` — *„`updatedInput` ist das **unveränderte**
  Eingabeobjekt plus `"run_in_background": false`"*) und §3 Berührte Dateien (`:187` — *„ihr Text
  steht im Zeitdokument aus DoD (2) und ist von dort reproduzierbar"*)
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:193-209`
- **befund:** Die verbatim abgelegte Kontroll-Sonde konsumiert stdin nach `/dev/null` und gibt ein
  statisches `updatedInput` mit fremdem `description` **und** fremdem `prompt` (*„Gib als
  Endergebnis genau das Wort OK zurück."*) zurück. Jeder `Agent`-Aufruf einer Sitzung, in der sie
  verdrahtet ist, wird damit durch einen anderen Auftrag ersetzt. Das Dokument benennt den
  Unterschied zur DoD-Sonde als Bauart (*„parst NICHTS"*), nicht als Wirkung auf den Aufruf.
- **failure-szenario:** Wer den Abschnitt als Reproduktions-Anleitung nimmt und die Fassung in
  einer Arbeitssitzung verdrahtet, bekommt für jeden Subagenten-Aufruf still das Ergebnis eines
  anderen Auftrags. Da die Sonde bewusst in keine Datei schreibt, bleibt im Repo keine Spur, an der
  das auffiele; nur der `ask`-Dialog zeigt es, und der wird weggeklickt.
- **verifizierbar:** ja, ohne Gate-Lauf.

### LOW-3 — `3f27b7c` ändert drei Dateien, die der Plan nicht als berührt führt — darunter die, die §6 ausdrücklich ausnimmt

- **kategorie:** LOW
- **quelle:** Slice-Plan §3 (Tabelle *Berührte Dateien*) und §6 letzter Punkt (`:242-246` —
  *„Nicht in diesem Slice: … jede Änderung an `CO-002` selbst"*)
- **pfad:** `docs/plan/carveouts/CO-002-token-achse-je-rolle.md:26,97,150` ·
  `docs/plan/planning/welle-09-modul-15-konformitaet.md:204` ·
  `docs/plan/planning/done/slice-062-emittierte-modul-15-regeln.md:461`
- **befund:** Die Tabelle in §3 führt vier Einträge (`.claude/hooks/`, `.claude/settings.json`,
  das Zeitdokument, die Fixture) und eine Zeile *unverändert* (`Makefile`, `.d-check.yml`, `spec/`,
  `test/`); `CO-002`, `welle-09` und `done/slice-062` kommen darin nicht vor, und §6 nimmt `CO-002`
  ausdrücklich aus. `3f27b7c` ändert in allen dreien Zeilen. Geprüft (`git show 3f27b7c -U2`): es
  sind ausschließlich Pfad-Reparaturen `open/` → `in-progress/`; keine Aussage ist bewegt. Ohne sie
  wäre `make docs-check` rot — die `docs/reviews/**`-Ausnahmen aus `.d-check.yml` gelten für diese
  drei Dateien nicht.
- **failure-szenario:** Ein späterer Schnitt beruft sich auf diesen Commit als Präzedenz dafür,
  dass „Link-Reconciliation" eine als ausgenommen geführte Datei berühren darf, und nimmt dabei
  eine inhaltliche Zeile mit — bei `CO-002` etwa den Status oder eine Trigger-Hälfte. Die
  Plan-Grenze aus §6 unterscheidet die zwei Fälle nicht, und ein Wächter, der Commit-Zuschnitte
  liest, existiert nicht (`AGENTS.md` §3.8 §*Ein Wächter existiert nicht*).
- **verifizierbar:** ja, ohne Gate-Lauf — `git show 3f27b7c -U2` gegen `sed -n '180,195p;242,246p'
  docs/plan/planning/in-progress/slice-086-vordergrund-per-updatedinput.md`.

### LOW-4 — Die Fundstelle „Sitzung `a2195604…`, seq 65" ist im Bestand nicht eindeutig

- **kategorie:** LOW
- **quelle:** `LH-QA-02`; `ADR-0011` Festlegung 3 (*„Je (Sitzung, Agent) ein eigener Strom"*)
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:157`
- **befund:** `grep -h '"seq":65,' .harness/state/spans/a2195604*.jsonl` liefert **zwei** Zeilen:
  die zitierte `Agent`-Zeile (`14:43:31Z`) und eine `Bash`-Zeile (`13:50:35Z`,
  `agent_role:"planner"`, anderer Commit) aus einem zweiten Strom derselben Sitzung. Die
  Sequenznummer ist je (Sitzung, Agent) vergeben, nicht je Sitzung; als Fundschlüssel taugt sie
  zusammen mit der Sitzung nicht.
- **failure-szenario:** Wer die Zeile über die angegebene Koordinate sucht, findet zuerst einen
  `Bash`-Span mit anderer Rolle und anderem Commit und hält das Zitat für falsch oder das Dokument
  für ungenau. Eindeutig ist allein die `tool_use_id`, die zwar in der zitierten Zeile steht, aber
  nicht als Fundschlüssel benannt ist.
- **verifizierbar:** ja, ohne Gate-Lauf — Kommando oben.

### INFO-1 — `AGENTS.md` §3.7 bindet den verbatim abgelegten Sondentext nicht, und die Geltungsbereich-Zeile kennt diesen Fall nicht

- **kategorie:** INFO
- **quelle:** `AGENTS.md` §3.7 §Geltungsbereich
- **pfad:** `docs/reviews/2026-08-21-updatedinput-messung.md:38-113` und `:193-209` gegen
  `AGENTS.md` §3.7
- **befund:** §3.7 gilt für *„Code, Konfiguration und Skripte, **die dieses Repo besitzt**"* und
  nimmt namentlich nur `.harness/baseline/` aus. Der Sondentext liegt als Code-Block in einer
  Markdown-Datei: die Datei ist weder Konfiguration noch Skript, die Sonde war nie committet und
  existiert nicht mehr. Ich habe die beiden Kopf-Kommentare deshalb **nicht** gegen die
  Fünf-Klassen-Regel geprüft. Zugleich ist derselbe Text laut Plan §3 die reproduzierbare Fassung
  der Sonde. Welcher der beiden Lesarten die Geltungsbereich-Zeile folgt, sagt sie nicht.
- **failure-szenario:** Ein späterer Messaufbau wird wieder nur im Zeitdokument abgelegt und trägt
  im Kopf, was §3.7 an committetem Code untersagt — Konjunktiv über die verworfene Alternative,
  Prosa über abwesenden Text. Weder eine Regel-Zeile noch ein Sensor widerspricht:
  `make comment-claims` schließt **jede** Markdown-Datei dauerhaft aus (`harness/README.md`
  §comment-claims, Punkt 2), und der Prüfbereich ist auf vier Pfad-Muster begrenzt.
- **verifizierbar:** nein — es gibt keinen Gate-Lauf, der den Geltungsbereich entscheidet. Der Text
  von §3 gehört dem Architect (`AGENTS.md` §3.8, `ADR-0015` Festlegung 1); hier nur benannt, damit
  die **Nicht**-Prüfung nicht als Prüfung gelesen wird.

---

## Negativbefunde

- **geprüft, ohne Befund: die zwei Move-Commits gegen `AGENTS.md` §3.3.** `git show 6c4c321 --raw
  -M` und `git show c874ae9 --raw -M` melden je `R100` mit identischem Blob auf beiden Seiten
  (`bf529b7 bf529b7`) — reine Renames, kein Inhalts-Byte bewegt, Move und Rewrite sauber getrennt.
- **geprüft, ohne Befund: die Link-Reconciliation `3f27b7c` inhaltlich.** Sieben geänderte Zeilen,
  ausschließlich `open/` → `in-progress/` bzw. `../open/`-Präfixe an den Geschwister-Links auf
  slice-071/slice-074. Keine stehengebliebene Adresse:
  `grep -rn 'open/slice-086\|next/slice-086' --include='*.md' . | grep -v '^./.harness/baseline'`
  → 3 Treffer, alle in `docs/reviews/**` (Zeitdokumente; per `.d-check.yml`
  `codepaths.exempt-paths` ausgenommen und per Repo-Praxis unangetastet). Der Zuschnitt (Move,
  Move, Reconciliation) folgt der Sequenz aus Plan §5.
- **geprüft, ohne Befund: die Grenzen des Slice.** `git diff --name-only f93f08d..31a7908 --
  Makefile .d-check.yml spec test internal cmd harness .claude` ist **leer** — nichts geht in
  `make gates`, keine Fixture unter `test/`, keine Spec-Festlegung, kein Gate-Name (`LH-QA-01`).
  Der Runde-2-Befund MEDIUM-3 (*Fixture unter `test/`*) kehrt **nicht** wieder: der Plan hat ihn
  aufgenommen (§3 *Berührte Dateien*, Zeile *„und **nicht** unter `test/`"*), und die
  Implementation hält ihn ein.
- **geprüft, ohne Befund: die Rücknahme des Messaufbaus.** `git status --porcelain` leer;
  `ls .claude/hooks/` zeigt die drei committeten Hooks und keine Sonde/Fixture;
  `.claude/settings.json` führt unter `PreToolUse` zwei Matcher mit je einem Kommando (kein dritter
  Eintrag) und liegt nicht im Range-Diff. Die Permission-Lage ist unverändert; die Präzedenz aus
  `ADR-0016` (uncommitteter Messaufbau) ist eingehalten, und `"ask"` statt `"allow"` heißt: keine
  Senkung, also kein Fall für `AGENTS.md` §3.5.
- **geprüft, ohne Befund: `LH-QA-03`.** Der Sondentext führt `bash`, `awk`, `printf`, `cat` und
  sonst nichts — kein `jq`, kein `node`, kein gebautes Binär. Selbst nachgefahren mit `bash`+`awk`
  allein; der Bauweg ist der Splice aus Plan §3, kein JSON-Encoder.
- **geprüft, ohne Befund: der Splice selbst.** Sonde (`:39-112`) und Fixture (`:121`) verbatim
  rekonstruiert und gefahren: CHECK1 (Marker durchgereicht), CHECK2 (Splice am Ende) und CHECK3
  (Byte-Identität) reproduzieren; `304` Bytes durchgereicht — die Zahl in `:131` ist die
  **Byte**-Zahl (`wc -c` 304 gegen `wc -m` 303, der Umlaut in der Fixture) und stimmt. Die vier
  eingebauten JSON-Fallen (zitiertes `"tool_input"`, Klammer im Freitext, `\\`-Escape, Umlaut)
  brechen den zeichenweisen Scanner nicht.
- **geprüft, ohne Befund: misst die Messung ihre eigene Kette?** Der Emitter **kann** die Achse
  schreiben — `grep -h 'spawned_role' .harness/state/spans/*.jsonl` liefert Zeilen vom 2026-08-03
  mit `spawned_role:"reviewer"` **und** allen vier Zählern. Das Fehlen am 2026-08-21 (6 `Agent`-
  Spans, 0 mit Rolle oder Zähler) ist damit keine Emitter-Eigenschaft. Die von Plan §6 gefürchtete
  Falsch-Ursache liegt auf der Hook-Ebene, nicht auf der Emitter-Ebene — dort ist sie durch den
  Kontroll-Lauf adressiert (Beleg-Form s. MEDIUM-4).
- **geprüft, ohne Befund: die Span-Zitate gegen den Bestand.** Alle drei zitierten Zeilen sind
  byte-identisch vorhanden (`:160`, `:179`, `:223`); die Nebenangabe zu `af347d77…`
  (`duration_ms: 14`, keine Rolle, keine Zähler) stimmt ebenfalls. `model_version:
  claude-opus-5[1m]` steht in allen dreien, wie `:15-16` sagt. Fundschlüssel s. LOW-4.
- **geprüft, ohne Befund: die Klasse „Abdeckungszahl statt Span" (Runde 1 MEDIUM-2 / Runde 2
  MEDIUM-2).** Sie kehrt **nicht** wieder: `:240-241` hält `make span-report` ausdrücklich daneben
  und nennt seinen schwächeren Test (ein Zähler genügt, Rolle wird nicht gefragt) — deckungsgleich
  mit `CO-002` §Auflösungs-Trigger.
- **geprüft, ohne Befund: Status-Schnappschüsse.** Das Zeitdokument nennt keinen ADR-Status. Die
  eine Status-Aussage im Range steht in einer Commit-Message (`6c4c321`: *„ADR-0019 ist
  Accepted"*) — an unveränderlicher Stelle, und am Prüftag zutreffend.
- **geprüft, ohne Befund (REFUTED): `AGENTS.md` §3.8 an `3f27b7c`.** Der Commit berührt mit
  `CO-002` **kein** Artefakt, das §3.8 bindet. §3.8 und `ADR-0015` Festlegung 1 binden
  ausschließlich `AGENTS.md` §3 und den Adaptions-Block in `harness/conventions.md` und sagen über
  andere Norm-Artefakte ausdrücklich nichts: *„Wo eine Quelle die schreibende Rolle benennt, gilt
  sie unverändert; wo keine sie benennt, bleibt das eine offene Frage."* Für Carveouts benennt
  keine Quelle eine schreibende Rolle — `grep -ni carveout harness/conventions.md` → 3 Treffer,
  alle zur Template-Ablage (`MR-008`); `docs/plan/carveouts/README.md` nennt keine Rolle. Der
  Befund wäre eine Zuordnung, die keine Quelle trägt.
- **geprüft, ohne Befund: `AGENTS.md` §3.6 am Gegenbeispiel.** Das Gegenbeispiel zur
  Nicht-Aufzeichnung ist real und **zuerst rot** gefahren (schreibende Variante → Suche findet die
  Datei, Exit 0), erst danach der fundfreie Lauf — die Reihenfolge, die DoD (1) verlangt. Kein
  Dauer-Fall, kein vakuös grüner Wächter. Beanstandet ist nur die Nachfahrbarkeit des Belegs heute
  (MEDIUM-2), nicht seine Entstehung.
- **geprüft, ohne Befund: `ADR-0011` Festlegung 2 (Prompt).** Die Sonde hat genau einen Ausgang
  (stdout an das Werkzeug), im Sondentext steht keine Umleitung in eine Datei. Im committeten
  Dokument steht kein fremder Prompt: Fixture-`prompt` und Kontroll-`prompt` sind selbst verfasst,
  die drei Span-Zeilen tragen keine Argumente (Agent-Default aus Festlegung 2: nur Name und
  Status).
- **geprüft, ohne Befund: Übergabe §8 gegen Plan DoD (3) negativer Zweig, `ADR-0019`
  §Re-Evaluierungs-Trigger dritter Punkt und `CO-002` §Auflösungs-Trigger zweiter Ausgang.** Alle
  vier sagen dasselbe: nur die zwei fremden Trigger bleiben, Modul-7-Frage 2 kippt auf *Nein*,
  `CO-002` ist in eine Folge-ADR zu überführen und endet in `done/`. Auch die Rollen-Zuordnung ist
  deckungsgleich: der Plan sagt an `:100-101` *„die ADR gehört dem Architect, der Status-Wechsel
  und der `git mv` des Carveouts dem Implementer"*, das Dokument an `:256-258` *„Der Status-Wechsel
  und der `git mv` des Carveouts gehören dem Implementer des Folge-Schnitts, die ADR dem
  Architect"*. §8 verschiebt nichts und nimmt nichts vorweg.
- **geprüft, ohne Befund: `MR-016` Setzung 2/3 und der WIP-Eintritt.**
  `grep -c 'slice-086' docs/plan/planning/in-progress/roadmap.md` → **0** (kein Roadmap-Eintrag,
  wie §3 zusagt); `ls docs/plan/planning/in-progress/` → `roadmap.md` plus genau ein Slice, das
  WIP-Limit aus §4 war frei.
- **geprüft, ohne Befund: die referenzierten ADRs sind aktiv.** `ADR-0011`, `ADR-0012`, `ADR-0015`,
  `ADR-0016`, `ADR-0019`, `ADR-0020` — sechsmal `**Status:** Accepted`. Keine superseded oder
  deprecated Referenz im Range.
- **nicht geprüft (nicht in diesem Diff):** Closure-Notiz und Steering-Loop-Lerneintrag (Plan §5
  hängt sie an die Closure, §7 der Plan-Datei ist leer); die DoD-Abhakung (Verifier, Modul 11); der
  `make mutate`-Lauf.

---

## Gate-Lauf

Nicht als Prüfergebnis gewertet, sondern als Nachweis, dass dieser Report das Doku-Gate nicht
bricht (`docs/reviews/**` ist in `.d-check.yml` von `ids` und `codepaths` ausgenommen, **nicht**
von `links`/`anchors` — dieser Report führt darum keine Markdown-Links, nur Inline-Code-Pfade):

| Kommando | Ergebnis |
|---|---|
| `make docs-check` | `d-check: 327 Datei(en) geprüft, 0 Befund(e)`, Exit 0 |

Der CI-Lauf `32514535860` auf `31a7908` (grün in gates/smoke/mutate/full-smoke) ist Eingangs-
Kontext, kein Prüfergebnis dieses Reports: *was CI prüft, ist genau der Inhalt der `make`-Targets*
(`AGENTS.md` §4), und keines davon liest ein Zeitdokument.

---

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 4 |
| LOW | 4 |
| INFO | 1 |

**Steering-Loop-Signal (Skill §Kontext-Eskalation).** MEDIUM-2, MEDIUM-3 und MEDIUM-4 sind
**dieselbe Klasse**: *ein Beleg, den das Kommando im Dokument nicht (mehr) herstellt*. Die dritte
Wiederholung derselben Klasse in einem Lauf ist laut Skill kein reines Melde-Ereignis mehr,
sondern ein Signal an Guide/Sensor. Verwandt sind Runde 1 MEDIUM-1 (*„das zweite Ende ist keine
Payload-Messung"*) und Runde 2 LOW-2 (*„der Verweis stützt die Sache, sagt aber etwas anderes als
die Stelle"*) am selben Gegenstand. Benannt, nicht gelöst — der Sensor ist nicht meine Rolle.

---

## Verdikt

**Merge-blockierend: ja.** Vier MEDIUM; HIGH und MEDIUM blockieren nach Skill §Ablage
typischerweise, und hier ist keine Abweichung begründbar.

**Begründung, und was sie ausdrücklich nicht sagt.** Keines der vier Findings bestreitet das
**Ergebnis**. Ich habe den negativen Ausgang gegengeprüft, soweit ein Subagent das kann: die
Span-Zitate stimmen byte-genau, am Messtag trägt keiner der sechs `Agent`-Spans Rolle oder Zähler,
der Emitter kann beides nachweislich schreiben, und der Splice reproduziert bis auf die Byte-Zahl.
Der Weg über `PreToolUse`-`updatedInput` trägt nach dieser Beleglage nicht.

Blockierend ist, **worauf dieses Ergebnis steht**. Das Werkstück des Slice ist kein Code, sondern
ein Beleg — und dieser Beleg soll eine Folge-ADR tragen, die ab *Accepted* immutabel ist
(`AGENTS.md` §3.4) und einen Carveout permanent macht. Drei der vier Findings treffen genau die
Nachfahrbarkeit dieses Belegs (MEDIUM-2/-3/-4), das vierte den ungeklärten Rang zwischen zwei
aktiven ADRs über die Frage, ob ein Span überhaupt belegen darf (MEDIUM-1). Ein Beleg, der beim
Nachfahren ein anderes Ergebnis liefert als er behauptet, ist an einer immutablen Entscheidung
teurer als eine falsche Zahl in einem Slice.

**Rollen-Trennung im Konfliktfall.** MEDIUM-1 ist kein Implementations-Befund: die Auflösung
zwischen `ADR-0011` Festlegung 3 und `ADR-0019` Festlegung 4 gehört dem Architect
(`AGENTS.md` §3.4/§3.8, `ADR-0015`). Wird der Befund herabgestuft, dann über den Konflikt-Pfad aus
Modul 8 mit Übergabe-Artefakt — nicht, weil die Implementation widerspricht.

**Übergabe:** Findings gehen an die Implementation; MEDIUM-1 zusätzlich an den Architect. Der
Report ersetzt keine Verifikation — DoD-/Spec-Konformität prüft der Verifier separat (Modul 11,
anderes Prüf-Artefakt, anderer Eingabe-Kontext).
