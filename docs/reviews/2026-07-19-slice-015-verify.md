# Verifikations-Report — slice-015 (Zitat-Verifikation via d-check / `codepaths.check-lines`)

- **Rolle:** Unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Datum:** 2026-07-19
- **Slice:** `docs/plan/planning/in-progress/slice-015-zitat-sensor.md`
- **Diff:** `git diff 792ab10 HEAD` (Impl-Commit `0f87af4`)
- **d-check-Pin:** `ghcr.io/pt9912/d-check:v0.50.0` @ `sha256:9b80ce1d74e745cc266b476701bccb76059783916fe18651e00df96480a94364`

## Gesamt-Verdikt: BESTÄTIGT (0 VIOLATED)

Alle DoD-Zeilen aus §2 bestätigt. Die einzige nicht-erfüllte Teil-Klausel
(Closure-Notiz aus DoD-7) ist **prozessual nachgelagert** (Closure erfolgt *nach*
der Verifikation, Slice liegt noch in `in-progress/`) — **kein** Verstoß.

---

## DoD-Zeilen (§2) — einzeln

### DoD-1 — d-check-Pin v0.46.0 → v0.50.0, Fragment tool-generiert + re-adaptiert, Digest dreifach belegt → **CONFIRMED**

- **Pin gesetzt:** `d-check.mk` `DCHECK_IMAGE ?= …:v0.50.0`, `DCHECK_DIGEST ?= sha256:9b80ce1d…a94364` (Z. 15–16).
- **Fragment ist genuin tool-generiert (unabhängig belegt):** `d-check@v0.50.0 --print-mk`
  selbst erzeugt und gegen das Repo-`d-check.mk` gedifft. Die **einzigen** Unterschiede sind
  die vier dokumentierten MR-010-Handgriffe + die MR-011-Kopfnotiz:
  1. Kopfkommentar adaptiert (MR-010/MR-011-Doku statt Tool-Default);
  2. `DCHECK_DIGEST` gepinnt (Tool liefert es **leer** — verifiziert);
  3. `doc-check` → `docs-check` in **Target und Hilfetext**;
  4. `doc-help`-Grep von `^doc-` auf `^docs?-` erweitert.
  Alle Advisory-Recipe-Bodies (inkl. der fünf neuen `--disable citations`) sind
  **byte-identisch** zum Tool-Output — **nicht** handgeschrieben. Das 18. Modul
  `citations` (opt-in) bestätigt: Rohausgabe enthält exakt 5× `--disable citations`.
- **Digest dreifach belegt (2 Achsen unabhängig reproduziert):**
  1. **Lokaler RepoDigest:** `docker inspect …:v0.50.0 .RepoDigests` = `…@sha256:9b80ce1d…a94364`
     (und `docker images --digests`, gleiche Zeile);
  2. **Registry-Inspektion:** `docker buildx imagetools inspect …:v0.50.0` → `Digest: sha256:9b80ce1d…a94364`;
  3. **Pin-Wert** in `d-check.mk`/`emit.go` = derselbe.
  Gegenprobe: v0.46.0 → `sha256:9c317bf1…36a1` (alter Pin, konsistent). LH-QA-02 erfüllt.

### DoD-2 — Emitter-Pin nachgezogen, Coupling-Test grün, Starter-Config minimal → **CONFIRMED**

- `internal/emit/emit.go`: `DefaultImage = …:v0.50.0`, `DefaultDigest = sha256:9b80ce1d…a94364`
  — **identisch** zu `d-check.mk` (`DCHECK_IMAGE`/`DCHECK_DIGEST`).
- **Coupling-Test läuft grün:** `TestDefaultDigest_MatchesCanonical` + `TestDefaultImage_MatchesCanonical`
  (lesen `d-check.mk` und vergleichen) — `make test` meldet `ok internal/emit`.
- **Emittierte Starter-Config bleibt Dogfood-frei:** `internal/emit/templates/d-check.yml`
  = `modules: [links, anchors]`, `codepaths` auskommentiert → **kein** `check-lines`.
  Emitter ≠ Dogfood bestätigt.

### DoD-3 — Pflicht-Trockenlauf beide Achsen, netzlos → **CONFIRMED (selbst reproduziert)**

Beide Achsen selbst gefahren, alle Läufe `--network none`:

- **(a) Pin-Sprung inert (0-Befund-Differenz):** `.d-check.yml` temporär ohne `check-lines`
  (Backup→Strip→Lauf→byte-identischer Restore, Tree danach clean):
  - v0.46.0: `63 Datei(en), 0 Befund(e)`, Exit 0
  - v0.50.0: `63 Datei(en), 0 Befund(e)`, Exit 0
  → identisch, Pin-Sprung inert.
- **(b) v0.50.0 mit `check-lines: true` (aktueller Stand):** `make docs-check`
  = `63 Datei(en), 0 Befund(e)`, Exit 0 — grün über dem realen Korpus.

**Beleg-Teeth (kein No-Op):** In isoliertem Temp-Repo mit `codepaths.roots` erzeugt eine
Out-of-range-Zeilenreferenz reproduzierbar den Befund `citation-out-of-range` (Exit 1);
gültiger Bereich → 0 Befunde. Die Prüfung feuert, wenn das **Ziel** unter `codepaths.roots`
auflöst — genau das Modell des Repos (`roots: [spec, docs, harness]`). Live sind u. a.
`docs/plan/planning/README.md:26` (36 Zeilen) und `harness/conventions.md:18` (481 Zeilen)
in nicht-exempten `done/`-Slices tatsächlich zeilen-geprüft und bestehen → Korpus **nicht-leer**.

*Beobachtung (kein Verstoß):* Zahl **63** statt der in MR-011 notierten **62** — Delta = der
inzwischen im Tree liegende Reviewer-Report (`docs/reviews/2026-07-19-slice-015-review.md`,
untracked). 0 Befunde gilt in beiden Fällen; docs/reviews/** ist codepaths-exempt.

### DoD-4 — `.d-check.yml` mit `check-lines: true` + Kommentar, keine spekulative Exemption → **CONFIRMED**

- `codepaths.check-lines: true` gesetzt (Z. 50), mit begründendem Kommentar (additive Härtung,
  nicht-leerer Prüfbereich, kein eigenständiges Gate, LH-QA-01).
- Diff fügt **nur** den `check-lines`-Block hinzu: `exempt-paths` (nur `docs/reviews/**`) und
  `ignore-refs` (die fünf slice-013-Tombstones) sind **unverändert** — keine neue Exemption.

### DoD-5 — `harness/conventions.md` §Baseline v0.50.0 + neuer MR-011 → **CONFIRMED**

- §Baseline: `d-check: Image v0.50.0 (…, MR-010, MR-011)`.
- MR-011 als vollständiger Adaptions-Block ergänzt (Pin-Sprung + `check-lines` + `citations`-Aufschub
  + Trockenlauf-Beleg). Anker/Link auflösend (docs-check grün belegt die ids/anchors-Konformität).

### DoD-6 — Kein Eigenbau-Artefakt → **CONFIRMED**

- `harness/tools/cite-check.sh`: **absent**.
- `test/cite-check.bats`: **absent**.

### DoD-7 — `make gates` grün; Closure-Notiz mit Steering-Loop-Eintrag → **CONFIRMED (gates)** / **prozessual offen (Closure)**

- **`make gates` = Exit 0** (selbst gefahren, warmer Cache):
  - `baseline-verify`: v3.1.0 OK — 42 Dateien (netzlos)
  - `docs-check`: 63 Dateien, 0 Befunde (v0.50.0-Digest, inkl. `check-lines`)
  - `lint`: golangci-lint `0 issues`
  - `build`: `go build` OK
  - `test`: 53/53 bats **ok** + `go test ./...` alle Pakete **ok** (inkl. `internal/emit`-Coupling)
  - `shell-lint`: shellcheck clean
- **Closure-Notiz (§7) noch leer / DoD-Checkboxen ungehakt:** **erwartungsgemäß** — Closure
  ist der Schritt *nach* der Verifikation (Slice liegt in `in-progress/`; Task „Closure + Move
  nach done/" ist offen). Kein Verifier-Defekt; **Hinweis an Planner:** §7 mit
  Steering-Loop-Lerneintrag füllen und DoD abhaken vor `git mv` nach `done/`.

---

## ADR- / Hard-Rule-Konformität (unabhängig geprüft)

- **ADR-0003 (Docker-only, kein Host-`go`):** Diff berührt nur Config/Doku + 2 Go-Konstanten
  (`emit.go`). **Keine** Host-Toolchain, kein `pip`/`npm`/`go`-Aufruf hinzugefügt; alle Gates
  laufen im gepinnten Image. **Konform.**
- **Hard Rule 3.1 / LH-QA-01 (keine halluzinierten Gates):** Kein neuer Gate-Name in
  `AGENTS.md` §4, `harness/README.md` §Sensors oder `Makefile` (alle drei im Diff **unberührt**).
  `check-lines` ist ein Property an `codepaths`, kein Target. **Konform.**
- **Hard Rule 3.4 (ADRs immutable):** Kein ADR im Diff. **Konform.**
- **Hard Rule 3.5 (Gates nicht ohne ADR lockern):** `check-lines` **verschärft** (MR-001
  „Gate-Anheben → Steering-Loop"), senkt keine Schwelle → kein ADR nötig. **Konform.**
- **Hard Rule 3.3 (git mv + Inhalt = zwei Commits):** Re-Scope-Rewrite (`929721d`, in `open/`)
  und reiner Move (`dd0bff6`, → `in-progress/`) sind getrennte Commits. **Konform.**
- **`citations`-Modul zu Recht nicht aktiviert:** 0 funktionale `<!-- d-check:cite -->`-Direktiven
  im Repo (alle 5 Treffer sind Prosa-Erwähnungen in Inline-Code, inkl. eines `…`-Platzhalters).
  Modul-Liste enthält kein `citations`. Aktivierung wäre nie-feuerndes Gate → korrekt aufgeschoben.

## Beobachtungen (nicht abnahme-relevant)

1. **Stale-Zahl in Slice §6:** „`harness/conventions.md:18` → 438 Zeilen" — Datei hat nach dem
   MR-011-Insert nun **481** Zeilen. Die Referenz `:18 ≤ 481` bleibt gültig (Gate grün); nur die
   Prosa-Zahl im Risiko-Abschnitt ist veraltet. Kein DoD-Effekt.
2. **Trockenlauf-Zahl 62 vs. gemessene 63** — s. DoD-3-Beobachtung (Reviewer-Report im Tree).

## Tree-Zustand

Nach Abschluss: `git status --short` zeigt nur untracked Reports
(`docs/reviews/2026-07-19-slice-015-review.md` vom Reviewer + dieser Verify-Report).
Keine getrackten Änderungen; das temporär modifizierte `.d-check.yml` wurde byte-identisch
restauriert (`git diff` leer). Alle Trockenlauf-Sandboxen lagen außerhalb des Repos.
