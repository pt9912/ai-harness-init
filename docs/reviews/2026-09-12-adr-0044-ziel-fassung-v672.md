# Review — ADR-0044: Ziel-Fassung regiert den Sprung `v6.5.0` → `v6.7.2`

**Rolle:** Reviewer (frischer Kontext — kein Anteil am Architect-Lauf, keiner an den Vorrunden zu
[ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md)) · **Datum:** 2026-09-12 ·
**Gegenstand:** Commit `cb702147` — [ADR-0044](../plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md)
(`Proposed`, 674 Zeilen neu), [ADR-Index](../plan/adr/README.md) (eine Zeile),
[`harness/conventions.md`](../../harness/conventions.md) §Baseline (+9/−3).

**Prüfgrundlage:** [ADR-0018](../plan/adr/0018-ziel-fassung-regiert-die-migration.md),
[ADR-0031](../plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md),
[ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md),
[ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md) (alle `Accepted`) — die vier,
die der Acceptance-Trigger namentlich nennt; dazu
[ADR-0032](../plan/adr/0032-eingefrorene-referenz-folgt-ihrem-rumpf.md) und
[ADR-0034](../plan/adr/0034-register-verzeichnis-form-und-die-ortsfestigkeit-der-register-datei.md)
als Form-Vorbild der Teil-Ablösung · [`AGENTS.md`](../../AGENTS.md) §3.1/§3.4/§3.5/§3.7/§3.8/§3.11 ·
[`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert),
[`MR-033`](../../harness/conventions.md#mr-033--eine-aussage-über-die-baseline-nennt-den-tag-gegen-den-sie-gemessen-ist),
[`MR-054`](../../harness/conventions.md#mr-054--ein-modul-geht-ins-emittierte-doc-gate-nur-mit-erprobung-grünem-start-und-rotem-gegenbeispiel) ·
[`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6),
[`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit).

**Vorherige Findings am gleichen Gegenstand:**
[`2026-09-12-adr-0043-ziel-fassung-v671.md`](2026-09-12-adr-0043-ziel-fassung-v671.md)
(0 HIGH / 1 MEDIUM / 3 LOW, tragende Klasse *Messwert ohne Kommando*) und
[`2026-09-12-adr-0043-ziel-fassung-v671-r2.md`](2026-09-12-adr-0043-ziel-fassung-v671-r2.md)
(0/0/0/1 INFO). LOW-3 jener Runde — *abgeleitete Kopie einer Norm im lebenden Register* — tritt in
diesem Commit zur Hälfte erneut auf (LOW-4 unten).

**Messbasis:** Kurs-Klon `/Development/KI/ai-harness-course` (Tags `v6.5.0`, `v6.7.1`, `v6.7.2`
vorhanden), zwei `git archive`-Bäume im Scratchpad; jede unten genannte Zahl ist in diesem Lauf
selbst gefahren. **Kein Gate-Lauf** — `make gates`/`make docs-check` sind auf Anweisung des
Auftraggebers nicht gefahren; dieser Report trifft deshalb **keine** Aussage über Gate-Farbe.

---

## Findings

### HIGH-1 — Schnitt der Teil-Ablösung · `docs/plan/adr/0044-…-v672.md:465` gegen `0043-…-v671.md:523-525`

`quelle` [`AGENTS.md`](../../AGENTS.md) §3.4 / [ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md) ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:465` ·
`befund` Die Teil-Ablösung schneidet auf §Entscheidung Festlegung 1 und erklärt ausdrücklich
*„**Nicht abgelöst** ist alles Übrige jener Datei"*. Damit bleibt der **unerfüllte** Planner-Auftrag
in [ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md) §Konsequenzen in Kraft:
*„zwei Slices sind zu schneiden — der **Baum-Tausch** samt Nachzug der fünf Pins auf `v6.7.1` und
den sha256 `5d3dba9c…` des Assets `lab-regelwerk.zip` … und der **Adaptions-Durchgang** mit
Delta-Basis `v6.0.0`"* (`:523-527`). Er widerspricht Punkt für Punkt dem gleichlautenden Auftrag in
ADR-0044 (`:590-591`: fünf Pins auf `v6.7.2`, sha256 beim Vollzug am Asset gemessen) und ist nicht
als Ganzes hinfällig: seine zweite Hälfte (Durchgang mit Basis `v6.0.0`) folgt aus Festlegung **2**,
die fortgilt — eine Halbierung, die kein Satz dieser ADR vornimmt. §Konsequenzen benennt als
unsichtbaren Rest nur *„eine Festlegung 1, deren Sprung nicht mehr stattfindet"*; der operative
Auftrag samt konkretem, für `v6.7.1` gemessenem Hash ist nicht benannt. Mit `Accepted` frieren
**beide** Dateien (§3.4), und der Widerspruch ist danach in keiner von beiden mehr behebbar — die
Schadensform, die [ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md)
§Kontext beschreibt. **Versagensbild:** Der Planner schneidet den Tausch-Slice, findet den
konkreten Wert nur in der `Accepted`-ADR und pinnt `v6.7.1`/`5d3dba9c…` gegen ein `v6.7.2`-Asset
([`LH-QA-02`](../../spec/lastenheft.md#lh-qa-02--reproduzierbarkeit)). ·
`verifizierbar` **nein** — kein Modul der [`.d-check.yml`](../../.d-check.yml) liest den Umfang
einer Teil-Ablösung; `make adr-immutable` belegt nur, dass an ADR-0043 nichts geschrieben wurde ·
`klasse` Teil-Ablösung schneidet die Festlegung, nicht ihre Folgepflicht

Beleg (in diesem Lauf gefahren):

```sh
awk '/^#+ /{h=$0} /v6\.7\.1/{printf "%5d  [%s]\n", NR, h}' \
  docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md
# -> u. a. 519, 524 unter [## Konsequenzen] — ausserhalb von §Entscheidung Festlegung 1
sed -n '523,525p' docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md
grep -n 'Nicht abgelöst' docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md   # -> 465
```

Dass `5d3dba9c8dc2df25d5b7dc0bd9b6f5aa123b0ef876dcd9330fe1254898ec72ac` der Hash des
**`v6.7.1`**-Assets ist, steht in ADR-0043 selbst und ist in
[`…-v671.md`](2026-09-12-adr-0043-ziel-fassung-v671.md) §Negativbefunde Prüfpunkt 4 netz-gemessen;
dieser Lauf hat es **nicht** nachgefahren (netzlos).

### MEDIUM-1 — Die Statuszeilen-Behauptung steht gegen die eigene Durchsetzung · `…-v672.md:472-476` und `:548` (Option D)

`quelle` [`AGENTS.md`](../../AGENTS.md) §3.4 / [`.d-check.yml`](../../.d-check.yml) `vcs.head-allow` ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:548` ·
`befund` Option D wird u. a. damit verworfen, ein voller Supersede *„verlangte zudem eine
Statuszeile `Superseded by …` in einer `Accepted`-ADR — das Byte, das
[`AGENTS.md`](../../AGENTS.md) §3.4 und ADR-0040 ausdrücklich einfrieren"*; §Was der Teil-Supersede
umfasst wiederholt es als Tatsache (*„ADR-0040 liest Statuszeile und Trigger-Abschnitt ausdrücklich
als Teil davon"*). Die Durchsetzungsschicht dieses Repos sagt das Gegenteil: `vcs.status-line`
nimmt genau diese Zeile aus dem Kern, und `vcs.head-allow` lässt den Übergang namentlich zu.
[`harness/sensors/adr-immutable.md`](../../harness/sensors/adr-immutable.md) führt ihn als
*„die im Bestand gelebte Link-Form des Supersede-Übergangs"* und als *„erlaubten Übergang"*. Der
Bestand kennt ihn zweimal. Die Schlussfolgerung (an ADR-0043 wird nichts geschrieben) bleibt für
eine **Teil**-Ablösung richtig — der Grund trägt sie nicht. **Versagensbild:** Ein späterer Lauf
zitiert die eingefrorene ADR als Beleg, ein voller Supersede sei in diesem Repo verstellt, oder
liest die zwei bestehenden `Superseded by …`-Statuszeilen als §3.4-Verstöße. ·
`verifizierbar` **ja** (negativ) — der Übergang färbt `make adr-immutable` nicht rot; die
Regex steht in [`.d-check.yml`](../../.d-check.yml) ·
`klasse` Aussage über die Durchsetzungsschicht ohne Messung an ihr

Beleg (in diesem Lauf gefahren):

```sh
sed -n '/^vcs:/,/^[a-z-]*:/p' .d-check.yml | grep -E 'status-line|head-allow'
#   status-line: '^\*\*Status:\*\*'
#   head-allow: '^\*\*Status:\*\* (Accepted|Deprecated|Superseded by \[ADR-[0-9]{4}\]\([0-9]{4}-[^)]+\))$'
grep -h '^\*\*Status:\*\*' docs/plan/adr/[0-9]*.md | sort | uniq -c
#   37 Accepted · 5 Proposed · 1 Superseded by [ADR-0003](…) · 1 Superseded by [ADR-0005](…)
git log -p --follow -- docs/plan/adr/0002-test-tooling-grenze.md | grep -E '^[+-]\*\*Status:'
#   -**Status:** Accepted        (Commit bc447fe9)
#   +**Status:** Superseded by [ADR-0003](0003-go-native-binaries.md)
```

### LOW-1 — *„verböte danach jeden Verweis darauf"* ist weiter als die Konfiguration · `…-v672.md:548`

`quelle` [`.d-check.yml`](../../.d-check.yml) `matrix` ·
`pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:548` ·
`befund` `matrix.status: {forbidden: [superseded, deprecated]}` wirkt nicht auf *jeden* Verweis:
`matrix.exclude-sections: [Historie, "7. Historie", Geschichte]` nimmt ganze Abschnitte aus, und
`matrix.classes` erfasst nur `spec/`-Straten, `docs/plan/adr/[0-9]*.md` und `slice-*.md` — der
ADR-Index selbst (`README.md`) fällt aus dem Muster. Gemessen: alle lebenden Verweise auf die zwei
superseded ADRs liegen genau in diesen Freiräumen. Der tragende Kern des Arguments bleibt (die
23 lebenden Verweise auf ADR-0043 liegen zu 18 im Rumpf einer ADR der Klasse `adr`). ·
`verifizierbar` ja — ein Verweis auf eine superseded ADR aus einem Geschichte-Abschnitt bleibt in
`make docs-check` grün ·
`klasse` Allquantor über eine Konfiguration mit Ausnahmen

```sh
awk 'NR<=407 && /^#+ /{h=NR": "$0} NR==407{print h}' spec/lastenheft.md   # -> 397: ## 7. Historie
awk 'NR<=79  && /^#+ /{h=NR": "$0} NR==79 {print h}' docs/plan/adr/0003-go-native-binaries.md
#                                                                         # -> 75: ## Geschichte
git grep -oE '\]\([^)]*0043-ziel-fassung-regiert-den-sprung-v671\.md[^)]*\)' \
  -- ':!docs/reviews' ':!docs/plan/planning/done' | wc -l                  # -> 23
```

### LOW-2 — Der dokumentierte Fundweg für angeordnete Zusätze findet die Feldform nicht · `docs/plan/adr/README.md:67-78`

`quelle` [`docs/plan/adr/README.md`](../plan/adr/README.md) §Konventionen ·
`pfad` `docs/plan/adr/README.md:76` ·
`befund` §Konventionen beantwortet *„welche ADRs anordnen"* mit
`git grep -l 'Revidiert (Teil-Supersede)' -- 'docs/plan/adr/0*.md'` und zählt ADR-0006, ADR-0013
und ADR-0022 auf. ADR-0032 und ADR-0034 ordnen ebenfalls einen Zusatz an (die Zellen von ADR-0026
und ADR-0030 tragen ihn), führen aber die neuere Feldform `**Supersedes (Teil):**` — der Fundweg
trifft sie nicht, die Aufzählung nennt sie nicht. ADR-0044 nutzt dieselbe Feldform und macht die
Index-Zelle zum **einzigen** Träger ihrer Sichtbarkeit (`:472-476`), ohne die Lücke zu benennen;
mit ihrer Annahme wären es drei von fünf. **Versagensbild:** Ein Audit über den dokumentierten
Fundweg hält die Zelle von ADR-0043 für unangeordnet. **Nicht neu mit diesem Commit** — der
Rückstand besteht seit ADR-0032. ·
`verifizierbar` nein — [`docs/plan/adr/README.md`](../plan/adr/README.md) §Konventionen sagt selbst
*„Kein Sensor hält die Titel- und die Status-Regel"* ·
`klasse` Fundweg deckt nur die ältere von zwei gelebten Formen

```sh
git grep -l 'Revidiert (Teil-Supersede)' -- 'docs/plan/adr/0*.md'   # -> 0006, 0013, 0022
git grep -n '^\*\*Supersedes (Teil):\*\*' -- 'docs/plan/adr/*.md'   # -> 0032, 0034, 0044
```

### LOW-3 — Der zweite Rest in ADR-0043 ist nicht benannt · `…-v672.md:566-568`

`quelle` [`AGENTS.md`](../../AGENTS.md) §3.4 ·
`pfad` `docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md:48` ·
`befund` Das Kopffeld `Kopplung:` von ADR-0043 sagt *„dort ist die Zielstand-Setzung auf `v6.7.1`
verbucht"*. Seit diesem Commit trägt §Baseline `v6.7.2`; die Aussage ist falsch und in der
eingefrorenen Datei nicht korrigierbar. Die §Konsequenzen-Position *„Negativ: Die Teil-Ablösung ist
an der abgelösten Datei nicht sichtbar"* benennt allein den Festlegung-1-Rest. ·
`verifizierbar` nein ·
`klasse` eingefrorene Tatsachenaussage über ein lebendes Register, beim Bewegen nicht benannt

### LOW-4 — Die Buchung trägt weiter eine abgeleitete Folgerung · `harness/conventions.md:51-54`

`quelle` [ADR-0031](../plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md)
Festlegung 2 (*„Sonst nichts"*) ·
`pfad` `harness/conventions.md:53-54` ·
`befund` Der **Wert** der Delta-Basis steht korrekt nicht mehr in der Buchung (Korrektur aus
Runde 1, LOW-3) — die Folgerung *„der Durchgang schließt damit den ausstehenden
`v6.5.0`-Nachweis ein"* steht weiter dort und zweitgefasst in ADR-0044 Festlegung 2. Sie ist aus
der Aufzählung ableitbar und veraltet still, sobald das `v6.5.0`-Nachweis-Feld gefüllt wird; kein
Sensor hält die zwei Fassungen zusammen. **Zweites Auftreten derselben Klasse** in zwei Runden. ·
`verifizierbar` nein ·
`klasse` abgeleitete Kopie einer Norm-Folgerung im lebenden Register

### INFO-1 — Zwei Zahlen mit paraphrasiertem statt literalem Kommando · `…-v672.md:153`, `:255`

`quelle` [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert)
Setzung 1 · `pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:153` ·
`befund` *„(`grep -c` wie oben gegen … am Tag `v6.7.2` → **1**)"* und *„(`diff` je Sektion zwischen
den Bäumen `v6.7.1` und `v6.7.2` → **4**)"* beschreiben ihr Kommando, statt es zu zeigen. Beide
sind in diesem Lauf rekonstruiert und bestätigt (→ `1`, → `4`); dieselbe Form steht in der bereits
angenommenen ADR-0043 (`:148`). · `verifizierbar` nein · `klasse` Zahl neben paraphrasiertem Kommando

### INFO-2 — *„in keinem lebenden Artefakt"* trifft nur die Harness-Ebene · `…-v672.md:286`

`quelle` Maintainability · `pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:286` ·
`befund` `gate.template.md` steht in vier lebenden, getrackten Dateien — `internal/emit/templates.go`
(2×), `internal/emit/templates_test.go`, `test/courseset-fixture.bats`. Gemeint (und richtig) ist
die **Dogfood**-Ebene: weder [`harness/README.md`](../../harness/README.md) noch eine Datei unter
`harness/sensors/` nennt die Vorlage. Die Ebene steht im Satz nicht. · `verifizierbar` ja —
`git grep -n 'gate\.template\.md' -- ':!docs/reviews' ':!docs/plan/planning/done' ':!.harness/baseline'` ·
`klasse` Allaussage ohne Ebenen-Angabe (Dogfood vs. emittiert)

### INFO-3 — Das Stützzitat beantwortet eine andere Bedingung · `…-v672.md:12-15`

`quelle` [ADR-0043](../plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md)
§Re-Evaluierungs-Trigger · `pfad` `docs/plan/adr/0044-ziel-fassung-regiert-den-sprung-v672.md:15` ·
`befund` Das Feld `Supersedes (Teil):` stützt *„nicht an das Ziel eines einzelnen Sprungs
gebunden"* auf den vierten Re-Evaluierungs-Trigger (*„sie bleibt trotzdem die Leseregel"*). Dessen
Bedingung ist eine andere — *„wenn ein Durchgang seine Basis erreicht, ohne dass eine
Re-Baseline-Zeile ein leeres Nachweis-Feld trägt"*. Die Aussage selbst trägt unabhängig davon aus
dem Wortlaut von Festlegung 2 (s. Negativbefund 1); das Zitat ist eine Verallgemeinerung, keine
wörtliche Deckung. · `verifizierbar` nein · `klasse` Zitat trägt weiter als seine Bedingung

---

## Negativbefunde

1. **Der Schnitt der Teil-Ablösung, Hälfte „Festlegung 2 trägt unabhängig": geprüft, ohne
   Befund.** Der normative Satz von ADR-0043 Festlegung 2 (`:403-406`) nennt **kein** Sprungziel: er
   bindet die Basis an *„den letzten [Stand], für den §Baseline von `harness/conventions.md` einen
   Slice mit Delta-Nachweis ausweist"*. Sprung-gebunden sind nur zwei Begleitsätze, und beide
   überleben den Zielwechsel inhaltlich: *„Ein Durchgang gegen `v6.7.1` ab `v6.0.0` sieht 42 statt
   33 Dateien"* gilt für `v6.7.2` unverändert (nachgefahren: `42 Dateien +907 −404` gegen
   `33 Dateien +295 −263`, Dateimenge identisch — `diff <(… --name-only v6.0.0 v6.7.1) <(… v6.0.0
   v6.7.2)` ist leer); *„dieser Sprung schließt den `v6.5.0`-Nachweis ein"* wird von ADR-0044
   Festlegung 2 für den neuen Sprung wiederholt. Die Ablösung allein von Festlegung 1 ist auf
   dieser Achse richtig geschnitten.
2. **Der Schnitt, Hälfte „Festlegung 1 hat ihr Objekt verloren": geprüft, ohne Befund.** Keine der
   fünf Pin-Stellen trug je `v6.7.1` — `BASELINE_TAG`/`BASELINE_ZIP_SHA256` (`Makefile:25,34`), das
   `sources`-Paar (`.d-check.yml:383-384`) und `DefaultTag`/`DefaultBaselineSHA256`
   (`internal/fetch/baseline.go:48,54`) stehen alle auf `v6.5.0` bzw. dessen sha256
   `80684c17…`. Ausserhalb der zwei ADRs nennt nur die Titel-Zelle des Index `v6.7.1`
   (`git grep -n 'v6\.7\.1' -- ':!docs/reviews' ':!docs/plan/planning/done' ':!docs/plan/adr/0043*'
   ':!docs/plan/adr/0044*'` → 1 Treffer).
3. **Buchung in §Baseline: geprüft, ohne Befund über LOW-4 hinaus.** Ziel-Tag `v6.7.2`, Datum
   2026-09-12, *„sein Vollzug steht aus"* — deckungsgleich mit der ADR. `Stand:` trägt unverändert
   `v6.5.0`, also den **adoptierten**, nicht den gesetzten Stand; die Zeile `auf v6.5.0: …
   Delta-Nachweis steht aus` bleibt korrekt offen. Der Zeiger nennt ADR-0044 Festlegung 1 mit dem
   Statuswert `Proposed` und ADR-0043 Festlegung 2 mit `Accepted` — beide Statuswerte stimmen mit
   den Dateien überein (der vorige Stand der Zeile führte ADR-0043 noch als `Proposed`; das ist mit
   diesem Commit mitkorrigiert). Der **Wert** `v6.0.0` steht nicht in der Buchung.
4. **Zahlen und Kommandos: geprüft, ohne Befund über INFO-1 hinaus.** Jede Messung der ADR ist in
   diesem Lauf unabhängig nachgefahren und reproduziert: Achse `28`/`58`/`0`; Sprung-Umfang
   `33 Dateien +295 −263`; `v6.7.1→v6.7.2` `3 Dateien +7 −4`; Basis-Diffs `32 Dateien +624 −153`
   und `42 Dateien +907 −404`; `54` Dateien im Zielbaum; Stufe (a) `1`/`1`; Stufe (b) `123`/`123`
   Zeilen, `diff` leer, `7` Eigenschaften, `9` Verweise in `4` Dateien; Delegat-Netto
   `43`/`2`/`0`/`0` bei `quelle=0`; Sektions-Netto `25`/`4`/`2`/`12`/`0`/`0` (Summe 43, deckt den
   Datei-Netto); `v6.7.1→v6.7.2` je Sektion `4`; `cmp` der Sensor-Vorlage still, EXIT 0; Bestand
   `15`/`15`/`15`; Gate-Index `48 = 11 + 37` und `28 + 37`, Deckungsrest je `0`; Kennungs-Notation
   `46`/`22`; die dreizehn Suchbegriffe `0`; `ls -1 .harness/baseline/` → `v6.5.0`. Alle als *keine
   Erwartungswerte* gekennzeichneten sind es auch.
5. **Zitat-Treue ([ADR-0016](../plan/adr/0016-verweis-traegt-tag-und-zitat.md),
   [`MR-033`](../../harness/conventions.md#mr-033--eine-aussage-über-die-baseline-nennt-den-tag-gegen-den-sie-gemessen-ist)):
   geprüft, ohne Befund.** Alle fünf tragenden Baseline-Zitate sind im Zielbaum verbatim gefunden,
   jeweils mit Tag, Datei und Sektion: *„Der Gate-Index steht einmal, und zwar hier"*
   (`grundlagen-harness-dateien.md:196`, innerhalb §harness/README.md als Einstiegspunkt, Zeilen
   168–350), *„Der Gate-Index steht **einmal**, in `harness/README.md` §Sensors"*
   (`AGENTS.template.md:193`), *„die **Autoritäts-Doku**, und es gibt genau eine"*
   (`modul-13-quality-gates.md:97`, innerhalb §Hard Rule (Doku-Disziplin)),
   *„`harness/sensors/<target>.md` (Ziel-Form …)"* (`:251`) und *„Ein Gate je Datei …"* (`:249`,
   byte-gleich in `v6.5.0`). Der Anker-Nachzug in §Konventionsspeicher ist die belegte Ursache der
   vier Netto-Zeilen (`#…-nummer-kommt` → `#…-kennung-kommt`).
6. **[`MR-054`](../../harness/conventions.md#mr-054--ein-modul-geht-ins-emittierte-doc-gate-nur-mit-erprobung-grünem-start-und-rotem-gegenbeispiel):
   geprüft, ohne Befund.** `modules: [links, anchors]` in beiden Vorlagen identisch; der Zuwachs in
   `lab/templates/.d-check.yml` ist ausschließlich Kommentar (`diff` der zwei Vorlagen liefert nur
   `#`-Zeilen). Die Aussage *„`MR-054` ist dadurch nicht ausgelöst"* trägt.
7. **[`LH-QA-01`](../../spec/lastenheft.md#lh-qa-01--keine-halluzinierten-gates-f4-f5-f6):
   geprüft, ohne Befund.** §Fitness Function behauptet kein Gate, führt sechs Kandidaten mit Grund
   ihres Nicht-Greifens und markiert den teilweise mechanisierbaren Sensor ausdrücklich als **nicht
   gebaut**. Die Kandidaten-Aussagen sind an der Konfiguration gedeckt (`matrix` kennt keine
   Teil-Ablösung; `comment-claims` hat keine Markdown-Datei im Prüfbereich).
8. **Kein fabrizierter sha256: geprüft, ohne Befund.** Die ADR nennt keinen Hash
   (`grep -nE '[0-9a-f]{40,}'` liefert ausschließlich `SHA256SUMS` als Dateinamen in `diff -x`) und
   sagt ausdrücklich *„Sie nennt keinen sha256. Der des `v6.7.2`-Assets wird beim Vollzug am Asset
   gemessen"*. Die Zusage ist eingehalten.
9. **[`AGENTS.md`](../../AGENTS.md) §3.8 (Commit-Zuschnitt): geprüft, ohne Befund.** `cb702147`
   berührt drei Dateien — die ADR, den ADR-Index und `harness/conventions.md` —, alle drei
   Architect-Eigentum ([ADR-0024](../plan/adr/0024-derivatives-register-gehoert-der-rolle-seines-originals.md)
   für den Index), und die Message nennt die Rolle.
10. **[`AGENTS.md`](../../AGENTS.md) §3.11 / §3.7: geprüft, ohne Befund.** Die zwei Slices stehen
    als Kennung ohne Pfad-Adresse (`slice-222` existiert in `open/`; `ids.patterns` führt kein
    `slice-`-Muster, die blanke Kennung ist also zulässig); genannt sind nur ortsfeste
    Verzeichnisse. Keine Zustandszelle trägt Chronik.
11. **[ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md)
    Festlegung 2/3: geprüft, ohne Befund.** Der Acceptance-Trigger ist formuliert, solange die Datei
    `Proposed` ist; dieser Kontext hat weder die ADR geschrieben noch an den ADR-0043-Runden
    teilgenommen und ist damit als Beleg zulässig.

## Was dieser Lauf **nicht** geprüft hat

- **Keinen Gate-Lauf** — `make gates`, `make docs-check`, `make adr-immutable` sind nicht gefahren
  (Anweisung des Auftraggebers). Über Gate-Farbe, Link-Auflösung und Anker dieses Reports steht
  hier nichts.
- **Nichts über Netz.** Der sha256 des `v6.7.2`-Assets ist nicht gemessen (die ADR nennt auch
  keinen); dass `5d3dba9c…` zum `v6.7.1`-Asset gehört, ist aus ADR-0043 und dem Report der ersten
  ADR-0043-Runde übernommen, nicht neu gemessen.
- **Den Arbeitsbereich von slice-221 nicht angefasst** (`internal/archive/`,
  `harness/tools/slice-mv.sh`, `test/mutations/`, `harness/sensors/archive-welle.md`).
- **Den Inhalt von `slice-222` nicht auf Deckung mit dieser ADR geprüft** — Planner-Eigentum; nur
  Existenz und Lage in `open/` festgestellt.
- **Die übrigen 40 ADRs** nur so weit gelesen, wie der Acceptance-Trigger und die Findings es
  verlangen; kein vollständiger Konsistenz-Durchgang über den ADR-Bestand.
- **Die Vollständigkeit des Delegat-Arguments** nicht bestritten und nicht nachgewiesen: dass
  ausser den vier verlinkten Zieldateien keine weitere den Freshness-Audit trägt, folgt aus dem
  `grep` über die Verweise des Abschnitts — eine Delegation über Prosa statt Link fände er nicht.

## Kategorie-Summary

**1 HIGH · 1 MEDIUM · 4 LOW · 3 INFO.**
Wiederkehrende Klasse gegenüber den zwei ADR-0043-Runden: *abgeleitete Kopie einer Norm im lebenden
Register* (dort LOW-3, hier LOW-4) — zweites Auftreten, noch kein Steering-Loop-Signal.
Neue Klasse dieses Laufs: **Teil-Ablösung schneidet die Festlegung, nicht ihre Folgepflicht**
(HIGH-1).

## Verdikt

**Blockierender Befund: ja — HIGH-1, gestützt von MEDIUM-1.** Dieser Report ist **nicht** der
Beleg, den der Acceptance-Trigger von ADR-0044 verlangt; die Datei bleibt `Proposed`.

Der **Schnitt der Teil-Ablösung ist in seiner Hauptaussage richtig** und in beide Richtungen
belegt: Festlegung 1 hat ihr Objekt verloren (kein Pin trug je `v6.7.1`), und Festlegung 2 ist als
Leseregel geschrieben, die ihr Sprungziel nicht braucht — nachgewiesen am Wortlaut, nicht an der
Zusammenfassung, und ihre zwei sprung-gebundenen Begleitsätze überstehen den Zielwechsel auch
zahlenmäßig. **Falsch gezogen ist die Grenze nur an einer Stelle:** Sie verläuft entlang der
Abschnitts-Überschrift §Entscheidung Festlegung 1 statt entlang des Gegenstands. Der operative
Auftrag, der Festlegung 1 vollzieht, steht in §Konsequenzen, ist unerfüllt, nennt `v6.7.1` und
einen konkreten Hash — und wird vom Satz *„Nicht abgelöst ist alles Übrige jener Datei"*
ausdrücklich in Kraft gelassen. Genau dieser Punkt ist zugleich der einzige, der nach `Accepted` in
keiner der beiden Dateien mehr zu heilen ist.

MEDIUM-1 blockiert aus demselben Zeitgrund: Eine Aussage über die Durchsetzungsschicht, die deren
Konfiguration widerspricht, ist heute eine Textkorrektur und nach `Accepted` ein zitierbarer
Irrtum. Die vier LOW und drei INFO blockieren nicht; LOW-2 und LOW-3 sind ohnehin nicht in der ADR
zu beheben.
