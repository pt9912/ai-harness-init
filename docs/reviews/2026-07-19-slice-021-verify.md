# Verifikation — slice-021 (d-check-Pin v0.50.0 → v0.51.1, `sources` verfügbar)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths` / ids-`exempt-paths`) —
> bare IDs/Pfade hier ok. Bewusst ohne Markdown-Links, damit `links`/`anchors` nichts findet.

## Kopf-Metadaten

- **Rolle:** unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Gegenstand:** DoD-Behauptung von slice-021 (7 Punkte) gegen realen Zustand + Plan-vs-Code + ADR/Hard Rules.
- **Slice:** `docs/plan/planning/in-progress/slice-021-dcheck-pin-v0511.md` (noch in-progress/, DoD-Boxen offen — Verify vor Closure).
- **Impl-Commit:** `c0e4242` (ein Content-Commit); vorangehend `786b899` (reiner Move open→in-progress). HEAD=`c0e4242`, tracked Tree clean.
- **Berührte Dateien (c0e4242):** `d-check.mk`, `internal/emit/emit.go`, `harness/conventions.md`. `.d-check.yml` / `AGENTS.md` / `harness/README.md` bewusst NICHT berührt.
- **Selbst ausgeführt (belegt, nicht nur nachgelesen):**
  - `docker buildx imagetools inspect ghcr.io/pt9912/d-check:v0.51.1` → Registry-Manifest-Digest.
  - `docker images --digests` → lokaler RepoDigest v0.51.1 und v0.50.0.
  - `docker run --network none …@sha256:fede…/…@sha256:9b80… --print-mk` → Roh-Regeneration + Versions-Diff.
  - Rekonstruktion print-mk(v0.51.1)+4 Handgriffe vs. committetes `d-check.mk` (Body-Diff).
  - `make docs-check` (v0.51.1) UND `make docs-check DCHECK_DIGEST=sha256:9b80…` (v0.50.0-Override) — netzlos.
  - `make gates` SELBST einmal (Cache warm) → Exit 0.

## DoD-Verdikt je Zeile

### DoD-1 — Pin v0.51.1 + Digest dreifach belegt — **CONFIRMED**

`d-check.mk` (`DCHECK_IMAGE ?= …:v0.51.1`, `DCHECK_DIGEST ?= sha256:fede3d027b2ebc1dd8534460853e57b67cc7a9a182cad2e2138c8eebf7a2d03c`)
und `internal/emit/emit.go` (`DefaultImage`/`DefaultDigest`, Zeilen 33-34) pinnen exakt diesen Wert.
Beleg 1 — Registry: `imagetools inspect …:v0.51.1` → `Digest: sha256:fede3d02…f7a2d03c` (live gegen ghcr abgefragt).
Beleg 2 — lokaler RepoDigest: `docker images --digests` → v0.51.1 = `sha256:fede3d02…f7a2d03c`, byte-identisch.
Beleg 3 — d-check-`version.md`/Handbuch: liegt außerhalb dieses Repos (Schwester-Repo), hier NICHT unabhängig
re-verifiziert; die zwei docker-autoritativen Quellen (Registry-Manifest + lokaler Pull-Beleg) sind voneinander
unabhängig und stimmen überein → die v0.51.1-Zuordnung des Pins ist belegt (LH-QA-02). Keine Fabrikation.

### DoD-2 — `d-check.mk` faithful, einzige neue Differenz `--disable sources` — **CONFIRMED**

Roh-`--print-mk`-Diff v0.50.0 → v0.51.1 (beide `@digest`, netzlos): NUR der Tag (`DCHECK_IMAGE`) und **genau fünf**
fokussierte advisory-Recipes (`doc-immutable`/`doc-commits`/`doc-planning`/`doc-tracked`/`doc-targets`) gewinnen je ein
`--disable sources` — verbatim vom Tool, unmittelbar hinter `--disable citations`. Sonst keine inhaltliche Differenz.
Committetes `d-check.mk` = Body von print-mk(v0.51.1) + die vier dokumentierten MR-010-Handgriffe: Rekonstruktion
(Digest-Pin + `doc-check`→`docs-check` + `doc-help`-Grep `^docs?-` + adaptierter Kopfkommentar) diff't gegen den
Repo-Body auf **nur** zwei Rest-Zeilen — beide reine `##`-Hilfetexte (`docs-check`-Beschreibung, `doc-help`-Liste),
die MR-010 Setzung 1 ausdrücklich als Adaption „Target UND Hilfetext" nennt. Die docker-run-Recipe-Kommandos sind
byte-identisch (inkl. der 5 `--disable sources`). Die zwei Hilfetexte wurden von `c0e4242` NICHT angefasst (git-Diff) —
also keine NEUE inhaltliche Differenz über `--disable sources` hinaus. Faithful.

### DoD-3 — Emitter-Pin gekoppelt, `TestDefault…_MatchesCanonical` grün — **CONFIRMED**

`emit.DefaultImage`/`DefaultDigest` (`emit.go:33-34`) byte-gleich zu `d-check.mk` `DCHECK_IMAGE`/`DCHECK_DIGEST`.
`TestDefaultImage_MatchesCanonical` und `TestDefaultDigest_MatchesCanonical` (`emit_test.go:30-47`) lesen die
kanonische Quelle `../../d-check.mk` (Helper `mkVar`) und erzwingen Gleichheit + `sha256:`-Präfix. Im selbst gefahrenen
`make gates`-Lauf: `ok  github.com/pt9912/ai-harness-init/internal/emit  0.020s` (Dockerfile-`test`-Stage, `go test ./...`).

### DoD-4 — conventions §Baseline v0.51.1 + neuer MR-012, Anker löst auf, historische Bodies eingefroren — **CONFIRMED**

`conventions.md` §Baseline: `v0.50.0`→`v0.51.1` + `MR-012`-Link ergänzt. Neuer `### MR-012 — d-check-Pin v0.51.1
(sources verfügbar)`-Eintrag rein additiv nach dem unveränderten MR-011-Body. Der `c0e4242`-Diff zeigt EXAKT zwei Hunks
(§Baseline-Zeile + MR-012-Anhang); MR-009/010/011-Bodies unberührt (Kontext-Zeilen bestätigen — Historie eingefroren).
Anker: berechneter GitHub-Slug der Überschrift = `mr-012--d-check-pin-v0511-sources-verfügbar` = der in §Baseline und
im Slice referenzierte Anker (identisch). Der grüne `docs-check`-Lauf mit aktivem `anchors`+`ids`-Modul bestätigt
Auflösbarkeit sämtlicher IDs/Anker.

### DoD-5 — Pflicht-Trockenlauf inert, 0-Befund-Differenz — **CONFIRMED**

Selbst gefahren, netzlos (`--network none`), gleicher Arbeitsbaum:
- `make docs-check` (v0.51.1, Default-Pin) → `d-check: 70 Datei(en) geprüft, 0 Befund(e)`, Exit 0.
- `make docs-check DCHECK_DIGEST=sha256:9b80ce1d74e745cc266b476701bccb76059783916fe18651e00df96480a94364` (v0.50.0-Override)
  → `d-check: 70 Datei(en) geprüft, 0 Befund(e)`, Exit 0.

Identische Befundzahl (0) unter beiden Pins — inert **gemessen**, nicht angenommen. Einzige Ausgabe-Differenz: der
echo'te Image-Digest-Ref. (70 statt der im Commit belegten 69 Dateien = der untracked Reviewer-Report zählt mit; die
Aussage ist die Gleichheit BEIDER Läufe auf demselben Baum, nicht die absolute Zahl. `sources` ist Netz-Modul + nicht
in `modules:` → unter `--network none` sowieso inert.)

### DoD-6 — `sources` NICHT aktiviert (LH-QA-01, kein Phantom-Gate) — **CONFIRMED**

`.d-check.yml` `modules: [links, anchors, ids, matrix, codepaths, spans]` — **kein** `sources` (und kein `citations`).
`.d-check.yml` im `c0e4242`-Diff unberührt (Plan §3 „Nicht hier"). Kein neuer Gate-Name in `AGENTS.md` §4 oder
`harness/README.md` (beide vom Commit nicht berührt). Der Bump macht das 19. Modul nur *verfügbar*; die Aktivierung mit
realem `source-pin` bleibt slice-020. Kein leer aktiviertes Netz-Modul → kein Phantom-Gate.

### DoD-7 — `make gates` grün (+ Closure-Notiz) — **CONFIRMED (gates); Closure-Notiz nachgelagert**

`make gates` SELBST einmal gefahren (Cache warm, nichts parallel) → **GATES_EXIT=0**. Alle Gates real gelaufen:
`baseline-verify` (v3.5.0 OK — 42 Dateien, netzlos), `docs-check` (70 Dateien, 0 Befunde), `lint` (golangci-lint
v2.12.2, Dockerfile-lint-Stage), `build` (cross-compile), `test` (bats `ok 4..52` + `ok internal/emit`), `shell-lint`
(shellcheck), `record-gates`. `record-gates` mutiert keine getrackte Datei (git status danach unverändert).
Die **Closure-Notiz** (§7 des Slice) ist noch das leere Template — das ist ein POST-Closure-Artefakt (Verify läuft VOR
der Closure; der Slice liegt noch in `in-progress/`, DoD-Boxen offen). Kein Defekt zum Verify-Zeitpunkt; sie wird beim
`done/`-Move ergänzt.

## Hard Rules / ADR (task-Punkt 8)

- **ADR-0003 (Docker-only, kein Host-Toolchain im Diff):** CONFIRMED. Die drei berührten Dateien sind Make-Recipes (alle
  `docker run`/`docker build`), Go-Konstanten und Doku — kein `pip`/`npm`/`cargo`/`go`/`brew`/`apt` auf dem Host, keine
  neue Host-Abhängigkeit. `--disable sources` ist ein d-check-Modul-Flag (Tool), keine Host-Toolchain.
- **Hard Rule 3.3 (Move eigener Commit):** CONFIRMED. `786b899` = 100%-reiner Rename open→in-progress (0 Inhaltszeilen);
  der Content-Rewrite liegt getrennt in `c0e4242`. Der eventuelle in-progress→done-Move ist ein künftiger eigener
  Closure-Commit.
- **Hard Rule 3.1 (keine halluzinierten Gates):** CONFIRMED. `sources` nicht aktiv, kein neuer Gate-Name; `docs-check`
  bleibt das einzige behauptete Doc-Target und läuft grün.
- **Hard Rule 3.5 (Gate-Lockerung nur per ADR):** CONFIRMED. Keine Schwellen-Senkung / Modul-Aktivierung / Strenge-Senkung
  — ein Modul wird nur *verfügbar* gemacht, nichts aktiviert; MR-012 (Steering-Loop, gate-anheben-analog) genügt, kein ADR
  nötig. Konsistent mit MR-009/010/011.
- **Hard Rule 3.2 (Lint-Suppression):** kein `//nolint`, kein `# shellcheck disable` im Diff.
- **Hard Rule 3.4 (ADR immutable):** keine ADR-Datei im Diff-Range.

## Gesamt-Verdikt

**Alle 7 DoD-Zeilen CONFIRMED — 0 VIOLATED.** Einzige Nuance: DoD-7s Teil-Item „Closure-Notiz" ist zum Verify-Zeitpunkt
bewusst noch leer (Post-Closure-Schritt; Slice steht in `in-progress/`, Boxen offen) — kein Defekt, sondern der korrekte
Lifecycle-Zustand vor der Closure. Der `make gates`-Teil ist selbst als Exit 0 belegt. Digest v0.51.1 unabhängig
zweifach (Registry-Manifest + lokaler RepoDigest) bestätigt, `d-check.mk` faithful (nur `--disable sources` neu, verbatim,
in genau 5 advisory-Recipes), Emitter-Pin gekoppelt und emittierte Config bewusst minimal, `sources` verfügbar aber nicht
aktiviert (kein Phantom-Gate, kein neuer Gate-Name), Inertheit am realen Korpus gemessen (70/0 unter beiden Pins), Move
sauber vom Inhalt getrennt, historische MR-Bodies eingefroren, MR-012-Anker aufgelöst, ADR-0003 / Hard Rules §3.1-3.5
konform.

**Empfehlung:** Verifikation bestanden — Closure freigegeben (Closure-Notiz + DoD-Abhakung + `done/`-Move durchführen,
entsperrt slice-020).
