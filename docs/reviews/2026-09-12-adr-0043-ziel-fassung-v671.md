# Review — ADR-0043: Ziel-Fassung regiert den Sprung `v6.5.0` → `v6.7.1`

**Rolle:** Reviewer (frischer Kontext) · **Datum:** 2026-09-12 · **Gegenstand:** Commit `8fc0ec41` — ADR-0043 (`Proposed`), ADR-Index, `harness/conventions.md` §Baseline
**Prüfgrundlage:** [ADR-0018](../plan/adr/0018-ziel-fassung-regiert-die-migration.md), [ADR-0031](../plan/adr/0031-regierende-fassung-und-ort-der-zielstand-setzung.md), [ADR-0036](../plan/adr/0036-ziel-fassung-regiert-den-sprung-v600.md), [ADR-0038](../plan/adr/0038-ziel-fassung-regiert-den-sprung-v650.md), [ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md) (alle `Accepted`) · [`AGENTS.md`](../../AGENTS.md) §3.4/§3.5/§3.7/§3.11 · [`MR-025`](../../harness/conventions.md#mr-025--eine-zahl-im-text-steht-neben-dem-kommando-das-sie-liefert), [`MR-033`](../../harness/conventions.md#mr-033--eine-aussage-über-die-baseline-nennt-den-tag-gegen-den-sie-gemessen-ist), [`MR-054`](../../harness/conventions.md#mr-054--ein-modul-geht-ins-emittierte-doc-gate-nur-mit-erprobung-grünem-start-und-rotem-gegenbeispiel)
**Messbasis:** jedes Kommando der ADR unabhängig nachgefahren am Kurs-Klon `/Development/KI/ai-harness-course` · Sensor `make docs-check` → `1185 Datei(en) geprüft, 0 Befund(e)`

## Findings

### MEDIUM-1 — `MR-025` Setzung 1 · `docs/plan/adr/0043-ziel-fassung-regiert-den-sprung-v671.md:127`
*„die Kostenreihe der **fünf** bisherigen Re-Baselines lautet 2, 1, 1, 1, 13 Slices"* — §Baseline führt **acht**; die Reihe und die *„13 Slices"* in Option F (`:484`) tragen kein Kommando und keinen Hinweis auf Handzählung. Mit `Accepted` friert die veraltete Bezugsmenge ein und wird zitierbar (§3.4).
Beleg: ``grep -oE 'auf `v[0-9.]+`:\*\*' harness/conventions.md | wc -l`` → **8**. · verifizierbar: nein (kein Gate liest Prosa-Zahlen) · klasse: Zahl ohne Kommando, Bezugsmenge veraltet

### LOW-1 — `MR-025` Setzung 1 · `…-v671.md:344,416,484`
*„Der Anteil ist mit 32 Dateien und +624/−153 größer als der Sprung"* — auf der Zeilen-Achse wahr (777 gegen 549 bewegte Zeilen), auf der **Datei**-Achse falsch (32 < 33); beide Achsen stehen im selben Satz. Der tragende Satz von Festlegung 2 (*der Anteil fällt still heraus*) hängt nicht daran.
Beleg: die zwei `git -C "$K" diff --numstat`-Läufe der ADR → `32 Dateien +624 -153` bzw. `33 Dateien +289 -260`. · verifizierbar: nein · klasse: Komparativ gegen eine Achse, die ihn widerlegt

### LOW-2 — `MR-025` Setzung 1 · `…-v671.md:229`
*„siebzehn neue Zeilen, die den Gate-Index an **einen** Ort binden"*: 17 ist die Länge des Hunks; davon tragen 6 Zeilen die Ein-Index-Setzung, 11 eine zweite Regel (*„Die Target-Zelle trägt den nackten Target-Namen"*). Kein Kommando der ADR gibt die 17 aus (der nächststehende liefert `netto=21`).
Beleg: der `sec()`-Sektions-`diff` der ADR, ohne `grep -c` gefahren → Hunk `28a29,45`, zweiter Hunk `142,143c159,160`. · verifizierbar: nein · klasse: Zahl ohne Kommando, Zuschreibung weiter als der Beleg

### LOW-3 — `ADR-0031` Festlegung 2 / §3.7 · `harness/conventions.md:51-53`
Die Buchung wiederholt den **Inhalt** von Festlegung 2 (*„setzt die Delta-Basis … auf `v6.0.0` … und schließt den ausstehenden `v6.5.0`-Nachweis damit ein"*), statt nur auf sie zu zeigen — eine zweite Fassung einer normativen Aussage in einem lebenden Register. Dazu bleibt die Zelle *„auf `v6.5.0`: … Delta-Nachweis steht aus"* nach dem eingeschlossenen Durchgang ohne vorgesehenen Nachzug stehen und trägt dann einen falschen Zustand.
Beleg: `grep -n 'Delta-Nachweis' harness/conventions.md` gegen ADR-0031 §Entscheidung Festlegung 2 (*„Sonst nichts"*). · verifizierbar: nein · klasse: abgeleitete Kopie einer Norm im lebenden Register

## Negativbefunde

- **Prüfpunkt 1 (Delta-Basis, Festlegung 2):** geprüft, ohne Befund über die Relation hinaus (LOW-1). Alle vier Beträge exakt reproduziert — `32 Dateien +624 -153`, `33 Dateien +289 -260`, `42 Dateien +904 -404`, `54` Dateien im Baum. Die Leseregel trägt: `v6.0.0` ist der letzte Tag mit gefülltem Nachweis-Feld (slice-176), `v6.5.0` führt *steht aus*.
- **Prüfpunkt 2 (Preis, 42 Dateien):** geprüft, ohne Befund. Die ADR führt ihn als Negativ-Konsequenz, benennt die Schranke *„in einer Review-Sitzung prüfbar"* des Baseline-Regelwerks `modul-05-planning-harness.md` und lässt den Schnitt in zwei Slices offen — das ist die Planner-Frage, keine ADR-Frage; Festlegung 2 verschiebt sie nicht, sondern macht sie sichtbar.
- **Prüfpunkt 3 (Achse gegen ADR-0038):** geprüft, ohne Befund. `28` Dateien / `58` Zeilen / `0` außerhalb der URL-Klasse exakt nachgefahren; ADR-0038 §Die Achse (*„byte-gleich"*, belegt nur für `v6.0.0`) ist korrekt als **benannt** geführt, nicht nachgetragen (§3.4). Die als offen benannte Grenze *„die `v6.7.1`-Seite ist an nichts als den Klon gebunden"* ist zusätzlich geschlossen: das Release-Asset `v6.7.1` gegen `git archive v6.7.1` ergibt dieselben `28`/`58`/`0`.
- **Prüfpunkt 4 (sha256):** geprüft, ohne Befund — zweifach bestätigt: Eintrag in `releases/download/v6.7.1/SHA256SUMS` **und** `sha256sum` des geladenen `lab-regelwerk.zip` (227144 Bytes) → beide `5d3dba9c8dc2df25d5b7dc0bd9b6f5aa123b0ef876dcd9330fe1254898ec72ac`.
- **Prüfpunkt 5 (Buchung):** geprüft, Befund als LOW-3. Ort, Ziel-Tag und Datum entsprechen ADR-0031 Festlegung 2; der fehlende zweite Teil ist korrekt begründet (der Slice existiert nicht); **keine Chronik** in der Zustandszelle, kein Konformitäts-Urteil, keine Ausgangs-Liste.
- **Prüfpunkt 6 (§3.4-Tauglichkeit):** geprüft, ohne Befund über MEDIUM-1/LOW-1/LOW-2 hinaus. Überschriften-Folge identisch mit `templates/docs/plan/adr/NNNN-titel.template.md` inkl. `## Geschichte` (eine Zeile, Zustand statt Verlauf); keine wandernde Pfad-Adresse — Slices stehen als Kennung, genannt sind nur ortsfeste Verzeichnisse (§3.11, [ADR-0030](../plan/adr/0030-eingefrorene-adresse-auf-den-planning-lifecycle.md) Festlegung 3); jedes Baseline-Zitat nennt Tag, Datei und Sektion (`MR-033`, ADR-0016).
- **Stufen (a)/(b) und Delegate:** geprüft, ohne Befund. `grep -c` → `1` in beiden Tags; Abschnitt `123`/`123` Zeilen, `diff` leer, `7` Eigenschaften; Delegat-Netto `39`/`2`/`0`/`0`; Sektions-Netto `21`/`4`/`2`/`12`/`0`/`0`; die drei tragenden Zitate (`grundlagen-harness-dateien.md` §Einstiegspunkt, `AGENTS.template.md`, `modul-13-quality-gates.md`) verbatim in `v6.7.1` gefunden, und die Gate-Tabelle der AGENTS-Vorlage fällt von `7` auf `0` Zeilen.
- **`targets.authority`-Messung:** geprüft, ohne Befund. `48` Rezepte `= 11 + 37` und `28 + 37`, Deckungs-Rest über die **Namen** je `0` — in beiden Richtungen, auch phantomfrei. Die ADR folgert daraus korrekt nur die Vollziehbarkeit und überlässt die §3.5-Frage dem Durchgang; sie lockert hier keine Schwelle.
- **`MR-054`:** geprüft, ohne Befund. `modules: [links, anchors]` in `v6.5.0` und `v6.7.1` identisch, der Zuwachs in `lab/templates/.d-check.yml` ist ausschließlich Kommentar (`diff` der zwei Vorlagen: nur `#`-Zeilen) — die Modul-Zusammensetzung, der Geltungsbereich von `MR-054`, bleibt unberührt.
- **Konsistenz mit den Vorgängern:** geprüft, ohne Befund. Kein `Supersedes` nötig (ADR-0018 Festlegung 3 wird angewandt, nicht ersetzt); alle fünf Re-Evaluierungs-Trigger von ADR-0038 sind adressiert, zwei davon mit gemessenem Negativ (`0` Treffer der dreizehn Suchbegriffe; Zwei-Fassungen-Phase lebt, `ls -1 .harness/baseline/` → `v6.5.0`).

## Kategorie-Summary

1 MEDIUM · 3 LOW · 0 HIGH · 0 INFO — wiederkehrende Klasse: **Messwert ohne Kommando bzw. mit weiterer Zuschreibung als sein Beleg** (MEDIUM-1, LOW-1, LOW-2; dritte Wiederholung in einem Lauf ⇒ Steering-Loop-Signal nach `MR-025`).

## Verdikt

**Beide Festlegungen tragen; blockierend ist allein MEDIUM-1.** Festlegung 1 und 2 sind in jeder tragenden Messung unabhängig reproduziert — die korrigierte Achse, beide Stufen, das Netto je Sektion, die Delta-Beträge, der sha256 und die Deckungs-Messung stimmen exakt; kein HIGH, kein Verstoß gegen §3.4, §3.5, §3.11 oder gegen eine der vier `Accepted`-Vorgänger-ADRs.
MEDIUM-1 blockiert nicht die Entscheidung, sondern den **Accept-Übergang**: eine falsche Bezugsmenge in einem Satz, der ab `Accepted` unveränderlich ist und heute noch kostenlos korrigiert werden kann. Nach Korrektur (und ohne dass dieser Kontext sie nachmisst — [ADR-0040](../plan/adr/0040-accept-uebergang-nennt-den-beleg-seines-triggers.md) Festlegung 2) ist **dieser Report der Beleg**, den der Acceptance-Trigger von ADR-0043 verlangt; LOW-1 bis LOW-3 sind vor dem Einfrieren zu klären, blockieren aber für sich genommen nicht.
