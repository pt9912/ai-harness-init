# Review — slice-033 (Workflow-Commands emittieren: `.claude/commands/`)

**Rolle:** Unabhängiger Reviewer (Modul 10, frischer Kontext — kein Selbst-Review).
**Datum:** 2026-07-22.
**Prüfgegenstand:** Implementierungs-Commit `00160c6` (10 Dateien, +552) + Folge-Fix `35d6654`
(Mutation-37 shell-lint/SC2016-Backticks, 1 Zeile). Die Plan-Cut- und Lifecycle-Move-Commits sind
Planner-/Lifecycle-Artefakte (nicht Teil des Code-Reviews).
**Reviewer-Skill:** `.harness/skills/reviewer.md` v1.2.0.

**Pflicht-Kontext gelesen:** Diff · `spec/lastenheft.md`-Bezüge (LH-FA-08, LH-FA-02, LH-QA-01) ·
`docs/plan/adr/0006-durchsetzung-commands-tool-als-quelle.md` (aktiv; Commands = Tool-als-Quelle,
Abgrenzung LH-FA-08 Anleitung ≠ LH-FA-06 Durchsetzung) · `AGENTS.md` §3 (3.1/3.5/3.6) ·
`harness/conventions.md`-Bezug (MR-Block, adaptierbare Marker) · vorherige Findings
(`docs/reviews/2026-07-22-slice-032-review.md` als Muster, insb. N-1 Verbatim, N-4 Substitution/Sensor,
N-6 Sensor-Integrität, N-11 Vorpass/Pre-Flight-Kopplung; L-2 Genericisierungs-Rest) · Slice-Plan
`docs/plan/planning/in-progress/slice-033-workflow-commands-emit.md`.

**Verglichene Dogfood-Originale (Zeile für Zeile, `diff`):** `.claude/commands/{implement-slice,
plan-welle,close-welle}.md` gegen `internal/emit/templates/commands/*.md`. Kopplung:
`internal/emit/commands.go` (`Commands`/`CommandPaths`/`commandFiles`), `cmd/ai-harness-init/main.go:198/246`
(Phase-4-Emit + Phase-3-Pre-Flight), `internal/emit/commands_test.go`, `harness/tools/{smoke,full-smoke}.sh`,
`test/mutations/{36,37}-*.sh`.

**Gate-Läufe durch den Reviewer:** keine teuren Läufe (rollenkonform — `make gates`/`smoke`/`full-smoke`/
`mutate` sind Sache der Verifikation); Analyse via `diff` Dogfood↔Emit, Grep über die Templates + Sensor-Inspektion.

---

## Findings

Keine HIGH- oder MEDIUM-Findings.

### L-1 · LOW — `TestCommands_NoInternalLeak` prüft Slice-Nummern als 4-Literal-Aufzählung, nicht als Klasse
- **kategorie:** LOW
- **quelle:** AGENTS §3.6 (Test misst Eigenschaft, nicht heutigen Ist-Bestand) / LH-QA-01
- **pfad:** `internal/emit/commands_test.go:60`
- **befund:** Der Leak-Wächter verbietet konkrete Dogfood-Slice-Nummern als **feste Liste**
  `"slice-027","slice-030","slice-031","slice-032"`. Der Test trägt im Namen die Eigenschaft „NoInternalLeak",
  deckt bei den Slice-Nummern aber nur genau diese vier ab. Sickerte bei einer künftigen Template-Änderung eine
  **andere** Dogfood-Slice-Nummer ins emittierte Command (z. B. `slice-005`, `slice-024`, `slice-026`), bliebe
  der Test grün — obwohl die im Ziel bedeutungslose Referenz genau der Klasse angehört, die die anderen vier
  benennen. Die substanziellen Leak-Token (`ai-harness-init`, `make mutate`, `make smoke`, `test/mutations`)
  sind als robuste Substring-Prüfung abgedeckt; die Lücke betrifft nur die Slice-Nummern-Achse. Aktuell **kein**
  Leak (Grep `slice-[0-9]{3}` über die Templates trifft nur den generischen Platzhalter `slice-<NN>` in
  `plan-welle.md:58`) — daher nicht-blockierend.
- **verifizierbar:** teilweise (ein numerisches `slice-[0-9]{3}`-Muster im Test würde den Fall decken; heute
  kein Gate-Rot, da kein Leak vorliegt — Sicht-Prüfung + `make test`).

### I-1 · INFO — ADR-0006 formuliert „per `--lang` parametriert" auch für Commands; `emit.Commands` ist sprach-agnostisch (kein lang-Param)
- **kategorie:** INFO
- **quelle:** ADR-0006 §Entscheidung („generische, per `--lang` parametrierte Fassung … auch die Workflow-Commands")
- **pfad:** `internal/emit/commands.go:54` (`Commands(targetDir, force)` — kein `lang`), `cmd/ai-harness-init/main.go:200`
- **befund:** ADR-0006 beschreibt Durchsetzung **und** Commands gemeinsam als „per `--lang` parametrierte Fassung
  (analog Sprachskelett)". `emit.Commands` trägt **keinen** `lang`-Parameter — anders als `emit.Enforce`
  (BLOCKED-Set je `--lang`). Das ist **bewusst** und im Plan §6 („Sprach-Agnostik, Messbefund") begründet: die
  Commands sind der sprach-**unabhängige** harness-Prozess (Lifecycle/Rollen/Review); das einzige sprach-nahe
  Detail — die Guard-Toolchain — ist in den adaptierbaren „ANPASSEN"-Marker verschoben („die Host-Toolchain
  deiner Sprache") statt hart kodiert. Die ADR-Determinismus-Fitness-Function („gleiche `--lang` → byte-identisch")
  ist damit trivial erfüllt (kein sprach-abhängiges Byte in der Ausgabe), und die substanzielle ADR-Entscheidung
  (Tool-als-Quelle statt Picker, adaptierbare Marker, kein Upstream-Warten) ist voll gewahrt. Reine
  Wortlaut-vs-Intent-Notiz — kein Verstoß gegen die tragende Setzung der aktiven ADR.
- **verifizierbar:** nein (Design-Notiz; kein Gate färbt eine wortlaut-treue vs. intent-treue Lesart).

---

## Negativbefunde (geprüft, ohne blockierenden Befund)

### N-1 · Verbatim-Treue des Prozesses — byte-identisch außer den 3 Genericisierungs-Hunks je Command
- **quelle:** LH-QA-01 (kein aus-dem-Nichts; der erprobte Prozess darf nicht driften), Plan §1/§6
- **pfad:** `internal/emit/templates/commands/*.md` gegen `.claude/commands/*.md` (`diff`)
- **befund:** Zeilenweiser `diff` Dogfood↔Emit zeigt je Command **nur** die geplanten Hunks: (a) die
  „Repo-lokale Adaptionen"-Sektionsüberschrift + ein neuer `<!-- ANPASSEN -->`-Kommentarblock; (b) die
  Toolchain-Zeile generisch („die Host-Toolchain deiner Sprache" statt `go`/`pip`/`npm`/`golangci-lint`) +
  Nennung des emittierten Guard-Pfads; (c) „CLAUDE.md lesen" → „CLAUDE.md lesen (falls dein Repo eines führt /
  falls vorhanden)"; (d) in `implement-slice.md` die Sensor-/Mutations-Prosa generisch (Mutations-Sensor/
  Emit-Smoke statt `make mutate`/`make smoke`/`test/mutations`, entfernte Slice-Nr `slice-027`). **Kein**
  Prozess-Schritt entfernt: die nummerierten Top-Level-Schritte sind paritätisch (implement-slice 23/23,
  plan-welle 10/10, close-welle 7/7); Rollen-Regeln (frischer-Kontext-Reviewer `.harness/skills/reviewer.md`,
  „Zusage ohne rot gesehenes Gegenbeispiel", Verifier-Trennung, Modul-Verweise) stehen **wörtlich** in der
  emittierten Fassung. Keine Abschwächung einer Rollen-/Review-/Verifikations-Regel.
- **verifizierbar:** ja (`diff` reproduzierbar; `make full-smoke` legt die Commands real ins Ziel).

### N-2 · Genericisierung vollständig (LH-FA-08 „nicht 1:1 hart") — kein ai-harness-init-interner Leak
- **quelle:** LH-FA-08/LH-FA-02 (adaptierbar), LH-QA-01, Plan §3-DoD
- **pfad:** `internal/emit/templates/commands/*.md` (Grep)
- **befund:** Grep über die drei Templates trifft **keinen** verbotenen Token: kein `ai-harness-init`, kein
  `make mutate`/`make smoke`/`make full-smoke`, kein `test/mutations`, keine konkrete Dogfood-Slice-Nummer
  (nur der generische Platzhalter `slice-<NN>` in `plan-welle.md`), keine hart kodierte Sprach-Toolchain
  (`gofmt`/`golangci-lint`/`staticcheck`/`go build`/`go test`). Der Werkzeug-Name `ai-harness-init` (der L-2-Rest
  aus slice-032 im Guard) ist hier **nicht** wiederholt.
- **verifizierbar:** ja (`make test` via `TestCommands_NoInternalLeak`; `make smoke`/`make full-smoke`-Grep).

### N-3 · ANPASSEN-Marker in ALLEN 3 Commands (nicht nur implement-slice)
- **quelle:** LH-FA-02/LH-FA-08 (adaptierbarer Marker), Plan §2-DoD
- **pfad:** `implement-slice.md`, `plan-welle.md`, `close-welle.md` (je 2× `ANPASSEN`: Überschrift + Kommentar)
- **befund:** Jeder der drei Commands trägt in seiner „Repo-lokale Adaptionen"-Sektion die Überschrift
  „(ANPASSEN an dein Repo)" **und** einen `<!-- ANPASSEN: … ergänze/streiche nach deinem Repo -->`-Block.
  `TestCommands_AdaptationMarker` iteriert über **alle** `CommandPaths()` und verlangt `ANPASSEN` in jedem —
  greift also nicht nur implement-slice. Der Marker ist konkret gerahmt (verweist auf die real emittierte
  Durchsetzungsschicht als Default-Adaption), nicht bloßer Platzhalter — der Plan-Kompromiss (§6) ist umgesetzt.
- **verifizierbar:** ja (`make test`).

### N-4 · Keine Über-Genericisierung — im Ziel EXISTIERENDE Refs konkret gelassen
- **quelle:** Plan §6 („den erprobten Prozess NICHT verwässern; nur tote/falsche Refs adaptierbar")
- **pfad:** Grep `make gates|record-gates|harness/conventions|reviewer\.md|\.harness/skills` über die Templates
- **befund:** Die Refs auf Mechanik, die slice-030/031/032 **emittiert** haben (`make gates`, `record-gates`,
  `harness/conventions.md`/„MR-Block", `.harness/skills/reviewer.md`, Stop-Hook, PreToolUse-Guard,
  Doc-Gate/d-check), stehen **konkret** in den Commands — im gebooteten Ziel korrekt. Nur die ai-harness-init-
  **internen** Refs (nicht emittierte Sensoren, Slice-Nrn, Sprach-Toolchain) sind genericisiert. Die Grenze aus
  Plan §6 ist eingehalten — keine unnötige Verwässerung.
- **verifizierbar:** ja (Grep + `make full-smoke`, das die konkreten Refs im Ziel real trägt).

### N-5 · CLAUDE.md-Referenz sauber gehandhabt — Prosa „falls vorhanden", kein toter Inline-Code-Pfad
- **quelle:** ADR-0006 §Entscheidung 3 (CLAUDE.md autort, nicht emittiert), Plan §6, LH-QA-01
- **pfad:** `implement-slice.md:50`, `plan-welle.md:38`, `close-welle.md:35`
- **befund:** Alle drei Commands sagen ``CLAUDE.md`` (falls vorhanden / falls dein Repo eines führt) lesen —
  als **Anleitung** formuliert, nicht als harter Existenz-Verweis. `CLAUDE.md` ist autort (nicht emittiert), im
  frisch gebooteten Ziel erst da, wenn der Adopter es schreibt; die „falls vorhanden"-Rahmung fängt genau das ab.
  Kein toter Codepath-Verweis, der einen Ziel-d-check auf `codepath-missing` triggern könnte; laut Commit-Message
  fährt der Ziel-d-check 12 Dateien / 0 Befunde (Verifier bestätigt den realen Lauf). `.claude/**` wird vom
  Ziel-d-check ohnehin nicht gescannt (Plan §6).
- **verifizierbar:** ja (`make full-smoke` → Ziel-`make gates` inkl. docs-check grün).

### N-6 · Emit-Mechanik spiegelt `Enforce` — Vorpass, Pre-Flight-Kopplung, Mode 0644 korrekt
- **quelle:** ADR-0006 (Tool-als-Quelle, go:embed), slice-025/028/031-Muster, Plan §6 (Zielmengen-Kopplung)
- **pfad:** `commands.go:25/31/42/54`, `main.go:200/247`, `commands_test.go`
- **befund:** `commandsFS` ist per `//go:embed all:templates/commands` eingebettet (Tool-als-Quelle, nicht
  gefetcht). `Commands` fährt denselben **Kollisions-Vorpass** wie `Enforce` (existiert EINES der 3 Ziele ohne
  `--force`, schreibt KEINES → kein Teil-Emit); `TestCommands_NoOverwriteWithoutForce` belegt zusätzlich, dass
  bei Kollision die anderen Commands **nicht** geschrieben sind und `--force` überschreibt. **Pre-Flight-Kopplung:**
  `CommandPaths()` leitet aus **derselben** `commandFiles()`-Liste ab wie der reale Emit und wird in `main.go:247`
  in den Phase-3-Pre-Flight gehängt — dieselben 3 Pfade, die Phase 4 (`main.go:200` `emit.Commands`) schreibt →
  eine vorhandene `.claude/commands/…` fällt VOR dem Emit auf (kein Falsch-Grün wie slice-028/031 warnten).
  **Mode 0644** korrekt: die Commands sind reine `.md`-Anleitung, nicht exec — anders als die 0755-Hooks in
  `Enforce`; folglich braucht `Commands` **kein** `os.Chmod`-nach-Write (die L2-Chmod-Lehre gilt nur für den
  Executable-Bit-Reset, für 0644-`.md` bedeutungslos). Getrennte Funktion `emit.Commands` ≠ `emit.Enforce`
  (Anleitung ≠ Durchsetzung, ADR-0006). Sauber.
- **verifizierbar:** ja (`make test` — `TestCommands_EmitsAll`/`_NoOverwriteWithoutForce`; `make build`).

### N-7 · Sensor-Integrität 36/37 — echte Mutationen, färben den GENANNTEN Wächter
- **quelle:** AGENTS §3.6 (rot gesehenes Gegenbeispiel je Zusage), slice-026-Klasse
- **pfad:** `test/mutations/36-commands-adaptation-marker.sh`, `test/mutations/37-commands-interner-leak.sh`
- **befund:** (a) **36**: `sed 's/ANPASSEN/ANGEPASST/g' implement-slice.md` — entfernt den Marker aus der
  Template-Quelle → `TestCommands_AdaptationMarker` rot (der emittierte Command trägt keinen ANPASSEN mehr).
  (b) **37**: `sed 's/in den Mutations-Sensor deines Repos/in den make mutate Sensor/'` — der Ziel-String
  existiert verbatim in `implement-slice.md`, die Ersetzung führt den verbotenen Token `make mutate` wieder ein
  → `TestCommands_NoInternalLeak` rot. Der Folge-Commit `35d6654` ersetzt eine backtick-behaftete
  sed-Replacement-Variante (SC2016) durch die aktuelle ohne Backticks — Token `make mutate` bleibt erhalten,
  Wächter-Wirkung unverändert. Beide Fälle tragen `# files:`/`# expect:` im Parser-Format, lassen `# verify:`
  weg (Default `test`), **ändern real** ihre gelistete Datei und färben den **genannten** Test. Kein zahnloser
  Fall. Dass 37 nur `implement-slice.md` mutiert, während der Test alle 3 Commands scannt, ist ausreichend
  (ein Rot-Beleg pro Wächter genügt).
- **verifizierbar:** ja (`make mutate` — 36/37 rot gesehen; Commit meldet 37/37, neu 36/37 rot).

### N-8 · smoke.sh / full-smoke.sh — Präsenz-Vertreter + No-Leak-Grep im real gebooteten Ziel
- **quelle:** Plan §2-DoD (Commands real im Ziel belegt), LH-FA-08
- **pfad:** `harness/tools/smoke.sh:94-105`, `harness/tools/full-smoke.sh:105-116`
- **befund:** Beide Smokes prüfen im real gebootstrappten `$tmprepo`: (i) alle 3 `.claude/commands/*.md`
  existieren (Präsenz-Vertreter) und (ii) `grep -rqE 'ai-harness-init|make mutate|test/mutations'` über
  `.claude/commands/` trifft **nicht** (No-Leak, real). Der behaviorale Grep ergänzt die Go-Unit-Prüfung
  (`TestCommands_NoInternalLeak` deckt zusätzlich `make smoke` + die 4 Slice-Nrn). Der Smoke-Grep verzichtet
  auf `make smoke`/Slice-Nrn — vertretbare Arbeitsteilung (der Go-Test trägt die breitere Menge, der Smoke die
  reale-Ziel-Gegenprobe). Sinnvoll, kein „Stempel-da"-Schwachtest.
- **verifizierbar:** ja (`make smoke` + `make full-smoke`).

### N-9 · ADR-0006-Konformität + Hard Rules — Tool-als-Quelle, getrennte Klasse, keine Gate-Lockerung
- **quelle:** ADR-0006 §Entscheidung 1 (Tool-als-Quelle), AGENTS §3.1/§3.5, LH-FA-08 ≠ LH-FA-06
- **pfad:** `commands.go`, `main.go`, `commands_test.go`
- **befund:** Die Commands liegen als eingebettete Tool-Quelle (`//go:embed all:templates/commands`), nicht
  gefetcht/Picker — genau die ADR-0006-Herkunftsklasse. Eigene Funktion `emit.Commands` (getrennt von
  `emit.Enforce`) hält LH-FA-08 (Anleitung) und LH-FA-06 (Durchsetzung) als verschiedene Klassen auseinander.
  **Kein** neues halluziniertes Gate (§3.1): keine Makefile-/AGENTS-Gate-Zeile hinzugefügt, nur bestehende
  `smoke`/`full-smoke`/`mutate`-Nicht-Gate-Verifies erweitert. **Keine** Gate-Lockerung ohne ADR (§3.5) —
  additive Sensoren. Determinismus (ADR-0006-Fitness): statischer Embed-Inhalt, keine `--lang`-Substitution
  → gleiche Eingabe byte-identisch. Die einzige Wortlaut-Notiz (fehlender lang-Param) siehe I-1.
- **verifizierbar:** ja (`make build`/`make test`; `make full-smoke` für die Determinismus-/Präsenz-Achse).

---

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 1 (L-1) |
| INFO | 1 (I-1) |
| Negativbefunde | 9 (N-1 … N-9) |

---

## Verdikt

**KONFORM — keine Blocker.** Alle Prüf-Achsen aus dem Auftrag bestanden: **Verbatim-Treue des Prozesses** —
`diff` Dogfood↔Emit zeigt je Command nur die geplanten Genericisierungs-Hunks (ANPASSEN-Sektion,
Toolchain-Generalisierung, CLAUDE.md-„falls vorhanden", Sensor-Prosa), **kein** Prozess-Schritt entfernt
(Schritt-Parität 23/23 · 10/10 · 7/7), keine Rollen-/Review-/Verifikations-Regel abgeschwächt (N-1);
**Genericisierung vollständig** ohne ai-harness-init-internen Leak — kein `ai-harness-init`/`make mutate`/
`make smoke`/`test/mutations`/konkrete Slice-Nr/hart kodierte Sprach-Toolchain in den Templates (N-2);
**ANPASSEN-Marker in ALLEN 3** Commands, `TestCommands_AdaptationMarker` iteriert über alle (N-3); **keine
Über-Genericisierung** — die im Ziel real emittierten Refs (`make gates`, `record-gates`, `harness/conventions.md`,
`.harness/skills/reviewer.md`) bleiben konkret (N-4); **CLAUDE.md-Referenz** als „falls vorhanden"-Prosa
gehandhabt, kein toter Inline-Pfad (N-5); **Emit-Mechanik** spiegelt `Enforce` sauber (Kollisions-Vorpass,
Pre-Flight-Kopplung `CommandPaths()`==`Commands`, getrennte Funktion, Mode 0644 korrekt ohne unnötigen Chmod,
N-6); **Sensoren 36/37** sind echte, den genannten Wächter färbende Mutationen (N-7); der smoke/full-smoke
No-Leak-Grep ist sinnvoll (N-8); **ADR-0006** (Tool-als-Quelle via go:embed, Anleitung ≠ Durchsetzung, keine
Gate-Lockerung) gewahrt (N-9).

Ein **LOW** (nicht-blockierend): **L-1** — `TestCommands_NoInternalLeak` prüft die Slice-Nummern-Achse als
4-Literal-Aufzählung statt als numerische Klasse; eine künftig durchsickernde andere Dogfood-Slice-Nummer bliebe
grün. Aktuell kein Leak, daher kein Merge-Blocker — Kandidat für eine Nachzieh-Zeile (`slice-[0-9]{3}`-Muster).
Ein **INFO**: **I-1** — ADR-0006 formuliert „per `--lang` parametriert" wörtlich auch für Commands, während
`emit.Commands` sprach-agnostisch (kein lang-Param) ist; die Abweichung ist im Plan §6 begründet, erfüllt die
Determinismus-/Adaptierbarkeits-Intent der ADR und tastet die tragende ADR-Entscheidung nicht an — Wortlaut-vs-
Intent-Notiz, kein Verstoß.

**Offen für die Verifikation** (nicht Reviewer-Rolle): `make gates` grün · `make full-smoke` (Commands real im
Ziel, Ziel-d-check 12/0, N-5/N-8) · `make mutate` (realer Rot-Lauf 36/37, N-7) · `make smoke` (N-8) · DoD-Abhakung
inkl. Closure-Notiz.
</content>
