# Verifikation — slice-034 (Gate-Fragment-Assembly)

> `docs/reviews/**` ist doc-gate-exempt (MR-009 `exempt-paths` / ids-`exempt-paths`) —
> bare IDs/Pfade hier ok. Bewusst ohne Markdown-Links, damit `links`/`anchors` nichts findet.

## Kopf-Metadaten

- **Rolle:** unabhängiger Verifier (Modul 11), frischer Kontext — weder Autor noch Reviewer.
- **Gegenstand:** DoD-Behauptung von slice-034 (6 Punkte, §2) gegen realen Zustand + Spec + Plan + ADR-0007 + Hard Rules. Der Verifier bestätigt DoD+Spec+Plan; der Reviewer (getrenntes Artefakt, 2026-07-23-slice-034-review.md) prüfte gegen Plan+ADR (F-1 MEDIUM, als aufgelöst annotiert).
- **Slice:** docs/plan/planning/in-progress/slice-034-gate-fragment-assembly.md (noch in-progress/, DoD-Boxen offen — Verify VOR Closure).
- **Impl-Commits:** c77ac4e (Assembly, 18 Dateien +310/−159) · caaf40f (F-1-Fix: netzloser Doc-Gate-Wächter) · 445acbf (Mermaid-Parse-Fix architecture.md). HEAD=445acbf, tracked Tree clean vor UND nach allen Läufen.
- **Selbst ausgeführt (belegt, nicht nur nachgelesen), SEQUENZIELL:**
  - `make gates` SELBST → **Exit 0** (go test aller 5 Pakete ok; shellcheck + actionlint ok). git status leer davor.
  - `make full-smoke` SELBST → **Exit 0**; 3/3 OK-Zeilen: (1) frisch gebootstrapptes Repo fährt `make -j gates` out-of-the-box grün inkl. baseline-verify via Fragment-Assembly, record-gates zuletzt; (2) Gate-Nachweis-Kreis geschlossen (record-gates stempelt, Hash == frische Berechnung, .harness/.gitignore greift); (3) emittierter Command-Guard blockt `go build`, lässt `make test` durch. Alle fünf Silent-Subset-Marker (`--target lint/build/test`, `geprüft`, `Integritaet + Vollstaendigkeit`) gefunden — sonst wäre der Lauf rot.
  - `make mutate` SELBST → **Exit 0, 38 ok / 0 Befund(e)**; git status leer davor UND danach (mutate.sh restauriert). Grün-Vorlauf fuhr `make test` + `make ci-lint` + `make smoke` real grün (Vorbedingung).
  - Statische Belege: internal/gen/golang.go (aggregatorMakefile + goMkFragmentTmpl), internal/emit/{emit,baseline,enforce}.go, internal/wire/wire.go, cmd/ai-harness-init/main.go (emitTargets), spec/architecture.md §5.

## DoD-Verdikt je Zeile

### DoD-1 — Emittierte Root-Makefile ist Aggregator (`include harness/mk/*.mk`), kein `gates:`-Prereq-Anhang mehr — **CONFIRMED**

Im Code: internal/gen/golang.go:115-137 `aggregatorMakefile` trägt `GATE_CHECKS :=`, `include harness/mk/*.mk`, `gates: record-gates` und die Ordnungskante `record-gates: $(GATE_CHECKS)` NACH dem Include; kein `gates: lint build test`-Prereq mehr. internal/wire/wire.go ist reiner Placer — der frühere `dCheckInclude`/`enforceWiring`-Anhang ist entfernt (Place schreibt verbatim, kein Makefile-Rewrite mehr; wire.go:54-103). Im real gebootstrappten Ziel: full-smoke fährt `make -j gates` über genau diesen Aggregator grün (Exit 0). Der Migrations-Bruch (wire-Inline → Aggregator + Fragmente) ist vollzogen; die Deckungs-Migration der dabei entfernten Mutationsfälle 21/33 ist unter DoD-3/F-1 belegt.

### DoD-2 — Bootstrap emittiert `harness/mk/{go,doc-gate,baseline,enforce}.mk`; jedes `GATE_CHECKS += …`; jedes Target existiert (LH-QA-01); baseline-verify neu verdrahtet — **CONFIRMED**

Vier Fragmente je Emitter: go.mk (golang.go:141-160, `GATE_CHECKS += lint build test`), doc-gate.mk (emit.go:58-63, `include d-check.mk` + `GATE_CHECKS += docs-check`), baseline.mk (baseline.go:35-44, Rezept + `GATE_CHECKS += baseline-verify`), enforce.mk (templates/enforce/enforce.mk, record-gates-Rezept). Jedes referenzierte Target existiert: lint/build/test als Dockerfile-Stages (TestGenerate_GoMkTargetsMatchStages, Mutation 15 rot gesehen), docs-check via d-check.mk, baseline-verify + record-gates mit Rezept. **baseline-verify lief WIRKLICH im Ziel** (nicht nur emittiert): full-smoke assertiert den Erfolgs-Marker `Integritaet + Vollstaendigkeit` aus baseline-verify.sh im `make gates`-Output — der Marker war da (Exit 0), also fuhr das Skript real unter dem Ziel-`make gates`. Vorher orphaned, jetzt verdrahtet. Pre-Flight-Vollständigkeit: cmd/main.go:242 listet BaselineMkPath + DocGateMkPath explizit, enforce.mk via EnforcePaths(), go.mk via wire.Targets — alle vier im Phase-3-Pre-Flight.

### DoD-3 — Ordnungskante `record-gates: $(GATE_CHECKS)` + `gates: record-gates`, strikt zuletzt, `make -j` parallel, kein `.NOTPARALLEL`; Mutation färbt Wächter rot — **CONFIRMED**

golang.go:135-136 trägt beide Kanten im Aggregator (nach dem Glob-Include, damit GATE_CHECKS vollständig gesehen wird); `.NOTPARALLEL` nirgends gesetzt. enforce.mk trägt bewusst NICHT die Ordnungskante (TestEnforce_EmitsGateFragment assertiert die Abwesenheit von `$(GATE_CHECKS)` im Fragment — sie lebt im Aggregator). full-smoke fährt `make -j -C $tmprepo gates` (real parallel, Exit 0). §3.6-Zahn: `make mutate` Fall 38 (`sed 's/^record-gates: .*/record-gates:/'`, entfernt den `$(GATE_CHECKS)`-Prereq) → **TestGenerate_AggregatorHasOrderEdge rot gesehen**. Der Wächter prüft die Eigenschaft (Substring `record-gates: $(GATE_CHECKS)` UND kein eigenes `gates:`-Target im Fragment), nicht ein Implementierungsdetail — er kann unter der benannten Mutation rot werden und tat es. Echtes Gegenbeispiel.

### DoD-4 — `make full-smoke` grün: emittiertes `make -j gates` grün, Stempel-Match, byte-identische/reproduzierbare Emission (LH-QA-02) — **CONFIRMED**

full-smoke SELBST → Exit 0. Emittiertes `make -j gates` grün (alle Silent-Subset-Marker vorhanden). record-gates-Stempel: full-smoke.sh:77-87 belegt (a) Stempel existiert, (b) Stempel == frische working-tree-hash-Berechnung — Match bestätigt (sonst rot mit „Hash weicht vom Stempel ab"). Byte-Determinismus: TestGenerate_Deterministic (gen_test.go:44) vergleicht den VOLLEN Datei-Satz byte-für-byte über zwei Läufe — lief in `make test` (Teil von gates, grün) und im mutate-Grün-Vorlauf. aggregatorMakefile/goMkFragmentTmpl sind deterministische const/render — keine Map-Iteration, kein Zeitstempel im Inhalt. LH-QA-02 gehalten.

### DoD-5 — `make gates` grün (Dogfood) — **CONFIRMED**

`make gates` SELBST → Exit 0 (Dogfood-Makefile, das seine eigene Nicht-Aggregator-Form behält: `gates: baseline-verify docs-check lint build test shell-lint ci-lint record-gates`). go test aller 5 Pakete ok, shellcheck + actionlint ok. Kein Silent-Grün-Pfad beobachtet.

### DoD-6 — Doku architecture.md §5 trägt den Umbau; conventions.md-MR nötig? — **CONFIRMED (Doku trägt; keine MR nötig, Begründung tragfähig)**

spec/architecture.md §5 („Idempotenz, Fragment-Assembly und Resume", Zeilen 105-128) dokumentiert Aggregator + `include harness/mk/*.mk`, `GATE_CHECKS += …`, Ordnungskante `record-gates: $(GATE_CHECKS)`, kein `.NOTPARALLEL`, add-lang als Fragment-Drop — konsistent mit ADR-0007 Entscheidung 2. Der Mermaid-4.2-Block parst (445acbf-Fix; `make docs-check` grün). **Keine conventions.md-MR nötig:** die conventions.md-MRs dokumentieren, wo der DOGFOOD-Repo von der Ziel-Form abweicht (MR-005 harness/tools-Layout, MR-010 d-check.mk-Fragment). slice-034 ändert die EMITTIERTE Ziel-Form (via ADR-0007 + architecture.md §5 governt), nicht die Dogfood-Layout-Adaption — der Dogfood-Makefile behält seine bestehende Form (verifiziert: kein Aggregator-Umbau am eigenen Makefile). Die Begründung des Implementers (keine MR) trägt.

### Zusatz — Closure-Notiz (§7 des Slice)

Noch leer (Slice in in-progress/). Die DoD-Zeile „Closure-Notiz mit Steering-Loop-Lerneintrag" ist ein Post-`done/`-Schritt (Modul 5 Lifecycle) — erwartet offen zur Verify-Zeit, kein Verify-Blocker.

## Verifier-only-Klassen (gezielt gesucht)

- **Property-vs-Impl-Detail (§3.6-Falsch-Beispiel):** TestGenerate_AggregatorHasOrderEdge prüft die Ordnungs-Eigenschaft, wird unter Mutation 38 rot — kein zahnloser Namens-Proxy. Analog TestEnforce_EmitsGateFragment (39), TestDocGate_FragmentWiresDocsCheck (40), TestGenerate_GoMkTargetsMatchStages (15) — alle vier real rot gesehen im mutate-Lauf.
- **Stille Teilmenge im Ziel-`make gates`:** ausgeschlossen — full-smoke assertiert fünf Marker (lint/build/test/docs-check/baseline-verify), fehlte die Ordnungskante hinge gates nur an record-gates und alle Marker fehlten → rot. Kein Check außerhalb GATE_CHECKS.
- **F-1-Auflösung echt oder kosmetisch:** ECHT. emit.DocGateMk()-Accessor + TestDocGate_FragmentWiresDocsCheck (prüft `include d-check.mk` + `GATE_CHECKS += docs-check`) + Mutation 40 (bricht die GATE_CHECKS-Zeile zeilen-verankert) → Test **rot gesehen** (mutate 40 ok). Fall 21s Deckung ist als Fall 40 migriert; das Doc-Gate trägt jetzt dieselbe Doppel-Absicherung wie die drei Geschwister.
- **„baseline-verify neu verdrahtet" real gelaufen:** JA — der Erfolgs-Marker `Integritaet + Vollstaendigkeit` erschien im Ziel-`make gates`-Output (full-smoke Exit 0), nicht nur die Datei emittiert.
- **Option A (Aggregator vorübergehend im gen) vs. ADR-0007:** legitime Scoping-Verengung, keine DoD-Lücke für slice-034. Die slice-034-DoD verlangt „emittierte Root-Makefile ist Aggregator" — erfüllt; sie verlangt NICHT die Init-Emitter-Relocation (ADR-0007 Entscheidung 1), die Plan §1/§6 ausdrücklich nach slice-035 scopen (green-before-extend, Relocation kein Rewrite). Der Slice ändert bewusst keine CLI (`--lang` bleibt Pflicht). Die volle ADR-0007-Realisierung ist welle-05-weit; slice-034 behauptet nichts darüber hinaus.

## Gesamturteil

**DoD BESTÄTIGT.** Alle sechs DoD-Punkte CONFIRMED (DoD-6 mit tragfähiger „keine MR"-Begründung). Real gemessen: `make gates` Exit 0 · `make full-smoke` Exit 0 (3/3 OK, alle Silent-Subset-Marker + Stempel-Match) · `make mutate` 38 ok / 0 Befund (Fälle 15/38/39/40 je ihren benannten Wächter rot gesehen). Byte-Determinismus (LH-QA-02) durch TestGenerate_Deterministic gedeckt (grün). Tree vor und nach jedem Lauf clean, keine Mutation zurückgeblieben. Die vom Reviewer gemeldete F-1 (MEDIUM) ist mit rot gesehenem Gegenbeispiel (Mutation 40) real geschlossen, nicht kosmetisch.
