# Harness-Laufzustand (lokal — NUR state/ und cache/;
# .harness/skills/ ist versionierte Harness-Mechanik)
.harness/state/
# Lokaler, gefetchter Verbatim-Regelwerk-Cache (nicht committet; via
# `make regelwerk-fetch`, sha256-gepinnt — MR-004)
.harness/cache/
